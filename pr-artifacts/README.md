# PR Artifacts — manual-test log snippets

These files are snapshots of actual runtime log output produced by the changes in this PR. They're checked in so reviewers can see the before/after without spinning up a workbench. They should be removed before merging.

Each file is named `NN-<scenario>.log` and contains the raw console output (stripped of ANSI colors where noisy, but kept where useful for visual framing).

