# Hoist @workflow/swc-plugin so NestJS's SWC builder can resolve it
# This allows packages/nest to own the SWC dependency while making it
# available to workbench apps that use NestJS's built-in SWC compilation
public-hoist-pattern[]=@workflow/swc-plugin
