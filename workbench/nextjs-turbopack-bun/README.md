# Next.js Turbopack on Bun

This workbench mirrors `../nextjs-turbopack` and runs its Next.js build and
Vercel Functions on Vercel's latest stable Bun 1.x runtime. It exists as a
separate deployment target so the E2E suite can compare Bun with the Node.js
`nextjs-turbopack` control lane.
