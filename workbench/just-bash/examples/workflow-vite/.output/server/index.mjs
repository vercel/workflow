var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key2, value) => key2 in obj ? __defProp(obj, key2, { enumerable: true, configurable: true, writable: true, value }) : obj[key2] = value;
var __publicField = (obj, key2, value) => __defNormalProp(obj, typeof key2 !== "symbol" ? key2 + "" : key2, value);
var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y, _z, _A, _B, _C, _D, _E, _F, _G;
globalThis.__nitro_main__ = import.meta.url;
import { f as functionsExports } from "./_chunks/_libs/@vercel/functions.mjs";
import { N as NodeResponse, s as serve } from "./_libs/srvx.mjs";
import { d as defineHandler, H as HTTPError, t as toEventHandler, a as defineLazyEventHandler, b as H3Core } from "./_libs/h3.mjs";
import { m as ms } from "./_libs/ms.mjs";
import { m as monotonicFactory, d as decodeTime } from "./_libs/ulid.mjs";
import { createRequire } from "node:module";
import * as path from "node:path";
import path__default, { join as join$1, dirname, resolve } from "node:path";
import { execFile } from "node:child_process";
import { readdir, readlink, readFile, mkdir, access, constants, writeFile, unlink } from "node:fs/promises";
import { promisify, inspect, types } from "node:util";
import { fileURLToPath } from "node:url";
import { setTimeout as setTimeout$1 } from "node:timers/promises";
import { J as JsonTransport, C as Client, D as DuplicateMessageError } from "./_chunks/_libs/@vercel/queue.mjs";
import { l as libExports } from "./_chunks/_libs/async-sema.mjs";
import { u as undiciExports } from "./_chunks/_libs/undici.mjs";
import { d as discriminatedUnion, _ as _enum, o as object, n as number, a as date, s as string, l as literal, r as record, b as array, c as any, e as boolean, f as lazy, u as union, g as _null, t as templateLiteral, h as _undefined, z, Z as ZodError } from "./_libs/zod.mjs";
import { EventEmitter } from "node:events";
import * as fs2 from "node:fs";
import { promises } from "node:fs";
import os__default from "node:os";
import { d as distExports } from "./_chunks/_libs/@vercel/oidc.mjs";
import { e as encode, d as decode } from "./_libs/cbor-x.mjs";
import { AsyncLocalStorage } from "node:async_hooks";
import { u as unflatten, s as stringify, p as parse$1, D as DevalueError } from "./_libs/devalue.mjs";
import { d as debug } from "./_chunks/_libs/debug.mjs";
import { T as TraceMap, o as originalPositionFor } from "./_chunks/_libs/@jridgewell/trace-mapping.mjs";
import { createContext as createContext$1, runInContext } from "node:vm";
import { c as customRandom, u as urlAlphabet } from "./_libs/nanoid.mjs";
import { s as seedrandom } from "./_libs/seedrandom.mjs";
import { d as decodePath, w as withLeadingSlash, a as withoutTrailingSlash, j as joinURL } from "./_libs/ufo.mjs";
import "node:http";
import "node:stream";
import "node:https";
import "node:http2";
import "./_libs/rou3.mjs";
import "./_chunks/_libs/@mongodb-js/zstd.mjs";
import "util";
import "util/types";
import "node:crypto";
import "./_libs/mixpart.mjs";
import "events";
import "node:assert";
import "node:net";
import "node:buffer";
import "node:querystring";
import "node:diagnostics_channel";
import "node:tls";
import "node:zlib";
import "node:perf_hooks";
import "node:util/types";
import "node:worker_threads";
import "node:console";
import "node:dns";
import "string_decoder";
import "path";
import "fs";
import "os";
import "tty";
import "./_libs/supports-color.mjs";
import "./_libs/has-flag.mjs";
import "./_chunks/_libs/@jridgewell/sourcemap-codec.mjs";
import "./_chunks/_libs/@jridgewell/resolve-uri.mjs";
const services = {};
globalThis.__nitro_vite_envs__ = services;
const errorHandler$1 = (error, event) => {
  const res = defaultHandler(error, event);
  return new NodeResponse(typeof res.body === "string" ? res.body : JSON.stringify(res.body, null, 2), res);
};
function defaultHandler(error, event, opts) {
  const isSensitive = error.unhandled;
  const status = error.status || 500;
  const url = event.url || new URL(event.req.url);
  if (status === 404) {
    const baseURL = "/";
    if (/^\/[^/]/.test(baseURL) && !url.pathname.startsWith(baseURL)) {
      const redirectTo = `${baseURL}${url.pathname.slice(1)}${url.search}`;
      return {
        status: 302,
        statusText: "Found",
        headers: { location: redirectTo },
        body: `Redirecting...`
      };
    }
  }
  if (isSensitive && !opts?.silent) {
    const tags = [error.unhandled && "[unhandled]"].filter(Boolean).join(" ");
    console.error(`[request error] ${tags} [${event.req.method}] ${url}
`, error);
  }
  const headers = {
    "content-type": "application/json",
    "x-content-type-options": "nosniff",
    "x-frame-options": "DENY",
    "referrer-policy": "no-referrer",
    "content-security-policy": "script-src 'none'; frame-ancestors 'none';"
  };
  if (status === 404 || !event.res.headers.has("cache-control")) {
    headers["cache-control"] = "no-cache";
  }
  const body = {
    error: true,
    url: url.href,
    status,
    statusText: error.statusText,
    message: isSensitive ? "Server Error" : error.message,
    data: isSensitive ? void 0 : error.data
  };
  return {
    status,
    statusText: error.statusText,
    headers,
    body
  };
}
const errorHandlers = [errorHandler$1];
async function errorHandler(error, event) {
  for (const handler2 of errorHandlers) {
    try {
      const response = await handler2(error, event, { defaultHandler });
      if (response) {
        return response;
      }
    } catch (error2) {
      console.error(error2);
    }
  }
}
function pluralize(singular, plural, count) {
  return count === 1 ? singular : plural;
}
function withResolvers() {
  let resolve2;
  let reject;
  const promise = new Promise((_resolve, _reject) => {
    resolve2 = _resolve;
    reject = _reject;
  });
  return { promise, resolve: resolve2, reject };
}
function once$1(fn) {
  const result2 = {
    get value() {
      const value = fn();
      Object.defineProperty(result2, "value", { value });
      return value;
    }
  };
  return result2;
}
function parseDurationToDate(param) {
  if (typeof param === "string") {
    const durationMs = ms(param);
    if (typeof durationMs !== "number" || durationMs < 0) {
      throw new Error(`Invalid duration: "${param}". Expected a valid duration string like "1s", "1m", "1h", etc.`);
    }
    return new Date(Date.now() + durationMs);
  } else if (typeof param === "number") {
    if (param < 0 || !Number.isFinite(param)) {
      throw new Error(`Invalid duration: ${param}. Expected a non-negative finite number of milliseconds.`);
    }
    return new Date(Date.now() + param);
  } else if (param instanceof Date || param && typeof param === "object" && typeof param.getTime === "function") {
    return param instanceof Date ? param : new Date(param.getTime());
  } else {
    throw new Error(`Invalid duration parameter. Expected a duration string, number (milliseconds), or Date object.`);
  }
}
const BASE_URL = "https://useworkflow.dev/err";
function isError(value) {
  return typeof value === "object" && value !== null && "name" in value && "message" in value;
}
const ERROR_SLUGS = {
  WEBHOOK_INVALID_RESPOND_WITH_VALUE: "webhook-invalid-respond-with-value",
  WEBHOOK_RESPONSE_NOT_SENT: "webhook-response-not-sent",
  FETCH_IN_WORKFLOW_FUNCTION: "fetch-in-workflow",
  TIMEOUT_FUNCTIONS_IN_WORKFLOW: "timeout-in-workflow",
  HOOK_CONFLICT: "hook-conflict"
};
class WorkflowError extends Error {
  cause;
  constructor(message, options) {
    const msgDocs = options?.slug ? `${message}

Learn more: ${BASE_URL}/${options.slug}` : message;
    super(msgDocs, { cause: options?.cause });
    this.cause = options?.cause;
    if (options?.cause instanceof Error) {
      this.stack = `${this.stack}
Caused by: ${options.cause.stack}`;
    }
  }
  static is(value) {
    return isError(value) && value.name === "WorkflowError";
  }
}
class WorkflowAPIError extends WorkflowError {
  status;
  code;
  url;
  constructor(message, options) {
    super(message, {
      cause: options?.cause
    });
    this.name = "WorkflowAPIError";
    this.status = options?.status;
    this.code = options?.code;
    this.url = options?.url;
  }
  static is(value) {
    return isError(value) && value.name === "WorkflowAPIError";
  }
}
class WorkflowRunFailedError extends WorkflowError {
  runId;
  constructor(runId, error) {
    const causeError = new Error(error.message);
    if (error.stack) {
      causeError.stack = error.stack;
    }
    if (error.code) {
      causeError.code = error.code;
    }
    super(`Workflow run "${runId}" failed: ${error.message}`, {
      cause: causeError
    });
    this.name = "WorkflowRunFailedError";
    this.runId = runId;
  }
  static is(value) {
    return isError(value) && value.name === "WorkflowRunFailedError";
  }
}
class WorkflowRunNotCompletedError extends WorkflowError {
  runId;
  status;
  constructor(runId, status) {
    super(`Workflow run "${runId}" has not completed`, {});
    this.name = "WorkflowRunNotCompletedError";
    this.runId = runId;
    this.status = status;
  }
  static is(value) {
    return isError(value) && value.name === "WorkflowRunNotCompletedError";
  }
}
class WorkflowRuntimeError extends WorkflowError {
  constructor(message, options) {
    super(message, {
      ...options
    });
    this.name = "WorkflowRuntimeError";
  }
  static is(value) {
    return isError(value) && value.name === "WorkflowRuntimeError";
  }
}
class WorkflowRunNotFoundError extends WorkflowError {
  runId;
  constructor(runId) {
    super(`Workflow run "${runId}" not found`, {});
    this.name = "WorkflowRunNotFoundError";
    this.runId = runId;
  }
  static is(value) {
    return isError(value) && value.name === "WorkflowRunNotFoundError";
  }
}
class WorkflowRunCancelledError extends WorkflowError {
  runId;
  constructor(runId) {
    super(`Workflow run "${runId}" cancelled`, {});
    this.name = "WorkflowRunCancelledError";
    this.runId = runId;
  }
  static is(value) {
    return isError(value) && value.name === "WorkflowRunCancelledError";
  }
}
class RunNotSupportedError extends WorkflowError {
  runSpecVersion;
  worldSpecVersion;
  constructor(runSpecVersion, worldSpecVersion) {
    super(`Run requires spec version ${runSpecVersion}, but world supports version ${worldSpecVersion}. Please upgrade 'workflow' package.`);
    this.name = "RunNotSupportedError";
    this.runSpecVersion = runSpecVersion;
    this.worldSpecVersion = worldSpecVersion;
  }
  static is(value) {
    return isError(value) && value.name === "RunNotSupportedError";
  }
}
class FatalError extends Error {
  fatal = true;
  constructor(message) {
    super(message);
    this.name = "FatalError";
  }
  static is(value) {
    return isError(value) && value.name === "FatalError";
  }
}
class RetryableError extends Error {
  /**
   * The Date when the step should be retried.
   */
  retryAfter;
  constructor(message, options = {}) {
    super(message);
    this.name = "RetryableError";
    if (options.retryAfter !== void 0) {
      this.retryAfter = parseDurationToDate(options.retryAfter);
    } else {
      this.retryAfter = new Date(Date.now() + 1e3);
    }
  }
  static is(value) {
    return isError(value) && value.name === "RetryableError";
  }
}
function parseName(tag, name) {
  if (typeof name !== "string") {
    return null;
  }
  const [prefix, path2, ...functionNameParts] = name.split("//");
  if (prefix !== tag || !path2 || functionNameParts.length === 0) {
    return null;
  }
  let shortName = functionNameParts.at(-1) ?? "";
  const functionName = functionNameParts.join("//");
  const filename = path2.split("/").at(-1) ?? "";
  const fileNameWithoutExtension = filename.split(".").at(0) ?? "";
  if (["default", "__default"].includes(shortName) && fileNameWithoutExtension) {
    shortName = fileNameWithoutExtension;
  }
  return {
    shortName,
    path: path2,
    functionName
  };
}
function parseWorkflowName(name) {
  return parseName("workflow", name);
}
const EventTypeSchema = _enum([
  // Run lifecycle events
  "run_created",
  "run_started",
  "run_completed",
  "run_failed",
  "run_cancelled",
  // Step lifecycle events
  "step_created",
  "step_completed",
  "step_failed",
  "step_retrying",
  "step_started",
  // Hook lifecycle events
  "hook_created",
  "hook_received",
  "hook_disposed",
  "hook_conflict",
  // Created by world when hook token already exists
  // Wait lifecycle events
  "wait_created",
  "wait_completed"
]);
const BaseEventSchema = object({
  eventType: EventTypeSchema,
  correlationId: string().optional(),
  specVersion: number().optional()
});
const StepCompletedEventSchema = BaseEventSchema.extend({
  eventType: literal("step_completed"),
  correlationId: string(),
  eventData: object({
    result: any()
  })
});
const StepFailedEventSchema = BaseEventSchema.extend({
  eventType: literal("step_failed"),
  correlationId: string(),
  eventData: object({
    error: any(),
    stack: string().optional()
  })
});
const StepRetryingEventSchema = BaseEventSchema.extend({
  eventType: literal("step_retrying"),
  correlationId: string(),
  eventData: object({
    error: any(),
    stack: string().optional(),
    retryAfter: date().optional()
  })
});
const StepStartedEventSchema = BaseEventSchema.extend({
  eventType: literal("step_started"),
  correlationId: string(),
  eventData: object({
    attempt: number().optional()
  }).optional()
});
const StepCreatedEventSchema = BaseEventSchema.extend({
  eventType: literal("step_created"),
  correlationId: string(),
  eventData: object({
    stepName: string(),
    input: any()
    // SerializedData
  })
});
const HookCreatedEventSchema = BaseEventSchema.extend({
  eventType: literal("hook_created"),
  correlationId: string(),
  eventData: object({
    token: string(),
    metadata: any().optional()
    // SerializedData
  })
});
const HookReceivedEventSchema = BaseEventSchema.extend({
  eventType: literal("hook_received"),
  correlationId: string(),
  eventData: object({
    payload: any()
    // Serialized payload
  })
});
const HookDisposedEventSchema = BaseEventSchema.extend({
  eventType: literal("hook_disposed"),
  correlationId: string()
});
const HookConflictEventSchema = BaseEventSchema.extend({
  eventType: literal("hook_conflict"),
  correlationId: string(),
  eventData: object({
    token: string()
  })
});
const WaitCreatedEventSchema = BaseEventSchema.extend({
  eventType: literal("wait_created"),
  correlationId: string(),
  eventData: object({
    resumeAt: date()
  })
});
const WaitCompletedEventSchema = BaseEventSchema.extend({
  eventType: literal("wait_completed"),
  correlationId: string()
});
const RunCreatedEventSchema = BaseEventSchema.extend({
  eventType: literal("run_created"),
  eventData: object({
    deploymentId: string(),
    workflowName: string(),
    input: array(any()),
    // SerializedData[]
    executionContext: record(string(), any()).optional()
  })
});
const RunStartedEventSchema = BaseEventSchema.extend({
  eventType: literal("run_started")
});
const RunCompletedEventSchema = BaseEventSchema.extend({
  eventType: literal("run_completed"),
  eventData: object({
    output: any().optional()
    // SerializedData
  })
});
const RunFailedEventSchema = BaseEventSchema.extend({
  eventType: literal("run_failed"),
  eventData: object({
    error: any(),
    errorCode: string().optional()
  })
});
const RunCancelledEventSchema = BaseEventSchema.extend({
  eventType: literal("run_cancelled")
});
discriminatedUnion("eventType", [
  // Run lifecycle events
  RunCreatedEventSchema,
  RunStartedEventSchema,
  RunCompletedEventSchema,
  RunFailedEventSchema,
  RunCancelledEventSchema,
  // Step lifecycle events
  StepCreatedEventSchema,
  StepCompletedEventSchema,
  StepFailedEventSchema,
  StepRetryingEventSchema,
  StepStartedEventSchema,
  // Hook lifecycle events
  HookCreatedEventSchema,
  HookReceivedEventSchema,
  HookDisposedEventSchema,
  // Wait lifecycle events
  WaitCreatedEventSchema,
  WaitCompletedEventSchema
]);
const AllEventsSchema = discriminatedUnion("eventType", [
  // Run lifecycle events
  RunCreatedEventSchema,
  RunStartedEventSchema,
  RunCompletedEventSchema,
  RunFailedEventSchema,
  RunCancelledEventSchema,
  // Step lifecycle events
  StepCreatedEventSchema,
  StepCompletedEventSchema,
  StepFailedEventSchema,
  StepRetryingEventSchema,
  StepStartedEventSchema,
  // Hook lifecycle events
  HookCreatedEventSchema,
  HookReceivedEventSchema,
  HookDisposedEventSchema,
  HookConflictEventSchema,
  // World-only: created when hook token conflicts
  // Wait lifecycle events
  WaitCreatedEventSchema,
  WaitCompletedEventSchema
]);
const EventSchema = AllEventsSchema.and(object({
  runId: string(),
  eventId: string(),
  createdAt: date(),
  specVersion: number().optional()
}));
const zodJsonSchema = lazy(() => {
  return union([
    string(),
    number(),
    boolean(),
    _null(),
    array(zodJsonSchema),
    record(string(), zodJsonSchema)
  ]);
});
const PaginatedResponseSchema = (dataSchema) => object({
  data: array(dataSchema),
  cursor: string().nullable(),
  hasMore: boolean()
});
const StructuredErrorSchema = object({
  message: string(),
  stack: string().optional(),
  code: string().optional()
  // TODO: currently unused. make this an enum maybe
});
const HookSchema = object({
  runId: string(),
  hookId: string(),
  token: string(),
  ownerId: string(),
  projectId: string(),
  environment: string(),
  metadata: zodJsonSchema.optional(),
  createdAt: date(),
  // Optional in database for backwards compatibility, defaults to 1 (legacy) when reading
  specVersion: number().optional()
});
const QueuePrefix = union([
  literal("__wkf_step_"),
  literal("__wkf_workflow_")
]);
const ValidQueueName = templateLiteral([QueuePrefix, string()]);
const MessageId = string().brand().describe("A stored queue message ID");
const TraceCarrierSchema = record(string(), string());
const WorkflowInvokePayloadSchema = object({
  runId: string(),
  traceCarrier: TraceCarrierSchema.optional(),
  requestedAt: date().optional()
});
const StepInvokePayloadSchema = object({
  workflowName: string(),
  workflowRunId: string(),
  workflowStartedAt: number(),
  stepId: string(),
  traceCarrier: TraceCarrierSchema.optional(),
  requestedAt: date().optional()
});
const HealthCheckPayloadSchema = object({
  __healthCheck: literal(true),
  correlationId: string()
});
const QueuePayloadSchema = union([
  WorkflowInvokePayloadSchema,
  StepInvokePayloadSchema,
  HealthCheckPayloadSchema
]);
const WorkflowRunStatusSchema = _enum([
  "pending",
  "running",
  "completed",
  "failed",
  "cancelled"
]);
const WorkflowRunBaseSchema = object({
  runId: string(),
  status: WorkflowRunStatusSchema,
  deploymentId: string(),
  workflowName: string(),
  // Optional in database for backwards compatibility, defaults to 1 (legacy) when reading
  specVersion: number().optional(),
  executionContext: record(string(), any()).optional(),
  input: array(any()),
  output: any().optional(),
  error: StructuredErrorSchema.optional(),
  expiredAt: date().optional(),
  startedAt: date().optional(),
  completedAt: date().optional(),
  createdAt: date(),
  updatedAt: date()
});
const WorkflowRunSchema = discriminatedUnion("status", [
  // Non-final states
  WorkflowRunBaseSchema.extend({
    status: _enum(["pending", "running"]),
    output: _undefined(),
    error: _undefined(),
    completedAt: _undefined()
  }),
  // Cancelled state
  WorkflowRunBaseSchema.extend({
    status: literal("cancelled"),
    output: _undefined(),
    error: _undefined(),
    completedAt: date()
  }),
  // Completed state
  WorkflowRunBaseSchema.extend({
    status: literal("completed"),
    output: any(),
    error: _undefined(),
    completedAt: date()
  }),
  // Failed state
  WorkflowRunBaseSchema.extend({
    status: literal("failed"),
    output: _undefined(),
    error: StructuredErrorSchema,
    completedAt: date()
  })
]);
const StepStatusSchema = _enum([
  "pending",
  "running",
  "completed",
  "failed",
  "cancelled"
]);
const StepSchema = object({
  runId: string(),
  stepId: string(),
  stepName: string(),
  status: StepStatusSchema,
  input: array(any()),
  output: any().optional(),
  /**
   * The error from a step_retrying or step_failed event.
   * This tracks the most recent error the step encountered, which may
   * be from a retry attempt (step_retrying) or the final failure (step_failed).
   */
  error: StructuredErrorSchema.optional(),
  attempt: number(),
  /**
   * When the step first started executing. Set by the first step_started event
   * and not updated on subsequent retries.
   */
  startedAt: date().optional(),
  completedAt: date().optional(),
  createdAt: date(),
  updatedAt: date(),
  retryAfter: date().optional(),
  // Optional in database for backwards compatibility, defaults to 1 (legacy) when reading
  specVersion: number().optional()
});
const SPEC_VERSION_CURRENT = 2;
function isLegacySpecVersion(v) {
  if (v === void 0 || v === null)
    return true;
  return v < SPEC_VERSION_CURRENT;
}
function requiresNewerWorld(v) {
  if (v === void 0 || v === null)
    return false;
  return v > SPEC_VERSION_CURRENT;
}
class WorkflowSuspension extends Error {
  steps;
  globalThis;
  stepCount;
  hookCount;
  waitCount;
  constructor(stepsInput, global) {
    const steps = [...stepsInput.values()];
    let stepCount = 0;
    let hookCount = 0;
    let waitCount = 0;
    for (const item of steps) {
      if (item.type === "step")
        stepCount++;
      else if (item.type === "hook")
        hookCount++;
      else if (item.type === "wait")
        waitCount++;
    }
    const parts = [];
    if (stepCount > 0) {
      parts.push(`${stepCount} ${pluralize("step", "steps", stepCount)}`);
    }
    if (hookCount > 0) {
      parts.push(`${hookCount} ${pluralize("hook", "hooks", hookCount)}`);
    }
    if (waitCount > 0) {
      parts.push(`${waitCount} ${pluralize("wait", "waits", waitCount)}`);
    }
    const totalCount = stepCount + hookCount + waitCount;
    const hasOrHave = pluralize("has", "have", totalCount);
    let action;
    if (stepCount > 0) {
      action = "run";
    } else if (hookCount > 0) {
      action = "created";
    } else if (waitCount > 0) {
      action = "created";
    } else {
      action = "received";
    }
    const description = parts.length > 0 ? `${parts.join(" and ")} ${hasOrHave} not been ${action} yet` : "0 steps have not been run yet";
    super(description);
    this.name = "WorkflowSuspension";
    this.steps = steps;
    this.globalThis = global;
    this.stepCount = stepCount;
    this.hookCount = hookCount;
    this.waitCount = waitCount;
  }
  static is(value) {
    return value instanceof WorkflowSuspension;
  }
}
function ENOTSUP() {
  throw new Error("Not supported in workflow functions");
}
function SemanticConvention(...names) {
  return (value) => Object.fromEntries(names.map((name) => [name, value]));
}
const WorkflowName = SemanticConvention("workflow.name");
const WorkflowOperation = SemanticConvention("workflow.operation");
const WorkflowRunId = SemanticConvention("workflow.run.id");
const WorkflowRunStatus = SemanticConvention("workflow.run.status");
const WorkflowStartedAt = SemanticConvention("workflow.started_at");
const WorkflowEventsCount = SemanticConvention("workflow.events.count");
const WorkflowArgumentsCount = SemanticConvention("workflow.arguments.count");
const WorkflowResultType = SemanticConvention("workflow.result.type");
const WorkflowTracePropagated = SemanticConvention("workflow.trace.propagated");
const WorkflowErrorName = SemanticConvention("workflow.error.name");
const WorkflowErrorMessage = SemanticConvention("workflow.error.message");
const WorkflowStepsCreated = SemanticConvention("workflow.steps.created");
const WorkflowHooksCreated = SemanticConvention("workflow.hooks.created");
const WorkflowWaitsCreated = SemanticConvention("workflow.waits.created");
const StepName = SemanticConvention("step.name");
const StepId = SemanticConvention("step.id");
const StepAttempt = SemanticConvention("step.attempt");
const StepStatus = SemanticConvention("step.status");
const StepMaxRetries = SemanticConvention("step.max_retries");
const StepTracePropagated = SemanticConvention("step.trace.propagated");
const StepSkipped = SemanticConvention("step.skipped");
const StepSkipReason = SemanticConvention("step.skip_reason");
const StepArgumentsCount = SemanticConvention("step.arguments.count");
const StepResultType = SemanticConvention("step.result.type");
const StepErrorName = SemanticConvention("step.error.name");
const StepErrorMessage = SemanticConvention("step.error.message");
const StepFatalError = SemanticConvention("step.fatal_error");
const StepRetryExhausted = SemanticConvention("step.retry.exhausted");
const StepRetryTimeoutSeconds = SemanticConvention("step.retry.timeout_seconds");
const StepRetryWillRetry = SemanticConvention("step.retry.will_retry");
const QueueName = SemanticConvention("queue.name");
const QueueMessageId = SemanticConvention("messaging.message.id", "queue.message.id");
const QueueOverheadMs = SemanticConvention("queue.overhead_ms");
const DeploymentId = SemanticConvention("deployment.id");
const HookToken = SemanticConvention("workflow.hook.token");
const HookId = SemanticConvention("workflow.hook.id");
const HookFound = SemanticConvention("workflow.hook.found");
const WorkflowSuspensionState = SemanticConvention("workflow.suspension.state");
const WorkflowSuspensionHookCount = SemanticConvention("workflow.suspension.hook_count");
const WorkflowSuspensionStepCount = SemanticConvention("workflow.suspension.step_count");
const WorkflowSuspensionWaitCount = SemanticConvention("workflow.suspension.wait_count");
async function serializeTraceCarrier() {
  const otel = await OtelApi.value;
  if (!otel)
    return {};
  const carrier = {};
  otel.propagation.inject(otel.context.active(), carrier);
  return carrier;
}
async function deserializeTraceCarrier(traceCarrier) {
  const otel = await OtelApi.value;
  if (!otel)
    return;
  return otel.propagation.extract(otel.context.active(), traceCarrier);
}
async function withTraceContext(traceCarrier, fn) {
  if (!traceCarrier) {
    return fn();
  }
  const otel = await OtelApi.value;
  if (!otel)
    return fn();
  const extractedContext = await deserializeTraceCarrier(traceCarrier);
  if (!extractedContext) {
    return fn();
  }
  return otel.context.with(extractedContext, async () => await fn());
}
const OtelApi = once$1(async () => {
  try {
    return await import("./_chunks/_libs/@opentelemetry/api.mjs");
  } catch {
    console.warn("OpenTelemetry not available, tracing will be disabled");
    return null;
  }
});
const Tracer = once$1(async () => {
  const api = await OtelApi.value;
  if (!api)
    return null;
  return api.trace.getTracer("workflow");
});
async function trace(spanName, ...args) {
  const [tracer, otel] = await Promise.all([Tracer.value, OtelApi.value]);
  const { fn, opts } = typeof args[0] === "function" ? { fn: args[0], opts: {} } : { fn: args[1], opts: args[0] };
  if (!fn)
    throw new Error("Function to trace must be provided");
  if (!tracer || !otel) {
    return await fn();
  }
  return tracer.startActiveSpan(spanName, opts, async (span) => {
    try {
      const result2 = await fn(span);
      span.setStatus({ code: otel.SpanStatusCode.OK });
      return result2;
    } catch (e) {
      span.setStatus({
        code: otel.SpanStatusCode.ERROR,
        message: e.message
      });
      applyWorkflowSuspensionToSpan(e, otel, span);
      throw e;
    } finally {
      span.end();
    }
  });
}
function applyWorkflowSuspensionToSpan(error, otel, span) {
  if (!error || !WorkflowSuspension.is(error)) {
    return;
  }
  span.setStatus({ code: otel.SpanStatusCode.OK });
  span.setAttributes({
    ...WorkflowSuspensionState("suspended"),
    ...WorkflowSuspensionStepCount(error.stepCount),
    ...WorkflowSuspensionHookCount(error.hookCount),
    ...WorkflowSuspensionWaitCount(error.waitCount)
  });
}
async function getSpanContextForTraceCarrier(carrier) {
  const [deserialized, otel] = await Promise.all([
    deserializeTraceCarrier(carrier),
    OtelApi.value
  ]);
  if (!deserialized || !otel)
    return;
  return otel.trace.getSpanContext(deserialized);
}
async function getActiveSpan() {
  return await withOtel((otel) => otel.trace.getActiveSpan());
}
async function getSpanKind(field) {
  return withOtel((x) => x.SpanKind[field]);
}
async function withOtel(fn) {
  const otel = await OtelApi.value;
  if (!otel)
    return void 0;
  return await fn(otel);
}
function linkToCurrentContext() {
  return withOtel((otel) => {
    const context = otel.trace.getActiveSpan()?.spanContext();
    if (!context)
      return;
    return [{ context }];
  });
}
function createLogger(namespace) {
  const baseDebug = debug(`workflow:${namespace}`);
  const logger = (level) => {
    const levelDebug = baseDebug.extend(level);
    return (message, metadata) => {
      levelDebug(message, metadata);
      if (levelDebug.enabled) {
        getActiveSpan().then((span) => {
          span?.addEvent(`${level}.${namespace}`, { message, ...metadata });
        }).catch(() => {
        });
      }
    };
  };
  return {
    debug: logger("debug"),
    info: logger("info"),
    warn: logger("warn"),
    error: logger("error")
  };
}
const stepLogger = createLogger("step");
const runtimeLogger = createLogger("runtime");
const webhookLogger = createLogger("webhook");
const eventsLogger = createLogger("events");
createLogger("adapter");
const execFileAsync = promisify(execFile);
function parsePort(value, radix = 10) {
  const port2 = parseInt(value, radix);
  if (!Number.isNaN(port2) && port2 >= 0 && port2 <= 65535) {
    return port2;
  }
  return void 0;
}
const join = (arr, sep) => arr.join(sep);
const PROC_ROOT = join(["", "proc"], "/");
async function getLinuxPorts(pid) {
  const listenState = "0A";
  const tcpFiles = [`${PROC_ROOT}/net/tcp`, `${PROC_ROOT}/net/tcp6`];
  const socketInodes = [];
  const socketInodesSet = /* @__PURE__ */ new Set();
  const fdPath = `${PROC_ROOT}/${pid}/fd`;
  try {
    const fds = await readdir(fdPath);
    const sortedFds = fds.sort((a, b) => {
      const numA = Number.parseInt(a, 10);
      const numB = Number.parseInt(b, 10);
      return numA - numB;
    });
    const results = await Promise.allSettled(sortedFds.map(async (fd) => {
      const link = await readlink(`${fdPath}/${fd}`);
      const match = link.match(/^socket:\[(\d+)\]$/);
      return match?.[1] ?? null;
    }));
    for (const result2 of results) {
      if (result2.status === "fulfilled" && result2.value) {
        socketInodes.push(result2.value);
        socketInodesSet.add(result2.value);
      }
    }
  } catch {
    return [];
  }
  if (socketInodes.length === 0) {
    return [];
  }
  const inodeToPort = /* @__PURE__ */ new Map();
  for (const tcpFile of tcpFiles) {
    try {
      const content = await readFile(tcpFile, "utf8");
      const lines = content.split("\n").slice(1);
      for (const line of lines) {
        if (!line.trim())
          continue;
        const parts = line.trim().split(/\s+/);
        if (parts.length < 10)
          continue;
        const localAddr = parts[1];
        const state = parts[3];
        const inode = parts[9];
        if (!localAddr || state !== listenState || !inode)
          continue;
        if (!socketInodesSet.has(inode))
          continue;
        const colonIndex = localAddr.indexOf(":");
        if (colonIndex === -1)
          continue;
        const portHex = localAddr.slice(colonIndex + 1);
        if (!portHex)
          continue;
        const port2 = parsePort(portHex, 16);
        if (port2 !== void 0) {
          inodeToPort.set(inode, port2);
        }
      }
    } catch {
    }
  }
  const ports = [];
  for (const inode of socketInodes) {
    const port2 = inodeToPort.get(inode);
    if (port2 !== void 0) {
      ports.push(port2);
    }
  }
  return ports;
}
async function getDarwinPorts(pid) {
  try {
    const { stdout } = await execFileAsync("lsof", [
      "-a",
      "-i",
      "-P",
      "-n",
      "-p",
      pid.toString()
    ]);
    const ports = [];
    const lines = stdout.split("\n");
    for (const line of lines) {
      if (line.includes("LISTEN")) {
        const parts = line.trim().split(/\s+/);
        const addr = parts[8];
        if (addr) {
          const colonIndex = addr.lastIndexOf(":");
          if (colonIndex !== -1) {
            const port2 = parsePort(addr.slice(colonIndex + 1));
            if (port2 !== void 0) {
              ports.push(port2);
            }
          }
        }
      }
    }
    return ports;
  } catch {
    return [];
  }
}
async function getWindowsPorts(pid) {
  try {
    const { stdout } = await execFileAsync("cmd", [
      "/c",
      `netstat -ano | findstr ${pid} | findstr LISTENING`
    ]);
    const ports = [];
    const trimmedOutput = stdout.trim();
    if (trimmedOutput) {
      const lines = trimmedOutput.split("\n");
      for (const line of lines) {
        const match = line.trim().match(/^\s*TCP\s+(?:\[[\da-f:]+\]|[\d.]+):(\d+)\s+/i);
        if (match) {
          const port2 = parsePort(match[1]);
          if (port2 !== void 0) {
            ports.push(port2);
          }
        }
      }
    }
    return ports;
  } catch {
    return [];
  }
}
async function getAllPorts() {
  const { pid, platform } = process;
  try {
    switch (platform) {
      case "linux":
        return await getLinuxPorts(pid);
      case "darwin":
        return await getDarwinPorts(pid);
      case "win32":
        return await getWindowsPorts(pid);
      default:
        return [];
    }
  } catch (error) {
    return [];
  }
}
async function getPort() {
  const ports = await getAllPorts();
  return ports[0];
}
const PROBE_TIMEOUT_MS = 500;
const PROBE_ENDPOINT = "/.well-known/workflow/v1/flow?__health";
async function probePort(port2, options = {}) {
  const { endpoint = PROBE_ENDPOINT, timeout = PROBE_TIMEOUT_MS } = options;
  const controller = new AbortController();
  const timeoutId = setTimeout(() => controller.abort(), timeout);
  try {
    const response = await fetch(`http://localhost:${port2}${endpoint}`, {
      method: "HEAD",
      signal: controller.signal
    });
    return response.status === 200;
  } catch {
    return false;
  } finally {
    clearTimeout(timeoutId);
  }
}
async function getWorkflowPort(options) {
  const ports = await getAllPorts();
  if (ports.length === 0) {
    return void 0;
  }
  if (ports.length === 1) {
    return ports[0];
  }
  const probeResults = await Promise.all(ports.map(async (port2) => ({
    port: port2,
    isWorkflow: await probePort(port2, options)
  })));
  const workflowPort = probeResults.find((r) => r.isWorkflow);
  if (workflowPort) {
    return workflowPort.port;
  }
  return ports[0];
}
function once(fn) {
  const result2 = {
    get value() {
      const value = fn();
      Object.defineProperty(result2, "value", { value });
      return value;
    }
  };
  return result2;
}
const getDataDirFromEnv = () => {
  return process.env.WORKFLOW_LOCAL_DATA_DIR || ".workflow-data";
};
const DEFAULT_RESOLVE_DATA_OPTION$1 = "all";
const getBaseUrlFromEnv = () => {
  return process.env.WORKFLOW_LOCAL_BASE_URL;
};
const config = once(() => {
  const dataDir = getDataDirFromEnv();
  const baseUrl = getBaseUrlFromEnv();
  return { dataDir, baseUrl };
});
async function resolveBaseUrl(config2) {
  if (config2.baseUrl) {
    return config2.baseUrl;
  }
  if (process.env.WORKFLOW_LOCAL_BASE_URL) {
    return process.env.WORKFLOW_LOCAL_BASE_URL;
  }
  if (typeof config2.port === "number") {
    return `http://localhost:${config2.port}`;
  }
  if (process.env.PORT) {
    return `http://localhost:${process.env.PORT}`;
  }
  const detectedPort = await getWorkflowPort();
  if (detectedPort) {
    return `http://localhost:${detectedPort}`;
  }
  throw new Error("Unable to resolve base URL for workflow queue.");
}
const PACKAGE_NAME = "@workflow/world-local";
let cachedPackageInfo = null;
function getModuleDir() {
  if (typeof import.meta.url === "string" && import.meta.url) {
    return path__default.dirname(fileURLToPath(import.meta.url));
  }
  return null;
}
async function getPackageInfo() {
  if (cachedPackageInfo) {
    return cachedPackageInfo;
  }
  const moduleDir = getModuleDir();
  if (moduleDir) {
    try {
      const content = await readFile(path__default.join(moduleDir, "../package.json"), "utf-8");
      cachedPackageInfo = JSON.parse(content);
      return cachedPackageInfo;
    } catch {
    }
  }
  cachedPackageInfo = {
    name: PACKAGE_NAME,
    version: "bundled"
  };
  return cachedPackageInfo;
}
const VERSION_FILENAME = "version.txt";
class DataDirAccessError extends Error {
  dataDir;
  code;
  constructor(message, dataDir, code) {
    super(message);
    this.name = "DataDirAccessError";
    this.dataDir = dataDir;
    this.code = code;
  }
}
class DataDirVersionError extends Error {
  oldVersion;
  newVersion;
  suggestedVersion;
  constructor(message, oldVersion, newVersion, suggestedVersion) {
    super(message);
    this.name = "DataDirVersionError";
    this.oldVersion = oldVersion;
    this.newVersion = newVersion;
    this.suggestedVersion = suggestedVersion;
  }
}
function parseVersion(versionString) {
  const match = versionString.match(/^(\d+)\.(\d+)\.(\d+)(?:-(.+))?$/);
  if (!match) {
    throw new Error(`Invalid version string: "${versionString}"`);
  }
  return {
    major: parseInt(match[1], 10),
    minor: parseInt(match[2], 10),
    patch: parseInt(match[3], 10),
    prerelease: match[4],
    raw: versionString
  };
}
function formatVersion(version2) {
  const base = `${version2.major}.${version2.minor}.${version2.patch}`;
  return version2.prerelease ? `${base}-${version2.prerelease}` : base;
}
function parseVersionFile(content) {
  const trimmed = content.trim();
  const lastAtIndex = trimmed.lastIndexOf("@");
  if (lastAtIndex <= 0) {
    throw new Error(`Invalid version file content: "${content}"`);
  }
  const packageName = trimmed.substring(0, lastAtIndex);
  const versionString = trimmed.substring(lastAtIndex + 1);
  return {
    packageName,
    version: parseVersion(versionString)
  };
}
function formatVersionFile(packageName, version2) {
  return `${packageName}@${formatVersion(version2)}`;
}
function upgradeVersion(oldVersion, newVersion) {
  console.log(`[world-local] Upgrading from version ${formatVersion(oldVersion)} to ${formatVersion(newVersion)}`);
}
async function ensureDataDir(dataDir) {
  const absolutePath = path__default.resolve(dataDir);
  try {
    await mkdir(absolutePath, { recursive: true });
  } catch (error) {
    const nodeError = error;
    if (nodeError.code !== "EEXIST") {
      throw new DataDirAccessError(`Failed to create data directory "${absolutePath}": ${nodeError.message}`, absolutePath, nodeError.code);
    }
  }
  try {
    await access(absolutePath, constants.R_OK);
  } catch (error) {
    const nodeError = error;
    throw new DataDirAccessError(`Data directory "${absolutePath}" is not readable: ${nodeError.message}`, absolutePath, nodeError.code);
  }
  const testFile = path__default.join(absolutePath, `.workflow-write-test-${Date.now()}`);
  try {
    await writeFile(testFile, "");
    await unlink(testFile);
  } catch (error) {
    const nodeError = error;
    throw new DataDirAccessError(`Data directory "${absolutePath}" is not writable: ${nodeError.message}`, absolutePath, nodeError.code);
  }
}
async function readVersionFile(dataDir) {
  const versionFilePath = path__default.join(path__default.resolve(dataDir), VERSION_FILENAME);
  try {
    const content = await readFile(versionFilePath, "utf-8");
    return parseVersionFile(content);
  } catch (error) {
    const nodeError = error;
    if (nodeError.code === "ENOENT") {
      return null;
    }
    throw error;
  }
}
async function writeVersionFile(dataDir, version2) {
  const versionFilePath = path__default.join(path__default.resolve(dataDir), VERSION_FILENAME);
  const packageInfo = await getPackageInfo();
  const content = formatVersionFile(packageInfo.name, version2);
  await writeFile(versionFilePath, content);
}
function getSuggestedDowngradeVersion(oldVersion, suggestedVersion) {
  if (suggestedVersion) {
    return suggestedVersion;
  }
  return formatVersion(oldVersion);
}
async function initDataDir(dataDir) {
  await ensureDataDir(dataDir);
  const packageInfo = await getPackageInfo();
  const currentVersion = parseVersion(packageInfo.version);
  const existingVersionInfo = await readVersionFile(dataDir);
  if (existingVersionInfo === null) {
    await writeVersionFile(dataDir, currentVersion);
    return;
  }
  const { version: oldVersion } = existingVersionInfo;
  if (formatVersion(oldVersion) === formatVersion(currentVersion)) {
    return;
  }
  try {
    upgradeVersion(oldVersion, currentVersion);
    await writeVersionFile(dataDir, currentVersion);
  } catch (error) {
    const suggestedVersion = error instanceof DataDirVersionError ? error.suggestedVersion : void 0;
    const downgradeTarget = getSuggestedDowngradeVersion(oldVersion, suggestedVersion);
    console.error(`[world-local] Failed to upgrade data directory from version ${formatVersion(oldVersion)} to ${formatVersion(currentVersion)}:`, error instanceof Error ? error.message : error);
    console.error(`[world-local] Data is not compatible with the current version. Please downgrade to ${packageInfo.name}@${downgradeTarget}`);
    throw error;
  }
}
const LOCAL_QUEUE_MAX_VISIBILITY = parseInt(process.env.WORKFLOW_LOCAL_QUEUE_MAX_VISIBILITY ?? "0", 10) || Infinity;
const MAX_SAFE_TIMEOUT_MS = 2147483647;
const DEFAULT_CONCURRENCY_LIMIT = 100;
const WORKFLOW_LOCAL_QUEUE_CONCURRENCY = parseInt(process.env.WORKFLOW_LOCAL_QUEUE_CONCURRENCY ?? "0", 10) || DEFAULT_CONCURRENCY_LIMIT;
const httpAgent = new undiciExports.Agent({
  headersTimeout: 0,
  connections: 100,
  keepAliveTimeout: 3e4
});
function createQueue$1(config2) {
  const transport = new JsonTransport();
  const generateId2 = monotonicFactory();
  const semaphore = new libExports.Sema(WORKFLOW_LOCAL_QUEUE_CONCURRENCY);
  const inflightMessages = /* @__PURE__ */ new Map();
  const queue = async (queueName, message, opts) => {
    const cleanup = [];
    if (opts?.idempotencyKey) {
      const existing = inflightMessages.get(opts.idempotencyKey);
      if (existing) {
        return { messageId: existing };
      }
    }
    const body = transport.serialize(message);
    let pathname;
    if (queueName.startsWith("__wkf_step_")) {
      pathname = `step`;
    } else if (queueName.startsWith("__wkf_workflow_")) {
      pathname = `flow`;
    } else {
      throw new Error("Unknown queue name prefix");
    }
    const messageId = MessageId.parse(`msg_${generateId2()}`);
    if (opts?.idempotencyKey) {
      const key2 = opts.idempotencyKey;
      inflightMessages.set(key2, messageId);
      cleanup.push(() => {
        inflightMessages.delete(key2);
      });
    }
    (async () => {
      const token = semaphore.tryAcquire();
      if (!token) {
        console.warn(`[world-local]: concurrency limit (${WORKFLOW_LOCAL_QUEUE_CONCURRENCY}) reached, waiting for queue to free up`);
        await semaphore.acquire();
      }
      try {
        let defaultRetriesLeft = 3;
        const baseUrl = await resolveBaseUrl(config2);
        for (let attempt = 0; defaultRetriesLeft > 0; attempt++) {
          defaultRetriesLeft--;
          const response = await fetch(`${baseUrl}/.well-known/workflow/v1/${pathname}`, {
            method: "POST",
            duplex: "half",
            dispatcher: httpAgent,
            headers: {
              "content-type": "application/json",
              "x-vqs-queue-name": queueName,
              "x-vqs-message-id": messageId,
              "x-vqs-message-attempt": String(attempt + 1)
            },
            body
          });
          if (response.ok) {
            return;
          }
          const text = await response.text();
          if (response.status === 503) {
            try {
              const timeoutSeconds = Number(JSON.parse(text).timeoutSeconds);
              const timeoutMs = Math.min(timeoutSeconds * 1e3, MAX_SAFE_TIMEOUT_MS);
              await setTimeout$1(timeoutMs);
              defaultRetriesLeft++;
              continue;
            } catch {
            }
          }
          console.error(`[local world] Failed to queue message`, {
            queueName,
            text,
            status: response.status,
            headers: Object.fromEntries(response.headers.entries()),
            body: body.toString()
          });
        }
        console.error(`[local world] Reached max retries of local world queue implementation`);
      } finally {
        semaphore.release();
      }
    })().catch((err) => {
      const isAbortError = err?.name === "AbortError" || err?.name === "ResponseAborted";
      if (!isAbortError) {
        console.error("[local world] Queue operation failed:", err);
      }
    }).finally(() => {
      for (const fn of cleanup) {
        fn();
      }
    });
    return { messageId };
  };
  const HeaderParser = z.object({
    "x-vqs-queue-name": ValidQueueName,
    "x-vqs-message-id": MessageId,
    "x-vqs-message-attempt": z.coerce.number()
  });
  const createQueueHandler = (prefix, handler2) => {
    return async (req) => {
      const headers = HeaderParser.safeParse(Object.fromEntries(req.headers));
      if (!headers.success || !req.body) {
        return Response.json({
          error: !req.body ? "Missing request body" : "Missing required headers"
        }, { status: 400 });
      }
      const queueName = headers.data["x-vqs-queue-name"];
      const messageId = headers.data["x-vqs-message-id"];
      const attempt = headers.data["x-vqs-message-attempt"];
      if (!queueName.startsWith(prefix)) {
        return Response.json({ error: "Unhandled queue" }, { status: 400 });
      }
      const body = await new JsonTransport().deserialize(req.body);
      try {
        const result2 = await handler2(body, { attempt, queueName, messageId });
        let timeoutSeconds = null;
        if (typeof result2?.timeoutSeconds === "number") {
          timeoutSeconds = Math.min(result2.timeoutSeconds, LOCAL_QUEUE_MAX_VISIBILITY);
        }
        if (timeoutSeconds) {
          return Response.json({ timeoutSeconds }, { status: 503 });
        }
        return Response.json({ ok: true });
      } catch (error) {
        return Response.json(String(error), { status: 500 });
      }
    };
  };
  const getDeploymentId = async () => {
    const packageInfo = await getPackageInfo();
    return `dpl_local@${packageInfo.version}`;
  };
  return { queue, createQueueHandler, getDeploymentId };
}
const ulid = monotonicFactory(() => Math.random());
const Ulid = string().ulid();
const isWindows = process.platform === "win32";
async function withWindowsRetry(fn, maxRetries = 5) {
  if (!isWindows)
    return fn();
  const retryableErrors = ["EPERM", "EBUSY", "EACCES"];
  const baseDelayMs = 10;
  for (let attempt = 0; attempt <= maxRetries; attempt++) {
    try {
      return await fn();
    } catch (error) {
      const isRetryable = attempt < maxRetries && retryableErrors.includes(error.code);
      if (!isRetryable)
        throw error;
      const delay = baseDelayMs * Math.pow(2, attempt) + Math.random() * baseDelayMs;
      await new Promise((resolve2) => setTimeout(resolve2, delay));
    }
  }
  throw new Error("Retry loop exited unexpectedly");
}
const createdFilesCache = /* @__PURE__ */ new Set();
function ulidToDate(maybeUlid) {
  const ulid2 = Ulid.safeParse(maybeUlid);
  if (!ulid2.success) {
    return null;
  }
  return new Date(decodeTime(ulid2.data));
}
async function ensureDir(dirPath) {
  try {
    await promises.mkdir(dirPath, { recursive: true });
  } catch (_error) {
  }
}
async function writeJSON(filePath, data, opts) {
  return write(filePath, JSON.stringify(data, null, 2), opts);
}
async function write(filePath, data, opts) {
  if (!opts?.overwrite) {
    if (createdFilesCache.has(filePath)) {
      throw new WorkflowAPIError(`File ${filePath} already exists and 'overwrite' is false`, { status: 409 });
    }
    try {
      await promises.access(filePath);
      createdFilesCache.add(filePath);
      throw new WorkflowAPIError(`File ${filePath} already exists and 'overwrite' is false`, { status: 409 });
    } catch (error) {
      if (error.code !== "ENOENT") {
        throw error;
      }
    }
  }
  const tempPath = `${filePath}.tmp.${ulid()}`;
  let tempFileCreated = false;
  try {
    await ensureDir(path__default.dirname(filePath));
    await promises.writeFile(tempPath, data);
    tempFileCreated = true;
    await withWindowsRetry(() => promises.rename(tempPath, filePath));
    createdFilesCache.add(filePath);
  } catch (error) {
    if (tempFileCreated) {
      await withWindowsRetry(() => promises.unlink(tempPath), 3).catch(() => {
      });
    }
    throw error;
  }
}
async function readJSON(filePath, decoder) {
  try {
    const content = await promises.readFile(filePath, "utf-8");
    return decoder.parse(JSON.parse(content));
  } catch (error) {
    if (error.code === "ENOENT")
      return null;
    throw error;
  }
}
async function readBuffer(filePath) {
  const content = await promises.readFile(filePath);
  return content;
}
async function deleteJSON(filePath) {
  try {
    await promises.unlink(filePath);
  } catch (error) {
    if (error.code !== "ENOENT")
      throw error;
  }
}
async function listJSONFiles(dirPath) {
  try {
    const files = await promises.readdir(dirPath);
    return files.filter((f) => f.endsWith(".json")).map((f) => f.replace(".json", ""));
  } catch (error) {
    if (error.code === "ENOENT")
      return [];
    throw error;
  }
}
function parseCursor(cursor) {
  if (!cursor)
    return null;
  const parts = cursor.split("|");
  return {
    timestamp: new Date(parts[0]),
    id: parts[1] || null
  };
}
function createCursor(timestamp, id) {
  return id ? `${timestamp.toISOString()}|${id}` : timestamp.toISOString();
}
async function paginatedFileSystemQuery(config2) {
  const { directory, schema, filePrefix, filter, sortOrder = "desc", limit = 20, cursor, getCreatedAt, getId } = config2;
  const fileIds = await listJSONFiles(directory);
  const relevantFileIds = filePrefix ? fileIds.filter((fileId) => fileId.startsWith(filePrefix)) : fileIds;
  const parsedCursor = parseCursor(cursor);
  let candidateFileIds = relevantFileIds;
  if (parsedCursor) {
    candidateFileIds = relevantFileIds.filter((fileId) => {
      const filenameDate = getCreatedAt(`${fileId}.json`);
      if (filenameDate) {
        const cursorTime = parsedCursor.timestamp.getTime();
        const fileTime = filenameDate.getTime();
        if (parsedCursor.id) {
          return sortOrder === "desc" ? fileTime <= cursorTime : fileTime >= cursorTime;
        } else {
          return sortOrder === "desc" ? fileTime < cursorTime : fileTime > cursorTime;
        }
      }
      return true;
    });
  }
  const validItems = [];
  for (const fileId of candidateFileIds) {
    const filePath = path__default.join(directory, `${fileId}.json`);
    let item = null;
    try {
      item = await readJSON(filePath, schema);
    } catch (error) {
      if (error instanceof ZodError) {
        console.warn(`Skipping item ${fileId} due to malformed JSON: ${error.message}`);
        continue;
      }
      throw error;
    }
    if (item) {
      if (filter && !filter(item))
        continue;
      if (parsedCursor) {
        const itemTime = item.createdAt.getTime();
        const cursorTime = parsedCursor.timestamp.getTime();
        if (sortOrder === "desc") {
          if (itemTime > cursorTime)
            continue;
          if (itemTime === cursorTime && parsedCursor.id && getId) {
            const itemId = getId(item);
            if (itemId >= parsedCursor.id)
              continue;
          }
        } else {
          if (itemTime < cursorTime)
            continue;
          if (itemTime === cursorTime && parsedCursor.id && getId) {
            const itemId = getId(item);
            if (itemId <= parsedCursor.id)
              continue;
          }
        }
      }
      validItems.push(item);
    }
  }
  validItems.sort((a, b) => {
    const aTime = a.createdAt.getTime();
    const bTime = b.createdAt.getTime();
    const timeComparison = sortOrder === "asc" ? aTime - bTime : bTime - aTime;
    if (timeComparison === 0 && getId) {
      const aId = getId(a);
      const bId = getId(b);
      return sortOrder === "asc" ? aId.localeCompare(bId) : bId.localeCompare(aId);
    }
    return timeComparison;
  });
  const hasMore = validItems.length > limit;
  const items = hasMore ? validItems.slice(0, limit) : validItems;
  const nextCursor = items.length > 0 ? createCursor(items[items.length - 1].createdAt, getId?.(items[items.length - 1])) : null;
  return {
    data: items,
    cursor: nextCursor,
    hasMore
  };
}
function filterRunData$1(run, resolveData) {
  if (resolveData === "none") {
    return {
      ...run,
      input: [],
      output: void 0
    };
  }
  return run;
}
function filterStepData$1(step, resolveData) {
  if (resolveData === "none") {
    return {
      ...step,
      input: [],
      output: void 0
    };
  }
  return step;
}
function filterEventData$1(event, resolveData) {
  if (resolveData === "none") {
    const { eventData: _eventData, ...rest } = event;
    return rest;
  }
  return event;
}
function filterHookData$1(hook, resolveData) {
  if (resolveData === "none") {
    const { metadata: _metadata, ...rest } = hook;
    return rest;
  }
  return hook;
}
const monotonicUlid$1 = monotonicFactory(() => Math.random());
const getObjectCreatedAt = (idPrefix) => (filename) => {
  const replaceRegex = new RegExp(`^${idPrefix}_`, "g");
  const dashIndex = filename.indexOf("-");
  if (dashIndex === -1) {
    const ulid3 = filename.replace(/\.json$/, "").replace(replaceRegex, "");
    return ulidToDate(ulid3);
  }
  if (idPrefix === "step") {
    return null;
  }
  const id = filename.substring(dashIndex + 1).replace(/\.json$/, "");
  const ulid2 = id.replace(replaceRegex, "");
  return ulidToDate(ulid2);
};
function createHooksStorage(basedir) {
  async function findHookByToken(token) {
    const hooksDir = path__default.join(basedir, "hooks");
    const files = await listJSONFiles(hooksDir);
    for (const file of files) {
      const hookPath = path__default.join(hooksDir, `${file}.json`);
      const hook = await readJSON(hookPath, HookSchema);
      if (hook && hook.token === token) {
        return hook;
      }
    }
    return null;
  }
  async function get(hookId, params) {
    const hookPath = path__default.join(basedir, "hooks", `${hookId}.json`);
    const hook = await readJSON(hookPath, HookSchema);
    if (!hook) {
      throw new Error(`Hook ${hookId} not found`);
    }
    const resolveData = params?.resolveData || DEFAULT_RESOLVE_DATA_OPTION$1;
    return filterHookData$1(hook, resolveData);
  }
  async function getByToken(token) {
    const hook = await findHookByToken(token);
    if (!hook) {
      throw new Error(`Hook with token ${token} not found`);
    }
    return hook;
  }
  async function list(params) {
    const hooksDir = path__default.join(basedir, "hooks");
    const resolveData = params.resolveData || DEFAULT_RESOLVE_DATA_OPTION$1;
    const result2 = await paginatedFileSystemQuery({
      directory: hooksDir,
      schema: HookSchema,
      sortOrder: params.pagination?.sortOrder,
      limit: params.pagination?.limit,
      cursor: params.pagination?.cursor,
      filePrefix: void 0,
      // Hooks don't have ULIDs, so we can't optimize by filename
      filter: (hook) => {
        if (params.runId && hook.runId !== params.runId) {
          return false;
        }
        return true;
      },
      getCreatedAt: () => {
        return /* @__PURE__ */ new Date(0);
      },
      getId: (hook) => hook.hookId
    });
    return {
      ...result2,
      data: result2.data.map((hook) => filterHookData$1(hook, resolveData))
    };
  }
  return { get, getByToken, list };
}
async function deleteAllHooksForRun(basedir, runId) {
  const hooksDir = path__default.join(basedir, "hooks");
  const files = await listJSONFiles(hooksDir);
  for (const file of files) {
    const hookPath = path__default.join(hooksDir, `${file}.json`);
    const hook = await readJSON(hookPath, HookSchema);
    if (hook && hook.runId === runId) {
      await deleteJSON(hookPath);
    }
  }
}
async function handleLegacyEvent(basedir, runId, data, currentRun, params) {
  const resolveData = params?.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION$1;
  switch (data.eventType) {
    case "run_cancelled": {
      const now = /* @__PURE__ */ new Date();
      const run = {
        runId: currentRun.runId,
        deploymentId: currentRun.deploymentId,
        workflowName: currentRun.workflowName,
        specVersion: currentRun.specVersion,
        executionContext: currentRun.executionContext,
        input: currentRun.input,
        createdAt: currentRun.createdAt,
        expiredAt: currentRun.expiredAt,
        startedAt: currentRun.startedAt,
        status: "cancelled",
        output: void 0,
        error: void 0,
        completedAt: now,
        updatedAt: now
      };
      const runPath = path__default.join(basedir, "runs", `${runId}.json`);
      await writeJSON(runPath, run, { overwrite: true });
      await deleteAllHooksForRun(basedir, runId);
      return { event: void 0, run: filterRunData$1(run, resolveData) };
    }
    case "wait_completed":
    case "hook_received": {
      const eventId = `evnt_${monotonicUlid$1()}`;
      const now = /* @__PURE__ */ new Date();
      const event = {
        ...data,
        runId,
        eventId,
        createdAt: now,
        specVersion: SPEC_VERSION_CURRENT
      };
      const compositeKey = `${runId}-${eventId}`;
      const eventPath = path__default.join(basedir, "events", `${compositeKey}.json`);
      await writeJSON(eventPath, event);
      return { event: filterEventData$1(event, resolveData) };
    }
    default:
      throw new Error(`Event type '${data.eventType}' not supported for legacy runs (specVersion: ${currentRun.specVersion || "undefined"}). Please upgrade 'workflow' package.`);
  }
}
function createEventsStorage(basedir) {
  return {
    async create(runId, data, params) {
      const eventId = `evnt_${monotonicUlid$1()}`;
      const now = /* @__PURE__ */ new Date();
      let effectiveRunId;
      if (data.eventType === "run_created" && (!runId || runId === "")) {
        effectiveRunId = `wrun_${monotonicUlid$1()}`;
      } else if (!runId) {
        throw new Error("runId is required for non-run_created events");
      } else {
        effectiveRunId = runId;
      }
      const effectiveSpecVersion = data.specVersion ?? SPEC_VERSION_CURRENT;
      const isRunTerminal = (status) => ["completed", "failed", "cancelled"].includes(status);
      const isStepTerminal = (status) => ["completed", "failed"].includes(status);
      let currentRun = null;
      const skipRunValidationEvents = ["step_completed", "step_retrying"];
      if (data.eventType !== "run_created" && !skipRunValidationEvents.includes(data.eventType)) {
        const runPath = path__default.join(basedir, "runs", `${effectiveRunId}.json`);
        currentRun = await readJSON(runPath, WorkflowRunSchema);
      }
      if (currentRun) {
        if (requiresNewerWorld(currentRun.specVersion)) {
          throw new RunNotSupportedError(currentRun.specVersion, SPEC_VERSION_CURRENT);
        }
        if (isLegacySpecVersion(currentRun.specVersion)) {
          return handleLegacyEvent(basedir, effectiveRunId, data, currentRun, params);
        }
      }
      if (currentRun && isRunTerminal(currentRun.status)) {
        const runTerminalEvents = [
          "run_started",
          "run_completed",
          "run_failed"
        ];
        if (data.eventType === "run_cancelled" && currentRun.status === "cancelled") {
          const event2 = {
            ...data,
            runId: effectiveRunId,
            eventId,
            createdAt: now,
            specVersion: effectiveSpecVersion
          };
          const compositeKey2 = `${effectiveRunId}-${eventId}`;
          const eventPath2 = path__default.join(basedir, "events", `${compositeKey2}.json`);
          await writeJSON(eventPath2, event2);
          const resolveData2 = params?.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION$1;
          return {
            event: filterEventData$1(event2, resolveData2),
            run: currentRun
          };
        }
        if (runTerminalEvents.includes(data.eventType) || data.eventType === "run_cancelled") {
          throw new WorkflowAPIError(`Cannot transition run from terminal state "${currentRun.status}"`, { status: 410 });
        }
        if (data.eventType === "step_created" || data.eventType === "hook_created") {
          throw new WorkflowAPIError(`Cannot create new entities on run in terminal state "${currentRun.status}"`, { status: 410 });
        }
      }
      let validatedStep = null;
      const stepEvents = [
        "step_started",
        "step_completed",
        "step_failed",
        "step_retrying"
      ];
      if (stepEvents.includes(data.eventType) && data.correlationId) {
        const stepCompositeKey = `${effectiveRunId}-${data.correlationId}`;
        const stepPath = path__default.join(basedir, "steps", `${stepCompositeKey}.json`);
        validatedStep = await readJSON(stepPath, StepSchema);
        if (!validatedStep) {
          throw new WorkflowAPIError(`Step "${data.correlationId}" not found`, {
            status: 404
          });
        }
        if (isStepTerminal(validatedStep.status)) {
          throw new WorkflowAPIError(`Cannot modify step in terminal state "${validatedStep.status}"`, { status: 410 });
        }
        if (currentRun && isRunTerminal(currentRun.status)) {
          if (validatedStep.status !== "running") {
            throw new WorkflowAPIError(`Cannot modify non-running step on run in terminal state "${currentRun.status}"`, { status: 410 });
          }
        }
      }
      const hookEventsRequiringExistence = ["hook_disposed", "hook_received"];
      if (hookEventsRequiringExistence.includes(data.eventType) && data.correlationId) {
        const hookPath = path__default.join(basedir, "hooks", `${data.correlationId}.json`);
        const existingHook = await readJSON(hookPath, HookSchema);
        if (!existingHook) {
          throw new WorkflowAPIError(`Hook "${data.correlationId}" not found`, {
            status: 404
          });
        }
      }
      const event = {
        ...data,
        runId: effectiveRunId,
        eventId,
        createdAt: now,
        specVersion: effectiveSpecVersion
      };
      let run;
      let step;
      let hook;
      if (data.eventType === "run_created" && "eventData" in data) {
        const runData = data.eventData;
        run = {
          runId: effectiveRunId,
          deploymentId: runData.deploymentId,
          status: "pending",
          workflowName: runData.workflowName,
          // Propagate specVersion from the event to the run entity
          specVersion: effectiveSpecVersion,
          executionContext: runData.executionContext,
          input: runData.input || [],
          output: void 0,
          error: void 0,
          startedAt: void 0,
          completedAt: void 0,
          createdAt: now,
          updatedAt: now
        };
        const runPath = path__default.join(basedir, "runs", `${effectiveRunId}.json`);
        await writeJSON(runPath, run);
      } else if (data.eventType === "run_started") {
        if (currentRun) {
          const runPath = path__default.join(basedir, "runs", `${effectiveRunId}.json`);
          run = {
            runId: currentRun.runId,
            deploymentId: currentRun.deploymentId,
            workflowName: currentRun.workflowName,
            specVersion: currentRun.specVersion,
            executionContext: currentRun.executionContext,
            input: currentRun.input,
            createdAt: currentRun.createdAt,
            expiredAt: currentRun.expiredAt,
            status: "running",
            output: void 0,
            error: void 0,
            completedAt: void 0,
            startedAt: currentRun.startedAt ?? now,
            updatedAt: now
          };
          await writeJSON(runPath, run, { overwrite: true });
        }
      } else if (data.eventType === "run_completed" && "eventData" in data) {
        const completedData = data.eventData;
        if (currentRun) {
          const runPath = path__default.join(basedir, "runs", `${effectiveRunId}.json`);
          run = {
            runId: currentRun.runId,
            deploymentId: currentRun.deploymentId,
            workflowName: currentRun.workflowName,
            specVersion: currentRun.specVersion,
            executionContext: currentRun.executionContext,
            input: currentRun.input,
            createdAt: currentRun.createdAt,
            expiredAt: currentRun.expiredAt,
            startedAt: currentRun.startedAt,
            status: "completed",
            output: completedData.output,
            error: void 0,
            completedAt: now,
            updatedAt: now
          };
          await writeJSON(runPath, run, { overwrite: true });
          await deleteAllHooksForRun(basedir, effectiveRunId);
        }
      } else if (data.eventType === "run_failed" && "eventData" in data) {
        const failedData = data.eventData;
        if (currentRun) {
          const runPath = path__default.join(basedir, "runs", `${effectiveRunId}.json`);
          run = {
            runId: currentRun.runId,
            deploymentId: currentRun.deploymentId,
            workflowName: currentRun.workflowName,
            specVersion: currentRun.specVersion,
            executionContext: currentRun.executionContext,
            input: currentRun.input,
            createdAt: currentRun.createdAt,
            expiredAt: currentRun.expiredAt,
            startedAt: currentRun.startedAt,
            status: "failed",
            output: void 0,
            error: {
              message: typeof failedData.error === "string" ? failedData.error : failedData.error?.message ?? "Unknown error",
              stack: failedData.error?.stack,
              code: failedData.errorCode
            },
            completedAt: now,
            updatedAt: now
          };
          await writeJSON(runPath, run, { overwrite: true });
          await deleteAllHooksForRun(basedir, effectiveRunId);
        }
      } else if (data.eventType === "run_cancelled") {
        if (currentRun) {
          const runPath = path__default.join(basedir, "runs", `${effectiveRunId}.json`);
          run = {
            runId: currentRun.runId,
            deploymentId: currentRun.deploymentId,
            workflowName: currentRun.workflowName,
            specVersion: currentRun.specVersion,
            executionContext: currentRun.executionContext,
            input: currentRun.input,
            createdAt: currentRun.createdAt,
            expiredAt: currentRun.expiredAt,
            startedAt: currentRun.startedAt,
            status: "cancelled",
            output: void 0,
            error: void 0,
            completedAt: now,
            updatedAt: now
          };
          await writeJSON(runPath, run, { overwrite: true });
          await deleteAllHooksForRun(basedir, effectiveRunId);
        }
      } else if (
        // Step lifecycle events
        data.eventType === "step_created" && "eventData" in data
      ) {
        const stepData = data.eventData;
        step = {
          runId: effectiveRunId,
          stepId: data.correlationId,
          stepName: stepData.stepName,
          status: "pending",
          input: stepData.input,
          output: void 0,
          error: void 0,
          attempt: 0,
          startedAt: void 0,
          completedAt: void 0,
          createdAt: now,
          updatedAt: now,
          // Propagate specVersion from the event to the step entity
          specVersion: effectiveSpecVersion
        };
        const stepCompositeKey = `${effectiveRunId}-${data.correlationId}`;
        const stepPath = path__default.join(basedir, "steps", `${stepCompositeKey}.json`);
        await writeJSON(stepPath, step);
      } else if (data.eventType === "step_started") {
        if (validatedStep) {
          const stepCompositeKey = `${effectiveRunId}-${data.correlationId}`;
          const stepPath = path__default.join(basedir, "steps", `${stepCompositeKey}.json`);
          step = {
            ...validatedStep,
            status: "running",
            // Only set startedAt on the first start
            startedAt: validatedStep.startedAt ?? now,
            // Increment attempt counter on every start
            attempt: validatedStep.attempt + 1,
            updatedAt: now
          };
          await writeJSON(stepPath, step, { overwrite: true });
        }
      } else if (data.eventType === "step_completed" && "eventData" in data) {
        const completedData = data.eventData;
        if (validatedStep) {
          const stepCompositeKey = `${effectiveRunId}-${data.correlationId}`;
          const stepPath = path__default.join(basedir, "steps", `${stepCompositeKey}.json`);
          step = {
            ...validatedStep,
            status: "completed",
            output: completedData.result,
            completedAt: now,
            updatedAt: now
          };
          await writeJSON(stepPath, step, { overwrite: true });
        }
      } else if (data.eventType === "step_failed" && "eventData" in data) {
        const failedData = data.eventData;
        if (validatedStep) {
          const stepCompositeKey = `${effectiveRunId}-${data.correlationId}`;
          const stepPath = path__default.join(basedir, "steps", `${stepCompositeKey}.json`);
          const error = {
            message: typeof failedData.error === "string" ? failedData.error : failedData.error?.message ?? "Unknown error",
            stack: failedData.stack
          };
          step = {
            ...validatedStep,
            status: "failed",
            error,
            completedAt: now,
            updatedAt: now
          };
          await writeJSON(stepPath, step, { overwrite: true });
        }
      } else if (data.eventType === "step_retrying" && "eventData" in data) {
        const retryData = data.eventData;
        if (validatedStep) {
          const stepCompositeKey = `${effectiveRunId}-${data.correlationId}`;
          const stepPath = path__default.join(basedir, "steps", `${stepCompositeKey}.json`);
          step = {
            ...validatedStep,
            status: "pending",
            error: {
              message: typeof retryData.error === "string" ? retryData.error : retryData.error?.message ?? "Unknown error",
              stack: retryData.stack
            },
            retryAfter: retryData.retryAfter,
            updatedAt: now
          };
          await writeJSON(stepPath, step, { overwrite: true });
        }
      } else if (
        // Hook lifecycle events
        data.eventType === "hook_created" && "eventData" in data
      ) {
        const hookData = data.eventData;
        const hooksDir = path__default.join(basedir, "hooks");
        const hookFiles = await listJSONFiles(hooksDir);
        let hasConflict = false;
        for (const file of hookFiles) {
          const existingHookPath = path__default.join(hooksDir, `${file}.json`);
          const existingHook = await readJSON(existingHookPath, HookSchema);
          if (existingHook && existingHook.token === hookData.token) {
            hasConflict = true;
            break;
          }
        }
        if (hasConflict) {
          const conflictEvent = {
            eventType: "hook_conflict",
            correlationId: data.correlationId,
            eventData: {
              token: hookData.token
            },
            runId: effectiveRunId,
            eventId,
            createdAt: now,
            specVersion: effectiveSpecVersion
          };
          const compositeKey2 = `${effectiveRunId}-${eventId}`;
          const eventPath2 = path__default.join(basedir, "events", `${compositeKey2}.json`);
          await writeJSON(eventPath2, conflictEvent);
          const resolveData2 = params?.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION$1;
          const filteredEvent2 = filterEventData$1(conflictEvent, resolveData2);
          return {
            event: filteredEvent2,
            run,
            step,
            hook: void 0
          };
        }
        hook = {
          runId: effectiveRunId,
          hookId: data.correlationId,
          token: hookData.token,
          metadata: hookData.metadata,
          ownerId: "local-owner",
          projectId: "local-project",
          environment: "local",
          createdAt: now,
          // Propagate specVersion from the event to the hook entity
          specVersion: effectiveSpecVersion
        };
        const hookPath = path__default.join(basedir, "hooks", `${data.correlationId}.json`);
        await writeJSON(hookPath, hook);
      } else if (data.eventType === "hook_disposed") {
        const hookPath = path__default.join(basedir, "hooks", `${data.correlationId}.json`);
        await deleteJSON(hookPath);
      }
      const compositeKey = `${effectiveRunId}-${eventId}`;
      const eventPath = path__default.join(basedir, "events", `${compositeKey}.json`);
      await writeJSON(eventPath, event);
      const resolveData = params?.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION$1;
      const filteredEvent = filterEventData$1(event, resolveData);
      return {
        event: filteredEvent,
        run,
        step,
        hook
      };
    },
    async list(params) {
      const { runId } = params;
      const resolveData = params.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION$1;
      const result2 = await paginatedFileSystemQuery({
        directory: path__default.join(basedir, "events"),
        schema: EventSchema,
        filePrefix: `${runId}-`,
        // Events in chronological order (oldest first) by default,
        // different from the default for other list calls.
        sortOrder: params.pagination?.sortOrder ?? "asc",
        limit: params.pagination?.limit,
        cursor: params.pagination?.cursor,
        getCreatedAt: getObjectCreatedAt("evnt"),
        getId: (event) => event.eventId
      });
      if (resolveData === "none") {
        return {
          ...result2,
          data: result2.data.map((event) => {
            const { eventData: _eventData, ...rest } = event;
            return rest;
          })
        };
      }
      return result2;
    },
    async listByCorrelationId(params) {
      const correlationId = params.correlationId;
      const resolveData = params.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION$1;
      const result2 = await paginatedFileSystemQuery({
        directory: path__default.join(basedir, "events"),
        schema: EventSchema,
        // No filePrefix - search all events
        filter: (event) => event.correlationId === correlationId,
        // Events in chronological order (oldest first) by default,
        // different from the default for other list calls.
        sortOrder: params.pagination?.sortOrder ?? "asc",
        limit: params.pagination?.limit,
        cursor: params.pagination?.cursor,
        getCreatedAt: getObjectCreatedAt("evnt"),
        getId: (event) => event.eventId
      });
      if (resolveData === "none") {
        return {
          ...result2,
          data: result2.data.map((event) => {
            const { eventData: _eventData, ...rest } = event;
            return rest;
          })
        };
      }
      return result2;
    }
  };
}
function createRunsStorage(basedir) {
  return {
    async get(id, params) {
      const runPath = path__default.join(basedir, "runs", `${id}.json`);
      const run = await readJSON(runPath, WorkflowRunSchema);
      if (!run) {
        throw new WorkflowRunNotFoundError(id);
      }
      const resolveData = params?.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION$1;
      return filterRunData$1(run, resolveData);
    },
    async list(params) {
      const resolveData = params?.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION$1;
      const result2 = await paginatedFileSystemQuery({
        directory: path__default.join(basedir, "runs"),
        schema: WorkflowRunSchema,
        filter: (run) => {
          if (params?.workflowName && run.workflowName !== params.workflowName) {
            return false;
          }
          if (params?.status && run.status !== params.status) {
            return false;
          }
          return true;
        },
        sortOrder: params?.pagination?.sortOrder ?? "desc",
        limit: params?.pagination?.limit,
        cursor: params?.pagination?.cursor,
        getCreatedAt: getObjectCreatedAt("wrun"),
        getId: (run) => run.runId
      });
      if (resolveData === "none") {
        return {
          ...result2,
          data: result2.data.map((run) => ({
            ...run,
            input: [],
            output: void 0
          }))
        };
      }
      return result2;
    }
  };
}
function createStepsStorage(basedir) {
  return {
    async get(runId, stepId, params) {
      if (!runId) {
        const fileIds = await listJSONFiles(path__default.join(basedir, "steps"));
        const fileId = fileIds.find((fileId2) => fileId2.endsWith(`-${stepId}`));
        if (!fileId) {
          throw new Error(`Step ${stepId} not found`);
        }
        runId = fileId.split("-")[0];
      }
      const compositeKey = `${runId}-${stepId}`;
      const stepPath = path__default.join(basedir, "steps", `${compositeKey}.json`);
      const step = await readJSON(stepPath, StepSchema);
      if (!step) {
        throw new Error(`Step ${stepId} in run ${runId} not found`);
      }
      const resolveData = params?.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION$1;
      return filterStepData$1(step, resolveData);
    },
    async list(params) {
      const resolveData = params.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION$1;
      const result2 = await paginatedFileSystemQuery({
        directory: path__default.join(basedir, "steps"),
        schema: StepSchema,
        filePrefix: `${params.runId}-`,
        sortOrder: params.pagination?.sortOrder ?? "desc",
        limit: params.pagination?.limit,
        cursor: params.pagination?.cursor,
        getCreatedAt: getObjectCreatedAt("step"),
        getId: (step) => step.stepId
      });
      if (resolveData === "none") {
        return {
          ...result2,
          data: result2.data.map((step) => ({
            ...step,
            input: [],
            output: void 0
          }))
        };
      }
      return result2;
    }
  };
}
function createStorage$1(basedir) {
  return {
    runs: createRunsStorage(basedir),
    steps: createStepsStorage(basedir),
    events: createEventsStorage(basedir),
    hooks: createHooksStorage(basedir)
  };
}
const monotonicUlid = monotonicFactory(() => Math.random());
const RunStreamsSchema = object({
  streams: array(string())
});
function serializeChunk(chunk) {
  const eofByte = Buffer.from([chunk.eof ? 1 : 0]);
  return Buffer.concat([eofByte, chunk.chunk]);
}
function deserializeChunk(serialized) {
  const eof = serialized[0] === 1;
  const chunk = Buffer.from(serialized.subarray(1));
  return { eof, chunk };
}
function createStreamer$1(basedir) {
  const streamEmitter = new EventEmitter();
  const registeredStreams = /* @__PURE__ */ new Set();
  async function registerStreamForRun(runId, streamName) {
    const cacheKey = `${runId}:${streamName}`;
    if (registeredStreams.has(cacheKey)) {
      return;
    }
    const runStreamsPath = path__default.join(basedir, "streams", "runs", `${runId}.json`);
    const existing = await readJSON(runStreamsPath, RunStreamsSchema);
    const streams = existing?.streams ?? [];
    if (!streams.includes(streamName)) {
      streams.push(streamName);
      await writeJSON(runStreamsPath, { streams }, { overwrite: true });
    }
    registeredStreams.add(cacheKey);
  }
  return {
    async writeToStream(name, _runId, chunk) {
      const chunkId = `chnk_${monotonicUlid()}`;
      const runId = await _runId;
      await registerStreamForRun(runId, name);
      let chunkBuffer;
      if (typeof chunk === "string") {
        chunkBuffer = Buffer.from(new TextEncoder().encode(chunk));
      } else if (chunk instanceof Buffer) {
        chunkBuffer = chunk;
      } else {
        chunkBuffer = Buffer.from(chunk);
      }
      const serialized = serializeChunk({
        chunk: chunkBuffer,
        eof: false
      });
      const chunkPath = path__default.join(basedir, "streams", "chunks", `${name}-${chunkId}.json`);
      await write(chunkPath, serialized);
      const chunkData = Uint8Array.from(chunkBuffer);
      streamEmitter.emit(`chunk:${name}`, {
        streamName: name,
        chunkData,
        chunkId
      });
    },
    async closeStream(name, _runId) {
      const chunkId = `chnk_${monotonicUlid()}`;
      const runId = await _runId;
      await registerStreamForRun(runId, name);
      const chunkPath = path__default.join(basedir, "streams", "chunks", `${name}-${chunkId}.json`);
      await write(chunkPath, serializeChunk({ chunk: Buffer.from([]), eof: true }));
      streamEmitter.emit(`close:${name}`, { streamName: name });
    },
    async listStreamsByRunId(runId) {
      const runStreamsPath = path__default.join(basedir, "streams", "runs", `${runId}.json`);
      const data = await readJSON(runStreamsPath, RunStreamsSchema);
      return data?.streams ?? [];
    },
    async readFromStream(name, startIndex = 0) {
      const chunksDir = path__default.join(basedir, "streams", "chunks");
      let removeListeners = () => {
      };
      return new ReadableStream({
        async start(controller) {
          const deliveredChunkIds = /* @__PURE__ */ new Set();
          const bufferedEventChunks = [];
          let isReadingFromDisk = true;
          let pendingClose = false;
          const chunkListener = (event) => {
            deliveredChunkIds.add(event.chunkId);
            if (event.chunkData.byteLength === 0) {
              return;
            }
            if (isReadingFromDisk) {
              bufferedEventChunks.push({
                chunkId: event.chunkId,
                chunkData: Uint8Array.from(event.chunkData)
              });
            } else {
              controller.enqueue(Uint8Array.from(event.chunkData));
            }
          };
          const closeListener = () => {
            if (isReadingFromDisk) {
              pendingClose = true;
              return;
            }
            streamEmitter.off(`chunk:${name}`, chunkListener);
            streamEmitter.off(`close:${name}`, closeListener);
            try {
              controller.close();
            } catch {
            }
          };
          removeListeners = closeListener;
          streamEmitter.on(`chunk:${name}`, chunkListener);
          streamEmitter.on(`close:${name}`, closeListener);
          const files = await listJSONFiles(chunksDir);
          const chunkFiles = files.filter((file) => file.startsWith(`${name}-`)).sort();
          let isComplete = false;
          for (let i = startIndex; i < chunkFiles.length; i++) {
            const file = chunkFiles[i];
            const chunkId = file.substring(name.length + 1);
            if (deliveredChunkIds.has(chunkId)) {
              continue;
            }
            const chunk = deserializeChunk(await readBuffer(path__default.join(chunksDir, `${file}.json`)));
            if (chunk?.eof === true) {
              isComplete = true;
              break;
            }
            if (chunk.chunk.byteLength) {
              controller.enqueue(Uint8Array.from(chunk.chunk));
            }
          }
          isReadingFromDisk = false;
          bufferedEventChunks.sort((a, b) => a.chunkId.localeCompare(b.chunkId));
          for (const buffered of bufferedEventChunks) {
            controller.enqueue(Uint8Array.from(buffered.chunkData));
          }
          if (isComplete) {
            removeListeners();
            try {
              controller.close();
            } catch {
            }
            return;
          }
          if (pendingClose) {
            streamEmitter.off(`chunk:${name}`, chunkListener);
            streamEmitter.off(`close:${name}`, closeListener);
            try {
              controller.close();
            } catch {
            }
          }
        },
        cancel() {
          removeListeners();
        }
      });
    }
  };
}
function createLocalWorld(args) {
  const definedArgs = args ? Object.fromEntries(Object.entries(args).filter(([, value]) => value !== void 0)) : {};
  const mergedConfig = { ...config.value, ...definedArgs };
  return {
    ...createQueue$1(mergedConfig),
    ...createStorage$1(mergedConfig.dataDir),
    ...createStreamer$1(mergedConfig.dataDir),
    async start() {
      await initDataDir(mergedConfig.dataDir);
    }
  };
}
const version$1 = "4.0.1-beta.28";
const DEFAULT_RESOLVE_DATA_OPTION = "all";
function deserializeError(obj) {
  const { error, ...rest } = obj;
  if (!error) {
    return obj;
  }
  if (typeof error === "object" && error !== null) {
    const result2 = StructuredErrorSchema.safeParse(error);
    if (result2.success) {
      return {
        ...rest,
        error: {
          message: result2.data.message,
          stack: result2.data.stack,
          code: result2.data.code
        }
      };
    }
  }
  if (typeof error === "string") {
    try {
      const parsed = StructuredErrorSchema.parse(JSON.parse(error));
      return {
        ...rest,
        error: {
          message: parsed.message,
          stack: parsed.stack,
          code: parsed.code
        }
      };
    } catch {
      return {
        ...rest,
        error: {
          message: error
        }
      };
    }
  }
  return obj;
}
const getUserAgent = () => {
  return `@workflow/world-vercel/${version$1} node-${process.version} ${os__default.platform()} (${os__default.arch()})`;
};
const getHttpUrl = (config2) => {
  const projectConfig = config2?.projectConfig;
  const defaultHost = "https://vercel-workflow.com";
  const defaultProxyUrl = "https://api.vercel.com/v1/workflow";
  const usingProxy = Boolean(projectConfig?.projectId && projectConfig?.teamId);
  const baseUrl = usingProxy ? defaultProxyUrl : `${defaultHost}/api`;
  return { baseUrl, usingProxy };
};
const getHeaders = (config2, options) => {
  const projectConfig = config2?.projectConfig;
  const headers = new Headers(config2?.headers);
  headers.set("User-Agent", getUserAgent());
  if (projectConfig) {
    headers.set("x-vercel-environment", projectConfig.environment || "production");
    if (projectConfig.projectId) {
      headers.set("x-vercel-project-id", projectConfig.projectId);
    }
    if (projectConfig.teamId) {
      headers.set("x-vercel-team-id", projectConfig.teamId);
    }
  }
  return headers;
};
async function getHttpConfig(config2) {
  const { baseUrl, usingProxy } = getHttpUrl(config2);
  const headers = getHeaders(config2);
  const token = config2?.token ?? await distExports.getVercelOidcToken();
  if (token) {
    headers.set("Authorization", `Bearer ${token}`);
  }
  return { baseUrl, headers, usingProxy };
}
async function makeRequest({ endpoint, options = {}, config: config2 = {}, schema, data }) {
  const { baseUrl, headers } = await getHttpConfig(config2);
  headers.set("Accept", "application/cbor");
  headers.set("X-Request-Time", Date.now().toString());
  let body;
  if (data !== void 0) {
    headers.set("Content-Type", "application/cbor");
    body = encode(data);
  }
  const url = `${baseUrl}${endpoint}`;
  const request = new Request(url, {
    ...options,
    body,
    headers
  });
  const response = await fetch(request);
  if (!response.ok) {
    const errorData = await parseResponseBody(response).then((r) => r.data).catch(() => ({}));
    if (process.env.DEBUG === "1") {
      const stringifiedHeaders = Array.from(headers.entries()).map(([key2, value]) => `-H "${key2}: ${value}"`).join(" ");
      console.error(`Failed to fetch, reproduce with:
curl -X ${request.method} ${stringifiedHeaders} "${url}"`);
    }
    throw new WorkflowAPIError(errorData.message || `${request.method} ${endpoint} -> HTTP ${response.status}: ${response.statusText}`, { url, status: response.status, code: errorData.code });
  }
  let parseResult;
  try {
    parseResult = await parseResponseBody(response);
  } catch (error) {
    const contentType = response.headers.get("Content-Type") || "unknown";
    throw new WorkflowAPIError(`Failed to parse response body for ${request.method} ${endpoint} (Content-Type: ${contentType}):

${error}`, { url, cause: error });
  }
  const result2 = schema.safeParse(parseResult.data);
  if (!result2.success) {
    throw new WorkflowAPIError(`Schema validation failed for ${request.method} ${endpoint}:

${result2.error}

Response context: ${parseResult.getDebugContext()}`, { url, cause: result2.error });
  }
  return result2.data;
}
const MAX_PREVIEW_LENGTH = 500;
function createPreview(data) {
  const str = inspect(data, { depth: 3, maxArrayLength: 10, breakLength: 120 });
  return str.length > MAX_PREVIEW_LENGTH ? `${str.slice(0, MAX_PREVIEW_LENGTH)}...` : str;
}
async function parseResponseBody(response) {
  const contentType = response.headers.get("Content-Type") || "";
  if (contentType.includes("application/cbor")) {
    const buffer = await response.arrayBuffer();
    const data2 = decode(new Uint8Array(buffer));
    return {
      data: data2,
      getDebugContext: () => `Content-Type: ${contentType}, ${buffer.byteLength} bytes (CBOR), preview: ${createPreview(data2)}`
    };
  }
  const text = await response.text();
  const data = JSON.parse(text);
  return {
    data,
    getDebugContext: () => `Content-Type: ${contentType}, ${text.length} bytes, preview: ${createPreview(data)}`
  };
}
const MessageWrapper = object({
  payload: QueuePayloadSchema,
  queueName: ValidQueueName,
  /**
   * The deployment ID to use when re-enqueueing the message.
   * This ensures the message is processed by the same deployment.
   */
  deploymentId: string().optional()
});
const MAX_DELAY_SECONDS = Number(
  process.env.VERCEL_QUEUE_MAX_DELAY_SECONDS || 82800
  // 23 hours - leave 1h buffer before 24h retention limit
);
function createQueue(config2) {
  const { baseUrl, usingProxy } = getHttpUrl(config2);
  const headers = getHeaders(config2);
  const baseClientOptions = {
    baseUrl: usingProxy ? baseUrl : void 0,
    // The proxy will strip `/queues` from the path, and add `/api` in front,
    // so this ends up being `/api/v3/topic` when arriving at the queue server,
    // which is the same as the default basePath in VQS client.
    basePath: usingProxy ? "/queues/v3/topic" : void 0,
    token: usingProxy ? config2?.token : void 0,
    headers: Object.fromEntries(headers.entries())
  };
  const queue = async (queueName, payload, opts) => {
    const deploymentId = opts?.deploymentId ?? process.env.VERCEL_DEPLOYMENT_ID;
    if (!deploymentId) {
      throw new Error("No deploymentId provided and VERCEL_DEPLOYMENT_ID environment variable is not set. Queue messages require a deployment ID to route correctly. Either set VERCEL_DEPLOYMENT_ID or provide deploymentId in options.");
    }
    const sendMessageClient = new Client({
      ...baseClientOptions,
      deploymentId
    });
    const hasEncoder = typeof MessageWrapper.encode === "function";
    if (!hasEncoder) {
      console.warn("Using zod v3 compatibility mode for queue() calls - this may not work as expected");
    }
    const encoder = hasEncoder ? MessageWrapper.encode : (data) => data;
    const encoded = encoder({
      payload,
      queueName,
      // Store deploymentId in the message so it can be preserved when re-enqueueing
      deploymentId: opts?.deploymentId
    });
    const sanitizedQueueName = queueName.replace(/[^A-Za-z0-9-_]/g, "-");
    try {
      const { messageId } = await sendMessageClient.send(sanitizedQueueName, encoded, {
        idempotencyKey: opts?.idempotencyKey,
        delaySeconds: opts?.delaySeconds
      });
      return { messageId: MessageId.parse(messageId) };
    } catch (error) {
      if (error instanceof DuplicateMessageError) {
        return {
          messageId: MessageId.parse(`msg_duplicate_${error.idempotencyKey ?? opts?.idempotencyKey ?? "unknown"}`)
        };
      }
      throw error;
    }
  };
  const handleCallbackClient = new Client({
    ...baseClientOptions
  });
  const createQueueHandler = (prefix, handler2) => {
    return handleCallbackClient.handleCallback({
      [`${prefix}*`]: {
        default: async (body, meta) => {
          const { payload, queueName, deploymentId } = MessageWrapper.parse(body);
          const result2 = await handler2(payload, {
            queueName,
            messageId: MessageId.parse(meta.messageId),
            attempt: meta.deliveryCount
          });
          if (typeof result2?.timeoutSeconds === "number") {
            const delaySeconds = Math.min(result2.timeoutSeconds, MAX_DELAY_SECONDS);
            await queue(queueName, payload, {
              deploymentId,
              delaySeconds
            });
            return void 0;
          }
          return void 0;
        }
      }
    });
  };
  const getDeploymentId = async () => {
    const deploymentId = process.env.VERCEL_DEPLOYMENT_ID;
    if (!deploymentId) {
      throw new Error("VERCEL_DEPLOYMENT_ID environment variable is not set");
    }
    return deploymentId;
  };
  return { queue, createQueueHandler, getDeploymentId };
}
const StepWireSchema = StepSchema.omit({
  error: true
}).extend({
  // Backend returns error either as:
  // - A JSON string (legacy/lazy mode)
  // - An object {message, stack} (when errorRef is resolved)
  // This will be deserialized and mapped to error
  error: union([
    string(),
    object({
      message: string(),
      stack: string().optional(),
      code: string().optional()
    })
  ]).optional(),
  errorRef: any().optional()
});
const StepWireWithRefsSchema = StepWireSchema.omit({
  input: true,
  output: true
}).extend({
  // We discard the results of the refs, so we don't care about the type here
  inputRef: any().optional(),
  outputRef: any().optional(),
  input: array(any()).optional(),
  output: any().optional()
});
function deserializeStep(wireStep) {
  const { error, errorRef, ...rest } = wireStep;
  const result2 = {
    ...rest
  };
  const errorSource = error ?? errorRef;
  if (errorSource) {
    if (typeof errorSource === "string") {
      try {
        const parsed = JSON.parse(errorSource);
        if (typeof parsed === "object" && parsed.message !== void 0) {
          result2.error = {
            message: parsed.message,
            stack: parsed.stack,
            code: parsed.code
          };
        } else {
          result2.error = { message: String(parsed) };
        }
      } catch {
        result2.error = { message: errorSource };
      }
    } else if (typeof errorSource === "object" && errorSource !== null) {
      result2.error = {
        message: errorSource.message ?? "Unknown error",
        stack: errorSource.stack,
        code: errorSource.code
      };
    }
  }
  return result2;
}
function filterStepData(step, resolveData) {
  if (resolveData === "none") {
    const { inputRef: _inputRef, outputRef: _outputRef, ...rest } = step;
    const deserialized = deserializeStep(rest);
    return {
      ...deserialized,
      input: [],
      output: void 0
    };
  }
  return deserializeStep(step);
}
async function listWorkflowRunSteps(params, config2) {
  const { runId, pagination, resolveData = DEFAULT_RESOLVE_DATA_OPTION } = params;
  const searchParams = new URLSearchParams();
  if (pagination?.cursor)
    searchParams.set("cursor", pagination.cursor);
  if (pagination?.limit)
    searchParams.set("limit", pagination.limit.toString());
  if (pagination?.sortOrder)
    searchParams.set("sortOrder", pagination.sortOrder);
  const remoteRefBehavior = resolveData === "none" ? "lazy" : "resolve";
  searchParams.set("remoteRefBehavior", remoteRefBehavior);
  const queryString = searchParams.toString();
  const endpoint = `/v2/runs/${runId}/steps${queryString ? `?${queryString}` : ""}`;
  const response = await makeRequest({
    endpoint,
    options: { method: "GET" },
    config: config2,
    schema: PaginatedResponseSchema(remoteRefBehavior === "lazy" ? StepWireWithRefsSchema : StepWireSchema)
  });
  return {
    ...response,
    data: response.data.map((step) => filterStepData(step, resolveData))
  };
}
async function getStep(runId, stepId, params, config2) {
  const resolveData = params?.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION;
  const remoteRefBehavior = resolveData === "none" ? "lazy" : "resolve";
  const searchParams = new URLSearchParams();
  searchParams.set("remoteRefBehavior", remoteRefBehavior);
  const queryString = searchParams.toString();
  const endpoint = runId ? `/v2/runs/${runId}/steps/${stepId}${queryString ? `?${queryString}` : ""}` : `/v2/steps/${stepId}${queryString ? `?${queryString}` : ""}`;
  const step = await makeRequest({
    endpoint,
    options: { method: "GET" },
    config: config2,
    schema: remoteRefBehavior === "lazy" ? StepWireWithRefsSchema : StepWireSchema
  });
  return filterStepData(step, resolveData);
}
function filterEventData(event, resolveData) {
  if (resolveData === "none") {
    const { eventData: _eventData, ...rest } = event;
    return rest;
  }
  return event;
}
const EventResultWireSchema = z.object({
  event: EventSchema,
  run: WorkflowRunSchema.optional(),
  step: StepWireSchema.optional(),
  hook: HookSchema.optional()
});
const EventWithRefsSchema = z.object({
  eventId: z.string(),
  runId: z.string(),
  eventType: EventTypeSchema,
  correlationId: z.string().optional(),
  eventDataRef: z.any().optional(),
  createdAt: z.coerce.date(),
  specVersion: z.number().default(1)
});
async function getWorkflowRunEvents(params, config2) {
  const searchParams = new URLSearchParams();
  const { pagination, resolveData = DEFAULT_RESOLVE_DATA_OPTION } = params;
  let runId;
  let correlationId;
  if ("runId" in params) {
    runId = params.runId;
  } else {
    correlationId = params.correlationId;
  }
  if (!runId && !correlationId) {
    throw new Error("Either runId or correlationId must be provided");
  }
  if (pagination?.limit)
    searchParams.set("limit", pagination.limit.toString());
  if (pagination?.cursor)
    searchParams.set("cursor", pagination.cursor);
  if (pagination?.sortOrder)
    searchParams.set("sortOrder", pagination.sortOrder);
  if (correlationId)
    searchParams.set("correlationId", correlationId);
  const remoteRefBehavior = resolveData === "none" ? "lazy" : "resolve";
  searchParams.set("remoteRefBehavior", remoteRefBehavior);
  const queryString = searchParams.toString();
  const query = queryString ? `?${queryString}` : "";
  const endpoint = correlationId ? `/v2/events${query}` : `/v2/runs/${runId}/events${query}`;
  const response = await makeRequest({
    endpoint,
    options: { method: "GET" },
    config: config2,
    schema: PaginatedResponseSchema(remoteRefBehavior === "lazy" ? EventWithRefsSchema : EventSchema)
  });
  return {
    ...response,
    data: response.data.map((event) => filterEventData(event, resolveData))
  };
}
async function createWorkflowRunEvent(id, data, params, config2) {
  const resolveData = params?.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION;
  const runIdPath = id === null ? "null" : id;
  const wireResult = await makeRequest({
    endpoint: `/v2/runs/${runIdPath}/events`,
    options: { method: "POST" },
    data,
    config: config2,
    schema: EventResultWireSchema
  });
  return {
    event: filterEventData(wireResult.event, resolveData),
    run: wireResult.run,
    step: wireResult.step ? deserializeStep(wireResult.step) : void 0,
    hook: wireResult.hook
  };
}
function filterHookData(hook, resolveData) {
  if (resolveData === "none") {
    const { metadataRef: _metadataRef, ...rest } = hook;
    return rest;
  }
  return hook;
}
const HookWithRefsSchema = HookSchema.omit({
  metadata: true
}).extend({
  metadataRef: z.any().optional()
});
async function listHooks(params, config2) {
  const { runId, pagination, resolveData = DEFAULT_RESOLVE_DATA_OPTION } = params;
  const searchParams = new URLSearchParams();
  if (pagination?.limit)
    searchParams.set("limit", pagination.limit.toString());
  if (pagination?.cursor)
    searchParams.set("cursor", pagination.cursor);
  if (pagination?.sortOrder)
    searchParams.set("sortOrder", pagination.sortOrder);
  const remoteRefBehavior = resolveData === "none" ? "lazy" : "resolve";
  searchParams.set("remoteRefBehavior", remoteRefBehavior);
  if (runId)
    searchParams.set("runId", runId);
  const queryString = searchParams.toString();
  const endpoint = `/v2/hooks${queryString ? `?${queryString}` : ""}`;
  const response = await makeRequest({
    endpoint,
    options: { method: "GET" },
    config: config2,
    schema: PaginatedResponseSchema(remoteRefBehavior === "lazy" ? HookWithRefsSchema : HookSchema)
  });
  return {
    ...response,
    data: response.data.map((hook) => filterHookData(hook, resolveData))
  };
}
async function getHook(hookId, params, config2) {
  const resolveData = params?.resolveData || "all";
  const endpoint = `/v2/hooks/${hookId}`;
  const hook = await makeRequest({
    endpoint,
    options: { method: "GET" },
    config: config2,
    schema: HookSchema
  });
  return filterHookData(hook, resolveData);
}
async function getHookByToken$1(token, config2) {
  return makeRequest({
    endpoint: `/v2/hooks/by-token?token=${encodeURIComponent(token)}`,
    options: {
      method: "GET"
    },
    config: config2,
    schema: HookSchema
  });
}
const WorkflowRunWireBaseSchema = WorkflowRunBaseSchema.omit({
  error: true
}).extend({
  // Backend returns error as either a JSON string or structured object
  error: union([string(), StructuredErrorSchema]).optional()
});
const WorkflowRunWireSchema = WorkflowRunWireBaseSchema;
const WorkflowRunWireWithRefsSchema = WorkflowRunWireBaseSchema.omit({
  input: true,
  output: true
}).extend({
  // We discard the results of the refs, so we don't care about the type here
  inputRef: any().optional(),
  outputRef: any().optional(),
  input: array(any()).optional(),
  output: any().optional(),
  blobStorageBytes: number().optional(),
  streamStorageBytes: number().optional()
});
function filterRunData(run, resolveData) {
  if (resolveData === "none") {
    const { inputRef: _inputRef, outputRef: _outputRef, ...rest } = run;
    const deserialized = deserializeError(rest);
    return {
      ...deserialized,
      input: [],
      output: void 0
    };
  }
  return deserializeError(run);
}
async function listWorkflowRuns(params = {}, config2) {
  const { workflowName, status, pagination, resolveData = DEFAULT_RESOLVE_DATA_OPTION } = params;
  const searchParams = new URLSearchParams();
  if (workflowName)
    searchParams.set("workflowName", workflowName);
  if (status)
    searchParams.set("status", status);
  if (pagination?.limit)
    searchParams.set("limit", pagination.limit.toString());
  if (pagination?.cursor)
    searchParams.set("cursor", pagination.cursor);
  if (pagination?.sortOrder)
    searchParams.set("sortOrder", pagination.sortOrder);
  const remoteRefBehavior = resolveData === "none" ? "lazy" : "resolve";
  searchParams.set("remoteRefBehavior", remoteRefBehavior);
  const queryString = searchParams.toString();
  const endpoint = `/v2/runs${queryString ? `?${queryString}` : ""}`;
  const response = await makeRequest({
    endpoint,
    options: { method: "GET" },
    config: config2,
    schema: PaginatedResponseSchema(remoteRefBehavior === "lazy" ? WorkflowRunWireWithRefsSchema : WorkflowRunWireSchema)
  });
  return {
    ...response,
    data: response.data.map((run) => filterRunData(run, resolveData))
  };
}
async function getWorkflowRun(id, params, config2) {
  const resolveData = params?.resolveData ?? DEFAULT_RESOLVE_DATA_OPTION;
  const remoteRefBehavior = resolveData === "none" ? "lazy" : "resolve";
  const searchParams = new URLSearchParams();
  searchParams.set("remoteRefBehavior", remoteRefBehavior);
  const queryString = searchParams.toString();
  const endpoint = `/v2/runs/${id}${queryString ? `?${queryString}` : ""}`;
  try {
    const run = await makeRequest({
      endpoint,
      options: { method: "GET" },
      config: config2,
      schema: remoteRefBehavior === "lazy" ? WorkflowRunWireWithRefsSchema : WorkflowRunWireSchema
    });
    return filterRunData(run, resolveData);
  } catch (error) {
    if (error instanceof WorkflowAPIError && error.status === 404) {
      throw new WorkflowRunNotFoundError(id);
    }
    throw error;
  }
}
function createStorage(config2) {
  return {
    // Storage interface with namespaced methods
    runs: {
      get: (id, params) => getWorkflowRun(id, params, config2),
      list: (params) => listWorkflowRuns(params, config2)
    },
    steps: {
      get: (runId, stepId, params) => getStep(runId, stepId, params, config2),
      list: (params) => listWorkflowRunSteps(params, config2)
    },
    events: {
      create: (runId, data, params) => createWorkflowRunEvent(runId, data, params, config2),
      list: (params) => getWorkflowRunEvents(params, config2),
      listByCorrelationId: (params) => getWorkflowRunEvents(params, config2)
    },
    hooks: {
      get: (hookId, params) => getHook(hookId, params, config2),
      getByToken: (token) => getHookByToken$1(token, config2),
      list: (params) => listHooks(params, config2)
    }
  };
}
function getStreamUrl(name, runId, httpConfig) {
  if (runId) {
    return new URL(`${httpConfig.baseUrl}/v2/runs/${runId}/stream/${encodeURIComponent(name)}`);
  }
  return new URL(`${httpConfig.baseUrl}/v2/stream/${encodeURIComponent(name)}`);
}
function createStreamer(config2) {
  return {
    async writeToStream(name, runId, chunk) {
      const resolvedRunId = await runId;
      const httpConfig = await getHttpConfig(config2);
      await fetch(getStreamUrl(name, resolvedRunId, httpConfig), {
        method: "PUT",
        body: chunk,
        headers: httpConfig.headers,
        duplex: "half"
      });
    },
    async closeStream(name, runId) {
      const resolvedRunId = await runId;
      const httpConfig = await getHttpConfig(config2);
      httpConfig.headers.set("X-Stream-Done", "true");
      await fetch(getStreamUrl(name, resolvedRunId, httpConfig), {
        method: "PUT",
        headers: httpConfig.headers
      });
    },
    async readFromStream(name, startIndex) {
      const httpConfig = await getHttpConfig(config2);
      const url = getStreamUrl(name, void 0, httpConfig);
      if (typeof startIndex === "number") {
        url.searchParams.set("startIndex", String(startIndex));
      }
      const res = await fetch(url, { headers: httpConfig.headers });
      if (!res.ok)
        throw new Error(`Failed to fetch stream: ${res.status}`);
      return res.body;
    },
    async listStreamsByRunId(runId) {
      const httpConfig = await getHttpConfig(config2);
      const url = new URL(`${httpConfig.baseUrl}/v2/runs/${runId}/streams`);
      const res = await fetch(url, { headers: httpConfig.headers });
      if (!res.ok)
        throw new Error(`Failed to list streams: ${res.status}`);
      return await res.json();
    }
  };
}
function createVercelWorld(config2) {
  return {
    ...createQueue(config2),
    ...createStorage(config2),
    ...createStreamer(config2)
  };
}
const require$1 = createRequire(join$1(process.cwd(), "index.js"));
const WorldCache = Symbol.for("@workflow/world//cache");
const StubbedWorldCache = Symbol.for("@workflow/world//stubbedCache");
const globalSymbols = globalThis;
function defaultWorld() {
  if (process.env.VERCEL_DEPLOYMENT_ID) {
    return "vercel";
  }
  return "local";
}
const createWorld = () => {
  const targetWorld = process.env.WORKFLOW_TARGET_WORLD || defaultWorld();
  if (targetWorld === "vercel") {
    return createVercelWorld({
      token: process.env.WORKFLOW_VERCEL_AUTH_TOKEN,
      projectConfig: {
        environment: process.env.WORKFLOW_VERCEL_ENV,
        projectId: process.env.WORKFLOW_VERCEL_PROJECT,
        teamId: process.env.WORKFLOW_VERCEL_TEAM
      }
    });
  }
  if (targetWorld === "local") {
    return createLocalWorld({
      dataDir: process.env.WORKFLOW_LOCAL_DATA_DIR
    });
  }
  const mod = require$1(targetWorld);
  if (typeof mod === "function") {
    return mod();
  } else if (typeof mod.default === "function") {
    return mod.default();
  } else if (typeof mod.createWorld === "function") {
    return mod.createWorld();
  }
  throw new Error(`Invalid target world module: ${targetWorld}, must export a default function or createWorld function that returns a World instance.`);
};
const getWorldHandlers = () => {
  if (globalSymbols[StubbedWorldCache]) {
    return globalSymbols[StubbedWorldCache];
  }
  const _world = createWorld();
  globalSymbols[StubbedWorldCache] = _world;
  return {
    createQueueHandler: _world.createQueueHandler
  };
};
const getWorld = () => {
  if (globalSymbols[WorldCache]) {
    return globalSymbols[WorldCache];
  }
  globalSymbols[WorldCache] = createWorld();
  return globalSymbols[WorldCache];
};
const generateId = monotonicFactory();
function getHealthCheckStreamName(correlationId) {
  return `__health_check__${correlationId}`;
}
function parseHealthCheckPayload(message) {
  const result2 = HealthCheckPayloadSchema.safeParse(message);
  if (result2.success) {
    return result2.data;
  }
  return void 0;
}
function generateHealthCheckRunId() {
  return `wrun_${generateId()}`;
}
async function handleHealthCheckMessage(healthCheck, endpoint) {
  const world = getWorld();
  const streamName = getHealthCheckStreamName(healthCheck.correlationId);
  const response = JSON.stringify({
    healthy: true,
    endpoint,
    correlationId: healthCheck.correlationId,
    timestamp: Date.now()
  });
  const fakeRunId = generateHealthCheckRunId();
  await world.writeToStream(streamName, fakeRunId, response);
  await world.closeStream(streamName, fakeRunId);
}
async function getAllWorkflowRunEvents(runId) {
  const allEvents = [];
  let cursor = null;
  let hasMore = true;
  const world = getWorld();
  while (hasMore) {
    const response = await world.events.list({
      runId,
      pagination: {
        sortOrder: "asc",
        // Required: events must be in chronological order for replay
        cursor: cursor ?? void 0
      }
    });
    allEvents.push(...response.data);
    hasMore = response.hasMore;
    cursor = response.cursor;
  }
  return allEvents;
}
const HEALTH_CHECK_CORS_HEADERS = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Methods": "POST, OPTIONS, GET, HEAD",
  "Access-Control-Allow-Headers": "Content-Type"
};
function withHealthCheck(handler2) {
  return async (req) => {
    const url = new URL(req.url);
    const isHealthCheck = url.searchParams.has("__health");
    if (isHealthCheck) {
      if (req.method === "OPTIONS") {
        return new Response(null, {
          status: 204,
          headers: HEALTH_CHECK_CORS_HEADERS
        });
      }
      return new Response(`Workflow DevKit "${url.pathname}" endpoint is healthy`, {
        status: 200,
        headers: {
          "Content-Type": "text/plain",
          ...HEALTH_CHECK_CORS_HEADERS
        }
      });
    }
    return await handler2(req);
  };
}
async function queueMessage(world, ...args) {
  const queueName = args[0];
  await trace("queueMessage", {
    attributes: QueueName(queueName),
    kind: await getSpanKind("PRODUCER")
  }, async (span) => {
    const { messageId } = await world.queue(...args);
    span?.setAttributes(QueueMessageId(messageId));
  });
}
function getQueueOverhead(message) {
  if (!message.requestedAt)
    return;
  try {
    return QueueOverheadMs(Date.now() - message.requestedAt.getTime());
  } catch {
    return;
  }
}
const WORKFLOW_SERIALIZE$2 = Symbol.for("workflow-serialize");
const WORKFLOW_DESERIALIZE$2 = Symbol.for("workflow-deserialize");
const WORKFLOW_USE_STEP = Symbol.for("WORKFLOW_USE_STEP");
const WORKFLOW_CREATE_HOOK = Symbol.for("WORKFLOW_CREATE_HOOK");
const WORKFLOW_SLEEP = Symbol.for("WORKFLOW_SLEEP");
const WORKFLOW_GET_STREAM_ID = Symbol.for("WORKFLOW_GET_STREAM_ID");
const STABLE_ULID = Symbol.for("WORKFLOW_STABLE_ULID");
const STREAM_NAME_SYMBOL = Symbol.for("WORKFLOW_STREAM_NAME");
const STREAM_TYPE_SYMBOL = Symbol.for("WORKFLOW_STREAM_TYPE");
const BODY_INIT_SYMBOL = Symbol.for("BODY_INIT");
const WEBHOOK_RESPONSE_WRITABLE = Symbol.for("WEBHOOK_RESPONSE_WRITABLE");
const WORKFLOW_CLASS_REGISTRY = Symbol.for("workflow-class-registry");
function getRegistry$1(global = globalThis) {
  const g = global;
  let registry = g[WORKFLOW_CLASS_REGISTRY];
  if (!registry) {
    registry = /* @__PURE__ */ new Map();
    g[WORKFLOW_CLASS_REGISTRY] = registry;
  }
  return registry;
}
function getSerializationClass(classId, global) {
  return getRegistry$1(global).get(classId);
}
const LOCK_POLL_INTERVAL_MS = 100;
function createFlushableState() {
  return {
    ...withResolvers(),
    pendingOps: 0,
    doneResolved: false,
    streamEnded: false
  };
}
function isWritableUnlockedNotClosed(writable) {
  if (writable.locked)
    return false;
  let writer;
  try {
    writer = writable.getWriter();
  } catch {
    return false;
  }
  try {
    writer.releaseLock();
  } catch {
    return false;
  }
  return true;
}
function isReadableUnlockedNotClosed(readable) {
  if (readable.locked)
    return false;
  let reader;
  try {
    reader = readable.getReader();
  } catch {
    return false;
  }
  try {
    reader.releaseLock();
  } catch {
    return false;
  }
  return true;
}
function pollWritableLock(writable, state) {
  if (state.writablePollingInterval !== void 0) {
    return;
  }
  const intervalId = setInterval(() => {
    if (state.doneResolved || state.streamEnded) {
      clearInterval(intervalId);
      state.writablePollingInterval = void 0;
      return;
    }
    if (isWritableUnlockedNotClosed(writable) && state.pendingOps === 0) {
      state.doneResolved = true;
      state.resolve();
      clearInterval(intervalId);
      state.writablePollingInterval = void 0;
    }
  }, LOCK_POLL_INTERVAL_MS);
  state.writablePollingInterval = intervalId;
}
function pollReadableLock(readable, state) {
  if (state.readablePollingInterval !== void 0) {
    return;
  }
  const intervalId = setInterval(() => {
    if (state.doneResolved || state.streamEnded) {
      clearInterval(intervalId);
      state.readablePollingInterval = void 0;
      return;
    }
    if (isReadableUnlockedNotClosed(readable) && state.pendingOps === 0) {
      state.doneResolved = true;
      state.resolve();
      clearInterval(intervalId);
      state.readablePollingInterval = void 0;
    }
  }, LOCK_POLL_INTERVAL_MS);
  state.readablePollingInterval = intervalId;
}
async function flushablePipe(source, sink, state) {
  const reader = source.getReader();
  const writer = sink.getWriter();
  try {
    while (true) {
      if (state.streamEnded) {
        return;
      }
      const readResult = await reader.read();
      if (state.streamEnded) {
        return;
      }
      if (readResult.done) {
        state.streamEnded = true;
        await writer.close();
        if (!state.doneResolved) {
          state.doneResolved = true;
          state.resolve();
        }
        return;
      }
      state.pendingOps++;
      try {
        await writer.write(readResult.value);
      } finally {
        state.pendingOps--;
      }
    }
  } catch (err) {
    state.streamEnded = true;
    if (!state.doneResolved) {
      state.doneResolved = true;
      state.reject(err);
    }
    throw err;
  } finally {
    reader.releaseLock();
    writer.releaseLock();
  }
}
const contextStorage = /* @__PURE__ */ new AsyncLocalStorage();
const registeredSteps$1 = /* @__PURE__ */ new Map();
function getStepFunction(stepId) {
  return registeredSteps$1.get(stepId);
}
const defaultUlid = monotonicFactory();
function formatSerializationError(context, error) {
  const verb = context.includes("return value") ? "returning" : "passing";
  let message = `Failed to serialize ${context}`;
  if (error instanceof DevalueError && error.path) {
    message += ` at path "${error.path}"`;
  }
  message += `. Ensure you're ${verb} serializable types (plain objects, arrays, primitives, Date, RegExp, Map, Set).`;
  if (error instanceof DevalueError && error.value !== void 0) {
    console.error(`[Workflows] Serialization failed for ${context}. Problematic value:`);
    console.error(error.value);
  }
  return message;
}
function getStreamType(stream) {
  try {
    const reader = stream.getReader({ mode: "byob" });
    reader.releaseLock();
    return "bytes";
  } catch {
  }
}
function getSerializeStream(reducers) {
  const encoder = new TextEncoder();
  const stream = new TransformStream({
    transform(chunk, controller) {
      try {
        const serialized = stringify(chunk, reducers);
        controller.enqueue(encoder.encode(`${serialized}
`));
      } catch (error) {
        controller.error(new WorkflowRuntimeError(formatSerializationError("stream chunk", error), { slug: "serialization-failed", cause: error }));
      }
    }
  });
  return stream;
}
function getDeserializeStream(revivers) {
  const decoder = new TextDecoder();
  let buffer = "";
  const stream = new TransformStream({
    transform(chunk, controller) {
      buffer += decoder.decode(chunk, { stream: true });
      while (true) {
        const newlineIndex = buffer.indexOf("\n");
        if (newlineIndex === -1)
          break;
        const line = buffer.slice(0, newlineIndex);
        buffer = buffer.slice(newlineIndex + 1);
        if (line.length > 0) {
          const obj = parse$1(line, revivers);
          controller.enqueue(obj);
        }
      }
    },
    flush(controller) {
      if (buffer && buffer.length > 0) {
        const obj = parse$1(buffer, revivers);
        controller.enqueue(obj);
      }
    }
  });
  return stream;
}
class WorkflowServerReadableStream extends ReadableStream {
  #reader;
  constructor(name, startIndex) {
    if (typeof name !== "string" || name.length === 0) {
      throw new Error(`"name" is required, got "${name}"`);
    }
    super({
      // @ts-expect-error Not sure why TypeScript is complaining about this
      type: "bytes",
      pull: async (controller) => {
        let reader = this.#reader;
        if (!reader) {
          const world = getWorld();
          const stream = await world.readFromStream(name, startIndex);
          reader = this.#reader = stream.getReader();
        }
        if (!reader) {
          controller.error(new Error("Failed to get reader"));
          return;
        }
        const result2 = await reader.read();
        if (result2.done) {
          this.#reader = void 0;
          controller.close();
        } else {
          controller.enqueue(result2.value);
        }
      }
    });
  }
}
class WorkflowServerWritableStream extends WritableStream {
  constructor(name, runId) {
    if (typeof runId !== "string" && !(runId instanceof Promise)) {
      throw new Error(`"runId" must be a string or a promise that resolves to a string, got "${typeof runId}"`);
    }
    if (typeof name !== "string" || name.length === 0) {
      throw new Error(`"name" is required, got "${name}"`);
    }
    const world = getWorld();
    super({
      async write(chunk) {
        const _runId = await runId;
        await world.writeToStream(name, _runId, chunk);
      },
      async close() {
        const _runId = await runId;
        await world.closeStream(name, _runId);
      }
    });
  }
}
function revive(str) {
  return (0, eval)(`(${str})`);
}
function getCommonReducers(global = globalThis) {
  const abToBase64 = (value, offset, length) => {
    if (length === 0)
      return ".";
    const uint8 = new Uint8Array(value, offset, length);
    return Buffer.from(uint8).toString("base64");
  };
  const viewToBase64 = (value) => abToBase64(value.buffer, value.byteOffset, value.byteLength);
  return {
    ArrayBuffer: (value) => value instanceof global.ArrayBuffer && abToBase64(value, 0, value.byteLength),
    BigInt: (value) => typeof value === "bigint" && value.toString(),
    BigInt64Array: (value) => value instanceof global.BigInt64Array && viewToBase64(value),
    BigUint64Array: (value) => value instanceof global.BigUint64Array && viewToBase64(value),
    Date: (value) => {
      if (!(value instanceof global.Date))
        return false;
      const valid = !Number.isNaN(value.getDate());
      return valid ? value.toISOString() : ".";
    },
    Error: (value) => {
      if (!(value instanceof global.Error))
        return false;
      return {
        name: value.name,
        message: value.message,
        stack: value.stack
      };
    },
    Float32Array: (value) => value instanceof global.Float32Array && viewToBase64(value),
    Float64Array: (value) => value instanceof global.Float64Array && viewToBase64(value),
    Headers: (value) => value instanceof global.Headers && Array.from(value),
    Int8Array: (value) => value instanceof global.Int8Array && viewToBase64(value),
    Int16Array: (value) => value instanceof global.Int16Array && viewToBase64(value),
    Int32Array: (value) => value instanceof global.Int32Array && viewToBase64(value),
    Map: (value) => value instanceof global.Map && Array.from(value),
    RegExp: (value) => value instanceof global.RegExp && {
      source: value.source,
      flags: value.flags
    },
    Request: (value) => {
      if (!(value instanceof global.Request))
        return false;
      const data = {
        method: value.method,
        url: value.url,
        headers: value.headers,
        body: value.body,
        duplex: value.duplex
      };
      const responseWritable = value[WEBHOOK_RESPONSE_WRITABLE];
      if (responseWritable) {
        data.responseWritable = responseWritable;
      }
      return data;
    },
    Response: (value) => {
      if (!(value instanceof global.Response))
        return false;
      return {
        type: value.type,
        url: value.url,
        status: value.status,
        statusText: value.statusText,
        headers: value.headers,
        body: value.body,
        redirected: value.redirected
      };
    },
    Class: (value) => {
      if (typeof value !== "function")
        return false;
      const classId = value.classId;
      if (typeof classId !== "string")
        return false;
      return { classId };
    },
    Instance: (value) => {
      if (value === null || typeof value !== "object")
        return false;
      const ctor = value.constructor;
      if (!ctor || typeof ctor !== "function")
        return false;
      const serialize = ctor[WORKFLOW_SERIALIZE$2];
      if (typeof serialize !== "function") {
        return false;
      }
      const classId = ctor.classId;
      if (typeof classId !== "string") {
        throw new Error(`Class "${ctor.name}" with ${String(WORKFLOW_SERIALIZE$2)} must have a static "classId" property.`);
      }
      const data = serialize(value);
      return { classId, data };
    },
    Set: (value) => value instanceof global.Set && Array.from(value),
    StepFunction: (value) => {
      if (typeof value !== "function")
        return false;
      const stepId = value.stepId;
      if (typeof stepId !== "string")
        return false;
      const closureVarsFn = value.__closureVarsFn;
      if (closureVarsFn && typeof closureVarsFn === "function") {
        const closureVars = closureVarsFn();
        return { stepId, closureVars };
      }
      return { stepId };
    },
    URL: (value) => value instanceof global.URL && value.href,
    URLSearchParams: (value) => {
      if (!(value instanceof global.URLSearchParams))
        return false;
      if (value.size === 0)
        return ".";
      return String(value);
    },
    Uint8Array: (value) => value instanceof global.Uint8Array && viewToBase64(value),
    Uint8ClampedArray: (value) => value instanceof global.Uint8ClampedArray && viewToBase64(value),
    Uint16Array: (value) => value instanceof global.Uint16Array && viewToBase64(value),
    Uint32Array: (value) => value instanceof global.Uint32Array && viewToBase64(value)
  };
}
function getExternalReducers(global = globalThis, ops, runId) {
  return {
    ...getCommonReducers(global),
    ReadableStream: (value) => {
      if (!(value instanceof global.ReadableStream))
        return false;
      if (value.locked) {
        throw new Error("ReadableStream is locked");
      }
      const streamId = (global[STABLE_ULID] || defaultUlid)();
      const name = `strm_${streamId}`;
      const type = getStreamType(value);
      const writable = new WorkflowServerWritableStream(name, runId);
      if (type === "bytes") {
        ops.push(value.pipeTo(writable));
      } else {
        ops.push(value.pipeThrough(getSerializeStream(getExternalReducers(global, ops, runId))).pipeTo(writable));
      }
      const s = { name };
      if (type)
        s.type = type;
      return s;
    },
    WritableStream: (value) => {
      if (!(value instanceof global.WritableStream))
        return false;
      const streamId = (global[STABLE_ULID] || defaultUlid)();
      const name = `strm_${streamId}`;
      const readable = new WorkflowServerReadableStream(name);
      ops.push(readable.pipeTo(value));
      return { name };
    }
  };
}
function getWorkflowReducers(global = globalThis) {
  return {
    ...getCommonReducers(global),
    // Readable/Writable streams from within the workflow execution environment
    // are simply "handles" that can be passed around to other steps.
    ReadableStream: (value) => {
      if (!(value instanceof global.ReadableStream))
        return false;
      const bodyInit = value[BODY_INIT_SYMBOL];
      if (bodyInit !== void 0) {
        return { bodyInit };
      }
      const name = value[STREAM_NAME_SYMBOL];
      if (!name) {
        throw new Error("ReadableStream `name` is not set");
      }
      const s = { name };
      const type = value[STREAM_TYPE_SYMBOL];
      if (type)
        s.type = type;
      return s;
    },
    WritableStream: (value) => {
      if (!(value instanceof global.WritableStream))
        return false;
      const name = value[STREAM_NAME_SYMBOL];
      if (!name) {
        throw new Error("WritableStream `name` is not set");
      }
      return { name };
    }
  };
}
function getStepReducers(global = globalThis, ops, runId) {
  return {
    ...getCommonReducers(global),
    ReadableStream: (value) => {
      if (!(value instanceof global.ReadableStream))
        return false;
      if (value.locked) {
        throw new Error("ReadableStream is locked");
      }
      let name = value[STREAM_NAME_SYMBOL];
      let type = value[STREAM_TYPE_SYMBOL];
      if (!name) {
        if (!runId) {
          throw new Error("ReadableStream cannot be serialized without a valid runId");
        }
        const streamId = (global[STABLE_ULID] || defaultUlid)();
        name = `strm_${streamId}`;
        type = getStreamType(value);
        const writable = new WorkflowServerWritableStream(name, runId);
        if (type === "bytes") {
          ops.push(value.pipeTo(writable));
        } else {
          ops.push(value.pipeThrough(getSerializeStream(getStepReducers(global, ops, runId))).pipeTo(writable));
        }
      }
      const s = { name };
      if (type)
        s.type = type;
      return s;
    },
    WritableStream: (value) => {
      if (!(value instanceof global.WritableStream))
        return false;
      let name = value[STREAM_NAME_SYMBOL];
      if (!name) {
        if (!runId) {
          throw new Error("WritableStream cannot be serialized without a valid runId");
        }
        const streamId = (global[STABLE_ULID] || defaultUlid)();
        name = `strm_${streamId}`;
        ops.push(new WorkflowServerReadableStream(name).pipeThrough(getDeserializeStream(getStepRevivers(global, ops, runId))).pipeTo(value));
      }
      return { name };
    }
  };
}
function getCommonRevivers(global = globalThis) {
  function reviveArrayBuffer(value) {
    const base64 = value === "." ? "" : value;
    const buffer = Buffer.from(base64, "base64");
    const arrayBuffer = new global.ArrayBuffer(buffer.length);
    const uint8Array = new global.Uint8Array(arrayBuffer);
    uint8Array.set(buffer);
    return arrayBuffer;
  }
  return {
    ArrayBuffer: reviveArrayBuffer,
    BigInt: (value) => global.BigInt(value),
    BigInt64Array: (value) => {
      const ab = reviveArrayBuffer(value);
      return new global.BigInt64Array(ab);
    },
    BigUint64Array: (value) => {
      const ab = reviveArrayBuffer(value);
      return new global.BigUint64Array(ab);
    },
    Date: (value) => new global.Date(value),
    Error: (value) => {
      const error = new global.Error(value.message);
      error.name = value.name;
      error.stack = value.stack;
      return error;
    },
    Float32Array: (value) => {
      const ab = reviveArrayBuffer(value);
      return new global.Float32Array(ab);
    },
    Float64Array: (value) => {
      const ab = reviveArrayBuffer(value);
      return new global.Float64Array(ab);
    },
    Headers: (value) => new global.Headers(value),
    Int8Array: (value) => {
      const ab = reviveArrayBuffer(value);
      return new global.Int8Array(ab);
    },
    Int16Array: (value) => {
      const ab = reviveArrayBuffer(value);
      return new global.Int16Array(ab);
    },
    Int32Array: (value) => {
      const ab = reviveArrayBuffer(value);
      return new global.Int32Array(ab);
    },
    Map: (value) => new global.Map(value),
    RegExp: (value) => new global.RegExp(value.source, value.flags),
    Class: (value) => {
      const classId = value.classId;
      const cls = getSerializationClass(classId, global);
      if (!cls) {
        throw new Error(`Class "${classId}" not found. Make sure the class is registered with registerSerializationClass.`);
      }
      return cls;
    },
    Instance: (value) => {
      const classId = value.classId;
      const data = value.data;
      const cls = getSerializationClass(classId, global);
      if (!cls) {
        throw new Error(`Class "${classId}" not found. Make sure the class is registered with registerSerializationClass.`);
      }
      const deserialize = cls[WORKFLOW_DESERIALIZE$2];
      if (typeof deserialize !== "function") {
        throw new Error(`Class "${classId}" does not have a static ${String(WORKFLOW_DESERIALIZE$2)} method.`);
      }
      return deserialize(data);
    },
    Set: (value) => new global.Set(value),
    StepFunction: (value) => {
      const stepId = value.stepId;
      const closureVars = value.closureVars;
      const stepFn = getStepFunction(stepId);
      if (!stepFn) {
        throw new Error(`Step function "${stepId}" not found. Make sure the step function is registered.`);
      }
      if (closureVars) {
        const wrappedStepFn = ((...args) => {
          const currentContext = contextStorage.getStore();
          if (!currentContext) {
            throw new Error("Cannot call step function with closure variables outside step context");
          }
          const newContext = {
            ...currentContext,
            closureVars
          };
          return contextStorage.run(newContext, () => stepFn(...args));
        });
        Object.defineProperty(wrappedStepFn, "name", {
          value: stepFn.name
        });
        Object.defineProperty(wrappedStepFn, "stepId", {
          value: stepId,
          writable: false,
          enumerable: false,
          configurable: false
        });
        if (stepFn.maxRetries !== void 0) {
          wrappedStepFn.maxRetries = stepFn.maxRetries;
        }
        return wrappedStepFn;
      }
      return stepFn;
    },
    URL: (value) => new global.URL(value),
    URLSearchParams: (value) => new global.URLSearchParams(value === "." ? "" : value),
    Uint8Array: (value) => {
      const ab = reviveArrayBuffer(value);
      return new global.Uint8Array(ab);
    },
    Uint8ClampedArray: (value) => {
      const ab = reviveArrayBuffer(value);
      return new global.Uint8ClampedArray(ab);
    },
    Uint16Array: (value) => {
      const ab = reviveArrayBuffer(value);
      return new global.Uint16Array(ab);
    },
    Uint32Array: (value) => {
      const ab = reviveArrayBuffer(value);
      return new global.Uint32Array(ab);
    }
  };
}
function getExternalRevivers(global = globalThis, ops, runId) {
  return {
    ...getCommonRevivers(global),
    Request: (value) => {
      return new global.Request(value.url, {
        method: value.method,
        headers: new global.Headers(value.headers),
        body: value.body,
        duplex: value.duplex
      });
    },
    Response: (value) => {
      return new global.Response(value.body, {
        status: value.status,
        statusText: value.statusText,
        headers: new global.Headers(value.headers)
      });
    },
    ReadableStream: (value) => {
      if ("bodyInit" in value) {
        const bodyInit = value.bodyInit;
        const response = new global.Response(bodyInit);
        return response.body;
      }
      const readable = new WorkflowServerReadableStream(value.name, value.startIndex);
      if (value.type === "bytes") {
        const state = createFlushableState();
        ops.push(state.promise);
        const { readable: userReadable, writable } = new global.TransformStream();
        flushablePipe(readable, writable, state).catch(() => {
        });
        pollReadableLock(userReadable, state);
        return userReadable;
      } else {
        const transform = getDeserializeStream(getExternalRevivers(global, ops, runId));
        const state = createFlushableState();
        ops.push(state.promise);
        flushablePipe(readable, transform.writable, state).catch(() => {
        });
        pollReadableLock(transform.readable, state);
        return transform.readable;
      }
    },
    WritableStream: (value) => {
      const serialize = getSerializeStream(getExternalReducers(global, ops, runId));
      const serverWritable = new WorkflowServerWritableStream(value.name, runId);
      const state = createFlushableState();
      ops.push(state.promise);
      flushablePipe(serialize.readable, serverWritable, state).catch(() => {
      });
      pollWritableLock(serialize.writable, state);
      return serialize.writable;
    }
  };
}
function getWorkflowRevivers(global = globalThis) {
  return {
    ...getCommonRevivers(global),
    Request: (value) => {
      Object.setPrototypeOf(value, global.Request.prototype);
      const responseWritable = value.responseWritable;
      if (responseWritable) {
        value[WEBHOOK_RESPONSE_WRITABLE] = responseWritable;
        delete value.responseWritable;
        value.respondWith = () => {
          throw new Error("`respondWith()` must be called from within a step function");
        };
      }
      return value;
    },
    Response: (value) => {
      Object.setPrototypeOf(value, global.Response.prototype);
      return value;
    },
    ReadableStream: (value) => {
      if ("bodyInit" in value) {
        return Object.create(global.ReadableStream.prototype, {
          [BODY_INIT_SYMBOL]: {
            value: value.bodyInit,
            writable: false
          }
        });
      }
      return Object.create(global.ReadableStream.prototype, {
        [STREAM_NAME_SYMBOL]: {
          value: value.name,
          writable: false
        },
        [STREAM_TYPE_SYMBOL]: {
          value: value.type,
          writable: false
        }
      });
    },
    WritableStream: (value) => {
      return Object.create(global.WritableStream.prototype, {
        [STREAM_NAME_SYMBOL]: {
          value: value.name,
          writable: false
        }
      });
    }
  };
}
function getStepRevivers(global = globalThis, ops, runId) {
  return {
    ...getCommonRevivers(global),
    Request: (value) => {
      const responseWritable = value.responseWritable;
      const request = new global.Request(value.url, {
        method: value.method,
        headers: new global.Headers(value.headers),
        body: value.body,
        duplex: value.duplex
      });
      if (responseWritable) {
        request.respondWith = async (response) => {
          const writer = responseWritable.getWriter();
          await writer.write(response);
          await writer.close();
        };
      }
      return request;
    },
    Response: (value) => {
      return new global.Response(value.body, {
        status: value.status,
        statusText: value.statusText,
        headers: new global.Headers(value.headers)
      });
    },
    ReadableStream: (value) => {
      if ("bodyInit" in value) {
        const bodyInit = value.bodyInit;
        const response = new global.Response(bodyInit);
        return response.body;
      }
      const readable = new WorkflowServerReadableStream(value.name);
      if (value.type === "bytes") {
        const state = createFlushableState();
        ops.push(state.promise);
        const { readable: userReadable, writable } = new global.TransformStream();
        flushablePipe(readable, writable, state).catch(() => {
        });
        pollReadableLock(userReadable, state);
        return userReadable;
      } else {
        const transform = getDeserializeStream(getStepRevivers(global, ops, runId));
        const state = createFlushableState();
        ops.push(state.promise);
        flushablePipe(readable, transform.writable, state).catch(() => {
        });
        pollReadableLock(transform.readable, state);
        return transform.readable;
      }
    },
    WritableStream: (value) => {
      if (!runId) {
        throw new Error("WritableStream cannot be revived without a valid runId");
      }
      const serialize = getSerializeStream(getStepReducers(global, ops, runId));
      const serverWritable = new WorkflowServerWritableStream(value.name, runId);
      const state = createFlushableState();
      ops.push(state.promise);
      flushablePipe(serialize.readable, serverWritable, state).catch(() => {
      });
      pollWritableLock(serialize.writable, state);
      return serialize.writable;
    }
  };
}
function dehydrateWorkflowArguments(value, ops, runId, global = globalThis) {
  try {
    const str = stringify(value, getExternalReducers(global, ops, runId));
    return revive(str);
  } catch (error) {
    throw new WorkflowRuntimeError(formatSerializationError("workflow arguments", error), { slug: "serialization-failed", cause: error });
  }
}
function hydrateWorkflowArguments(value, global = globalThis, extraRevivers = {}) {
  const obj = unflatten(value, {
    ...getWorkflowRevivers(global),
    ...extraRevivers
  });
  return obj;
}
function dehydrateWorkflowReturnValue(value, global = globalThis) {
  try {
    const str = stringify(value, getWorkflowReducers(global));
    return revive(str);
  } catch (error) {
    throw new WorkflowRuntimeError(formatSerializationError("workflow return value", error), { slug: "serialization-failed", cause: error });
  }
}
function hydrateWorkflowReturnValue(value, ops, runId, global = globalThis, extraRevivers = {}) {
  const obj = unflatten(value, {
    ...getExternalRevivers(global, ops, runId),
    ...extraRevivers
  });
  return obj;
}
function dehydrateStepArguments(value, global) {
  try {
    const str = stringify(value, getWorkflowReducers(global));
    return revive(str);
  } catch (error) {
    throw new WorkflowRuntimeError(formatSerializationError("step arguments", error), { slug: "serialization-failed", cause: error });
  }
}
function hydrateStepArguments(value, ops, runId, global = globalThis, extraRevivers = {}) {
  const obj = unflatten(value, {
    ...getStepRevivers(global, ops, runId),
    ...extraRevivers
  });
  return obj;
}
function dehydrateStepReturnValue(value, ops, runId, global = globalThis) {
  try {
    const str = stringify(value, getStepReducers(global, ops, runId));
    return revive(str);
  } catch (error) {
    throw new WorkflowRuntimeError(formatSerializationError("step return value", error), { slug: "serialization-failed", cause: error });
  }
}
function hydrateStepReturnValue(value, global = globalThis, extraRevivers = {}) {
  const obj = unflatten(value, {
    ...getWorkflowRevivers(global),
    ...extraRevivers
  });
  return obj;
}
async function handleSuspension({ suspension, world, runId, workflowName, workflowStartedAt, span }) {
  const stepItems = suspension.steps.filter((item) => item.type === "step");
  const hookItems = suspension.steps.filter((item) => item.type === "hook");
  const waitItems = suspension.steps.filter((item) => item.type === "wait");
  const hookEvents = hookItems.map((queueItem) => {
    const hookMetadata = typeof queueItem.metadata === "undefined" ? void 0 : dehydrateStepArguments(queueItem.metadata, suspension.globalThis);
    return {
      eventType: "hook_created",
      specVersion: SPEC_VERSION_CURRENT,
      correlationId: queueItem.correlationId,
      eventData: {
        token: queueItem.token,
        metadata: hookMetadata
      }
    };
  });
  let hasHookConflict = false;
  if (hookEvents.length > 0) {
    await Promise.all(hookEvents.map(async (hookEvent) => {
      try {
        const result2 = await world.events.create(runId, hookEvent);
        if (result2.event.eventType === "hook_conflict") {
          hasHookConflict = true;
        }
      } catch (err) {
        if (WorkflowAPIError.is(err)) {
          if (err.status === 410) {
            console.warn(`Workflow run "${runId}" has already completed, skipping hook: ${err.message}`);
          } else {
            throw err;
          }
        } else {
          throw err;
        }
      }
    }));
  }
  const stepsNeedingCreation = new Set(stepItems.filter((queueItem) => !queueItem.hasCreatedEvent).map((queueItem) => queueItem.correlationId));
  const ops = [];
  for (const queueItem of stepItems) {
    ops.push((async () => {
      if (stepsNeedingCreation.has(queueItem.correlationId)) {
        const dehydratedInput = dehydrateStepArguments({
          args: queueItem.args,
          closureVars: queueItem.closureVars,
          thisVal: queueItem.thisVal
        }, suspension.globalThis);
        const stepEvent = {
          eventType: "step_created",
          specVersion: SPEC_VERSION_CURRENT,
          correlationId: queueItem.correlationId,
          eventData: {
            stepName: queueItem.stepName,
            input: dehydratedInput
          }
        };
        try {
          await world.events.create(runId, stepEvent);
        } catch (err) {
          if (WorkflowAPIError.is(err) && err.status === 409) {
            console.warn(`Step already exists, continuing: ${err.message}`);
          } else {
            throw err;
          }
        }
      }
      await queueMessage(world, `__wkf_step_${queueItem.stepName}`, {
        workflowName,
        workflowRunId: runId,
        workflowStartedAt,
        stepId: queueItem.correlationId,
        traceCarrier: await serializeTraceCarrier(),
        requestedAt: /* @__PURE__ */ new Date()
      }, {
        idempotencyKey: queueItem.correlationId
      });
    })());
  }
  for (const queueItem of waitItems) {
    if (!queueItem.hasCreatedEvent) {
      ops.push((async () => {
        const waitEvent = {
          eventType: "wait_created",
          specVersion: SPEC_VERSION_CURRENT,
          correlationId: queueItem.correlationId,
          eventData: {
            resumeAt: queueItem.resumeAt
          }
        };
        try {
          await world.events.create(runId, waitEvent);
        } catch (err) {
          if (WorkflowAPIError.is(err) && err.status === 409) {
            console.warn(`Wait already exists, continuing: ${err.message}`);
          } else {
            throw err;
          }
        }
      })());
    }
  }
  functionsExports.waitUntil(Promise.all(ops).catch((opErr) => {
    const isAbortError = opErr?.name === "AbortError" || opErr?.name === "ResponseAborted";
    if (!isAbortError)
      throw opErr;
  }));
  await Promise.all(ops);
  const now = Date.now();
  const minTimeoutSeconds = waitItems.reduce((min, queueItem) => {
    const resumeAtMs = queueItem.resumeAt.getTime();
    const delayMs = Math.max(1e3, resumeAtMs - now);
    const timeoutSeconds = Math.ceil(delayMs / 1e3);
    if (min === null)
      return timeoutSeconds;
    return Math.min(min, timeoutSeconds);
  }, null);
  span?.setAttributes({
    ...WorkflowRunStatus("workflow_suspended"),
    ...WorkflowStepsCreated(stepItems.length),
    ...WorkflowHooksCreated(hookItems.length),
    ...WorkflowWaitsCreated(waitItems.length)
  });
  if (hasHookConflict) {
    return { timeoutSeconds: 1 };
  }
  if (minTimeoutSeconds !== null) {
    return { timeoutSeconds: minTimeoutSeconds };
  }
  return {};
}
function remapErrorStack(stack, filename, workflowCode2) {
  const sourceMapMatch = workflowCode2.match(/\/\/# sourceMappingURL=data:application\/json;base64,(.+)/);
  if (!sourceMapMatch) {
    return stack;
  }
  try {
    const base64 = sourceMapMatch[1];
    const sourceMapJson = Buffer.from(base64, "base64").toString("utf-8");
    const sourceMapData = JSON.parse(sourceMapJson);
    const tracer = new TraceMap(sourceMapData);
    const lines = stack.split("\n");
    const remappedLines = lines.map((line) => {
      const frameMatch = line.match(/^\s*at\s+(?:(.+?)\s+\()?(.+?):(\d+):(\d+)\)?$/);
      if (!frameMatch) {
        return line;
      }
      const [, functionName, file, lineStr, colStr] = frameMatch;
      if (!file.includes(filename)) {
        return line;
      }
      const lineNumber = parseInt(lineStr, 10);
      const columnNumber = parseInt(colStr, 10);
      const original = originalPositionFor(tracer, {
        line: lineNumber,
        column: columnNumber
      });
      if (original.source && original.line !== null) {
        const func = functionName || original.name || "anonymous";
        const col = original.column !== null ? original.column : columnNumber;
        return `    at ${func} (${original.source}:${original.line}:${col})`;
      }
      return line;
    });
    return remappedLines.join("\n");
  } catch (e) {
    return stack;
  }
}
function getErrorName(v) {
  if (types.isNativeError(v)) {
    return v.name;
  }
  return "Error";
}
function getErrorStack(v) {
  if (types.isNativeError(v)) {
    return v.stack ?? "";
  }
  return "";
}
function buildWorkflowSuspensionMessage(runId, stepCount, hookCount, waitCount) {
  if (stepCount === 0 && hookCount === 0 && waitCount === 0) {
    return null;
  }
  const parts = [];
  if (stepCount > 0) {
    parts.push(`${stepCount} ${pluralize("step", "steps", stepCount)}`);
  }
  if (hookCount > 0) {
    parts.push(`${hookCount} ${pluralize("hook", "hooks", hookCount)}`);
  }
  if (waitCount > 0) {
    parts.push(`${waitCount} ${pluralize("timer", "timers", waitCount)}`);
  }
  const resumeMsgParts = [];
  if (stepCount > 0) {
    resumeMsgParts.push("steps are completed");
  }
  if (hookCount > 0) {
    resumeMsgParts.push("hooks are received");
  }
  if (waitCount > 0) {
    resumeMsgParts.push("timers have elapsed");
  }
  const resumeMsg = resumeMsgParts.join(" and ");
  return `[Workflows] "${runId}" - ${parts.join(" and ")} to be enqueued
  Workflow will suspend and resume when ${resumeMsg}`;
}
function getWorkflowRunStreamId(runId, namespace) {
  const streamId = `${runId.replace("wrun_", "strm_")}_user`;
  if (!namespace) {
    return streamId;
  }
  const encodedNamespace = Buffer.from(namespace, "utf-8").toString("base64url");
  return `${streamId}_${encodedNamespace}`;
}
async function waitedUntil(fn) {
  const result2 = fn();
  functionsExports.waitUntil(result2.catch(() => {
  }));
  return result2;
}
var EventConsumerResult;
(function(EventConsumerResult2) {
  EventConsumerResult2[EventConsumerResult2["Consumed"] = 0] = "Consumed";
  EventConsumerResult2[EventConsumerResult2["NotConsumed"] = 1] = "NotConsumed";
  EventConsumerResult2[EventConsumerResult2["Finished"] = 2] = "Finished";
})(EventConsumerResult || (EventConsumerResult = {}));
class EventsConsumer {
  eventIndex;
  events = [];
  callbacks = [];
  constructor(events) {
    this.events = events;
    this.eventIndex = 0;
    eventsLogger.debug("EventsConsumer initialized", { events });
  }
  /**
   * Registers a callback function to be called after an event has been consumed
   * by a different callback. The callback can return:
   *  - `EventConsumerResult.Consumed` the event is considered consumed and will not be passed to any other callback, but the callback will remain in the callbacks list
   *  - `EventConsumerResult.NotConsumed` the event is passed to the next callback
   *  - `EventConsumerResult.Finished` the event is considered consumed and the callback is removed from the callbacks list
   *
   * @param fn - The callback function to register.
   */
  subscribe(fn) {
    this.callbacks.push(fn);
    process.nextTick(this.consume);
  }
  consume = () => {
    const currentEvent = this.events[this.eventIndex] ?? null;
    for (let i = 0; i < this.callbacks.length; i++) {
      const callback = this.callbacks[i];
      let handled = EventConsumerResult.NotConsumed;
      try {
        handled = callback(currentEvent);
      } catch (error) {
        eventsLogger.error("EventConsumer callback threw an error", { error });
        console.error("EventConsumer callback threw an error", error);
      }
      eventsLogger.debug("EventConsumer callback result", {
        handled: EventConsumerResult[handled],
        eventIndex: this.eventIndex,
        eventId: currentEvent?.eventId
      });
      if (handled === EventConsumerResult.Consumed || handled === EventConsumerResult.Finished) {
        this.eventIndex++;
        if (handled === EventConsumerResult.Finished) {
          this.callbacks.splice(i, 1);
        }
        process.nextTick(this.consume);
        return;
      }
    }
  };
}
function createUseStep(ctx) {
  return function useStep(stepName, closureVarsFn) {
    const stepFunction = function(...args) {
      const { promise, resolve: resolve2, reject } = withResolvers();
      const correlationId = `step_${ctx.generateUlid()}`;
      const queueItem = {
        type: "step",
        correlationId,
        stepName,
        args
      };
      if (this !== void 0 && this !== null && this !== globalThis) {
        queueItem.thisVal = this;
      }
      const closureVars = closureVarsFn?.();
      if (closureVars) {
        queueItem.closureVars = closureVars;
      }
      ctx.invocationsQueue.set(correlationId, queueItem);
      stepLogger.debug("Step consumer setup", {
        correlationId,
        stepName,
        args
      });
      ctx.eventsConsumer.subscribe((event) => {
        if (!event) {
          setTimeout(() => {
            ctx.onWorkflowError(new WorkflowSuspension(ctx.invocationsQueue, ctx.globalThis));
          }, 0);
          return EventConsumerResult.NotConsumed;
        }
        stepLogger.debug("Step consumer event processing", {
          correlationId,
          stepName,
          args: args.join(", "),
          incomingCorrelationId: event.correlationId,
          isMatch: correlationId === event.correlationId,
          eventType: event.eventType
        });
        if (event.correlationId !== correlationId) {
          return EventConsumerResult.NotConsumed;
        }
        if (event.eventType === "step_created") {
          const queueItem2 = ctx.invocationsQueue.get(correlationId);
          if (!queueItem2 || queueItem2.type !== "step") {
            setTimeout(() => {
              reject(new WorkflowRuntimeError(`Corrupted event log: step ${correlationId} (${stepName}) created but not found in invocation queue`));
            }, 0);
            return EventConsumerResult.Finished;
          }
          queueItem2.hasCreatedEvent = true;
          return EventConsumerResult.Consumed;
        }
        if (event.eventType === "step_started") {
          return EventConsumerResult.Consumed;
        }
        if (event.eventType === "step_retrying") {
          return EventConsumerResult.Consumed;
        }
        if (event.eventType === "step_failed") {
          ctx.invocationsQueue.delete(event.correlationId);
          setTimeout(() => {
            const errorData = event.eventData.error;
            const isErrorObject = typeof errorData === "object" && errorData !== null;
            const errorMessage = isErrorObject ? errorData.message ?? "Unknown error" : typeof errorData === "string" ? errorData : "Unknown error";
            const errorStack = (isErrorObject ? errorData.stack : void 0) ?? event.eventData.stack;
            const error = new FatalError(errorMessage);
            if (errorStack) {
              error.stack = errorStack;
            }
            reject(error);
          }, 0);
          return EventConsumerResult.Finished;
        }
        if (event.eventType === "step_completed") {
          ctx.invocationsQueue.delete(event.correlationId);
          const hydratedResult = hydrateStepReturnValue(event.eventData.result, ctx.globalThis);
          setTimeout(() => {
            resolve2(hydratedResult);
          }, 0);
          return EventConsumerResult.Finished;
        }
        setTimeout(() => {
          ctx.onWorkflowError(new WorkflowRuntimeError(`Unexpected event type for step ${correlationId} (name: ${stepName}) "${event.eventType}"`));
        }, 0);
        return EventConsumerResult.Finished;
      });
      return promise;
    };
    const functionName = stepName.split("//").pop();
    Object.defineProperty(stepFunction, "name", {
      value: functionName
    });
    Object.defineProperty(stepFunction, "stepId", {
      value: stepName,
      writable: false,
      enumerable: false,
      configurable: false
    });
    if (closureVarsFn) {
      Object.defineProperty(stepFunction, "__closureVarsFn", {
        value: closureVarsFn,
        writable: false,
        enumerable: false,
        configurable: false
      });
    }
    return stepFunction;
  };
}
function createRandomUUID(rng) {
  return function randomUUID() {
    const chars = "0123456789abcdef";
    let uuid = "";
    for (let i = 0; i < 36; i++) {
      if (i === 8 || i === 13 || i === 18 || i === 23) {
        uuid += "-";
      } else if (i === 14) {
        uuid += "4";
      } else if (i === 19) {
        uuid += chars[Math.floor(rng() * 4) + 8];
      } else {
        uuid += chars[Math.floor(rng() * 16)];
      }
    }
    return uuid;
  };
}
function createContext(options) {
  let { fixedTimestamp } = options;
  const { seed } = options;
  const rng = seedrandom(seed);
  const context = createContext$1();
  const g = runInContext("globalThis", context);
  g.Math.random = rng;
  const Date_ = g.Date;
  g.Date = function Date2(...args) {
    if (args.length === 0) {
      return new Date_(fixedTimestamp);
    }
    return new Date_(...args);
  };
  g.Date.prototype = Date_.prototype;
  Object.setPrototypeOf(g.Date, Date_);
  g.Date.now = () => fixedTimestamp;
  const originalCrypto = globalThis.crypto;
  const originalSubtle = originalCrypto.subtle;
  function getRandomValues(array2) {
    for (let i = 0; i < array2.length; i++) {
      array2[i] = Math.floor(rng() * 256);
    }
    return array2;
  }
  const randomUUID = createRandomUUID(rng);
  const boundDigest = originalSubtle.digest.bind(originalSubtle);
  g.crypto = new Proxy(originalCrypto, {
    get(target, prop) {
      if (prop === "getRandomValues") {
        return getRandomValues;
      }
      if (prop === "randomUUID") {
        return randomUUID;
      }
      if (prop === "subtle") {
        return new Proxy(originalSubtle, {
          get(target2, prop2) {
            if (prop2 === "generateKey") {
              return () => {
                throw new Error("Not implemented");
              };
            } else if (prop2 === "digest") {
              return boundDigest;
            }
            return target2[prop2];
          }
        });
      }
      return target[prop];
    }
  });
  g.process = {
    env: Object.freeze({ ...process.env })
  };
  g.Headers = globalThis.Headers;
  g.TextEncoder = globalThis.TextEncoder;
  g.TextDecoder = globalThis.TextDecoder;
  g.console = globalThis.console;
  g.URL = globalThis.URL;
  g.URLSearchParams = globalThis.URLSearchParams;
  g.structuredClone = globalThis.structuredClone;
  g.exports = {};
  g.module = { exports: g.exports };
  return {
    context,
    globalThis: g,
    updateTimestamp: (timestamp) => {
      fixedTimestamp = timestamp;
    }
  };
}
const WORKFLOW_CONTEXT_SYMBOL = /* @__PURE__ */ Symbol.for("WORKFLOW_CONTEXT");
function createCreateHook(ctx) {
  return function createHookImpl(options = {}) {
    const correlationId = `hook_${ctx.generateUlid()}`;
    const token = options.token ?? ctx.generateNanoid();
    ctx.invocationsQueue.set(correlationId, {
      type: "hook",
      correlationId,
      token,
      metadata: options.metadata
    });
    const payloadsQueue = [];
    const promises2 = [];
    let eventLogEmpty = false;
    let hasConflict = false;
    let conflictErrorRef = null;
    webhookLogger.debug("Hook consumer setup", { correlationId, token });
    ctx.eventsConsumer.subscribe((event) => {
      if (!event) {
        eventLogEmpty = true;
        if (promises2.length > 0) {
          setTimeout(() => {
            ctx.onWorkflowError(new WorkflowSuspension(ctx.invocationsQueue, ctx.globalThis));
          }, 0);
        }
        return EventConsumerResult.NotConsumed;
      }
      if (event.correlationId !== correlationId) {
        return EventConsumerResult.NotConsumed;
      }
      if (event.eventType === "hook_created") {
        ctx.invocationsQueue.delete(correlationId);
        return EventConsumerResult.Consumed;
      }
      if (event.eventType === "hook_conflict") {
        ctx.invocationsQueue.delete(correlationId);
        const conflictEvent = event;
        const conflictError = new WorkflowRuntimeError(`Hook token "${conflictEvent.eventData.token}" is already in use by another workflow`, { slug: ERROR_SLUGS.HOOK_CONFLICT });
        for (const resolver of promises2) {
          resolver.reject(conflictError);
        }
        promises2.length = 0;
        hasConflict = true;
        conflictErrorRef = conflictError;
        return EventConsumerResult.Consumed;
      }
      if (event.eventType === "hook_received") {
        if (promises2.length > 0) {
          const next = promises2.shift();
          if (next) {
            const payload = hydrateStepReturnValue(event.eventData.payload, ctx.globalThis);
            next.resolve(payload);
          }
        } else {
          payloadsQueue.push(event);
        }
        return EventConsumerResult.Consumed;
      }
      if (event.eventType === "hook_disposed") {
        return EventConsumerResult.Finished;
      }
      setTimeout(() => {
        ctx.onWorkflowError(new WorkflowRuntimeError(`Unexpected event type for hook ${correlationId} (token: ${token}) "${event.eventType}"`));
      }, 0);
      return EventConsumerResult.Finished;
    });
    function createHookPromise() {
      const resolvers = withResolvers();
      if (hasConflict && conflictErrorRef) {
        resolvers.reject(conflictErrorRef);
        return resolvers.promise;
      }
      if (payloadsQueue.length > 0) {
        const nextPayload = payloadsQueue.shift();
        if (nextPayload) {
          const payload = hydrateStepReturnValue(nextPayload.eventData.payload, ctx.globalThis);
          resolvers.resolve(payload);
          return resolvers.promise;
        }
      }
      if (eventLogEmpty) {
        setTimeout(() => {
          ctx.onWorkflowError(new WorkflowSuspension(ctx.invocationsQueue, ctx.globalThis));
        }, 0);
      }
      promises2.push(resolvers);
      return resolvers.promise;
    }
    const hook = {
      token,
      // biome-ignore lint/suspicious/noThenProperty: Intentionally thenable
      then(onfulfilled, onrejected) {
        return createHookPromise().then(onfulfilled, onrejected);
      },
      // Support `for await (const payload of hook) { … }` syntax
      async *[Symbol.asyncIterator]() {
        while (true) {
          yield await this;
        }
      }
    };
    return hook;
  };
}
function createSleep(ctx) {
  return async function sleepImpl(param) {
    const { promise, resolve: resolve2 } = withResolvers();
    const correlationId = `wait_${ctx.generateUlid()}`;
    const resumeAt = parseDurationToDate(param);
    const waitItem = {
      type: "wait",
      correlationId,
      resumeAt
    };
    ctx.invocationsQueue.set(correlationId, waitItem);
    ctx.eventsConsumer.subscribe((event) => {
      if (!event) {
        setTimeout(() => {
          ctx.onWorkflowError(new WorkflowSuspension(ctx.invocationsQueue, ctx.globalThis));
        }, 0);
        return EventConsumerResult.NotConsumed;
      }
      if (event.correlationId !== correlationId) {
        return EventConsumerResult.NotConsumed;
      }
      if (event.eventType === "wait_created") {
        const queueItem = ctx.invocationsQueue.get(correlationId);
        if (queueItem && queueItem.type === "wait") {
          queueItem.hasCreatedEvent = true;
          queueItem.resumeAt = event.eventData.resumeAt;
        }
        return EventConsumerResult.Consumed;
      }
      if (event.eventType === "wait_completed") {
        ctx.invocationsQueue.delete(correlationId);
        setTimeout(() => {
          resolve2();
        }, 0);
        return EventConsumerResult.Finished;
      }
      setTimeout(() => {
        ctx.onWorkflowError(new WorkflowRuntimeError(`Unexpected event type for wait ${correlationId} "${event.eventType}"`));
      }, 0);
      return EventConsumerResult.Finished;
    });
    return promise;
  };
}
async function runWorkflow(workflowCode2, workflowRun, events) {
  return trace(`WORKFLOW.run ${workflowRun.workflowName}`, async (span) => {
    span?.setAttributes({
      ...WorkflowName(workflowRun.workflowName),
      ...WorkflowRunId(workflowRun.runId),
      ...WorkflowRunStatus(workflowRun.status),
      ...WorkflowEventsCount(events.length)
    });
    const startedAt = workflowRun.startedAt;
    if (!startedAt) {
      throw new Error(`Workflow run "${workflowRun.runId}" has no "startedAt" timestamp (should not happen)`);
    }
    const port2 = await getPort();
    const { context, globalThis: vmGlobalThis, updateTimestamp } = createContext({
      seed: workflowRun.runId,
      fixedTimestamp: +startedAt
    });
    const workflowDiscontinuation = withResolvers();
    const ulid2 = monotonicFactory(() => vmGlobalThis.Math.random());
    const generateNanoid = customRandom(urlAlphabet, 21, (size) => new Uint8Array(size).map(() => 256 * vmGlobalThis.Math.random()));
    const workflowContext = {
      globalThis: vmGlobalThis,
      onWorkflowError: workflowDiscontinuation.reject,
      eventsConsumer: new EventsConsumer(events),
      generateUlid: () => ulid2(+startedAt),
      generateNanoid,
      invocationsQueue: /* @__PURE__ */ new Map()
    };
    workflowContext.eventsConsumer.subscribe((event) => {
      const createdAt = event?.createdAt;
      if (createdAt) {
        updateTimestamp(+createdAt);
      }
      return EventConsumerResult.NotConsumed;
    });
    workflowContext.eventsConsumer.subscribe((event) => {
      if (!event) {
        return EventConsumerResult.NotConsumed;
      }
      if (event.eventType === "run_created") {
        return EventConsumerResult.Consumed;
      }
      if (event.eventType === "run_started") {
        return EventConsumerResult.Consumed;
      }
      return EventConsumerResult.NotConsumed;
    });
    const useStep = createUseStep(workflowContext);
    const createHook = createCreateHook(workflowContext);
    const sleep = createSleep(workflowContext);
    vmGlobalThis[WORKFLOW_USE_STEP] = useStep;
    vmGlobalThis[WORKFLOW_CREATE_HOOK] = createHook;
    vmGlobalThis[WORKFLOW_SLEEP] = sleep;
    vmGlobalThis[WORKFLOW_GET_STREAM_ID] = (namespace) => getWorkflowRunStreamId(workflowRun.runId, namespace);
    const url = process.env.VERCEL_URL ? `https://${process.env.VERCEL_URL}` : `http://localhost:${port2 ?? 3e3}`;
    const ctx = {
      workflowRunId: workflowRun.runId,
      workflowStartedAt: new vmGlobalThis.Date(+startedAt),
      url
    };
    vmGlobalThis[WORKFLOW_CONTEXT_SYMBOL] = ctx;
    vmGlobalThis[STABLE_ULID] = ulid2;
    vmGlobalThis.fetch = () => {
      throw new vmGlobalThis.Error(`Global "fetch" is unavailable in workflow functions. Use the "fetch" step function from "workflow" to make HTTP requests.

Learn more: https://useworkflow.dev/err/${ERROR_SLUGS.FETCH_IN_WORKFLOW_FUNCTION}`);
    };
    const timeoutErrorMessage = 'Timeout functions like "setTimeout" and "setInterval" are not supported in workflow functions. Use the "sleep" function from "workflow" for time-based delays.';
    vmGlobalThis.setTimeout = () => {
      throw new WorkflowRuntimeError(timeoutErrorMessage, {
        slug: ERROR_SLUGS.TIMEOUT_FUNCTIONS_IN_WORKFLOW
      });
    };
    vmGlobalThis.setInterval = () => {
      throw new WorkflowRuntimeError(timeoutErrorMessage, {
        slug: ERROR_SLUGS.TIMEOUT_FUNCTIONS_IN_WORKFLOW
      });
    };
    vmGlobalThis.clearTimeout = () => {
      throw new WorkflowRuntimeError(timeoutErrorMessage, {
        slug: ERROR_SLUGS.TIMEOUT_FUNCTIONS_IN_WORKFLOW
      });
    };
    vmGlobalThis.clearInterval = () => {
      throw new WorkflowRuntimeError(timeoutErrorMessage, {
        slug: ERROR_SLUGS.TIMEOUT_FUNCTIONS_IN_WORKFLOW
      });
    };
    vmGlobalThis.setImmediate = () => {
      throw new WorkflowRuntimeError(timeoutErrorMessage, {
        slug: ERROR_SLUGS.TIMEOUT_FUNCTIONS_IN_WORKFLOW
      });
    };
    vmGlobalThis.clearImmediate = () => {
      throw new WorkflowRuntimeError(timeoutErrorMessage, {
        slug: ERROR_SLUGS.TIMEOUT_FUNCTIONS_IN_WORKFLOW
      });
    };
    class Request2 {
      cache;
      credentials;
      destination;
      headers;
      integrity;
      method;
      mode;
      redirect;
      referrer;
      referrerPolicy;
      url;
      keepalive;
      signal;
      duplex;
      body;
      constructor(input, init) {
        if (typeof input === "string" || input instanceof vmGlobalThis.URL) {
          const urlString = String(input);
          try {
            new vmGlobalThis.URL(urlString);
            this.url = urlString;
          } catch (cause) {
            throw new TypeError(`Failed to parse URL from ${urlString}`, {
              cause
            });
          }
        } else {
          this.url = input.url;
          if (!init) {
            this.method = input.method;
            this.headers = new vmGlobalThis.Headers(input.headers);
            this.body = input.body;
            this.mode = input.mode;
            this.credentials = input.credentials;
            this.cache = input.cache;
            this.redirect = input.redirect;
            this.referrer = input.referrer;
            this.referrerPolicy = input.referrerPolicy;
            this.integrity = input.integrity;
            this.keepalive = input.keepalive;
            this.signal = input.signal;
            this.duplex = input.duplex;
            this.destination = input.destination;
            return;
          }
          this.method = input.method;
          this.headers = new vmGlobalThis.Headers(input.headers);
          this.body = input.body;
          this.mode = input.mode;
          this.credentials = input.credentials;
          this.cache = input.cache;
          this.redirect = input.redirect;
          this.referrer = input.referrer;
          this.referrerPolicy = input.referrerPolicy;
          this.integrity = input.integrity;
          this.keepalive = input.keepalive;
          this.signal = input.signal;
          this.duplex = input.duplex;
          this.destination = input.destination;
        }
        if (init?.method) {
          this.method = init.method.toUpperCase();
        } else if (typeof this.method !== "string") {
          this.method = "GET";
        }
        if (init?.headers) {
          this.headers = new vmGlobalThis.Headers(init.headers);
        } else if (typeof input === "string" || input instanceof vmGlobalThis.URL) {
          this.headers = new vmGlobalThis.Headers();
        }
        if (init?.mode !== void 0) {
          this.mode = init.mode;
        } else if (typeof this.mode !== "string") {
          this.mode = "cors";
        }
        if (init?.credentials !== void 0) {
          this.credentials = init.credentials;
        } else if (typeof this.credentials !== "string") {
          this.credentials = "same-origin";
        }
        if (init?.cache !== void 0) {
          this.cache = init.cache;
        } else if (typeof this.cache !== "string") {
          this.cache = "default";
        }
        if (init?.redirect !== void 0) {
          this.redirect = init.redirect;
        } else if (typeof this.redirect !== "string") {
          this.redirect = "follow";
        }
        if (init?.referrer !== void 0) {
          this.referrer = init.referrer;
        } else if (typeof this.referrer !== "string") {
          this.referrer = "about:client";
        }
        if (init?.referrerPolicy !== void 0) {
          this.referrerPolicy = init.referrerPolicy;
        } else if (typeof this.referrerPolicy !== "string") {
          this.referrerPolicy = "";
        }
        if (init?.integrity !== void 0) {
          this.integrity = init.integrity;
        } else if (typeof this.integrity !== "string") {
          this.integrity = "";
        }
        if (init?.keepalive !== void 0) {
          this.keepalive = init.keepalive;
        } else if (typeof this.keepalive !== "boolean") {
          this.keepalive = false;
        }
        if (init?.signal !== void 0) {
          this.signal = init.signal;
        } else if (!this.signal) {
          this.signal = { aborted: false };
        }
        if (!this.duplex) {
          this.duplex = "half";
        }
        if (!this.destination) {
          this.destination = "document";
        }
        const body = init?.body;
        if (body !== null && body !== void 0 && (this.method === "GET" || this.method === "HEAD")) {
          throw new TypeError(`Request with GET/HEAD method cannot have body.`);
        }
        if (body !== null && body !== void 0) {
          this.body = Object.create(vmGlobalThis.ReadableStream.prototype, {
            [BODY_INIT_SYMBOL]: {
              value: body,
              writable: false
            }
          });
        } else {
          this.body = null;
        }
      }
      clone() {
        ENOTSUP();
      }
      get bodyUsed() {
        return false;
      }
      // TODO: implement these
      blob;
      formData;
      async arrayBuffer() {
        return resArrayBuffer(this);
      }
      async bytes() {
        return new Uint8Array(await resArrayBuffer(this));
      }
      async json() {
        return resJson(this);
      }
      async text() {
        return resText(this);
      }
    }
    vmGlobalThis.Request = Request2;
    const resJson = useStep("__builtin_response_json");
    const resText = useStep("__builtin_response_text");
    const resArrayBuffer = useStep("__builtin_response_array_buffer");
    class Response2 {
      type;
      url;
      status;
      statusText;
      body;
      headers;
      redirected;
      constructor(body, init) {
        this.status = init?.status ?? 200;
        this.statusText = init?.statusText ?? "";
        this.headers = new vmGlobalThis.Headers(init?.headers);
        this.type = "default";
        this.url = "";
        this.redirected = false;
        if (body !== null && body !== void 0 && (this.status === 204 || this.status === 205 || this.status === 304)) {
          throw new TypeError(`Response constructor: Invalid response status code ${this.status}`);
        }
        if (body !== null && body !== void 0) {
          this.body = Object.create(vmGlobalThis.ReadableStream.prototype, {
            [BODY_INIT_SYMBOL]: {
              value: body,
              writable: false
            }
          });
        } else {
          this.body = null;
        }
      }
      // TODO: implement these
      clone;
      blob;
      formData;
      get ok() {
        return this.status >= 200 && this.status < 300;
      }
      get bodyUsed() {
        return false;
      }
      async arrayBuffer() {
        return resArrayBuffer(this);
      }
      async bytes() {
        return new Uint8Array(await resArrayBuffer(this));
      }
      async json() {
        return resJson(this);
      }
      static json(data, init) {
        const body = JSON.stringify(data);
        const headers = new vmGlobalThis.Headers(init?.headers);
        if (!headers.has("content-type")) {
          headers.set("content-type", "application/json");
        }
        return new Response2(body, { ...init, headers });
      }
      async text() {
        return resText(this);
      }
      static error() {
        ENOTSUP();
      }
      static redirect(url2, status = 302) {
        if (![301, 302, 303, 307, 308].includes(status)) {
          throw new RangeError(`Invalid redirect status code: ${status}. Must be one of: 301, 302, 303, 307, 308`);
        }
        const headers = new vmGlobalThis.Headers();
        headers.set("Location", String(url2));
        const response = Object.create(Response2.prototype);
        response.status = status;
        response.statusText = "";
        response.headers = headers;
        response.body = null;
        response.type = "default";
        response.url = "";
        response.redirected = false;
        return response;
      }
    }
    vmGlobalThis.Response = Response2;
    class ReadableStream2 {
      constructor() {
        ENOTSUP();
      }
      get locked() {
        return false;
      }
      cancel() {
        ENOTSUP();
      }
      getReader() {
        ENOTSUP();
      }
      pipeThrough() {
        ENOTSUP();
      }
      pipeTo() {
        ENOTSUP();
      }
      tee() {
        ENOTSUP();
      }
      values() {
        ENOTSUP();
      }
      static from() {
        ENOTSUP();
      }
      [Symbol.asyncIterator]() {
        ENOTSUP();
      }
    }
    vmGlobalThis.ReadableStream = ReadableStream2;
    class WritableStream2 {
      constructor() {
        ENOTSUP();
      }
      get locked() {
        return false;
      }
      abort() {
        ENOTSUP();
      }
      close() {
        ENOTSUP();
      }
      getWriter() {
        ENOTSUP();
      }
    }
    vmGlobalThis.WritableStream = WritableStream2;
    class TransformStream2 {
      readable;
      writable;
      constructor() {
        ENOTSUP();
      }
    }
    vmGlobalThis.TransformStream = TransformStream2;
    vmGlobalThis.console = globalThis.console;
    const SYMBOL_FOR_REQ_CONTEXT = Symbol.for("@vercel/request-context");
    vmGlobalThis[SYMBOL_FOR_REQ_CONTEXT] = globalThis[SYMBOL_FOR_REQ_CONTEXT];
    const parsedName = parseWorkflowName(workflowRun.workflowName);
    const filename = parsedName?.path || workflowRun.workflowName;
    const workflowFn = runInContext(`${workflowCode2}; globalThis.__private_workflows?.get(${JSON.stringify(workflowRun.workflowName)})`, context, { filename });
    if (typeof workflowFn !== "function") {
      throw new ReferenceError(`Workflow ${JSON.stringify(workflowRun.workflowName)} must be a function, but got "${typeof workflowFn}" instead`);
    }
    const args = hydrateWorkflowArguments(workflowRun.input, vmGlobalThis);
    span?.setAttributes({
      ...WorkflowArgumentsCount(args.length)
    });
    const result2 = await Promise.race([
      workflowFn(...args),
      workflowDiscontinuation.promise
    ]);
    const dehydrated = dehydrateWorkflowReturnValue(result2, vmGlobalThis);
    span?.setAttributes({
      ...WorkflowResultType(typeof result2)
    });
    return dehydrated;
  });
}
async function getHookByToken(token) {
  const world = getWorld();
  const hook = await world.hooks.getByToken(token);
  if (typeof hook.metadata !== "undefined") {
    hook.metadata = hydrateStepArguments(hook.metadata, [], hook.runId);
  }
  return hook;
}
async function resumeHook(tokenOrHook, payload) {
  return await waitedUntil(() => {
    return trace("HOOK.resume", async (span) => {
      const world = getWorld();
      try {
        const hook = typeof tokenOrHook === "string" ? await getHookByToken(tokenOrHook) : tokenOrHook;
        span?.setAttributes({
          ...HookToken(hook.token),
          ...HookId(hook.hookId),
          ...WorkflowRunId(hook.runId)
        });
        const ops = [];
        const dehydratedPayload = dehydrateStepReturnValue(payload, ops, hook.runId);
        functionsExports.waitUntil(Promise.all(ops).catch((err) => {
          if (err !== void 0)
            throw err;
        }));
        await world.events.create(hook.runId, {
          eventType: "hook_received",
          specVersion: SPEC_VERSION_CURRENT,
          correlationId: hook.hookId,
          eventData: {
            payload: dehydratedPayload
          }
        });
        const workflowRun = await world.runs.get(hook.runId);
        span?.setAttributes({
          ...WorkflowName(workflowRun.workflowName)
        });
        const traceCarrier = workflowRun.executionContext?.traceCarrier;
        if (traceCarrier) {
          const context = await getSpanContextForTraceCarrier(traceCarrier);
          if (context) {
            span?.addLink?.({ context });
          }
        }
        await world.queue(`__wkf_workflow_${workflowRun.workflowName}`, {
          runId: hook.runId,
          // attach the trace carrier from the workflow run
          traceCarrier: workflowRun.executionContext?.traceCarrier ?? void 0
        }, {
          deploymentId: workflowRun.deploymentId
        });
        return hook;
      } catch (err) {
        span?.setAttributes({
          ...HookToken(typeof tokenOrHook === "string" ? tokenOrHook : tokenOrHook.token),
          ...HookFound(false)
        });
        throw err;
      }
    });
  });
}
async function resumeWebhook(token, request) {
  const hook = await getHookByToken(token);
  let response;
  let responseReadable;
  if (hook.metadata && typeof hook.metadata === "object" && "respondWith" in hook.metadata) {
    if (hook.metadata.respondWith === "manual") {
      const { readable, writable } = new TransformStream();
      responseReadable = readable;
      request[WEBHOOK_RESPONSE_WRITABLE] = writable;
    } else if (hook.metadata.respondWith instanceof Response) {
      response = hook.metadata.respondWith;
    } else {
      throw new WorkflowRuntimeError(`Invalid \`respondWith\` value: ${hook.metadata.respondWith}`, { slug: ERROR_SLUGS.WEBHOOK_INVALID_RESPOND_WITH_VALUE });
    }
  } else {
    response = new Response(null, { status: 202 });
  }
  await resumeHook(hook, request);
  if (responseReadable) {
    const reader = responseReadable.getReader();
    const chunk = await reader.read();
    if (chunk.value) {
      response = chunk.value;
    }
    reader.cancel();
  }
  if (!response) {
    throw new WorkflowRuntimeError("Workflow run did not send a response", {
      slug: ERROR_SLUGS.WEBHOOK_RESPONSE_NOT_SENT
    });
  }
  return response;
}
const version = "4.0.1-beta.41";
async function start(workflow, argsOrOptions, options) {
  return await waitedUntil(() => {
    const workflowName = workflow?.workflowId;
    if (!workflowName) {
      throw new WorkflowRuntimeError(`'start' received an invalid workflow function. Ensure the Workflow Development Kit is configured correctly and the function includes a 'use workflow' directive.`, { slug: "start-invalid-workflow-function" });
    }
    return trace(`WORKFLOW.start ${workflowName}`, async (span) => {
      span?.setAttributes({
        ...WorkflowName(workflowName),
        ...WorkflowOperation("start")
      });
      let args = [];
      let opts = {};
      if (Array.isArray(argsOrOptions)) {
        args = argsOrOptions;
      } else if (typeof argsOrOptions === "object") {
        opts = argsOrOptions;
      }
      span?.setAttributes({
        ...WorkflowArgumentsCount(args.length)
      });
      const world = opts?.world ?? getWorld();
      const deploymentId = opts.deploymentId ?? await world.getDeploymentId();
      const ops = [];
      const { promise: runIdPromise, resolve: resolveRunId } = withResolvers();
      const traceCarrier = await serializeTraceCarrier();
      const workflowArguments = dehydrateWorkflowArguments(args, ops, runIdPromise);
      const result2 = await world.events.create(null, {
        eventType: "run_created",
        specVersion: SPEC_VERSION_CURRENT,
        eventData: {
          deploymentId,
          workflowName,
          input: workflowArguments,
          executionContext: { traceCarrier, workflowCoreVersion: version }
        }
      });
      if (!result2.run) {
        throw new WorkflowRuntimeError("Missing 'run' in server response for 'run_created' event");
      }
      const runId = result2.run.runId;
      resolveRunId(runId);
      functionsExports.waitUntil(Promise.all(ops).catch((err) => {
        const isAbortError = err?.name === "AbortError" || err?.name === "ResponseAborted";
        if (!isAbortError)
          throw err;
      }));
      span?.setAttributes({
        ...WorkflowRunId(runId),
        ...WorkflowRunStatus(result2.run.status),
        ...DeploymentId(deploymentId)
      });
      await world.queue(`__wkf_workflow_${workflowName}`, {
        runId,
        traceCarrier
      }, {
        deploymentId
      });
      return new Run(runId);
    });
  });
}
const DEFAULT_STEP_MAX_RETRIES = 3;
const stepHandler = getWorldHandlers().createQueueHandler("__wkf_step_", async (message_, metadata) => {
  const healthCheck = parseHealthCheckPayload(message_);
  if (healthCheck) {
    await handleHealthCheckMessage(healthCheck, "step");
    return;
  }
  const { workflowName, workflowRunId, workflowStartedAt, stepId, traceCarrier: traceContext, requestedAt } = StepInvokePayloadSchema.parse(message_);
  const spanLinks = await linkToCurrentContext();
  return await withTraceContext(traceContext, async () => {
    const stepName = metadata.queueName.slice("__wkf_step_".length);
    const world = getWorld();
    const port2 = await getPort();
    return trace(`STEP ${stepName}`, { kind: await getSpanKind("CONSUMER"), links: spanLinks }, async (span) => {
      span?.setAttributes({
        ...StepName(stepName),
        ...StepAttempt(metadata.attempt),
        ...QueueName(metadata.queueName),
        ...QueueMessageId(metadata.messageId),
        ...getQueueOverhead({ requestedAt })
      });
      const stepFn = getStepFunction(stepName);
      if (!stepFn) {
        throw new Error(`Step "${stepName}" not found`);
      }
      if (typeof stepFn !== "function") {
        throw new Error(`Step "${stepName}" is not a function (got ${typeof stepFn})`);
      }
      const maxRetries = stepFn.maxRetries ?? DEFAULT_STEP_MAX_RETRIES;
      span?.setAttributes({
        ...WorkflowName(workflowName),
        ...WorkflowRunId(workflowRunId),
        ...StepId(stepId),
        ...StepMaxRetries(maxRetries),
        ...StepTracePropagated(!!traceContext)
      });
      let step = await world.steps.get(workflowRunId, stepId);
      runtimeLogger.debug("Step execution details", {
        stepName,
        stepId: step.stepId,
        status: step.status,
        attempt: step.attempt
      });
      span?.setAttributes({
        ...StepStatus(step.status)
      });
      const now = Date.now();
      if (step.retryAfter && step.retryAfter.getTime() > now) {
        const timeoutSeconds = Math.ceil((step.retryAfter.getTime() - now) / 1e3);
        span?.setAttributes({
          ...StepRetryTimeoutSeconds(timeoutSeconds)
        });
        runtimeLogger.debug("Step retryAfter timestamp not yet reached", {
          stepName,
          stepId: step.stepId,
          retryAfter: step.retryAfter,
          timeoutSeconds
        });
        return { timeoutSeconds };
      }
      let result2;
      if (step.attempt > maxRetries + 1) {
        const retryCount = step.attempt - 1;
        const errorMessage = `Step "${stepName}" exceeded max retries (${retryCount} ${pluralize("retry", "retries", retryCount)})`;
        console.error(`[Workflows] "${workflowRunId}" - ${errorMessage}`);
        await world.events.create(workflowRunId, {
          eventType: "step_failed",
          specVersion: SPEC_VERSION_CURRENT,
          correlationId: stepId,
          eventData: {
            error: errorMessage,
            stack: step.error?.stack
          }
        });
        span?.setAttributes({
          ...StepStatus("failed"),
          ...StepRetryExhausted(true)
        });
        await queueMessage(world, `__wkf_workflow_${workflowName}`, {
          runId: workflowRunId,
          traceCarrier: await serializeTraceCarrier(),
          requestedAt: /* @__PURE__ */ new Date()
        });
        return;
      }
      try {
        if (!["pending", "running"].includes(step.status)) {
          console.error(`[Workflows] "${workflowRunId}" - Step invoked erroneously, expected status "pending" or "running", got "${step.status}" instead, skipping execution`);
          span?.setAttributes({
            ...StepSkipped(true),
            ...StepSkipReason(step.status)
          });
          const isTerminalStep = [
            "completed",
            "failed",
            "cancelled"
          ].includes(step.status);
          if (isTerminalStep) {
            await queueMessage(world, `__wkf_workflow_${workflowName}`, {
              runId: workflowRunId,
              traceCarrier: await serializeTraceCarrier(),
              requestedAt: /* @__PURE__ */ new Date()
            });
          }
          return;
        }
        const startResult = await world.events.create(workflowRunId, {
          eventType: "step_started",
          specVersion: SPEC_VERSION_CURRENT,
          correlationId: stepId
        });
        if (!startResult.step) {
          throw new WorkflowRuntimeError(`step_started event for "${stepId}" did not return step entity`);
        }
        step = startResult.step;
        const attempt = step.attempt;
        if (!step.startedAt) {
          throw new WorkflowRuntimeError(`Step "${stepId}" has no "startedAt" timestamp`);
        }
        const ops = [];
        const hydratedInput = hydrateStepArguments(step.input, ops, workflowRunId);
        const args = hydratedInput.args;
        const thisVal = hydratedInput.thisVal ?? null;
        span?.setAttributes({
          ...StepArgumentsCount(args.length)
        });
        result2 = await contextStorage.run({
          stepMetadata: {
            stepId,
            stepStartedAt: /* @__PURE__ */ new Date(+step.startedAt),
            attempt
          },
          workflowMetadata: {
            workflowRunId,
            workflowStartedAt: /* @__PURE__ */ new Date(+workflowStartedAt),
            // TODO: there should be a getUrl method on the world interface itself. This
            // solution only works for vercel + local worlds.
            url: process.env.VERCEL_URL ? `https://${process.env.VERCEL_URL}` : `http://localhost:${port2 ?? 3e3}`
          },
          ops,
          closureVars: hydratedInput.closureVars
        }, () => stepFn.apply(thisVal, args));
        result2 = dehydrateStepReturnValue(result2, ops, workflowRunId);
        functionsExports.waitUntil(Promise.all(ops).catch((err) => {
          const isAbortError = err?.name === "AbortError" || err?.name === "ResponseAborted";
          if (!isAbortError)
            throw err;
        }));
        await world.events.create(workflowRunId, {
          eventType: "step_completed",
          specVersion: SPEC_VERSION_CURRENT,
          correlationId: stepId,
          eventData: {
            result: result2
          }
        });
        span?.setAttributes({
          ...StepStatus("completed"),
          ...StepResultType(typeof result2)
        });
      } catch (err) {
        span?.setAttributes({
          ...StepErrorName(getErrorName(err)),
          ...StepErrorMessage(String(err))
        });
        if (WorkflowAPIError.is(err)) {
          if (err.status === 410) {
            console.warn(`Workflow run "${workflowRunId}" has already completed, skipping step "${stepId}": ${err.message}`);
            return;
          }
        }
        if (FatalError.is(err)) {
          const errorStack = getErrorStack(err);
          const stackLines = errorStack.split("\n").slice(0, 4);
          console.error(`[Workflows] "${workflowRunId}" - Encountered \`FatalError\` while executing step "${stepName}":
  > ${stackLines.join("\n    > ")}

Bubbling up error to parent workflow`);
          await world.events.create(workflowRunId, {
            eventType: "step_failed",
            specVersion: SPEC_VERSION_CURRENT,
            correlationId: stepId,
            eventData: {
              error: String(err),
              stack: errorStack
            }
          });
          span?.setAttributes({
            ...StepStatus("failed"),
            ...StepFatalError(true)
          });
        } else {
          const maxRetries2 = stepFn.maxRetries ?? DEFAULT_STEP_MAX_RETRIES;
          const currentAttempt = step.attempt;
          span?.setAttributes({
            ...StepAttempt(currentAttempt),
            ...StepMaxRetries(maxRetries2)
          });
          if (currentAttempt >= maxRetries2 + 1) {
            const errorStack = getErrorStack(err);
            const stackLines = errorStack.split("\n").slice(0, 4);
            const retryCount = step.attempt - 1;
            console.error(`[Workflows] "${workflowRunId}" - Encountered \`Error\` while executing step "${stepName}" (attempt ${step.attempt}, ${retryCount} ${pluralize("retry", "retries", retryCount)}):
  > ${stackLines.join("\n    > ")}

  Max retries reached
  Bubbling error to parent workflow`);
            const errorMessage = `Step "${stepName}" failed after ${maxRetries2} ${pluralize("retry", "retries", maxRetries2)}: ${String(err)}`;
            await world.events.create(workflowRunId, {
              eventType: "step_failed",
              specVersion: SPEC_VERSION_CURRENT,
              correlationId: stepId,
              eventData: {
                error: errorMessage,
                stack: errorStack
              }
            });
            span?.setAttributes({
              ...StepStatus("failed"),
              ...StepRetryExhausted(true)
            });
          } else {
            if (RetryableError.is(err)) {
              console.warn(`[Workflows] "${workflowRunId}" - Encountered \`RetryableError\` while executing step "${stepName}" (attempt ${currentAttempt}):
  > ${String(err.message)}

  This step has failed but will be retried`);
            } else {
              const stackLines = getErrorStack(err).split("\n").slice(0, 4);
              console.error(`[Workflows] "${workflowRunId}" - Encountered \`Error\` while executing step "${stepName}" (attempt ${currentAttempt}):
  > ${stackLines.join("\n    > ")}

  This step has failed but will be retried`);
            }
            const errorStack = getErrorStack(err);
            await world.events.create(workflowRunId, {
              eventType: "step_retrying",
              specVersion: SPEC_VERSION_CURRENT,
              correlationId: stepId,
              eventData: {
                error: String(err),
                stack: errorStack,
                ...RetryableError.is(err) && {
                  retryAfter: err.retryAfter
                }
              }
            });
            const timeoutSeconds = Math.max(1, RetryableError.is(err) ? Math.ceil((+err.retryAfter.getTime() - Date.now()) / 1e3) : 1);
            span?.setAttributes({
              ...StepRetryTimeoutSeconds(timeoutSeconds),
              ...StepRetryWillRetry(true)
            });
            return { timeoutSeconds };
          }
        }
      }
      await queueMessage(world, `__wkf_workflow_${workflowName}`, {
        runId: workflowRunId,
        traceCarrier: await serializeTraceCarrier(),
        requestedAt: /* @__PURE__ */ new Date()
      });
    });
  });
});
const stepEntrypoint = /* @__PURE__ */ withHealthCheck(stepHandler);
class Run {
  /**
   * The ID of the workflow run.
   */
  runId;
  /**
   * The world object.
   * @internal
   */
  world;
  constructor(runId) {
    this.runId = runId;
    this.world = getWorld();
  }
  /**
   * Cancels the workflow run.
   */
  async cancel() {
    await this.world.events.create(this.runId, {
      eventType: "run_cancelled",
      specVersion: SPEC_VERSION_CURRENT
    });
  }
  /**
   * The status of the workflow run.
   */
  get status() {
    return this.world.runs.get(this.runId).then((run) => run.status);
  }
  /**
   * The return value of the workflow run.
   * Polls the workflow return value until it is completed.
   */
  get returnValue() {
    return this.pollReturnValue();
  }
  /**
   * The name of the workflow.
   */
  get workflowName() {
    return this.world.runs.get(this.runId).then((run) => run.workflowName);
  }
  /**
   * The timestamp when the workflow run was created.
   */
  get createdAt() {
    return this.world.runs.get(this.runId).then((run) => run.createdAt);
  }
  /**
   * The timestamp when the workflow run started execution.
   * Returns undefined if the workflow has not started yet.
   */
  get startedAt() {
    return this.world.runs.get(this.runId).then((run) => run.startedAt);
  }
  /**
   * The timestamp when the workflow run completed.
   * Returns undefined if the workflow has not completed yet.
   */
  get completedAt() {
    return this.world.runs.get(this.runId).then((run) => run.completedAt);
  }
  /**
   * The readable stream of the workflow run.
   */
  get readable() {
    return this.getReadable();
  }
  /**
   * Retrieves the workflow run's default readable stream, which reads chunks
   * written to the corresponding writable stream {@link getWritable}.
   *
   * @param options - The options for the readable stream.
   * @returns The `ReadableStream` for the workflow run.
   */
  getReadable(options = {}) {
    const { ops = [], global = globalThis, startIndex, namespace } = options;
    const name = getWorkflowRunStreamId(this.runId, namespace);
    return getExternalRevivers(global, ops, this.runId).ReadableStream({
      name,
      startIndex
    });
  }
  /**
   * Polls the workflow return value every 1 second until it is completed.
   * @internal
   * @returns The workflow return value.
   */
  async pollReturnValue() {
    while (true) {
      try {
        const run = await this.world.runs.get(this.runId);
        if (run.status === "completed") {
          return hydrateWorkflowReturnValue(run.output, [], this.runId);
        }
        if (run.status === "cancelled") {
          throw new WorkflowRunCancelledError(this.runId);
        }
        if (run.status === "failed") {
          throw new WorkflowRunFailedError(this.runId, run.error);
        }
        throw new WorkflowRunNotCompletedError(this.runId, run.status);
      } catch (error) {
        if (WorkflowRunNotCompletedError.is(error)) {
          await new Promise((resolve2) => setTimeout(resolve2, 1e3));
          continue;
        }
        throw error;
      }
    }
  }
}
function workflowEntrypoint(workflowCode2) {
  const handler2 = getWorldHandlers().createQueueHandler("__wkf_workflow_", async (message_, metadata) => {
    const healthCheck = parseHealthCheckPayload(message_);
    if (healthCheck) {
      await handleHealthCheckMessage(healthCheck, "workflow");
      return;
    }
    const { runId, traceCarrier: traceContext, requestedAt } = WorkflowInvokePayloadSchema.parse(message_);
    const workflowName = metadata.queueName.slice("__wkf_workflow_".length);
    const spanLinks = await linkToCurrentContext();
    return await withTraceContext(traceContext, async () => {
      const world = getWorld();
      return trace(`WORKFLOW ${workflowName}`, { links: spanLinks }, async (span) => {
        span?.setAttributes({
          ...WorkflowName(workflowName),
          ...WorkflowOperation("execute"),
          ...QueueName(metadata.queueName),
          ...QueueMessageId(metadata.messageId),
          ...getQueueOverhead({ requestedAt })
        });
        span?.setAttributes({
          ...WorkflowRunId(runId),
          ...WorkflowTracePropagated(!!traceContext)
        });
        let workflowStartedAt = -1;
        try {
          let workflowRun = await world.runs.get(runId);
          if (workflowRun.status === "pending") {
            const result3 = await world.events.create(runId, {
              eventType: "run_started",
              specVersion: SPEC_VERSION_CURRENT
            });
            if (!result3.run) {
              throw new WorkflowRuntimeError(`Event creation for 'run_started' did not return the run entity for run "${runId}"`);
            }
            workflowRun = result3.run;
          }
          if (!workflowRun.startedAt) {
            throw new WorkflowRuntimeError(`Workflow run "${runId}" has no "startedAt" timestamp`);
          }
          workflowStartedAt = +workflowRun.startedAt;
          span?.setAttributes({
            ...WorkflowRunStatus(workflowRun.status),
            ...WorkflowStartedAt(workflowStartedAt)
          });
          if (workflowRun.status !== "running") {
            console.warn(`Workflow "${runId}" has status "${workflowRun.status}", skipping`);
            return;
          }
          const events = await getAllWorkflowRunEvents(workflowRun.runId);
          const now = Date.now();
          const completedWaitIds = new Set(events.filter((e) => e.eventType === "wait_completed").map((e) => e.correlationId));
          const waitsToComplete = events.filter((e) => e.eventType === "wait_created" && e.correlationId !== void 0 && !completedWaitIds.has(e.correlationId) && now >= e.eventData.resumeAt.getTime()).map((e) => ({
            eventType: "wait_completed",
            specVersion: SPEC_VERSION_CURRENT,
            correlationId: e.correlationId
          }));
          for (const waitEvent of waitsToComplete) {
            const result3 = await world.events.create(runId, waitEvent);
            events.push(result3.event);
          }
          const result2 = await runWorkflow(workflowCode2, workflowRun, events);
          await world.events.create(runId, {
            eventType: "run_completed",
            specVersion: SPEC_VERSION_CURRENT,
            eventData: {
              output: result2
            }
          });
          span?.setAttributes({
            ...WorkflowRunStatus("completed"),
            ...WorkflowEventsCount(events.length)
          });
        } catch (err) {
          if (WorkflowSuspension.is(err)) {
            const suspensionMessage = buildWorkflowSuspensionMessage(runId, err.stepCount, err.hookCount, err.waitCount);
            if (suspensionMessage) {
              runtimeLogger.debug(suspensionMessage);
            }
            const result2 = await handleSuspension({
              suspension: err,
              world,
              runId,
              workflowName,
              workflowStartedAt,
              span
            });
            if (result2.timeoutSeconds !== void 0) {
              return { timeoutSeconds: result2.timeoutSeconds };
            }
          } else {
            const errorName = getErrorName(err);
            const errorMessage = err instanceof Error ? err.message : String(err);
            let errorStack = getErrorStack(err);
            if (errorStack) {
              const parsedName = parseWorkflowName(workflowName);
              const filename = parsedName?.path || workflowName;
              errorStack = remapErrorStack(errorStack, filename, workflowCode2);
            }
            console.error(`${errorName} while running "${runId}" workflow:

${errorStack}`);
            await world.events.create(runId, {
              eventType: "run_failed",
              specVersion: SPEC_VERSION_CURRENT,
              eventData: {
                error: {
                  message: errorMessage,
                  stack: errorStack
                }
                // TODO: include error codes when we define them
              }
            });
            span?.setAttributes({
              ...WorkflowRunStatus("failed"),
              ...WorkflowErrorName(errorName),
              ...WorkflowErrorMessage(String(err))
            });
          }
        }
      });
    });
  });
  return withHealthCheck(handler2);
}
async function handler(request) {
  const url = new URL(request.url);
  const pathParts = url.pathname.split("/");
  const token = decodeURIComponent(pathParts[pathParts.length - 1]);
  if (!token) {
    return new Response("Missing token", { status: 400 });
  }
  try {
    const response = await resumeWebhook(token, request);
    return response;
  } catch (error) {
    console.error("Error during resumeWebhook", error);
    return new Response(null, { status: 404 });
  }
}
const POST$1 = handler;
const _n9RDGO = async ({ req }) => {
  try {
    return await POST$1(req);
  } catch (error) {
    console.error("Handler error:", error);
    return new Response("Internal Server Error", { status: 500 });
  }
};
var __create = Object.create;
var __defProp$1 = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name$1 = (target, value) => __defProp$1(target, "name", { value, configurable: true });
var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
  get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
}) : x)(function(x) {
  if (typeof require !== "undefined") return require.apply(this, arguments);
  throw Error('Dynamic require of "' + x + '" is not supported');
});
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __commonJS = (cb, mod) => function __require2() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp$1(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key2 of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key2) && key2 !== except)
        __defProp$1(to, key2, { get: () => from[key2], enumerable: !(desc = __getOwnPropDesc(from, key2)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  __defProp$1(target, "default", { value: mod, enumerable: true }),
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp$1({}, "__esModule", { value: true }), mod);
var ControlFlowError = (_a = class extends Error {
  stdout;
  stderr;
  constructor(message, stdout = "", stderr = "") {
    super(message);
    this.stdout = stdout;
    this.stderr = stderr;
  }
  /**
   * Prepend output from the current context before re-throwing.
   */
  prependOutput(stdout, stderr) {
    this.stdout = stdout + this.stdout;
    this.stderr = stderr + this.stderr;
  }
}, __name$1(_a, "ControlFlowError"), _a);
var BreakError = (_b = class extends ControlFlowError {
  levels;
  name = "BreakError";
  constructor(levels = 1, stdout = "", stderr = "") {
    super("break", stdout, stderr);
    this.levels = levels;
  }
}, __name$1(_b, "BreakError"), _b);
var ContinueError = (_c = class extends ControlFlowError {
  levels;
  name = "ContinueError";
  constructor(levels = 1, stdout = "", stderr = "") {
    super("continue", stdout, stderr);
    this.levels = levels;
  }
}, __name$1(_c, "ContinueError"), _c);
var ReturnError = (_d = class extends ControlFlowError {
  exitCode;
  name = "ReturnError";
  constructor(exitCode = 0, stdout = "", stderr = "") {
    super("return", stdout, stderr);
    this.exitCode = exitCode;
  }
}, __name$1(_d, "ReturnError"), _d);
var ErrexitError = (_e = class extends ControlFlowError {
  exitCode;
  name = "ErrexitError";
  constructor(exitCode, stdout = "", stderr = "") {
    super(`errexit: command exited with status ${exitCode}`, stdout, stderr);
    this.exitCode = exitCode;
  }
}, __name$1(_e, "ErrexitError"), _e);
var NounsetError = (_f = class extends ControlFlowError {
  varName;
  name = "NounsetError";
  constructor(varName, stdout = "") {
    super(`${varName}: unbound variable`, stdout, `bash: ${varName}: unbound variable
`);
    this.varName = varName;
  }
}, __name$1(_f, "NounsetError"), _f);
var ExitError = (_g = class extends ControlFlowError {
  exitCode;
  name = "ExitError";
  constructor(exitCode, stdout = "", stderr = "") {
    super(`exit`, stdout, stderr);
    this.exitCode = exitCode;
  }
}, __name$1(_g, "ExitError"), _g);
var ArithmeticError = (_h = class extends ControlFlowError {
  name = "ArithmeticError";
  /**
   * If true, this error should abort script execution (like missing operand after binary operator).
   * If false, the error is recoverable and execution can continue.
   */
  fatal;
  constructor(message, stdout = "", stderr = "", fatal = false) {
    super(message, stdout, stderr);
    this.stderr = stderr || `bash: ${message}
`;
    this.fatal = fatal;
  }
}, __name$1(_h, "ArithmeticError"), _h);
var BadSubstitutionError = (_i = class extends ControlFlowError {
  name = "BadSubstitutionError";
  constructor(message, stdout = "", stderr = "") {
    super(message, stdout, stderr);
    this.stderr = stderr || `bash: ${message}: bad substitution
`;
  }
}, __name$1(_i, "BadSubstitutionError"), _i);
var GlobError = (_j = class extends ControlFlowError {
  name = "GlobError";
  constructor(pattern, stdout = "", stderr = "") {
    super(`no match: ${pattern}`, stdout, stderr);
    this.stderr = stderr || `bash: no match: ${pattern}
`;
  }
}, __name$1(_j, "GlobError"), _j);
var BraceExpansionError = (_k = class extends ControlFlowError {
  name = "BraceExpansionError";
  constructor(message, stdout = "", stderr = "") {
    super(message, stdout, stderr);
    this.stderr = stderr || `bash: ${message}
`;
  }
}, __name$1(_k, "BraceExpansionError"), _k);
var ExecutionLimitError = (_l = class extends ControlFlowError {
  limitType;
  name = "ExecutionLimitError";
  constructor(message, limitType, stdout = "", stderr = "") {
    super(message, stdout, stderr);
    this.limitType = limitType;
    this.stderr = stderr || `bash: ${message}
`;
  }
}, __name$1(_l, "ExecutionLimitError"), __publicField(_l, "EXIT_CODE", 126), _l);
var SubshellExitError = (_m = class extends ControlFlowError {
  name = "SubshellExitError";
  constructor(stdout = "", stderr = "") {
    super("subshell exit", stdout, stderr);
  }
}, __name$1(_m, "SubshellExitError"), _m);
function isScopeExitError(error) {
  return error instanceof BreakError || error instanceof ContinueError || error instanceof ReturnError;
}
__name$1(isScopeExitError, "isScopeExitError");
var PosixFatalError = (_n = class extends ControlFlowError {
  exitCode;
  name = "PosixFatalError";
  constructor(exitCode, stdout = "", stderr = "") {
    super("posix fatal error", stdout, stderr);
    this.exitCode = exitCode;
  }
}, __name$1(_n, "PosixFatalError"), _n);
var DEFAULT_BATCH_SIZE = 100;
function skipArithWhitespace(input, pos) {
  while (pos < input.length) {
    if (input[pos] === "\\" && input[pos + 1] === "\n") {
      pos += 2;
      continue;
    }
    if (/\s/.test(input[pos])) {
      pos++;
      continue;
    }
    break;
  }
  return pos;
}
__name$1(skipArithWhitespace, "skipArithWhitespace");
var ARITH_ASSIGN_OPS = [
  "=",
  "+=",
  "-=",
  "*=",
  "/=",
  "%=",
  "<<=",
  ">>=",
  "&=",
  "|=",
  "^="
];
function parseArithNumber(str) {
  if (str.includes("#")) {
    const [baseStr, numStr] = str.split("#");
    const base = Number.parseInt(baseStr, 10);
    if (base < 2 || base > 64) {
      return Number.NaN;
    }
    if (base <= 36) {
      return Number.parseInt(numStr, base);
    }
    let result2 = 0;
    for (const ch of numStr) {
      let digitValue;
      if (/[0-9]/.test(ch)) {
        digitValue = ch.charCodeAt(0) - "0".charCodeAt(0);
      } else if (/[a-z]/.test(ch)) {
        digitValue = ch.charCodeAt(0) - "a".charCodeAt(0) + 10;
      } else if (/[A-Z]/.test(ch)) {
        digitValue = ch.charCodeAt(0) - "A".charCodeAt(0) + 36;
      } else if (ch === "@") {
        digitValue = 62;
      } else if (ch === "_") {
        digitValue = 63;
      } else {
        return Number.NaN;
      }
      if (digitValue >= base) {
        return Number.NaN;
      }
      result2 = result2 * base + digitValue;
    }
    return result2;
  }
  if (str.startsWith("0x") || str.startsWith("0X")) {
    return Number.parseInt(str.slice(2), 16);
  }
  if (str.startsWith("0") && str.length > 1 && /^[0-9]+$/.test(str)) {
    if (/[89]/.test(str)) {
      return Number.NaN;
    }
    return Number.parseInt(str, 8);
  }
  return Number.parseInt(str, 10);
}
__name$1(parseArithNumber, "parseArithNumber");
function parseNestedArithmetic(parseArithExpr2, p, input, currentPos) {
  if (input.slice(currentPos, currentPos + 3) !== "$((") {
    return null;
  }
  let pos = currentPos + 3;
  let depth = 1;
  const exprStart = pos;
  while (pos < input.length - 1 && depth > 0) {
    if (input[pos] === "(" && input[pos + 1] === "(") {
      depth++;
      pos += 2;
    } else if (input[pos] === ")" && input[pos + 1] === ")") {
      depth--;
      if (depth > 0)
        pos += 2;
    } else {
      pos++;
    }
  }
  const nestedExpr = input.slice(exprStart, pos);
  const { expr } = parseArithExpr2(p, nestedExpr, 0);
  pos += 2;
  return { expr: { type: "ArithNested", expression: expr }, pos };
}
__name$1(parseNestedArithmetic, "parseNestedArithmetic");
function parseAnsiCQuoting(input, currentPos) {
  if (input.slice(currentPos, currentPos + 2) !== "$'") {
    return null;
  }
  let pos = currentPos + 2;
  let content = "";
  while (pos < input.length && input[pos] !== "'") {
    if (input[pos] === "\\" && pos + 1 < input.length) {
      const nextChar = input[pos + 1];
      switch (nextChar) {
        case "n":
          content += "\n";
          break;
        case "t":
          content += "	";
          break;
        case "r":
          content += "\r";
          break;
        case "\\":
          content += "\\";
          break;
        case "'":
          content += "'";
          break;
        default:
          content += nextChar;
      }
      pos += 2;
    } else {
      content += input[pos];
      pos++;
    }
  }
  if (input[pos] === "'")
    pos++;
  const numValue = Number.parseInt(content, 10);
  return {
    expr: {
      type: "ArithNumber",
      value: Number.isNaN(numValue) ? 0 : numValue
    },
    pos
  };
}
__name$1(parseAnsiCQuoting, "parseAnsiCQuoting");
function parseLocalizationQuoting(input, currentPos) {
  if (input.slice(currentPos, currentPos + 2) !== '$"') {
    return null;
  }
  let pos = currentPos + 2;
  let content = "";
  while (pos < input.length && input[pos] !== '"') {
    if (input[pos] === "\\" && pos + 1 < input.length) {
      content += input[pos + 1];
      pos += 2;
    } else {
      content += input[pos];
      pos++;
    }
  }
  if (input[pos] === '"')
    pos++;
  const numValue = Number.parseInt(content, 10);
  return {
    expr: {
      type: "ArithNumber",
      value: Number.isNaN(numValue) ? 0 : numValue
    },
    pos
  };
}
__name$1(parseLocalizationQuoting, "parseLocalizationQuoting");
function preprocessArithInput(input) {
  let result2 = "";
  let i = 0;
  while (i < input.length) {
    if (input[i] === '"') {
      i++;
      while (i < input.length && input[i] !== '"') {
        if (input[i] === "\\" && i + 1 < input.length) {
          result2 += input[i + 1];
          i += 2;
        } else {
          result2 += input[i];
          i++;
        }
      }
      if (i < input.length)
        i++;
    } else {
      result2 += input[i];
      i++;
    }
  }
  return result2;
}
__name$1(preprocessArithInput, "preprocessArithInput");
function parseArithmeticExpression(_p2, input) {
  const preprocessed = preprocessArithInput(input);
  const { expr: expression, pos } = parseArithExpr(_p2, preprocessed, 0);
  const finalPos = skipArithWhitespace(preprocessed, pos);
  if (finalPos < preprocessed.length) {
    const remaining = input.slice(finalPos).trim();
    if (remaining) {
      return {
        type: "ArithmeticExpression",
        originalText: input,
        expression: {
          type: "ArithSyntaxError",
          errorToken: remaining,
          message: `${remaining}: syntax error: invalid arithmetic operator (error token is "${remaining}")`
        }
      };
    }
  }
  return { type: "ArithmeticExpression", expression, originalText: input };
}
__name$1(parseArithmeticExpression, "parseArithmeticExpression");
function makeMissingOperandError(op, pos) {
  return {
    expr: {
      type: "ArithSyntaxError",
      errorToken: op,
      message: `syntax error: operand expected (error token is "${op}")`
    },
    pos
  };
}
__name$1(makeMissingOperandError, "makeMissingOperandError");
function isMissingOperand(input, pos) {
  return skipArithWhitespace(input, pos) >= input.length;
}
__name$1(isMissingOperand, "isMissingOperand");
function parseArithExpr(p, input, pos) {
  return parseArithComma(p, input, pos);
}
__name$1(parseArithExpr, "parseArithExpr");
function parseArithComma(p, input, pos) {
  let { expr: left, pos: currentPos } = parseArithTernary(p, input, pos);
  currentPos = skipArithWhitespace(input, currentPos);
  while (input[currentPos] === ",") {
    const op = ",";
    currentPos++;
    if (isMissingOperand(input, currentPos)) {
      return makeMissingOperandError(op, currentPos);
    }
    const { expr: right, pos: p2 } = parseArithTernary(p, input, currentPos);
    left = { type: "ArithBinary", operator: ",", left, right };
    currentPos = skipArithWhitespace(input, p2);
  }
  return { expr: left, pos: currentPos };
}
__name$1(parseArithComma, "parseArithComma");
function parseArithTernary(p, input, pos) {
  let { expr: condition, pos: currentPos } = parseArithLogicalOr(p, input, pos);
  currentPos = skipArithWhitespace(input, currentPos);
  if (input[currentPos] === "?") {
    currentPos++;
    const { expr: consequent, pos: p2 } = parseArithExpr(p, input, currentPos);
    currentPos = skipArithWhitespace(input, p2);
    if (input[currentPos] === ":") {
      currentPos++;
      const { expr: alternate, pos: p3 } = parseArithExpr(p, input, currentPos);
      return {
        expr: { type: "ArithTernary", condition, consequent, alternate },
        pos: p3
      };
    }
  }
  return { expr: condition, pos: currentPos };
}
__name$1(parseArithTernary, "parseArithTernary");
function parseArithLogicalOr(p, input, pos) {
  let { expr: left, pos: currentPos } = parseArithLogicalAnd(p, input, pos);
  while (true) {
    currentPos = skipArithWhitespace(input, currentPos);
    if (input.slice(currentPos, currentPos + 2) === "||") {
      const op = "||";
      currentPos += 2;
      if (isMissingOperand(input, currentPos)) {
        return makeMissingOperandError(op, currentPos);
      }
      const { expr: right, pos: p2 } = parseArithLogicalAnd(p, input, currentPos);
      left = { type: "ArithBinary", operator: "||", left, right };
      currentPos = p2;
    } else {
      break;
    }
  }
  return { expr: left, pos: currentPos };
}
__name$1(parseArithLogicalOr, "parseArithLogicalOr");
function parseArithLogicalAnd(p, input, pos) {
  let { expr: left, pos: currentPos } = parseArithBitwiseOr(p, input, pos);
  while (true) {
    currentPos = skipArithWhitespace(input, currentPos);
    if (input.slice(currentPos, currentPos + 2) === "&&") {
      const op = "&&";
      currentPos += 2;
      if (isMissingOperand(input, currentPos)) {
        return makeMissingOperandError(op, currentPos);
      }
      const { expr: right, pos: p2 } = parseArithBitwiseOr(p, input, currentPos);
      left = { type: "ArithBinary", operator: "&&", left, right };
      currentPos = p2;
    } else {
      break;
    }
  }
  return { expr: left, pos: currentPos };
}
__name$1(parseArithLogicalAnd, "parseArithLogicalAnd");
function parseArithBitwiseOr(p, input, pos) {
  let { expr: left, pos: currentPos } = parseArithBitwiseXor(p, input, pos);
  while (true) {
    currentPos = skipArithWhitespace(input, currentPos);
    if (input[currentPos] === "|" && input[currentPos + 1] !== "|") {
      const op = "|";
      currentPos++;
      if (isMissingOperand(input, currentPos)) {
        return makeMissingOperandError(op, currentPos);
      }
      const { expr: right, pos: p2 } = parseArithBitwiseXor(p, input, currentPos);
      left = { type: "ArithBinary", operator: "|", left, right };
      currentPos = p2;
    } else {
      break;
    }
  }
  return { expr: left, pos: currentPos };
}
__name$1(parseArithBitwiseOr, "parseArithBitwiseOr");
function parseArithBitwiseXor(p, input, pos) {
  let { expr: left, pos: currentPos } = parseArithBitwiseAnd(p, input, pos);
  while (true) {
    currentPos = skipArithWhitespace(input, currentPos);
    if (input[currentPos] === "^") {
      const op = "^";
      currentPos++;
      if (isMissingOperand(input, currentPos)) {
        return makeMissingOperandError(op, currentPos);
      }
      const { expr: right, pos: p2 } = parseArithBitwiseAnd(p, input, currentPos);
      left = { type: "ArithBinary", operator: "^", left, right };
      currentPos = p2;
    } else {
      break;
    }
  }
  return { expr: left, pos: currentPos };
}
__name$1(parseArithBitwiseXor, "parseArithBitwiseXor");
function parseArithBitwiseAnd(p, input, pos) {
  let { expr: left, pos: currentPos } = parseArithEquality(p, input, pos);
  while (true) {
    currentPos = skipArithWhitespace(input, currentPos);
    if (input[currentPos] === "&" && input[currentPos + 1] !== "&") {
      const op = "&";
      currentPos++;
      if (isMissingOperand(input, currentPos)) {
        return makeMissingOperandError(op, currentPos);
      }
      const { expr: right, pos: p2 } = parseArithEquality(p, input, currentPos);
      left = { type: "ArithBinary", operator: "&", left, right };
      currentPos = p2;
    } else {
      break;
    }
  }
  return { expr: left, pos: currentPos };
}
__name$1(parseArithBitwiseAnd, "parseArithBitwiseAnd");
function parseArithEquality(p, input, pos) {
  let { expr: left, pos: currentPos } = parseArithRelational(p, input, pos);
  while (true) {
    currentPos = skipArithWhitespace(input, currentPos);
    if (input.slice(currentPos, currentPos + 2) === "==" || input.slice(currentPos, currentPos + 2) === "!=") {
      const op = input.slice(currentPos, currentPos + 2);
      currentPos += 2;
      if (isMissingOperand(input, currentPos)) {
        return makeMissingOperandError(op, currentPos);
      }
      const { expr: right, pos: p2 } = parseArithRelational(p, input, currentPos);
      left = { type: "ArithBinary", operator: op, left, right };
      currentPos = p2;
    } else {
      break;
    }
  }
  return { expr: left, pos: currentPos };
}
__name$1(parseArithEquality, "parseArithEquality");
function parseArithRelational(p, input, pos) {
  let { expr: left, pos: currentPos } = parseArithShift(p, input, pos);
  while (true) {
    currentPos = skipArithWhitespace(input, currentPos);
    if (input.slice(currentPos, currentPos + 2) === "<=" || input.slice(currentPos, currentPos + 2) === ">=") {
      const op = input.slice(currentPos, currentPos + 2);
      currentPos += 2;
      if (isMissingOperand(input, currentPos)) {
        return makeMissingOperandError(op, currentPos);
      }
      const { expr: right, pos: p2 } = parseArithShift(p, input, currentPos);
      left = { type: "ArithBinary", operator: op, left, right };
      currentPos = p2;
    } else if (input[currentPos] === "<" || input[currentPos] === ">") {
      const op = input[currentPos];
      currentPos++;
      if (isMissingOperand(input, currentPos)) {
        return makeMissingOperandError(op, currentPos);
      }
      const { expr: right, pos: p2 } = parseArithShift(p, input, currentPos);
      left = { type: "ArithBinary", operator: op, left, right };
      currentPos = p2;
    } else {
      break;
    }
  }
  return { expr: left, pos: currentPos };
}
__name$1(parseArithRelational, "parseArithRelational");
function parseArithShift(p, input, pos) {
  let { expr: left, pos: currentPos } = parseArithAdditive(p, input, pos);
  while (true) {
    currentPos = skipArithWhitespace(input, currentPos);
    if (input.slice(currentPos, currentPos + 2) === "<<" || input.slice(currentPos, currentPos + 2) === ">>") {
      const op = input.slice(currentPos, currentPos + 2);
      currentPos += 2;
      if (isMissingOperand(input, currentPos)) {
        return makeMissingOperandError(op, currentPos);
      }
      const { expr: right, pos: p2 } = parseArithAdditive(p, input, currentPos);
      left = { type: "ArithBinary", operator: op, left, right };
      currentPos = p2;
    } else {
      break;
    }
  }
  return { expr: left, pos: currentPos };
}
__name$1(parseArithShift, "parseArithShift");
function parseArithAdditive(p, input, pos) {
  let { expr: left, pos: currentPos } = parseArithMultiplicative(p, input, pos);
  while (true) {
    currentPos = skipArithWhitespace(input, currentPos);
    if ((input[currentPos] === "+" || input[currentPos] === "-") && input[currentPos + 1] !== input[currentPos]) {
      const op = input[currentPos];
      currentPos++;
      if (isMissingOperand(input, currentPos)) {
        return makeMissingOperandError(op, currentPos);
      }
      const { expr: right, pos: p2 } = parseArithMultiplicative(p, input, currentPos);
      left = { type: "ArithBinary", operator: op, left, right };
      currentPos = p2;
    } else {
      break;
    }
  }
  return { expr: left, pos: currentPos };
}
__name$1(parseArithAdditive, "parseArithAdditive");
function parseArithMultiplicative(p, input, pos) {
  let { expr: left, pos: currentPos } = parseArithPower(p, input, pos);
  while (true) {
    currentPos = skipArithWhitespace(input, currentPos);
    if (input[currentPos] === "*" && input[currentPos + 1] !== "*") {
      const op = "*";
      currentPos++;
      if (isMissingOperand(input, currentPos)) {
        return makeMissingOperandError(op, currentPos);
      }
      const { expr: right, pos: p2 } = parseArithPower(p, input, currentPos);
      left = { type: "ArithBinary", operator: "*", left, right };
      currentPos = p2;
    } else if (input[currentPos] === "/" || input[currentPos] === "%") {
      const op = input[currentPos];
      currentPos++;
      if (isMissingOperand(input, currentPos)) {
        return makeMissingOperandError(op, currentPos);
      }
      const { expr: right, pos: p2 } = parseArithPower(p, input, currentPos);
      left = { type: "ArithBinary", operator: op, left, right };
      currentPos = p2;
    } else {
      break;
    }
  }
  return { expr: left, pos: currentPos };
}
__name$1(parseArithMultiplicative, "parseArithMultiplicative");
function parseArithPower(p, input, pos) {
  const { expr: base, pos: currentPos } = parseArithUnary(p, input, pos);
  let p2 = skipArithWhitespace(input, currentPos);
  if (input.slice(p2, p2 + 2) === "**") {
    const op = "**";
    p2 += 2;
    if (isMissingOperand(input, p2)) {
      return makeMissingOperandError(op, p2);
    }
    const { expr: exponent, pos: p3 } = parseArithPower(p, input, p2);
    return {
      expr: {
        type: "ArithBinary",
        operator: "**",
        left: base,
        right: exponent
      },
      pos: p3
    };
  }
  return { expr: base, pos: currentPos };
}
__name$1(parseArithPower, "parseArithPower");
function parseArithUnary(p, input, pos) {
  let currentPos = skipArithWhitespace(input, pos);
  if (input.slice(currentPos, currentPos + 2) === "++" || input.slice(currentPos, currentPos + 2) === "--") {
    const op = input.slice(currentPos, currentPos + 2);
    currentPos += 2;
    const { expr: operand, pos: p2 } = parseArithUnary(p, input, currentPos);
    return {
      expr: { type: "ArithUnary", operator: op, operand, prefix: true },
      pos: p2
    };
  }
  if (input[currentPos] === "+" || input[currentPos] === "-" || input[currentPos] === "!" || input[currentPos] === "~") {
    const op = input[currentPos];
    currentPos++;
    const { expr: operand, pos: p2 } = parseArithUnary(p, input, currentPos);
    return {
      expr: { type: "ArithUnary", operator: op, operand, prefix: true },
      pos: p2
    };
  }
  return parseArithPostfix(p, input, currentPos);
}
__name$1(parseArithUnary, "parseArithUnary");
function canStartConcatPrimary(input, pos) {
  const c = input[pos];
  if (c === "$")
    return true;
  if (c === "`")
    return true;
  return false;
}
__name$1(canStartConcatPrimary, "canStartConcatPrimary");
function parseArithPostfix(p, input, pos) {
  let { expr, pos: currentPos } = parseArithPrimary(p, input, pos, false);
  const parts = [expr];
  while (canStartConcatPrimary(input, currentPos)) {
    const { expr: nextExpr, pos: nextPos } = parseArithPrimary(p, input, currentPos, true);
    parts.push(nextExpr);
    currentPos = nextPos;
  }
  if (parts.length > 1) {
    expr = { type: "ArithConcat", parts };
  }
  let subscript;
  if (input[currentPos] === "[" && expr.type === "ArithConcat") {
    currentPos++;
    const { expr: indexExpr, pos: p2 } = parseArithExpr(p, input, currentPos);
    subscript = indexExpr;
    currentPos = p2;
    if (input[currentPos] === "]")
      currentPos++;
  }
  if (subscript && expr.type === "ArithConcat") {
    expr = { type: "ArithDynamicElement", nameExpr: expr, subscript };
    subscript = void 0;
  }
  currentPos = skipArithWhitespace(input, currentPos);
  if (expr.type === "ArithConcat" || expr.type === "ArithVariable" || expr.type === "ArithDynamicElement") {
    for (const op of ARITH_ASSIGN_OPS) {
      if (input.slice(currentPos, currentPos + op.length) === op && input.slice(currentPos, currentPos + op.length + 1) !== "==") {
        currentPos += op.length;
        const { expr: value, pos: p2 } = parseArithTernary(p, input, currentPos);
        if (expr.type === "ArithDynamicElement") {
          return {
            expr: {
              type: "ArithDynamicAssignment",
              operator: op,
              target: expr.nameExpr,
              subscript: expr.subscript,
              value
            },
            pos: p2
          };
        }
        if (expr.type === "ArithConcat") {
          return {
            expr: {
              type: "ArithDynamicAssignment",
              operator: op,
              target: expr,
              value
            },
            pos: p2
          };
        }
        return {
          expr: {
            type: "ArithAssignment",
            operator: op,
            variable: expr.name,
            value
          },
          pos: p2
        };
      }
    }
  }
  if (input.slice(currentPos, currentPos + 2) === "++" || input.slice(currentPos, currentPos + 2) === "--") {
    const op = input.slice(currentPos, currentPos + 2);
    currentPos += 2;
    return {
      expr: {
        type: "ArithUnary",
        operator: op,
        operand: expr,
        prefix: false
      },
      pos: currentPos
    };
  }
  return { expr, pos: currentPos };
}
__name$1(parseArithPostfix, "parseArithPostfix");
function parseArithPrimary(p, input, pos, skipAssignment = false) {
  let currentPos = skipArithWhitespace(input, pos);
  const nestedResult = parseNestedArithmetic(parseArithExpr, p, input, currentPos);
  if (nestedResult)
    return nestedResult;
  const ansiResult = parseAnsiCQuoting(input, currentPos);
  if (ansiResult)
    return ansiResult;
  const locResult = parseLocalizationQuoting(input, currentPos);
  if (locResult)
    return locResult;
  if (input.slice(currentPos, currentPos + 2) === "$(" && input[currentPos + 2] !== "(") {
    currentPos += 2;
    let depth = 1;
    const cmdStart = currentPos;
    while (currentPos < input.length && depth > 0) {
      if (input[currentPos] === "(")
        depth++;
      else if (input[currentPos] === ")")
        depth--;
      if (depth > 0)
        currentPos++;
    }
    const cmd = input.slice(cmdStart, currentPos);
    currentPos++;
    return {
      expr: { type: "ArithCommandSubst", command: cmd },
      pos: currentPos
    };
  }
  if (input[currentPos] === "`") {
    currentPos++;
    const cmdStart = currentPos;
    while (currentPos < input.length && input[currentPos] !== "`") {
      currentPos++;
    }
    const cmd = input.slice(cmdStart, currentPos);
    if (input[currentPos] === "`")
      currentPos++;
    return {
      expr: { type: "ArithCommandSubst", command: cmd },
      pos: currentPos
    };
  }
  if (input[currentPos] === "(") {
    currentPos++;
    const { expr, pos: p2 } = parseArithExpr(p, input, currentPos);
    currentPos = skipArithWhitespace(input, p2);
    if (input[currentPos] === ")")
      currentPos++;
    return { expr: { type: "ArithGroup", expression: expr }, pos: currentPos };
  }
  if (input[currentPos] === "'") {
    currentPos++;
    let content = "";
    while (currentPos < input.length && input[currentPos] !== "'") {
      content += input[currentPos];
      currentPos++;
    }
    if (input[currentPos] === "'")
      currentPos++;
    const numValue = Number.parseInt(content, 10);
    return {
      expr: {
        type: "ArithSingleQuote",
        content,
        value: Number.isNaN(numValue) ? 0 : numValue
      },
      pos: currentPos
    };
  }
  if (input[currentPos] === '"') {
    currentPos++;
    let content = "";
    while (currentPos < input.length && input[currentPos] !== '"') {
      if (input[currentPos] === "\\" && currentPos + 1 < input.length) {
        content += input[currentPos + 1];
        currentPos += 2;
      } else {
        content += input[currentPos];
        currentPos++;
      }
    }
    if (input[currentPos] === '"')
      currentPos++;
    const trimmed = content.trim();
    if (!trimmed) {
      return { expr: { type: "ArithNumber", value: 0 }, pos: currentPos };
    }
    const { expr } = parseArithExpr(p, trimmed, 0);
    return { expr, pos: currentPos };
  }
  if (/[0-9]/.test(input[currentPos])) {
    let numStr = "";
    let seenHash = false;
    let isHex = false;
    while (currentPos < input.length) {
      const ch = input[currentPos];
      if (seenHash) {
        if (/[0-9a-zA-Z@_]/.test(ch)) {
          numStr += ch;
          currentPos++;
        } else {
          break;
        }
      } else if (ch === "#") {
        seenHash = true;
        numStr += ch;
        currentPos++;
      } else if (numStr === "0" && (ch === "x" || ch === "X") && currentPos + 1 < input.length && /[0-9a-fA-F]/.test(input[currentPos + 1])) {
        isHex = true;
        numStr += ch;
        currentPos++;
      } else if (isHex && /[0-9a-fA-F]/.test(ch)) {
        numStr += ch;
        currentPos++;
      } else if (!isHex && /[0-9]/.test(ch)) {
        numStr += ch;
        currentPos++;
      } else {
        break;
      }
    }
    if (currentPos < input.length && /[a-zA-Z_]/.test(input[currentPos])) {
      let invalidToken = numStr;
      while (currentPos < input.length && /[a-zA-Z0-9_]/.test(input[currentPos])) {
        invalidToken += input[currentPos];
        currentPos++;
      }
      return {
        expr: {
          type: "ArithSyntaxError",
          errorToken: invalidToken,
          message: `${invalidToken}: value too great for base (error token is "${invalidToken}")`
        },
        pos: currentPos
      };
    }
    if (input[currentPos] === "." && /[0-9]/.test(input[currentPos + 1])) {
      throw new ArithmeticError(`${numStr}.${input[currentPos + 1]}...: syntax error: invalid arithmetic operator`);
    }
    if (input[currentPos] === "[") {
      const errorToken = input.slice(currentPos).trim();
      return {
        expr: {
          type: "ArithNumberSubscript",
          number: numStr,
          errorToken
        },
        pos: input.length
        // Consume the rest
      };
    }
    const value = parseArithNumber(numStr);
    return { expr: { type: "ArithNumber", value }, pos: currentPos };
  }
  if (input[currentPos] === "$" && input[currentPos + 1] === "{") {
    const braceStart = currentPos + 2;
    let braceDepth = 1;
    let i = braceStart;
    while (i < input.length && braceDepth > 0) {
      if (input[i] === "{")
        braceDepth++;
      else if (input[i] === "}")
        braceDepth--;
      if (braceDepth > 0)
        i++;
    }
    const content = input.slice(braceStart, i);
    const afterBrace = i + 1;
    if (input[afterBrace] === "#") {
      let valueEnd = afterBrace + 1;
      while (valueEnd < input.length && /[0-9a-zA-Z@_]/.test(input[valueEnd])) {
        valueEnd++;
      }
      const valueStr = input.slice(afterBrace + 1, valueEnd);
      return {
        expr: { type: "ArithDynamicBase", baseExpr: content, value: valueStr },
        pos: valueEnd
      };
    }
    if (/[0-9]/.test(input[afterBrace]) || input[afterBrace] === "x" || input[afterBrace] === "X") {
      let numEnd = afterBrace;
      if (input[afterBrace] === "x" || input[afterBrace] === "X") {
        numEnd++;
        while (numEnd < input.length && /[0-9a-fA-F]/.test(input[numEnd])) {
          numEnd++;
        }
      } else {
        while (numEnd < input.length && /[0-9]/.test(input[numEnd])) {
          numEnd++;
        }
      }
      const suffix = input.slice(afterBrace, numEnd);
      return {
        expr: { type: "ArithDynamicNumber", prefix: content, suffix },
        pos: numEnd
      };
    }
    currentPos = afterBrace;
    return { expr: { type: "ArithBracedExpansion", content }, pos: currentPos };
  }
  if (input[currentPos] === "$" && currentPos + 1 < input.length && /[0-9]/.test(input[currentPos + 1])) {
    currentPos++;
    let name = "";
    while (currentPos < input.length && /[0-9]/.test(input[currentPos])) {
      name += input[currentPos];
      currentPos++;
    }
    return {
      expr: { type: "ArithVariable", name, hasDollarPrefix: true },
      pos: currentPos
    };
  }
  if (input[currentPos] === "$" && currentPos + 1 < input.length && /[*@#?\-!$]/.test(input[currentPos + 1])) {
    const name = input[currentPos + 1];
    currentPos += 2;
    return { expr: { type: "ArithSpecialVar", name }, pos: currentPos };
  }
  let hasDollarPrefix = false;
  if (input[currentPos] === "$" && currentPos + 1 < input.length && /[a-zA-Z_]/.test(input[currentPos + 1])) {
    hasDollarPrefix = true;
    currentPos++;
  }
  if (currentPos < input.length && /[a-zA-Z_]/.test(input[currentPos])) {
    let name = "";
    while (currentPos < input.length && /[a-zA-Z0-9_]/.test(input[currentPos])) {
      name += input[currentPos];
      currentPos++;
    }
    if (input[currentPos] === "[" && !skipAssignment) {
      currentPos++;
      let stringKey;
      if (input[currentPos] === "'" || input[currentPos] === '"') {
        const quote = input[currentPos];
        currentPos++;
        stringKey = "";
        while (currentPos < input.length && input[currentPos] !== quote) {
          stringKey += input[currentPos];
          currentPos++;
        }
        if (input[currentPos] === quote)
          currentPos++;
        currentPos = skipArithWhitespace(input, currentPos);
        if (input[currentPos] === "]")
          currentPos++;
      }
      let indexExpr;
      if (stringKey === void 0) {
        const { expr, pos: p2 } = parseArithExpr(p, input, currentPos);
        indexExpr = expr;
        currentPos = p2;
        if (input[currentPos] === "]")
          currentPos++;
      }
      currentPos = skipArithWhitespace(input, currentPos);
      if (input[currentPos] === "[" && indexExpr) {
        return {
          expr: { type: "ArithDoubleSubscript", array: name, index: indexExpr },
          pos: currentPos
        };
      }
      if (!skipAssignment) {
        for (const op of ARITH_ASSIGN_OPS) {
          if (input.slice(currentPos, currentPos + op.length) === op && input.slice(currentPos, currentPos + op.length + 1) !== "==") {
            currentPos += op.length;
            const { expr: value, pos: p2 } = parseArithTernary(p, input, currentPos);
            return {
              expr: {
                type: "ArithAssignment",
                operator: op,
                variable: name,
                subscript: indexExpr,
                stringKey,
                value
              },
              pos: p2
            };
          }
        }
      }
      return {
        expr: {
          type: "ArithArrayElement",
          array: name,
          index: indexExpr,
          stringKey
        },
        pos: currentPos
      };
    }
    currentPos = skipArithWhitespace(input, currentPos);
    if (!skipAssignment) {
      for (const op of ARITH_ASSIGN_OPS) {
        if (input.slice(currentPos, currentPos + op.length) === op && input.slice(currentPos, currentPos + op.length + 1) !== "==") {
          currentPos += op.length;
          const { expr: value, pos: p2 } = parseArithTernary(p, input, currentPos);
          return {
            expr: {
              type: "ArithAssignment",
              operator: op,
              variable: name,
              value
            },
            pos: p2
          };
        }
      }
    }
    return {
      expr: { type: "ArithVariable", name, hasDollarPrefix },
      pos: currentPos
    };
  }
  if (input[currentPos] === "#") {
    let errorEnd = currentPos + 1;
    while (errorEnd < input.length && input[errorEnd] !== "\n") {
      errorEnd++;
    }
    const errorToken = input.slice(currentPos, errorEnd).trim() || "#";
    return {
      expr: {
        type: "ArithSyntaxError",
        errorToken,
        message: `${errorToken}: syntax error: invalid arithmetic operator (error token is "${errorToken}")`
      },
      pos: input.length
      // Consume the rest
    };
  }
  return { expr: { type: "ArithNumber", value: 0 }, pos: currentPos };
}
__name$1(parseArithPrimary, "parseArithPrimary");
var AST = {
  script(statements) {
    return { type: "Script", statements };
  },
  statement(pipelines, operators = [], background = false, deferredError, sourceText) {
    const node = {
      type: "Statement",
      pipelines,
      operators,
      background
    };
    if (deferredError) {
      node.deferredError = deferredError;
    }
    if (sourceText !== void 0) {
      node.sourceText = sourceText;
    }
    return node;
  },
  pipeline(commands, negated = false, timed = false, timePosix = false, pipeStderr) {
    return {
      type: "Pipeline",
      commands,
      negated,
      timed,
      timePosix,
      pipeStderr
    };
  },
  simpleCommand(name, args = [], assignments = [], redirections = []) {
    return { type: "SimpleCommand", name, args, assignments, redirections };
  },
  word(parts) {
    return { type: "Word", parts };
  },
  literal(value) {
    return { type: "Literal", value };
  },
  singleQuoted(value) {
    return { type: "SingleQuoted", value };
  },
  doubleQuoted(parts) {
    return { type: "DoubleQuoted", parts };
  },
  escaped(value) {
    return { type: "Escaped", value };
  },
  parameterExpansion(parameter, operation = null) {
    return { type: "ParameterExpansion", parameter, operation };
  },
  commandSubstitution(body, legacy = false) {
    return { type: "CommandSubstitution", body, legacy };
  },
  arithmeticExpansion(expression) {
    return { type: "ArithmeticExpansion", expression };
  },
  assignment(name, value, append = false, array2 = null) {
    return { type: "Assignment", name, value, append, array: array2 };
  },
  redirection(operator, target, fd = null, fdVariable) {
    const node = { type: "Redirection", fd, operator, target };
    if (fdVariable) {
      node.fdVariable = fdVariable;
    }
    return node;
  },
  hereDoc(delimiter, content, stripTabs = false, quoted = false) {
    return { type: "HereDoc", delimiter, content, stripTabs, quoted };
  },
  ifNode(clauses, elseBody = null, redirections = []) {
    return { type: "If", clauses, elseBody, redirections };
  },
  forNode(variable, words, body, redirections = []) {
    return { type: "For", variable, words, body, redirections };
  },
  whileNode(condition, body, redirections = []) {
    return { type: "While", condition, body, redirections };
  },
  untilNode(condition, body, redirections = []) {
    return { type: "Until", condition, body, redirections };
  },
  caseNode(word, items, redirections = []) {
    return { type: "Case", word, items, redirections };
  },
  caseItem(patterns, body, terminator = ";;") {
    return { type: "CaseItem", patterns, body, terminator };
  },
  subshell(body, redirections = []) {
    return { type: "Subshell", body, redirections };
  },
  group(body, redirections = []) {
    return { type: "Group", body, redirections };
  },
  functionDef(name, body, redirections = [], sourceFile) {
    return { type: "FunctionDef", name, body, redirections, sourceFile };
  },
  conditionalCommand(expression, redirections = [], line) {
    return { type: "ConditionalCommand", expression, redirections, line };
  },
  arithmeticCommand(expression, redirections = [], line) {
    return { type: "ArithmeticCommand", expression, redirections, line };
  }
};
var TokenType;
(function(TokenType2) {
  TokenType2["EOF"] = "EOF";
  TokenType2["NEWLINE"] = "NEWLINE";
  TokenType2["SEMICOLON"] = "SEMICOLON";
  TokenType2["AMP"] = "AMP";
  TokenType2["PIPE"] = "PIPE";
  TokenType2["PIPE_AMP"] = "PIPE_AMP";
  TokenType2["AND_AND"] = "AND_AND";
  TokenType2["OR_OR"] = "OR_OR";
  TokenType2["BANG"] = "BANG";
  TokenType2["LESS"] = "LESS";
  TokenType2["GREAT"] = "GREAT";
  TokenType2["DLESS"] = "DLESS";
  TokenType2["DGREAT"] = "DGREAT";
  TokenType2["LESSAND"] = "LESSAND";
  TokenType2["GREATAND"] = "GREATAND";
  TokenType2["LESSGREAT"] = "LESSGREAT";
  TokenType2["DLESSDASH"] = "DLESSDASH";
  TokenType2["CLOBBER"] = "CLOBBER";
  TokenType2["TLESS"] = "TLESS";
  TokenType2["AND_GREAT"] = "AND_GREAT";
  TokenType2["AND_DGREAT"] = "AND_DGREAT";
  TokenType2["LPAREN"] = "LPAREN";
  TokenType2["RPAREN"] = "RPAREN";
  TokenType2["LBRACE"] = "LBRACE";
  TokenType2["RBRACE"] = "RBRACE";
  TokenType2["DSEMI"] = "DSEMI";
  TokenType2["SEMI_AND"] = "SEMI_AND";
  TokenType2["SEMI_SEMI_AND"] = "SEMI_SEMI_AND";
  TokenType2["DBRACK_START"] = "DBRACK_START";
  TokenType2["DBRACK_END"] = "DBRACK_END";
  TokenType2["DPAREN_START"] = "DPAREN_START";
  TokenType2["DPAREN_END"] = "DPAREN_END";
  TokenType2["IF"] = "IF";
  TokenType2["THEN"] = "THEN";
  TokenType2["ELSE"] = "ELSE";
  TokenType2["ELIF"] = "ELIF";
  TokenType2["FI"] = "FI";
  TokenType2["FOR"] = "FOR";
  TokenType2["WHILE"] = "WHILE";
  TokenType2["UNTIL"] = "UNTIL";
  TokenType2["DO"] = "DO";
  TokenType2["DONE"] = "DONE";
  TokenType2["CASE"] = "CASE";
  TokenType2["ESAC"] = "ESAC";
  TokenType2["IN"] = "IN";
  TokenType2["FUNCTION"] = "FUNCTION";
  TokenType2["SELECT"] = "SELECT";
  TokenType2["TIME"] = "TIME";
  TokenType2["COPROC"] = "COPROC";
  TokenType2["WORD"] = "WORD";
  TokenType2["NAME"] = "NAME";
  TokenType2["NUMBER"] = "NUMBER";
  TokenType2["ASSIGNMENT_WORD"] = "ASSIGNMENT_WORD";
  TokenType2["FD_VARIABLE"] = "FD_VARIABLE";
  TokenType2["COMMENT"] = "COMMENT";
  TokenType2["HEREDOC_CONTENT"] = "HEREDOC_CONTENT";
})(TokenType || (TokenType = {}));
var LexerError = (_o = class extends Error {
  line;
  column;
  constructor(message, line, column) {
    super(`line ${line}: ${message}`);
    this.line = line;
    this.column = column;
    this.name = "LexerError";
  }
}, __name$1(_o, "LexerError"), _o);
var RESERVED_WORDS = {
  if: TokenType.IF,
  // biome-ignore lint/suspicious/noThenProperty: "then" is a bash reserved word
  then: TokenType.THEN,
  else: TokenType.ELSE,
  elif: TokenType.ELIF,
  fi: TokenType.FI,
  for: TokenType.FOR,
  while: TokenType.WHILE,
  until: TokenType.UNTIL,
  do: TokenType.DO,
  done: TokenType.DONE,
  case: TokenType.CASE,
  esac: TokenType.ESAC,
  in: TokenType.IN,
  function: TokenType.FUNCTION,
  select: TokenType.SELECT,
  time: TokenType.TIME,
  coproc: TokenType.COPROC
};
function isValidAssignmentLHS(str) {
  const match = str.match(/^[a-zA-Z_][a-zA-Z0-9_]*/);
  if (!match)
    return false;
  const afterName = str.slice(match[0].length);
  if (afterName === "" || afterName === "+")
    return true;
  if (afterName[0] === "[") {
    let depth = 0;
    let i = 0;
    for (; i < afterName.length; i++) {
      if (afterName[i] === "[")
        depth++;
      else if (afterName[i] === "]") {
        depth--;
        if (depth === 0)
          break;
      }
    }
    if (depth !== 0 || i >= afterName.length)
      return false;
    const afterBracket = afterName.slice(i + 1);
    return afterBracket === "" || afterBracket === "+";
  }
  return false;
}
__name$1(isValidAssignmentLHS, "isValidAssignmentLHS");
function findAssignmentEq(str) {
  let depth = 0;
  for (let i = 0; i < str.length; i++) {
    const c = str[i];
    if (c === "[") {
      depth++;
    } else if (c === "]") {
      depth--;
    } else if (depth === 0 && c === "=") {
      return i;
    } else if (depth === 0 && c === "+" && str[i + 1] === "=") {
      return i + 1;
    }
  }
  return -1;
}
__name$1(findAssignmentEq, "findAssignmentEq");
var THREE_CHAR_OPS = [
  [";", ";", "&", TokenType.SEMI_SEMI_AND],
  ["<", "<", "<", TokenType.TLESS],
  ["&", ">", ">", TokenType.AND_DGREAT]
  // Note: <<- has special handling for heredoc, not included here
];
var TWO_CHAR_OPS = [
  ["[", "[", TokenType.DBRACK_START],
  ["]", "]", TokenType.DBRACK_END],
  ["(", "(", TokenType.DPAREN_START],
  [")", ")", TokenType.DPAREN_END],
  ["&", "&", TokenType.AND_AND],
  ["|", "|", TokenType.OR_OR],
  [";", ";", TokenType.DSEMI],
  [";", "&", TokenType.SEMI_AND],
  ["|", "&", TokenType.PIPE_AMP],
  [">", ">", TokenType.DGREAT],
  ["<", "&", TokenType.LESSAND],
  [">", "&", TokenType.GREATAND],
  ["<", ">", TokenType.LESSGREAT],
  [">", "|", TokenType.CLOBBER],
  ["&", ">", TokenType.AND_GREAT]
];
var SINGLE_CHAR_OPS = {
  "|": TokenType.PIPE,
  "&": TokenType.AMP,
  ";": TokenType.SEMICOLON,
  "(": TokenType.LPAREN,
  ")": TokenType.RPAREN,
  "<": TokenType.LESS,
  ">": TokenType.GREAT
};
function isValidName(s) {
  return /^[a-zA-Z_][a-zA-Z0-9_]*$/.test(s);
}
__name$1(isValidName, "isValidName");
function isWordBoundary(char) {
  return char === " " || char === "	" || char === "\n" || char === ";" || char === "&" || char === "|" || char === "(" || char === ")" || char === "<" || char === ">";
}
__name$1(isWordBoundary, "isWordBoundary");
var Lexer = (_p = class {
  input;
  pos = 0;
  line = 1;
  column = 1;
  tokens = [];
  pendingHeredocs = [];
  // Track depth inside (( )) for C-style for loops and arithmetic commands
  // When > 0, we're inside (( )) and need to track nested parens
  dparenDepth = 0;
  constructor(input) {
    this.input = input;
  }
  /**
   * Tokenize the entire input
   */
  tokenize() {
    const input = this.input;
    const len = input.length;
    const tokens = this.tokens;
    const pendingHeredocs = this.pendingHeredocs;
    while (this.pos < len) {
      if (pendingHeredocs.length > 0 && tokens.length > 0 && tokens[tokens.length - 1].type === TokenType.NEWLINE) {
        this.readHeredocContent();
        continue;
      }
      this.skipWhitespace();
      if (this.pos >= len)
        break;
      const token = this.nextToken();
      if (token) {
        tokens.push(token);
      }
    }
    tokens.push({
      type: TokenType.EOF,
      value: "",
      start: this.pos,
      end: this.pos,
      line: this.line,
      column: this.column
    });
    return tokens;
  }
  skipWhitespace() {
    const input = this.input;
    const len = input.length;
    let pos = this.pos;
    let col = this.column;
    let ln = this.line;
    while (pos < len) {
      const char = input[pos];
      if (char === " " || char === "	") {
        pos++;
        col++;
      } else if (char === "\\" && input[pos + 1] === "\n") {
        pos += 2;
        ln++;
        col = 1;
      } else {
        break;
      }
    }
    this.pos = pos;
    this.column = col;
    this.line = ln;
  }
  nextToken() {
    const input = this.input;
    const pos = this.pos;
    const startLine = this.line;
    const startColumn = this.column;
    const c0 = input[pos];
    const c1 = input[pos + 1];
    const c2 = input[pos + 2];
    if (c0 === "#" && this.dparenDepth === 0) {
      return this.readComment(pos, startLine, startColumn);
    }
    if (c0 === "\n") {
      this.pos = pos + 1;
      this.line++;
      this.column = 1;
      return {
        type: TokenType.NEWLINE,
        value: "\n",
        start: pos,
        end: pos + 1,
        line: startLine,
        column: startColumn
      };
    }
    if (c0 === "<" && c1 === "<" && c2 === "-") {
      this.pos = pos + 3;
      this.column = startColumn + 3;
      this.registerHeredocFromLookahead(true);
      return this.makeToken(TokenType.DLESSDASH, "<<-", pos, startLine, startColumn);
    }
    for (const [first, second, third, type] of THREE_CHAR_OPS) {
      if (c0 === first && c1 === second && c2 === third) {
        this.pos = pos + 3;
        this.column = startColumn + 3;
        return this.makeToken(type, first + second + third, pos, startLine, startColumn);
      }
    }
    if (c0 === "<" && c1 === "<") {
      this.pos = pos + 2;
      this.column = startColumn + 2;
      this.registerHeredocFromLookahead(false);
      return this.makeToken(TokenType.DLESS, "<<", pos, startLine, startColumn);
    }
    if (c0 === "(" && c1 === "(") {
      if (this.dparenDepth > 0) {
        this.pos = pos + 1;
        this.column = startColumn + 1;
        this.dparenDepth++;
        return this.makeToken(TokenType.LPAREN, "(", pos, startLine, startColumn);
      }
      if (this.looksLikeNestedSubshells(pos + 2) || this.dparenClosesWithSpacedParens(pos + 2)) {
        this.pos = pos + 1;
        this.column = startColumn + 1;
        return this.makeToken(TokenType.LPAREN, "(", pos, startLine, startColumn);
      }
      this.pos = pos + 2;
      this.column = startColumn + 2;
      this.dparenDepth = 1;
      return this.makeToken(TokenType.DPAREN_START, "((", pos, startLine, startColumn);
    }
    if (c0 === ")" && c1 === ")") {
      if (this.dparenDepth === 1) {
        this.pos = pos + 2;
        this.column = startColumn + 2;
        this.dparenDepth = 0;
        return this.makeToken(TokenType.DPAREN_END, "))", pos, startLine, startColumn);
      } else if (this.dparenDepth > 1) {
        this.pos = pos + 1;
        this.column = startColumn + 1;
        this.dparenDepth--;
        return this.makeToken(TokenType.RPAREN, ")", pos, startLine, startColumn);
      }
      this.pos = pos + 1;
      this.column = startColumn + 1;
      return this.makeToken(TokenType.RPAREN, ")", pos, startLine, startColumn);
    }
    for (const [first, second, type] of TWO_CHAR_OPS) {
      if (first === "(" && second === "(" || first === ")" && second === ")") {
        continue;
      }
      if (this.dparenDepth > 0 && first === ";" && (type === TokenType.DSEMI || type === TokenType.SEMI_AND || type === TokenType.SEMI_SEMI_AND)) {
        continue;
      }
      if (c0 === first && c1 === second) {
        if (type === TokenType.DBRACK_START || type === TokenType.DBRACK_END) {
          const afterOp = input[pos + 2];
          if (afterOp !== void 0 && afterOp !== " " && afterOp !== "	" && afterOp !== "\n" && afterOp !== ";" && afterOp !== "&" && afterOp !== "|" && afterOp !== "(" && afterOp !== ")" && afterOp !== "<" && afterOp !== ">") {
            break;
          }
        }
        this.pos = pos + 2;
        this.column = startColumn + 2;
        return this.makeToken(type, first + second, pos, startLine, startColumn);
      }
    }
    if (c0 === "(" && this.dparenDepth > 0) {
      this.pos = pos + 1;
      this.column = startColumn + 1;
      this.dparenDepth++;
      return this.makeToken(TokenType.LPAREN, "(", pos, startLine, startColumn);
    }
    if (c0 === ")" && this.dparenDepth > 1) {
      this.pos = pos + 1;
      this.column = startColumn + 1;
      this.dparenDepth--;
      return this.makeToken(TokenType.RPAREN, ")", pos, startLine, startColumn);
    }
    const singleCharType = SINGLE_CHAR_OPS[c0];
    if (singleCharType) {
      this.pos = pos + 1;
      this.column = startColumn + 1;
      return this.makeToken(singleCharType, c0, pos, startLine, startColumn);
    }
    if (c0 === "{") {
      const fdVarResult = this.scanFdVariable(pos);
      if (fdVarResult !== null) {
        this.pos = fdVarResult.end;
        this.column = startColumn + (fdVarResult.end - pos);
        return {
          type: TokenType.FD_VARIABLE,
          value: fdVarResult.varname,
          start: pos,
          end: fdVarResult.end,
          line: startLine,
          column: startColumn
        };
      }
      if (c1 === "}") {
        this.pos = pos + 2;
        this.column = startColumn + 2;
        return {
          type: TokenType.WORD,
          value: "{}",
          start: pos,
          end: pos + 2,
          line: startLine,
          column: startColumn,
          quoted: false,
          singleQuoted: false
        };
      }
      const braceContent = this.scanBraceExpansion(pos);
      if (braceContent !== null) {
        return this.readWordWithBraceExpansion(pos, startLine, startColumn);
      }
      const literalBrace = this.scanLiteralBraceWord(pos);
      if (literalBrace !== null) {
        return this.readWordWithBraceExpansion(pos, startLine, startColumn);
      }
      if (c1 !== void 0 && c1 !== " " && c1 !== "	" && c1 !== "\n") {
        return this.readWord(pos, startLine, startColumn);
      }
      this.pos = pos + 1;
      this.column = startColumn + 1;
      return this.makeToken(TokenType.LBRACE, "{", pos, startLine, startColumn);
    }
    if (c0 === "}") {
      if (this.isWordCharFollowing(pos + 1)) {
        return this.readWord(pos, startLine, startColumn);
      }
      this.pos = pos + 1;
      this.column = startColumn + 1;
      return this.makeToken(TokenType.RBRACE, "}", pos, startLine, startColumn);
    }
    if (c0 === "!") {
      if (c1 === "=") {
        this.pos = pos + 2;
        this.column = startColumn + 2;
        return this.makeToken(TokenType.WORD, "!=", pos, startLine, startColumn);
      }
      this.pos = pos + 1;
      this.column = startColumn + 1;
      return this.makeToken(TokenType.BANG, "!", pos, startLine, startColumn);
    }
    return this.readWord(pos, startLine, startColumn);
  }
  /**
   * Look ahead from position after (( to determine if this is nested subshells
   * like ((cmd) || (cmd2)) rather than arithmetic like ((1+2)).
   *
   * Returns true if it looks like nested subshells (command invocation).
   */
  looksLikeNestedSubshells(startPos) {
    const input = this.input;
    const len = input.length;
    let pos = startPos;
    while (pos < len && (input[pos] === " " || input[pos] === "	")) {
      pos++;
    }
    if (pos >= len)
      return false;
    const c = input[pos];
    if (c === "(") {
      return this.looksLikeNestedSubshells(pos + 1);
    }
    const isLetter = /[a-zA-Z_]/.test(c);
    const isSpecialCommand = c === "!" || c === "[";
    if (!isLetter && !isSpecialCommand) {
      return false;
    }
    let wordEnd = pos;
    while (wordEnd < len && /[a-zA-Z0-9_\-.]/.test(input[wordEnd])) {
      wordEnd++;
    }
    if (wordEnd === pos) {
      return isSpecialCommand;
    }
    let afterWord = wordEnd;
    while (afterWord < len && (input[afterWord] === " " || input[afterWord] === "	")) {
      afterWord++;
    }
    if (afterWord >= len)
      return false;
    const nextChar = input[afterWord];
    if (nextChar === "=" && input[afterWord + 1] !== "=") {
      return false;
    }
    if (nextChar === "\n") {
      return false;
    }
    if (wordEnd === afterWord && /[+\-*/%<>&|^!~?:]/.test(nextChar) && nextChar !== "-") {
      return false;
    }
    if (nextChar === ")" && input[afterWord + 1] === ")") {
      return false;
    }
    if (afterWord > wordEnd && (nextChar === "-" || nextChar === '"' || nextChar === "'" || nextChar === "$" || /[a-zA-Z_/.]/.test(nextChar))) {
      let scanPos = afterWord;
      while (scanPos < len && input[scanPos] !== "\n") {
        if (input[scanPos] === ")") {
          return true;
        }
        scanPos++;
      }
      return false;
    }
    if (nextChar === ")") {
      let afterParen = afterWord + 1;
      while (afterParen < len && (input[afterParen] === " " || input[afterParen] === "	")) {
        afterParen++;
      }
      if (input[afterParen] === "|" && input[afterParen + 1] === "|" || input[afterParen] === "&" && input[afterParen + 1] === "&" || input[afterParen] === ";" || input[afterParen] === "|" && input[afterParen + 1] !== "|") {
        return true;
      }
    }
    return false;
  }
  makeToken(type, value, start2, line, column) {
    return {
      type,
      value,
      start: start2,
      end: this.pos,
      line,
      column
    };
  }
  readComment(start2, line, column) {
    const input = this.input;
    const len = input.length;
    let pos = this.pos;
    while (pos < len && input[pos] !== "\n") {
      pos++;
    }
    const value = input.slice(start2, pos);
    this.pos = pos;
    this.column = column + (pos - start2);
    return {
      type: TokenType.COMMENT,
      value,
      start: start2,
      end: pos,
      line,
      column
    };
  }
  readWord(start2, line, column) {
    const input = this.input;
    const len = input.length;
    let pos = this.pos;
    const fastStart = pos;
    while (pos < len) {
      const c = input[pos];
      if (c === " " || c === "	" || c === "\n" || c === ";" || c === "&" || c === "|" || c === "(" || c === ")" || c === "<" || c === ">" || c === "'" || c === '"' || c === "\\" || c === "$" || c === "`" || c === "{" || c === "}" || c === "~" || c === "*" || c === "?" || c === "[") {
        break;
      }
      pos++;
    }
    if (pos > fastStart) {
      const c = input[pos];
      if (c === "(" && pos > fastStart && "@*+?!".includes(input[pos - 1])) ;
      else if (
        // If we hit end or a simple delimiter, we can use the fast path result
        pos >= len || c === " " || c === "	" || c === "\n" || c === ";" || c === "&" || c === "|" || c === "(" || c === ")" || c === "<" || c === ">"
      ) {
        const value2 = input.slice(fastStart, pos);
        this.pos = pos;
        this.column = column + (pos - fastStart);
        if (RESERVED_WORDS[value2]) {
          return {
            type: RESERVED_WORDS[value2],
            value: value2,
            start: start2,
            end: pos,
            line,
            column
          };
        }
        const eqIdx = findAssignmentEq(value2);
        if (eqIdx > 0 && isValidAssignmentLHS(value2.slice(0, eqIdx))) {
          return {
            type: TokenType.ASSIGNMENT_WORD,
            value: value2,
            start: start2,
            end: pos,
            line,
            column
          };
        }
        if (/^[0-9]+$/.test(value2)) {
          return {
            type: TokenType.NUMBER,
            value: value2,
            start: start2,
            end: pos,
            line,
            column
          };
        }
        if (/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(value2)) {
          return {
            type: TokenType.NAME,
            value: value2,
            start: start2,
            end: pos,
            line,
            column,
            quoted: false,
            singleQuoted: false
          };
        }
        return {
          type: TokenType.WORD,
          value: value2,
          start: start2,
          end: pos,
          line,
          column,
          quoted: false,
          singleQuoted: false
        };
      }
    }
    pos = this.pos;
    let col = this.column;
    let ln = this.line;
    let value = "";
    let quoted = false;
    let singleQuoted = false;
    let inSingleQuote = false;
    let inDoubleQuote = false;
    let startsWithQuote = input[pos] === '"' || input[pos] === "'";
    let hasContentAfterQuote = false;
    let bracketDepth = 0;
    while (pos < len) {
      const char = input[pos];
      if (!inSingleQuote && !inDoubleQuote) {
        if (char === "(" && value.length > 0 && "@*+?!".includes(value[value.length - 1])) {
          const extglobResult = this.scanExtglobPattern(pos);
          if (extglobResult !== null) {
            value += extglobResult.content;
            pos = extglobResult.end;
            col += extglobResult.content.length;
            continue;
          }
        }
        if (char === "[" && bracketDepth === 0) {
          if (/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(value)) {
            const afterBracket = pos + 1 < len ? input[pos + 1] : "";
            if (afterBracket === "^" || afterBracket === "!") {
              value += char;
              pos++;
              col++;
              continue;
            }
            bracketDepth = 1;
            value += char;
            pos++;
            col++;
            continue;
          }
        } else if (char === "[" && bracketDepth > 0) {
          if (value.length > 0 && value[value.length - 1] !== "\\") {
            bracketDepth++;
          }
          value += char;
          pos++;
          col++;
          continue;
        } else if (char === "]" && bracketDepth > 0) {
          if (value.length > 0 && value[value.length - 1] !== "\\") {
            bracketDepth--;
          }
          value += char;
          pos++;
          col++;
          continue;
        }
        if (bracketDepth > 0) {
          if (char === "\n") {
            break;
          }
          value += char;
          pos++;
          col++;
          continue;
        }
        if (char === " " || char === "	" || char === "\n" || char === ";" || char === "&" || char === "|" || char === "(" || char === ")" || char === "<" || char === ">") {
          break;
        }
      }
      if (char === "$" && pos + 1 < len && input[pos + 1] === "'" && !inSingleQuote && !inDoubleQuote) {
        value += "$'";
        pos += 2;
        col += 2;
        while (pos < len && input[pos] !== "'") {
          if (input[pos] === "\\" && pos + 1 < len) {
            value += input[pos] + input[pos + 1];
            pos += 2;
            col += 2;
          } else {
            value += input[pos];
            pos++;
            col++;
          }
        }
        if (pos < len) {
          value += "'";
          pos++;
          col++;
        }
        continue;
      }
      if (char === "$" && pos + 1 < len && input[pos + 1] === '"' && !inSingleQuote && !inDoubleQuote) {
        pos++;
        col++;
        inDoubleQuote = true;
        quoted = true;
        if (value === "") {
          startsWithQuote = true;
        }
        pos++;
        col++;
        continue;
      }
      if (char === "'" && !inDoubleQuote) {
        if (inSingleQuote) {
          inSingleQuote = false;
          if (!startsWithQuote || hasContentAfterQuote) {
            value += char;
          } else {
            const nextChar = pos + 1 < len ? input[pos + 1] : "";
            if (nextChar && !isWordBoundary(nextChar) && nextChar !== "'") {
              if (nextChar === '"') {
                hasContentAfterQuote = true;
                value += char;
                singleQuoted = false;
                quoted = false;
              } else {
                hasContentAfterQuote = true;
                value += char;
              }
            }
          }
        } else {
          inSingleQuote = true;
          if (startsWithQuote && !hasContentAfterQuote) {
            singleQuoted = true;
            quoted = true;
          } else {
            value += char;
          }
        }
        pos++;
        col++;
        continue;
      }
      if (char === '"' && !inSingleQuote) {
        if (inDoubleQuote) {
          inDoubleQuote = false;
          if (!startsWithQuote || hasContentAfterQuote) {
            value += char;
          } else {
            const nextChar = pos + 1 < len ? input[pos + 1] : "";
            if (nextChar && !isWordBoundary(nextChar) && nextChar !== '"') {
              if (nextChar === "'") {
                hasContentAfterQuote = true;
                value += char;
                singleQuoted = false;
                quoted = false;
              } else {
                hasContentAfterQuote = true;
                value += char;
              }
            }
          }
        } else {
          inDoubleQuote = true;
          if (startsWithQuote && !hasContentAfterQuote) {
            quoted = true;
          } else {
            value += char;
          }
        }
        pos++;
        col++;
        continue;
      }
      if (char === "\\" && !inSingleQuote && pos + 1 < len) {
        const nextChar = input[pos + 1];
        if (nextChar === "\n") {
          pos += 2;
          ln++;
          col = 1;
          continue;
        }
        if (inDoubleQuote) {
          if (nextChar === '"' || nextChar === "\\" || nextChar === "$" || nextChar === "`" || nextChar === "\n") {
            if (nextChar === "\n") {
              pos += 2;
              col = 1;
              ln++;
              continue;
            }
            value += char + nextChar;
            pos += 2;
            col += 2;
            continue;
          }
        } else {
          if (nextChar === "\\" || nextChar === '"' || nextChar === "'" || nextChar === "`" || nextChar === "*" || nextChar === "?" || nextChar === "[" || nextChar === "]" || nextChar === "(" || nextChar === ")" || nextChar === "$" || nextChar === "-" || // Regex-specific metacharacters for [[ =~ ]] patterns
          nextChar === "." || nextChar === "^" || nextChar === "+" || nextChar === "{" || nextChar === "}") {
            value += char + nextChar;
          } else {
            value += nextChar;
          }
          pos += 2;
          col += 2;
          continue;
        }
      }
      if (char === "$" && pos + 1 < len && input[pos + 1] === "(" && !inSingleQuote) {
        value += char;
        pos++;
        col++;
        value += input[pos];
        pos++;
        col++;
        let depth = 1;
        let inSingleQuote2 = false;
        let inDoubleQuote2 = false;
        let caseDepth = 0;
        let inCasePattern = false;
        let wordBuffer = "";
        const isArithmetic = input[pos] === "(" && !this.dollarDparenIsSubshell(pos);
        while (depth > 0 && pos < len) {
          const c = input[pos];
          value += c;
          if (inSingleQuote2) {
            if (c === "'")
              inSingleQuote2 = false;
          } else if (inDoubleQuote2) {
            if (c === "\\" && pos + 1 < len) {
              value += input[pos + 1];
              pos++;
              col++;
            } else if (c === '"') {
              inDoubleQuote2 = false;
            }
          } else {
            if (c === "'") {
              inSingleQuote2 = true;
              wordBuffer = "";
            } else if (c === '"') {
              inDoubleQuote2 = true;
              wordBuffer = "";
            } else if (c === "\\" && pos + 1 < len) {
              value += input[pos + 1];
              pos++;
              col++;
              wordBuffer = "";
            } else if (c === "$" && pos + 1 < len && input[pos + 1] === "{") {
              pos++;
              col++;
              value += input[pos];
              pos++;
              col++;
              let braceDepth = 1;
              let inBraceSingleQuote = false;
              let inBraceDoubleQuote = false;
              while (braceDepth > 0 && pos < len) {
                const bc = input[pos];
                if (bc === "\\" && pos + 1 < len && !inBraceSingleQuote) {
                  value += bc;
                  pos++;
                  col++;
                  value += input[pos];
                  pos++;
                  col++;
                  continue;
                }
                value += bc;
                if (inBraceSingleQuote) {
                  if (bc === "'")
                    inBraceSingleQuote = false;
                } else if (inBraceDoubleQuote) {
                  if (bc === '"')
                    inBraceDoubleQuote = false;
                } else {
                  if (bc === "'")
                    inBraceSingleQuote = true;
                  else if (bc === '"')
                    inBraceDoubleQuote = true;
                  else if (bc === "{")
                    braceDepth++;
                  else if (bc === "}")
                    braceDepth--;
                }
                if (bc === "\n") {
                  ln++;
                  col = 0;
                } else {
                  col++;
                }
                pos++;
              }
              wordBuffer = "";
              continue;
            } else if (c === "#" && !isArithmetic && // # is NOT a comment in arithmetic expansion
            (wordBuffer === "" || /\s/.test(input[pos - 1] || ""))) {
              while (pos + 1 < len && input[pos + 1] !== "\n") {
                pos++;
                col++;
                value += input[pos];
              }
              wordBuffer = "";
            } else if (/[a-zA-Z_]/.test(c)) {
              wordBuffer += c;
            } else {
              if (wordBuffer === "case") {
                caseDepth++;
                inCasePattern = false;
              } else if (wordBuffer === "in" && caseDepth > 0) {
                inCasePattern = true;
              } else if (wordBuffer === "esac" && caseDepth > 0) {
                caseDepth--;
                inCasePattern = false;
              }
              wordBuffer = "";
              if (c === "(") {
                if (pos > 0 && input[pos - 1] === "$") {
                  depth++;
                } else if (!inCasePattern) {
                  depth++;
                }
              } else if (c === ")") {
                if (inCasePattern) {
                  inCasePattern = false;
                } else {
                  depth--;
                }
              } else if (c === ";") {
                if (caseDepth > 0 && pos + 1 < len && input[pos + 1] === ";") {
                  inCasePattern = true;
                }
              }
            }
          }
          if (c === "\n") {
            ln++;
            col = 0;
            wordBuffer = "";
          }
          pos++;
          col++;
        }
        continue;
      }
      if (char === "$" && pos + 1 < len && input[pos + 1] === "[" && !inSingleQuote) {
        value += char;
        pos++;
        col++;
        value += input[pos];
        pos++;
        col++;
        let depth = 1;
        while (depth > 0 && pos < len) {
          const c = input[pos];
          value += c;
          if (c === "[")
            depth++;
          else if (c === "]")
            depth--;
          else if (c === "\n") {
            ln++;
            col = 0;
          }
          pos++;
          col++;
        }
        continue;
      }
      if (char === "$" && pos + 1 < len && input[pos + 1] === "{" && !inSingleQuote) {
        value += char;
        pos++;
        col++;
        value += input[pos];
        pos++;
        col++;
        let depth = 1;
        let inParamSingleQuote = false;
        let inParamDoubleQuote = false;
        let singleQuoteStartLine = ln;
        let singleQuoteStartCol = col;
        let doubleQuoteStartLine = ln;
        let doubleQuoteStartCol = col;
        while (depth > 0 && pos < len) {
          const c = input[pos];
          if (c === "\\" && pos + 1 < len && input[pos + 1] === "\n") {
            pos += 2;
            ln++;
            col = 1;
            continue;
          }
          if (c === "\\" && pos + 1 < len && !inParamSingleQuote) {
            value += c;
            pos++;
            col++;
            value += input[pos];
            pos++;
            col++;
            continue;
          }
          value += c;
          if (inParamSingleQuote) {
            if (c === "'") {
              inParamSingleQuote = false;
            }
          } else if (inParamDoubleQuote) {
            if (c === '"') {
              inParamDoubleQuote = false;
            }
          } else {
            if (c === "'") {
              inParamSingleQuote = true;
              singleQuoteStartLine = ln;
              singleQuoteStartCol = col;
            } else if (c === '"') {
              inParamDoubleQuote = true;
              doubleQuoteStartLine = ln;
              doubleQuoteStartCol = col;
            } else if (c === "{") {
              depth++;
            } else if (c === "}") {
              depth--;
            }
          }
          if (c === "\n") {
            ln++;
            col = 0;
          }
          pos++;
          col++;
        }
        if (inParamSingleQuote) {
          throw new LexerError("unexpected EOF while looking for matching `''", singleQuoteStartLine, singleQuoteStartCol);
        }
        if (inParamDoubleQuote) {
          throw new LexerError("unexpected EOF while looking for matching `\"'", doubleQuoteStartLine, doubleQuoteStartCol);
        }
        continue;
      }
      if (char === "$" && pos + 1 < len && !inSingleQuote) {
        const next = input[pos + 1];
        if (next === "#" || next === "?" || next === "$" || next === "!" || next === "@" || next === "*" || next === "-" || next >= "0" && next <= "9") {
          value += char + next;
          pos += 2;
          col += 2;
          continue;
        }
      }
      if (char === "`" && !inSingleQuote) {
        value += char;
        pos++;
        col++;
        while (pos < len && input[pos] !== "`") {
          const c = input[pos];
          value += c;
          if (c === "\\" && pos + 1 < len) {
            value += input[pos + 1];
            pos++;
            col++;
          }
          if (c === "\n") {
            ln++;
            col = 0;
          }
          pos++;
          col++;
        }
        if (pos < len) {
          value += input[pos];
          pos++;
          col++;
        }
        continue;
      }
      value += char;
      pos++;
      if (char === "\n") {
        ln++;
        col = 1;
      } else {
        col++;
      }
    }
    this.pos = pos;
    this.column = col;
    this.line = ln;
    if (hasContentAfterQuote && startsWithQuote) {
      const openQuote = input[start2];
      value = openQuote + value;
      quoted = false;
      singleQuoted = false;
    }
    if (inSingleQuote || inDoubleQuote) {
      const quoteType = inSingleQuote ? "'" : '"';
      throw new LexerError(`unexpected EOF while looking for matching \`${quoteType}'`, line, column);
    }
    if (!startsWithQuote && value.length >= 2) {
      if (value[0] === "'" && value[value.length - 1] === "'") {
        const inner = value.slice(1, -1);
        if (!inner.includes("'") && !inner.includes('"')) {
          value = inner;
          quoted = true;
          singleQuoted = true;
        }
      } else if (value[0] === '"' && value[value.length - 1] === '"') {
        const inner = value.slice(1, -1);
        let hasUnescapedQuote = false;
        for (let i = 0; i < inner.length; i++) {
          if (inner[i] === '"') {
            hasUnescapedQuote = true;
            break;
          }
          if (inner[i] === "\\" && i + 1 < inner.length) {
            i++;
          }
        }
        if (!hasUnescapedQuote) {
          value = inner;
          quoted = true;
          singleQuoted = false;
        }
      }
    }
    if (value === "") {
      return {
        type: TokenType.WORD,
        value: "",
        start: start2,
        end: pos,
        line,
        column,
        quoted,
        singleQuoted
      };
    }
    if (!quoted && RESERVED_WORDS[value]) {
      return {
        type: RESERVED_WORDS[value],
        value,
        start: start2,
        end: pos,
        line,
        column
      };
    }
    if (!startsWithQuote) {
      const eqIdx = findAssignmentEq(value);
      if (eqIdx > 0 && isValidAssignmentLHS(value.slice(0, eqIdx))) {
        return {
          type: TokenType.ASSIGNMENT_WORD,
          value,
          start: start2,
          end: pos,
          line,
          column,
          quoted,
          singleQuoted
        };
      }
    }
    if (/^[0-9]+$/.test(value)) {
      return {
        type: TokenType.NUMBER,
        value,
        start: start2,
        end: pos,
        line,
        column
      };
    }
    if (isValidName(value)) {
      return {
        type: TokenType.NAME,
        value,
        start: start2,
        end: pos,
        line,
        column,
        quoted,
        singleQuoted
      };
    }
    return {
      type: TokenType.WORD,
      value,
      start: start2,
      end: pos,
      line,
      column,
      quoted,
      singleQuoted
    };
  }
  readHeredocContent() {
    while (this.pendingHeredocs.length > 0) {
      const heredoc = this.pendingHeredocs.shift();
      if (!heredoc)
        break;
      const start2 = this.pos;
      const startLine = this.line;
      const startColumn = this.column;
      let content = "";
      while (this.pos < this.input.length) {
        this.pos;
        let line = "";
        while (this.pos < this.input.length && this.input[this.pos] !== "\n") {
          line += this.input[this.pos];
          this.pos++;
          this.column++;
        }
        const lineToCheck = heredoc.stripTabs ? line.replace(/^\t+/, "") : line;
        if (lineToCheck === heredoc.delimiter) {
          if (this.pos < this.input.length && this.input[this.pos] === "\n") {
            this.pos++;
            this.line++;
            this.column = 1;
          }
          break;
        }
        content += line;
        if (this.pos < this.input.length && this.input[this.pos] === "\n") {
          content += "\n";
          this.pos++;
          this.line++;
          this.column = 1;
        }
      }
      this.tokens.push({
        type: TokenType.HEREDOC_CONTENT,
        value: content,
        start: start2,
        end: this.pos,
        line: startLine,
        column: startColumn
      });
    }
  }
  /**
   * Register a here-document to be read after the next newline
   */
  addPendingHeredoc(delimiter, stripTabs, quoted) {
    this.pendingHeredocs.push({ delimiter, stripTabs, quoted });
  }
  /**
   * Look ahead from current position to find the here-doc delimiter
   * and register it as a pending here-doc
   */
  registerHeredocFromLookahead(stripTabs) {
    const savedPos = this.pos;
    const savedColumn = this.column;
    while (this.pos < this.input.length && (this.input[this.pos] === " " || this.input[this.pos] === "	")) {
      this.pos++;
      this.column++;
    }
    let delimiter = "";
    let quoted = false;
    while (this.pos < this.input.length) {
      const char = this.input[this.pos];
      if (/[\s;<>&|()]/.test(char)) {
        break;
      }
      if (char === "'" || char === '"') {
        quoted = true;
        const quoteChar = char;
        this.pos++;
        this.column++;
        while (this.pos < this.input.length && this.input[this.pos] !== quoteChar) {
          delimiter += this.input[this.pos];
          this.pos++;
          this.column++;
        }
        if (this.pos < this.input.length && this.input[this.pos] === quoteChar) {
          this.pos++;
          this.column++;
        }
      } else if (char === "\\") {
        quoted = true;
        this.pos++;
        this.column++;
        if (this.pos < this.input.length) {
          delimiter += this.input[this.pos];
          this.pos++;
          this.column++;
        }
      } else {
        delimiter += char;
        this.pos++;
        this.column++;
      }
    }
    this.pos = savedPos;
    this.column = savedColumn;
    if (delimiter) {
      this.pendingHeredocs.push({ delimiter, stripTabs, quoted });
    }
  }
  /**
   * Check if position is followed by word characters (not a word boundary).
   * Used to determine if } should be literal or RBRACE token.
   */
  isWordCharFollowing(pos) {
    if (pos >= this.input.length)
      return false;
    const c = this.input[pos];
    return !(c === " " || c === "	" || c === "\n" || c === ";" || c === "&" || c === "|" || c === "(" || c === ")" || c === "<" || c === ">");
  }
  /**
   * Read a word that starts with a brace expansion.
   * Includes the brace expansion plus any suffix characters and additional brace expansions.
   */
  readWordWithBraceExpansion(start2, line, column) {
    const input = this.input;
    const len = input.length;
    let pos = start2;
    let col = column;
    while (pos < len) {
      const c = input[pos];
      if (c === " " || c === "	" || c === "\n" || c === ";" || c === "&" || c === "|" || c === "(" || c === ")" || c === "<" || c === ">") {
        break;
      }
      if (c === "{") {
        const braceExp = this.scanBraceExpansion(pos);
        if (braceExp !== null) {
          let depth = 1;
          pos++;
          col++;
          while (pos < len && depth > 0) {
            if (input[pos] === "{")
              depth++;
            else if (input[pos] === "}")
              depth--;
            pos++;
            col++;
          }
          continue;
        }
        pos++;
        col++;
        continue;
      }
      if (c === "}") {
        pos++;
        col++;
        continue;
      }
      if (c === "$" && pos + 1 < len && input[pos + 1] === "(") {
        pos++;
        col++;
        pos++;
        col++;
        let depth = 1;
        while (depth > 0 && pos < len) {
          if (input[pos] === "(")
            depth++;
          else if (input[pos] === ")")
            depth--;
          pos++;
          col++;
        }
        continue;
      }
      if (c === "$" && pos + 1 < len && input[pos + 1] === "{") {
        pos++;
        col++;
        pos++;
        col++;
        let depth = 1;
        while (depth > 0 && pos < len) {
          if (input[pos] === "{")
            depth++;
          else if (input[pos] === "}")
            depth--;
          pos++;
          col++;
        }
        continue;
      }
      if (c === "`") {
        pos++;
        col++;
        while (pos < len && input[pos] !== "`") {
          if (input[pos] === "\\" && pos + 1 < len) {
            pos += 2;
            col += 2;
          } else {
            pos++;
            col++;
          }
        }
        if (pos < len) {
          pos++;
          col++;
        }
        continue;
      }
      pos++;
      col++;
    }
    const value = input.slice(start2, pos);
    this.pos = pos;
    this.column = col;
    return {
      type: TokenType.WORD,
      value,
      start: start2,
      end: pos,
      line,
      column,
      quoted: false,
      singleQuoted: false
    };
  }
  /**
   * Scan ahead to detect brace expansion pattern.
   * Returns the full brace expansion string if found, null otherwise.
   * Brace expansion must contain either:
   * - A comma (e.g., {a,b,c})
   * - A range with .. (e.g., {1..10})
   */
  scanBraceExpansion(startPos) {
    const input = this.input;
    const len = input.length;
    let pos = startPos + 1;
    let depth = 1;
    let hasComma = false;
    let hasRange = false;
    while (pos < len && depth > 0) {
      const c = input[pos];
      if (c === "{") {
        depth++;
        pos++;
      } else if (c === "}") {
        depth--;
        pos++;
      } else if (c === "," && depth === 1) {
        hasComma = true;
        pos++;
      } else if (c === "." && pos + 1 < len && input[pos + 1] === ".") {
        hasRange = true;
        pos += 2;
      } else if (c === " " || c === "	" || c === "\n" || c === ";" || c === "&" || c === "|") {
        return null;
      } else {
        pos++;
      }
    }
    if (depth === 0 && (hasComma || hasRange)) {
      return input.slice(startPos, pos);
    }
    return null;
  }
  /**
   * Scan a literal brace word like {foo} (no comma, no range).
   * Returns the literal string if found, null otherwise.
   * This is used when {} contains something but it's not a valid brace expansion.
   */
  scanLiteralBraceWord(startPos) {
    const input = this.input;
    const len = input.length;
    let pos = startPos + 1;
    let depth = 1;
    while (pos < len && depth > 0) {
      const c = input[pos];
      if (c === "{") {
        depth++;
        pos++;
      } else if (c === "}") {
        depth--;
        if (depth === 0) {
          return input.slice(startPos, pos + 1);
        }
        pos++;
      } else if (c === " " || c === "	" || c === "\n" || c === ";" || c === "&" || c === "|") {
        return null;
      } else {
        pos++;
      }
    }
    return null;
  }
  /**
   * Scan an extglob pattern starting at the opening parenthesis.
   * Extglob patterns are: @(...), *(...), +(...), ?(...), !(...)
   * The operator (@, *, +, ?, !) is already consumed; we start at the (.
   * Returns the content including parentheses, or null if not a valid extglob.
   */
  scanExtglobPattern(startPos) {
    const input = this.input;
    const len = input.length;
    let pos = startPos + 1;
    let depth = 1;
    while (pos < len && depth > 0) {
      const c = input[pos];
      if (c === "\\" && pos + 1 < len) {
        pos += 2;
        continue;
      }
      if ("@*+?!".includes(c) && pos + 1 < len && input[pos + 1] === "(") {
        pos++;
        depth++;
        pos++;
        continue;
      }
      if (c === "(") {
        depth++;
        pos++;
      } else if (c === ")") {
        depth--;
        pos++;
      } else if (c === "\n") {
        return null;
      } else {
        pos++;
      }
    }
    if (depth === 0) {
      return {
        content: input.slice(startPos, pos),
        end: pos
      };
    }
    return null;
  }
  /**
   * Scan for FD variable syntax: {varname} immediately followed by a redirect operator.
   * This is the bash 4.1+ feature where {fd}>file allocates an FD and stores it in variable.
   * Returns the variable name and end position if found, null otherwise.
   *
   * Valid patterns:
   * - {varname}>file, {varname}>>file, {varname}>|file
   * - {varname}<file, {varname}<<word, {varname}<<<word
   * - {varname}<>file
   * - {varname}>&N, {varname}<&N
   * - {varname}>&-, {varname}<&- (close FD)
   */
  scanFdVariable(startPos) {
    const input = this.input;
    const len = input.length;
    let pos = startPos + 1;
    const nameStart = pos;
    while (pos < len) {
      const c3 = input[pos];
      if (pos === nameStart) {
        if (!(c3 >= "a" && c3 <= "z" || c3 >= "A" && c3 <= "Z" || c3 === "_")) {
          return null;
        }
      } else {
        if (!(c3 >= "a" && c3 <= "z" || c3 >= "A" && c3 <= "Z" || c3 >= "0" && c3 <= "9" || c3 === "_")) {
          break;
        }
      }
      pos++;
    }
    if (pos === nameStart) {
      return null;
    }
    const varname = input.slice(nameStart, pos);
    if (pos >= len || input[pos] !== "}") {
      return null;
    }
    pos++;
    if (pos >= len) {
      return null;
    }
    const c = input[pos];
    const c2 = pos + 1 < len ? input[pos + 1] : "";
    const isRedirectOp = c === ">" || // >, >>, >&, >|
    c === "<" || // <, <<, <&, <<<, <>
    c === "&" && (c2 === ">" || c2 === "<");
    if (!isRedirectOp) {
      return null;
    }
    return { varname, end: pos };
  }
  /**
   * Scan ahead from a $(( position to determine if it should be treated as
   * $( ( subshell ) ) instead of $(( arithmetic )).
   * This handles cases like:
   *   echo $(( echo 1
   *   echo 2
   *   ) )
   * which should be a command substitution containing a subshell, not arithmetic.
   *
   * @param startPos - position at the second ( (i.e., at input[startPos] === "(")
   * @returns true if this is a subshell (closes with ) )), false if arithmetic (closes with )))
   */
  dollarDparenIsSubshell(startPos) {
    const input = this.input;
    const len = input.length;
    let pos = startPos + 1;
    let depth = 2;
    let inSingleQuote = false;
    let inDoubleQuote = false;
    let hasNewline = false;
    while (pos < len && depth > 0) {
      const c = input[pos];
      if (inSingleQuote) {
        if (c === "'") {
          inSingleQuote = false;
        }
        if (c === "\n")
          hasNewline = true;
        pos++;
        continue;
      }
      if (inDoubleQuote) {
        if (c === "\\") {
          pos += 2;
          continue;
        }
        if (c === '"') {
          inDoubleQuote = false;
        }
        if (c === "\n")
          hasNewline = true;
        pos++;
        continue;
      }
      if (c === "'") {
        inSingleQuote = true;
        pos++;
        continue;
      }
      if (c === '"') {
        inDoubleQuote = true;
        pos++;
        continue;
      }
      if (c === "\\") {
        pos += 2;
        continue;
      }
      if (c === "\n") {
        hasNewline = true;
      }
      if (c === "(") {
        depth++;
        pos++;
        continue;
      }
      if (c === ")") {
        depth--;
        if (depth === 1) {
          const nextPos = pos + 1;
          if (nextPos < len && input[nextPos] === ")") {
            return false;
          }
          let scanPos = nextPos;
          let hasWhitespace = false;
          while (scanPos < len && (input[scanPos] === " " || input[scanPos] === "	" || input[scanPos] === "\n")) {
            hasWhitespace = true;
            scanPos++;
          }
          if (hasWhitespace && scanPos < len && input[scanPos] === ")") {
            return true;
          }
          if (hasNewline) {
            return true;
          }
        }
        if (depth === 0) {
          return false;
        }
        pos++;
        continue;
      }
      pos++;
    }
    return false;
  }
  /**
   * Scan ahead from a (( position to determine if it closes with ) ) (nested subshells)
   * or )) (arithmetic). We need to track paren depth and quotes to find the matching close.
   * @param startPos - position after the (( (i.e., at the first char of content)
   * @returns true if it closes with ) ) (space between parens), false otherwise
   */
  dparenClosesWithSpacedParens(startPos) {
    const input = this.input;
    const len = input.length;
    let pos = startPos;
    let depth = 2;
    let inSingleQuote = false;
    let inDoubleQuote = false;
    while (pos < len && depth > 0) {
      const c = input[pos];
      if (inSingleQuote) {
        if (c === "'") {
          inSingleQuote = false;
        }
        pos++;
        continue;
      }
      if (inDoubleQuote) {
        if (c === "\\") {
          pos += 2;
          continue;
        }
        if (c === '"') {
          inDoubleQuote = false;
        }
        pos++;
        continue;
      }
      if (c === "'") {
        inSingleQuote = true;
        pos++;
        continue;
      }
      if (c === '"') {
        inDoubleQuote = true;
        pos++;
        continue;
      }
      if (c === "\\") {
        pos += 2;
        continue;
      }
      if (c === "(") {
        depth++;
        pos++;
        continue;
      }
      if (c === ")") {
        depth--;
        if (depth === 1) {
          const nextPos = pos + 1;
          if (nextPos < len && input[nextPos] === ")") {
            return false;
          }
          let scanPos = nextPos;
          let hasWhitespace = false;
          while (scanPos < len && (input[scanPos] === " " || input[scanPos] === "	" || input[scanPos] === "\n")) {
            hasWhitespace = true;
            scanPos++;
          }
          if (hasWhitespace && scanPos < len && input[scanPos] === ")") {
            return true;
          }
        }
        if (depth === 0) {
          return false;
        }
        pos++;
        continue;
      }
      if (depth === 1) {
        if (c === "|" && pos + 1 < len && input[pos + 1] === "|") {
          return true;
        }
        if (c === "&" && pos + 1 < len && input[pos + 1] === "&") {
          return true;
        }
        if (c === "|" && pos + 1 < len && input[pos + 1] !== "|") {
          return true;
        }
      }
      pos++;
    }
    return false;
  }
}, __name$1(_p, "Lexer"), _p);
var MAX_INPUT_SIZE = 1e6;
var MAX_TOKENS = 1e5;
var MAX_PARSE_ITERATIONS = 1e6;
var REDIRECTION_TOKENS = /* @__PURE__ */ new Set([
  TokenType.LESS,
  TokenType.GREAT,
  TokenType.DLESS,
  TokenType.DGREAT,
  TokenType.LESSAND,
  TokenType.GREATAND,
  TokenType.LESSGREAT,
  TokenType.DLESSDASH,
  TokenType.CLOBBER,
  TokenType.TLESS,
  TokenType.AND_GREAT,
  TokenType.AND_DGREAT
]);
var REDIRECTION_AFTER_NUMBER = /* @__PURE__ */ new Set([
  TokenType.LESS,
  TokenType.GREAT,
  TokenType.DLESS,
  TokenType.DGREAT,
  TokenType.LESSAND,
  TokenType.GREATAND,
  TokenType.LESSGREAT,
  TokenType.DLESSDASH,
  TokenType.CLOBBER,
  TokenType.TLESS
]);
var REDIRECTION_AFTER_FD_VARIABLE = /* @__PURE__ */ new Set([
  TokenType.LESS,
  TokenType.GREAT,
  TokenType.DLESS,
  TokenType.DGREAT,
  TokenType.LESSAND,
  TokenType.GREATAND,
  TokenType.LESSGREAT,
  TokenType.DLESSDASH,
  TokenType.CLOBBER,
  TokenType.TLESS,
  TokenType.AND_GREAT,
  TokenType.AND_DGREAT
]);
var ParseException = (_q = class extends Error {
  line;
  column;
  token;
  constructor(message, line, column, token = void 0) {
    super(`Parse error at ${line}:${column}: ${message}`);
    this.line = line;
    this.column = column;
    this.token = token;
    this.name = "ParseException";
  }
}, __name$1(_q, "ParseException"), _q);
function decodeUtf8WithRecovery(bytes) {
  let result2 = "";
  let i = 0;
  while (i < bytes.length) {
    const b0 = bytes[i];
    if (b0 < 128) {
      result2 += String.fromCharCode(b0);
      i++;
      continue;
    }
    if ((b0 & 224) === 192) {
      if (i + 1 < bytes.length && (bytes[i + 1] & 192) === 128 && b0 >= 194) {
        const codePoint = (b0 & 31) << 6 | bytes[i + 1] & 63;
        result2 += String.fromCharCode(codePoint);
        i += 2;
        continue;
      }
      result2 += String.fromCharCode(b0);
      i++;
      continue;
    }
    if ((b0 & 240) === 224) {
      if (i + 2 < bytes.length && (bytes[i + 1] & 192) === 128 && (bytes[i + 2] & 192) === 128) {
        if (b0 === 224 && bytes[i + 1] < 160) {
          result2 += String.fromCharCode(b0);
          i++;
          continue;
        }
        const codePoint = (b0 & 15) << 12 | (bytes[i + 1] & 63) << 6 | bytes[i + 2] & 63;
        if (codePoint >= 55296 && codePoint <= 57343) {
          result2 += String.fromCharCode(b0);
          i++;
          continue;
        }
        result2 += String.fromCharCode(codePoint);
        i += 3;
        continue;
      }
      result2 += String.fromCharCode(b0);
      i++;
      continue;
    }
    if ((b0 & 248) === 240 && b0 <= 244) {
      if (i + 3 < bytes.length && (bytes[i + 1] & 192) === 128 && (bytes[i + 2] & 192) === 128 && (bytes[i + 3] & 192) === 128) {
        if (b0 === 240 && bytes[i + 1] < 144) {
          result2 += String.fromCharCode(b0);
          i++;
          continue;
        }
        const codePoint = (b0 & 7) << 18 | (bytes[i + 1] & 63) << 12 | (bytes[i + 2] & 63) << 6 | bytes[i + 3] & 63;
        if (codePoint > 1114111) {
          result2 += String.fromCharCode(b0);
          i++;
          continue;
        }
        result2 += String.fromCodePoint(codePoint);
        i += 4;
        continue;
      }
      result2 += String.fromCharCode(b0);
      i++;
      continue;
    }
    result2 += String.fromCharCode(b0);
    i++;
  }
  return result2;
}
__name$1(decodeUtf8WithRecovery, "decodeUtf8WithRecovery");
function findTildeEnd(_p2, value, start2) {
  let i = start2 + 1;
  while (i < value.length && /[a-zA-Z0-9_-]/.test(value[i])) {
    i++;
  }
  return i;
}
__name$1(findTildeEnd, "findTildeEnd");
function findMatchingBracket(_p2, value, start2, open, close) {
  let depth = 1;
  let i = start2 + 1;
  while (i < value.length && depth > 0) {
    if (value[i] === open)
      depth++;
    else if (value[i] === close)
      depth--;
    if (depth > 0)
      i++;
  }
  return depth === 0 ? i : -1;
}
__name$1(findMatchingBracket, "findMatchingBracket");
function findParameterOperationEnd(_p2, value, start2) {
  let i = start2;
  let depth = 1;
  while (i < value.length && depth > 0) {
    const char = value[i];
    if (char === "\\" && i + 1 < value.length) {
      i += 2;
      continue;
    }
    if (char === "'") {
      const closeIdx = value.indexOf("'", i + 1);
      if (closeIdx !== -1) {
        i = closeIdx + 1;
        continue;
      }
    }
    if (char === '"') {
      i++;
      while (i < value.length && value[i] !== '"') {
        if (value[i] === "\\" && i + 1 < value.length) {
          i += 2;
        } else {
          i++;
        }
      }
      if (i < value.length)
        i++;
      continue;
    }
    if (char === "{")
      depth++;
    else if (char === "}")
      depth--;
    if (depth > 0)
      i++;
  }
  return i;
}
__name$1(findParameterOperationEnd, "findParameterOperationEnd");
function findPatternEnd(_p2, value, start2) {
  let i = start2;
  let consumedAny = false;
  while (i < value.length) {
    const char = value[i];
    if (char === "/" && consumedAny || char === "}")
      break;
    if (char === "'") {
      const closeIdx = value.indexOf("'", i + 1);
      if (closeIdx !== -1) {
        i = closeIdx + 1;
        consumedAny = true;
        continue;
      }
    }
    if (char === '"') {
      i++;
      while (i < value.length && value[i] !== '"') {
        if (value[i] === "\\" && i + 1 < value.length) {
          i += 2;
        } else {
          i++;
        }
      }
      if (i < value.length)
        i++;
      consumedAny = true;
      continue;
    }
    if (char === "\\") {
      i += 2;
      consumedAny = true;
    } else {
      i++;
      consumedAny = true;
    }
  }
  return i;
}
__name$1(findPatternEnd, "findPatternEnd");
function parseGlobPattern(_p2, value, start2) {
  let i = start2;
  let pattern = "";
  while (i < value.length) {
    const char = value[i];
    if (char === "*" || char === "?") {
      pattern += char;
      i++;
    } else if (char === "[") {
      const closeIdx = findCharacterClassEnd(value, i);
      if (closeIdx === -1) {
        pattern += char;
        i++;
      } else {
        pattern += value.slice(i, closeIdx + 1);
        i = closeIdx + 1;
      }
    } else {
      break;
    }
  }
  return { pattern, endIndex: i };
}
__name$1(parseGlobPattern, "parseGlobPattern");
function findCharacterClassEnd(value, start2) {
  let i = start2 + 1;
  if (i < value.length && value[i] === "^") {
    i++;
  }
  if (i < value.length && value[i] === "]") {
    i++;
  }
  while (i < value.length) {
    const char = value[i];
    if (char === "\\" && i + 1 < value.length) {
      const next = value[i + 1];
      if (next === '"' || next === "'") {
        return -1;
      }
      i += 2;
      continue;
    }
    if (char === "]") {
      return i;
    }
    if (char === '"' || char === "$" || char === "`") {
      return -1;
    }
    if (char === "'") {
      const closeQuote = value.indexOf("'", i + 1);
      if (closeQuote !== -1) {
        i = closeQuote + 1;
        continue;
      }
    }
    if (char === "[" && i + 1 < value.length && value[i + 1] === ":") {
      const closePos = value.indexOf(":]", i + 2);
      if (closePos !== -1) {
        i = closePos + 2;
        continue;
      }
    }
    if (char === "[" && i + 1 < value.length && (value[i + 1] === "." || value[i + 1] === "=")) {
      const closeChar = value[i + 1];
      const closeSeq = `${closeChar}]`;
      const closePos = value.indexOf(closeSeq, i + 2);
      if (closePos !== -1) {
        i = closePos + 2;
        continue;
      }
    }
    i++;
  }
  return -1;
}
__name$1(findCharacterClassEnd, "findCharacterClassEnd");
function parseAnsiCQuoted(_p2, value, start2) {
  let result2 = "";
  let i = start2;
  while (i < value.length && value[i] !== "'") {
    const char = value[i];
    if (char === "\\" && i + 1 < value.length) {
      const next = value[i + 1];
      switch (next) {
        case "n":
          result2 += "\n";
          i += 2;
          break;
        case "t":
          result2 += "	";
          i += 2;
          break;
        case "r":
          result2 += "\r";
          i += 2;
          break;
        case "\\":
          result2 += "\\";
          i += 2;
          break;
        case "'":
          result2 += "'";
          i += 2;
          break;
        case '"':
          result2 += '"';
          i += 2;
          break;
        case "a":
          result2 += "\x07";
          i += 2;
          break;
        case "b":
          result2 += "\b";
          i += 2;
          break;
        case "e":
        case "E":
          result2 += "\x1B";
          i += 2;
          break;
        case "f":
          result2 += "\f";
          i += 2;
          break;
        case "v":
          result2 += "\v";
          i += 2;
          break;
        case "x": {
          const bytes = [];
          let j = i;
          while (j + 1 < value.length && value[j] === "\\" && value[j + 1] === "x") {
            const hex = value.slice(j + 2, j + 4);
            const code = parseInt(hex, 16);
            if (!Number.isNaN(code) && hex.length > 0) {
              bytes.push(code);
              j += 2 + hex.length;
            } else {
              break;
            }
          }
          if (bytes.length > 0) {
            result2 += decodeUtf8WithRecovery(bytes);
            i = j;
          } else {
            result2 += "\\x";
            i += 2;
          }
          break;
        }
        case "u": {
          const hex = value.slice(i + 2, i + 6);
          const code = parseInt(hex, 16);
          if (!Number.isNaN(code)) {
            result2 += String.fromCharCode(code);
            i += 6;
          } else {
            result2 += "\\u";
            i += 2;
          }
          break;
        }
        case "c": {
          if (i + 2 < value.length) {
            const ctrlChar = value[i + 2];
            const code = ctrlChar.charCodeAt(0) & 31;
            result2 += String.fromCharCode(code);
            i += 3;
          } else {
            result2 += "\\c";
            i += 2;
          }
          break;
        }
        case "0":
        case "1":
        case "2":
        case "3":
        case "4":
        case "5":
        case "6":
        case "7": {
          let octal = "";
          let j = i + 1;
          while (j < value.length && j < i + 4 && /[0-7]/.test(value[j])) {
            octal += value[j];
            j++;
          }
          const code = parseInt(octal, 8);
          result2 += String.fromCharCode(code);
          i = j;
          break;
        }
        default:
          result2 += char;
          i++;
      }
    } else {
      result2 += char;
      i++;
    }
  }
  if (i < value.length && value[i] === "'") {
    i++;
  }
  return {
    part: AST.literal(result2),
    endIndex: i
  };
}
__name$1(parseAnsiCQuoted, "parseAnsiCQuoted");
function parseArithExprFromString(p, str) {
  const trimmed = str.trim();
  if (trimmed === "") {
    return {
      type: "ArithmeticExpression",
      expression: { type: "ArithNumber", value: 0 }
    };
  }
  return parseArithmeticExpression(p, trimmed);
}
__name$1(parseArithExprFromString, "parseArithExprFromString");
function splitBraceItems(inner) {
  const items = [];
  let current = "";
  let depth = 0;
  for (let i = 0; i < inner.length; i++) {
    const c = inner[i];
    if (c === "{") {
      depth++;
      current += c;
    } else if (c === "}") {
      depth--;
      current += c;
    } else if (c === "," && depth === 0) {
      items.push(current);
      current = "";
    } else {
      current += c;
    }
  }
  items.push(current);
  return items;
}
__name$1(splitBraceItems, "splitBraceItems");
function tryParseBraceExpansion(p, value, start2, parseWordPartsFn) {
  const closeIdx = findMatchingBracket(p, value, start2, "{", "}");
  if (closeIdx === -1)
    return null;
  const inner = value.slice(start2 + 1, closeIdx);
  const rangeMatch = inner.match(/^(-?\d+)\.\.(-?\d+)(?:\.\.(-?\d+))?$/);
  if (rangeMatch) {
    return {
      part: {
        type: "BraceExpansion",
        items: [
          {
            type: "Range",
            start: Number.parseInt(rangeMatch[1], 10),
            end: Number.parseInt(rangeMatch[2], 10),
            step: rangeMatch[3] ? Number.parseInt(rangeMatch[3], 10) : void 0,
            // Store original strings for zero-padding support
            startStr: rangeMatch[1],
            endStr: rangeMatch[2]
          }
        ]
      },
      endIndex: closeIdx + 1
    };
  }
  const charRangeMatch = inner.match(/^([a-zA-Z])\.\.([a-zA-Z])(?:\.\.(-?\d+))?$/);
  if (charRangeMatch) {
    return {
      part: {
        type: "BraceExpansion",
        items: [
          {
            type: "Range",
            start: charRangeMatch[1],
            end: charRangeMatch[2],
            step: charRangeMatch[3] ? Number.parseInt(charRangeMatch[3], 10) : void 0
          }
        ]
      },
      endIndex: closeIdx + 1
    };
  }
  if (inner.includes(",") && parseWordPartsFn) {
    const rawItems = splitBraceItems(inner);
    const items = rawItems.map((s) => ({
      type: "Word",
      word: AST.word(parseWordPartsFn(p, s, false, false, false))
    }));
    return {
      part: { type: "BraceExpansion", items },
      endIndex: closeIdx + 1
    };
  }
  if (inner.includes(",")) {
    const rawItems = splitBraceItems(inner);
    const items = rawItems.map((s) => ({
      type: "Word",
      word: AST.word([AST.literal(s)])
    }));
    return {
      part: { type: "BraceExpansion", items },
      endIndex: closeIdx + 1
    };
  }
  return null;
}
__name$1(tryParseBraceExpansion, "tryParseBraceExpansion");
function wordToString(_p2, word) {
  let result2 = "";
  for (const part of word.parts) {
    switch (part.type) {
      case "Literal":
        result2 += part.value;
        break;
      case "SingleQuoted":
        result2 += `'${part.value}'`;
        break;
      case "Escaped":
        result2 += part.value;
        break;
      case "DoubleQuoted":
        result2 += '"';
        for (const inner of part.parts) {
          if (inner.type === "Literal" || inner.type === "Escaped") {
            result2 += inner.value;
          } else if (inner.type === "ParameterExpansion") {
            result2 += `\${${inner.parameter}}`;
          }
        }
        result2 += '"';
        break;
      case "ParameterExpansion":
        result2 += `\${${part.parameter}}`;
        break;
      case "Glob":
        result2 += part.pattern;
        break;
      case "TildeExpansion":
        result2 += "~";
        if (part.user) {
          result2 += part.user;
        }
        break;
      case "BraceExpansion": {
        result2 += "{";
        const braceItems = [];
        for (const item of part.items) {
          if (item.type === "Range") {
            const startVal = item.startStr ?? String(item.start);
            const endVal = item.endStr ?? String(item.end);
            if (item.step !== void 0) {
              braceItems.push(`${startVal}..${endVal}..${item.step}`);
            } else {
              braceItems.push(`${startVal}..${endVal}`);
            }
          } else {
            braceItems.push(wordToString(_p2, item.word));
          }
        }
        if (braceItems.length === 1 && part.items[0].type === "Range") {
          result2 += braceItems[0];
        } else {
          result2 += braceItems.join(",");
        }
        result2 += "}";
        break;
      }
      default:
        result2 += part.type;
    }
  }
  return result2;
}
__name$1(wordToString, "wordToString");
function tokenToRedirectOp(_p2, type) {
  const map = {
    [TokenType.LESS]: "<",
    [TokenType.GREAT]: ">",
    [TokenType.DGREAT]: ">>",
    [TokenType.LESSAND]: "<&",
    [TokenType.GREATAND]: ">&",
    [TokenType.LESSGREAT]: "<>",
    [TokenType.CLOBBER]: ">|",
    [TokenType.TLESS]: "<<<",
    [TokenType.AND_GREAT]: "&>",
    [TokenType.AND_DGREAT]: "&>>",
    [TokenType.DLESS]: "<",
    // Here-doc operator is <
    [TokenType.DLESSDASH]: "<"
  };
  return map[type] || ">";
}
__name$1(tokenToRedirectOp, "tokenToRedirectOp");
function isRedirection(p) {
  const currentToken = p.current();
  const t = currentToken.type;
  if (t === TokenType.NUMBER) {
    const nextToken = p.peek(1);
    if (currentToken.end !== nextToken.start) {
      return false;
    }
    return REDIRECTION_AFTER_NUMBER.has(nextToken.type);
  }
  if (t === TokenType.FD_VARIABLE) {
    const nextToken = p.peek(1);
    return REDIRECTION_AFTER_FD_VARIABLE.has(nextToken.type);
  }
  return REDIRECTION_TOKENS.has(t);
}
__name$1(isRedirection, "isRedirection");
function parseRedirection(p) {
  let fd = null;
  let fdVariable;
  if (p.check(TokenType.NUMBER)) {
    fd = Number.parseInt(p.advance().value, 10);
  } else if (p.check(TokenType.FD_VARIABLE)) {
    fdVariable = p.advance().value;
  }
  const opToken = p.advance();
  const operator = tokenToRedirectOp(p, opToken.type);
  if (opToken.type === TokenType.DLESS || opToken.type === TokenType.DLESSDASH) {
    return parseHeredocStart(p, operator, fd, opToken.type === TokenType.DLESSDASH);
  }
  if (!p.isWord()) {
    p.error("Expected redirection target");
  }
  const target = p.parseWord();
  return AST.redirection(operator, target, fd, fdVariable);
}
__name$1(parseRedirection, "parseRedirection");
function parseHeredocStart(p, _operator, fd, stripTabs) {
  if (!p.isWord()) {
    p.error("Expected here-document delimiter");
  }
  const delimToken = p.advance();
  let delimiter = delimToken.value;
  const quoted = delimToken.quoted || false;
  if (delimiter.startsWith("'") && delimiter.endsWith("'")) {
    delimiter = delimiter.slice(1, -1);
  } else if (delimiter.startsWith('"') && delimiter.endsWith('"')) {
    delimiter = delimiter.slice(1, -1);
  }
  const redirect = AST.redirection(
    stripTabs ? "<<-" : "<<",
    // Use proper here-doc operator
    AST.hereDoc(delimiter, AST.word([]), stripTabs, quoted),
    fd
  );
  p.addPendingHeredoc(redirect, delimiter, stripTabs, quoted);
  return redirect;
}
__name$1(parseHeredocStart, "parseHeredocStart");
function parseSimpleCommand(p) {
  const startLine = p.current().line;
  const assignments = [];
  let name = null;
  const args = [];
  const redirections = [];
  while (p.check(TokenType.ASSIGNMENT_WORD) || isRedirection(p)) {
    p.checkIterationLimit();
    if (p.check(TokenType.ASSIGNMENT_WORD)) {
      assignments.push(parseAssignment$1(p));
    } else {
      redirections.push(parseRedirection(p));
    }
  }
  if (p.isWord()) {
    name = p.parseWord();
  } else if (assignments.length > 0 && (p.check(TokenType.DBRACK_START) || p.check(TokenType.DPAREN_START))) {
    const token = p.advance();
    name = AST.word([AST.literal(token.value)]);
  }
  while ((!p.isStatementEnd() || p.check(TokenType.RBRACE)) && !p.check(TokenType.PIPE, TokenType.PIPE_AMP)) {
    p.checkIterationLimit();
    if (isRedirection(p)) {
      redirections.push(parseRedirection(p));
    } else if (p.check(TokenType.RBRACE)) {
      const token = p.advance();
      args.push(p.parseWordFromString(token.value, false, false));
    } else if (p.check(TokenType.LBRACE)) {
      const token = p.advance();
      args.push(p.parseWordFromString(token.value, false, false));
    } else if (p.check(TokenType.DBRACK_END)) {
      const token = p.advance();
      args.push(p.parseWordFromString(token.value, false, false));
    } else if (p.isWord()) {
      args.push(p.parseWord());
    } else if (p.check(TokenType.ASSIGNMENT_WORD)) {
      const token = p.advance();
      const tokenValue = token.value;
      const endsWithEq = tokenValue.endsWith("=");
      const endsWithEqParen = tokenValue.endsWith("=(");
      if ((endsWithEq || endsWithEqParen) && (endsWithEqParen || p.check(TokenType.LPAREN))) {
        const baseName = endsWithEqParen ? tokenValue.slice(0, -2) : tokenValue.slice(0, -1);
        if (!endsWithEqParen) {
          p.expect(TokenType.LPAREN);
        }
        const elements = parseArrayElements$1(p);
        p.expect(TokenType.RPAREN);
        const elemStrings = elements.map((e) => wordToString(p, e));
        const arrayStr = `${baseName}=(${elemStrings.join(" ")})`;
        args.push(p.parseWordFromString(arrayStr, false, false));
      } else {
        args.push(p.parseWordFromString(tokenValue, token.quoted, token.singleQuoted));
      }
    } else if (p.check(TokenType.LPAREN)) {
      p.error(`syntax error near unexpected token \`('`);
    } else {
      break;
    }
  }
  const node = AST.simpleCommand(name, args, assignments, redirections);
  node.line = startLine;
  return node;
}
__name$1(parseSimpleCommand, "parseSimpleCommand");
function parseAssignment$1(p) {
  const token = p.expect(TokenType.ASSIGNMENT_WORD);
  const value = token.value;
  const nameMatch = value.match(/^[a-zA-Z_][a-zA-Z0-9_]*/);
  if (!nameMatch) {
    p.error(`Invalid assignment: ${value}`);
  }
  const name = nameMatch[0];
  let subscript;
  let pos = name.length;
  if (value[pos] === "[") {
    let depth = 0;
    const subscriptStart = pos + 1;
    for (; pos < value.length; pos++) {
      if (value[pos] === "[")
        depth++;
      else if (value[pos] === "]") {
        depth--;
        if (depth === 0)
          break;
      }
    }
    if (depth !== 0) {
      p.error(`Invalid assignment: ${value}`);
    }
    subscript = value.slice(subscriptStart, pos);
    pos++;
  }
  const append = value[pos] === "+";
  if (append)
    pos++;
  if (value[pos] !== "=") {
    p.error(`Invalid assignment: ${value}`);
  }
  pos++;
  const valueStr = value.slice(pos);
  if (valueStr === "(") {
    const elements = parseArrayElements$1(p);
    p.expect(TokenType.RPAREN);
    const assignName2 = subscript !== void 0 ? `${name}[${subscript}]` : name;
    return AST.assignment(assignName2, null, append, elements);
  }
  if (valueStr === "" && p.check(TokenType.LPAREN)) {
    const currentToken = p.current();
    if (token.end === currentToken.start) {
      p.advance();
      const elements = parseArrayElements$1(p);
      p.expect(TokenType.RPAREN);
      const assignName2 = subscript !== void 0 ? `${name}[${subscript}]` : name;
      return AST.assignment(assignName2, null, append, elements);
    }
  }
  const wordValue = valueStr ? p.parseWordFromString(valueStr, token.quoted, token.singleQuoted, true) : null;
  const assignName = subscript !== void 0 ? `${name}[${subscript}]` : name;
  return AST.assignment(assignName, wordValue, append, null);
}
__name$1(parseAssignment$1, "parseAssignment");
var INVALID_ARRAY_TOKENS = /* @__PURE__ */ new Set([
  TokenType.AMP,
  // &
  TokenType.PIPE,
  // |
  TokenType.PIPE_AMP,
  // |&
  TokenType.SEMICOLON,
  // ;
  TokenType.AND_AND,
  // &&
  TokenType.OR_OR,
  // ||
  TokenType.DSEMI,
  // ;;
  TokenType.SEMI_AND,
  // ;&
  TokenType.SEMI_SEMI_AND
  // ;;&
]);
function parseArrayElements$1(p) {
  const elements = [];
  p.skipNewlines();
  while (!p.check(TokenType.RPAREN, TokenType.EOF)) {
    p.checkIterationLimit();
    if (p.isWord()) {
      elements.push(p.parseWord());
    } else if (INVALID_ARRAY_TOKENS.has(p.current().type)) {
      p.error(`syntax error near unexpected token \`${p.current().value}'`);
    } else {
      p.advance();
    }
    p.skipNewlines();
  }
  return elements;
}
__name$1(parseArrayElements$1, "parseArrayElements");
function parseIf(p, options) {
  p.expect(TokenType.IF);
  const clauses = [];
  const condition = p.parseCompoundList();
  p.expect(TokenType.THEN);
  const body = p.parseCompoundList();
  if (body.length === 0) {
    const nextTok = p.check(TokenType.FI) ? "fi" : p.check(TokenType.ELSE) ? "else" : p.check(TokenType.ELIF) ? "elif" : "fi";
    p.error(`syntax error near unexpected token \`${nextTok}'`);
  }
  clauses.push({ condition, body });
  while (p.check(TokenType.ELIF)) {
    p.advance();
    const elifCondition = p.parseCompoundList();
    p.expect(TokenType.THEN);
    const elifBody = p.parseCompoundList();
    if (elifBody.length === 0) {
      const nextTok = p.check(TokenType.FI) ? "fi" : p.check(TokenType.ELSE) ? "else" : p.check(TokenType.ELIF) ? "elif" : "fi";
      p.error(`syntax error near unexpected token \`${nextTok}'`);
    }
    clauses.push({ condition: elifCondition, body: elifBody });
  }
  let elseBody = null;
  if (p.check(TokenType.ELSE)) {
    p.advance();
    elseBody = p.parseCompoundList();
    if (elseBody.length === 0) {
      p.error("syntax error near unexpected token `fi'");
    }
  }
  p.expect(TokenType.FI);
  const redirections = options?.skipRedirections ? [] : p.parseOptionalRedirections();
  return AST.ifNode(clauses, elseBody, redirections);
}
__name$1(parseIf, "parseIf");
function parseFor(p, options) {
  const forToken = p.expect(TokenType.FOR);
  if (p.check(TokenType.DPAREN_START)) {
    return parseCStyleFor(p, options, forToken.line);
  }
  if (!p.isWord()) {
    p.error("Expected variable name in for loop");
  }
  const varToken = p.advance();
  const variable = varToken.value;
  let words = null;
  p.skipNewlines();
  if (p.check(TokenType.IN)) {
    p.advance();
    words = [];
    while (!p.check(TokenType.SEMICOLON, TokenType.NEWLINE, TokenType.DO, TokenType.EOF)) {
      if (p.isWord()) {
        words.push(p.parseWord());
      } else {
        break;
      }
    }
  }
  if (p.check(TokenType.SEMICOLON)) {
    p.advance();
  }
  p.skipNewlines();
  p.expect(TokenType.DO);
  const body = p.parseCompoundList();
  p.expect(TokenType.DONE);
  const redirections = options?.skipRedirections ? [] : p.parseOptionalRedirections();
  return AST.forNode(variable, words, body, redirections);
}
__name$1(parseFor, "parseFor");
function parseCStyleFor(p, options, startLine) {
  p.expect(TokenType.DPAREN_START);
  let init = null;
  let condition = null;
  let update = null;
  const parts = ["", "", ""];
  let partIdx = 0;
  let depth = 0;
  while (!p.check(TokenType.DPAREN_END, TokenType.EOF)) {
    const token = p.advance();
    if (token.type === TokenType.SEMICOLON && depth === 0) {
      partIdx++;
      if (partIdx > 2)
        break;
    } else {
      if (token.value === "(")
        depth++;
      if (token.value === ")")
        depth--;
      parts[partIdx] += token.value;
    }
  }
  p.expect(TokenType.DPAREN_END);
  if (parts[0].trim()) {
    init = parseArithmeticExpression(p, parts[0].trim());
  }
  if (parts[1].trim()) {
    condition = parseArithmeticExpression(p, parts[1].trim());
  }
  if (parts[2].trim()) {
    update = parseArithmeticExpression(p, parts[2].trim());
  }
  p.skipNewlines();
  if (p.check(TokenType.SEMICOLON)) {
    p.advance();
  }
  p.skipNewlines();
  let body;
  if (p.check(TokenType.LBRACE)) {
    p.advance();
    body = p.parseCompoundList();
    p.expect(TokenType.RBRACE);
  } else {
    p.expect(TokenType.DO);
    body = p.parseCompoundList();
    p.expect(TokenType.DONE);
  }
  const redirections = options?.skipRedirections ? [] : p.parseOptionalRedirections();
  return {
    type: "CStyleFor",
    init,
    condition,
    update,
    body,
    redirections,
    line: startLine
  };
}
__name$1(parseCStyleFor, "parseCStyleFor");
function parseWhile(p, options) {
  p.expect(TokenType.WHILE);
  const condition = p.parseCompoundList();
  p.expect(TokenType.DO);
  const body = p.parseCompoundList();
  if (body.length === 0) {
    p.error("syntax error near unexpected token `done'");
  }
  p.expect(TokenType.DONE);
  const redirections = options?.skipRedirections ? [] : p.parseOptionalRedirections();
  return AST.whileNode(condition, body, redirections);
}
__name$1(parseWhile, "parseWhile");
function parseUntil(p, options) {
  p.expect(TokenType.UNTIL);
  const condition = p.parseCompoundList();
  p.expect(TokenType.DO);
  const body = p.parseCompoundList();
  if (body.length === 0) {
    p.error("syntax error near unexpected token `done'");
  }
  p.expect(TokenType.DONE);
  const redirections = options?.skipRedirections ? [] : p.parseOptionalRedirections();
  return AST.untilNode(condition, body, redirections);
}
__name$1(parseUntil, "parseUntil");
function parseCase(p, options) {
  p.expect(TokenType.CASE);
  if (!p.isWord()) {
    p.error("Expected word after 'case'");
  }
  const word = p.parseWord();
  p.skipNewlines();
  p.expect(TokenType.IN);
  p.skipNewlines();
  const items = [];
  while (!p.check(TokenType.ESAC, TokenType.EOF)) {
    p.checkIterationLimit();
    const posBefore = p.getPos();
    const item = parseCaseItem(p);
    if (item) {
      items.push(item);
    }
    p.skipNewlines();
    if (p.getPos() === posBefore && !item) {
      break;
    }
  }
  p.expect(TokenType.ESAC);
  const redirections = options?.skipRedirections ? [] : p.parseOptionalRedirections();
  return AST.caseNode(word, items, redirections);
}
__name$1(parseCase, "parseCase");
function parseCaseItem(p) {
  if (p.check(TokenType.LPAREN)) {
    p.advance();
  }
  const patterns = [];
  while (p.isWord()) {
    patterns.push(p.parseWord());
    if (p.check(TokenType.PIPE)) {
      p.advance();
    } else {
      break;
    }
  }
  if (patterns.length === 0) {
    return null;
  }
  p.expect(TokenType.RPAREN);
  p.skipNewlines();
  const body = [];
  while (!p.check(TokenType.DSEMI, TokenType.SEMI_AND, TokenType.SEMI_SEMI_AND, TokenType.ESAC, TokenType.EOF)) {
    p.checkIterationLimit();
    if (p.isWord() && p.peek(1).type === TokenType.RPAREN) {
      p.error(`syntax error near unexpected token \`)'`);
    }
    if (p.check(TokenType.LPAREN) && p.peek(1).type === TokenType.WORD) {
      p.error(`syntax error near unexpected token \`${p.peek(1).value}'`);
    }
    const posBefore = p.getPos();
    const stmt = p.parseStatement();
    if (stmt) {
      body.push(stmt);
    }
    p.skipSeparators(false);
    if (p.getPos() === posBefore && !stmt) {
      break;
    }
  }
  let terminator = ";;";
  if (p.check(TokenType.DSEMI)) {
    p.advance();
    terminator = ";;";
  } else if (p.check(TokenType.SEMI_AND)) {
    p.advance();
    terminator = ";&";
  } else if (p.check(TokenType.SEMI_SEMI_AND)) {
    p.advance();
    terminator = ";;&";
  }
  return AST.caseItem(patterns, body, terminator);
}
__name$1(parseCaseItem, "parseCaseItem");
function parseSubshell(p, options) {
  p.expect(TokenType.LPAREN);
  const body = p.parseCompoundList();
  p.expect(TokenType.RPAREN);
  const redirections = options?.skipRedirections ? [] : p.parseOptionalRedirections();
  return AST.subshell(body, redirections);
}
__name$1(parseSubshell, "parseSubshell");
function parseGroup(p, options) {
  p.expect(TokenType.LBRACE);
  const body = p.parseCompoundList();
  p.expect(TokenType.RBRACE);
  const redirections = options?.skipRedirections ? [] : p.parseOptionalRedirections();
  return AST.group(body, redirections);
}
__name$1(parseGroup, "parseGroup");
var UNARY_OPS = [
  "-a",
  "-b",
  "-c",
  "-d",
  "-e",
  "-f",
  "-g",
  "-h",
  "-k",
  "-p",
  "-r",
  "-s",
  "-t",
  "-u",
  "-w",
  "-x",
  "-G",
  "-L",
  "-N",
  "-O",
  "-S",
  "-z",
  "-n",
  "-o",
  "-v",
  "-R"
];
var BINARY_OPS = [
  "==",
  "!=",
  "=~",
  "<",
  ">",
  "-eq",
  "-ne",
  "-lt",
  "-le",
  "-gt",
  "-ge",
  "-nt",
  "-ot",
  "-ef"
];
function isCondOperand(p) {
  return p.isWord() || p.check(TokenType.LBRACE) || p.check(TokenType.RBRACE) || p.check(TokenType.ASSIGNMENT_WORD);
}
__name$1(isCondOperand, "isCondOperand");
function parsePatternWord(p) {
  if (p.check(TokenType.BANG) && p.peek(1).type === TokenType.LPAREN) {
    p.advance();
    p.advance();
    let depth = 1;
    let pattern = "!(";
    while (depth > 0 && !p.check(TokenType.EOF)) {
      if (p.check(TokenType.LPAREN)) {
        depth++;
        pattern += "(";
        p.advance();
      } else if (p.check(TokenType.RPAREN)) {
        depth--;
        if (depth > 0) {
          pattern += ")";
        }
        p.advance();
      } else if (p.isWord()) {
        pattern += p.advance().value;
      } else if (p.check(TokenType.PIPE)) {
        pattern += "|";
        p.advance();
      } else {
        break;
      }
    }
    pattern += ")";
    return p.parseWordFromString(pattern, false, false, false, false, true);
  }
  return p.parseWordNoBraceExpansion();
}
__name$1(parsePatternWord, "parsePatternWord");
function parseConditionalExpression(p) {
  p.skipNewlines();
  return parseCondOr(p);
}
__name$1(parseConditionalExpression, "parseConditionalExpression");
function parseCondOr(p) {
  let left = parseCondAnd(p);
  p.skipNewlines();
  while (p.check(TokenType.OR_OR)) {
    p.advance();
    p.skipNewlines();
    const right = parseCondAnd(p);
    left = { type: "CondOr", left, right };
    p.skipNewlines();
  }
  return left;
}
__name$1(parseCondOr, "parseCondOr");
function parseCondAnd(p) {
  let left = parseCondNot(p);
  p.skipNewlines();
  while (p.check(TokenType.AND_AND)) {
    p.advance();
    p.skipNewlines();
    const right = parseCondNot(p);
    left = { type: "CondAnd", left, right };
    p.skipNewlines();
  }
  return left;
}
__name$1(parseCondAnd, "parseCondAnd");
function parseCondNot(p) {
  p.skipNewlines();
  if (p.check(TokenType.BANG)) {
    p.advance();
    p.skipNewlines();
    const operand = parseCondNot(p);
    return { type: "CondNot", operand };
  }
  return parseCondPrimary(p);
}
__name$1(parseCondNot, "parseCondNot");
function parseCondPrimary(p) {
  if (p.check(TokenType.LPAREN)) {
    p.advance();
    const expression = parseConditionalExpression(p);
    p.expect(TokenType.RPAREN);
    return { type: "CondGroup", expression };
  }
  if (isCondOperand(p)) {
    const firstToken = p.current();
    const first = firstToken.value;
    if (UNARY_OPS.includes(first) && !firstToken.quoted) {
      p.advance();
      if (p.check(TokenType.DBRACK_END)) {
        p.error(`Expected operand after ${first}`);
      }
      if (isCondOperand(p)) {
        const operand = p.parseWordNoBraceExpansion();
        return {
          type: "CondUnary",
          operator: first,
          operand
        };
      }
      const badToken = p.current();
      p.error(`unexpected argument \`${badToken.value}' to conditional unary operator`);
    }
    const left = p.parseWordNoBraceExpansion();
    if (p.isWord() && BINARY_OPS.includes(p.current().value)) {
      const operator = p.advance().value;
      let right;
      if (operator === "=~") {
        right = parseRegexPattern(p);
      } else if (operator === "==" || operator === "!=") {
        right = parsePatternWord(p);
      } else {
        right = p.parseWordNoBraceExpansion();
      }
      return {
        type: "CondBinary",
        operator,
        left,
        right
      };
    }
    if (p.check(TokenType.LESS)) {
      p.advance();
      const right = p.parseWordNoBraceExpansion();
      return {
        type: "CondBinary",
        operator: "<",
        left,
        right
      };
    }
    if (p.check(TokenType.GREAT)) {
      p.advance();
      const right = p.parseWordNoBraceExpansion();
      return {
        type: "CondBinary",
        operator: ">",
        left,
        right
      };
    }
    if (p.isWord() && p.current().value === "=") {
      p.advance();
      const right = parsePatternWord(p);
      return {
        type: "CondBinary",
        operator: "==",
        left,
        right
      };
    }
    return { type: "CondWord", word: left };
  }
  p.error("Expected conditional expression");
}
__name$1(parseCondPrimary, "parseCondPrimary");
function parseRegexPattern(p) {
  const parts = [];
  let parenDepth = 0;
  let lastTokenEnd = -1;
  const input = p.getInput();
  const isTerminator = /* @__PURE__ */ __name$1(() => p.check(TokenType.DBRACK_END) || p.check(TokenType.AND_AND) || p.check(TokenType.OR_OR) || p.check(TokenType.NEWLINE) || p.check(TokenType.EOF), "isTerminator");
  while (!isTerminator()) {
    const currentToken = p.current();
    const hasGap = lastTokenEnd >= 0 && currentToken.start > lastTokenEnd;
    if (parenDepth === 0 && hasGap) {
      break;
    }
    if (parenDepth > 0 && hasGap) {
      const whitespace = input.slice(lastTokenEnd, currentToken.start);
      parts.push({ type: "Literal", value: whitespace });
    }
    if (p.isWord() || p.check(TokenType.ASSIGNMENT_WORD)) {
      const word = p.parseWordForRegex();
      parts.push(...word.parts);
      lastTokenEnd = p.peek(-1).end;
    } else if (p.check(TokenType.LPAREN)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: "(" });
      parenDepth++;
      lastTokenEnd = token.end;
    } else if (p.check(TokenType.DPAREN_START)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: "((" });
      parenDepth += 2;
      lastTokenEnd = token.end;
    } else if (p.check(TokenType.DPAREN_END)) {
      if (parenDepth >= 2) {
        const token = p.advance();
        parts.push({ type: "Literal", value: "))" });
        parenDepth -= 2;
        lastTokenEnd = token.end;
      } else if (parenDepth === 1) {
        break;
      } else {
        break;
      }
    } else if (p.check(TokenType.RPAREN)) {
      if (parenDepth > 0) {
        const token = p.advance();
        parts.push({ type: "Literal", value: ")" });
        parenDepth--;
        lastTokenEnd = token.end;
      } else {
        break;
      }
    } else if (p.check(TokenType.PIPE)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: "|" });
      lastTokenEnd = token.end;
    } else if (p.check(TokenType.SEMICOLON)) {
      if (parenDepth > 0) {
        const token = p.advance();
        parts.push({ type: "Literal", value: ";" });
        lastTokenEnd = token.end;
      } else {
        break;
      }
    } else if (parenDepth > 0 && p.check(TokenType.LESS)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: "<" });
      lastTokenEnd = token.end;
    } else if (parenDepth > 0 && p.check(TokenType.GREAT)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: ">" });
      lastTokenEnd = token.end;
    } else if (parenDepth > 0 && p.check(TokenType.DGREAT)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: ">>" });
      lastTokenEnd = token.end;
    } else if (parenDepth > 0 && p.check(TokenType.DLESS)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: "<<" });
      lastTokenEnd = token.end;
    } else if (parenDepth > 0 && p.check(TokenType.LESSAND)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: "<&" });
      lastTokenEnd = token.end;
    } else if (parenDepth > 0 && p.check(TokenType.GREATAND)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: ">&" });
      lastTokenEnd = token.end;
    } else if (parenDepth > 0 && p.check(TokenType.LESSGREAT)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: "<>" });
      lastTokenEnd = token.end;
    } else if (parenDepth > 0 && p.check(TokenType.CLOBBER)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: ">|" });
      lastTokenEnd = token.end;
    } else if (parenDepth > 0 && p.check(TokenType.TLESS)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: "<<<" });
      lastTokenEnd = token.end;
    } else if (parenDepth > 0 && p.check(TokenType.AMP)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: "&" });
      lastTokenEnd = token.end;
    } else if (parenDepth > 0 && p.check(TokenType.LBRACE)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: "{" });
      lastTokenEnd = token.end;
    } else if (parenDepth > 0 && p.check(TokenType.RBRACE)) {
      const token = p.advance();
      parts.push({ type: "Literal", value: "}" });
      lastTokenEnd = token.end;
    } else {
      break;
    }
  }
  if (parts.length === 0) {
    p.error("Expected regex pattern after =~");
  }
  return { type: "Word", parts };
}
__name$1(parseRegexPattern, "parseRegexPattern");
function findExtglobClose(value, openIdx) {
  let depth = 1;
  let i = openIdx + 1;
  while (i < value.length && depth > 0) {
    const c = value[i];
    if (c === "\\") {
      i += 2;
      continue;
    }
    if ("@*+?!".includes(c) && i + 1 < value.length && value[i + 1] === "(") {
      i++;
      depth++;
      i++;
      continue;
    }
    if (c === "(") {
      depth++;
    } else if (c === ")") {
      depth--;
      if (depth === 0) {
        return i;
      }
    }
    i++;
  }
  return -1;
}
__name$1(findExtglobClose, "findExtglobClose");
function parseSimpleParameter(_p2, value, start2) {
  let i = start2 + 1;
  const char = value[i];
  if ("@*#?$!-0123456789".includes(char)) {
    return {
      part: AST.parameterExpansion(char),
      endIndex: i + 1
    };
  }
  let name = "";
  while (i < value.length && /[a-zA-Z0-9_]/.test(value[i])) {
    name += value[i];
    i++;
  }
  return {
    part: AST.parameterExpansion(name),
    endIndex: i
  };
}
__name$1(parseSimpleParameter, "parseSimpleParameter");
function parseParameterExpansion(p, value, start2, quoted = false) {
  let i = start2 + 2;
  let indirection = false;
  if (value[i] === "!") {
    indirection = true;
    i++;
  }
  let lengthOp = false;
  if (value[i] === "#" && !/[}:#%/^,]/.test(value[i + 1] || "}")) {
    lengthOp = true;
    i++;
  }
  let name = "";
  const firstChar = value[i];
  if (/[@*#?$!-]/.test(firstChar) && !/[a-zA-Z0-9_]/.test(value[i + 1] || "")) {
    name = firstChar;
    i++;
  } else {
    while (i < value.length && /[a-zA-Z0-9_]/.test(value[i])) {
      name += value[i];
      i++;
    }
  }
  if (value[i] === "[") {
    const closeIdx = findMatchingBracket(p, value, i, "[", "]");
    name += value.slice(i, closeIdx + 1);
    i = closeIdx + 1;
    if (value[i] === "[") {
      let depth = 1;
      let j = i;
      while (j < value.length && depth > 0) {
        if (value[j] === "{")
          depth++;
        else if (value[j] === "}")
          depth--;
        if (depth > 0)
          j++;
      }
      const badText = value.slice(start2 + 2, j);
      return {
        part: AST.parameterExpansion("", {
          type: "BadSubstitution",
          text: badText
        }),
        endIndex: j + 1
      };
    }
  }
  if (name === "" && !indirection && !lengthOp && value[i] !== "}") {
    let depth = 1;
    let j = i;
    while (j < value.length && depth > 0) {
      if (value[j] === "{")
        depth++;
      else if (value[j] === "}")
        depth--;
      if (depth > 0)
        j++;
    }
    if (depth > 0) {
      throw new ParseException("unexpected EOF while looking for matching '}'", 0, 0);
    }
    const badText = value.slice(start2 + 2, j);
    return {
      part: AST.parameterExpansion("", {
        type: "BadSubstitution",
        text: badText
      }),
      endIndex: j + 1
    };
  }
  let operation = null;
  if (indirection) {
    const arrayKeysMatch = name.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
    if (arrayKeysMatch) {
      if (i < value.length && value[i] !== "}" && /[:=\-+?#%/^,@]/.test(value[i])) {
        const opResult = parseParameterOperation(p, value, i, name, quoted);
        if (opResult.operation) {
          operation = {
            type: "Indirection",
            innerOp: opResult.operation
          };
          i = opResult.endIndex;
        } else {
          operation = {
            type: "ArrayKeys",
            array: arrayKeysMatch[1],
            star: arrayKeysMatch[2] === "*"
          };
          name = "";
        }
      } else {
        operation = {
          type: "ArrayKeys",
          array: arrayKeysMatch[1],
          star: arrayKeysMatch[2] === "*"
        };
        name = "";
      }
    } else if (value[i] === "*" || value[i] === "@" && !/[QPaAEKkuUL]/.test(value[i + 1] || "")) {
      const suffix = value[i];
      i++;
      operation = {
        type: "VarNamePrefix",
        prefix: name,
        star: suffix === "*"
      };
      name = "";
    } else {
      if (i < value.length && value[i] !== "}" && /[:=\-+?#%/^,@]/.test(value[i])) {
        const opResult = parseParameterOperation(p, value, i, name, quoted);
        if (opResult.operation) {
          operation = {
            type: "Indirection",
            innerOp: opResult.operation
          };
          i = opResult.endIndex;
        } else {
          operation = { type: "Indirection" };
        }
      } else {
        operation = { type: "Indirection" };
      }
    }
  } else if (lengthOp) {
    if (value[i] === ":") {
      operation = { type: "LengthSliceError" };
      while (i < value.length && value[i] !== "}") {
        i++;
      }
    } else if (value[i] !== "}" && /[-+=?]/.test(value[i])) {
      p.error(`\${#${name}${value.slice(i, value.indexOf("}", i))}}: bad substitution`);
    } else if (value[i] === "/") {
      p.error(`\${#${name}${value.slice(i, value.indexOf("}", i))}}: bad substitution`);
    } else {
      operation = { type: "Length" };
    }
  }
  if (!operation && i < value.length && value[i] !== "}") {
    const opResult = parseParameterOperation(p, value, i, name, quoted);
    operation = opResult.operation;
    i = opResult.endIndex;
  }
  if (i < value.length && value[i] !== "}") {
    const c = value[i];
    if (!/[:\-+=?#%/^,@[]/.test(c)) {
      let endIdx = i;
      while (endIdx < value.length && value[endIdx] !== "}")
        endIdx++;
      const badExp = value.slice(start2, endIdx + 1);
      p.error(`\${${badExp.slice(2, -1)}}: bad substitution`);
    }
  }
  while (i < value.length && value[i] !== "}") {
    i++;
  }
  if (i >= value.length) {
    throw new ParseException("unexpected EOF while looking for matching '}'", 0, 0);
  }
  return {
    part: AST.parameterExpansion(name, operation),
    endIndex: i + 1
  };
}
__name$1(parseParameterExpansion, "parseParameterExpansion");
function parseParameterOperation(p, value, start2, _paramName, quoted = false) {
  let i = start2;
  const char = value[i];
  const nextChar = value[i + 1] || "";
  if (char === ":") {
    const op = nextChar;
    if ("-=?+".includes(op)) {
      const checkEmpty = true;
      i += 2;
      const wordEnd2 = findParameterOperationEnd(p, value, i);
      const wordStr2 = value.slice(i, wordEnd2);
      const wordParts = parseWordParts(
        p,
        wordStr2,
        false,
        false,
        true,
        // isAssignment=true for tilde expansion after : in default values
        false,
        quoted,
        false,
        // noBraceExpansion
        false,
        // regexPattern
        true
      );
      const word = AST.word(wordParts.length > 0 ? wordParts : [AST.literal("")]);
      if (op === "-") {
        return {
          operation: { type: "DefaultValue", word, checkEmpty },
          endIndex: wordEnd2
        };
      }
      if (op === "=") {
        return {
          operation: { type: "AssignDefault", word, checkEmpty },
          endIndex: wordEnd2
        };
      }
      if (op === "?") {
        return {
          operation: { type: "ErrorIfUnset", word, checkEmpty },
          endIndex: wordEnd2
        };
      }
      if (op === "+") {
        return {
          operation: { type: "UseAlternative", word, checkEmpty },
          endIndex: wordEnd2
        };
      }
    }
    i++;
    const wordEnd = findParameterOperationEnd(p, value, i);
    const wordStr = value.slice(i, wordEnd);
    let colonIdx = -1;
    let depth = 0;
    let ternaryDepth = 0;
    for (let j = 0; j < wordStr.length; j++) {
      const c = wordStr[j];
      if (c === "(" || c === "[")
        depth++;
      else if (c === ")" || c === "]")
        depth--;
      else if (c === "?" && depth === 0)
        ternaryDepth++;
      else if (c === ":" && depth === 0) {
        if (ternaryDepth > 0) {
          ternaryDepth--;
        } else {
          colonIdx = j;
          break;
        }
      }
    }
    const offsetStr = colonIdx >= 0 ? wordStr.slice(0, colonIdx) : wordStr;
    const lengthStr = colonIdx >= 0 ? wordStr.slice(colonIdx + 1) : null;
    return {
      operation: {
        type: "Substring",
        offset: parseArithExprFromString(p, offsetStr),
        // Note: lengthStr can be "" (empty string after second colon like ${a::})
        // which should be treated as length 0, not "no length specified"
        length: lengthStr !== null ? parseArithExprFromString(p, lengthStr) : null
      },
      endIndex: wordEnd
    };
  }
  if ("-=?+".includes(char)) {
    i++;
    const wordEnd = findParameterOperationEnd(p, value, i);
    const wordStr = value.slice(i, wordEnd);
    const wordParts = parseWordParts(
      p,
      wordStr,
      false,
      false,
      true,
      // isAssignment=true for tilde expansion after : in default values
      false,
      quoted,
      false,
      // noBraceExpansion
      false,
      // regexPattern
      true
    );
    const word = AST.word(wordParts.length > 0 ? wordParts : [AST.literal("")]);
    if (char === "-") {
      return {
        operation: { type: "DefaultValue", word, checkEmpty: false },
        endIndex: wordEnd
      };
    }
    if (char === "=") {
      return {
        operation: { type: "AssignDefault", word, checkEmpty: false },
        endIndex: wordEnd
      };
    }
    if (char === "?") {
      return {
        operation: {
          type: "ErrorIfUnset",
          word: wordStr ? word : null,
          checkEmpty: false
        },
        endIndex: wordEnd
      };
    }
    if (char === "+") {
      return {
        operation: { type: "UseAlternative", word, checkEmpty: false },
        endIndex: wordEnd
      };
    }
  }
  if (char === "#" || char === "%") {
    const greedy = nextChar === char;
    const side = char === "#" ? "prefix" : "suffix";
    i += greedy ? 2 : 1;
    const patternEnd = findParameterOperationEnd(p, value, i);
    const patternStr = value.slice(i, patternEnd);
    const patternParts = parseWordParts(p, patternStr, false, false, false);
    const pattern = AST.word(patternParts.length > 0 ? patternParts : [AST.literal("")]);
    return {
      operation: { type: "PatternRemoval", pattern, side, greedy },
      endIndex: patternEnd
    };
  }
  if (char === "/") {
    const all = nextChar === "/";
    i += all ? 2 : 1;
    let anchor = null;
    if (value[i] === "#") {
      anchor = "start";
      i++;
    } else if (value[i] === "%") {
      anchor = "end";
      i++;
    }
    let patternEnd;
    if (anchor !== null && (value[i] === "/" || value[i] === "}")) {
      patternEnd = i;
    } else {
      patternEnd = findPatternEnd(p, value, i);
    }
    const patternStr = value.slice(i, patternEnd);
    const patternParts = parseWordParts(p, patternStr, false, false, false);
    const pattern = AST.word(patternParts.length > 0 ? patternParts : [AST.literal("")]);
    let replacement = null;
    let endIdx = patternEnd;
    if (value[patternEnd] === "/") {
      const replaceStart = patternEnd + 1;
      const replaceEnd = findParameterOperationEnd(p, value, replaceStart);
      const replaceStr = value.slice(replaceStart, replaceEnd);
      const replaceParts = parseWordParts(p, replaceStr, false, false, false);
      replacement = AST.word(replaceParts.length > 0 ? replaceParts : [AST.literal("")]);
      endIdx = replaceEnd;
    }
    return {
      operation: {
        type: "PatternReplacement",
        pattern,
        replacement,
        all,
        anchor
      },
      endIndex: endIdx
    };
  }
  if (char === "^" || char === ",") {
    const all = nextChar === char;
    const direction = char === "^" ? "upper" : "lower";
    i += all ? 2 : 1;
    const patternEnd = findParameterOperationEnd(p, value, i);
    const patternStr = value.slice(i, patternEnd);
    const pattern = patternStr ? AST.word([AST.literal(patternStr)]) : null;
    return {
      operation: {
        type: "CaseModification",
        direction,
        all,
        pattern
      },
      endIndex: patternEnd
    };
  }
  if (char === "@" && /[QPaAEKkuUL]/.test(nextChar)) {
    const operator = nextChar;
    return {
      operation: {
        type: "Transform",
        operator
      },
      endIndex: i + 2
    };
  }
  return { operation: null, endIndex: i };
}
__name$1(parseParameterOperation, "parseParameterOperation");
function parseExpansion(p, value, start2, quoted = false) {
  const i = start2 + 1;
  if (i >= value.length) {
    return { part: AST.literal("$"), endIndex: i };
  }
  const char = value[i];
  if (char === "(" && value[i + 1] === "(") {
    if (p.isDollarDparenSubshell(value, start2)) {
      return p.parseCommandSubstitution(value, start2);
    }
    return p.parseArithmeticExpansion(value, start2);
  }
  if (char === "[") {
    let depth = 1;
    let j = i + 1;
    while (j < value.length && depth > 0) {
      if (value[j] === "[")
        depth++;
      else if (value[j] === "]")
        depth--;
      if (depth > 0)
        j++;
    }
    if (depth === 0) {
      const expr = value.slice(i + 1, j);
      const arithExpr = parseArithmeticExpression(p, expr);
      return { part: AST.arithmeticExpansion(arithExpr), endIndex: j + 1 };
    }
  }
  if (char === "(") {
    return p.parseCommandSubstitution(value, start2);
  }
  if (char === "{") {
    return parseParameterExpansion(p, value, start2, quoted);
  }
  if (/[a-zA-Z_0-9@*#?$!-]/.test(char)) {
    return parseSimpleParameter(p, value, start2);
  }
  return { part: AST.literal("$"), endIndex: i };
}
__name$1(parseExpansion, "parseExpansion");
function parseDoubleQuotedContent(p, value) {
  const parts = [];
  let i = 0;
  let literal2 = "";
  const flushLiteral = /* @__PURE__ */ __name$1(() => {
    if (literal2) {
      parts.push(AST.literal(literal2));
      literal2 = "";
    }
  }, "flushLiteral");
  while (i < value.length) {
    const char = value[i];
    if (char === "\\" && i + 1 < value.length) {
      const next = value[i + 1];
      if (next === "$" || next === "`" || next === '"' || next === "\\") {
        literal2 += next;
        i += 2;
        continue;
      }
      literal2 += char;
      i++;
      continue;
    }
    if (char === "$") {
      flushLiteral();
      const { part, endIndex } = parseExpansion(p, value, i, true);
      if (part) {
        parts.push(part);
      }
      i = endIndex;
      continue;
    }
    if (char === "`") {
      flushLiteral();
      const { part, endIndex } = p.parseBacktickSubstitution(value, i, true);
      parts.push(part);
      i = endIndex;
      continue;
    }
    literal2 += char;
    i++;
  }
  flushLiteral();
  return parts;
}
__name$1(parseDoubleQuotedContent, "parseDoubleQuotedContent");
function parseDoubleQuoted(p, value, start2) {
  const innerParts = [];
  let i = start2;
  let literal2 = "";
  const flushLiteral = /* @__PURE__ */ __name$1(() => {
    if (literal2) {
      innerParts.push(AST.literal(literal2));
      literal2 = "";
    }
  }, "flushLiteral");
  while (i < value.length && value[i] !== '"') {
    const char = value[i];
    if (char === "\\" && i + 1 < value.length) {
      const next = value[i + 1];
      if ('"\\$`\n'.includes(next)) {
        literal2 += next;
        i += 2;
        continue;
      }
      literal2 += char;
      i++;
      continue;
    }
    if (char === "$") {
      flushLiteral();
      const { part, endIndex } = parseExpansion(p, value, i, true);
      if (part) {
        innerParts.push(part);
      }
      i = endIndex;
      continue;
    }
    if (char === "`") {
      flushLiteral();
      const { part, endIndex } = p.parseBacktickSubstitution(value, i, true);
      innerParts.push(part);
      i = endIndex;
      continue;
    }
    literal2 += char;
    i++;
  }
  flushLiteral();
  return {
    part: AST.doubleQuoted(innerParts),
    endIndex: i
  };
}
__name$1(parseDoubleQuoted, "parseDoubleQuoted");
function parseWordParts(p, value, quoted = false, singleQuoted = false, isAssignment = false, hereDoc = false, singleQuotesAreLiteral = false, noBraceExpansion = false, regexPattern = false, inParameterExpansion = false) {
  if (singleQuoted) {
    return [AST.singleQuoted(value)];
  }
  if (quoted) {
    const innerParts = parseDoubleQuotedContent(p, value);
    return [AST.doubleQuoted(innerParts)];
  }
  if (value.length >= 2 && value[0] === '"' && value[value.length - 1] === '"') {
    const inner = value.slice(1, -1);
    let hasUnescapedQuote = false;
    for (let j = 0; j < inner.length; j++) {
      if (inner[j] === '"') {
        hasUnescapedQuote = true;
        break;
      }
      if (inner[j] === "\\" && j + 1 < inner.length) {
        j++;
      }
    }
    if (!hasUnescapedQuote) {
      const innerParts = parseDoubleQuotedContent(p, inner);
      return [AST.doubleQuoted(innerParts)];
    }
  }
  const parts = [];
  let i = 0;
  let literal2 = "";
  const flushLiteral = /* @__PURE__ */ __name$1(() => {
    if (literal2) {
      parts.push(AST.literal(literal2));
      literal2 = "";
    }
  }, "flushLiteral");
  while (i < value.length) {
    const char = value[i];
    if (char === "\\" && i + 1 < value.length) {
      const next = value[i + 1];
      if (regexPattern) {
        flushLiteral();
        parts.push(AST.escaped(next));
        i += 2;
        continue;
      }
      const isEscapable = hereDoc ? next === "$" || next === "`" || next === "\n" : next === "$" || next === "`" || next === '"' || next === "'" || next === "\n" || inParameterExpansion && next === "}";
      const isGlobMetaOrBackslash = singleQuotesAreLiteral ? "*?[]\\".includes(next) : "*?[]\\(){}.^+".includes(next);
      if (isEscapable) {
        literal2 += next;
      } else if (isGlobMetaOrBackslash) {
        flushLiteral();
        parts.push(AST.escaped(next));
      } else {
        literal2 += `\\${next}`;
      }
      i += 2;
      continue;
    }
    if (char === "'" && !singleQuotesAreLiteral && !hereDoc) {
      flushLiteral();
      const closeQuote = value.indexOf("'", i + 1);
      if (closeQuote === -1) {
        literal2 += value.slice(i);
        break;
      }
      parts.push(AST.singleQuoted(value.slice(i + 1, closeQuote)));
      i = closeQuote + 1;
      continue;
    }
    if (char === '"' && !hereDoc) {
      flushLiteral();
      const { part, endIndex } = parseDoubleQuoted(p, value, i + 1);
      parts.push(part);
      i = endIndex + 1;
      continue;
    }
    if (char === "$" && value[i + 1] === "'") {
      flushLiteral();
      const { part, endIndex } = parseAnsiCQuoted(p, value, i + 2);
      parts.push(part);
      i = endIndex;
      continue;
    }
    if (char === "$") {
      flushLiteral();
      const { part, endIndex } = parseExpansion(p, value, i);
      if (part) {
        parts.push(part);
      }
      i = endIndex;
      continue;
    }
    if (char === "`") {
      flushLiteral();
      const { part, endIndex } = p.parseBacktickSubstitution(value, i);
      parts.push(part);
      i = endIndex;
      continue;
    }
    if (char === "~") {
      const prevChar = i > 0 ? value[i - 1] : "";
      const canExpandAfterColon = isAssignment && prevChar === ":";
      if (i === 0 || prevChar === "=" || canExpandAfterColon) {
        const tildeEnd = findTildeEnd(p, value, i);
        const afterTilde = value[tildeEnd];
        if (afterTilde === void 0 || afterTilde === "/" || afterTilde === ":") {
          flushLiteral();
          const user = value.slice(i + 1, tildeEnd) || null;
          parts.push({ type: "TildeExpansion", user });
          i = tildeEnd;
          continue;
        }
      }
    }
    if ("@*+?!".includes(char) && i + 1 < value.length && value[i + 1] === "(") {
      const closeIdx = findExtglobClose(value, i + 1);
      if (closeIdx !== -1) {
        flushLiteral();
        const pattern = value.slice(i, closeIdx + 1);
        parts.push({ type: "Glob", pattern });
        i = closeIdx + 1;
        continue;
      }
    }
    if (char === "*" || char === "?" || char === "[") {
      flushLiteral();
      const { pattern, endIndex } = parseGlobPattern(p, value, i);
      parts.push({ type: "Glob", pattern });
      i = endIndex;
      continue;
    }
    if (char === "{" && !isAssignment && !noBraceExpansion) {
      const braceResult = tryParseBraceExpansion(p, value, i, parseWordParts);
      if (braceResult) {
        flushLiteral();
        parts.push(braceResult.part);
        i = braceResult.endIndex;
        continue;
      }
    }
    literal2 += char;
    i++;
  }
  flushLiteral();
  return parts;
}
__name$1(parseWordParts, "parseWordParts");
function isDollarDparenSubshell(value, start2) {
  const len = value.length;
  let pos = start2 + 3;
  let depth = 2;
  let inSingleQuote = false;
  let inDoubleQuote = false;
  while (pos < len && depth > 0) {
    const c = value[pos];
    if (inSingleQuote) {
      if (c === "'") {
        inSingleQuote = false;
      }
      pos++;
      continue;
    }
    if (inDoubleQuote) {
      if (c === "\\") {
        pos += 2;
        continue;
      }
      if (c === '"') {
        inDoubleQuote = false;
      }
      pos++;
      continue;
    }
    if (c === "'") {
      inSingleQuote = true;
      pos++;
      continue;
    }
    if (c === '"') {
      inDoubleQuote = true;
      pos++;
      continue;
    }
    if (c === "\\") {
      pos += 2;
      continue;
    }
    if (c === "(") {
      depth++;
      pos++;
      continue;
    }
    if (c === ")") {
      depth--;
      if (depth === 1) {
        const nextPos = pos + 1;
        if (nextPos < len && value[nextPos] === ")") {
          return false;
        }
        return true;
      }
      if (depth === 0) {
        return false;
      }
      pos++;
      continue;
    }
    if (depth === 1) {
      if (c === "|" && pos + 1 < len && value[pos + 1] === "|") {
        return true;
      }
      if (c === "&" && pos + 1 < len && value[pos + 1] === "&") {
        return true;
      }
      if (c === "|" && pos + 1 < len && value[pos + 1] !== "|") {
        return true;
      }
    }
    pos++;
  }
  return false;
}
__name$1(isDollarDparenSubshell, "isDollarDparenSubshell");
function parseCommandSubstitutionFromString(value, start2, createParser, error) {
  const cmdStart = start2 + 2;
  let depth = 1;
  let i = cmdStart;
  let inSingleQuote = false;
  let inDoubleQuote = false;
  let caseDepth = 0;
  let inCasePattern = false;
  let wordBuffer = "";
  while (i < value.length && depth > 0) {
    const c = value[i];
    if (inSingleQuote) {
      if (c === "'")
        inSingleQuote = false;
    } else if (inDoubleQuote) {
      if (c === "\\" && i + 1 < value.length) {
        i++;
      } else if (c === '"') {
        inDoubleQuote = false;
      }
    } else {
      if (c === "'") {
        inSingleQuote = true;
        wordBuffer = "";
      } else if (c === '"') {
        inDoubleQuote = true;
        wordBuffer = "";
      } else if (c === "\\" && i + 1 < value.length) {
        i++;
        wordBuffer = "";
      } else if (/[a-zA-Z_]/.test(c)) {
        wordBuffer += c;
      } else {
        if (wordBuffer === "case") {
          caseDepth++;
          inCasePattern = false;
        } else if (wordBuffer === "in" && caseDepth > 0) {
          inCasePattern = true;
        } else if (wordBuffer === "esac" && caseDepth > 0) {
          caseDepth--;
          inCasePattern = false;
        }
        wordBuffer = "";
        if (c === "(") {
          if (i > 0 && value[i - 1] === "$") {
            depth++;
          } else if (!inCasePattern) {
            depth++;
          }
        } else if (c === ")") {
          if (inCasePattern) {
            inCasePattern = false;
          } else {
            depth--;
          }
        } else if (c === ";") {
          if (caseDepth > 0 && i + 1 < value.length && value[i + 1] === ";") {
            inCasePattern = true;
          }
        }
      }
    }
    if (depth > 0)
      i++;
  }
  if (depth > 0) {
    error("unexpected EOF while looking for matching `)'");
  }
  const cmdStr = value.slice(cmdStart, i);
  const nestedParser = createParser();
  const body = nestedParser.parse(cmdStr);
  return {
    part: AST.commandSubstitution(body, false),
    endIndex: i + 1
  };
}
__name$1(parseCommandSubstitutionFromString, "parseCommandSubstitutionFromString");
function parseBacktickSubstitutionFromString(value, start2, inDoubleQuotes, createParser, error) {
  const cmdStart = start2 + 1;
  let i = cmdStart;
  let cmdStr = "";
  while (i < value.length && value[i] !== "`") {
    if (value[i] === "\\") {
      const next = value[i + 1];
      const isSpecial = next === "$" || next === "`" || next === "\\" || next === "\n" || inDoubleQuotes && next === '"';
      if (isSpecial) {
        if (next !== "\n") {
          cmdStr += next;
        }
        i += 2;
      } else {
        cmdStr += value[i];
        i++;
      }
    } else {
      cmdStr += value[i];
      i++;
    }
  }
  if (i >= value.length) {
    error("unexpected EOF while looking for matching ``'");
  }
  const nestedParser = createParser();
  const body = nestedParser.parse(cmdStr);
  return {
    part: AST.commandSubstitution(body, true),
    endIndex: i + 1
  };
}
__name$1(parseBacktickSubstitutionFromString, "parseBacktickSubstitutionFromString");
var Parser = (_r = class {
  tokens = [];
  pos = 0;
  pendingHeredocs = [];
  parseIterations = 0;
  _input = "";
  /**
   * Get the raw input string being parsed.
   * Used by conditional-parser for extracting exact whitespace in regex patterns.
   */
  getInput() {
    return this._input;
  }
  /**
   * Check parse iteration limit to prevent infinite loops
   */
  checkIterationLimit() {
    this.parseIterations++;
    if (this.parseIterations > MAX_PARSE_ITERATIONS) {
      throw new ParseException("Maximum parse iterations exceeded (possible infinite loop)", this.current().line, this.current().column);
    }
  }
  /**
   * Parse a bash script string
   */
  parse(input) {
    if (input.length > MAX_INPUT_SIZE) {
      throw new ParseException(`Input too large: ${input.length} bytes exceeds limit of ${MAX_INPUT_SIZE}`, 1, 1);
    }
    this._input = input;
    const lexer = new Lexer(input);
    this.tokens = lexer.tokenize();
    if (this.tokens.length > MAX_TOKENS) {
      throw new ParseException(`Too many tokens: ${this.tokens.length} exceeds limit of ${MAX_TOKENS}`, 1, 1);
    }
    this.pos = 0;
    this.pendingHeredocs = [];
    this.parseIterations = 0;
    return this.parseScript();
  }
  /**
   * Parse from pre-tokenized input
   */
  parseTokens(tokens) {
    this.tokens = tokens;
    this.pos = 0;
    this.pendingHeredocs = [];
    return this.parseScript();
  }
  // ===========================================================================
  // HELPER METHODS
  // ===========================================================================
  current() {
    return this.tokens[this.pos] || this.tokens[this.tokens.length - 1];
  }
  peek(offset = 0) {
    return this.tokens[this.pos + offset] || this.tokens[this.tokens.length - 1];
  }
  advance() {
    const token = this.current();
    if (this.pos < this.tokens.length - 1) {
      this.pos++;
    }
    return token;
  }
  getPos() {
    return this.pos;
  }
  /**
   * Check if current token matches any of the given types.
   * Optimized to avoid array allocation for common cases (1-4 args).
   */
  check(t1, t2, t3, t4, ...rest) {
    const type = this.tokens[this.pos]?.type;
    if (type === t1)
      return true;
    if (t2 !== void 0 && type === t2)
      return true;
    if (t3 !== void 0 && type === t3)
      return true;
    if (t4 !== void 0 && type === t4)
      return true;
    if (rest.length > 0)
      return rest.includes(type);
    return false;
  }
  expect(type, message) {
    if (this.check(type)) {
      return this.advance();
    }
    const token = this.current();
    throw new ParseException(message || `Expected ${type}, got ${token.type}`, token.line, token.column, token);
  }
  error(message) {
    const token = this.current();
    throw new ParseException(message, token.line, token.column, token);
  }
  skipNewlines() {
    while (this.check(TokenType.NEWLINE, TokenType.COMMENT)) {
      if (this.check(TokenType.NEWLINE)) {
        this.advance();
        this.processHeredocs();
      } else {
        this.advance();
      }
    }
  }
  skipSeparators(includeCaseTerminators = true) {
    while (true) {
      if (this.check(TokenType.NEWLINE)) {
        this.advance();
        this.processHeredocs();
        continue;
      }
      if (this.check(TokenType.SEMICOLON, TokenType.COMMENT)) {
        this.advance();
        continue;
      }
      if (includeCaseTerminators && this.check(TokenType.DSEMI, TokenType.SEMI_AND, TokenType.SEMI_SEMI_AND)) {
        this.advance();
        continue;
      }
      break;
    }
  }
  addPendingHeredoc(redirect, delimiter, stripTabs, quoted) {
    this.pendingHeredocs.push({ redirect, delimiter, stripTabs, quoted });
  }
  processHeredocs() {
    for (const heredoc of this.pendingHeredocs) {
      if (this.check(TokenType.HEREDOC_CONTENT)) {
        const content = this.advance();
        let contentWord;
        if (heredoc.quoted) {
          contentWord = AST.word([AST.literal(content.value)]);
        } else {
          contentWord = this.parseWordFromString(content.value, false, false, false, true);
        }
        heredoc.redirect.target = AST.hereDoc(heredoc.delimiter, contentWord, heredoc.stripTabs, heredoc.quoted);
      }
    }
    this.pendingHeredocs = [];
  }
  isStatementEnd() {
    return this.check(TokenType.EOF, TokenType.NEWLINE, TokenType.SEMICOLON, TokenType.AMP, TokenType.AND_AND, TokenType.OR_OR, TokenType.RPAREN, TokenType.RBRACE, TokenType.DSEMI, TokenType.SEMI_AND, TokenType.SEMI_SEMI_AND);
  }
  isCommandStart() {
    const t = this.current().type;
    return t === TokenType.WORD || t === TokenType.NAME || t === TokenType.NUMBER || t === TokenType.ASSIGNMENT_WORD || t === TokenType.IF || t === TokenType.FOR || t === TokenType.WHILE || t === TokenType.UNTIL || t === TokenType.CASE || t === TokenType.LPAREN || t === TokenType.LBRACE || t === TokenType.DPAREN_START || t === TokenType.DBRACK_START || t === TokenType.FUNCTION || t === TokenType.BANG || // 'time' is a pipeline prefix that can start a command
    t === TokenType.TIME || // 'in' can appear as a command name (e.g., 'in' is not reserved outside for/case)
    t === TokenType.IN || // Redirections can appear before command name (e.g., <<EOF tac)
    // POSIX allows simple_command to start with io_redirect
    t === TokenType.LESS || t === TokenType.GREAT || t === TokenType.DLESS || t === TokenType.DGREAT || t === TokenType.LESSAND || t === TokenType.GREATAND || t === TokenType.LESSGREAT || t === TokenType.DLESSDASH || t === TokenType.CLOBBER || t === TokenType.TLESS || t === TokenType.AND_GREAT || t === TokenType.AND_DGREAT;
  }
  // ===========================================================================
  // SCRIPT PARSING
  // ===========================================================================
  parseScript() {
    const statements = [];
    const maxIterations = 1e4;
    let iterations = 0;
    this.skipNewlines();
    while (!this.check(TokenType.EOF)) {
      iterations++;
      if (iterations > maxIterations) {
        this.error(`Parser stuck: too many iterations (>${maxIterations})`);
      }
      const deferredErrorStmt = this.checkUnexpectedToken();
      if (deferredErrorStmt) {
        statements.push(deferredErrorStmt);
        this.skipSeparators(false);
        continue;
      }
      const posBefore = this.pos;
      const stmt = this.parseStatement();
      if (stmt) {
        statements.push(stmt);
      }
      this.skipSeparators(false);
      if (this.check(TokenType.DSEMI, TokenType.SEMI_AND, TokenType.SEMI_SEMI_AND)) {
        this.error(`syntax error near unexpected token \`${this.current().value}'`);
      }
      if (this.pos === posBefore && !this.check(TokenType.EOF)) {
        this.advance();
      }
    }
    return AST.script(statements);
  }
  /**
   * Check for unexpected tokens that can't appear at statement start.
   * Returns a deferred error statement for tokens that should cause errors
   * at execution time rather than parse time (to match bash's incremental behavior).
   */
  checkUnexpectedToken() {
    const t = this.current().type;
    const v = this.current().value;
    if (t === TokenType.DO || t === TokenType.DONE || t === TokenType.THEN || t === TokenType.ELSE || t === TokenType.ELIF || t === TokenType.FI || t === TokenType.ESAC) {
      this.error(`syntax error near unexpected token \`${v}'`);
    }
    if (t === TokenType.RBRACE || t === TokenType.RPAREN) {
      const errorMsg = `syntax error near unexpected token \`${v}'`;
      this.advance();
      return AST.statement([AST.pipeline([AST.simpleCommand(null, [], [], [])])], [], false, { message: errorMsg, token: v });
    }
    if (t === TokenType.DSEMI || t === TokenType.SEMI_AND || t === TokenType.SEMI_SEMI_AND) {
      this.error(`syntax error near unexpected token \`${v}'`);
    }
    if (t === TokenType.SEMICOLON) {
      this.error(`syntax error near unexpected token \`${v}'`);
    }
    if (t === TokenType.PIPE || t === TokenType.PIPE_AMP) {
      this.error(`syntax error near unexpected token \`${v}'`);
    }
    return null;
  }
  // ===========================================================================
  // STATEMENT PARSING
  // ===========================================================================
  parseStatement() {
    this.skipNewlines();
    if (!this.isCommandStart()) {
      return null;
    }
    const startOffset = this.current().start;
    const pipelines = [];
    const operators = [];
    let background = false;
    const firstPipeline = this.parsePipeline();
    pipelines.push(firstPipeline);
    while (this.check(TokenType.AND_AND, TokenType.OR_OR)) {
      const op = this.advance();
      operators.push(op.type === TokenType.AND_AND ? "&&" : "||");
      this.skipNewlines();
      const nextPipeline = this.parsePipeline();
      pipelines.push(nextPipeline);
    }
    if (this.check(TokenType.AMP)) {
      this.advance();
      background = true;
    }
    const endOffset = this.pos > 0 ? this.tokens[this.pos - 1].end : startOffset;
    const sourceText = this._input.slice(startOffset, endOffset);
    return AST.statement(pipelines, operators, background, void 0, sourceText);
  }
  // ===========================================================================
  // PIPELINE PARSING
  // ===========================================================================
  parsePipeline() {
    let timed = false;
    let timePosix = false;
    if (this.check(TokenType.TIME)) {
      this.advance();
      timed = true;
      if (this.check(TokenType.WORD, TokenType.NAME) && this.current().value === "-p") {
        this.advance();
        timePosix = true;
      }
    }
    let negationCount = 0;
    while (this.check(TokenType.BANG)) {
      this.advance();
      negationCount++;
    }
    const negated = negationCount % 2 === 1;
    const commands = [];
    const pipeStderr = [];
    const firstCmd = this.parseCommand();
    commands.push(firstCmd);
    while (this.check(TokenType.PIPE, TokenType.PIPE_AMP)) {
      const pipeToken = this.advance();
      this.skipNewlines();
      pipeStderr.push(pipeToken.type === TokenType.PIPE_AMP);
      const nextCmd = this.parseCommand();
      commands.push(nextCmd);
    }
    return AST.pipeline(commands, negated, timed, timePosix, pipeStderr.length > 0 ? pipeStderr : void 0);
  }
  // ===========================================================================
  // COMMAND PARSING
  // ===========================================================================
  parseCommand() {
    if (this.check(TokenType.IF)) {
      return parseIf(this);
    }
    if (this.check(TokenType.FOR)) {
      return parseFor(this);
    }
    if (this.check(TokenType.WHILE)) {
      return parseWhile(this);
    }
    if (this.check(TokenType.UNTIL)) {
      return parseUntil(this);
    }
    if (this.check(TokenType.CASE)) {
      return parseCase(this);
    }
    if (this.check(TokenType.LPAREN)) {
      return parseSubshell(this);
    }
    if (this.check(TokenType.LBRACE)) {
      return parseGroup(this);
    }
    if (this.check(TokenType.DPAREN_START)) {
      if (this.dparenClosesWithSpacedParens()) {
        return this.parseNestedSubshellsFromDparen();
      }
      return this.parseArithmeticCommand();
    }
    if (this.check(TokenType.DBRACK_START)) {
      return this.parseConditionalCommand();
    }
    if (this.check(TokenType.FUNCTION)) {
      return this.parseFunctionDef();
    }
    if (this.check(TokenType.NAME, TokenType.WORD) && this.peek(1).type === TokenType.LPAREN && this.peek(2).type === TokenType.RPAREN) {
      return this.parseFunctionDef();
    }
    return parseSimpleCommand(this);
  }
  /**
   * Scan ahead from current DPAREN_START to determine if it closes with ) )
   * (two separate RPAREN tokens) or )) (DPAREN_END token).
   * Returns true if it closes with ) ) (nested subshells case).
   */
  dparenClosesWithSpacedParens() {
    let depth = 1;
    let offset = 1;
    while (offset < this.tokens.length - this.pos) {
      const tok = this.peek(offset);
      if (tok.type === TokenType.EOF) {
        return false;
      }
      if (tok.type === TokenType.DPAREN_START || tok.type === TokenType.LPAREN) {
        depth++;
      } else if (tok.type === TokenType.DPAREN_END) {
        depth -= 2;
        if (depth <= 0) {
          return false;
        }
      } else if (tok.type === TokenType.RPAREN) {
        depth--;
        if (depth === 0) {
          const nextTok = this.peek(offset + 1);
          if (nextTok.type === TokenType.RPAREN) {
            return true;
          }
        }
      }
      offset++;
    }
    return false;
  }
  /**
   * Parse (( ... ) ) as nested subshells when we know it closes with ) ).
   * We've already determined via dparenClosesWithSpacedParens() that this
   * DPAREN_START should be treated as two LPAREN tokens.
   */
  parseNestedSubshellsFromDparen() {
    this.advance();
    const innerBody = this.parseCompoundList();
    this.expect(TokenType.RPAREN);
    this.expect(TokenType.RPAREN);
    const redirections = this.parseOptionalRedirections();
    const innerSubshell = AST.subshell(innerBody, []);
    return AST.subshell([AST.statement([AST.pipeline([innerSubshell], false, false, false)])], redirections);
  }
  // ===========================================================================
  // WORD PARSING
  // ===========================================================================
  isWord() {
    const t = this.current().type;
    return t === TokenType.WORD || t === TokenType.NAME || t === TokenType.NUMBER || // Reserved words can be used as words in certain contexts (e.g., "echo if")
    t === TokenType.IF || t === TokenType.FOR || t === TokenType.WHILE || t === TokenType.UNTIL || t === TokenType.CASE || t === TokenType.FUNCTION || t === TokenType.ELSE || t === TokenType.ELIF || t === TokenType.FI || t === TokenType.THEN || t === TokenType.DO || t === TokenType.DONE || t === TokenType.ESAC || t === TokenType.IN || t === TokenType.SELECT || t === TokenType.TIME || t === TokenType.COPROC || // Operators that can appear as words in command arguments (e.g., "[ ! -z foo ]")
    t === TokenType.BANG;
  }
  parseWord() {
    const token = this.advance();
    return this.parseWordFromString(token.value, token.quoted, token.singleQuoted);
  }
  /**
   * Parse a word without brace expansion (for [[ ]] conditionals).
   * In bash, brace expansion does not occur inside [[ ]].
   */
  parseWordNoBraceExpansion() {
    const token = this.advance();
    return this.parseWordFromString(
      token.value,
      token.quoted,
      token.singleQuoted,
      false,
      // isAssignment
      false,
      // hereDoc
      true
    );
  }
  /**
   * Parse a word for regex patterns (in [[ =~ ]]).
   * All escaped characters create Escaped nodes so the backslash is preserved
   * for the regex engine. For example, \$ creates Escaped("$") which becomes \$
   * in the final regex pattern.
   */
  parseWordForRegex() {
    const token = this.advance();
    return this.parseWordFromString(
      token.value,
      token.quoted,
      token.singleQuoted,
      false,
      // isAssignment
      false,
      // hereDoc
      true,
      // noBraceExpansion
      true
    );
  }
  parseWordFromString(value, quoted = false, singleQuoted = false, isAssignment = false, hereDoc = false, noBraceExpansion = false, regexPattern = false) {
    const parts = parseWordParts(
      this,
      value,
      quoted,
      singleQuoted,
      isAssignment,
      hereDoc,
      false,
      // singleQuotesAreLiteral
      noBraceExpansion,
      regexPattern
    );
    return AST.word(parts);
  }
  parseCommandSubstitution(value, start2) {
    return parseCommandSubstitutionFromString(value, start2, () => new _r(), (msg) => this.error(msg));
  }
  parseBacktickSubstitution(value, start2, inDoubleQuotes = false) {
    return parseBacktickSubstitutionFromString(value, start2, inDoubleQuotes, () => new _r(), (msg) => this.error(msg));
  }
  /**
   * Check if $(( at position `start` in `value` is a command substitution with nested
   * subshell rather than arithmetic expansion.
   */
  isDollarDparenSubshell(value, start2) {
    return isDollarDparenSubshell(value, start2);
  }
  parseArithmeticExpansion(value, start2) {
    const exprStart = start2 + 3;
    let arithDepth = 1;
    let parenDepth = 0;
    let i = exprStart;
    while (i < value.length - 1 && arithDepth > 0) {
      if (value[i] === "$" && value[i + 1] === "(") {
        if (value[i + 2] === "(") {
          arithDepth++;
          i += 3;
        } else {
          parenDepth++;
          i += 2;
        }
      } else if (value[i] === "(" && value[i + 1] === "(") {
        arithDepth++;
        i += 2;
      } else if (value[i] === ")" && value[i + 1] === ")") {
        if (parenDepth > 0) {
          parenDepth--;
          i++;
        } else {
          arithDepth--;
          if (arithDepth > 0)
            i += 2;
        }
      } else if (value[i] === "(") {
        parenDepth++;
        i++;
      } else if (value[i] === ")") {
        if (parenDepth > 0) {
          parenDepth--;
        }
        i++;
      } else {
        i++;
      }
    }
    const exprStr = value.slice(exprStart, i);
    const expression = this.parseArithmeticExpression(exprStr);
    return {
      part: AST.arithmeticExpansion(expression),
      endIndex: i + 2
    };
  }
  parseArithmeticCommand() {
    const startToken = this.expect(TokenType.DPAREN_START);
    let exprStr = "";
    let dparenDepth = 1;
    let parenDepth = 0;
    let pendingRparen = false;
    let foundClosing = false;
    while (dparenDepth > 0 && !this.check(TokenType.EOF)) {
      if (pendingRparen) {
        pendingRparen = false;
        if (parenDepth > 0) {
          parenDepth--;
          exprStr += ")";
          continue;
        }
        if (this.check(TokenType.RPAREN)) {
          dparenDepth--;
          foundClosing = true;
          this.advance();
          continue;
        }
        if (this.check(TokenType.DPAREN_END)) {
          dparenDepth--;
          foundClosing = true;
          continue;
        }
        exprStr += ")";
        continue;
      }
      if (this.check(TokenType.DPAREN_START)) {
        dparenDepth++;
        exprStr += "((";
        this.advance();
      } else if (this.check(TokenType.DPAREN_END)) {
        if (parenDepth >= 2) {
          parenDepth -= 2;
          exprStr += "))";
          this.advance();
        } else if (parenDepth === 1) {
          parenDepth--;
          exprStr += ")";
          pendingRparen = true;
          this.advance();
        } else {
          dparenDepth--;
          foundClosing = true;
          if (dparenDepth > 0) {
            exprStr += "))";
          }
          this.advance();
        }
      } else if (this.check(TokenType.LPAREN)) {
        parenDepth++;
        exprStr += "(";
        this.advance();
      } else if (this.check(TokenType.RPAREN)) {
        if (parenDepth > 0) {
          parenDepth--;
        }
        exprStr += ")";
        this.advance();
      } else {
        const value = this.current().value;
        const lastChar = exprStr.length > 0 ? exprStr[exprStr.length - 1] : "";
        const needsSpace = exprStr.length > 0 && !exprStr.endsWith(" ") && // Don't add space before = after operators that can form compound assignments
        !(value === "=" && /[|&^+\-*/%<>]$/.test(exprStr)) && // Don't add space before second < or > (for << or >>)
        !(value === "<" && lastChar === "<") && !(value === ">" && lastChar === ">");
        if (needsSpace) {
          exprStr += " ";
        }
        exprStr += value;
        this.advance();
      }
    }
    if (!foundClosing) {
      this.expect(TokenType.DPAREN_END);
    }
    const expression = this.parseArithmeticExpression(exprStr.trim());
    const redirections = this.parseOptionalRedirections();
    return AST.arithmeticCommand(expression, redirections, startToken.line);
  }
  parseConditionalCommand() {
    const startToken = this.expect(TokenType.DBRACK_START);
    const expression = parseConditionalExpression(this);
    this.expect(TokenType.DBRACK_END);
    const redirections = this.parseOptionalRedirections();
    return AST.conditionalCommand(expression, redirections, startToken.line);
  }
  parseFunctionDef() {
    let name;
    if (this.check(TokenType.FUNCTION)) {
      this.advance();
      if (this.check(TokenType.NAME) || this.check(TokenType.WORD)) {
        name = this.advance().value;
      } else {
        const token = this.current();
        throw new ParseException("Expected function name", token.line, token.column, token);
      }
      if (this.check(TokenType.LPAREN)) {
        this.advance();
        this.expect(TokenType.RPAREN);
      }
    } else {
      name = this.advance().value;
      if (name.includes("$")) {
        this.error(`\`${name}': not a valid identifier`);
      }
      this.expect(TokenType.LPAREN);
      this.expect(TokenType.RPAREN);
    }
    this.skipNewlines();
    const body = this.parseCompoundCommandBody({ forFunctionBody: true });
    const redirections = this.parseOptionalRedirections();
    return AST.functionDef(name, body, redirections);
  }
  parseCompoundCommandBody(options) {
    const skipRedirections = options?.forFunctionBody;
    if (this.check(TokenType.LBRACE)) {
      return parseGroup(this, { skipRedirections });
    }
    if (this.check(TokenType.LPAREN)) {
      return parseSubshell(this, { skipRedirections });
    }
    if (this.check(TokenType.IF)) {
      return parseIf(this, { skipRedirections });
    }
    if (this.check(TokenType.FOR)) {
      return parseFor(this, { skipRedirections });
    }
    if (this.check(TokenType.WHILE)) {
      return parseWhile(this, { skipRedirections });
    }
    if (this.check(TokenType.UNTIL)) {
      return parseUntil(this, { skipRedirections });
    }
    if (this.check(TokenType.CASE)) {
      return parseCase(this, { skipRedirections });
    }
    this.error("Expected compound command for function body");
  }
  // ===========================================================================
  // HELPER PARSING
  // ===========================================================================
  parseCompoundList() {
    const statements = [];
    this.skipNewlines();
    while (!this.check(TokenType.EOF, TokenType.FI, TokenType.ELSE, TokenType.ELIF, TokenType.THEN, TokenType.DO, TokenType.DONE, TokenType.ESAC, TokenType.RPAREN, TokenType.RBRACE, TokenType.DSEMI, TokenType.SEMI_AND, TokenType.SEMI_SEMI_AND) && this.isCommandStart()) {
      this.checkIterationLimit();
      const posBefore = this.pos;
      const stmt = this.parseStatement();
      if (stmt) {
        statements.push(stmt);
      }
      this.skipSeparators();
      if (this.pos === posBefore && !stmt) {
        break;
      }
    }
    return statements;
  }
  parseOptionalRedirections() {
    const redirections = [];
    while (isRedirection(this)) {
      this.checkIterationLimit();
      const posBefore = this.pos;
      redirections.push(parseRedirection(this));
      if (this.pos === posBefore) {
        break;
      }
    }
    return redirections;
  }
  // ===========================================================================
  // ARITHMETIC EXPRESSION PARSING
  // ===========================================================================
  parseArithmeticExpression(input) {
    return parseArithmeticExpression(this, input);
  }
}, __name$1(_r, "Parser"), _r);
function parse(input) {
  const parser = new Parser();
  return parser.parse(input);
}
__name$1(parse, "parse");
var POSIX_CLASSES = {
  alnum: "a-zA-Z0-9",
  alpha: "a-zA-Z",
  ascii: "\\x00-\\x7F",
  blank: " \\t",
  cntrl: "\\x00-\\x1F\\x7F",
  digit: "0-9",
  graph: "!-~",
  lower: "a-z",
  print: " -~",
  punct: "!-/:-@\\[-`{-~",
  space: " \\t\\n\\r\\f\\v",
  upper: "A-Z",
  word: "a-zA-Z0-9_",
  xdigit: "0-9a-fA-F"
};
function posixClassToRegex(className) {
  return POSIX_CLASSES[className] || "";
}
__name$1(posixClassToRegex, "posixClassToRegex");
function splitGlobignorePatterns(globignore) {
  const patterns = [];
  let current = "";
  let i = 0;
  while (i < globignore.length) {
    const c = globignore[i];
    if (c === "[") {
      current += c;
      i++;
      if (i < globignore.length && (globignore[i] === "!" || globignore[i] === "^")) {
        current += globignore[i];
        i++;
      }
      if (i < globignore.length && globignore[i] === "]") {
        current += globignore[i];
        i++;
      }
      while (i < globignore.length && globignore[i] !== "]") {
        if (globignore[i] === "[" && i + 1 < globignore.length && globignore[i + 1] === ":") {
          const posixEnd = globignore.indexOf(":]", i + 2);
          if (posixEnd !== -1) {
            current += globignore.slice(i, posixEnd + 2);
            i = posixEnd + 2;
            continue;
          }
        }
        if (globignore[i] === "\\" && i + 1 < globignore.length) {
          current += globignore[i] + globignore[i + 1];
          i += 2;
          continue;
        }
        current += globignore[i];
        i++;
      }
      if (i < globignore.length && globignore[i] === "]") {
        current += globignore[i];
        i++;
      }
    } else if (c === ":") {
      if (current !== "") {
        patterns.push(current);
      }
      current = "";
      i++;
    } else if (c === "\\" && i + 1 < globignore.length) {
      current += c + globignore[i + 1];
      i += 2;
    } else {
      current += c;
      i++;
    }
  }
  if (current !== "") {
    patterns.push(current);
  }
  return patterns;
}
__name$1(splitGlobignorePatterns, "splitGlobignorePatterns");
function globignorePatternToRegex(pattern) {
  let regex = "^";
  for (let i = 0; i < pattern.length; i++) {
    const c = pattern[i];
    if (c === "*") {
      regex += "[^/]*";
    } else if (c === "?") {
      regex += "[^/]";
    } else if (c === "[") {
      let j = i + 1;
      let classContent = "[";
      if (j < pattern.length && (pattern[j] === "^" || pattern[j] === "!")) {
        classContent += "^";
        j++;
      }
      if (j < pattern.length && pattern[j] === "]") {
        classContent += "\\]";
        j++;
      }
      let classEnd = j;
      while (classEnd < pattern.length) {
        if (pattern[classEnd] === "\\" && classEnd + 1 < pattern.length) {
          classEnd += 2;
          continue;
        }
        if (pattern[classEnd] === "[" && classEnd + 1 < pattern.length && pattern[classEnd + 1] === ":") {
          const posixEnd = pattern.indexOf(":]", classEnd + 2);
          if (posixEnd !== -1) {
            classEnd = posixEnd + 2;
            continue;
          }
        }
        if (pattern[classEnd] === "]") {
          break;
        }
        classEnd++;
      }
      const classStartPos = j;
      while (j < pattern.length && pattern[j] !== "]") {
        if (pattern[j] === "[" && j + 1 < pattern.length && pattern[j + 1] === ":") {
          const posixEnd = pattern.indexOf(":]", j + 2);
          if (posixEnd !== -1) {
            const posixClass = pattern.slice(j + 2, posixEnd);
            const regexClass = posixClassToRegex(posixClass);
            classContent += regexClass;
            j = posixEnd + 2;
            continue;
          }
        }
        if (pattern[j] === "\\" && j + 1 < pattern.length) {
          classContent += `\\${pattern[j + 1]}`;
          j += 2;
          continue;
        }
        if (pattern[j] === "-") {
          const atStart = j === classStartPos;
          const atEnd = j + 1 === classEnd;
          if (atStart || atEnd) {
            classContent += "\\-";
          } else {
            classContent += "-";
          }
        } else {
          classContent += pattern[j];
        }
        j++;
      }
      classContent += "]";
      regex += classContent;
      i = j;
    } else if (c === "\\" && i + 1 < pattern.length) {
      const nextChar = pattern[i + 1];
      if (/[.+^${}()|\\*?[\]]/.test(nextChar)) {
        regex += `\\${nextChar}`;
      } else {
        regex += nextChar;
      }
      i++;
    } else if (/[.+^${}()|]/.test(c)) {
      regex += `\\${c}`;
    } else {
      regex += c;
    }
  }
  regex += "$";
  return new RegExp(regex);
}
__name$1(globignorePatternToRegex, "globignorePatternToRegex");
function findMatchingParen$1(pattern, openIdx) {
  let depth = 1;
  let i = openIdx + 1;
  while (i < pattern.length && depth > 0) {
    const c = pattern[i];
    if (c === "\\") {
      i += 2;
      continue;
    }
    if (c === "(") {
      depth++;
    } else if (c === ")") {
      depth--;
      if (depth === 0) {
        return i;
      }
    }
    i++;
  }
  return -1;
}
__name$1(findMatchingParen$1, "findMatchingParen");
function splitExtglobAlternatives$1(content) {
  const alternatives = [];
  let current = "";
  let depth = 0;
  let inSingleQuote = false;
  let i = 0;
  while (i < content.length) {
    const c = content[i];
    if (c === "'" && !inSingleQuote) {
      inSingleQuote = true;
      current += "\0QUOTE_START\0";
      i++;
      continue;
    }
    if (c === "'" && inSingleQuote) {
      inSingleQuote = false;
      current += "\0QUOTE_END\0";
      i++;
      continue;
    }
    if (inSingleQuote) {
      current += c;
      i++;
      continue;
    }
    if (c === "\\") {
      current += c;
      if (i + 1 < content.length) {
        current += content[i + 1];
        i += 2;
      } else {
        i++;
      }
      continue;
    }
    if (c === "(") {
      depth++;
      current += c;
    } else if (c === ")") {
      depth--;
      current += c;
    } else if (c === "|" && depth === 0) {
      alternatives.push(current);
      current = "";
    } else {
      current += c;
    }
    i++;
  }
  alternatives.push(current);
  return alternatives;
}
__name$1(splitExtglobAlternatives$1, "splitExtglobAlternatives");
var GlobExpander = (_s = class {
  fs;
  cwd;
  globignorePatterns = [];
  hasGlobignore = false;
  globstar = false;
  nullglob = false;
  failglob = false;
  dotglob = false;
  extglob = false;
  globskipdots = true;
  // Default to true in bash >=5.2
  constructor(fs, cwd, env, options) {
    this.fs = fs;
    this.cwd = cwd;
    if (typeof options === "boolean") {
      this.globstar = options;
    } else if (options) {
      this.globstar = options.globstar ?? false;
      this.nullglob = options.nullglob ?? false;
      this.failglob = options.failglob ?? false;
      this.dotglob = options.dotglob ?? false;
      this.extglob = options.extglob ?? false;
      this.globskipdots = options.globskipdots ?? true;
    }
    const globignore = env?.GLOBIGNORE;
    if (globignore !== void 0 && globignore !== "") {
      this.hasGlobignore = true;
      this.globignorePatterns = splitGlobignorePatterns(globignore);
    }
  }
  /**
   * Check if nullglob is enabled (return empty for non-matching patterns)
   */
  hasNullglob() {
    return this.nullglob;
  }
  /**
   * Check if failglob is enabled (throw error for non-matching patterns)
   */
  hasFailglob() {
    return this.failglob;
  }
  /**
   * Filter results based on GLOBIGNORE patterns and globskipdots option
   * When GLOBIGNORE is set, . and .. are always filtered
   * When globskipdots is enabled (default in bash >=5.2), . and .. are also filtered
   */
  filterGlobignore(results) {
    if (!this.hasGlobignore && !this.globskipdots) {
      return results;
    }
    return results.filter((path2) => {
      const basename = path2.split("/").pop() || path2;
      if ((this.hasGlobignore || this.globskipdots) && (basename === "." || basename === "..")) {
        return false;
      }
      if (this.hasGlobignore) {
        for (const ignorePattern of this.globignorePatterns) {
          if (this.matchGlobignorePattern(path2, ignorePattern)) {
            return false;
          }
        }
      }
      return true;
    });
  }
  /**
   * Match a path against a GLOBIGNORE pattern
   * GLOBIGNORE patterns are matched against the complete result path.
   * Unlike regular glob matching, * does NOT match / in GLOBIGNORE patterns.
   * This means *.txt filters "foo.txt" but NOT "dir/foo.txt".
   */
  matchGlobignorePattern(path2, pattern) {
    const regex = globignorePatternToRegex(pattern);
    return regex.test(path2);
  }
  /**
   * Check if a string contains glob characters
   */
  isGlobPattern(str) {
    if (str.includes("*") || str.includes("?") || /\[.*\]/.test(str)) {
      return true;
    }
    if (this.extglob && /[@*+?!]\(/.test(str)) {
      return true;
    }
    return false;
  }
  /**
   * Expand an array of arguments, replacing glob patterns with matched files
   * @param args - Array of argument strings
   * @param quotedFlags - Optional array indicating which args were quoted (should not expand)
   */
  async expandArgs(args, quotedFlags) {
    const expansionPromises = args.map((arg, i) => {
      const isQuoted = quotedFlags?.[i] ?? false;
      if (isQuoted || !this.isGlobPattern(arg)) {
        return null;
      }
      return this.expand(arg);
    });
    const expandedResults = await Promise.all(expansionPromises.map((p) => p ? p : Promise.resolve(null)));
    const result2 = [];
    for (let i = 0; i < args.length; i++) {
      const expanded = expandedResults[i];
      if (expanded === null) {
        result2.push(args[i]);
      } else if (expanded.length > 0) {
        result2.push(...expanded);
      } else {
        result2.push(args[i]);
      }
    }
    return result2;
  }
  /**
   * Expand a single glob pattern
   */
  async expand(pattern) {
    let results;
    if (pattern.includes("**") && this.globstar && this.isGlobstarValid(pattern)) {
      results = await this.expandRecursive(pattern);
    } else {
      const normalizedPattern = pattern.replace(/\*\*+/g, "*");
      results = await this.expandSimple(normalizedPattern);
    }
    return this.filterGlobignore(results);
  }
  /**
   * Check if ** is used as a complete path segment (not adjacent to other chars).
   * ** only recurses when it appears as a standalone segment like:
   *   - ** at start
   *   - / ** / in middle (no spaces)
   *   - / ** at end (no spaces)
   * NOT when adjacent to other characters like d** or **y
   */
  isGlobstarValid(pattern) {
    const segments = pattern.split("/");
    for (const segment of segments) {
      if (segment.includes("**")) {
        if (segment !== "**") {
          return false;
        }
      }
    }
    return true;
  }
  /**
   * Check if a path segment contains glob characters
   */
  hasGlobChars(str) {
    if (str.includes("*") || str.includes("?") || /\[.*\]/.test(str)) {
      return true;
    }
    if (this.extglob && /[@*+?!]\(/.test(str)) {
      return true;
    }
    return false;
  }
  /**
   * Expand a simple glob pattern (no **).
   * Handles multi-segment patterns like /dm/star/star.json
   */
  async expandSimple(pattern) {
    const isAbsolute = pattern.startsWith("/");
    const segments = pattern.split("/").filter((s) => s !== "");
    let firstGlobIndex = -1;
    for (let i = 0; i < segments.length; i++) {
      if (this.hasGlobChars(segments[i])) {
        firstGlobIndex = i;
        break;
      }
    }
    if (firstGlobIndex === -1) {
      return [pattern];
    }
    let fsBasePath;
    let resultPrefix;
    if (firstGlobIndex === 0) {
      if (isAbsolute) {
        fsBasePath = "/";
        resultPrefix = "/";
      } else {
        fsBasePath = this.cwd;
        resultPrefix = "";
      }
    } else {
      const baseSegments = segments.slice(0, firstGlobIndex);
      if (isAbsolute) {
        fsBasePath = `/${baseSegments.join("/")}`;
        resultPrefix = `/${baseSegments.join("/")}`;
      } else {
        fsBasePath = this.fs.resolvePath(this.cwd, baseSegments.join("/"));
        resultPrefix = baseSegments.join("/");
      }
    }
    const remainingSegments = segments.slice(firstGlobIndex);
    const results = await this.expandSegments(fsBasePath, resultPrefix, remainingSegments);
    return results.sort();
  }
  /**
   * Recursively expand path segments with glob patterns
   * @param fsPath - The actual filesystem path to read from
   * @param resultPrefix - The prefix to use when building result paths
   * @param segments - Remaining glob segments to match
   */
  async expandSegments(fsPath, resultPrefix, segments) {
    if (segments.length === 0) {
      return [resultPrefix];
    }
    const [currentSegment, ...remainingSegments] = segments;
    const results = [];
    try {
      if (this.fs.readdirWithFileTypes) {
        const entriesWithTypes = await this.fs.readdirWithFileTypes(fsPath);
        const matchPromises = [];
        const allEntries = [...entriesWithTypes];
        const effectiveDotglob = this.dotglob || this.hasGlobignore;
        if (currentSegment.startsWith(".") || this.dotglob) {
          const hasCurrentDir = entriesWithTypes.some((e) => e.name === ".");
          const hasParentDir = entriesWithTypes.some((e) => e.name === "..");
          if (!hasCurrentDir) {
            allEntries.push({
              name: ".",
              isFile: false,
              isDirectory: true,
              isSymbolicLink: false
            });
          }
          if (!hasParentDir) {
            allEntries.push({
              name: "..",
              isFile: false,
              isDirectory: true,
              isSymbolicLink: false
            });
          }
        }
        for (const entry of allEntries) {
          if (entry.name.startsWith(".") && !currentSegment.startsWith(".") && !effectiveDotglob) {
            continue;
          }
          if (this.matchPattern(entry.name, currentSegment)) {
            const newFsPath = fsPath === "/" ? `/${entry.name}` : `${fsPath}/${entry.name}`;
            let newResultPrefix;
            if (resultPrefix === "") {
              newResultPrefix = entry.name;
            } else if (resultPrefix === "/") {
              newResultPrefix = `/${entry.name}`;
            } else {
              newResultPrefix = `${resultPrefix}/${entry.name}`;
            }
            if (remainingSegments.length === 0) {
              matchPromises.push(Promise.resolve([newResultPrefix]));
            } else if (entry.isDirectory) {
              matchPromises.push(this.expandSegments(newFsPath, newResultPrefix, remainingSegments));
            }
          }
        }
        const allResults = await Promise.all(matchPromises);
        for (const pathList of allResults) {
          results.push(...pathList);
        }
      } else {
        const entries = await this.fs.readdir(fsPath);
        const matchPromises = [];
        const allEntries = [...entries];
        const effectiveDotglobFallback = this.dotglob || this.hasGlobignore;
        if (currentSegment.startsWith(".") || this.dotglob) {
          if (!entries.includes(".")) {
            allEntries.push(".");
          }
          if (!entries.includes("..")) {
            allEntries.push("..");
          }
        }
        for (const entry of allEntries) {
          if (entry.startsWith(".") && !currentSegment.startsWith(".") && !effectiveDotglobFallback) {
            continue;
          }
          if (this.matchPattern(entry, currentSegment)) {
            const newFsPath = fsPath === "/" ? `/${entry}` : `${fsPath}/${entry}`;
            let newResultPrefix;
            if (resultPrefix === "") {
              newResultPrefix = entry;
            } else if (resultPrefix === "/") {
              newResultPrefix = `/${entry}`;
            } else {
              newResultPrefix = `${resultPrefix}/${entry}`;
            }
            if (remainingSegments.length === 0) {
              matchPromises.push(Promise.resolve([newResultPrefix]));
            } else {
              matchPromises.push((async () => {
                try {
                  const stat = await this.fs.stat(newFsPath);
                  if (stat.isDirectory) {
                    return this.expandSegments(newFsPath, newResultPrefix, remainingSegments);
                  }
                } catch {
                }
                return [];
              })());
            }
          }
        }
        const allResults = await Promise.all(matchPromises);
        for (const pathList of allResults) {
          results.push(...pathList);
        }
      }
    } catch {
    }
    return results;
  }
  /**
   * Expand a recursive glob pattern (contains **)
   */
  async expandRecursive(pattern) {
    const results = [];
    const doubleStarIndex = pattern.indexOf("**");
    const beforeDoubleStar = pattern.slice(0, doubleStarIndex).replace(/\/$/, "") || ".";
    const afterDoubleStar = pattern.slice(doubleStarIndex + 2);
    const filePattern = afterDoubleStar.replace(/^\//, "");
    if (filePattern.includes("**") && this.isGlobstarValid(filePattern)) {
      await this.walkDirectoryMultiGlobstar(beforeDoubleStar, filePattern, results);
      const unique = [...new Set(results)];
      return unique.sort();
    } else {
      await this.walkDirectory(beforeDoubleStar, filePattern, results);
    }
    return results.sort();
  }
  /**
   * Walk directory for patterns with multiple globstar (like dir/star-star/subdir/star-star/etc)
   * At each directory level, recursively expand the sub-pattern that contains globstar
   */
  async walkDirectoryMultiGlobstar(dir, subPattern, results) {
    const fullPath = this.fs.resolvePath(this.cwd, dir);
    try {
      const entriesWithTypes = this.fs.readdirWithFileTypes ? await this.fs.readdirWithFileTypes(fullPath) : null;
      if (entriesWithTypes) {
        const dirs = [];
        for (const entry of entriesWithTypes) {
          const entryPath = dir === "." ? entry.name : `${dir}/${entry.name}`;
          if (entry.isDirectory) {
            dirs.push(entryPath);
          }
        }
        const patternFromHere = dir === "." ? subPattern : `${dir}/${subPattern}`;
        const subResults = await this.expandRecursive(patternFromHere);
        results.push(...subResults);
        for (let i = 0; i < dirs.length; i += DEFAULT_BATCH_SIZE) {
          const batch = dirs.slice(i, i + DEFAULT_BATCH_SIZE);
          await Promise.all(batch.map((dirPath) => this.walkDirectoryMultiGlobstar(dirPath, subPattern, results)));
        }
      } else {
        const entries = await this.fs.readdir(fullPath);
        const dirs = [];
        for (const entry of entries) {
          const entryPath = dir === "." ? entry : `${dir}/${entry}`;
          const fullEntryPath = this.fs.resolvePath(this.cwd, entryPath);
          try {
            const stat = await this.fs.stat(fullEntryPath);
            if (stat.isDirectory) {
              dirs.push(entryPath);
            }
          } catch {
          }
        }
        const patternFromHere = dir === "." ? subPattern : `${dir}/${subPattern}`;
        const subResults = await this.expandRecursive(patternFromHere);
        results.push(...subResults);
        for (let i = 0; i < dirs.length; i += DEFAULT_BATCH_SIZE) {
          const batch = dirs.slice(i, i + DEFAULT_BATCH_SIZE);
          await Promise.all(batch.map((dirPath) => this.walkDirectoryMultiGlobstar(dirPath, subPattern, results)));
        }
      }
    } catch {
    }
  }
  /**
   * Recursively walk a directory and collect matching files
   */
  async walkDirectory(dir, filePattern, results) {
    const fullPath = this.fs.resolvePath(this.cwd, dir);
    try {
      if (this.fs.readdirWithFileTypes) {
        const entriesWithTypes = await this.fs.readdirWithFileTypes(fullPath);
        const files = [];
        const dirs = [];
        for (const entry of entriesWithTypes) {
          const entryPath = dir === "." ? entry.name : `${dir}/${entry.name}`;
          if (entry.isDirectory) {
            dirs.push(entryPath);
          } else if (filePattern && this.matchPattern(entry.name, filePattern)) {
            files.push(entryPath);
          }
        }
        results.push(...files);
        for (let i = 0; i < dirs.length; i += DEFAULT_BATCH_SIZE) {
          const batch = dirs.slice(i, i + DEFAULT_BATCH_SIZE);
          await Promise.all(batch.map((dirPath) => this.walkDirectory(dirPath, filePattern, results)));
        }
      } else {
        const entries = await this.fs.readdir(fullPath);
        const entryInfos = [];
        for (let i = 0; i < entries.length; i += DEFAULT_BATCH_SIZE) {
          const batch = entries.slice(i, i + DEFAULT_BATCH_SIZE);
          const batchResults = await Promise.all(batch.map(async (entry) => {
            const entryPath = dir === "." ? entry : `${dir}/${entry}`;
            const fullEntryPath = this.fs.resolvePath(this.cwd, entryPath);
            try {
              const stat = await this.fs.stat(fullEntryPath);
              return {
                name: entry,
                path: entryPath,
                isDirectory: stat.isDirectory
              };
            } catch {
              return null;
            }
          }));
          entryInfos.push(...batchResults.filter((r) => r !== null));
        }
        for (const entry of entryInfos) {
          if (!entry.isDirectory && filePattern) {
            if (this.matchPattern(entry.name, filePattern)) {
              results.push(entry.path);
            }
          }
        }
        const dirs = entryInfos.filter((e) => e.isDirectory);
        for (let i = 0; i < dirs.length; i += DEFAULT_BATCH_SIZE) {
          const batch = dirs.slice(i, i + DEFAULT_BATCH_SIZE);
          await Promise.all(batch.map((entry) => this.walkDirectory(entry.path, filePattern, results)));
        }
      }
    } catch {
    }
  }
  /**
   * Match a filename against a glob pattern
   */
  matchPattern(name, pattern) {
    const regex = this.patternToRegex(pattern);
    return regex.test(name);
  }
  /**
   * Convert a glob pattern to a RegExp
   */
  patternToRegex(pattern) {
    const regex = this.patternToRegexStr(pattern);
    return new RegExp(`^${regex}$`);
  }
  /**
   * Convert a glob pattern to a regex string (without anchors)
   */
  patternToRegexStr(pattern) {
    let regex = "";
    let inQuotedSection = false;
    for (let i = 0; i < pattern.length; i++) {
      if (pattern.slice(i, i + 13) === "\0QUOTE_START\0") {
        inQuotedSection = true;
        i += 12;
        continue;
      }
      if (pattern.slice(i, i + 11) === "\0QUOTE_END\0") {
        inQuotedSection = false;
        i += 10;
        continue;
      }
      const c = pattern[i];
      if (inQuotedSection) {
        if (/[.+^${}()|\\*?[\]]/.test(c)) {
          regex += `\\${c}`;
        } else {
          regex += c;
        }
        continue;
      }
      if (this.extglob && (c === "@" || c === "*" || c === "+" || c === "?" || c === "!") && i + 1 < pattern.length && pattern[i + 1] === "(") {
        const closeIdx = findMatchingParen$1(pattern, i + 1);
        if (closeIdx !== -1) {
          const content = pattern.slice(i + 2, closeIdx);
          const alternatives = splitExtglobAlternatives$1(content);
          const altRegexes = alternatives.map((alt) => this.patternToRegexStr(alt));
          const altGroup = altRegexes.length > 0 ? altRegexes.join("|") : "(?:)";
          if (c === "@") {
            regex += `(?:${altGroup})`;
          } else if (c === "*") {
            regex += `(?:${altGroup})*`;
          } else if (c === "+") {
            regex += `(?:${altGroup})+`;
          } else if (c === "?") {
            regex += `(?:${altGroup})?`;
          } else if (c === "!") {
            const hasMorePattern = closeIdx < pattern.length - 1;
            if (hasMorePattern) {
              const lengths = alternatives.map((alt) => this.computePatternLength(alt));
              const allSameLength = lengths.every((l) => l !== null) && lengths.every((l) => l === lengths[0]);
              if (allSameLength && lengths[0] !== null) {
                const n = lengths[0];
                if (n === 0) {
                  regex += "(?:.+)";
                } else {
                  const parts = [];
                  if (n > 0) {
                    parts.push(`.{0,${n - 1}}`);
                  }
                  parts.push(`.{${n + 1},}`);
                  parts.push(`(?!(?:${altGroup})).{${n}}`);
                  regex += `(?:${parts.join("|")})`;
                }
              } else {
                regex += `(?:(?!(?:${altGroup})).)*?`;
              }
            } else {
              regex += `(?!(?:${altGroup})$).*`;
            }
          }
          i = closeIdx;
          continue;
        }
      }
      if (c === "*") {
        regex += ".*";
      } else if (c === "?") {
        regex += ".";
      } else if (c === "[") {
        let j = i + 1;
        let classContent = "[";
        if (j < pattern.length && (pattern[j] === "^" || pattern[j] === "!")) {
          classContent += "^";
          j++;
        }
        if (j < pattern.length && pattern[j] === "]") {
          classContent += "\\]";
          j++;
        }
        let classEnd = j;
        while (classEnd < pattern.length) {
          if (pattern[classEnd] === "\\" && classEnd + 1 < pattern.length) {
            classEnd += 2;
            continue;
          }
          if (pattern[classEnd] === "[" && classEnd + 1 < pattern.length && pattern[classEnd + 1] === ":") {
            const posixEnd = pattern.indexOf(":]", classEnd + 2);
            if (posixEnd !== -1) {
              classEnd = posixEnd + 2;
              continue;
            }
          }
          if (pattern[classEnd] === "]") {
            break;
          }
          classEnd++;
        }
        const classStartPos = j;
        while (j < pattern.length && pattern[j] !== "]") {
          if (pattern[j] === "[" && j + 1 < pattern.length && pattern[j + 1] === ":") {
            const posixEnd = pattern.indexOf(":]", j + 2);
            if (posixEnd !== -1) {
              const posixClass = pattern.slice(j + 2, posixEnd);
              const regexClass = posixClassToRegex(posixClass);
              classContent += regexClass;
              j = posixEnd + 2;
              continue;
            }
          }
          if (pattern[j] === "\\" && j + 1 < pattern.length) {
            classContent += `\\${pattern[j + 1]}`;
            j += 2;
            continue;
          }
          if (pattern[j] === "-") {
            const atStart = j === classStartPos;
            const atEnd = j + 1 === classEnd;
            if (atStart || atEnd) {
              classContent += "\\-";
            } else {
              classContent += "-";
            }
          } else {
            classContent += pattern[j];
          }
          j++;
        }
        classContent += "]";
        regex += classContent;
        i = j;
      } else if (c === "\\" && i + 1 < pattern.length) {
        const nextChar = pattern[i + 1];
        if (/[.+^${}()|\\*?[\]]/.test(nextChar)) {
          regex += `\\${nextChar}`;
        } else {
          regex += nextChar;
        }
        i++;
      } else if (/[.+^${}()|]/.test(c)) {
        regex += `\\${c}`;
      } else {
        regex += c;
      }
    }
    return regex;
  }
  /**
   * Compute the fixed length of a pattern, if it has one.
   * Returns null if the pattern has variable length (contains *, +, etc.).
   * Used to optimize !() extglob patterns.
   */
  computePatternLength(pattern) {
    let length = 0;
    let i = 0;
    let inQuotedSection = false;
    while (i < pattern.length) {
      if (pattern.slice(i, i + 13) === "\0QUOTE_START\0") {
        inQuotedSection = true;
        i += 13;
        continue;
      }
      if (pattern.slice(i, i + 11) === "\0QUOTE_END\0") {
        inQuotedSection = false;
        i += 11;
        continue;
      }
      const c = pattern[i];
      if (inQuotedSection) {
        length += 1;
        i++;
        continue;
      }
      if ((c === "@" || c === "*" || c === "+" || c === "?" || c === "!") && i + 1 < pattern.length && pattern[i + 1] === "(") {
        const closeIdx = findMatchingParen$1(pattern, i + 1);
        if (closeIdx !== -1) {
          if (c === "@") {
            const content = pattern.slice(i + 2, closeIdx);
            const alts = splitExtglobAlternatives$1(content);
            const altLengths = alts.map((a) => this.computePatternLength(a));
            if (altLengths.every((l) => l !== null) && altLengths.every((l) => l === altLengths[0])) {
              length += altLengths[0];
              i = closeIdx + 1;
              continue;
            }
            return null;
          }
          return null;
        }
      }
      if (c === "*") {
        return null;
      }
      if (c === "?") {
        length += 1;
        i++;
        continue;
      }
      if (c === "[") {
        const closeIdx = pattern.indexOf("]", i + 1);
        if (closeIdx !== -1) {
          length += 1;
          i = closeIdx + 1;
          continue;
        }
        length += 1;
        i++;
        continue;
      }
      if (c === "\\") {
        length += 1;
        i += 2;
        continue;
      }
      length += 1;
      i++;
    }
    return length;
  }
}, __name$1(_s, "GlobExpander"), _s);
function applyBinaryOp(left, right, operator) {
  switch (operator) {
    case "+":
      return left + right;
    case "-":
      return left - right;
    case "*":
      return left * right;
    case "/":
      if (right === 0) {
        throw new ArithmeticError("division by 0");
      }
      return Math.trunc(left / right);
    case "%":
      if (right === 0) {
        throw new ArithmeticError("division by 0");
      }
      return left % right;
    case "**":
      if (right < 0) {
        throw new ArithmeticError("exponent less than 0");
      }
      return left ** right;
    case "<<":
      return left << right;
    case ">>":
      return left >> right;
    case "<":
      return left < right ? 1 : 0;
    case "<=":
      return left <= right ? 1 : 0;
    case ">":
      return left > right ? 1 : 0;
    case ">=":
      return left >= right ? 1 : 0;
    case "==":
      return left === right ? 1 : 0;
    case "!=":
      return left !== right ? 1 : 0;
    case "&":
      return left & right;
    case "|":
      return left | right;
    case "^":
      return left ^ right;
    case ",":
      return right;
    default:
      return 0;
  }
}
__name$1(applyBinaryOp, "applyBinaryOp");
function applyAssignmentOp(current, value, operator) {
  switch (operator) {
    case "=":
      return value;
    case "+=":
      return current + value;
    case "-=":
      return current - value;
    case "*=":
      return current * value;
    case "/=":
      return value !== 0 ? Math.trunc(current / value) : 0;
    case "%=":
      return value !== 0 ? current % value : 0;
    case "<<=":
      return current << value;
    case ">>=":
      return current >> value;
    case "&=":
      return current & value;
    case "|=":
      return current | value;
    case "^=":
      return current ^ value;
    default:
      return value;
  }
}
__name$1(applyAssignmentOp, "applyAssignmentOp");
function applyUnaryOp(operand, operator) {
  switch (operator) {
    case "-":
      return -operand;
    case "+":
      return +operand;
    case "!":
      return operand === 0 ? 1 : 0;
    case "~":
      return ~operand;
    default:
      return operand;
  }
}
__name$1(applyUnaryOp, "applyUnaryOp");
async function getArithVariable(ctx, name) {
  const directValue = ctx.state.env[name];
  if (directValue !== void 0) {
    return directValue;
  }
  const arrayZeroValue = ctx.state.env[`${name}_0`];
  if (arrayZeroValue !== void 0) {
    return arrayZeroValue;
  }
  return await getVariable(ctx, name);
}
__name$1(getArithVariable, "getArithVariable");
function parseArithValue(value) {
  if (!value) {
    return 0;
  }
  const num = Number.parseInt(value, 10);
  if (!Number.isNaN(num) && /^-?\d+$/.test(value.trim())) {
    return num;
  }
  const trimmed = value.trim();
  if (!trimmed) {
    return 0;
  }
  try {
    const parser = new Parser();
    const { expr, pos } = parseArithExpr(parser, trimmed, 0);
    if (pos < trimmed.length) {
      const errorToken = trimmed.slice(pos).trim().split(/\s+/)[0];
      throw new ArithmeticError(`${trimmed}: syntax error in expression (error token is "${errorToken}")`);
    }
    if (expr.type === "ArithNumber") {
      return expr.value;
    }
    return num || 0;
  } catch (error) {
    if (error instanceof ArithmeticError) {
      throw error;
    }
    const errorToken = trimmed.split(/\s+/).slice(1)[0] || trimmed;
    throw new ArithmeticError(`${trimmed}: syntax error in expression (error token is "${errorToken}")`);
  }
}
__name$1(parseArithValue, "parseArithValue");
async function evaluateArithValue(ctx, value) {
  if (!value) {
    return 0;
  }
  const num = Number.parseInt(value, 10);
  if (!Number.isNaN(num) && /^-?\d+$/.test(value.trim())) {
    return num;
  }
  const trimmed = value.trim();
  if (!trimmed) {
    return 0;
  }
  const parser = new Parser();
  const { expr, pos } = parseArithExpr(parser, trimmed, 0);
  if (pos < trimmed.length) {
    const unparsed = trimmed.slice(pos).trim();
    const errorToken = unparsed.split(/\s+/)[0] || unparsed;
    throw new ArithmeticError(`syntax error in expression (error token is "${errorToken}")`, "", "");
  }
  return await evaluateArithmetic(ctx, expr);
}
__name$1(evaluateArithValue, "evaluateArithValue");
async function resolveArithVariable(ctx, name, visited = /* @__PURE__ */ new Set()) {
  if (visited.has(name)) {
    return 0;
  }
  visited.add(name);
  const value = await getArithVariable(ctx, name);
  if (!value) {
    return 0;
  }
  const num = Number.parseInt(value, 10);
  if (!Number.isNaN(num) && /^-?\d+$/.test(value.trim())) {
    return num;
  }
  const trimmed = value.trim();
  if (/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(trimmed)) {
    return await resolveArithVariable(ctx, trimmed, visited);
  }
  const parser = new Parser();
  const { expr, pos } = parseArithExpr(parser, trimmed, 0);
  if (pos < trimmed.length) {
    const unparsed = trimmed.slice(pos).trim();
    const errorToken = unparsed.split(/\s+/)[0] || unparsed;
    throw new ArithmeticError(`${trimmed}: syntax error in expression (error token is "${errorToken}")`);
  }
  return await evaluateArithmetic(ctx, expr);
}
__name$1(resolveArithVariable, "resolveArithVariable");
async function expandBracedContent(ctx, content) {
  if (content.startsWith("#")) {
    const varName2 = content.slice(1);
    const arrayMatch = varName2.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[[@*]\]$/);
    if (arrayMatch) {
      const arrayName = arrayMatch[1];
      const elements = getArrayElements(ctx, arrayName);
      return String(elements.length);
    }
    const value2 = ctx.state.env[varName2] || "";
    return String(value2.length);
  }
  if (content.startsWith("!")) {
    const varName2 = content.slice(1);
    const indirect = ctx.state.env[varName2] || "";
    return ctx.state.env[indirect] || "";
  }
  const operators = [":-", ":=", ":?", ":+", "-", "=", "?", "+"];
  let opIndex = -1;
  let op = "";
  for (const operator of operators) {
    const idx = content.indexOf(operator);
    if (idx > 0 && (opIndex === -1 || idx < opIndex)) {
      opIndex = idx;
      op = operator;
    }
  }
  if (opIndex === -1) {
    return await getVariable(ctx, content);
  }
  const varName = content.slice(0, opIndex);
  const defaultValue = content.slice(opIndex + op.length);
  const value = ctx.state.env[varName];
  const isUnset = value === void 0;
  const isEmpty = value === "";
  const checkEmpty = op.startsWith(":");
  switch (op) {
    case ":-":
    case "-": {
      const useDefault = isUnset || checkEmpty && isEmpty;
      return useDefault ? defaultValue : value || "";
    }
    case ":=":
    case "=": {
      const useDefault = isUnset || checkEmpty && isEmpty;
      if (useDefault) {
        ctx.state.env[varName] = defaultValue;
        return defaultValue;
      }
      return value || "";
    }
    case ":+":
    case "+": {
      const useAlternative = !(isUnset || checkEmpty && isEmpty);
      return useAlternative ? defaultValue : "";
    }
    case ":?":
    case "?": {
      const shouldError = isUnset || checkEmpty && isEmpty;
      if (shouldError) {
        throw new Error(defaultValue || `${varName}: parameter null or not set`);
      }
      return value || "";
    }
    default:
      return value || "";
  }
}
__name$1(expandBracedContent, "expandBracedContent");
async function evaluateArithmetic(ctx, expr, isExpansionContext = false) {
  switch (expr.type) {
    case "ArithNumber":
      if (Number.isNaN(expr.value)) {
        throw new ArithmeticError("value too great for base");
      }
      return expr.value;
    case "ArithVariable": {
      return await resolveArithVariable(ctx, expr.name);
    }
    case "ArithSpecialVar": {
      const value = await getVariable(ctx, expr.name);
      const trimmed = value.trim();
      if (!trimmed)
        return 0;
      const num = Number.parseInt(trimmed, 10);
      if (!Number.isNaN(num) && /^-?\d+$/.test(trimmed))
        return num;
      const parser = new Parser();
      const { expr: parsed } = parseArithExpr(parser, trimmed, 0);
      return await evaluateArithmetic(ctx, parsed);
    }
    case "ArithNested":
      return await evaluateArithmetic(ctx, expr.expression);
    case "ArithCommandSubst": {
      if (ctx.execFn) {
        const result2 = await ctx.execFn(expr.command);
        if (result2.stderr) {
          ctx.state.expansionStderr = (ctx.state.expansionStderr || "") + result2.stderr;
        }
        const output = result2.stdout.trim();
        return Number.parseInt(output, 10) || 0;
      }
      return 0;
    }
    case "ArithBracedExpansion": {
      const expanded = await expandBracedContent(ctx, expr.content);
      return Number.parseInt(expanded, 10) || 0;
    }
    case "ArithDynamicBase": {
      const baseStr = await expandBracedContent(ctx, expr.baseExpr);
      const base = Number.parseInt(baseStr, 10);
      if (base < 2 || base > 64)
        return 0;
      const numStr = `${base}#${expr.value}`;
      return parseArithNumber(numStr);
    }
    case "ArithDynamicNumber": {
      const prefix = await expandBracedContent(ctx, expr.prefix);
      const numStr = prefix + expr.suffix;
      return parseArithNumber(numStr);
    }
    case "ArithArrayElement": {
      const isAssoc = ctx.state.associativeArrays?.has(expr.array);
      const lookupArrayValue = /* @__PURE__ */ __name$1(async (envKey) => {
        const arrayValue = ctx.state.env[envKey];
        if (arrayValue !== void 0) {
          return await evaluateArithValue(ctx, arrayValue);
        }
        return 0;
      }, "lookupArrayValue");
      if (expr.stringKey !== void 0) {
        return await lookupArrayValue(`${expr.array}_${expr.stringKey}`);
      }
      if (isAssoc && expr.index?.type === "ArithVariable" && !expr.index.hasDollarPrefix) {
        return await lookupArrayValue(`${expr.array}_${expr.index.name}`);
      }
      if (isAssoc && expr.index?.type === "ArithVariable" && expr.index.hasDollarPrefix) {
        const expandedKey = await getVariable(ctx, expr.index.name);
        return await lookupArrayValue(`${expr.array}_${expandedKey}`);
      }
      if (expr.index) {
        let index = await evaluateArithmetic(ctx, expr.index, isExpansionContext);
        if (index < 0) {
          const elements = getArrayElements(ctx, expr.array);
          const lineNum = ctx.state.currentLine;
          if (elements.length === 0) {
            ctx.state.expansionStderr = (ctx.state.expansionStderr || "") + `bash: line ${lineNum}: ${expr.array}: bad array subscript
`;
            return 0;
          }
          const maxIndex = Math.max(...elements.map(([idx]) => typeof idx === "number" ? idx : 0));
          const actualIdx = maxIndex + 1 + index;
          if (actualIdx < 0) {
            ctx.state.expansionStderr = (ctx.state.expansionStderr || "") + `bash: line ${lineNum}: ${expr.array}: bad array subscript
`;
            return 0;
          }
          index = actualIdx;
        }
        const envKey = `${expr.array}_${index}`;
        const arrayValue = ctx.state.env[envKey];
        if (arrayValue !== void 0) {
          return evaluateArithValue(ctx, arrayValue);
        }
        if (index === 0) {
          const scalarValue = ctx.state.env[expr.array];
          if (scalarValue !== void 0) {
            return evaluateArithValue(ctx, scalarValue);
          }
        }
        if (ctx.state.options.nounset) {
          const hasAnyElement = Object.keys(ctx.state.env).some((key2) => key2 === expr.array || key2.startsWith(`${expr.array}_`));
          if (!hasAnyElement) {
            throw new NounsetError(`${expr.array}[${index}]`);
          }
        }
        return 0;
      }
      return 0;
    }
    case "ArithDoubleSubscript": {
      throw new ArithmeticError("double subscript", "", "");
    }
    case "ArithNumberSubscript": {
      throw new ArithmeticError(`${expr.number}${expr.errorToken}: syntax error: invalid arithmetic operator (error token is "${expr.errorToken}")`);
    }
    case "ArithSyntaxError": {
      throw new ArithmeticError(expr.message, "", "", true);
    }
    case "ArithSingleQuote": {
      if (isExpansionContext) {
        throw new ArithmeticError(`syntax error: operand expected (error token is "'${expr.content}'")`);
      }
      return expr.value;
    }
    case "ArithBinary": {
      if (expr.operator === "||") {
        const left2 = await evaluateArithmetic(ctx, expr.left, isExpansionContext);
        if (left2)
          return 1;
        return await evaluateArithmetic(ctx, expr.right, isExpansionContext) ? 1 : 0;
      }
      if (expr.operator === "&&") {
        const left2 = await evaluateArithmetic(ctx, expr.left, isExpansionContext);
        if (!left2)
          return 0;
        return await evaluateArithmetic(ctx, expr.right, isExpansionContext) ? 1 : 0;
      }
      const left = await evaluateArithmetic(ctx, expr.left, isExpansionContext);
      const right = await evaluateArithmetic(ctx, expr.right, isExpansionContext);
      return applyBinaryOp(left, right, expr.operator);
    }
    case "ArithUnary": {
      const operand = await evaluateArithmetic(ctx, expr.operand, isExpansionContext);
      if (expr.operator === "++" || expr.operator === "--") {
        if (expr.operand.type === "ArithVariable") {
          const name = expr.operand.name;
          const current = Number.parseInt(await getVariable(ctx, name), 10) || 0;
          const newValue = expr.operator === "++" ? current + 1 : current - 1;
          ctx.state.env[name] = String(newValue);
          return expr.prefix ? newValue : current;
        }
        if (expr.operand.type === "ArithArrayElement") {
          const arrayName = expr.operand.array;
          const isAssoc = ctx.state.associativeArrays?.has(arrayName);
          let envKey;
          if (expr.operand.stringKey !== void 0) {
            envKey = `${arrayName}_${expr.operand.stringKey}`;
          } else if (isAssoc && expr.operand.index?.type === "ArithVariable" && !expr.operand.index.hasDollarPrefix) {
            envKey = `${arrayName}_${expr.operand.index.name}`;
          } else if (isAssoc && expr.operand.index?.type === "ArithVariable" && expr.operand.index.hasDollarPrefix) {
            const expandedKey = await getVariable(ctx, expr.operand.index.name);
            envKey = `${arrayName}_${expandedKey}`;
          } else if (expr.operand.index) {
            const index = await evaluateArithmetic(ctx, expr.operand.index, isExpansionContext);
            envKey = `${arrayName}_${index}`;
          } else {
            return operand;
          }
          const current = Number.parseInt(ctx.state.env[envKey] || "0", 10) || 0;
          const newValue = expr.operator === "++" ? current + 1 : current - 1;
          ctx.state.env[envKey] = String(newValue);
          return expr.prefix ? newValue : current;
        }
        if (expr.operand.type === "ArithConcat") {
          let varName = "";
          for (const part of expr.operand.parts) {
            varName += await evalConcatPartToStringAsync(ctx, part, isExpansionContext);
          }
          if (varName && /^[a-zA-Z_][a-zA-Z0-9_]*$/.test(varName)) {
            const current = Number.parseInt(ctx.state.env[varName] || "0", 10) || 0;
            const newValue = expr.operator === "++" ? current + 1 : current - 1;
            ctx.state.env[varName] = String(newValue);
            return expr.prefix ? newValue : current;
          }
        }
        if (expr.operand.type === "ArithDynamicElement") {
          let varName = "";
          if (expr.operand.nameExpr.type === "ArithConcat") {
            for (const part of expr.operand.nameExpr.parts) {
              varName += await evalConcatPartToStringAsync(ctx, part, isExpansionContext);
            }
          } else if (expr.operand.nameExpr.type === "ArithVariable") {
            varName = expr.operand.nameExpr.hasDollarPrefix ? await getVariable(ctx, expr.operand.nameExpr.name) : expr.operand.nameExpr.name;
          }
          if (varName && /^[a-zA-Z_][a-zA-Z0-9_]*$/.test(varName)) {
            const index = await evaluateArithmetic(ctx, expr.operand.subscript, isExpansionContext);
            const envKey = `${varName}_${index}`;
            const current = Number.parseInt(ctx.state.env[envKey] || "0", 10) || 0;
            const newValue = expr.operator === "++" ? current + 1 : current - 1;
            ctx.state.env[envKey] = String(newValue);
            return expr.prefix ? newValue : current;
          }
        }
        return operand;
      }
      return applyUnaryOp(operand, expr.operator);
    }
    case "ArithTernary": {
      const condition = await evaluateArithmetic(ctx, expr.condition, isExpansionContext);
      return condition ? await evaluateArithmetic(ctx, expr.consequent, isExpansionContext) : await evaluateArithmetic(ctx, expr.alternate, isExpansionContext);
    }
    case "ArithAssignment": {
      const name = expr.variable;
      let envKey = name;
      if (expr.stringKey !== void 0) {
        envKey = `${name}_${expr.stringKey}`;
      } else if (expr.subscript) {
        const isAssoc = ctx.state.associativeArrays?.has(name);
        if (isAssoc && expr.subscript.type === "ArithVariable" && !expr.subscript.hasDollarPrefix) {
          envKey = `${name}_${expr.subscript.name}`;
        } else if (isAssoc && expr.subscript.type === "ArithVariable" && expr.subscript.hasDollarPrefix) {
          const expandedKey = await getVariable(ctx, expr.subscript.name);
          envKey = `${name}_${expandedKey || "\\"}`;
        } else if (isAssoc) {
          const index = await evaluateArithmetic(ctx, expr.subscript, isExpansionContext);
          envKey = `${name}_${index}`;
        } else {
          let index = await evaluateArithmetic(ctx, expr.subscript, isExpansionContext);
          if (index < 0) {
            const elements = getArrayElements(ctx, name);
            if (elements.length > 0) {
              const maxIndex = Math.max(...elements.map(([idx]) => typeof idx === "number" ? idx : 0));
              index = maxIndex + 1 + index;
            }
          }
          envKey = `${name}_${index}`;
        }
      }
      const current = Number.parseInt(ctx.state.env[envKey] || "0", 10) || 0;
      const value = await evaluateArithmetic(ctx, expr.value, isExpansionContext);
      const newValue = applyAssignmentOp(current, value, expr.operator);
      ctx.state.env[envKey] = String(newValue);
      return newValue;
    }
    case "ArithGroup":
      return await evaluateArithmetic(ctx, expr.expression, isExpansionContext);
    case "ArithConcat": {
      let concatenated = "";
      for (const part of expr.parts) {
        concatenated += await evalConcatPartToStringAsync(ctx, part, isExpansionContext);
      }
      if (/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(concatenated)) {
        return await resolveArithVariable(ctx, concatenated);
      }
      return Number.parseInt(concatenated, 10) || 0;
    }
    case "ArithDynamicAssignment": {
      let varName = "";
      if (expr.target.type === "ArithConcat") {
        for (const part of expr.target.parts) {
          varName += await evalConcatPartToStringAsync(ctx, part, isExpansionContext);
        }
      } else if (expr.target.type === "ArithVariable") {
        varName = expr.target.hasDollarPrefix ? await getVariable(ctx, expr.target.name) : expr.target.name;
      }
      if (!varName || !/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(varName)) {
        return 0;
      }
      let envKey = varName;
      if (expr.subscript) {
        const index = await evaluateArithmetic(ctx, expr.subscript, isExpansionContext);
        envKey = `${varName}_${index}`;
      }
      const current = Number.parseInt(ctx.state.env[envKey] || "0", 10) || 0;
      const value = await evaluateArithmetic(ctx, expr.value, isExpansionContext);
      const newValue = applyAssignmentOp(current, value, expr.operator);
      ctx.state.env[envKey] = String(newValue);
      return newValue;
    }
    case "ArithDynamicElement": {
      let varName = "";
      if (expr.nameExpr.type === "ArithConcat") {
        for (const part of expr.nameExpr.parts) {
          varName += await evalConcatPartToStringAsync(ctx, part, isExpansionContext);
        }
      } else if (expr.nameExpr.type === "ArithVariable") {
        varName = expr.nameExpr.hasDollarPrefix ? await getVariable(ctx, expr.nameExpr.name) : expr.nameExpr.name;
      }
      if (!varName || !/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(varName)) {
        return 0;
      }
      const index = await evaluateArithmetic(ctx, expr.subscript, isExpansionContext);
      const envKey = `${varName}_${index}`;
      const value = ctx.state.env[envKey];
      if (value !== void 0) {
        return parseArithValue(value);
      }
      return 0;
    }
    default:
      return 0;
  }
}
__name$1(evaluateArithmetic, "evaluateArithmetic");
async function evalConcatPartToStringAsync(ctx, expr, isExpansionContext = false) {
  switch (expr.type) {
    case "ArithNumber":
      return String(expr.value);
    case "ArithSingleQuote":
      return String(await evaluateArithmetic(ctx, expr, isExpansionContext));
    case "ArithVariable":
      if (expr.hasDollarPrefix) {
        return await getVariable(ctx, expr.name);
      }
      return expr.name;
    case "ArithSpecialVar":
      return await getVariable(ctx, expr.name);
    case "ArithBracedExpansion":
      return await expandBracedContent(ctx, expr.content);
    case "ArithCommandSubst": {
      if (ctx.execFn) {
        const result2 = await ctx.execFn(expr.command);
        return result2.stdout.trim();
      }
      return "0";
    }
    case "ArithConcat": {
      let result2 = "";
      for (const part of expr.parts) {
        result2 += await evalConcatPartToStringAsync(ctx, part, isExpansionContext);
      }
      return result2;
    }
    default:
      return String(await evaluateArithmetic(ctx, expr, isExpansionContext));
  }
}
__name$1(evalConcatPartToStringAsync, "evalConcatPartToStringAsync");
function globPatternHasVarRef(pattern) {
  for (let i = 0; i < pattern.length; i++) {
    if (pattern[i] === "\\") {
      i++;
      continue;
    }
    if (pattern[i] === "$") {
      const next = pattern[i + 1];
      if (next === "{" || next && /[a-zA-Z_]/.test(next)) {
        return true;
      }
    }
  }
  return false;
}
__name$1(globPatternHasVarRef, "globPatternHasVarRef");
function hasQuotedOperationWord(part) {
  if (!part.operation)
    return false;
  const op = part.operation;
  let wordParts;
  if (op.type === "DefaultValue" || op.type === "AssignDefault" || op.type === "UseAlternative" || op.type === "ErrorIfUnset") {
    wordParts = op.word?.parts;
  }
  if (!wordParts)
    return false;
  for (const p of wordParts) {
    if (p.type === "DoubleQuoted" || p.type === "SingleQuoted") {
      return true;
    }
  }
  return false;
}
__name$1(hasQuotedOperationWord, "hasQuotedOperationWord");
function isOperationWordEntirelyQuoted(part) {
  if (!part.operation)
    return false;
  const op = part.operation;
  let wordParts;
  if (op.type === "DefaultValue" || op.type === "AssignDefault" || op.type === "UseAlternative" || op.type === "ErrorIfUnset") {
    wordParts = op.word?.parts;
  }
  if (!wordParts || wordParts.length === 0)
    return false;
  for (const p of wordParts) {
    if (p.type !== "DoubleQuoted" && p.type !== "SingleQuoted") {
      return false;
    }
  }
  return true;
}
__name$1(isOperationWordEntirelyQuoted, "isOperationWordEntirelyQuoted");
function analyzeWordParts(parts) {
  let hasQuoted = false;
  let hasCommandSub = false;
  let hasArrayVar = false;
  let hasArrayAtExpansion = false;
  let hasParamExpansion = false;
  let hasVarNamePrefixExpansion = false;
  let hasIndirection = false;
  for (const part of parts) {
    if (part.type === "SingleQuoted" || part.type === "DoubleQuoted") {
      hasQuoted = true;
      if (part.type === "DoubleQuoted") {
        for (const inner of part.parts) {
          if (inner.type === "ParameterExpansion") {
            const match = inner.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[[@*]\]$/);
            if (match && (!inner.operation || inner.operation.type === "PatternRemoval" || inner.operation.type === "PatternReplacement")) {
              hasArrayAtExpansion = true;
            }
            if (inner.operation?.type === "VarNamePrefix" || inner.operation?.type === "ArrayKeys") {
              hasVarNamePrefixExpansion = true;
            }
            if (inner.operation?.type === "Indirection") {
              hasIndirection = true;
            }
          }
        }
      }
    }
    if (part.type === "CommandSubstitution") {
      hasCommandSub = true;
    }
    if (part.type === "ParameterExpansion") {
      hasParamExpansion = true;
      if (part.parameter === "@" || part.parameter === "*") {
        hasArrayVar = true;
      }
      if (hasQuotedOperationWord(part)) {
        hasQuoted = true;
      }
      if (part.operation?.type === "VarNamePrefix" || part.operation?.type === "ArrayKeys") {
        hasVarNamePrefixExpansion = true;
      }
      if (part.operation?.type === "Indirection") {
        hasIndirection = true;
      }
    }
    if (part.type === "Glob" && globPatternHasVarRef(part.pattern)) {
      hasParamExpansion = true;
    }
  }
  return {
    hasQuoted,
    hasCommandSub,
    hasArrayVar,
    hasArrayAtExpansion,
    hasParamExpansion,
    hasVarNamePrefixExpansion,
    hasIndirection
  };
}
__name$1(analyzeWordParts, "analyzeWordParts");
var BASH_VERSION = "5.1.0(1)-release";
var KERNEL_VERSION = "Linux version 5.15.0-generic (just-bash) #1 SMP PREEMPT";
function getProcessInfo() {
  return {
    pid: process.pid,
    ppid: process.ppid,
    uid: process.getuid?.() ?? 1e3,
    gid: process.getgid?.() ?? 1e3
  };
}
__name$1(getProcessInfo, "getProcessInfo");
function formatProcStatus() {
  const { pid, ppid, uid, gid } = getProcessInfo();
  return `Name:	bash
State:	R (running)
Pid:	${pid}
PPid:	${ppid}
Uid:	${uid}	${uid}	${uid}	${uid}
Gid:	${gid}	${gid}	${gid}	${gid}
`;
}
__name$1(formatProcStatus, "formatProcStatus");
function getArrayIndices(ctx, arrayName) {
  const prefix = `${arrayName}_`;
  const indices = [];
  for (const key2 of Object.keys(ctx.state.env)) {
    if (key2.startsWith(prefix)) {
      const indexStr = key2.slice(prefix.length);
      const index = Number.parseInt(indexStr, 10);
      if (!Number.isNaN(index) && String(index) === indexStr) {
        indices.push(index);
      }
    }
  }
  return indices.sort((a, b) => a - b);
}
__name$1(getArrayIndices, "getArrayIndices");
function clearArray(ctx, arrayName) {
  const prefix = `${arrayName}_`;
  for (const key2 of Object.keys(ctx.state.env)) {
    if (key2.startsWith(prefix)) {
      delete ctx.state.env[key2];
    }
  }
}
__name$1(clearArray, "clearArray");
function getAssocArrayKeys(ctx, arrayName) {
  const prefix = `${arrayName}_`;
  const metadataSuffix = `${arrayName}__length`;
  const keys = [];
  for (const envKey of Object.keys(ctx.state.env)) {
    if (envKey === metadataSuffix) {
      continue;
    }
    if (envKey.startsWith(prefix)) {
      const key2 = envKey.slice(prefix.length);
      if (key2.startsWith("_length")) {
        continue;
      }
      keys.push(key2);
    }
  }
  return keys.sort();
}
__name$1(getAssocArrayKeys, "getAssocArrayKeys");
function unquoteKey(key2) {
  if (key2.startsWith("'") && key2.endsWith("'") || key2.startsWith('"') && key2.endsWith('"')) {
    return key2.slice(1, -1);
  }
  return key2;
}
__name$1(unquoteKey, "unquoteKey");
function parseKeyedElementFromWord(word) {
  if (word.parts.length < 2)
    return null;
  const first = word.parts[0];
  const second = word.parts[1];
  if (first.type !== "Glob" || !first.pattern.startsWith("[")) {
    return null;
  }
  let key2;
  let secondPart = second;
  let secondPartIndex = 1;
  if (second.type === "Literal" && second.value.startsWith("]")) {
    const afterBracket = second.value.slice(1);
    if (afterBracket.startsWith("+=") || afterBracket.startsWith("=")) {
      key2 = first.pattern.slice(1);
    } else if (afterBracket === "") {
      if (word.parts.length < 3)
        return null;
      const third = word.parts[2];
      if (third.type !== "Literal")
        return null;
      if (!third.value.startsWith("=") && !third.value.startsWith("+="))
        return null;
      key2 = first.pattern.slice(1);
      secondPart = third;
      secondPartIndex = 2;
    } else {
      return null;
    }
  } else if (first.pattern === "[" && (second.type === "DoubleQuoted" || second.type === "SingleQuoted")) {
    if (word.parts.length < 3)
      return null;
    const third = word.parts[2];
    if (third.type !== "Literal")
      return null;
    if (!third.value.startsWith("]=") && !third.value.startsWith("]+="))
      return null;
    if (second.type === "SingleQuoted") {
      key2 = second.value;
    } else {
      key2 = "";
      for (const inner of second.parts) {
        if (inner.type === "Literal") {
          key2 += inner.value;
        } else if (inner.type === "Escaped") {
          key2 += inner.value;
        }
      }
    }
    secondPart = third;
    secondPartIndex = 2;
  } else if (first.pattern.endsWith("]")) {
    if (second.type !== "Literal")
      return null;
    if (!second.value.startsWith("=") && !second.value.startsWith("+="))
      return null;
    key2 = first.pattern.slice(1, -1);
  } else {
    return null;
  }
  key2 = unquoteKey(key2);
  let assignmentContent;
  if (secondPart.type !== "Literal")
    return null;
  if (secondPart.value.startsWith("]=")) {
    assignmentContent = secondPart.value.slice(1);
  } else if (secondPart.value.startsWith("]+=")) {
    assignmentContent = secondPart.value.slice(1);
  } else {
    assignmentContent = secondPart.value;
  }
  const append = assignmentContent.startsWith("+=");
  if (!append && !assignmentContent.startsWith("="))
    return null;
  const valueParts = [];
  const eqLen = append ? 2 : 1;
  const afterEq = assignmentContent.slice(eqLen);
  if (afterEq) {
    valueParts.push({ type: "Literal", value: afterEq });
  }
  for (let i = secondPartIndex + 1; i < word.parts.length; i++) {
    const part = word.parts[i];
    if (part.type === "BraceExpansion") {
      valueParts.push({ type: "Literal", value: braceToLiteral(part) });
    } else {
      valueParts.push(part);
    }
  }
  return { key: key2, valueParts, append };
}
__name$1(parseKeyedElementFromWord, "parseKeyedElementFromWord");
function braceToLiteral(part) {
  const items = part.items.map((item) => {
    if (item.type === "Range") {
      const startS = item.startStr ?? String(item.start);
      const endS = item.endStr ?? String(item.end);
      let range = `${startS}..${endS}`;
      if (item.step)
        range += `..${item.step}`;
      return range;
    }
    return wordToLiteralString(item.word);
  });
  return `{${items.join(",")}}`;
}
__name$1(braceToLiteral, "braceToLiteral");
function wordToLiteralString(word) {
  let result2 = "";
  for (const part of word.parts) {
    switch (part.type) {
      case "Literal":
        result2 += part.value;
        break;
      case "Glob":
        result2 += part.pattern;
        break;
      case "SingleQuoted":
        result2 += part.value;
        break;
      case "DoubleQuoted":
        for (const inner of part.parts) {
          if (inner.type === "Literal") {
            result2 += inner.value;
          } else if (inner.type === "Escaped") {
            result2 += inner.value;
          }
        }
        break;
      case "Escaped":
        result2 += part.value;
        break;
      case "BraceExpansion":
        result2 += "{";
        result2 += part.items.map((item) => item.type === "Range" ? `${item.startStr}..${item.endStr}${item.step ? `..${item.step}` : ""}` : wordToLiteralString(item.word)).join(",");
        result2 += "}";
        break;
      case "TildeExpansion":
        result2 += "~";
        if (part.user) {
          result2 += part.user;
        }
        break;
    }
  }
  return result2;
}
__name$1(wordToLiteralString, "wordToLiteralString");
var DEFAULT_IFS = " 	\n";
function getIfs(env) {
  return env.IFS ?? DEFAULT_IFS;
}
__name$1(getIfs, "getIfs");
function isIfsEmpty(env) {
  return env.IFS === "";
}
__name$1(isIfsEmpty, "isIfsEmpty");
function isIfsWhitespaceOnly(env) {
  const ifs = getIfs(env);
  if (ifs === "")
    return true;
  for (const ch of ifs) {
    if (ch !== " " && ch !== "	" && ch !== "\n") {
      return false;
    }
  }
  return true;
}
__name$1(isIfsWhitespaceOnly, "isIfsWhitespaceOnly");
function buildIfsCharClassPattern(ifs) {
  return ifs.split("").map((c) => {
    if (/[\\^$.*+?()[\]{}|-]/.test(c))
      return `\\${c}`;
    if (c === "	")
      return "\\t";
    if (c === "\n")
      return "\\n";
    return c;
  }).join("");
}
__name$1(buildIfsCharClassPattern, "buildIfsCharClassPattern");
function getIfsSeparator(env) {
  const ifs = env.IFS;
  if (ifs === void 0)
    return " ";
  return ifs[0] || "";
}
__name$1(getIfsSeparator, "getIfsSeparator");
var IFS_WHITESPACE = " 	\n";
function isIfsWhitespace(ch) {
  return IFS_WHITESPACE.includes(ch);
}
__name$1(isIfsWhitespace, "isIfsWhitespace");
function categorizeIfs(ifs) {
  const whitespace = /* @__PURE__ */ new Set();
  const nonWhitespace = /* @__PURE__ */ new Set();
  for (const ch of ifs) {
    if (isIfsWhitespace(ch)) {
      whitespace.add(ch);
    } else {
      nonWhitespace.add(ch);
    }
  }
  return { whitespace, nonWhitespace };
}
__name$1(categorizeIfs, "categorizeIfs");
function splitByIfsForRead(value, ifs, maxSplit, raw) {
  if (ifs === "") {
    if (value === "") {
      return { words: [], wordStarts: [] };
    }
    return { words: [value], wordStarts: [0] };
  }
  const { whitespace, nonWhitespace } = categorizeIfs(ifs);
  const words = [];
  const wordStarts = [];
  let pos = 0;
  while (pos < value.length && whitespace.has(value[pos])) {
    pos++;
  }
  if (pos >= value.length) {
    return { words: [], wordStarts: [] };
  }
  if (nonWhitespace.has(value[pos])) {
    words.push("");
    wordStarts.push(pos);
    pos++;
    while (pos < value.length && whitespace.has(value[pos])) {
      pos++;
    }
  }
  while (pos < value.length) {
    if (maxSplit !== void 0 && words.length >= maxSplit) {
      break;
    }
    const wordStart = pos;
    wordStarts.push(wordStart);
    while (pos < value.length) {
      const ch = value[pos];
      if (!raw && ch === "\\") {
        pos++;
        if (pos < value.length) {
          pos++;
        }
        continue;
      }
      if (whitespace.has(ch) || nonWhitespace.has(ch)) {
        break;
      }
      pos++;
    }
    words.push(value.substring(wordStart, pos));
    if (pos >= value.length) {
      break;
    }
    while (pos < value.length && whitespace.has(value[pos])) {
      pos++;
    }
    if (pos < value.length && nonWhitespace.has(value[pos])) {
      pos++;
      while (pos < value.length && whitespace.has(value[pos])) {
        pos++;
      }
      while (pos < value.length && nonWhitespace.has(value[pos])) {
        if (maxSplit !== void 0 && words.length >= maxSplit) {
          break;
        }
        words.push("");
        wordStarts.push(pos);
        pos++;
        while (pos < value.length && whitespace.has(value[pos])) {
          pos++;
        }
      }
    }
  }
  return { words, wordStarts };
}
__name$1(splitByIfsForRead, "splitByIfsForRead");
function splitByIfsForExpansionEx(value, ifs) {
  if (ifs === "") {
    return {
      words: value ? [value] : [],
      hadLeadingDelimiter: false,
      hadTrailingDelimiter: false
    };
  }
  if (value === "") {
    return {
      words: [],
      hadLeadingDelimiter: false,
      hadTrailingDelimiter: false
    };
  }
  const { whitespace, nonWhitespace } = categorizeIfs(ifs);
  const words = [];
  let pos = 0;
  let hadLeadingDelimiter = false;
  let hadTrailingDelimiter = false;
  const leadingStart = pos;
  while (pos < value.length && whitespace.has(value[pos])) {
    pos++;
  }
  if (pos > leadingStart) {
    hadLeadingDelimiter = true;
  }
  if (pos >= value.length) {
    return { words: [], hadLeadingDelimiter: true, hadTrailingDelimiter: true };
  }
  if (nonWhitespace.has(value[pos])) {
    words.push("");
    pos++;
    while (pos < value.length && whitespace.has(value[pos])) {
      pos++;
    }
  }
  while (pos < value.length) {
    const wordStart = pos;
    while (pos < value.length) {
      const ch = value[pos];
      if (whitespace.has(ch) || nonWhitespace.has(ch)) {
        break;
      }
      pos++;
    }
    words.push(value.substring(wordStart, pos));
    if (pos >= value.length) {
      hadTrailingDelimiter = false;
      break;
    }
    const beforeDelimiterPos = pos;
    while (pos < value.length && whitespace.has(value[pos])) {
      pos++;
    }
    if (pos < value.length && nonWhitespace.has(value[pos])) {
      pos++;
      while (pos < value.length && whitespace.has(value[pos])) {
        pos++;
      }
      while (pos < value.length && nonWhitespace.has(value[pos])) {
        words.push("");
        pos++;
        while (pos < value.length && whitespace.has(value[pos])) {
          pos++;
        }
      }
    }
    if (pos >= value.length && pos > beforeDelimiterPos) {
      hadTrailingDelimiter = true;
    }
  }
  return { words, hadLeadingDelimiter, hadTrailingDelimiter };
}
__name$1(splitByIfsForExpansionEx, "splitByIfsForExpansionEx");
function splitByIfsForExpansion(value, ifs) {
  return splitByIfsForExpansionEx(value, ifs).words;
}
__name$1(splitByIfsForExpansion, "splitByIfsForExpansion");
function containsNonWsIfs(value, nonWhitespace) {
  for (const ch of value) {
    if (nonWhitespace.has(ch)) {
      return true;
    }
  }
  return false;
}
__name$1(containsNonWsIfs, "containsNonWsIfs");
function stripTrailingIfsWhitespace(value, ifs, raw) {
  if (ifs === "")
    return value;
  const { whitespace, nonWhitespace } = categorizeIfs(ifs);
  let end = value.length;
  while (end > 0) {
    if (!whitespace.has(value[end - 1])) {
      break;
    }
    if (!raw && end >= 2) {
      let backslashCount = 0;
      let pos = end - 2;
      while (pos >= 0 && value[pos] === "\\") {
        backslashCount++;
        pos--;
      }
      if (backslashCount % 2 === 1) {
        break;
      }
    }
    end--;
  }
  const result2 = value.substring(0, end);
  if (result2.length >= 1 && nonWhitespace.has(result2[result2.length - 1])) {
    if (!raw && result2.length >= 2) {
      let backslashCount = 0;
      let pos = result2.length - 2;
      while (pos >= 0 && result2[pos] === "\\") {
        backslashCount++;
        pos--;
      }
      if (backslashCount % 2 === 1) {
        return result2;
      }
    }
    const contentWithoutTrailing = result2.substring(0, result2.length - 1);
    if (!containsNonWsIfs(contentWithoutTrailing, nonWhitespace)) {
      return contentWithoutTrailing;
    }
  }
  return result2;
}
__name$1(stripTrailingIfsWhitespace, "stripTrailingIfsWhitespace");
function isNameref(ctx, name) {
  return ctx.state.namerefs?.has(name) ?? false;
}
__name$1(isNameref, "isNameref");
function markNameref(ctx, name) {
  ctx.state.namerefs ??= /* @__PURE__ */ new Set();
  ctx.state.namerefs.add(name);
}
__name$1(markNameref, "markNameref");
function unmarkNameref(ctx, name) {
  ctx.state.namerefs?.delete(name);
  ctx.state.boundNamerefs?.delete(name);
  ctx.state.invalidNamerefs?.delete(name);
}
__name$1(unmarkNameref, "unmarkNameref");
function markNamerefInvalid(ctx, name) {
  ctx.state.invalidNamerefs ??= /* @__PURE__ */ new Set();
  ctx.state.invalidNamerefs.add(name);
}
__name$1(markNamerefInvalid, "markNamerefInvalid");
function isNamerefInvalid(ctx, name) {
  return ctx.state.invalidNamerefs?.has(name) ?? false;
}
__name$1(isNamerefInvalid, "isNamerefInvalid");
function markNamerefBound(ctx, name) {
  ctx.state.boundNamerefs ??= /* @__PURE__ */ new Set();
  ctx.state.boundNamerefs.add(name);
}
__name$1(markNamerefBound, "markNamerefBound");
function targetExists(ctx, target) {
  const arrayMatch = target.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
  if (arrayMatch) {
    const arrayName = arrayMatch[1];
    const hasElements = Object.keys(ctx.state.env).some((k) => k.startsWith(`${arrayName}_`) && !k.includes("__"));
    const isAssoc = ctx.state.associativeArrays?.has(arrayName) ?? false;
    return hasElements || isAssoc;
  }
  const hasArrayElements = Object.keys(ctx.state.env).some((k) => k.startsWith(`${target}_`) && !k.includes("__"));
  if (hasArrayElements) {
    return true;
  }
  return ctx.state.env[target] !== void 0;
}
__name$1(targetExists, "targetExists");
function resolveNameref(ctx, name, maxDepth = 100) {
  if (!isNameref(ctx, name)) {
    return name;
  }
  if (isNamerefInvalid(ctx, name)) {
    return name;
  }
  const seen = /* @__PURE__ */ new Set();
  let current = name;
  while (maxDepth-- > 0) {
    if (seen.has(current)) {
      return void 0;
    }
    seen.add(current);
    if (!isNameref(ctx, current)) {
      return current;
    }
    const target = ctx.state.env[current];
    if (target === void 0 || target === "") {
      return current;
    }
    if (!/^[a-zA-Z_][a-zA-Z0-9_]*(\[.+\])?$/.test(target)) {
      return current;
    }
    current = target;
  }
  return void 0;
}
__name$1(resolveNameref, "resolveNameref");
function getNamerefTarget(ctx, name) {
  if (!isNameref(ctx, name)) {
    return void 0;
  }
  return ctx.state.env[name];
}
__name$1(getNamerefTarget, "getNamerefTarget");
function resolveNamerefForAssignment(ctx, name, valueBeingAssigned, maxDepth = 100) {
  if (!isNameref(ctx, name)) {
    return name;
  }
  if (isNamerefInvalid(ctx, name)) {
    return name;
  }
  const seen = /* @__PURE__ */ new Set();
  let current = name;
  while (maxDepth-- > 0) {
    if (seen.has(current)) {
      return void 0;
    }
    seen.add(current);
    if (!isNameref(ctx, current)) {
      return current;
    }
    const target = ctx.state.env[current];
    if (target === void 0 || target === "") {
      if (valueBeingAssigned !== void 0) {
        const isValidName2 = /^[a-zA-Z_][a-zA-Z0-9_]*$/.test(valueBeingAssigned);
        if (isValidName2 && targetExists(ctx, valueBeingAssigned)) {
          return current;
        }
        return null;
      }
      return current;
    }
    if (!/^[a-zA-Z_][a-zA-Z0-9_]*(\[.+\])?$/.test(target)) {
      return current;
    }
    current = target;
  }
  return void 0;
}
__name$1(resolveNamerefForAssignment, "resolveNamerefForAssignment");
function expandSimpleVarsInSubscript(ctx, subscript) {
  let result2 = subscript.replace(/\$\{([a-zA-Z_][a-zA-Z0-9_]*)\}/g, (_, name) => ctx.state.env[name] ?? "");
  result2 = result2.replace(/\$([a-zA-Z_][a-zA-Z0-9_]*)/g, (_, name) => ctx.state.env[name] ?? "");
  return result2;
}
__name$1(expandSimpleVarsInSubscript, "expandSimpleVarsInSubscript");
function getArrayElements(ctx, arrayName) {
  if (arrayName === "FUNCNAME") {
    const stack = ctx.state.funcNameStack ?? [];
    return stack.map((name, i) => [i, name]);
  }
  if (arrayName === "BASH_LINENO") {
    const stack = ctx.state.callLineStack ?? [];
    return stack.map((line, i) => [i, String(line)]);
  }
  if (arrayName === "BASH_SOURCE") {
    const stack = ctx.state.sourceStack ?? [];
    return stack.map((source, i) => [i, source]);
  }
  const isAssoc = ctx.state.associativeArrays?.has(arrayName);
  if (isAssoc) {
    const keys = getAssocArrayKeys(ctx, arrayName);
    return keys.map((key2) => [key2, ctx.state.env[`${arrayName}_${key2}`]]);
  }
  const indices = getArrayIndices(ctx, arrayName);
  return indices.map((index) => [
    index,
    ctx.state.env[`${arrayName}_${index}`]
  ]);
}
__name$1(getArrayElements, "getArrayElements");
function isArray(ctx, name) {
  if (name === "FUNCNAME") {
    return (ctx.state.funcNameStack?.length ?? 0) > 0;
  }
  if (name === "BASH_LINENO") {
    return (ctx.state.callLineStack?.length ?? 0) > 0;
  }
  if (name === "BASH_SOURCE") {
    return (ctx.state.sourceStack?.length ?? 0) > 0;
  }
  if (ctx.state.associativeArrays?.has(name)) {
    return getAssocArrayKeys(ctx, name).length > 0;
  }
  return getArrayIndices(ctx, name).length > 0;
}
__name$1(isArray, "isArray");
async function getVariable(ctx, name, checkNounset = true, _insideDoubleQuotes = false) {
  switch (name) {
    case "?":
      return String(ctx.state.lastExitCode);
    case "$":
      return String(process.pid);
    case "#":
      return ctx.state.env["#"] || "0";
    case "@":
      return ctx.state.env["@"] || "";
    case "_":
      return ctx.state.lastArg;
    case "-": {
      let flags = "";
      flags += "h";
      if (ctx.state.options.errexit)
        flags += "e";
      if (ctx.state.options.noglob)
        flags += "f";
      if (ctx.state.options.nounset)
        flags += "u";
      if (ctx.state.options.verbose)
        flags += "v";
      if (ctx.state.options.xtrace)
        flags += "x";
      flags += "B";
      if (ctx.state.options.noclobber)
        flags += "C";
      flags += "s";
      return flags;
    }
    case "*": {
      const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
      if (numParams === 0)
        return "";
      const params = [];
      for (let i = 1; i <= numParams; i++) {
        params.push(ctx.state.env[String(i)] || "");
      }
      return params.join(getIfsSeparator(ctx.state.env));
    }
    case "0":
      return ctx.state.env["0"] || "bash";
    case "PWD":
      if (ctx.state.env.PWD !== void 0) {
        return ctx.state.env.PWD;
      }
      return "";
    case "OLDPWD":
      if (ctx.state.env.OLDPWD !== void 0) {
        return ctx.state.env.OLDPWD;
      }
      return "";
    case "PPID": {
      const { ppid } = getProcessInfo();
      return String(ppid);
    }
    case "UID": {
      const { uid } = getProcessInfo();
      return String(uid);
    }
    case "EUID":
      return String(process.geteuid?.() ?? getProcessInfo().uid);
    case "RANDOM":
      return String(Math.floor(Math.random() * 32768));
    case "SECONDS":
      return String(Math.floor((Date.now() - ctx.state.startTime) / 1e3));
    case "BASH_VERSION":
      return BASH_VERSION;
    case "!":
      return String(ctx.state.lastBackgroundPid);
    case "BASHPID":
      return String(ctx.state.bashPid);
    case "LINENO":
      return String(ctx.state.currentLine);
    case "FUNCNAME": {
      const funcName = ctx.state.funcNameStack?.[0];
      if (funcName !== void 0) {
        return funcName;
      }
      if (checkNounset && ctx.state.options.nounset) {
        throw new NounsetError("FUNCNAME");
      }
      return "";
    }
    case "BASH_LINENO": {
      const line = ctx.state.callLineStack?.[0];
      if (line !== void 0) {
        return String(line);
      }
      if (checkNounset && ctx.state.options.nounset) {
        throw new NounsetError("BASH_LINENO");
      }
      return "";
    }
    case "BASH_SOURCE": {
      const source = ctx.state.sourceStack?.[0];
      if (source !== void 0) {
        return source;
      }
      if (checkNounset && ctx.state.options.nounset) {
        throw new NounsetError("BASH_SOURCE");
      }
      return "";
    }
  }
  if (/^[a-zA-Z_][a-zA-Z0-9_]*\[\]$/.test(name)) {
    throw new BadSubstitutionError(`\${${name}}`);
  }
  const bracketMatch = name.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
  if (bracketMatch) {
    let arrayName = bracketMatch[1];
    const subscript = bracketMatch[2];
    if (isNameref(ctx, arrayName)) {
      const resolved = resolveNameref(ctx, arrayName);
      if (resolved && resolved !== arrayName) {
        const resolvedBracket = resolved.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
        if (resolvedBracket) {
          return "";
        }
        arrayName = resolved;
      }
    }
    if (subscript === "@" || subscript === "*") {
      const elements = getArrayElements(ctx, arrayName);
      if (elements.length > 0) {
        return elements.map(([, v]) => v).join(" ");
      }
      const scalarValue = ctx.state.env[arrayName];
      if (scalarValue !== void 0) {
        return scalarValue;
      }
      return "";
    }
    if (arrayName === "FUNCNAME") {
      const index2 = Number.parseInt(subscript, 10);
      if (!Number.isNaN(index2) && index2 >= 0) {
        return ctx.state.funcNameStack?.[index2] ?? "";
      }
      return "";
    }
    if (arrayName === "BASH_LINENO") {
      const index2 = Number.parseInt(subscript, 10);
      if (!Number.isNaN(index2) && index2 >= 0) {
        const line = ctx.state.callLineStack?.[index2];
        return line !== void 0 ? String(line) : "";
      }
      return "";
    }
    if (arrayName === "BASH_SOURCE") {
      const index2 = Number.parseInt(subscript, 10);
      if (!Number.isNaN(index2) && index2 >= 0) {
        return ctx.state.sourceStack?.[index2] ?? "";
      }
      return "";
    }
    const isAssoc = ctx.state.associativeArrays?.has(arrayName);
    if (isAssoc) {
      let key2 = unquoteKey(subscript);
      key2 = expandSimpleVarsInSubscript(ctx, key2);
      const value3 = ctx.state.env[`${arrayName}_${key2}`];
      if (value3 === void 0 && checkNounset && ctx.state.options.nounset) {
        throw new NounsetError(`${arrayName}[${subscript}]`);
      }
      return value3 || "";
    }
    let index;
    if (/^-?\d+$/.test(subscript)) {
      index = Number.parseInt(subscript, 10);
    } else {
      try {
        const parser = new Parser();
        const arithAst = parseArithmeticExpression(parser, subscript);
        index = await evaluateArithmetic(ctx, arithAst.expression);
      } catch {
        const evalValue = ctx.state.env[subscript];
        index = evalValue ? Number.parseInt(evalValue, 10) : 0;
        if (Number.isNaN(index))
          index = 0;
      }
    }
    if (index < 0) {
      const elements = getArrayElements(ctx, arrayName);
      const lineNum = ctx.state.currentLine;
      if (elements.length === 0) {
        ctx.state.expansionStderr = (ctx.state.expansionStderr || "") + `bash: line ${lineNum}: ${arrayName}: bad array subscript
`;
        return "";
      }
      const maxIndex = Math.max(...elements.map(([idx]) => typeof idx === "number" ? idx : 0));
      const actualIdx = maxIndex + 1 + index;
      if (actualIdx < 0) {
        ctx.state.expansionStderr = (ctx.state.expansionStderr || "") + `bash: line ${lineNum}: ${arrayName}: bad array subscript
`;
        return "";
      }
      const value3 = ctx.state.env[`${arrayName}_${actualIdx}`];
      return value3 || "";
    }
    const value2 = ctx.state.env[`${arrayName}_${index}`];
    if (value2 !== void 0) {
      return value2;
    }
    if (index === 0) {
      const scalarValue = ctx.state.env[arrayName];
      if (scalarValue !== void 0) {
        return scalarValue;
      }
    }
    if (checkNounset && ctx.state.options.nounset) {
      throw new NounsetError(`${arrayName}[${index}]`);
    }
    return "";
  }
  if (/^[1-9][0-9]*$/.test(name)) {
    const value2 = ctx.state.env[name];
    if (value2 === void 0 && checkNounset && ctx.state.options.nounset) {
      throw new NounsetError(name);
    }
    return value2 || "";
  }
  if (isNameref(ctx, name)) {
    const resolved = resolveNameref(ctx, name);
    if (resolved === void 0) {
      return "";
    }
    if (resolved !== name) {
      return await getVariable(ctx, resolved, checkNounset, _insideDoubleQuotes);
    }
    const value2 = ctx.state.env[name];
    if ((value2 === void 0 || value2 === "") && checkNounset && ctx.state.options.nounset) {
      throw new NounsetError(name);
    }
    return value2 || "";
  }
  const value = ctx.state.env[name];
  if (value !== void 0) {
    if (ctx.state.tempEnvBindings?.some((b) => b.has(name))) {
      ctx.state.accessedTempEnvVars = ctx.state.accessedTempEnvVars || /* @__PURE__ */ new Set();
      ctx.state.accessedTempEnvVars.add(name);
    }
    return value;
  }
  if (isArray(ctx, name)) {
    const firstValue = ctx.state.env[`${name}_0`];
    if (firstValue !== void 0) {
      return firstValue;
    }
    return "";
  }
  if (checkNounset && ctx.state.options.nounset) {
    throw new NounsetError(name);
  }
  return "";
}
__name$1(getVariable, "getVariable");
async function isVariableSet(ctx, name) {
  const alwaysSetSpecialVars = /* @__PURE__ */ new Set([
    "?",
    "$",
    "#",
    "_",
    "-",
    "0",
    "PPID",
    "UID",
    "EUID",
    "RANDOM",
    "SECONDS",
    "BASH_VERSION",
    "!",
    "BASHPID",
    "LINENO"
  ]);
  if (alwaysSetSpecialVars.has(name)) {
    return true;
  }
  if (name === "@" || name === "*") {
    const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
    return numParams > 0;
  }
  if (name === "PWD" || name === "OLDPWD") {
    return name in ctx.state.env;
  }
  const bracketMatch = name.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
  if (bracketMatch) {
    let arrayName = bracketMatch[1];
    const subscript = bracketMatch[2];
    if (isNameref(ctx, arrayName)) {
      const resolved = resolveNameref(ctx, arrayName);
      if (resolved && resolved !== arrayName) {
        const resolvedBracket = resolved.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
        if (resolvedBracket) {
          return false;
        }
        arrayName = resolved;
      }
    }
    if (subscript === "@" || subscript === "*") {
      const elements = getArrayElements(ctx, arrayName);
      if (elements.length > 0)
        return true;
      return arrayName in ctx.state.env;
    }
    const isAssoc = ctx.state.associativeArrays?.has(arrayName);
    if (isAssoc) {
      const key2 = unquoteKey(subscript);
      return `${arrayName}_${key2}` in ctx.state.env;
    }
    let index;
    if (/^-?\d+$/.test(subscript)) {
      index = Number.parseInt(subscript, 10);
    } else {
      try {
        const parser = new Parser();
        const arithAst = parseArithmeticExpression(parser, subscript);
        index = await evaluateArithmetic(ctx, arithAst.expression);
      } catch {
        const evalValue = ctx.state.env[subscript];
        index = evalValue ? Number.parseInt(evalValue, 10) : 0;
        if (Number.isNaN(index))
          index = 0;
      }
    }
    if (index < 0) {
      const elements = getArrayElements(ctx, arrayName);
      if (elements.length === 0)
        return false;
      const maxIndex = Math.max(...elements.map(([idx]) => typeof idx === "number" ? idx : 0));
      const actualIdx = maxIndex + 1 + index;
      if (actualIdx < 0)
        return false;
      return `${arrayName}_${actualIdx}` in ctx.state.env;
    }
    return `${arrayName}_${index}` in ctx.state.env;
  }
  if (isNameref(ctx, name)) {
    const resolved = resolveNameref(ctx, name);
    if (resolved === void 0 || resolved === name) {
      return name in ctx.state.env;
    }
    return isVariableSet(ctx, resolved);
  }
  if (name in ctx.state.env) {
    return true;
  }
  if (isArray(ctx, name)) {
    return true;
  }
  return false;
}
__name$1(isVariableSet, "isVariableSet");
async function expandDollarVarsInArithText(ctx, text) {
  let result2 = "";
  let i = 0;
  while (i < text.length) {
    if (text[i] === "$") {
      if (text[i + 1] === "{") {
        let depth = 1;
        let j = i + 2;
        while (j < text.length && depth > 0) {
          if (text[j] === "{")
            depth++;
          else if (text[j] === "}")
            depth--;
          j++;
        }
        result2 += text.slice(i, j);
        i = j;
        continue;
      }
      if (text[i + 1] === "(") {
        let depth = 1;
        let j = i + 2;
        while (j < text.length && depth > 0) {
          if (text[j] === "(")
            depth++;
          else if (text[j] === ")")
            depth--;
          j++;
        }
        result2 += text.slice(i, j);
        i = j;
        continue;
      }
      if (/[a-zA-Z_]/.test(text[i + 1] || "")) {
        let j = i + 1;
        while (j < text.length && /[a-zA-Z0-9_]/.test(text[j])) {
          j++;
        }
        const varName = text.slice(i + 1, j);
        const value = await getVariable(ctx, varName);
        result2 += value;
        i = j;
        continue;
      }
      if (/[0-9]/.test(text[i + 1] || "")) {
        let j = i + 1;
        while (j < text.length && /[0-9]/.test(text[j])) {
          j++;
        }
        const varName = text.slice(i + 1, j);
        const value = await getVariable(ctx, varName);
        result2 += value;
        i = j;
        continue;
      }
      if (/[*@#?\-!$]/.test(text[i + 1] || "")) {
        const varName = text[i + 1];
        const value = await getVariable(ctx, varName);
        result2 += value;
        i += 2;
        continue;
      }
    }
    if (text[i] === '"') {
      result2 += '"';
      i++;
      while (i < text.length && text[i] !== '"') {
        if (text[i] === "$" && /[a-zA-Z_]/.test(text[i + 1] || "")) {
          let j = i + 1;
          while (j < text.length && /[a-zA-Z0-9_]/.test(text[j])) {
            j++;
          }
          const varName = text.slice(i + 1, j);
          const value = await getVariable(ctx, varName);
          result2 += value;
          i = j;
        } else if (text[i] === "\\") {
          result2 += text[i];
          i++;
          if (i < text.length) {
            result2 += text[i];
            i++;
          }
        } else {
          result2 += text[i];
          i++;
        }
      }
      if (i < text.length) {
        result2 += '"';
        i++;
      }
      continue;
    }
    result2 += text[i];
    i++;
  }
  return result2;
}
__name$1(expandDollarVarsInArithText, "expandDollarVarsInArithText");
async function expandSubscriptForAssocArray(ctx, subscript) {
  let inner = subscript;
  const hasDoubleQuotes = subscript.startsWith('"') && subscript.endsWith('"');
  const hasSingleQuotes = subscript.startsWith("'") && subscript.endsWith("'");
  if (hasDoubleQuotes || hasSingleQuotes) {
    inner = subscript.slice(1, -1);
  }
  if (hasSingleQuotes) {
    return inner;
  }
  let result2 = "";
  let i = 0;
  while (i < inner.length) {
    if (inner[i] === "$") {
      if (inner[i + 1] === "(") {
        let depth = 1;
        let j = i + 2;
        while (j < inner.length && depth > 0) {
          if (inner[j] === "(" && inner[j - 1] === "$") {
            depth++;
          } else if (inner[j] === "(") {
            depth++;
          } else if (inner[j] === ")") {
            depth--;
          }
          j++;
        }
        const cmdStr = inner.slice(i + 2, j - 1);
        if (ctx.execFn) {
          const cmdResult = await ctx.execFn(cmdStr);
          result2 += cmdResult.stdout.replace(/\n+$/, "");
          if (cmdResult.stderr) {
            ctx.state.expansionStderr = (ctx.state.expansionStderr || "") + cmdResult.stderr;
          }
        }
        i = j;
      } else if (inner[i + 1] === "{") {
        let depth = 1;
        let j = i + 2;
        while (j < inner.length && depth > 0) {
          if (inner[j] === "{")
            depth++;
          else if (inner[j] === "}")
            depth--;
          j++;
        }
        const varExpr = inner.slice(i + 2, j - 1);
        const value = await getVariable(ctx, varExpr);
        result2 += value;
        i = j;
      } else if (/[a-zA-Z_]/.test(inner[i + 1] || "")) {
        let j = i + 1;
        while (j < inner.length && /[a-zA-Z0-9_]/.test(inner[j])) {
          j++;
        }
        const varName = inner.slice(i + 1, j);
        const value = await getVariable(ctx, varName);
        result2 += value;
        i = j;
      } else {
        result2 += inner[i];
        i++;
      }
    } else if (inner[i] === "`") {
      let j = i + 1;
      while (j < inner.length && inner[j] !== "`") {
        j++;
      }
      const cmdStr = inner.slice(i + 1, j);
      if (ctx.execFn) {
        const cmdResult = await ctx.execFn(cmdStr);
        result2 += cmdResult.stdout.replace(/\n+$/, "");
        if (cmdResult.stderr) {
          ctx.state.expansionStderr = (ctx.state.expansionStderr || "") + cmdResult.stderr;
        }
      }
      i = j + 1;
    } else {
      result2 += inner[i];
      i++;
    }
  }
  return result2;
}
__name$1(expandSubscriptForAssocArray, "expandSubscriptForAssocArray");
var MAX_SAFE_RANGE_ITERATIONS = 1e4;
function safeExpandNumericRange(start2, end, rawStep, startStr, endStr) {
  let step = rawStep ?? 1;
  if (step === 0)
    step = 1;
  const absStep = Math.abs(step);
  const results = [];
  let padWidth = 0;
  if (startStr?.match(/^-?0\d/)) {
    padWidth = Math.max(padWidth, startStr.replace(/^-/, "").length);
  }
  if (endStr?.match(/^-?0\d/)) {
    padWidth = Math.max(padWidth, endStr.replace(/^-/, "").length);
  }
  const formatNum = /* @__PURE__ */ __name$1((n) => {
    if (padWidth > 0) {
      const neg = n < 0;
      const absStr = String(Math.abs(n)).padStart(padWidth, "0");
      return neg ? `-${absStr}` : absStr;
    }
    return String(n);
  }, "formatNum");
  if (start2 <= end) {
    for (let i = start2, count = 0; i <= end && count < MAX_SAFE_RANGE_ITERATIONS; i += absStep, count++) {
      results.push(formatNum(i));
    }
  } else {
    for (let i = start2, count = 0; i >= end && count < MAX_SAFE_RANGE_ITERATIONS; i -= absStep, count++) {
      results.push(formatNum(i));
    }
  }
  return results;
}
__name$1(safeExpandNumericRange, "safeExpandNumericRange");
function safeExpandCharRange(start2, end, rawStep) {
  let step = rawStep ?? 1;
  if (step === 0)
    step = 1;
  const startCode = start2.charCodeAt(0);
  const endCode = end.charCodeAt(0);
  const absStep = Math.abs(step);
  const startIsUpper = start2 >= "A" && start2 <= "Z";
  const startIsLower = start2 >= "a" && start2 <= "z";
  const endIsUpper = end >= "A" && end <= "Z";
  const endIsLower = end >= "a" && end <= "z";
  if (startIsUpper && endIsLower || startIsLower && endIsUpper) {
    const stepPart = rawStep !== void 0 ? `..${rawStep}` : "";
    throw new BraceExpansionError(`{${start2}..${end}${stepPart}}: invalid sequence`);
  }
  const results = [];
  if (startCode <= endCode) {
    for (let i = startCode, count = 0; i <= endCode && count < MAX_SAFE_RANGE_ITERATIONS; i += absStep, count++) {
      results.push(String.fromCharCode(i));
    }
  } else {
    for (let i = startCode, count = 0; i >= endCode && count < MAX_SAFE_RANGE_ITERATIONS; i -= absStep, count++) {
      results.push(String.fromCharCode(i));
    }
  }
  return results;
}
__name$1(safeExpandCharRange, "safeExpandCharRange");
function expandBraceRange(start2, end, step, startStr, endStr) {
  const stepPart = step !== void 0 ? `..${step}` : "";
  if (typeof start2 === "number" && typeof end === "number") {
    const expanded = safeExpandNumericRange(start2, end, step, startStr, endStr);
    return {
      expanded,
      literal: `{${start2}..${end}${stepPart}}`
    };
  }
  if (typeof start2 === "string" && typeof end === "string") {
    const expanded = safeExpandCharRange(start2, end, step);
    return {
      expanded,
      literal: `{${start2}..${end}${stepPart}}`
    };
  }
  return {
    expanded: null,
    literal: `{${start2}..${end}${stepPart}}`
  };
}
__name$1(expandBraceRange, "expandBraceRange");
function getFileReadShorthand(body) {
  if (body.statements.length !== 1)
    return null;
  const statement = body.statements[0];
  if (statement.operators.length !== 0)
    return null;
  if (statement.pipelines.length !== 1)
    return null;
  const pipeline = statement.pipelines[0];
  if (pipeline.negated)
    return null;
  if (pipeline.commands.length !== 1)
    return null;
  const cmd = pipeline.commands[0];
  if (cmd.type !== "SimpleCommand")
    return null;
  const simpleCmd = cmd;
  if (simpleCmd.name !== null)
    return null;
  if (simpleCmd.args.length !== 0)
    return null;
  if (simpleCmd.assignments.length !== 0)
    return null;
  if (simpleCmd.redirections.length !== 1)
    return null;
  const redirect = simpleCmd.redirections[0];
  if (redirect.operator !== "<")
    return null;
  if (redirect.target.type !== "Word")
    return null;
  return { target: redirect.target };
}
__name$1(getFileReadShorthand, "getFileReadShorthand");
function hasGlobPattern(value, extglob) {
  if (/[*?[]/.test(value)) {
    return true;
  }
  if (extglob && /[@*+?!]\(/.test(value)) {
    return true;
  }
  return false;
}
__name$1(hasGlobPattern, "hasGlobPattern");
function unescapeGlobPattern(pattern) {
  let result2 = "";
  let i = 0;
  while (i < pattern.length) {
    if (pattern[i] === "\\" && i + 1 < pattern.length) {
      result2 += pattern[i + 1];
      i += 2;
    } else {
      result2 += pattern[i];
      i++;
    }
  }
  return result2;
}
__name$1(unescapeGlobPattern, "unescapeGlobPattern");
function escapeGlobChars(str) {
  return str.replace(/([*?[\]\\()|])/g, "\\$1");
}
__name$1(escapeGlobChars, "escapeGlobChars");
function escapeRegexChars(str) {
  return str.replace(/[\\^$.*+?()[\]{}|]/g, "\\$&");
}
__name$1(escapeRegexChars, "escapeRegexChars");
function escapeRegex(str) {
  return str.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
__name$1(escapeRegex, "escapeRegex");
function patternToRegex(pattern, greedy, extglob = false) {
  let regex = "";
  let i = 0;
  while (i < pattern.length) {
    const char = pattern[i];
    if (extglob && (char === "@" || char === "*" || char === "+" || char === "?" || char === "!") && i + 1 < pattern.length && pattern[i + 1] === "(") {
      const closeIdx = findMatchingParen2(pattern, i + 1);
      if (closeIdx !== -1) {
        const content = pattern.slice(i + 2, closeIdx);
        const alternatives = splitExtglobAlternatives2(content);
        const altRegexes = alternatives.map((alt) => patternToRegex(alt, greedy, extglob));
        const altGroup = altRegexes.length > 0 ? altRegexes.join("|") : "(?:)";
        if (char === "@") {
          regex += `(?:${altGroup})`;
        } else if (char === "*") {
          regex += `(?:${altGroup})*`;
        } else if (char === "+") {
          regex += `(?:${altGroup})+`;
        } else if (char === "?") {
          regex += `(?:${altGroup})?`;
        } else if (char === "!") {
          regex += `(?!(?:${altGroup})$).*`;
        }
        i = closeIdx + 1;
        continue;
      }
    }
    if (char === "\\") {
      if (i + 1 < pattern.length) {
        const next = pattern[i + 1];
        if (/[\\^$.|+(){}[\]*?]/.test(next)) {
          regex += `\\${next}`;
        } else {
          regex += next;
        }
        i += 2;
      } else {
        regex += "\\\\";
        i++;
      }
    } else if (char === "*") {
      regex += greedy ? ".*" : ".*?";
      i++;
    } else if (char === "?") {
      regex += ".";
      i++;
    } else if (char === "[") {
      const classEnd = findCharClassEnd(pattern, i);
      if (classEnd === -1) {
        regex += "\\[";
        i++;
      } else {
        const classContent = pattern.slice(i + 1, classEnd);
        regex += convertCharClass(classContent);
        i = classEnd + 1;
      }
    } else if (/[\^$.|+(){}]/.test(char)) {
      regex += `\\${char}`;
      i++;
    } else {
      regex += char;
      i++;
    }
  }
  return regex;
}
__name$1(patternToRegex, "patternToRegex");
function findMatchingParen2(pattern, openIdx) {
  let depth = 1;
  let i = openIdx + 1;
  while (i < pattern.length && depth > 0) {
    const c = pattern[i];
    if (c === "\\") {
      i += 2;
      continue;
    }
    if (c === "(") {
      depth++;
    } else if (c === ")") {
      depth--;
      if (depth === 0) {
        return i;
      }
    }
    i++;
  }
  return -1;
}
__name$1(findMatchingParen2, "findMatchingParen");
function splitExtglobAlternatives2(content) {
  const alternatives = [];
  let current = "";
  let depth = 0;
  let i = 0;
  while (i < content.length) {
    const c = content[i];
    if (c === "\\") {
      current += c;
      if (i + 1 < content.length) {
        current += content[i + 1];
        i += 2;
      } else {
        i++;
      }
      continue;
    }
    if (c === "(") {
      depth++;
      current += c;
    } else if (c === ")") {
      depth--;
      current += c;
    } else if (c === "|" && depth === 0) {
      alternatives.push(current);
      current = "";
    } else {
      current += c;
    }
    i++;
  }
  alternatives.push(current);
  return alternatives;
}
__name$1(splitExtglobAlternatives2, "splitExtglobAlternatives");
function findCharClassEnd(pattern, start2) {
  let i = start2 + 1;
  if (i < pattern.length && pattern[i] === "^") {
    i++;
  }
  if (i < pattern.length && pattern[i] === "]") {
    i++;
  }
  while (i < pattern.length) {
    if (pattern[i] === "\\" && i + 1 < pattern.length) {
      i += 2;
      continue;
    }
    if (pattern[i] === "]") {
      return i;
    }
    if (pattern[i] === "'") {
      const closeQuote = pattern.indexOf("'", i + 1);
      if (closeQuote !== -1) {
        i = closeQuote + 1;
        continue;
      }
    }
    if (pattern[i] === "[" && i + 1 < pattern.length && pattern[i + 1] === ":") {
      const closePos = pattern.indexOf(":]", i + 2);
      if (closePos !== -1) {
        i = closePos + 2;
        continue;
      }
    }
    i++;
  }
  return -1;
}
__name$1(findCharClassEnd, "findCharClassEnd");
function convertCharClass(content) {
  let result2 = "[";
  let i = 0;
  if (content[0] === "^" || content[0] === "!") {
    result2 += "^";
    i++;
  }
  while (i < content.length) {
    if (content[i] === "'") {
      const closeQuote = content.indexOf("'", i + 1);
      if (closeQuote !== -1) {
        const quoted = content.slice(i + 1, closeQuote);
        for (const ch of quoted) {
          if (ch === "\\") {
            result2 += "\\\\";
          } else if (ch === "]") {
            result2 += "\\]";
          } else if (ch === "^" && result2 === "[") {
            result2 += "\\^";
          } else {
            result2 += ch;
          }
        }
        i = closeQuote + 1;
        continue;
      }
    }
    if (content[i] === "[" && i + 1 < content.length && content[i + 1] === ":") {
      const closePos = content.indexOf(":]", i + 2);
      if (closePos !== -1) {
        const posixClass = content.slice(i + 2, closePos);
        result2 += posixClassToRegex2(posixClass);
        i = closePos + 2;
        continue;
      }
    }
    const char = content[i];
    if (char === "\\") {
      if (i + 1 < content.length) {
        result2 += `\\${content[i + 1]}`;
        i += 2;
      } else {
        result2 += "\\\\";
        i++;
      }
    } else if (char === "-" && i > 0 && i < content.length - 1) {
      result2 += "-";
      i++;
    } else if (char === "^" && i === 0) {
      result2 += "^";
      i++;
    } else {
      if (char === "]" && i === 0) {
        result2 += "\\]";
      } else {
        result2 += char;
      }
      i++;
    }
  }
  result2 += "]";
  return result2;
}
__name$1(convertCharClass, "convertCharClass");
var POSIX_CLASSES2 = {
  alnum: "a-zA-Z0-9",
  alpha: "a-zA-Z",
  ascii: "\\x00-\\x7F",
  blank: " \\t",
  cntrl: "\\x00-\\x1F\\x7F",
  digit: "0-9",
  graph: "!-~",
  lower: "a-z",
  print: " -~",
  punct: "!-/:-@\\[-`{-~",
  space: " \\t\\n\\r\\f\\v",
  upper: "A-Z",
  word: "a-zA-Z0-9_",
  xdigit: "0-9A-Fa-f"
};
function posixClassToRegex2(name) {
  return POSIX_CLASSES2[name] ?? "";
}
__name$1(posixClassToRegex2, "posixClassToRegex");
function applyPatternRemoval(value, regexStr, side, greedy) {
  if (side === "prefix") {
    return value.replace(new RegExp(`^${regexStr}`, "s"), "");
  }
  const regex = new RegExp(`${regexStr}$`, "s");
  if (greedy) {
    return value.replace(regex, "");
  }
  for (let i = value.length; i >= 0; i--) {
    const suffix = value.slice(i);
    if (regex.test(suffix)) {
      return value.slice(0, i);
    }
  }
  return value;
}
__name$1(applyPatternRemoval, "applyPatternRemoval");
function getVarNamesWithPrefix(ctx, prefix) {
  const envKeys = Object.keys(ctx.state.env);
  const matchingVars = /* @__PURE__ */ new Set();
  const assocArrays = ctx.state.associativeArrays ?? /* @__PURE__ */ new Set();
  const indexedArrays = /* @__PURE__ */ new Set();
  for (const k of envKeys) {
    const match = k.match(/^([a-zA-Z_][a-zA-Z0-9_]*)_\d+$/);
    if (match) {
      indexedArrays.add(match[1]);
    }
    const lengthMatch = k.match(/^([a-zA-Z_][a-zA-Z0-9_]*)__length$/);
    if (lengthMatch) {
      indexedArrays.add(lengthMatch[1]);
    }
  }
  const isAssocArrayElement = /* @__PURE__ */ __name$1((key2) => {
    for (const arrayName of assocArrays) {
      const elemPrefix = `${arrayName}_`;
      if (key2.startsWith(elemPrefix) && key2 !== arrayName) {
        return true;
      }
    }
    return false;
  }, "isAssocArrayElement");
  for (const k of envKeys) {
    if (k.startsWith(prefix)) {
      if (k.includes("__")) {
        const lengthMatch = k.match(/^([a-zA-Z_][a-zA-Z0-9_]*)__length$/);
        if (lengthMatch?.[1].startsWith(prefix)) {
          matchingVars.add(lengthMatch[1]);
        }
      } else if (/_\d+$/.test(k)) {
        const match = k.match(/^([a-zA-Z_][a-zA-Z0-9_]*)_\d+$/);
        if (match?.[1].startsWith(prefix)) {
          matchingVars.add(match[1]);
        }
      } else if (isAssocArrayElement(k)) ;
      else {
        matchingVars.add(k);
      }
    }
  }
  return [...matchingVars].sort();
}
__name$1(getVarNamesWithPrefix, "getVarNamesWithPrefix");
function simpleStrftime(format, date2) {
  const pad = /* @__PURE__ */ __name$1((n, width = 2) => String(n).padStart(width, "0"), "pad");
  if (format === "") {
    const h = pad(date2.getHours());
    const m = pad(date2.getMinutes());
    const s = pad(date2.getSeconds());
    return `${h}:${m}:${s}`;
  }
  let result2 = "";
  let i = 0;
  while (i < format.length) {
    if (format[i] === "%") {
      if (i + 1 >= format.length) {
        result2 += "%";
        i++;
        continue;
      }
      const spec = format[i + 1];
      switch (spec) {
        case "H":
          result2 += pad(date2.getHours());
          break;
        case "M":
          result2 += pad(date2.getMinutes());
          break;
        case "S":
          result2 += pad(date2.getSeconds());
          break;
        case "d":
          result2 += pad(date2.getDate());
          break;
        case "m":
          result2 += pad(date2.getMonth() + 1);
          break;
        case "Y":
          result2 += date2.getFullYear();
          break;
        case "y":
          result2 += pad(date2.getFullYear() % 100);
          break;
        case "I": {
          let h = date2.getHours() % 12;
          if (h === 0)
            h = 12;
          result2 += pad(h);
          break;
        }
        case "p":
          result2 += date2.getHours() < 12 ? "AM" : "PM";
          break;
        case "P":
          result2 += date2.getHours() < 12 ? "am" : "pm";
          break;
        case "%":
          result2 += "%";
          break;
        case "a": {
          const days = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
          result2 += days[date2.getDay()];
          break;
        }
        case "b": {
          const months = [
            "Jan",
            "Feb",
            "Mar",
            "Apr",
            "May",
            "Jun",
            "Jul",
            "Aug",
            "Sep",
            "Oct",
            "Nov",
            "Dec"
          ];
          result2 += months[date2.getMonth()];
          break;
        }
        default:
          result2 += `%${spec}`;
      }
      i += 2;
    } else {
      result2 += format[i];
      i++;
    }
  }
  return result2;
}
__name$1(simpleStrftime, "simpleStrftime");
function expandPrompt(ctx, value) {
  let result2 = "";
  let i = 0;
  const user = ctx.state.env.USER || ctx.state.env.LOGNAME || "user";
  const hostname = ctx.state.env.HOSTNAME || "localhost";
  const shortHost = hostname.split(".")[0];
  const pwd = ctx.state.env.PWD || "/";
  const home = ctx.state.env.HOME || "/";
  const tildeExpanded = pwd.startsWith(home) ? `~${pwd.slice(home.length)}` : pwd;
  const pwdBasename = pwd.split("/").pop() || pwd;
  const now = /* @__PURE__ */ new Date();
  const weekdays = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
  const months = [
    "Jan",
    "Feb",
    "Mar",
    "Apr",
    "May",
    "Jun",
    "Jul",
    "Aug",
    "Sep",
    "Oct",
    "Nov",
    "Dec"
  ];
  const cmdNum = ctx.state.env.__COMMAND_NUMBER || "1";
  while (i < value.length) {
    const char = value[i];
    if (char === "\\") {
      if (i + 1 >= value.length) {
        result2 += "\\";
        i++;
        continue;
      }
      const next = value[i + 1];
      if (next >= "0" && next <= "7") {
        let octalStr = "";
        let j = i + 1;
        while (j < value.length && j < i + 4 && value[j] >= "0" && value[j] <= "7") {
          octalStr += value[j];
          j++;
        }
        const code = Number.parseInt(octalStr, 8) % 256;
        result2 += String.fromCharCode(code);
        i = j;
        continue;
      }
      switch (next) {
        case "\\":
          result2 += "\\";
          i += 2;
          break;
        case "a":
          result2 += "\x07";
          i += 2;
          break;
        case "e":
          result2 += "\x1B";
          i += 2;
          break;
        case "n":
          result2 += "\n";
          i += 2;
          break;
        case "r":
          result2 += "\r";
          i += 2;
          break;
        case "$":
          result2 += "$";
          i += 2;
          break;
        case "[":
        case "]":
          i += 2;
          break;
        case "u":
          result2 += user;
          i += 2;
          break;
        case "h":
          result2 += shortHost;
          i += 2;
          break;
        case "H":
          result2 += hostname;
          i += 2;
          break;
        case "w":
          result2 += tildeExpanded;
          i += 2;
          break;
        case "W":
          result2 += pwdBasename;
          i += 2;
          break;
        case "d": {
          const dayStr = String(now.getDate()).padStart(2, " ");
          result2 += `${weekdays[now.getDay()]} ${months[now.getMonth()]} ${dayStr}`;
          i += 2;
          break;
        }
        case "t": {
          const h = String(now.getHours()).padStart(2, "0");
          const m = String(now.getMinutes()).padStart(2, "0");
          const s = String(now.getSeconds()).padStart(2, "0");
          result2 += `${h}:${m}:${s}`;
          i += 2;
          break;
        }
        case "T": {
          let h = now.getHours() % 12;
          if (h === 0)
            h = 12;
          const hStr = String(h).padStart(2, "0");
          const m = String(now.getMinutes()).padStart(2, "0");
          const s = String(now.getSeconds()).padStart(2, "0");
          result2 += `${hStr}:${m}:${s}`;
          i += 2;
          break;
        }
        case "@": {
          let h = now.getHours() % 12;
          if (h === 0)
            h = 12;
          const hStr = String(h).padStart(2, "0");
          const m = String(now.getMinutes()).padStart(2, "0");
          const ampm = now.getHours() < 12 ? "AM" : "PM";
          result2 += `${hStr}:${m} ${ampm}`;
          i += 2;
          break;
        }
        case "A": {
          const h = String(now.getHours()).padStart(2, "0");
          const m = String(now.getMinutes()).padStart(2, "0");
          result2 += `${h}:${m}`;
          i += 2;
          break;
        }
        case "D":
          if (i + 2 < value.length && value[i + 2] === "{") {
            const closeIdx = value.indexOf("}", i + 3);
            if (closeIdx !== -1) {
              const format = value.slice(i + 3, closeIdx);
              result2 += simpleStrftime(format, now);
              i = closeIdx + 1;
            } else {
              result2 += "\\D";
              i += 2;
            }
          } else {
            result2 += "\\D";
            i += 2;
          }
          break;
        case "s":
          result2 += "bash";
          i += 2;
          break;
        case "v":
          result2 += "5.0";
          i += 2;
          break;
        case "V":
          result2 += "5.0.0";
          i += 2;
          break;
        case "j":
          result2 += "0";
          i += 2;
          break;
        case "l":
          result2 += "tty";
          i += 2;
          break;
        case "#":
          result2 += cmdNum;
          i += 2;
          break;
        case "!":
          result2 += cmdNum;
          i += 2;
          break;
        case "x":
          result2 += "\\x";
          i += 2;
          break;
        default:
          result2 += `\\${next}`;
          i += 2;
      }
    } else {
      result2 += char;
      i++;
    }
  }
  return result2;
}
__name$1(expandPrompt, "expandPrompt");
function quoteValue$1(value) {
  if (value === "")
    return "''";
  const needsDollarQuote = /[\n\r\t\x00-\x1f\x7f']/.test(value);
  if (needsDollarQuote) {
    let result2 = "$'";
    for (const char of value) {
      switch (char) {
        case "'":
          result2 += "\\'";
          break;
        case "\\":
          result2 += "\\\\";
          break;
        case "\n":
          result2 += "\\n";
          break;
        case "\r":
          result2 += "\\r";
          break;
        case "	":
          result2 += "\\t";
          break;
        default: {
          const code = char.charCodeAt(0);
          if (code < 32 || code === 127) {
            result2 += `\\${code.toString(8).padStart(3, "0")}`;
          } else {
            result2 += char;
          }
        }
      }
    }
    return `${result2}'`;
  }
  return `'${value}'`;
}
__name$1(quoteValue$1, "quoteValue");
function markReadonly(ctx, name) {
  ctx.state.readonlyVars = ctx.state.readonlyVars || /* @__PURE__ */ new Set();
  ctx.state.readonlyVars.add(name);
}
__name$1(markReadonly, "markReadonly");
function isReadonly(ctx, name) {
  return ctx.state.readonlyVars?.has(name) ?? false;
}
__name$1(isReadonly, "isReadonly");
function checkReadonlyError(ctx, name, command = "bash") {
  if (isReadonly(ctx, name)) {
    const stderr = `${command}: ${name}: readonly variable
`;
    throw new ExitError(1, "", stderr);
  }
  return null;
}
__name$1(checkReadonlyError, "checkReadonlyError");
function markExported(ctx, name) {
  const wasExported = ctx.state.exportedVars?.has(name) ?? false;
  ctx.state.exportedVars = ctx.state.exportedVars || /* @__PURE__ */ new Set();
  ctx.state.exportedVars.add(name);
  if (ctx.state.localScopes.length > 0) {
    const currentScope = ctx.state.localScopes[ctx.state.localScopes.length - 1];
    if (currentScope.has(name) && !wasExported) {
      if (!ctx.state.localExportedVars) {
        ctx.state.localExportedVars = [];
      }
      while (ctx.state.localExportedVars.length < ctx.state.localScopes.length) {
        ctx.state.localExportedVars.push(/* @__PURE__ */ new Set());
      }
      ctx.state.localExportedVars[ctx.state.localExportedVars.length - 1].add(name);
    }
  }
}
__name$1(markExported, "markExported");
function unmarkExported(ctx, name) {
  ctx.state.exportedVars?.delete(name);
}
__name$1(unmarkExported, "unmarkExported");
function getVariableAttributes(ctx, name) {
  if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(name)) {
    return "";
  }
  let attrs = "";
  const isIndexedArray = ctx.state.env[`${name}__length`] !== void 0 || Object.keys(ctx.state.env).some((k) => k.startsWith(`${name}_`) && /^[0-9]+$/.test(k.slice(name.length + 1)));
  const isAssocArray = ctx.state.associativeArrays?.has(name) ?? false;
  if (isIndexedArray && !isAssocArray) {
    attrs += "a";
  }
  if (isAssocArray) {
    attrs += "A";
  }
  if (ctx.state.integerVars?.has(name)) {
    attrs += "i";
  }
  if (isNameref(ctx, name)) {
    attrs += "n";
  }
  if (isReadonly(ctx, name)) {
    attrs += "r";
  }
  if (ctx.state.exportedVars?.has(name)) {
    attrs += "x";
  }
  return attrs;
}
__name$1(getVariableAttributes, "getVariableAttributes");
async function handleDefaultValue(ctx, operation, opCtx, expandWordPartsAsync2) {
  const useDefault = opCtx.isUnset || operation.checkEmpty && opCtx.isEmpty;
  if (useDefault && operation.word) {
    return expandWordPartsAsync2(ctx, operation.word.parts, opCtx.inDoubleQuotes);
  }
  return opCtx.effectiveValue;
}
__name$1(handleDefaultValue, "handleDefaultValue");
async function handleAssignDefault(ctx, parameter, operation, opCtx, expandWordPartsAsync2) {
  const useDefault = opCtx.isUnset || operation.checkEmpty && opCtx.isEmpty;
  if (useDefault && operation.word) {
    const defaultValue = await expandWordPartsAsync2(ctx, operation.word.parts, opCtx.inDoubleQuotes);
    const arrayMatch = parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
    if (arrayMatch) {
      const [, arrayName, subscriptExpr] = arrayMatch;
      let index;
      if (/^\d+$/.test(subscriptExpr)) {
        index = Number.parseInt(subscriptExpr, 10);
      } else {
        try {
          const parser = new Parser();
          const arithAst = parseArithmeticExpression(parser, subscriptExpr);
          index = await evaluateArithmetic(ctx, arithAst.expression);
        } catch {
          const varValue = ctx.state.env[subscriptExpr];
          index = varValue ? Number.parseInt(varValue, 10) : 0;
        }
        if (Number.isNaN(index))
          index = 0;
      }
      ctx.state.env[`${arrayName}_${index}`] = defaultValue;
      const currentLength = Number.parseInt(ctx.state.env[`${arrayName}__length`] || "0", 10);
      if (index >= currentLength) {
        ctx.state.env[`${arrayName}__length`] = String(index + 1);
      }
    } else {
      ctx.state.env[parameter] = defaultValue;
    }
    return defaultValue;
  }
  return opCtx.effectiveValue;
}
__name$1(handleAssignDefault, "handleAssignDefault");
async function handleErrorIfUnset(ctx, parameter, operation, opCtx, expandWordPartsAsync2) {
  const shouldError = opCtx.isUnset || operation.checkEmpty && opCtx.isEmpty;
  if (shouldError) {
    const message = operation.word ? await expandWordPartsAsync2(ctx, operation.word.parts, opCtx.inDoubleQuotes) : `${parameter}: parameter null or not set`;
    throw new ExitError(1, "", `bash: ${message}
`);
  }
  return opCtx.effectiveValue;
}
__name$1(handleErrorIfUnset, "handleErrorIfUnset");
async function handleUseAlternative(ctx, operation, opCtx, expandWordPartsAsync2) {
  const useAlternative = !(opCtx.isUnset || operation.checkEmpty && opCtx.isEmpty);
  if (useAlternative && operation.word) {
    return expandWordPartsAsync2(ctx, operation.word.parts, opCtx.inDoubleQuotes);
  }
  return "";
}
__name$1(handleUseAlternative, "handleUseAlternative");
async function handlePatternRemoval(ctx, value, operation, expandWordPartsAsync2, expandPart2) {
  let regexStr = "";
  const extglob = ctx.state.shoptOptions.extglob;
  if (operation.pattern) {
    for (const part of operation.pattern.parts) {
      if (part.type === "Glob") {
        regexStr += patternToRegex(part.pattern, operation.greedy, extglob);
      } else if (part.type === "Literal") {
        regexStr += patternToRegex(part.value, operation.greedy, extglob);
      } else if (part.type === "SingleQuoted" || part.type === "Escaped") {
        regexStr += escapeRegex(part.value);
      } else if (part.type === "DoubleQuoted") {
        const expanded = await expandWordPartsAsync2(ctx, part.parts);
        regexStr += escapeRegex(expanded);
      } else if (part.type === "ParameterExpansion") {
        const expanded = await expandPart2(ctx, part);
        regexStr += patternToRegex(expanded, operation.greedy, extglob);
      } else {
        const expanded = await expandPart2(ctx, part);
        regexStr += escapeRegex(expanded);
      }
    }
  }
  if (operation.side === "prefix") {
    return value.replace(new RegExp(`^${regexStr}`, "s"), "");
  }
  const regex = new RegExp(`${regexStr}$`, "s");
  if (operation.greedy) {
    return value.replace(regex, "");
  }
  for (let i = value.length; i >= 0; i--) {
    const suffix = value.slice(i);
    if (regex.test(suffix)) {
      return value.slice(0, i);
    }
  }
  return value;
}
__name$1(handlePatternRemoval, "handlePatternRemoval");
async function handlePatternReplacement(ctx, value, operation, expandWordPartsAsync2, expandPart2) {
  let regex = "";
  const extglob = ctx.state.shoptOptions.extglob;
  if (operation.pattern) {
    for (const part of operation.pattern.parts) {
      if (part.type === "Glob") {
        regex += patternToRegex(part.pattern, true, extglob);
      } else if (part.type === "Literal") {
        regex += patternToRegex(part.value, true, extglob);
      } else if (part.type === "SingleQuoted" || part.type === "Escaped") {
        regex += escapeRegex(part.value);
      } else if (part.type === "DoubleQuoted") {
        const expanded = await expandWordPartsAsync2(ctx, part.parts);
        regex += escapeRegex(expanded);
      } else if (part.type === "ParameterExpansion") {
        const expanded = await expandPart2(ctx, part);
        regex += patternToRegex(expanded, true, extglob);
      } else {
        const expanded = await expandPart2(ctx, part);
        regex += escapeRegex(expanded);
      }
    }
  }
  const replacement = operation.replacement ? await expandWordPartsAsync2(ctx, operation.replacement.parts) : "";
  if (operation.anchor === "start") {
    regex = `^${regex}`;
  } else if (operation.anchor === "end") {
    regex = `${regex}$`;
  }
  if (regex === "") {
    return value;
  }
  const flags = operation.all ? "gs" : "s";
  try {
    const re = new RegExp(regex, flags);
    if (operation.all) {
      let result2 = "";
      let lastIndex = 0;
      let match = re.exec(value);
      while (match !== null) {
        if (match[0].length === 0 && match.index === value.length) {
          break;
        }
        result2 += value.slice(lastIndex, match.index) + replacement;
        lastIndex = match.index + match[0].length;
        if (match[0].length === 0) {
          lastIndex++;
        }
        match = re.exec(value);
      }
      result2 += value.slice(lastIndex);
      return result2;
    }
    return value.replace(re, replacement);
  } catch {
    return value;
  }
}
__name$1(handlePatternReplacement, "handlePatternReplacement");
function handleLength(ctx, parameter, value) {
  const arrayMatch = parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[[@*]\]$/);
  if (arrayMatch) {
    const arrayName = arrayMatch[1];
    const elements = getArrayElements(ctx, arrayName);
    if (elements.length > 0) {
      return String(elements.length);
    }
    const scalarValue = ctx.state.env[arrayName];
    if (scalarValue !== void 0) {
      return "1";
    }
    return "0";
  }
  if (/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(parameter) && isArray(ctx, parameter)) {
    if (parameter === "FUNCNAME") {
      const firstElement2 = ctx.state.funcNameStack?.[0] || "";
      return String([...firstElement2].length);
    }
    if (parameter === "BASH_LINENO") {
      const firstElement2 = ctx.state.callLineStack?.[0];
      return String(firstElement2 !== void 0 ? [...String(firstElement2)].length : 0);
    }
    const firstElement = ctx.state.env[`${parameter}_0`] || "";
    return String([...firstElement].length);
  }
  return String([...value].length);
}
__name$1(handleLength, "handleLength");
async function handleSubstring(ctx, parameter, value, operation) {
  const offset = await evaluateArithmetic(ctx, operation.offset.expression);
  const length = operation.length ? await evaluateArithmetic(ctx, operation.length.expression) : void 0;
  if (parameter === "@" || parameter === "*") {
    const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
    const params = [];
    for (let i = 1; i <= numParams; i++) {
      params.push(ctx.state.env[String(i)] || "");
    }
    const shellName = ctx.state.env["0"] || "bash";
    let allArgs;
    let startIdx;
    if (offset <= 0) {
      allArgs = [shellName, ...params];
      if (offset < 0) {
        startIdx = allArgs.length + offset;
        if (startIdx < 0)
          return "";
      } else {
        startIdx = 0;
      }
    } else {
      allArgs = params;
      startIdx = offset - 1;
    }
    if (startIdx < 0 || startIdx >= allArgs.length) {
      return "";
    }
    if (length !== void 0) {
      const endIdx = length < 0 ? allArgs.length + length : startIdx + length;
      return allArgs.slice(startIdx, Math.max(startIdx, endIdx)).join(" ");
    }
    return allArgs.slice(startIdx).join(" ");
  }
  const arrayMatchSubstr = parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[[@*]\]$/);
  if (arrayMatchSubstr) {
    const arrayName = arrayMatchSubstr[1];
    if (ctx.state.associativeArrays?.has(arrayName)) {
      throw new ExitError(1, "", `bash: \${${arrayName}[@]: 0: 3}: bad substitution
`);
    }
    const elements = getArrayElements(ctx, arrayName);
    let startIdx = 0;
    if (offset < 0) {
      if (elements.length > 0) {
        const lastIdx = elements[elements.length - 1][0];
        const maxIndex = typeof lastIdx === "number" ? lastIdx : 0;
        const targetIndex = maxIndex + 1 + offset;
        if (targetIndex < 0)
          return "";
        startIdx = elements.findIndex(([idx]) => typeof idx === "number" && idx >= targetIndex);
        if (startIdx < 0)
          return "";
      }
    } else {
      startIdx = elements.findIndex(([idx]) => typeof idx === "number" && idx >= offset);
      if (startIdx < 0)
        return "";
    }
    if (length !== void 0) {
      if (length < 0) {
        throw new ArithmeticError(`${arrayMatchSubstr[1]}[@]: substring expression < 0`);
      }
      return elements.slice(startIdx, startIdx + length).map(([, v]) => v).join(" ");
    }
    return elements.slice(startIdx).map(([, v]) => v).join(" ");
  }
  const chars = [...value];
  let start2 = offset;
  if (start2 < 0)
    start2 = Math.max(0, chars.length + start2);
  if (length !== void 0) {
    if (length < 0) {
      const endPos = chars.length + length;
      return chars.slice(start2, Math.max(start2, endPos)).join("");
    }
    return chars.slice(start2, start2 + length).join("");
  }
  return chars.slice(start2).join("");
}
__name$1(handleSubstring, "handleSubstring");
async function handleCaseModification(ctx, value, operation, expandWordPartsAsync2, expandParameterAsync2) {
  if (operation.pattern) {
    const extglob = ctx.state.shoptOptions.extglob;
    let patternRegexStr = "";
    for (const part of operation.pattern.parts) {
      if (part.type === "Glob") {
        patternRegexStr += patternToRegex(part.pattern, true, extglob);
      } else if (part.type === "Literal") {
        patternRegexStr += patternToRegex(part.value, true, extglob);
      } else if (part.type === "SingleQuoted" || part.type === "Escaped") {
        patternRegexStr += escapeRegex(part.value);
      } else if (part.type === "DoubleQuoted") {
        const expanded = await expandWordPartsAsync2(ctx, part.parts);
        patternRegexStr += escapeRegex(expanded);
      } else if (part.type === "ParameterExpansion") {
        const expanded = await expandParameterAsync2(ctx, part);
        patternRegexStr += patternToRegex(expanded, true, extglob);
      }
    }
    const charPattern = new RegExp(`^(?:${patternRegexStr})$`);
    const transform = operation.direction === "upper" ? (c) => c.toUpperCase() : (c) => c.toLowerCase();
    let result2 = "";
    let converted = false;
    for (const char of value) {
      if (!operation.all && converted) {
        result2 += char;
      } else if (charPattern.test(char)) {
        result2 += transform(char);
        converted = true;
      } else {
        result2 += char;
      }
    }
    return result2;
  }
  if (operation.direction === "upper") {
    return operation.all ? value.toUpperCase() : value.charAt(0).toUpperCase() + value.slice(1);
  }
  return operation.all ? value.toLowerCase() : value.charAt(0).toLowerCase() + value.slice(1);
}
__name$1(handleCaseModification, "handleCaseModification");
function handleTransform(ctx, parameter, value, isUnset, operation) {
  const arrayMatchTransform = parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[[@*]\]$/);
  if (arrayMatchTransform && operation.operator === "Q") {
    const elements = getArrayElements(ctx, arrayMatchTransform[1]);
    const quotedElements = elements.map(([, v]) => quoteValue$1(v));
    return quotedElements.join(" ");
  }
  if (arrayMatchTransform && operation.operator === "a") {
    return getVariableAttributes(ctx, arrayMatchTransform[1]);
  }
  const arrayElemMatch = parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[.+\]$/);
  if (arrayElemMatch && operation.operator === "a") {
    return getVariableAttributes(ctx, arrayElemMatch[1]);
  }
  switch (operation.operator) {
    case "Q":
      if (isUnset)
        return "";
      return quoteValue$1(value);
    case "P":
      return expandPrompt(ctx, value);
    case "a":
      return getVariableAttributes(ctx, parameter);
    case "A":
      if (isUnset)
        return "";
      return `${parameter}=${quoteValue$1(value)}`;
    case "E":
      return value.replace(/\\([\\abefnrtv'"?])/g, (_, c) => {
        switch (c) {
          case "\\":
            return "\\";
          case "a":
            return "\x07";
          case "b":
            return "\b";
          case "e":
            return "\x1B";
          case "f":
            return "\f";
          case "n":
            return "\n";
          case "r":
            return "\r";
          case "t":
            return "	";
          case "v":
            return "\v";
          case "'":
            return "'";
          case '"':
            return '"';
          case "?":
            return "?";
          default:
            return c;
        }
      });
    case "K":
    case "k":
      if (isUnset)
        return "";
      return quoteValue$1(value);
    case "u":
      return value.charAt(0).toUpperCase() + value.slice(1);
    case "U":
      return value.toUpperCase();
    case "L":
      return value.toLowerCase();
    default:
      return value;
  }
}
__name$1(handleTransform, "handleTransform");
async function handleIndirection(ctx, parameter, value, isUnset, operation, expandParameterAsync2, inDoubleQuotes = false) {
  if (isNameref(ctx, parameter)) {
    return getNamerefTarget(ctx, parameter) || "";
  }
  const isArrayExpansionPattern = /^[a-zA-Z_][a-zA-Z0-9_]*\[([@*])\]$/.test(parameter);
  if (isUnset) {
    if (operation.innerOp?.type === "UseAlternative") {
      return "";
    }
    throw new BadSubstitutionError(`\${!${parameter}}`);
  }
  const targetName = value;
  if (isArrayExpansionPattern && (targetName === "" || targetName.includes(" "))) {
    throw new BadSubstitutionError(`\${!${parameter}}`);
  }
  const arraySubscriptMatch = targetName.match(/^[a-zA-Z_][a-zA-Z0-9_]*\[(.+)\]$/);
  if (arraySubscriptMatch) {
    const subscript = arraySubscriptMatch[1];
    if (subscript.includes("~")) {
      throw new BadSubstitutionError(`\${!${parameter}}`);
    }
  }
  if (operation.innerOp) {
    const syntheticPart = {
      type: "ParameterExpansion",
      parameter: targetName,
      operation: operation.innerOp
    };
    return expandParameterAsync2(ctx, syntheticPart, inDoubleQuotes);
  }
  return await getVariable(ctx, targetName);
}
__name$1(handleIndirection, "handleIndirection");
function handleArrayKeys(ctx, operation) {
  const elements = getArrayElements(ctx, operation.array);
  const keys = elements.map(([k]) => String(k));
  if (operation.star) {
    return keys.join(getIfsSeparator(ctx.state.env));
  }
  return keys.join(" ");
}
__name$1(handleArrayKeys, "handleArrayKeys");
function handleVarNamePrefix(ctx, operation) {
  const matchingVars = getVarNamesWithPrefix(ctx, operation.prefix);
  if (operation.star) {
    return matchingVars.join(getIfsSeparator(ctx.state.env));
  }
  return matchingVars.join(" ");
}
__name$1(handleVarNamePrefix, "handleVarNamePrefix");
function computeIsEmpty(ctx, parameter, value, inDoubleQuotes) {
  const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
  const arrayExpMatch = parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
  if (parameter === "*") {
    return { isEmpty: numParams === 0, effectiveValue: value };
  }
  if (parameter === "@") {
    return {
      isEmpty: numParams === 0 || numParams === 1 && ctx.state.env["1"] === "",
      effectiveValue: value
    };
  }
  if (arrayExpMatch) {
    const [, arrayName, subscript] = arrayExpMatch;
    const elements = getArrayElements(ctx, arrayName);
    if (elements.length === 0) {
      return { isEmpty: true, effectiveValue: "" };
    }
    if (subscript === "*") {
      const ifsSep = getIfsSeparator(ctx.state.env);
      const joined = elements.map(([, v]) => v).join(ifsSep);
      return {
        isEmpty: inDoubleQuotes ? joined === "" : false,
        effectiveValue: joined
        // Use IFS-joined value instead of space-joined
      };
    }
    return {
      isEmpty: elements.length === 1 && elements.every(([, v]) => v === ""),
      effectiveValue: elements.map(([, v]) => v).join(" ")
    };
  }
  return { isEmpty: value === "", effectiveValue: value };
}
__name$1(computeIsEmpty, "computeIsEmpty");
function patternHasCommandSubstitution(pattern) {
  let i = 0;
  while (i < pattern.length) {
    const c = pattern[i];
    if (c === "\\" && i + 1 < pattern.length) {
      i += 2;
      continue;
    }
    if (c === "'") {
      const closeIdx = pattern.indexOf("'", i + 1);
      if (closeIdx !== -1) {
        i = closeIdx + 1;
        continue;
      }
    }
    if (c === "$" && i + 1 < pattern.length && pattern[i + 1] === "(") {
      return true;
    }
    if (c === "`") {
      return true;
    }
    i++;
  }
  return false;
}
__name$1(patternHasCommandSubstitution, "patternHasCommandSubstitution");
function findCommandSubstitutionEnd(pattern, startIdx) {
  let depth = 1;
  let i = startIdx;
  let inSingleQuote = false;
  let inDoubleQuote = false;
  while (i < pattern.length && depth > 0) {
    const c = pattern[i];
    if (c === "\\" && !inSingleQuote && i + 1 < pattern.length) {
      i += 2;
      continue;
    }
    if (c === "'" && !inDoubleQuote) {
      inSingleQuote = !inSingleQuote;
      i++;
      continue;
    }
    if (c === '"' && !inSingleQuote) {
      inDoubleQuote = !inDoubleQuote;
      i++;
      continue;
    }
    if (!inSingleQuote && !inDoubleQuote) {
      if (c === "(") {
        depth++;
      } else if (c === ")") {
        depth--;
        if (depth === 0) {
          return i;
        }
      }
    }
    i++;
  }
  return -1;
}
__name$1(findCommandSubstitutionEnd, "findCommandSubstitutionEnd");
async function executeCommandSubstitutionFromString(ctx, commandStr) {
  const parser = new Parser();
  let ast;
  try {
    ast = parser.parse(commandStr);
  } catch {
    return "";
  }
  const savedBashPid = ctx.state.bashPid;
  ctx.state.bashPid = ctx.state.nextVirtualPid++;
  const savedEnv = { ...ctx.state.env };
  const savedCwd = ctx.state.cwd;
  const savedSuppressVerbose = ctx.state.suppressVerbose;
  ctx.state.suppressVerbose = true;
  try {
    const result2 = await ctx.executeScript(ast);
    const exitCode = result2.exitCode;
    ctx.state.env = savedEnv;
    ctx.state.cwd = savedCwd;
    ctx.state.suppressVerbose = savedSuppressVerbose;
    ctx.state.lastExitCode = exitCode;
    ctx.state.env["?"] = String(exitCode);
    if (result2.stderr) {
      ctx.state.expansionStderr = (ctx.state.expansionStderr || "") + result2.stderr;
    }
    ctx.state.bashPid = savedBashPid;
    return result2.stdout.replace(/\n+$/, "");
  } catch (error) {
    ctx.state.env = savedEnv;
    ctx.state.cwd = savedCwd;
    ctx.state.bashPid = savedBashPid;
    ctx.state.suppressVerbose = savedSuppressVerbose;
    if (error instanceof ExecutionLimitError) {
      throw error;
    }
    if (error instanceof ExitError) {
      ctx.state.lastExitCode = error.exitCode;
      ctx.state.env["?"] = String(error.exitCode);
      return error.stdout?.replace(/\n+$/, "") ?? "";
    }
    return "";
  }
}
__name$1(executeCommandSubstitutionFromString, "executeCommandSubstitutionFromString");
function expandVariablesInPattern(ctx, pattern) {
  let result2 = "";
  let i = 0;
  while (i < pattern.length) {
    const c = pattern[i];
    if (c === "'") {
      const closeIdx = pattern.indexOf("'", i + 1);
      if (closeIdx !== -1) {
        const content = pattern.slice(i + 1, closeIdx);
        result2 += escapeGlobChars(content);
        i = closeIdx + 1;
        continue;
      }
    }
    if (c === '"') {
      let closeIdx = -1;
      let j = i + 1;
      while (j < pattern.length) {
        if (pattern[j] === "\\") {
          j += 2;
          continue;
        }
        if (pattern[j] === '"') {
          closeIdx = j;
          break;
        }
        j++;
      }
      if (closeIdx !== -1) {
        const content = pattern.slice(i + 1, closeIdx);
        const expanded = expandVariablesInDoubleQuotedPattern(ctx, content);
        result2 += escapeGlobChars(expanded);
        i = closeIdx + 1;
        continue;
      }
    }
    if (c === "$") {
      if (i + 1 < pattern.length) {
        const next = pattern[i + 1];
        if (next === "{") {
          const closeIdx = pattern.indexOf("}", i + 2);
          if (closeIdx !== -1) {
            const varName = pattern.slice(i + 2, closeIdx);
            result2 += ctx.state.env[varName] ?? "";
            i = closeIdx + 1;
            continue;
          }
        } else if (/[a-zA-Z_]/.test(next)) {
          let end = i + 1;
          while (end < pattern.length && /[a-zA-Z0-9_]/.test(pattern[end])) {
            end++;
          }
          const varName = pattern.slice(i + 1, end);
          result2 += ctx.state.env[varName] ?? "";
          i = end;
          continue;
        }
      }
    }
    if (c === "\\" && i + 1 < pattern.length) {
      result2 += c + pattern[i + 1];
      i += 2;
      continue;
    }
    result2 += c;
    i++;
  }
  return result2;
}
__name$1(expandVariablesInPattern, "expandVariablesInPattern");
function expandVariablesInDoubleQuotedPattern(ctx, content) {
  let result2 = "";
  let i = 0;
  while (i < content.length) {
    const c = content[i];
    if (c === "\\" && i + 1 < content.length) {
      const next = content[i + 1];
      if (next === "$" || next === "`" || next === "\\" || next === '"') {
        result2 += next;
        i += 2;
        continue;
      }
      result2 += c;
      i++;
      continue;
    }
    if (c === "$") {
      if (i + 1 < content.length) {
        const next = content[i + 1];
        if (next === "{") {
          const closeIdx = content.indexOf("}", i + 2);
          if (closeIdx !== -1) {
            const varName = content.slice(i + 2, closeIdx);
            result2 += ctx.state.env[varName] ?? "";
            i = closeIdx + 1;
            continue;
          }
        } else if (/[a-zA-Z_]/.test(next)) {
          let end = i + 1;
          while (end < content.length && /[a-zA-Z0-9_]/.test(content[end])) {
            end++;
          }
          const varName = content.slice(i + 1, end);
          result2 += ctx.state.env[varName] ?? "";
          i = end;
          continue;
        }
      }
    }
    result2 += c;
    i++;
  }
  return result2;
}
__name$1(expandVariablesInDoubleQuotedPattern, "expandVariablesInDoubleQuotedPattern");
async function expandVariablesInPatternAsync(ctx, pattern) {
  let result2 = "";
  let i = 0;
  while (i < pattern.length) {
    const c = pattern[i];
    if (c === "'") {
      const closeIdx = pattern.indexOf("'", i + 1);
      if (closeIdx !== -1) {
        const content = pattern.slice(i + 1, closeIdx);
        result2 += escapeGlobChars(content);
        i = closeIdx + 1;
        continue;
      }
    }
    if (c === '"') {
      let closeIdx = -1;
      let j = i + 1;
      while (j < pattern.length) {
        if (pattern[j] === "\\") {
          j += 2;
          continue;
        }
        if (pattern[j] === '"') {
          closeIdx = j;
          break;
        }
        j++;
      }
      if (closeIdx !== -1) {
        const content = pattern.slice(i + 1, closeIdx);
        const expanded = await expandVariablesInDoubleQuotedPatternAsync(ctx, content);
        result2 += escapeGlobChars(expanded);
        i = closeIdx + 1;
        continue;
      }
    }
    if (c === "$" && i + 1 < pattern.length && pattern[i + 1] === "(") {
      const closeIdx = findCommandSubstitutionEnd(pattern, i + 2);
      if (closeIdx !== -1) {
        const commandStr = pattern.slice(i + 2, closeIdx);
        const output = await executeCommandSubstitutionFromString(ctx, commandStr);
        result2 += output;
        i = closeIdx + 1;
        continue;
      }
    }
    if (c === "`") {
      const closeIdx = pattern.indexOf("`", i + 1);
      if (closeIdx !== -1) {
        const commandStr = pattern.slice(i + 1, closeIdx);
        const output = await executeCommandSubstitutionFromString(ctx, commandStr);
        result2 += output;
        i = closeIdx + 1;
        continue;
      }
    }
    if (c === "$") {
      if (i + 1 < pattern.length) {
        const next = pattern[i + 1];
        if (next === "{") {
          const closeIdx = pattern.indexOf("}", i + 2);
          if (closeIdx !== -1) {
            const varName = pattern.slice(i + 2, closeIdx);
            result2 += ctx.state.env[varName] ?? "";
            i = closeIdx + 1;
            continue;
          }
        } else if (/[a-zA-Z_]/.test(next)) {
          let end = i + 1;
          while (end < pattern.length && /[a-zA-Z0-9_]/.test(pattern[end])) {
            end++;
          }
          const varName = pattern.slice(i + 1, end);
          result2 += ctx.state.env[varName] ?? "";
          i = end;
          continue;
        }
      }
    }
    if (c === "\\" && i + 1 < pattern.length) {
      result2 += c + pattern[i + 1];
      i += 2;
      continue;
    }
    result2 += c;
    i++;
  }
  return result2;
}
__name$1(expandVariablesInPatternAsync, "expandVariablesInPatternAsync");
async function expandVariablesInDoubleQuotedPatternAsync(ctx, content) {
  let result2 = "";
  let i = 0;
  while (i < content.length) {
    const c = content[i];
    if (c === "\\" && i + 1 < content.length) {
      const next = content[i + 1];
      if (next === "$" || next === "`" || next === "\\" || next === '"') {
        result2 += next;
        i += 2;
        continue;
      }
      result2 += c;
      i++;
      continue;
    }
    if (c === "$" && i + 1 < content.length && content[i + 1] === "(") {
      const closeIdx = findCommandSubstitutionEnd(content, i + 2);
      if (closeIdx !== -1) {
        const commandStr = content.slice(i + 2, closeIdx);
        const output = await executeCommandSubstitutionFromString(ctx, commandStr);
        result2 += output;
        i = closeIdx + 1;
        continue;
      }
    }
    if (c === "`") {
      const closeIdx = content.indexOf("`", i + 1);
      if (closeIdx !== -1) {
        const commandStr = content.slice(i + 1, closeIdx);
        const output = await executeCommandSubstitutionFromString(ctx, commandStr);
        result2 += output;
        i = closeIdx + 1;
        continue;
      }
    }
    if (c === "$") {
      if (i + 1 < content.length) {
        const next = content[i + 1];
        if (next === "{") {
          const closeIdx = content.indexOf("}", i + 2);
          if (closeIdx !== -1) {
            const varName = content.slice(i + 2, closeIdx);
            result2 += ctx.state.env[varName] ?? "";
            i = closeIdx + 1;
            continue;
          }
        } else if (/[a-zA-Z_]/.test(next)) {
          let end = i + 1;
          while (end < content.length && /[a-zA-Z0-9_]/.test(content[end])) {
            end++;
          }
          const varName = content.slice(i + 1, end);
          result2 += ctx.state.env[varName] ?? "";
          i = end;
          continue;
        }
      }
    }
    result2 += c;
    i++;
  }
  return result2;
}
__name$1(expandVariablesInDoubleQuotedPatternAsync, "expandVariablesInDoubleQuotedPatternAsync");
function applyTildeExpansion(ctx, value) {
  if (!value.startsWith("~")) {
    return value;
  }
  const home = ctx.state.env.HOME !== void 0 ? ctx.state.env.HOME : "/home/user";
  if (value === "~" || value.startsWith("~/")) {
    return home + value.slice(1);
  }
  let i = 1;
  while (i < value.length && /[a-zA-Z0-9_-]/.test(value[i])) {
    i++;
  }
  const username = value.slice(1, i);
  const rest = value.slice(i);
  if (rest !== "" && !rest.startsWith("/")) {
    return value;
  }
  if (username === "root") {
    return `/root${rest}`;
  }
  return value;
}
__name$1(applyTildeExpansion, "applyTildeExpansion");
async function buildPatternRegex(ctx, pattern, expandWordPartsAsync2, expandPart2) {
  let regex = "";
  for (const part of pattern.parts) {
    if (part.type === "Glob") {
      regex += patternToRegex(part.pattern, true, ctx.state.shoptOptions.extglob);
    } else if (part.type === "Literal") {
      regex += patternToRegex(part.value, true, ctx.state.shoptOptions.extglob);
    } else if (part.type === "SingleQuoted" || part.type === "Escaped") {
      regex += escapeRegex(part.value);
    } else if (part.type === "DoubleQuoted") {
      const expanded = await expandWordPartsAsync2(ctx, part.parts);
      regex += escapeRegex(expanded);
    } else if (part.type === "ParameterExpansion") {
      const expanded = await expandPart2(ctx, part);
      regex += patternToRegex(expanded, true, ctx.state.shoptOptions.extglob);
    } else {
      const expanded = await expandPart2(ctx, part);
      regex += escapeRegex(expanded);
    }
  }
  return regex;
}
__name$1(buildPatternRegex, "buildPatternRegex");
async function handleArrayPatternReplacement(ctx, wordParts, expandWordPartsAsync2, expandPart2) {
  if (wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  if (dqPart.parts.length !== 1 || dqPart.parts[0].type !== "ParameterExpansion" || dqPart.parts[0].operation?.type !== "PatternReplacement") {
    return null;
  }
  const paramPart = dqPart.parts[0];
  const arrayMatch = paramPart.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
  if (!arrayMatch) {
    return null;
  }
  const arrayName = arrayMatch[1];
  const isStar = arrayMatch[2] === "*";
  const operation = paramPart.operation;
  const elements = getArrayElements(ctx, arrayName);
  const values = elements.map(([, v]) => v);
  if (elements.length === 0) {
    const scalarValue = ctx.state.env[arrayName];
    if (scalarValue !== void 0) {
      values.push(scalarValue);
    }
  }
  if (values.length === 0) {
    return { values: [], quoted: true };
  }
  let regex = "";
  if (operation.pattern) {
    regex = await buildPatternRegex(ctx, operation.pattern, expandWordPartsAsync2, expandPart2);
  }
  const replacement = operation.replacement ? await expandWordPartsAsync2(ctx, operation.replacement.parts) : "";
  let regexPattern = regex;
  if (operation.anchor === "start") {
    regexPattern = `^${regex}`;
  } else if (operation.anchor === "end") {
    regexPattern = `${regex}$`;
  }
  const replacedValues = [];
  try {
    const re = new RegExp(regexPattern, operation.all ? "g" : "");
    for (const value of values) {
      replacedValues.push(value.replace(re, replacement));
    }
  } catch {
    replacedValues.push(...values);
  }
  if (isStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    return { values: [replacedValues.join(ifsSep)], quoted: true };
  }
  return { values: replacedValues, quoted: true };
}
__name$1(handleArrayPatternReplacement, "handleArrayPatternReplacement");
async function handleArrayPatternRemoval(ctx, wordParts, expandWordPartsAsync2, expandPart2) {
  if (wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  if (dqPart.parts.length !== 1 || dqPart.parts[0].type !== "ParameterExpansion" || dqPart.parts[0].operation?.type !== "PatternRemoval") {
    return null;
  }
  const paramPart = dqPart.parts[0];
  const arrayMatch = paramPart.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
  if (!arrayMatch) {
    return null;
  }
  const arrayName = arrayMatch[1];
  const isStar = arrayMatch[2] === "*";
  const operation = paramPart.operation;
  const elements = getArrayElements(ctx, arrayName);
  const values = elements.map(([, v]) => v);
  if (elements.length === 0) {
    const scalarValue = ctx.state.env[arrayName];
    if (scalarValue !== void 0) {
      values.push(scalarValue);
    }
  }
  if (values.length === 0) {
    return { values: [], quoted: true };
  }
  let regexStr = "";
  const extglob = ctx.state.shoptOptions.extglob;
  if (operation.pattern) {
    for (const part of operation.pattern.parts) {
      if (part.type === "Glob") {
        regexStr += patternToRegex(part.pattern, operation.greedy, extglob);
      } else if (part.type === "Literal") {
        regexStr += patternToRegex(part.value, operation.greedy, extglob);
      } else if (part.type === "SingleQuoted" || part.type === "Escaped") {
        regexStr += escapeRegex(part.value);
      } else if (part.type === "DoubleQuoted") {
        const expanded = await expandWordPartsAsync2(ctx, part.parts);
        regexStr += escapeRegex(expanded);
      } else if (part.type === "ParameterExpansion") {
        const expanded = await expandPart2(ctx, part);
        regexStr += patternToRegex(expanded, operation.greedy, extglob);
      } else {
        const expanded = await expandPart2(ctx, part);
        regexStr += escapeRegex(expanded);
      }
    }
  }
  const resultValues = [];
  for (const value of values) {
    resultValues.push(applyPatternRemoval(value, regexStr, operation.side, operation.greedy));
  }
  if (isStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    return { values: [resultValues.join(ifsSep)], quoted: true };
  }
  return { values: resultValues, quoted: true };
}
__name$1(handleArrayPatternRemoval, "handleArrayPatternRemoval");
async function handleArrayDefaultValue(ctx, wordParts) {
  if (wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  if (dqPart.parts.length !== 1 || dqPart.parts[0].type !== "ParameterExpansion" || dqPart.parts[0].operation?.type !== "DefaultValue" && dqPart.parts[0].operation?.type !== "UseAlternative" && dqPart.parts[0].operation?.type !== "AssignDefault") {
    return null;
  }
  const paramPart = dqPart.parts[0];
  const op = paramPart.operation;
  const arrayMatch = paramPart.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
  let shouldUseAlternate;
  let outerIsStar = false;
  if (arrayMatch) {
    const arrayName = arrayMatch[1];
    outerIsStar = arrayMatch[2] === "*";
    const elements = getArrayElements(ctx, arrayName);
    const isSet = elements.length > 0 || ctx.state.env[arrayName] !== void 0;
    const isEmpty = elements.length === 0 || elements.length === 1 && elements.every(([, v]) => v === "");
    const checkEmpty = op.checkEmpty ?? false;
    if (op.type === "UseAlternative") {
      shouldUseAlternate = isSet && !(checkEmpty && isEmpty);
    } else {
      shouldUseAlternate = !isSet || checkEmpty && isEmpty;
    }
    if (!shouldUseAlternate) {
      if (elements.length > 0) {
        const values = elements.map(([, v]) => v);
        if (outerIsStar) {
          const ifsSep = getIfsSeparator(ctx.state.env);
          return { values: [values.join(ifsSep)], quoted: true };
        }
        return { values, quoted: true };
      }
      const scalarValue = ctx.state.env[arrayName];
      if (scalarValue !== void 0) {
        return { values: [scalarValue], quoted: true };
      }
      return { values: [], quoted: true };
    }
  } else {
    const varName = paramPart.parameter;
    const isSet = await isVariableSet(ctx, varName);
    const varValue = await getVariable(ctx, varName);
    const isEmpty = varValue === "";
    const checkEmpty = op.checkEmpty ?? false;
    if (op.type === "UseAlternative") {
      shouldUseAlternate = isSet && !(checkEmpty && isEmpty);
    } else {
      shouldUseAlternate = !isSet || checkEmpty && isEmpty;
    }
    if (!shouldUseAlternate) {
      return { values: [varValue], quoted: true };
    }
  }
  if (shouldUseAlternate && op.word) {
    const opWordParts = op.word.parts;
    let defaultArrayName = null;
    let defaultIsStar = false;
    for (const part of opWordParts) {
      if (part.type === "ParameterExpansion" && !part.operation) {
        const defaultMatch = part.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
        if (defaultMatch) {
          defaultArrayName = defaultMatch[1];
          defaultIsStar = defaultMatch[2] === "*";
          break;
        }
      }
    }
    if (defaultArrayName) {
      const defaultElements = getArrayElements(ctx, defaultArrayName);
      if (defaultElements.length > 0) {
        const values = defaultElements.map(([, v]) => v);
        if (defaultIsStar || outerIsStar) {
          const ifsSep = getIfsSeparator(ctx.state.env);
          return { values: [values.join(ifsSep)], quoted: true };
        }
        return { values, quoted: true };
      }
      const scalarValue = ctx.state.env[defaultArrayName];
      if (scalarValue !== void 0) {
        return { values: [scalarValue], quoted: true };
      }
      return { values: [], quoted: true };
    }
  }
  return null;
}
__name$1(handleArrayDefaultValue, "handleArrayDefaultValue");
async function handleArrayPatternWithPrefixSuffix(ctx, wordParts, hasArrayAtExpansion, expandPart2, expandWordPartsAsync2) {
  if (!hasArrayAtExpansion || wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  let arrayAtIndex = -1;
  let arrayName = "";
  let isStar = false;
  let arrayOperation = null;
  for (let i = 0; i < dqPart.parts.length; i++) {
    const p = dqPart.parts[i];
    if (p.type === "ParameterExpansion" && (p.operation?.type === "PatternRemoval" || p.operation?.type === "PatternReplacement")) {
      const match = p.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
      if (match) {
        arrayAtIndex = i;
        arrayName = match[1];
        isStar = match[2] === "*";
        arrayOperation = p.operation;
        break;
      }
    }
  }
  if (arrayAtIndex === -1 || arrayAtIndex === 0 && arrayAtIndex === dqPart.parts.length - 1) {
    return null;
  }
  let prefix = "";
  for (let i = 0; i < arrayAtIndex; i++) {
    prefix += await expandPart2(ctx, dqPart.parts[i]);
  }
  let suffix = "";
  for (let i = arrayAtIndex + 1; i < dqPart.parts.length; i++) {
    suffix += await expandPart2(ctx, dqPart.parts[i]);
  }
  const elements = getArrayElements(ctx, arrayName);
  let values = elements.map(([, v]) => v);
  if (elements.length === 0) {
    const scalarValue = ctx.state.env[arrayName];
    if (scalarValue !== void 0) {
      values = [scalarValue];
    } else {
      if (isStar) {
        return { values: [prefix + suffix], quoted: true };
      }
      const combined = prefix + suffix;
      return { values: combined ? [combined] : [], quoted: true };
    }
  }
  if (arrayOperation?.type === "PatternRemoval") {
    const op = arrayOperation;
    let regexStr = "";
    const extglob = ctx.state.shoptOptions.extglob;
    if (op.pattern) {
      for (const part of op.pattern.parts) {
        if (part.type === "Glob") {
          regexStr += patternToRegex(part.pattern, op.greedy, extglob);
        } else if (part.type === "Literal") {
          regexStr += patternToRegex(part.value, op.greedy, extglob);
        } else if (part.type === "SingleQuoted" || part.type === "Escaped") {
          regexStr += escapeRegex(part.value);
        } else if (part.type === "DoubleQuoted") {
          const expanded = await expandWordPartsAsync2(ctx, part.parts);
          regexStr += escapeRegex(expanded);
        } else if (part.type === "ParameterExpansion") {
          const expanded = await expandPart2(ctx, part);
          regexStr += patternToRegex(expanded, op.greedy, extglob);
        } else {
          const expanded = await expandPart2(ctx, part);
          regexStr += escapeRegex(expanded);
        }
      }
    }
    values = values.map((value) => applyPatternRemoval(value, regexStr, op.side, op.greedy));
  } else if (arrayOperation?.type === "PatternReplacement") {
    const op = arrayOperation;
    let regex = "";
    if (op.pattern) {
      for (const part of op.pattern.parts) {
        if (part.type === "Glob") {
          regex += patternToRegex(part.pattern, true, ctx.state.shoptOptions.extglob);
        } else if (part.type === "Literal") {
          regex += patternToRegex(part.value, true, ctx.state.shoptOptions.extglob);
        } else if (part.type === "SingleQuoted" || part.type === "Escaped") {
          regex += escapeRegex(part.value);
        } else if (part.type === "DoubleQuoted") {
          const expanded = await expandWordPartsAsync2(ctx, part.parts);
          regex += escapeRegex(expanded);
        } else if (part.type === "ParameterExpansion") {
          const expanded = await expandPart2(ctx, part);
          regex += patternToRegex(expanded, true, ctx.state.shoptOptions.extglob);
        } else {
          const expanded = await expandPart2(ctx, part);
          regex += escapeRegex(expanded);
        }
      }
    }
    const replacement = op.replacement ? await expandWordPartsAsync2(ctx, op.replacement.parts) : "";
    let regexPattern = regex;
    if (op.anchor === "start") {
      regexPattern = `^${regex}`;
    } else if (op.anchor === "end") {
      regexPattern = `${regex}$`;
    }
    try {
      const re = new RegExp(regexPattern, op.all ? "g" : "");
      values = values.map((value) => value.replace(re, replacement));
    } catch {
    }
  }
  if (isStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    return {
      values: [prefix + values.join(ifsSep) + suffix],
      quoted: true
    };
  }
  if (values.length === 1) {
    return { values: [prefix + values[0] + suffix], quoted: true };
  }
  const result2 = [
    prefix + values[0],
    ...values.slice(1, -1),
    values[values.length - 1] + suffix
  ];
  return { values: result2, quoted: true };
}
__name$1(handleArrayPatternWithPrefixSuffix, "handleArrayPatternWithPrefixSuffix");
async function handleArrayWithPrefixSuffix(ctx, wordParts, hasArrayAtExpansion, expandPart2) {
  if (!hasArrayAtExpansion || wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  let arrayAtIndex = -1;
  let arrayName = "";
  let isStar = false;
  for (let i = 0; i < dqPart.parts.length; i++) {
    const p = dqPart.parts[i];
    if (p.type === "ParameterExpansion" && !p.operation) {
      const match = p.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
      if (match) {
        arrayAtIndex = i;
        arrayName = match[1];
        isStar = match[2] === "*";
        break;
      }
    }
  }
  if (arrayAtIndex === -1) {
    return null;
  }
  let prefix = "";
  for (let i = 0; i < arrayAtIndex; i++) {
    prefix += await expandPart2(ctx, dqPart.parts[i]);
  }
  let suffix = "";
  for (let i = arrayAtIndex + 1; i < dqPart.parts.length; i++) {
    suffix += await expandPart2(ctx, dqPart.parts[i]);
  }
  const elements = getArrayElements(ctx, arrayName);
  const values = elements.map(([, v]) => v);
  if (elements.length === 0) {
    const scalarValue = ctx.state.env[arrayName];
    if (scalarValue !== void 0) {
      return { values: [prefix + scalarValue + suffix], quoted: true };
    }
    if (isStar) {
      return { values: [prefix + suffix], quoted: true };
    }
    const combined = prefix + suffix;
    return { values: combined ? [combined] : [], quoted: true };
  }
  if (isStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    return {
      values: [prefix + values.join(ifsSep) + suffix],
      quoted: true
    };
  }
  if (values.length === 1) {
    return { values: [prefix + values[0] + suffix], quoted: true };
  }
  const result2 = [
    prefix + values[0],
    ...values.slice(1, -1),
    values[values.length - 1] + suffix
  ];
  return { values: result2, quoted: true };
}
__name$1(handleArrayWithPrefixSuffix, "handleArrayWithPrefixSuffix");
async function handleArraySlicing(ctx, wordParts, evaluateArithmetic2) {
  if (wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  if (dqPart.parts.length !== 1 || dqPart.parts[0].type !== "ParameterExpansion" || dqPart.parts[0].operation?.type !== "Substring") {
    return null;
  }
  const paramPart = dqPart.parts[0];
  const arrayMatch = paramPart.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
  if (!arrayMatch) {
    return null;
  }
  const arrayName = arrayMatch[1];
  const isStar = arrayMatch[2] === "*";
  const operation = paramPart.operation;
  if (ctx.state.associativeArrays?.has(arrayName)) {
    throw new ExitError(1, "", `bash: \${${arrayName}[@]: 0: 3}: bad substitution
`);
  }
  const offset = operation.offset ? await evaluateArithmetic2(ctx, operation.offset.expression) : 0;
  const length = operation.length ? await evaluateArithmetic2(ctx, operation.length.expression) : void 0;
  const elements = getArrayElements(ctx, arrayName);
  let startIdx = 0;
  if (offset < 0) {
    if (elements.length > 0) {
      const lastIdx = elements[elements.length - 1][0];
      const maxIndex = typeof lastIdx === "number" ? lastIdx : 0;
      const targetIndex = maxIndex + 1 + offset;
      if (targetIndex < 0) {
        return { values: [], quoted: true };
      }
      startIdx = elements.findIndex(([idx]) => typeof idx === "number" && idx >= targetIndex);
      if (startIdx < 0)
        startIdx = elements.length;
    }
  } else {
    startIdx = elements.findIndex(([idx]) => typeof idx === "number" && idx >= offset);
    if (startIdx < 0)
      startIdx = elements.length;
  }
  let slicedValues;
  if (length !== void 0) {
    if (length < 0) {
      throw new ArithmeticError(`${arrayName}[@]: substring expression < 0`);
    }
    slicedValues = elements.slice(startIdx, startIdx + length).map(([, v]) => v);
  } else {
    slicedValues = elements.slice(startIdx).map(([, v]) => v);
  }
  if (slicedValues.length === 0) {
    return { values: [], quoted: true };
  }
  if (isStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    return { values: [slicedValues.join(ifsSep)], quoted: true };
  }
  return { values: slicedValues, quoted: true };
}
__name$1(handleArraySlicing, "handleArraySlicing");
function handleArrayTransform(ctx, wordParts) {
  if (wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  if (dqPart.parts.length !== 1 || dqPart.parts[0].type !== "ParameterExpansion" || dqPart.parts[0].operation?.type !== "Transform") {
    return null;
  }
  const paramPart = dqPart.parts[0];
  const arrayMatch = paramPart.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
  if (!arrayMatch) {
    return null;
  }
  const arrayName = arrayMatch[1];
  const isStar = arrayMatch[2] === "*";
  const operation = paramPart.operation;
  const elements = getArrayElements(ctx, arrayName);
  if (elements.length === 0) {
    const scalarValue = ctx.state.env[arrayName];
    if (scalarValue !== void 0) {
      let resultValue;
      switch (operation.operator) {
        case "a":
          resultValue = "";
          break;
        case "P":
          resultValue = expandPrompt(ctx, scalarValue);
          break;
        case "Q":
          resultValue = quoteValue$1(scalarValue);
          break;
        default:
          resultValue = scalarValue;
      }
      return { values: [resultValue], quoted: true };
    }
    if (isStar) {
      return { values: [""], quoted: true };
    }
    return { values: [], quoted: true };
  }
  const arrayAttr = getVariableAttributes(ctx, arrayName);
  let transformedValues;
  switch (operation.operator) {
    case "a":
      transformedValues = elements.map(() => arrayAttr);
      break;
    case "P":
      transformedValues = elements.map(([, v]) => expandPrompt(ctx, v));
      break;
    case "Q":
      transformedValues = elements.map(([, v]) => quoteValue$1(v));
      break;
    case "u":
      transformedValues = elements.map(([, v]) => v.charAt(0).toUpperCase() + v.slice(1));
      break;
    case "U":
      transformedValues = elements.map(([, v]) => v.toUpperCase());
      break;
    case "L":
      transformedValues = elements.map(([, v]) => v.toLowerCase());
      break;
    default:
      transformedValues = elements.map(([, v]) => v);
  }
  if (isStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    return { values: [transformedValues.join(ifsSep)], quoted: true };
  }
  return { values: transformedValues, quoted: true };
}
__name$1(handleArrayTransform, "handleArrayTransform");
function handleSimpleArrayExpansion(ctx, wordParts) {
  if (wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  if (dqPart.parts.length !== 1 || dqPart.parts[0].type !== "ParameterExpansion") {
    return null;
  }
  const paramPart = dqPart.parts[0];
  if (paramPart.operation) {
    return null;
  }
  const arrayMatch = paramPart.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(@)\]$/);
  if (!arrayMatch) {
    return null;
  }
  const arrayName = arrayMatch[1];
  if (isNameref(ctx, arrayName)) {
    const target = getNamerefTarget(ctx, arrayName);
    if (target?.endsWith("[@]") || target?.endsWith("[*]")) {
      return { values: [], quoted: true };
    }
  }
  const elements = getArrayElements(ctx, arrayName);
  if (elements.length > 0) {
    return { values: elements.map(([, v]) => v), quoted: true };
  }
  const scalarValue = ctx.state.env[arrayName];
  if (scalarValue !== void 0) {
    return { values: [scalarValue], quoted: true };
  }
  return { values: [], quoted: true };
}
__name$1(handleSimpleArrayExpansion, "handleSimpleArrayExpansion");
function handleNamerefArrayExpansion(ctx, wordParts) {
  if (wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  if (dqPart.parts.length !== 1 || dqPart.parts[0].type !== "ParameterExpansion" || dqPart.parts[0].operation) {
    return null;
  }
  const paramPart = dqPart.parts[0];
  const varName = paramPart.parameter;
  if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(varName) || !isNameref(ctx, varName)) {
    return null;
  }
  const target = getNamerefTarget(ctx, varName);
  if (!target) {
    return null;
  }
  const targetArrayMatch = target.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(@)\]$/);
  if (!targetArrayMatch) {
    return null;
  }
  const arrayName = targetArrayMatch[1];
  const elements = getArrayElements(ctx, arrayName);
  if (elements.length > 0) {
    return { values: elements.map(([, v]) => v), quoted: true };
  }
  const scalarValue = ctx.state.env[arrayName];
  if (scalarValue !== void 0) {
    return { values: [scalarValue], quoted: true };
  }
  return { values: [], quoted: true };
}
__name$1(handleNamerefArrayExpansion, "handleNamerefArrayExpansion");
async function handleIndirectArrayExpansion(ctx, wordParts, hasIndirection, expandParameterAsync2, expandWordPartsAsync2) {
  if (!hasIndirection || wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  if (dqPart.parts.length !== 1 || dqPart.parts[0].type !== "ParameterExpansion" || dqPart.parts[0].operation?.type !== "Indirection") {
    return null;
  }
  const paramPart = dqPart.parts[0];
  const indirOp = paramPart.operation;
  const refValue = await getVariable(ctx, paramPart.parameter);
  const arrayMatch = refValue.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
  if (!arrayMatch) {
    if (!indirOp.innerOp) {
      if (refValue === "@" || refValue === "*") {
        const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
        const params = [];
        for (let i = 1; i <= numParams; i++) {
          params.push(ctx.state.env[String(i)] || "");
        }
        if (refValue === "*") {
          return {
            values: [params.join(getIfsSeparator(ctx.state.env))],
            quoted: true
          };
        }
        return { values: params, quoted: true };
      }
    }
    return null;
  }
  const arrayName = arrayMatch[1];
  const isStar = arrayMatch[2] === "*";
  const elements = getArrayElements(ctx, arrayName);
  if (indirOp.innerOp) {
    if (indirOp.innerOp.type === "Substring") {
      return handleIndirectArraySlicing(ctx, elements, arrayName, isStar, indirOp.innerOp);
    }
    if (indirOp.innerOp.type === "DefaultValue" || indirOp.innerOp.type === "UseAlternative" || indirOp.innerOp.type === "AssignDefault" || indirOp.innerOp.type === "ErrorIfUnset") {
      return handleIndirectArrayDefaultAlternative(ctx, elements, arrayName, isStar, indirOp.innerOp, expandWordPartsAsync2);
    }
    if (indirOp.innerOp.type === "Transform" && indirOp.innerOp.operator === "a") {
      const attrs = getVariableAttributes(ctx, arrayName);
      const values2 = elements.map(() => attrs);
      if (isStar) {
        return {
          values: [values2.join(getIfsSeparator(ctx.state.env))],
          quoted: true
        };
      }
      return { values: values2, quoted: true };
    }
    const values = [];
    for (const [, elemValue] of elements) {
      const syntheticPart = {
        type: "ParameterExpansion",
        parameter: "_indirect_elem_",
        operation: indirOp.innerOp
      };
      const oldVal = ctx.state.env._indirect_elem_;
      ctx.state.env._indirect_elem_ = elemValue;
      try {
        const result2 = await expandParameterAsync2(ctx, syntheticPart, true);
        values.push(result2);
      } finally {
        if (oldVal !== void 0) {
          ctx.state.env._indirect_elem_ = oldVal;
        } else {
          delete ctx.state.env._indirect_elem_;
        }
      }
    }
    if (isStar) {
      return {
        values: [values.join(getIfsSeparator(ctx.state.env))],
        quoted: true
      };
    }
    return { values, quoted: true };
  }
  if (elements.length > 0) {
    const values = elements.map(([, v]) => v);
    if (isStar) {
      return {
        values: [values.join(getIfsSeparator(ctx.state.env))],
        quoted: true
      };
    }
    return { values, quoted: true };
  }
  const scalarValue = ctx.state.env[arrayName];
  if (scalarValue !== void 0) {
    return { values: [scalarValue], quoted: true };
  }
  return { values: [], quoted: true };
}
__name$1(handleIndirectArrayExpansion, "handleIndirectArrayExpansion");
async function handleIndirectArraySlicing(ctx, elements, arrayName, isStar, innerOp) {
  const offset = innerOp.offset ? await evaluateArithmetic(ctx, innerOp.offset.expression) : 0;
  const length = innerOp.length ? await evaluateArithmetic(ctx, innerOp.length.expression) : void 0;
  let startIdx = 0;
  if (offset < 0) {
    if (elements.length > 0) {
      const lastIdx = elements[elements.length - 1][0];
      const maxIndex = typeof lastIdx === "number" ? lastIdx : 0;
      const targetIndex = maxIndex + 1 + offset;
      if (targetIndex < 0)
        return { values: [], quoted: true };
      startIdx = elements.findIndex(([idx]) => typeof idx === "number" && idx >= targetIndex);
      if (startIdx < 0)
        return { values: [], quoted: true };
    }
  } else {
    startIdx = elements.findIndex(([idx]) => typeof idx === "number" && idx >= offset);
    if (startIdx < 0)
      return { values: [], quoted: true };
  }
  let slicedElements;
  if (length !== void 0) {
    if (length < 0) {
      throw new ArithmeticError(`${arrayName}[@]: substring expression < 0`);
    }
    slicedElements = elements.slice(startIdx, startIdx + length);
  } else {
    slicedElements = elements.slice(startIdx);
  }
  const values = slicedElements.map(([, v]) => v);
  if (isStar) {
    return {
      values: [values.join(getIfsSeparator(ctx.state.env))],
      quoted: true
    };
  }
  return { values, quoted: true };
}
__name$1(handleIndirectArraySlicing, "handleIndirectArraySlicing");
async function handleIndirectArrayDefaultAlternative(ctx, elements, arrayName, isStar, innerOp, expandWordPartsAsync2) {
  const checkEmpty = innerOp.checkEmpty ?? false;
  const values = elements.map(([, v]) => v);
  const isEmpty = elements.length === 0;
  const isUnset = elements.length === 0;
  if (innerOp.type === "UseAlternative") {
    const shouldUseAlt = !isUnset && !(checkEmpty && isEmpty);
    if (shouldUseAlt && innerOp.word) {
      const altValue = await expandWordPartsAsync2(ctx, innerOp.word.parts, true);
      return { values: [altValue], quoted: true };
    }
    return { values: [], quoted: true };
  }
  if (innerOp.type === "DefaultValue") {
    const shouldUseDefault = isUnset || checkEmpty && isEmpty;
    if (shouldUseDefault && innerOp.word) {
      const defValue = await expandWordPartsAsync2(ctx, innerOp.word.parts, true);
      return { values: [defValue], quoted: true };
    }
    if (isStar) {
      return {
        values: [values.join(getIfsSeparator(ctx.state.env))],
        quoted: true
      };
    }
    return { values, quoted: true };
  }
  if (innerOp.type === "AssignDefault") {
    const shouldAssign = isUnset || checkEmpty && isEmpty;
    if (shouldAssign && innerOp.word) {
      const assignValue = await expandWordPartsAsync2(ctx, innerOp.word.parts, true);
      ctx.state.env[`${arrayName}_0`] = assignValue;
      ctx.state.env[`${arrayName}__length`] = "1";
      return { values: [assignValue], quoted: true };
    }
    if (isStar) {
      return {
        values: [values.join(getIfsSeparator(ctx.state.env))],
        quoted: true
      };
    }
    return { values, quoted: true };
  }
  if (isStar) {
    return {
      values: [values.join(getIfsSeparator(ctx.state.env))],
      quoted: true
    };
  }
  return { values, quoted: true };
}
__name$1(handleIndirectArrayDefaultAlternative, "handleIndirectArrayDefaultAlternative");
async function handleIndirectInAlternative(ctx, wordParts) {
  if (wordParts.length !== 1 || wordParts[0].type !== "ParameterExpansion" || wordParts[0].operation?.type !== "UseAlternative" && wordParts[0].operation?.type !== "DefaultValue") {
    return null;
  }
  const paramPart = wordParts[0];
  const op = paramPart.operation;
  const opWord = op?.word;
  if (!opWord || opWord.parts.length !== 1 || opWord.parts[0].type !== "DoubleQuoted") {
    return null;
  }
  const innerDq = opWord.parts[0];
  if (innerDq.parts.length !== 1 || innerDq.parts[0].type !== "ParameterExpansion" || innerDq.parts[0].operation?.type !== "Indirection") {
    return null;
  }
  const innerParam = innerDq.parts[0];
  const refValue = await getVariable(ctx, innerParam.parameter);
  const arrayMatch = refValue.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
  if (!arrayMatch) {
    return null;
  }
  const isSet = await isVariableSet(ctx, paramPart.parameter);
  const isEmpty = await getVariable(ctx, paramPart.parameter) === "";
  const checkEmpty = op.checkEmpty ?? false;
  let shouldExpand;
  if (op.type === "UseAlternative") {
    shouldExpand = isSet && !(checkEmpty && isEmpty);
  } else {
    shouldExpand = !isSet || checkEmpty && isEmpty;
  }
  if (shouldExpand) {
    const arrayName = arrayMatch[1];
    const isStar = arrayMatch[2] === "*";
    const elements = getArrayElements(ctx, arrayName);
    if (elements.length > 0) {
      const values = elements.map(([, v]) => v);
      if (isStar) {
        return {
          values: [values.join(getIfsSeparator(ctx.state.env))],
          quoted: true
        };
      }
      return { values, quoted: true };
    }
    const scalarValue = ctx.state.env[arrayName];
    if (scalarValue !== void 0) {
      return { values: [scalarValue], quoted: true };
    }
    return { values: [], quoted: true };
  }
  return { values: [], quoted: false };
}
__name$1(handleIndirectInAlternative, "handleIndirectInAlternative");
async function handleIndirectionWithInnerAlternative(ctx, wordParts) {
  if (wordParts.length !== 1 || wordParts[0].type !== "ParameterExpansion" || wordParts[0].operation?.type !== "Indirection") {
    return null;
  }
  const paramPart = wordParts[0];
  const indirOp = paramPart.operation;
  const innerOp = indirOp.innerOp;
  if (!innerOp || innerOp.type !== "UseAlternative" && innerOp.type !== "DefaultValue") {
    return null;
  }
  const opWord = innerOp.word;
  if (!opWord || opWord.parts.length !== 1 || opWord.parts[0].type !== "DoubleQuoted") {
    return null;
  }
  const innerDq = opWord.parts[0];
  if (innerDq.parts.length !== 1 || innerDq.parts[0].type !== "ParameterExpansion" || innerDq.parts[0].operation?.type !== "Indirection") {
    return null;
  }
  const innerParam = innerDq.parts[0];
  const refValue = await getVariable(ctx, innerParam.parameter);
  const arrayMatch = refValue.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
  if (!arrayMatch) {
    return null;
  }
  const outerRefValue = await getVariable(ctx, paramPart.parameter);
  const isSet = await isVariableSet(ctx, paramPart.parameter);
  const isEmpty = outerRefValue === "";
  const checkEmpty = innerOp.checkEmpty ?? false;
  let shouldExpand;
  if (innerOp.type === "UseAlternative") {
    shouldExpand = isSet && !(checkEmpty && isEmpty);
  } else {
    shouldExpand = !isSet || checkEmpty && isEmpty;
  }
  if (shouldExpand) {
    const arrayName = arrayMatch[1];
    const isStar = arrayMatch[2] === "*";
    const elements = getArrayElements(ctx, arrayName);
    if (elements.length > 0) {
      const values = elements.map(([, v]) => v);
      if (isStar) {
        return {
          values: [values.join(getIfsSeparator(ctx.state.env))],
          quoted: true
        };
      }
      return { values, quoted: true };
    }
    const scalarValue = ctx.state.env[arrayName];
    if (scalarValue !== void 0) {
      return { values: [scalarValue], quoted: true };
    }
    return { values: [], quoted: true };
  }
  return { values: [], quoted: false };
}
__name$1(handleIndirectionWithInnerAlternative, "handleIndirectionWithInnerAlternative");
function getPositionalParams(ctx) {
  const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
  const params = [];
  for (let i = 1; i <= numParams; i++) {
    params.push(ctx.state.env[String(i)] || "");
  }
  return params;
}
__name$1(getPositionalParams, "getPositionalParams");
async function handlePositionalSlicing(ctx, wordParts, evaluateArithmetic2, expandPart2) {
  if (wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  let sliceAtIndex = -1;
  let sliceIsStar = false;
  for (let i = 0; i < dqPart.parts.length; i++) {
    const p = dqPart.parts[i];
    if (p.type === "ParameterExpansion" && (p.parameter === "@" || p.parameter === "*") && p.operation?.type === "Substring") {
      sliceAtIndex = i;
      sliceIsStar = p.parameter === "*";
      break;
    }
  }
  if (sliceAtIndex === -1) {
    return null;
  }
  const paramPart = dqPart.parts[sliceAtIndex];
  const operation = paramPart.operation;
  const offset = operation.offset ? await evaluateArithmetic2(ctx, operation.offset.expression) : 0;
  const length = operation.length ? await evaluateArithmetic2(ctx, operation.length.expression) : void 0;
  const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
  const allParams = [];
  for (let i = 1; i <= numParams; i++) {
    allParams.push(ctx.state.env[String(i)] || "");
  }
  const shellName = ctx.state.env["0"] || "bash";
  let slicedParams;
  if (offset <= 0) {
    const withZero = [shellName, ...allParams];
    const computedIdx = withZero.length + offset;
    if (computedIdx < 0) {
      slicedParams = [];
    } else {
      const startIdx = offset < 0 ? computedIdx : 0;
      if (length !== void 0) {
        const endIdx = length < 0 ? withZero.length + length : startIdx + length;
        slicedParams = withZero.slice(startIdx, Math.max(startIdx, endIdx));
      } else {
        slicedParams = withZero.slice(startIdx);
      }
    }
  } else {
    const startIdx = offset - 1;
    if (startIdx >= allParams.length) {
      slicedParams = [];
    } else if (length !== void 0) {
      const endIdx = length < 0 ? allParams.length + length : startIdx + length;
      slicedParams = allParams.slice(startIdx, Math.max(startIdx, endIdx));
    } else {
      slicedParams = allParams.slice(startIdx);
    }
  }
  let prefix = "";
  for (let i = 0; i < sliceAtIndex; i++) {
    prefix += await expandPart2(ctx, dqPart.parts[i]);
  }
  let suffix = "";
  for (let i = sliceAtIndex + 1; i < dqPart.parts.length; i++) {
    suffix += await expandPart2(ctx, dqPart.parts[i]);
  }
  if (slicedParams.length === 0) {
    const combined = prefix + suffix;
    return { values: combined ? [combined] : [], quoted: true };
  }
  if (sliceIsStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    return {
      values: [prefix + slicedParams.join(ifsSep) + suffix],
      quoted: true
    };
  }
  if (slicedParams.length === 1) {
    return {
      values: [prefix + slicedParams[0] + suffix],
      quoted: true
    };
  }
  const result2 = [
    prefix + slicedParams[0],
    ...slicedParams.slice(1, -1),
    slicedParams[slicedParams.length - 1] + suffix
  ];
  return { values: result2, quoted: true };
}
__name$1(handlePositionalSlicing, "handlePositionalSlicing");
async function handlePositionalPatternReplacement(ctx, wordParts, expandPart2, expandWordPartsAsync2) {
  if (wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  let patReplAtIndex = -1;
  let patReplIsStar = false;
  for (let i = 0; i < dqPart.parts.length; i++) {
    const p = dqPart.parts[i];
    if (p.type === "ParameterExpansion" && (p.parameter === "@" || p.parameter === "*") && p.operation?.type === "PatternReplacement") {
      patReplAtIndex = i;
      patReplIsStar = p.parameter === "*";
      break;
    }
  }
  if (patReplAtIndex === -1) {
    return null;
  }
  const paramPart = dqPart.parts[patReplAtIndex];
  const operation = paramPart.operation;
  const params = getPositionalParams(ctx);
  let prefix = "";
  for (let i = 0; i < patReplAtIndex; i++) {
    prefix += await expandPart2(ctx, dqPart.parts[i]);
  }
  let suffix = "";
  for (let i = patReplAtIndex + 1; i < dqPart.parts.length; i++) {
    suffix += await expandPart2(ctx, dqPart.parts[i]);
  }
  if (params.length === 0) {
    const combined = prefix + suffix;
    return { values: combined ? [combined] : [], quoted: true };
  }
  let regex = "";
  if (operation.pattern) {
    for (const part of operation.pattern.parts) {
      if (part.type === "Glob") {
        regex += patternToRegex(part.pattern, true, ctx.state.shoptOptions.extglob);
      } else if (part.type === "Literal") {
        regex += patternToRegex(part.value, true, ctx.state.shoptOptions.extglob);
      } else if (part.type === "SingleQuoted" || part.type === "Escaped") {
        regex += escapeRegex(part.value);
      } else if (part.type === "DoubleQuoted") {
        const expanded = await expandWordPartsAsync2(ctx, part.parts);
        regex += escapeRegex(expanded);
      } else if (part.type === "ParameterExpansion") {
        const expanded = await expandPart2(ctx, part);
        regex += patternToRegex(expanded, true, ctx.state.shoptOptions.extglob);
      } else {
        const expanded = await expandPart2(ctx, part);
        regex += escapeRegex(expanded);
      }
    }
  }
  const replacement = operation.replacement ? await expandWordPartsAsync2(ctx, operation.replacement.parts) : "";
  let regexPattern = regex;
  if (operation.anchor === "start") {
    regexPattern = `^${regex}`;
  } else if (operation.anchor === "end") {
    regexPattern = `${regex}$`;
  }
  const replacedParams = [];
  try {
    const re = new RegExp(regexPattern, operation.all ? "g" : "");
    for (const param of params) {
      replacedParams.push(param.replace(re, replacement));
    }
  } catch {
    replacedParams.push(...params);
  }
  if (patReplIsStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    return {
      values: [prefix + replacedParams.join(ifsSep) + suffix],
      quoted: true
    };
  }
  if (replacedParams.length === 1) {
    return {
      values: [prefix + replacedParams[0] + suffix],
      quoted: true
    };
  }
  const result2 = [
    prefix + replacedParams[0],
    ...replacedParams.slice(1, -1),
    replacedParams[replacedParams.length - 1] + suffix
  ];
  return { values: result2, quoted: true };
}
__name$1(handlePositionalPatternReplacement, "handlePositionalPatternReplacement");
async function handlePositionalPatternRemoval(ctx, wordParts, expandPart2, expandWordPartsAsync2) {
  if (wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  let patRemAtIndex = -1;
  let patRemIsStar = false;
  for (let i = 0; i < dqPart.parts.length; i++) {
    const p = dqPart.parts[i];
    if (p.type === "ParameterExpansion" && (p.parameter === "@" || p.parameter === "*") && p.operation?.type === "PatternRemoval") {
      patRemAtIndex = i;
      patRemIsStar = p.parameter === "*";
      break;
    }
  }
  if (patRemAtIndex === -1) {
    return null;
  }
  const paramPart = dqPart.parts[patRemAtIndex];
  const operation = paramPart.operation;
  const params = getPositionalParams(ctx);
  let prefix = "";
  for (let i = 0; i < patRemAtIndex; i++) {
    prefix += await expandPart2(ctx, dqPart.parts[i]);
  }
  let suffix = "";
  for (let i = patRemAtIndex + 1; i < dqPart.parts.length; i++) {
    suffix += await expandPart2(ctx, dqPart.parts[i]);
  }
  if (params.length === 0) {
    const combined = prefix + suffix;
    return { values: combined ? [combined] : [], quoted: true };
  }
  let regexStr = "";
  const extglob = ctx.state.shoptOptions.extglob;
  if (operation.pattern) {
    for (const part of operation.pattern.parts) {
      if (part.type === "Glob") {
        regexStr += patternToRegex(part.pattern, operation.greedy, extglob);
      } else if (part.type === "Literal") {
        regexStr += patternToRegex(part.value, operation.greedy, extglob);
      } else if (part.type === "SingleQuoted" || part.type === "Escaped") {
        regexStr += escapeRegex(part.value);
      } else if (part.type === "DoubleQuoted") {
        const expanded = await expandWordPartsAsync2(ctx, part.parts);
        regexStr += escapeRegex(expanded);
      } else if (part.type === "ParameterExpansion") {
        const expanded = await expandPart2(ctx, part);
        regexStr += patternToRegex(expanded, operation.greedy, extglob);
      } else {
        const expanded = await expandPart2(ctx, part);
        regexStr += escapeRegex(expanded);
      }
    }
  }
  const strippedParams = [];
  for (const param of params) {
    strippedParams.push(applyPatternRemoval(param, regexStr, operation.side, operation.greedy));
  }
  if (patRemIsStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    return {
      values: [prefix + strippedParams.join(ifsSep) + suffix],
      quoted: true
    };
  }
  if (strippedParams.length === 1) {
    return {
      values: [prefix + strippedParams[0] + suffix],
      quoted: true
    };
  }
  const result2 = [
    prefix + strippedParams[0],
    ...strippedParams.slice(1, -1),
    strippedParams[strippedParams.length - 1] + suffix
  ];
  return { values: result2, quoted: true };
}
__name$1(handlePositionalPatternRemoval, "handlePositionalPatternRemoval");
async function handleSimplePositionalExpansion(ctx, wordParts, expandPart2) {
  if (wordParts.length !== 1 || wordParts[0].type !== "DoubleQuoted") {
    return null;
  }
  const dqPart = wordParts[0];
  let atIndex = -1;
  let isStar = false;
  for (let i = 0; i < dqPart.parts.length; i++) {
    const p = dqPart.parts[i];
    if (p.type === "ParameterExpansion" && (p.parameter === "@" || p.parameter === "*")) {
      atIndex = i;
      isStar = p.parameter === "*";
      break;
    }
  }
  if (atIndex === -1) {
    return null;
  }
  const paramPart = dqPart.parts[atIndex];
  if (paramPart.type === "ParameterExpansion" && paramPart.operation) {
    return null;
  }
  const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
  let prefix = "";
  for (let i = 0; i < atIndex; i++) {
    prefix += await expandPart2(ctx, dqPart.parts[i]);
  }
  let suffix = "";
  for (let i = atIndex + 1; i < dqPart.parts.length; i++) {
    suffix += await expandPart2(ctx, dqPart.parts[i]);
  }
  if (numParams === 0) {
    if (isStar) {
      return { values: [prefix + suffix], quoted: true };
    }
    const combined = prefix + suffix;
    return { values: combined ? [combined] : [], quoted: true };
  }
  const params = [];
  for (let i = 1; i <= numParams; i++) {
    params.push(ctx.state.env[String(i)] || "");
  }
  if (isStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    return {
      values: [prefix + params.join(ifsSep) + suffix],
      quoted: true
    };
  }
  if (params.length === 1) {
    return { values: [prefix + params[0] + suffix], quoted: true };
  }
  const result2 = [
    prefix + params[0],
    ...params.slice(1, -1),
    params[params.length - 1] + suffix
  ];
  return { values: result2, quoted: true };
}
__name$1(handleSimplePositionalExpansion, "handleSimplePositionalExpansion");
function createGlobExpander(ctx) {
  return new GlobExpander(ctx.fs, ctx.state.cwd, ctx.state.env, {
    globstar: ctx.state.shoptOptions.globstar,
    nullglob: ctx.state.shoptOptions.nullglob,
    failglob: ctx.state.shoptOptions.failglob,
    dotglob: ctx.state.shoptOptions.dotglob,
    extglob: ctx.state.shoptOptions.extglob,
    globskipdots: ctx.state.shoptOptions.globskipdots
  });
}
__name$1(createGlobExpander, "createGlobExpander");
async function applyGlobExpansion(ctx, words) {
  if (ctx.state.options.noglob) {
    return words;
  }
  const globExpander = createGlobExpander(ctx);
  const expandedValues = [];
  for (const w of words) {
    if (hasGlobPattern(w, ctx.state.shoptOptions.extglob)) {
      const matches = await globExpander.expand(w);
      if (matches.length > 0) {
        expandedValues.push(...matches);
      } else if (globExpander.hasFailglob()) {
        throw new GlobError(w);
      } else if (globExpander.hasNullglob()) ;
      else {
        expandedValues.push(w);
      }
    } else {
      expandedValues.push(w);
    }
  }
  return expandedValues;
}
__name$1(applyGlobExpansion, "applyGlobExpansion");
async function handleUnquotedArrayPatternReplacement(ctx, wordParts, expandWordPartsAsync2, expandPart2) {
  let unquotedArrayPatReplIdx = -1;
  let unquotedArrayName = "";
  let unquotedArrayIsStar = false;
  for (let i = 0; i < wordParts.length; i++) {
    const p = wordParts[i];
    if (p.type === "ParameterExpansion" && p.operation?.type === "PatternReplacement") {
      const arrayMatch = p.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
      if (arrayMatch) {
        unquotedArrayPatReplIdx = i;
        unquotedArrayName = arrayMatch[1];
        unquotedArrayIsStar = arrayMatch[2] === "*";
        break;
      }
    }
  }
  if (unquotedArrayPatReplIdx === -1) {
    return null;
  }
  const paramPart = wordParts[unquotedArrayPatReplIdx];
  const operation = paramPart.operation;
  const elements = getArrayElements(ctx, unquotedArrayName);
  let values = elements.map(([, v]) => v);
  if (elements.length === 0) {
    const scalarValue = ctx.state.env[unquotedArrayName];
    if (scalarValue !== void 0) {
      values = [scalarValue];
    }
  }
  if (values.length === 0) {
    return { values: [], quoted: false };
  }
  let regex = "";
  if (operation.pattern) {
    for (const part of operation.pattern.parts) {
      if (part.type === "Glob") {
        regex += patternToRegex(part.pattern, true, ctx.state.shoptOptions.extglob);
      } else if (part.type === "Literal") {
        regex += patternToRegex(part.value, true, ctx.state.shoptOptions.extglob);
      } else if (part.type === "SingleQuoted" || part.type === "Escaped") {
        regex += escapeRegex(part.value);
      } else if (part.type === "DoubleQuoted") {
        const expanded = await expandWordPartsAsync2(ctx, part.parts);
        regex += escapeRegex(expanded);
      } else if (part.type === "ParameterExpansion") {
        const expanded = await expandPart2(ctx, part);
        regex += patternToRegex(expanded, true, ctx.state.shoptOptions.extglob);
      } else {
        const expanded = await expandPart2(ctx, part);
        regex += escapeRegex(expanded);
      }
    }
  }
  const replacement = operation.replacement ? await expandWordPartsAsync2(ctx, operation.replacement.parts) : "";
  let regexPattern = regex;
  if (operation.anchor === "start") {
    regexPattern = `^${regex}`;
  } else if (operation.anchor === "end") {
    regexPattern = `${regex}$`;
  }
  const replacedValues = [];
  try {
    const re = new RegExp(regexPattern, operation.all ? "g" : "");
    for (const value of values) {
      replacedValues.push(value.replace(re, replacement));
    }
  } catch {
    replacedValues.push(...values);
  }
  const ifsChars = getIfs(ctx.state.env);
  const ifsEmpty = isIfsEmpty(ctx.state.env);
  if (unquotedArrayIsStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    const joined = replacedValues.join(ifsSep);
    if (ifsEmpty) {
      return { values: joined ? [joined] : [], quoted: false };
    }
    return {
      values: splitByIfsForExpansion(joined, ifsChars),
      quoted: false
    };
  }
  if (ifsEmpty) {
    return { values: replacedValues, quoted: false };
  }
  const allWords = [];
  for (const val of replacedValues) {
    if (val === "") {
      allWords.push("");
    } else {
      allWords.push(...splitByIfsForExpansion(val, ifsChars));
    }
  }
  return { values: allWords, quoted: false };
}
__name$1(handleUnquotedArrayPatternReplacement, "handleUnquotedArrayPatternReplacement");
async function handleUnquotedArrayPatternRemoval(ctx, wordParts, expandWordPartsAsync2, expandPart2) {
  let unquotedArrayPatRemIdx = -1;
  let unquotedArrayName = "";
  let unquotedArrayIsStar = false;
  for (let i = 0; i < wordParts.length; i++) {
    const p = wordParts[i];
    if (p.type === "ParameterExpansion" && p.operation?.type === "PatternRemoval") {
      const arrayMatch = p.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
      if (arrayMatch) {
        unquotedArrayPatRemIdx = i;
        unquotedArrayName = arrayMatch[1];
        unquotedArrayIsStar = arrayMatch[2] === "*";
        break;
      }
    }
  }
  if (unquotedArrayPatRemIdx === -1) {
    return null;
  }
  const paramPart = wordParts[unquotedArrayPatRemIdx];
  const operation = paramPart.operation;
  const elements = getArrayElements(ctx, unquotedArrayName);
  let values = elements.map(([, v]) => v);
  if (elements.length === 0) {
    const scalarValue = ctx.state.env[unquotedArrayName];
    if (scalarValue !== void 0) {
      values = [scalarValue];
    }
  }
  if (values.length === 0) {
    return { values: [], quoted: false };
  }
  let regexStr = "";
  const extglob = ctx.state.shoptOptions.extglob;
  if (operation.pattern) {
    for (const part of operation.pattern.parts) {
      if (part.type === "Glob") {
        regexStr += patternToRegex(part.pattern, operation.greedy, extglob);
      } else if (part.type === "Literal") {
        regexStr += patternToRegex(part.value, operation.greedy, extglob);
      } else if (part.type === "SingleQuoted" || part.type === "Escaped") {
        regexStr += escapeRegex(part.value);
      } else if (part.type === "DoubleQuoted") {
        const expanded = await expandWordPartsAsync2(ctx, part.parts);
        regexStr += escapeRegex(expanded);
      } else if (part.type === "ParameterExpansion") {
        const expanded = await expandPart2(ctx, part);
        regexStr += patternToRegex(expanded, operation.greedy, extglob);
      } else {
        const expanded = await expandPart2(ctx, part);
        regexStr += escapeRegex(expanded);
      }
    }
  }
  const strippedValues = [];
  for (const value of values) {
    strippedValues.push(applyPatternRemoval(value, regexStr, operation.side, operation.greedy));
  }
  const ifsChars = getIfs(ctx.state.env);
  const ifsEmpty = isIfsEmpty(ctx.state.env);
  if (unquotedArrayIsStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    const joined = strippedValues.join(ifsSep);
    if (ifsEmpty) {
      return { values: joined ? [joined] : [], quoted: false };
    }
    return {
      values: splitByIfsForExpansion(joined, ifsChars),
      quoted: false
    };
  }
  if (ifsEmpty) {
    return { values: strippedValues, quoted: false };
  }
  const allWords = [];
  for (const val of strippedValues) {
    if (val === "") {
      allWords.push("");
    } else {
      allWords.push(...splitByIfsForExpansion(val, ifsChars));
    }
  }
  return { values: allWords, quoted: false };
}
__name$1(handleUnquotedArrayPatternRemoval, "handleUnquotedArrayPatternRemoval");
async function handleUnquotedPositionalPatternRemoval(ctx, wordParts, expandWordPartsAsync2, expandPart2) {
  let unquotedPosPatRemIdx = -1;
  let unquotedPosPatRemIsStar = false;
  for (let i = 0; i < wordParts.length; i++) {
    const p = wordParts[i];
    if (p.type === "ParameterExpansion" && (p.parameter === "@" || p.parameter === "*") && p.operation?.type === "PatternRemoval") {
      unquotedPosPatRemIdx = i;
      unquotedPosPatRemIsStar = p.parameter === "*";
      break;
    }
  }
  if (unquotedPosPatRemIdx === -1) {
    return null;
  }
  const paramPart = wordParts[unquotedPosPatRemIdx];
  const operation = paramPart.operation;
  const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
  const params = [];
  for (let i = 1; i <= numParams; i++) {
    params.push(ctx.state.env[String(i)] || "");
  }
  if (params.length === 0) {
    return { values: [], quoted: false };
  }
  let regexStr = "";
  const extglob = ctx.state.shoptOptions.extglob;
  if (operation.pattern) {
    for (const part of operation.pattern.parts) {
      if (part.type === "Glob") {
        regexStr += patternToRegex(part.pattern, operation.greedy, extglob);
      } else if (part.type === "Literal") {
        regexStr += patternToRegex(part.value, operation.greedy, extglob);
      } else if (part.type === "SingleQuoted" || part.type === "Escaped") {
        regexStr += escapeRegex(part.value);
      } else if (part.type === "DoubleQuoted") {
        const expanded = await expandWordPartsAsync2(ctx, part.parts);
        regexStr += escapeRegex(expanded);
      } else if (part.type === "ParameterExpansion") {
        const expanded = await expandPart2(ctx, part);
        regexStr += patternToRegex(expanded, operation.greedy, extglob);
      } else {
        const expanded = await expandPart2(ctx, part);
        regexStr += escapeRegex(expanded);
      }
    }
  }
  const strippedParams = [];
  for (const param of params) {
    strippedParams.push(applyPatternRemoval(param, regexStr, operation.side, operation.greedy));
  }
  const ifsChars = getIfs(ctx.state.env);
  const ifsEmpty = isIfsEmpty(ctx.state.env);
  if (unquotedPosPatRemIsStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    const joined = strippedParams.join(ifsSep);
    if (ifsEmpty) {
      return { values: joined ? [joined] : [], quoted: false };
    }
    return {
      values: splitByIfsForExpansion(joined, ifsChars),
      quoted: false
    };
  }
  if (ifsEmpty) {
    return { values: strippedParams, quoted: false };
  }
  const allWords = [];
  for (const val of strippedParams) {
    if (val === "") {
      allWords.push("");
    } else {
      allWords.push(...splitByIfsForExpansion(val, ifsChars));
    }
  }
  return { values: allWords, quoted: false };
}
__name$1(handleUnquotedPositionalPatternRemoval, "handleUnquotedPositionalPatternRemoval");
async function handleUnquotedPositionalSlicing(ctx, wordParts, evaluateArithmetic2, expandPart2) {
  let unquotedSliceAtIndex = -1;
  let unquotedSliceIsStar = false;
  for (let i = 0; i < wordParts.length; i++) {
    const p = wordParts[i];
    if (p.type === "ParameterExpansion" && (p.parameter === "@" || p.parameter === "*") && p.operation?.type === "Substring") {
      unquotedSliceAtIndex = i;
      unquotedSliceIsStar = p.parameter === "*";
      break;
    }
  }
  if (unquotedSliceAtIndex === -1) {
    return null;
  }
  const paramPart = wordParts[unquotedSliceAtIndex];
  const operation = paramPart.operation;
  const offset = operation.offset ? await evaluateArithmetic2(ctx, operation.offset.expression) : 0;
  const length = operation.length ? await evaluateArithmetic2(ctx, operation.length.expression) : void 0;
  const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
  const allParams = [];
  for (let i = 1; i <= numParams; i++) {
    allParams.push(ctx.state.env[String(i)] || "");
  }
  const shellName = ctx.state.env["0"] || "bash";
  let slicedParams;
  if (offset <= 0) {
    const withZero = [shellName, ...allParams];
    const computedIdx = withZero.length + offset;
    if (computedIdx < 0) {
      slicedParams = [];
    } else {
      const startIdx = offset < 0 ? computedIdx : 0;
      if (length !== void 0) {
        const endIdx = length < 0 ? withZero.length + length : startIdx + length;
        slicedParams = withZero.slice(startIdx, Math.max(startIdx, endIdx));
      } else {
        slicedParams = withZero.slice(startIdx);
      }
    }
  } else {
    const startIdx = offset - 1;
    if (startIdx >= allParams.length) {
      slicedParams = [];
    } else if (length !== void 0) {
      const endIdx = length < 0 ? allParams.length + length : startIdx + length;
      slicedParams = allParams.slice(startIdx, Math.max(startIdx, endIdx));
    } else {
      slicedParams = allParams.slice(startIdx);
    }
  }
  let prefix = "";
  for (let i = 0; i < unquotedSliceAtIndex; i++) {
    prefix += await expandPart2(ctx, wordParts[i]);
  }
  let suffix = "";
  for (let i = unquotedSliceAtIndex + 1; i < wordParts.length; i++) {
    suffix += await expandPart2(ctx, wordParts[i]);
  }
  const ifsChars = getIfs(ctx.state.env);
  const ifsEmpty = isIfsEmpty(ctx.state.env);
  if (slicedParams.length === 0) {
    const combined = prefix + suffix;
    if (!combined) {
      return { values: [], quoted: false };
    }
    if (ifsEmpty) {
      return { values: [combined], quoted: false };
    }
    return {
      values: splitByIfsForExpansion(combined, ifsChars),
      quoted: false
    };
  }
  let allWords;
  if (unquotedSliceIsStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    const joined = prefix + slicedParams.join(ifsSep) + suffix;
    if (ifsEmpty) {
      allWords = joined ? [joined] : [];
    } else {
      allWords = splitByIfsForExpansion(joined, ifsChars);
    }
  } else {
    if (ifsEmpty) {
      if (slicedParams.length === 1) {
        allWords = [prefix + slicedParams[0] + suffix];
      } else {
        allWords = [
          prefix + slicedParams[0],
          ...slicedParams.slice(1, -1),
          slicedParams[slicedParams.length - 1] + suffix
        ];
      }
    } else {
      allWords = [];
      for (let i = 0; i < slicedParams.length; i++) {
        let param = slicedParams[i];
        if (i === 0)
          param = prefix + param;
        if (i === slicedParams.length - 1)
          param = param + suffix;
        if (param === "") {
          allWords.push("");
        } else {
          const parts = splitByIfsForExpansion(param, ifsChars);
          allWords.push(...parts);
        }
      }
    }
  }
  return { values: await applyGlobExpansion(ctx, allWords), quoted: false };
}
__name$1(handleUnquotedPositionalSlicing, "handleUnquotedPositionalSlicing");
async function handleUnquotedSimplePositional(ctx, wordParts) {
  if (wordParts.length !== 1 || wordParts[0].type !== "ParameterExpansion" || wordParts[0].parameter !== "@" && wordParts[0].parameter !== "*" || wordParts[0].operation) {
    return null;
  }
  const isStar = wordParts[0].parameter === "*";
  const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
  if (numParams === 0) {
    return { values: [], quoted: false };
  }
  const params = [];
  for (let i = 1; i <= numParams; i++) {
    params.push(ctx.state.env[String(i)] || "");
  }
  const ifsChars = getIfs(ctx.state.env);
  const ifsEmpty = isIfsEmpty(ctx.state.env);
  const ifsWhitespaceOnly = isIfsWhitespaceOnly(ctx.state.env);
  let allWords;
  if (isStar) {
    if (ifsEmpty) {
      allWords = params.filter((p) => p !== "");
    } else {
      const ifsSep = getIfsSeparator(ctx.state.env);
      const joined = params.join(ifsSep);
      allWords = splitByIfsForExpansion(joined, ifsChars);
    }
  } else {
    if (ifsEmpty) {
      allWords = params.filter((p) => p !== "");
    } else if (ifsWhitespaceOnly) {
      allWords = [];
      for (const param of params) {
        if (param === "") {
          continue;
        }
        const parts = splitByIfsForExpansion(param, ifsChars);
        allWords.push(...parts);
      }
    } else {
      allWords = [];
      for (const param of params) {
        if (param === "") {
          allWords.push("");
        } else {
          const parts = splitByIfsForExpansion(param, ifsChars);
          allWords.push(...parts);
        }
      }
      while (allWords.length > 0 && allWords[allWords.length - 1] === "") {
        allWords.pop();
      }
    }
  }
  return { values: await applyGlobExpansion(ctx, allWords), quoted: false };
}
__name$1(handleUnquotedSimplePositional, "handleUnquotedSimplePositional");
async function handleUnquotedSimpleArray(ctx, wordParts) {
  if (wordParts.length !== 1 || wordParts[0].type !== "ParameterExpansion" || wordParts[0].operation) {
    return null;
  }
  const arrayMatch = wordParts[0].parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
  if (!arrayMatch) {
    return null;
  }
  const arrayName = arrayMatch[1];
  const isStar = arrayMatch[2] === "*";
  const elements = getArrayElements(ctx, arrayName);
  let values;
  if (elements.length === 0) {
    const scalarValue = ctx.state.env[arrayName];
    if (scalarValue !== void 0) {
      values = [scalarValue];
    } else {
      return { values: [], quoted: false };
    }
  } else {
    values = elements.map(([, v]) => v);
  }
  const ifsChars = getIfs(ctx.state.env);
  const ifsEmpty = isIfsEmpty(ctx.state.env);
  const ifsWhitespaceOnly = isIfsWhitespaceOnly(ctx.state.env);
  let allWords;
  if (isStar) {
    if (ifsEmpty) {
      allWords = values.filter((v) => v !== "");
    } else {
      const ifsSep = getIfsSeparator(ctx.state.env);
      const joined = values.join(ifsSep);
      allWords = splitByIfsForExpansion(joined, ifsChars);
    }
  } else {
    if (ifsEmpty) {
      allWords = values.filter((v) => v !== "");
    } else if (ifsWhitespaceOnly) {
      allWords = [];
      for (const val of values) {
        if (val === "") {
          continue;
        }
        const parts = splitByIfsForExpansion(val, ifsChars);
        allWords.push(...parts);
      }
    } else {
      allWords = [];
      for (const val of values) {
        if (val === "") {
          allWords.push("");
        } else {
          const parts = splitByIfsForExpansion(val, ifsChars);
          allWords.push(...parts);
        }
      }
      while (allWords.length > 0 && allWords[allWords.length - 1] === "") {
        allWords.pop();
      }
    }
  }
  return { values: await applyGlobExpansion(ctx, allWords), quoted: false };
}
__name$1(handleUnquotedSimpleArray, "handleUnquotedSimpleArray");
function handleUnquotedVarNamePrefix(ctx, wordParts) {
  if (wordParts.length !== 1 || wordParts[0].type !== "ParameterExpansion" || wordParts[0].operation?.type !== "VarNamePrefix") {
    return null;
  }
  const op = wordParts[0].operation;
  const matchingVars = getVarNamesWithPrefix(ctx, op.prefix);
  if (matchingVars.length === 0) {
    return { values: [], quoted: false };
  }
  const ifsChars = getIfs(ctx.state.env);
  const ifsEmpty = isIfsEmpty(ctx.state.env);
  let allWords;
  if (op.star) {
    if (ifsEmpty) {
      allWords = matchingVars;
    } else {
      const ifsSep = getIfsSeparator(ctx.state.env);
      const joined = matchingVars.join(ifsSep);
      allWords = splitByIfsForExpansion(joined, ifsChars);
    }
  } else {
    if (ifsEmpty) {
      allWords = matchingVars;
    } else {
      allWords = [];
      for (const name of matchingVars) {
        const parts = splitByIfsForExpansion(name, ifsChars);
        allWords.push(...parts);
      }
    }
  }
  return { values: allWords, quoted: false };
}
__name$1(handleUnquotedVarNamePrefix, "handleUnquotedVarNamePrefix");
function handleUnquotedArrayKeys(ctx, wordParts) {
  if (wordParts.length !== 1 || wordParts[0].type !== "ParameterExpansion" || wordParts[0].operation?.type !== "ArrayKeys") {
    return null;
  }
  const op = wordParts[0].operation;
  const elements = getArrayElements(ctx, op.array);
  const keys = elements.map(([k]) => String(k));
  if (keys.length === 0) {
    return { values: [], quoted: false };
  }
  const ifsChars = getIfs(ctx.state.env);
  const ifsEmpty = isIfsEmpty(ctx.state.env);
  let allWords;
  if (op.star) {
    if (ifsEmpty) {
      allWords = keys;
    } else {
      const ifsSep = getIfsSeparator(ctx.state.env);
      const joined = keys.join(ifsSep);
      allWords = splitByIfsForExpansion(joined, ifsChars);
    }
  } else {
    if (ifsEmpty) {
      allWords = keys;
    } else {
      allWords = [];
      for (const key2 of keys) {
        const parts = splitByIfsForExpansion(key2, ifsChars);
        allWords.push(...parts);
      }
    }
  }
  return { values: allWords, quoted: false };
}
__name$1(handleUnquotedArrayKeys, "handleUnquotedArrayKeys");
async function handleUnquotedPositionalWithPrefixSuffix(ctx, wordParts, expandPart2) {
  let unquotedAtStarIndex = -1;
  for (let i = 0; i < wordParts.length; i++) {
    const p = wordParts[i];
    if (p.type === "ParameterExpansion" && (p.parameter === "@" || p.parameter === "*") && !p.operation) {
      unquotedAtStarIndex = i;
      break;
    }
  }
  if (unquotedAtStarIndex === -1 || wordParts.length <= 1) {
    return null;
  }
  const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
  const params = [];
  for (let i = 1; i <= numParams; i++) {
    params.push(ctx.state.env[String(i)] || "");
  }
  let prefix = "";
  for (let i = 0; i < unquotedAtStarIndex; i++) {
    prefix += await expandPart2(ctx, wordParts[i]);
  }
  let suffix = "";
  for (let i = unquotedAtStarIndex + 1; i < wordParts.length; i++) {
    suffix += await expandPart2(ctx, wordParts[i]);
  }
  const ifsChars = getIfs(ctx.state.env);
  const ifsEmpty = isIfsEmpty(ctx.state.env);
  const ifsWhitespaceOnly = isIfsWhitespaceOnly(ctx.state.env);
  if (numParams === 0) {
    const combined = prefix + suffix;
    return { values: combined ? [combined] : [], quoted: false };
  }
  let words;
  {
    const rawWords = [];
    for (let i = 0; i < params.length; i++) {
      let word = params[i];
      if (i === 0)
        word = prefix + word;
      if (i === params.length - 1)
        word = word + suffix;
      rawWords.push(word);
    }
    if (ifsEmpty) {
      words = rawWords.filter((w) => w !== "");
    } else if (ifsWhitespaceOnly) {
      words = [];
      for (const word of rawWords) {
        if (word === "")
          continue;
        const parts = splitByIfsForExpansion(word, ifsChars);
        words.push(...parts);
      }
    } else {
      words = [];
      for (const word of rawWords) {
        if (word === "") {
          words.push("");
        } else {
          const parts = splitByIfsForExpansion(word, ifsChars);
          words.push(...parts);
        }
      }
      while (words.length > 0 && words[words.length - 1] === "") {
        words.pop();
      }
    }
  }
  if (words.length === 0) {
    return { values: [], quoted: false };
  }
  return { values: await applyGlobExpansion(ctx, words), quoted: false };
}
__name$1(handleUnquotedPositionalWithPrefixSuffix, "handleUnquotedPositionalWithPrefixSuffix");
async function expandWordWithGlobImpl(ctx, word, deps) {
  const wordParts = word.parts;
  const { hasQuoted, hasCommandSub, hasArrayVar, hasArrayAtExpansion, hasParamExpansion, hasVarNamePrefixExpansion, hasIndirection } = analyzeWordParts(wordParts);
  const hasBraces = deps.hasBraceExpansion(wordParts);
  const braceExpanded = hasBraces ? await deps.expandWordWithBracesAsync(ctx, word) : null;
  if (braceExpanded && braceExpanded.length > 1) {
    return handleBraceExpansionResults(ctx, braceExpanded, hasQuoted);
  }
  const arrayResult = await handleArrayExpansionCases(ctx, wordParts, hasArrayAtExpansion, hasVarNamePrefixExpansion, hasIndirection, deps);
  if (arrayResult !== null) {
    return arrayResult;
  }
  const positionalResult = await handlePositionalExpansionCases(ctx, wordParts, deps);
  if (positionalResult !== null) {
    return positionalResult;
  }
  const unquotedResult = await handleUnquotedExpansionCases(ctx, wordParts, deps);
  if (unquotedResult !== null) {
    return unquotedResult;
  }
  const mixedWordResult = await expandMixedWordParts(ctx, wordParts, deps.expandPart);
  if (mixedWordResult !== null) {
    return applyGlobToValues(ctx, mixedWordResult);
  }
  if ((hasCommandSub || hasArrayVar || hasParamExpansion) && !isIfsEmpty(ctx.state.env)) {
    const ifsChars = getIfs(ctx.state.env);
    const ifsPattern = deps.buildIfsCharClassPattern(ifsChars);
    const splitResult = await deps.smartWordSplit(ctx, wordParts, ifsChars, ifsPattern, deps.expandPart);
    return applyGlobToValues(ctx, splitResult);
  }
  const value = await deps.expandWordAsync(ctx, word);
  return handleFinalGlobExpansion(ctx, word, wordParts, value, hasQuoted, deps.expandWordForGlobbing);
}
__name$1(expandWordWithGlobImpl, "expandWordWithGlobImpl");
async function handleBraceExpansionResults(ctx, braceExpanded, hasQuoted) {
  const allValues = [];
  for (const value of braceExpanded) {
    if (!hasQuoted && value === "") {
      continue;
    }
    if (!hasQuoted && !ctx.state.options.noglob && hasGlobPattern(value, ctx.state.shoptOptions.extglob)) {
      const matches = await expandGlobPattern(ctx, value);
      allValues.push(...matches);
    } else {
      allValues.push(value);
    }
  }
  return { values: allValues, quoted: false };
}
__name$1(handleBraceExpansionResults, "handleBraceExpansionResults");
async function handleArrayExpansionCases(ctx, wordParts, hasArrayAtExpansion, hasVarNamePrefixExpansion, hasIndirection, deps) {
  if (hasArrayAtExpansion) {
    const simpleArrayResult = handleSimpleArrayExpansion(ctx, wordParts);
    if (simpleArrayResult !== null) {
      return simpleArrayResult;
    }
  }
  {
    const namerefArrayResult = handleNamerefArrayExpansion(ctx, wordParts);
    if (namerefArrayResult !== null) {
      return namerefArrayResult;
    }
  }
  {
    const arrayDefaultResult = await handleArrayDefaultValue(ctx, wordParts);
    if (arrayDefaultResult !== null) {
      return arrayDefaultResult;
    }
  }
  {
    const arrayPatternPrefixSuffixResult = await handleArrayPatternWithPrefixSuffix(ctx, wordParts, hasArrayAtExpansion, deps.expandPart, deps.expandWordPartsAsync);
    if (arrayPatternPrefixSuffixResult !== null) {
      return arrayPatternPrefixSuffixResult;
    }
  }
  {
    const arrayPrefixSuffixResult = await handleArrayWithPrefixSuffix(ctx, wordParts, hasArrayAtExpansion, deps.expandPart);
    if (arrayPrefixSuffixResult !== null) {
      return arrayPrefixSuffixResult;
    }
  }
  {
    const arraySlicingResult = await handleArraySlicing(ctx, wordParts, deps.evaluateArithmetic);
    if (arraySlicingResult !== null) {
      return arraySlicingResult;
    }
  }
  {
    const arrayTransformResult = handleArrayTransform(ctx, wordParts);
    if (arrayTransformResult !== null) {
      return arrayTransformResult;
    }
  }
  {
    const arrayPatReplResult = await handleArrayPatternReplacement(ctx, wordParts, deps.expandWordPartsAsync, deps.expandPart);
    if (arrayPatReplResult !== null) {
      return arrayPatReplResult;
    }
  }
  {
    const arrayPatRemResult = await handleArrayPatternRemoval(ctx, wordParts, deps.expandWordPartsAsync, deps.expandPart);
    if (arrayPatRemResult !== null) {
      return arrayPatRemResult;
    }
  }
  if (hasVarNamePrefixExpansion && wordParts.length === 1 && wordParts[0].type === "DoubleQuoted") {
    const result2 = handleVarNamePrefixExpansion(ctx, wordParts);
    if (result2 !== null) {
      return result2;
    }
  }
  {
    const indirectArrayResult = await handleIndirectArrayExpansion(ctx, wordParts, hasIndirection, deps.expandParameterAsync, deps.expandWordPartsAsync);
    if (indirectArrayResult !== null) {
      return indirectArrayResult;
    }
  }
  {
    const indirectInAltResult = await handleIndirectInAlternative(ctx, wordParts);
    if (indirectInAltResult !== null) {
      return indirectInAltResult;
    }
  }
  {
    const indirectionWithInnerResult = await handleIndirectionWithInnerAlternative(ctx, wordParts);
    if (indirectionWithInnerResult !== null) {
      return indirectionWithInnerResult;
    }
  }
  return null;
}
__name$1(handleArrayExpansionCases, "handleArrayExpansionCases");
function handleVarNamePrefixExpansion(ctx, wordParts) {
  const dqPart = wordParts[0];
  if (dqPart.type !== "DoubleQuoted")
    return null;
  if (dqPart.parts.length === 1 && dqPart.parts[0].type === "ParameterExpansion" && dqPart.parts[0].operation?.type === "VarNamePrefix") {
    const op = dqPart.parts[0].operation;
    const matchingVars = getVarNamesWithPrefix(ctx, op.prefix);
    if (op.star) {
      return {
        values: [matchingVars.join(getIfsSeparator(ctx.state.env))],
        quoted: true
      };
    }
    return { values: matchingVars, quoted: true };
  }
  if (dqPart.parts.length === 1 && dqPart.parts[0].type === "ParameterExpansion" && dqPart.parts[0].operation?.type === "ArrayKeys") {
    const op = dqPart.parts[0].operation;
    const elements = getArrayElements(ctx, op.array);
    const keys = elements.map(([k]) => String(k));
    if (op.star) {
      return {
        values: [keys.join(getIfsSeparator(ctx.state.env))],
        quoted: true
      };
    }
    return { values: keys, quoted: true };
  }
  return null;
}
__name$1(handleVarNamePrefixExpansion, "handleVarNamePrefixExpansion");
async function handlePositionalExpansionCases(ctx, wordParts, deps) {
  {
    const positionalSlicingResult = await handlePositionalSlicing(ctx, wordParts, deps.evaluateArithmetic, deps.expandPart);
    if (positionalSlicingResult !== null) {
      return positionalSlicingResult;
    }
  }
  {
    const positionalPatReplResult = await handlePositionalPatternReplacement(ctx, wordParts, deps.expandPart, deps.expandWordPartsAsync);
    if (positionalPatReplResult !== null) {
      return positionalPatReplResult;
    }
  }
  {
    const positionalPatRemResult = await handlePositionalPatternRemoval(ctx, wordParts, deps.expandPart, deps.expandWordPartsAsync);
    if (positionalPatRemResult !== null) {
      return positionalPatRemResult;
    }
  }
  {
    const simplePositionalResult = await handleSimplePositionalExpansion(ctx, wordParts, deps.expandPart);
    if (simplePositionalResult !== null) {
      return simplePositionalResult;
    }
  }
  return null;
}
__name$1(handlePositionalExpansionCases, "handlePositionalExpansionCases");
async function handleUnquotedExpansionCases(ctx, wordParts, deps) {
  {
    const unquotedArrayPatReplResult = await handleUnquotedArrayPatternReplacement(ctx, wordParts, deps.expandWordPartsAsync, deps.expandPart);
    if (unquotedArrayPatReplResult !== null) {
      return unquotedArrayPatReplResult;
    }
  }
  {
    const unquotedArrayPatRemResult = await handleUnquotedArrayPatternRemoval(ctx, wordParts, deps.expandWordPartsAsync, deps.expandPart);
    if (unquotedArrayPatRemResult !== null) {
      return unquotedArrayPatRemResult;
    }
  }
  {
    const unquotedPosPatRemResult = await handleUnquotedPositionalPatternRemoval(ctx, wordParts, deps.expandWordPartsAsync, deps.expandPart);
    if (unquotedPosPatRemResult !== null) {
      return unquotedPosPatRemResult;
    }
  }
  {
    const unquotedSliceResult = await handleUnquotedPositionalSlicing(ctx, wordParts, deps.evaluateArithmetic, deps.expandPart);
    if (unquotedSliceResult !== null) {
      return unquotedSliceResult;
    }
  }
  {
    const unquotedSimplePositionalResult = await handleUnquotedSimplePositional(ctx, wordParts);
    if (unquotedSimplePositionalResult !== null) {
      return unquotedSimplePositionalResult;
    }
  }
  {
    const unquotedSimpleArrayResult = await handleUnquotedSimpleArray(ctx, wordParts);
    if (unquotedSimpleArrayResult !== null) {
      return unquotedSimpleArrayResult;
    }
  }
  {
    const unquotedVarNamePrefixResult = handleUnquotedVarNamePrefix(ctx, wordParts);
    if (unquotedVarNamePrefixResult !== null) {
      return unquotedVarNamePrefixResult;
    }
  }
  {
    const unquotedArrayKeysResult = handleUnquotedArrayKeys(ctx, wordParts);
    if (unquotedArrayKeysResult !== null) {
      return unquotedArrayKeysResult;
    }
  }
  {
    const unquotedPrefixSuffixResult = await handleUnquotedPositionalWithPrefixSuffix(ctx, wordParts, deps.expandPart);
    if (unquotedPrefixSuffixResult !== null) {
      return unquotedPrefixSuffixResult;
    }
  }
  return null;
}
__name$1(handleUnquotedExpansionCases, "handleUnquotedExpansionCases");
function findWordProducingExpansion(part) {
  if (part.type !== "DoubleQuoted")
    return null;
  for (let i = 0; i < part.parts.length; i++) {
    const inner = part.parts[i];
    if (inner.type !== "ParameterExpansion")
      continue;
    if (inner.operation)
      continue;
    const arrayMatch = inner.parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([@*])\]$/);
    if (arrayMatch) {
      return {
        type: "array",
        name: arrayMatch[1],
        atIndex: i,
        isStar: arrayMatch[2] === "*"
      };
    }
    if (inner.parameter === "@" || inner.parameter === "*") {
      return {
        type: "positional",
        atIndex: i,
        isStar: inner.parameter === "*"
      };
    }
  }
  return null;
}
__name$1(findWordProducingExpansion, "findWordProducingExpansion");
async function expandDoubleQuotedWithWordProducing(ctx, part, info, expandPart2) {
  let prefix = "";
  for (let i = 0; i < info.atIndex; i++) {
    prefix += await expandPart2(ctx, part.parts[i]);
  }
  let suffix = "";
  for (let i = info.atIndex + 1; i < part.parts.length; i++) {
    suffix += await expandPart2(ctx, part.parts[i]);
  }
  let values;
  if (info.type === "array") {
    const elements = getArrayElements(ctx, info.name);
    values = elements.map(([, v]) => v);
    if (values.length === 0) {
      const scalarValue = ctx.state.env[info.name];
      if (scalarValue !== void 0) {
        values = [scalarValue];
      }
    }
  } else {
    const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
    values = [];
    for (let i = 1; i <= numParams; i++) {
      values.push(ctx.state.env[String(i)] || "");
    }
  }
  if (info.isStar) {
    const ifsSep = getIfsSeparator(ctx.state.env);
    const joined = values.join(ifsSep);
    return [prefix + joined + suffix];
  }
  if (values.length === 0) {
    const combined = prefix + suffix;
    return combined ? [combined] : [];
  }
  if (values.length === 1) {
    return [prefix + values[0] + suffix];
  }
  return [
    prefix + values[0],
    ...values.slice(1, -1),
    values[values.length - 1] + suffix
  ];
}
__name$1(expandDoubleQuotedWithWordProducing, "expandDoubleQuotedWithWordProducing");
async function expandMixedWordParts(ctx, wordParts, expandPart2) {
  if (wordParts.length < 2)
    return null;
  let hasWordProducing = false;
  for (const part of wordParts) {
    if (findWordProducingExpansion(part)) {
      hasWordProducing = true;
      break;
    }
  }
  if (!hasWordProducing)
    return null;
  const ifsChars = getIfs(ctx.state.env);
  const ifsEmpty = isIfsEmpty(ctx.state.env);
  const partWords = [];
  for (const part of wordParts) {
    const wpInfo = findWordProducingExpansion(part);
    if (wpInfo && part.type === "DoubleQuoted") {
      const words = await expandDoubleQuotedWithWordProducing(ctx, part, wpInfo, expandPart2);
      partWords.push(words);
    } else if (part.type === "DoubleQuoted" || part.type === "SingleQuoted") {
      const value = await expandPart2(ctx, part);
      partWords.push([value]);
    } else if (part.type === "Literal") {
      partWords.push([part.value]);
    } else if (part.type === "ParameterExpansion") {
      const value = await expandPart2(ctx, part);
      if (ifsEmpty) {
        partWords.push(value ? [value] : []);
      } else {
        const split = splitByIfsForExpansion(value, ifsChars);
        partWords.push(split);
      }
    } else {
      const value = await expandPart2(ctx, part);
      if (ifsEmpty) {
        partWords.push(value ? [value] : []);
      } else {
        const split = splitByIfsForExpansion(value, ifsChars);
        partWords.push(split);
      }
    }
  }
  const result2 = [];
  for (const words of partWords) {
    if (words.length === 0) {
      continue;
    }
    if (result2.length === 0) {
      result2.push(...words);
    } else {
      const lastIdx = result2.length - 1;
      result2[lastIdx] = result2[lastIdx] + words[0];
      for (let j = 1; j < words.length; j++) {
        result2.push(words[j]);
      }
    }
  }
  return result2;
}
__name$1(expandMixedWordParts, "expandMixedWordParts");
async function applyGlobToValues(ctx, values) {
  if (ctx.state.options.noglob) {
    return { values, quoted: false };
  }
  const expandedValues = [];
  for (const v of values) {
    if (hasGlobPattern(v, ctx.state.shoptOptions.extglob)) {
      const matches = await expandGlobPattern(ctx, v);
      expandedValues.push(...matches);
    } else {
      expandedValues.push(v);
    }
  }
  return { values: expandedValues, quoted: false };
}
__name$1(applyGlobToValues, "applyGlobToValues");
async function expandGlobPattern(ctx, pattern) {
  const globExpander = new GlobExpander(ctx.fs, ctx.state.cwd, ctx.state.env, {
    globstar: ctx.state.shoptOptions.globstar,
    nullglob: ctx.state.shoptOptions.nullglob,
    failglob: ctx.state.shoptOptions.failglob,
    dotglob: ctx.state.shoptOptions.dotglob,
    extglob: ctx.state.shoptOptions.extglob,
    globskipdots: ctx.state.shoptOptions.globskipdots
  });
  const matches = await globExpander.expand(pattern);
  if (matches.length > 0) {
    return matches;
  }
  if (globExpander.hasFailglob()) {
    throw new GlobError(pattern);
  }
  if (globExpander.hasNullglob()) {
    return [];
  }
  return [pattern];
}
__name$1(expandGlobPattern, "expandGlobPattern");
async function handleFinalGlobExpansion(ctx, word, wordParts, value, hasQuoted, expandWordForGlobbing2) {
  const hasGlobParts = wordParts.some((p) => p.type === "Glob");
  if (!ctx.state.options.noglob && hasGlobParts) {
    const globPattern = await expandWordForGlobbing2(ctx, word);
    if (hasGlobPattern(globPattern, ctx.state.shoptOptions.extglob)) {
      const matches = await expandGlobPattern(ctx, globPattern);
      if (matches.length > 0 && matches[0] !== globPattern) {
        return { values: matches, quoted: false };
      }
      if (matches.length === 0) {
        return { values: [], quoted: false };
      }
    }
    const unescapedValue = unescapeGlobPattern(value);
    if (!isIfsEmpty(ctx.state.env)) {
      const ifsChars = getIfs(ctx.state.env);
      const splitValues = splitByIfsForExpansion(unescapedValue, ifsChars);
      return { values: splitValues, quoted: false };
    }
    return { values: [unescapedValue], quoted: false };
  }
  if (!hasQuoted && !ctx.state.options.noglob && hasGlobPattern(value, ctx.state.shoptOptions.extglob)) {
    const globPattern = await expandWordForGlobbing2(ctx, word);
    if (hasGlobPattern(globPattern, ctx.state.shoptOptions.extglob)) {
      const matches = await expandGlobPattern(ctx, globPattern);
      if (matches.length > 0 && matches[0] !== globPattern) {
        return { values: matches, quoted: false };
      }
    }
  }
  if (value === "" && !hasQuoted) {
    return { values: [], quoted: false };
  }
  if (hasGlobParts && !hasQuoted) {
    const unescapedValue = unescapeGlobPattern(value);
    if (!isIfsEmpty(ctx.state.env)) {
      const ifsChars = getIfs(ctx.state.env);
      const splitValues = splitByIfsForExpansion(unescapedValue, ifsChars);
      return { values: splitValues, quoted: false };
    }
    return { values: [unescapedValue], quoted: false };
  }
  return { values: [value], quoted: hasQuoted };
}
__name$1(handleFinalGlobExpansion, "handleFinalGlobExpansion");
async function shouldUseOperationWord(ctx, part) {
  const op = part.operation;
  if (!op)
    return null;
  if (op.type !== "DefaultValue" && op.type !== "AssignDefault" && op.type !== "UseAlternative") {
    return null;
  }
  const word = op.word;
  if (!word || word.parts.length === 0)
    return null;
  const isSet = await isVariableSet(ctx, part.parameter);
  const value = await getVariable(ctx, part.parameter, false);
  const isEmpty = value === "";
  const checkEmpty = op.checkEmpty ?? false;
  let shouldUse;
  if (op.type === "UseAlternative") {
    shouldUse = isSet && !(checkEmpty && isEmpty);
  } else {
    shouldUse = !isSet || checkEmpty && isEmpty;
  }
  if (!shouldUse)
    return null;
  return word.parts;
}
__name$1(shouldUseOperationWord, "shouldUseOperationWord");
function isSimpleQuotedLiteral(part) {
  if (part.type === "SingleQuoted") {
    return true;
  }
  if (part.type === "DoubleQuoted") {
    const dqPart = part;
    return dqPart.parts.every((p) => p.type === "Literal");
  }
  return false;
}
__name$1(isSimpleQuotedLiteral, "isSimpleQuotedLiteral");
async function hasMixedQuotedDefaultValue(ctx, part) {
  if (part.type !== "ParameterExpansion")
    return null;
  const opWordParts = await shouldUseOperationWord(ctx, part);
  if (!opWordParts || opWordParts.length <= 1)
    return null;
  const hasSimpleQuotedParts = opWordParts.some((p) => isSimpleQuotedLiteral(p));
  const hasUnquotedParts = opWordParts.some((p) => p.type === "Literal" || p.type === "ParameterExpansion" || p.type === "CommandSubstitution" || p.type === "ArithmeticExpansion");
  if (hasSimpleQuotedParts && hasUnquotedParts) {
    return opWordParts;
  }
  return null;
}
__name$1(hasMixedQuotedDefaultValue, "hasMixedQuotedDefaultValue");
function isPartSplittable(part) {
  if (part.type === "DoubleQuoted" || part.type === "SingleQuoted") {
    return false;
  }
  if (part.type === "Literal") {
    return false;
  }
  if (part.type === "Glob") {
    return globPatternHasVarRef(part.pattern);
  }
  const isSplittable = part.type === "ParameterExpansion" || part.type === "CommandSubstitution" || part.type === "ArithmeticExpansion";
  if (!isSplittable) {
    return false;
  }
  if (part.type === "ParameterExpansion" && isOperationWordEntirelyQuoted(part)) {
    return false;
  }
  return true;
}
__name$1(isPartSplittable, "isPartSplittable");
async function smartWordSplit(ctx, wordParts, ifsChars, _ifsPattern, expandPartFn) {
  if (wordParts.length === 1 && wordParts[0].type === "ParameterExpansion") {
    const paramPart = wordParts[0];
    const opWordParts = await shouldUseOperationWord(ctx, paramPart);
    if (opWordParts && opWordParts.length > 0) {
      const hasMixedParts = opWordParts.length > 1 && opWordParts.some((p) => p.type === "DoubleQuoted" || p.type === "SingleQuoted") && opWordParts.some((p) => p.type === "Literal" || p.type === "ParameterExpansion" || p.type === "CommandSubstitution" || p.type === "ArithmeticExpansion");
      if (hasMixedParts) {
        return smartWordSplitWithUnquotedLiterals(ctx, opWordParts, ifsChars, _ifsPattern, expandPartFn);
      }
    }
  }
  const segments = [];
  let hasAnySplittable = false;
  for (const part of wordParts) {
    const splittable = isPartSplittable(part);
    const isQuoted = part.type === "DoubleQuoted" || part.type === "SingleQuoted";
    const mixedDefaultParts = splittable ? await hasMixedQuotedDefaultValue(ctx, part) : null;
    const expanded = await expandPartFn(ctx, part);
    segments.push({
      value: expanded,
      isSplittable: splittable,
      isQuoted,
      mixedDefaultParts: mixedDefaultParts ?? void 0
    });
    if (splittable) {
      hasAnySplittable = true;
    }
  }
  if (!hasAnySplittable) {
    const joined = segments.map((s) => s.value).join("");
    return joined ? [joined] : [];
  }
  const words = [];
  let currentWord = "";
  let hasProducedWord = false;
  let pendingWordBreak = false;
  let prevWasQuotedEmpty = false;
  for (const segment of segments) {
    if (!segment.isSplittable) {
      if (pendingWordBreak) {
        if (segment.isQuoted && segment.value === "") {
          if (currentWord !== "") {
            words.push(currentWord);
          }
          words.push("");
          hasProducedWord = true;
          currentWord = "";
          pendingWordBreak = false;
          prevWasQuotedEmpty = true;
        } else if (segment.value !== "") {
          if (currentWord !== "") {
            words.push(currentWord);
          }
          currentWord = segment.value;
          pendingWordBreak = false;
          prevWasQuotedEmpty = false;
        } else {
          currentWord += segment.value;
          prevWasQuotedEmpty = false;
        }
      } else {
        currentWord += segment.value;
        prevWasQuotedEmpty = segment.isQuoted && segment.value === "";
      }
    } else if (segment.mixedDefaultParts) {
      const splitParts = await smartWordSplitWithUnquotedLiterals(ctx, segment.mixedDefaultParts, ifsChars, _ifsPattern, expandPartFn);
      if (splitParts.length === 0) ;
      else if (splitParts.length === 1) {
        currentWord += splitParts[0];
        hasProducedWord = true;
      } else {
        currentWord += splitParts[0];
        words.push(currentWord);
        hasProducedWord = true;
        for (let i = 1; i < splitParts.length - 1; i++) {
          words.push(splitParts[i]);
        }
        currentWord = splitParts[splitParts.length - 1];
      }
      pendingWordBreak = false;
      prevWasQuotedEmpty = false;
    } else {
      const { words: parts, hadLeadingDelimiter, hadTrailingDelimiter } = splitByIfsForExpansionEx(segment.value, ifsChars);
      if (prevWasQuotedEmpty && hadLeadingDelimiter && currentWord === "") {
        words.push("");
        hasProducedWord = true;
      }
      if (parts.length === 0) {
        if (hadTrailingDelimiter) {
          pendingWordBreak = true;
        }
      } else if (parts.length === 1) {
        currentWord += parts[0];
        hasProducedWord = true;
        pendingWordBreak = hadTrailingDelimiter;
      } else {
        currentWord += parts[0];
        words.push(currentWord);
        hasProducedWord = true;
        for (let i = 1; i < parts.length - 1; i++) {
          words.push(parts[i]);
        }
        currentWord = parts[parts.length - 1];
        pendingWordBreak = hadTrailingDelimiter;
      }
      prevWasQuotedEmpty = false;
    }
  }
  if (currentWord !== "") {
    words.push(currentWord);
  } else if (words.length === 0 && hasProducedWord) {
    words.push("");
  }
  return words;
}
__name$1(smartWordSplit, "smartWordSplit");
function startsWithIfs(value, ifsChars) {
  return value.length > 0 && ifsChars.includes(value[0]);
}
__name$1(startsWithIfs, "startsWithIfs");
async function smartWordSplitWithUnquotedLiterals(ctx, wordParts, ifsChars, _ifsPattern, expandPartFn) {
  const segments = [];
  for (const part of wordParts) {
    const isQuoted = part.type === "DoubleQuoted" || part.type === "SingleQuoted";
    const splittable = !isQuoted;
    const expanded = await expandPartFn(ctx, part);
    segments.push({ value: expanded, isSplittable: splittable });
  }
  const words = [];
  let currentWord = "";
  let hasProducedWord = false;
  let pendingWordBreak = false;
  for (const segment of segments) {
    if (!segment.isSplittable) {
      if (pendingWordBreak && segment.value !== "") {
        if (currentWord !== "") {
          words.push(currentWord);
        }
        currentWord = segment.value;
        pendingWordBreak = false;
      } else {
        currentWord += segment.value;
      }
    } else {
      const startsWithIfsChar = startsWithIfs(segment.value, ifsChars);
      if (startsWithIfsChar && currentWord !== "") {
        words.push(currentWord);
        currentWord = "";
        hasProducedWord = true;
      }
      const { words: parts, hadTrailingDelimiter } = splitByIfsForExpansionEx(segment.value, ifsChars);
      if (parts.length === 0) {
        if (hadTrailingDelimiter) {
          pendingWordBreak = true;
        }
      } else if (parts.length === 1) {
        currentWord += parts[0];
        hasProducedWord = true;
        pendingWordBreak = hadTrailingDelimiter;
      } else {
        currentWord += parts[0];
        words.push(currentWord);
        hasProducedWord = true;
        for (let i = 1; i < parts.length - 1; i++) {
          words.push(parts[i]);
        }
        currentWord = parts[parts.length - 1];
        pendingWordBreak = hadTrailingDelimiter;
      }
    }
  }
  if (currentWord !== "") {
    words.push(currentWord);
  } else if (words.length === 0 && hasProducedWord) {
    words.push("");
  }
  return words;
}
__name$1(smartWordSplitWithUnquotedLiterals, "smartWordSplitWithUnquotedLiterals");
function getLiteralValue$1(part) {
  switch (part.type) {
    case "Literal":
      return part.value;
    case "SingleQuoted":
      return part.value;
    case "Escaped":
      return part.value;
    default:
      return null;
  }
}
__name$1(getLiteralValue$1, "getLiteralValue");
function isQuotedPart(part) {
  switch (part.type) {
    case "SingleQuoted":
    case "Escaped":
    case "DoubleQuoted":
      return true;
    case "Literal":
      return part.value === "";
    default:
      return false;
  }
}
__name$1(isQuotedPart, "isQuotedPart");
async function expandWordPartsAsync(ctx, parts, inDoubleQuotes = false) {
  const results = [];
  for (const part of parts) {
    results.push(await expandPart(ctx, part, inDoubleQuotes));
  }
  return results.join("");
}
__name$1(expandWordPartsAsync, "expandWordPartsAsync");
function isPartFullyQuoted(part) {
  return isQuotedPart(part);
}
__name$1(isPartFullyQuoted, "isPartFullyQuoted");
function isWordFullyQuoted(word) {
  if (word.parts.length === 0)
    return true;
  for (const part of word.parts) {
    if (!isPartFullyQuoted(part)) {
      return false;
    }
  }
  return true;
}
__name$1(isWordFullyQuoted, "isWordFullyQuoted");
function expandSimplePart(ctx, part, inDoubleQuotes = false) {
  const literal2 = getLiteralValue$1(part);
  if (literal2 !== null)
    return literal2;
  switch (part.type) {
    case "TildeExpansion":
      if (inDoubleQuotes) {
        return part.user === null ? "~" : `~${part.user}`;
      }
      if (part.user === null) {
        return ctx.state.env.HOME !== void 0 ? ctx.state.env.HOME : "/home/user";
      }
      if (part.user === "root") {
        return "/root";
      }
      return `~${part.user}`;
    case "Glob":
      return expandVariablesInPattern(ctx, part.pattern);
    default:
      return null;
  }
}
__name$1(expandSimplePart, "expandSimplePart");
async function expandWord(ctx, word) {
  return expandWordAsync(ctx, word);
}
__name$1(expandWord, "expandWord");
async function expandWordForRegex(ctx, word) {
  const parts = [];
  for (const part of word.parts) {
    if (part.type === "Escaped") {
      parts.push(`\\${part.value}`);
    } else if (part.type === "SingleQuoted") {
      parts.push(part.value);
    } else if (part.type === "DoubleQuoted") {
      const expanded = await expandWordPartsAsync(ctx, part.parts);
      parts.push(expanded);
    } else if (part.type === "TildeExpansion") {
      const expanded = await expandPart(ctx, part);
      parts.push(escapeRegexChars(expanded));
    } else {
      parts.push(await expandPart(ctx, part));
    }
  }
  return parts.join("");
}
__name$1(expandWordForRegex, "expandWordForRegex");
async function expandWordForPattern(ctx, word) {
  const parts = [];
  for (const part of word.parts) {
    if (part.type === "Escaped") {
      const ch = part.value;
      if ("()|*?[]".includes(ch)) {
        parts.push(`\\${ch}`);
      } else {
        parts.push(ch);
      }
    } else if (part.type === "SingleQuoted") {
      parts.push(escapeGlobChars(part.value));
    } else if (part.type === "DoubleQuoted") {
      const expanded = await expandWordPartsAsync(ctx, part.parts);
      parts.push(escapeGlobChars(expanded));
    } else {
      parts.push(await expandPart(ctx, part));
    }
  }
  return parts.join("");
}
__name$1(expandWordForPattern, "expandWordForPattern");
async function expandWordForGlobbing(ctx, word) {
  const parts = [];
  for (const part of word.parts) {
    if (part.type === "SingleQuoted") {
      parts.push(escapeGlobChars(part.value));
    } else if (part.type === "Escaped") {
      const ch = part.value;
      if ("*?[]\\()|".includes(ch)) {
        parts.push(`\\${ch}`);
      } else {
        parts.push(ch);
      }
    } else if (part.type === "DoubleQuoted") {
      const expanded = await expandWordPartsAsync(ctx, part.parts);
      parts.push(escapeGlobChars(expanded));
    } else if (part.type === "Glob") {
      if (patternHasCommandSubstitution(part.pattern)) {
        parts.push(await expandVariablesInPatternAsync(ctx, part.pattern));
      } else {
        parts.push(expandVariablesInPattern(ctx, part.pattern));
      }
    } else if (part.type === "Literal") {
      parts.push(part.value);
    } else {
      parts.push(await expandPart(ctx, part));
    }
  }
  return parts.join("");
}
__name$1(expandWordForGlobbing, "expandWordForGlobbing");
function hasBraceExpansion(parts) {
  for (const part of parts) {
    if (part.type === "BraceExpansion")
      return true;
    if (part.type === "DoubleQuoted" && hasBraceExpansion(part.parts))
      return true;
  }
  return false;
}
__name$1(hasBraceExpansion, "hasBraceExpansion");
var MAX_BRACE_EXPANSION_RESULTS = 1e4;
var MAX_BRACE_OPERATIONS = 1e5;
async function expandBracesInPartsAsync(ctx, parts, operationCounter = { count: 0 }) {
  if (operationCounter.count > MAX_BRACE_OPERATIONS) {
    return [[]];
  }
  let results = [[]];
  for (const part of parts) {
    if (part.type === "BraceExpansion") {
      const braceValues = [];
      let hasInvalidRange = false;
      let invalidRangeLiteral = "";
      for (const item of part.items) {
        if (item.type === "Range") {
          const range = expandBraceRange(item.start, item.end, item.step, item.startStr, item.endStr);
          if (range.expanded) {
            for (const val of range.expanded) {
              operationCounter.count++;
              braceValues.push(val);
            }
          } else {
            hasInvalidRange = true;
            invalidRangeLiteral = range.literal;
            break;
          }
        } else {
          const expanded = await expandBracesInPartsAsync(ctx, item.word.parts, operationCounter);
          for (const exp of expanded) {
            operationCounter.count++;
            const joinedParts = [];
            for (const p of exp) {
              if (typeof p === "string") {
                joinedParts.push(p);
              } else {
                joinedParts.push(await expandPart(ctx, p));
              }
            }
            braceValues.push(joinedParts.join(""));
          }
        }
      }
      if (hasInvalidRange) {
        for (const result2 of results) {
          operationCounter.count++;
          result2.push(invalidRangeLiteral);
        }
        continue;
      }
      const newSize = results.length * braceValues.length;
      if (newSize > MAX_BRACE_EXPANSION_RESULTS || operationCounter.count > MAX_BRACE_OPERATIONS) {
        return results;
      }
      const newResults = [];
      for (const result2 of results) {
        for (const val of braceValues) {
          operationCounter.count++;
          if (operationCounter.count > MAX_BRACE_OPERATIONS) {
            return newResults.length > 0 ? newResults : results;
          }
          newResults.push([...result2, val]);
        }
      }
      results = newResults;
    } else {
      for (const result2 of results) {
        operationCounter.count++;
        result2.push(part);
      }
    }
  }
  return results;
}
__name$1(expandBracesInPartsAsync, "expandBracesInPartsAsync");
async function expandWordWithBracesAsync(ctx, word) {
  const parts = word.parts;
  if (!hasBraceExpansion(parts)) {
    return [await expandWord(ctx, word)];
  }
  const expanded = await expandBracesInPartsAsync(ctx, parts);
  const results = [];
  for (const resultParts of expanded) {
    const joinedParts = [];
    for (const p of resultParts) {
      if (typeof p === "string") {
        joinedParts.push(p);
      } else {
        joinedParts.push(await expandPart(ctx, p));
      }
    }
    results.push(applyTildeExpansion(ctx, joinedParts.join("")));
  }
  return results;
}
__name$1(expandWordWithBracesAsync, "expandWordWithBracesAsync");
function createWordGlobDeps() {
  return {
    expandWordAsync,
    expandWordForGlobbing,
    expandWordWithBracesAsync,
    expandWordPartsAsync,
    expandPart,
    expandParameterAsync,
    hasBraceExpansion,
    evaluateArithmetic,
    buildIfsCharClassPattern,
    smartWordSplit
  };
}
__name$1(createWordGlobDeps, "createWordGlobDeps");
async function expandWordWithGlob(ctx, word) {
  return expandWordWithGlobImpl(ctx, word, createWordGlobDeps());
}
__name$1(expandWordWithGlob, "expandWordWithGlob");
function getWordText(parts) {
  for (const p of parts) {
    if (p.type === "ParameterExpansion") {
      return p.parameter;
    }
    if (p.type === "Literal") {
      return p.value;
    }
  }
  return "";
}
__name$1(getWordText, "getWordText");
function hasQuotedMultiValueAt(ctx, word) {
  const numParams = Number.parseInt(ctx.state.env["#"] || "0", 10);
  if (numParams < 2)
    return false;
  function checkParts(parts) {
    for (const part of parts) {
      if (part.type === "DoubleQuoted") {
        for (const innerPart of part.parts) {
          if (innerPart.type === "ParameterExpansion" && innerPart.parameter === "@" && !innerPart.operation) {
            return true;
          }
        }
      }
    }
    return false;
  }
  __name$1(checkParts, "checkParts");
  return checkParts(word.parts);
}
__name$1(hasQuotedMultiValueAt, "hasQuotedMultiValueAt");
async function expandRedirectTarget(ctx, word) {
  if (hasQuotedMultiValueAt(ctx, word)) {
    return { error: "bash: $@: ambiguous redirect\n" };
  }
  const wordParts = word.parts;
  const { hasQuoted } = analyzeWordParts(wordParts);
  if (hasBraceExpansion(wordParts)) {
    const braceExpanded = await expandWordWithBracesAsync(ctx, word);
    if (braceExpanded.length > 1) {
      const originalText = wordParts.map((p) => {
        if (p.type === "Literal")
          return p.value;
        if (p.type === "BraceExpansion") {
          const items = p.items.map((item) => {
            if (item.type === "Range") {
              const step = item.step ? `..${item.step}` : "";
              return `${item.startStr ?? item.start}..${item.endStr ?? item.end}${step}`;
            }
            return item.word.parts.map((wp) => wp.type === "Literal" ? wp.value : "").join("");
          }).join(",");
          return `{${items}}`;
        }
        return "";
      }).join("");
      return { error: `bash: ${originalText}: ambiguous redirect
` };
    }
  }
  const value = await expandWordAsync(ctx, word);
  const { hasParamExpansion, hasCommandSub } = analyzeWordParts(wordParts);
  const hasUnquotedExpansion = (hasParamExpansion || hasCommandSub) && !hasQuoted;
  if (hasUnquotedExpansion && !isIfsEmpty(ctx.state.env)) {
    const ifsChars = getIfs(ctx.state.env);
    const splitWords = splitByIfsForExpansion(value, ifsChars);
    if (splitWords.length > 1) {
      return {
        error: `bash: $${getWordText(wordParts)}: ambiguous redirect
`
      };
    }
  }
  if (hasQuoted || ctx.state.options.noglob) {
    return { target: value };
  }
  const globPattern = await expandWordForGlobbing(ctx, word);
  if (!hasGlobPattern(globPattern, ctx.state.shoptOptions.extglob)) {
    return { target: value };
  }
  const globExpander = new GlobExpander(ctx.fs, ctx.state.cwd, ctx.state.env, {
    globstar: ctx.state.shoptOptions.globstar,
    nullglob: ctx.state.shoptOptions.nullglob,
    failglob: ctx.state.shoptOptions.failglob,
    dotglob: ctx.state.shoptOptions.dotglob,
    extglob: ctx.state.shoptOptions.extglob,
    globskipdots: ctx.state.shoptOptions.globskipdots
  });
  const matches = await globExpander.expand(globPattern);
  if (matches.length === 0) {
    if (globExpander.hasFailglob()) {
      return { error: `bash: no match: ${value}
` };
    }
    return { target: value };
  }
  if (matches.length === 1) {
    return { target: matches[0] };
  }
  return { error: `bash: ${value}: ambiguous redirect
` };
}
__name$1(expandRedirectTarget, "expandRedirectTarget");
async function expandWordAsync(ctx, word) {
  const wordParts = word.parts;
  const len = wordParts.length;
  if (len === 1) {
    return expandPart(ctx, wordParts[0]);
  }
  const parts = [];
  for (let i = 0; i < len; i++) {
    parts.push(await expandPart(ctx, wordParts[i]));
  }
  return parts.join("");
}
__name$1(expandWordAsync, "expandWordAsync");
async function expandPart(ctx, part, inDoubleQuotes = false) {
  if (part.type === "ParameterExpansion") {
    return expandParameterAsync(ctx, part, inDoubleQuotes);
  }
  const simple = expandSimplePart(ctx, part, inDoubleQuotes);
  if (simple !== null)
    return simple;
  switch (part.type) {
    case "DoubleQuoted": {
      const parts = [];
      for (const p of part.parts) {
        parts.push(await expandPart(ctx, p, true));
      }
      return parts.join("");
    }
    case "CommandSubstitution": {
      const fileReadShorthand = getFileReadShorthand(part.body);
      if (fileReadShorthand) {
        try {
          const filePath = await expandWord(ctx, fileReadShorthand.target);
          const resolvedPath = filePath.startsWith("/") ? filePath : `${ctx.state.cwd}/${filePath}`;
          const content = await ctx.fs.readFile(resolvedPath);
          ctx.state.lastExitCode = 0;
          ctx.state.env["?"] = "0";
          return content.replace(/\n+$/, "");
        } catch {
          ctx.state.lastExitCode = 1;
          ctx.state.env["?"] = "1";
          return "";
        }
      }
      const savedBashPid = ctx.state.bashPid;
      ctx.state.bashPid = ctx.state.nextVirtualPid++;
      const savedEnv = { ...ctx.state.env };
      const savedCwd = ctx.state.cwd;
      const savedSuppressVerbose = ctx.state.suppressVerbose;
      ctx.state.suppressVerbose = true;
      try {
        const result2 = await ctx.executeScript(part.body);
        const exitCode = result2.exitCode;
        ctx.state.env = savedEnv;
        ctx.state.cwd = savedCwd;
        ctx.state.suppressVerbose = savedSuppressVerbose;
        ctx.state.lastExitCode = exitCode;
        ctx.state.env["?"] = String(exitCode);
        if (result2.stderr) {
          ctx.state.expansionStderr = (ctx.state.expansionStderr || "") + result2.stderr;
        }
        ctx.state.bashPid = savedBashPid;
        return result2.stdout.replace(/\n+$/, "");
      } catch (error) {
        ctx.state.env = savedEnv;
        ctx.state.cwd = savedCwd;
        ctx.state.bashPid = savedBashPid;
        ctx.state.suppressVerbose = savedSuppressVerbose;
        if (error instanceof ExecutionLimitError) {
          throw error;
        }
        if (error instanceof ExitError) {
          ctx.state.lastExitCode = error.exitCode;
          ctx.state.env["?"] = String(error.exitCode);
          if (error.stderr) {
            ctx.state.expansionStderr = (ctx.state.expansionStderr || "") + error.stderr;
          }
          return error.stdout.replace(/\n+$/, "");
        }
        throw error;
      }
    }
    case "ArithmeticExpansion": {
      const originalText = part.expression.originalText;
      const hasDollarVars = originalText && /\$[a-zA-Z_][a-zA-Z0-9_]*(?![{[(])/.test(originalText);
      if (hasDollarVars) {
        const expandedText = await expandDollarVarsInArithText(ctx, originalText);
        const parser = new Parser();
        const newExpr = parseArithmeticExpression(parser, expandedText);
        return String(await evaluateArithmetic(ctx, newExpr.expression, true));
      }
      return String(await evaluateArithmetic(ctx, part.expression.expression, true));
    }
    case "BraceExpansion": {
      const results = [];
      for (const item of part.items) {
        if (item.type === "Range") {
          const range = expandBraceRange(item.start, item.end, item.step, item.startStr, item.endStr);
          if (range.expanded) {
            results.push(...range.expanded);
          } else {
            return range.literal;
          }
        } else {
          results.push(await expandWord(ctx, item.word));
        }
      }
      return results.join(" ");
    }
    default:
      return "";
  }
}
__name$1(expandPart, "expandPart");
async function expandParameterAsync(ctx, part, inDoubleQuotes = false) {
  let { parameter } = part;
  const { operation } = part;
  const bracketMatch = parameter.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
  if (bracketMatch) {
    const [, arrayName, subscript] = bracketMatch;
    const isAssoc = ctx.state.associativeArrays?.has(arrayName);
    if (isAssoc || subscript.includes("$(") || subscript.includes("`") || subscript.includes("${")) {
      const expandedSubscript = await expandSubscriptForAssocArray(ctx, subscript);
      parameter = `${arrayName}[${expandedSubscript}]`;
    }
  } else if (
    // Handle nameref pointing to array subscript with command substitution:
    // typeset -n ref='a[$(echo 2) + 1]'; echo $ref
    /^[a-zA-Z_][a-zA-Z0-9_]*$/.test(parameter) && isNameref(ctx, parameter)
  ) {
    const target = resolveNameref(ctx, parameter);
    if (target && target !== parameter) {
      const targetBracketMatch = target.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
      if (targetBracketMatch) {
        const [, targetArrayName, targetSubscript] = targetBracketMatch;
        const isAssoc = ctx.state.associativeArrays?.has(targetArrayName);
        if (isAssoc || targetSubscript.includes("$(") || targetSubscript.includes("`") || targetSubscript.includes("${")) {
          const expandedSubscript = await expandSubscriptForAssocArray(ctx, targetSubscript);
          parameter = `${targetArrayName}[${expandedSubscript}]`;
        }
      }
    }
  }
  const skipNounset = operation && (operation.type === "DefaultValue" || operation.type === "AssignDefault" || operation.type === "UseAlternative" || operation.type === "ErrorIfUnset");
  const value = await getVariable(ctx, parameter, !skipNounset);
  if (!operation) {
    return value;
  }
  const isUnset = !await isVariableSet(ctx, parameter);
  const { isEmpty, effectiveValue } = computeIsEmpty(ctx, parameter, value, inDoubleQuotes);
  const opCtx = {
    isUnset,
    isEmpty,
    effectiveValue,
    inDoubleQuotes
  };
  switch (operation.type) {
    case "DefaultValue":
      return handleDefaultValue(ctx, operation, opCtx, expandWordPartsAsync);
    case "AssignDefault":
      return handleAssignDefault(ctx, parameter, operation, opCtx, expandWordPartsAsync);
    case "ErrorIfUnset":
      return handleErrorIfUnset(ctx, parameter, operation, opCtx, expandWordPartsAsync);
    case "UseAlternative":
      return handleUseAlternative(ctx, operation, opCtx, expandWordPartsAsync);
    case "PatternRemoval":
      return handlePatternRemoval(ctx, value, operation, expandWordPartsAsync, expandPart);
    case "PatternReplacement":
      return handlePatternReplacement(ctx, value, operation, expandWordPartsAsync, expandPart);
    case "Length":
      return handleLength(ctx, parameter, value);
    case "LengthSliceError":
      throw new BadSubstitutionError(parameter);
    case "BadSubstitution":
      throw new BadSubstitutionError(operation.text);
    case "Substring":
      return handleSubstring(ctx, parameter, value, operation);
    case "CaseModification":
      return handleCaseModification(ctx, value, operation, expandWordPartsAsync, expandParameterAsync);
    case "Transform":
      return handleTransform(ctx, parameter, value, isUnset, operation);
    case "Indirection":
      return handleIndirection(ctx, parameter, value, isUnset, operation, expandParameterAsync, inDoubleQuotes);
    case "ArrayKeys":
      return handleArrayKeys(ctx, operation);
    case "VarNamePrefix":
      return handleVarNamePrefix(ctx, operation);
    default:
      return value;
  }
}
__name$1(expandParameterAsync, "expandParameterAsync");
function getErrorMessage(error) {
  return error instanceof Error ? error.message : String(error);
}
__name$1(getErrorMessage, "getErrorMessage");
var WORKFLOW_SERIALIZE$1 = /* @__PURE__ */ Symbol.for("workflow-serialize");
var WORKFLOW_DESERIALIZE$1 = /* @__PURE__ */ Symbol.for("workflow-deserialize");
var commandLoaders = [
  // Basic I/O
  {
    name: "echo",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/echo-4Q24LB22.mjs")).echoCommand, "load")
  },
  {
    name: "cat",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/cat-B5PH27R7.mjs")).catCommand, "load")
  },
  {
    name: "printf",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/printf-ZZ4KXEG7.mjs")).printfCommand, "load")
  },
  // File operations
  {
    name: "ls",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/ls-ITHR6NA5.mjs")).lsCommand, "load")
  },
  {
    name: "mkdir",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/mkdir-DPRWIIWK.mjs")).mkdirCommand, "load")
  },
  {
    name: "rmdir",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/rmdir-JZSE3DID.mjs")).rmdirCommand, "load")
  },
  {
    name: "touch",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/touch-OBPNOMXA.mjs")).touchCommand, "load")
  },
  {
    name: "rm",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/rm-EDUVHVWA.mjs")).rmCommand, "load")
  },
  {
    name: "cp",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/cp-GCW6DPKS.mjs")).cpCommand, "load")
  },
  {
    name: "mv",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/mv-O7MACADD.mjs")).mvCommand, "load")
  },
  {
    name: "ln",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/ln-YRTKPH24.mjs")).lnCommand, "load")
  },
  {
    name: "chmod",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/chmod-MZSIIAM2.mjs")).chmodCommand, "load")
  },
  // Navigation
  {
    name: "pwd",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/pwd-UZ5Z7KB2.mjs")).pwdCommand, "load")
  },
  {
    name: "readlink",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/readlink-UX2V3KZO.mjs")).readlinkCommand, "load")
  },
  // File viewing
  {
    name: "head",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/head-MAMZPQ4G.mjs")).headCommand, "load")
  },
  {
    name: "tail",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/tail-QAYF3564.mjs")).tailCommand, "load")
  },
  {
    name: "wc",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/wc-OEWLC524.mjs")).wcCommand, "load")
  },
  {
    name: "stat",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/stat-3JBZSO27.mjs")).statCommand, "load")
  },
  // Text processing
  {
    name: "grep",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/grep-2LQO4DI4.mjs")).grepCommand, "load")
  },
  {
    name: "fgrep",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/grep-2LQO4DI4.mjs")).fgrepCommand, "load")
  },
  {
    name: "egrep",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/grep-2LQO4DI4.mjs")).egrepCommand, "load")
  },
  {
    name: "rg",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/rg-3TQI6FXH.mjs")).rgCommand, "load")
  },
  {
    name: "sed",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/sed-2VVO2AX6.mjs")).sedCommand, "load")
  },
  {
    name: "awk",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/awk2-DZVMO7HO.mjs")).awkCommand2, "load")
  },
  {
    name: "sort",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/sort-HZ26NJFX.mjs")).sortCommand, "load")
  },
  {
    name: "uniq",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/uniq-2NSIND2Z.mjs")).uniqCommand, "load")
  },
  {
    name: "comm",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/comm-26O3DPXS.mjs")).commCommand, "load")
  },
  {
    name: "cut",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/cut-EIFIJONN.mjs")).cutCommand, "load")
  },
  {
    name: "paste",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/paste-CRP7HJIL.mjs")).pasteCommand, "load")
  },
  {
    name: "tr",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/tr-EU3C37E7.mjs")).trCommand, "load")
  },
  {
    name: "rev",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/rev-TO32M6QM.mjs")).rev, "load")
  },
  {
    name: "nl",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/nl-VB4EELAK.mjs")).nl, "load")
  },
  {
    name: "fold",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/fold-HRC5RKDU.mjs")).fold, "load")
  },
  {
    name: "expand",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/expand-45TJED4V.mjs")).expand, "load")
  },
  {
    name: "unexpand",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/unexpand-EFNDJ2XW.mjs")).unexpand, "load")
  },
  {
    name: "strings",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/strings-A3U3X73Y.mjs")).strings, "load")
  },
  {
    name: "split",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/split-EKWHRCLK.mjs")).split, "load")
  },
  {
    name: "column",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/column-44HJIOZZ.mjs")).column, "load")
  },
  {
    name: "join",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/join-GKVO4J5L.mjs")).join, "load")
  },
  {
    name: "tee",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/tee-E5IFE6IH.mjs")).teeCommand, "load")
  },
  // Search
  {
    name: "find",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/find-UMVOHB4Z.mjs")).findCommand, "load")
  },
  // Path utilities
  {
    name: "basename",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/basename-PRSHUE2H.mjs")).basenameCommand, "load")
  },
  {
    name: "dirname",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/dirname-UWVOMVEF.mjs")).dirnameCommand, "load")
  },
  // Directory utilities
  {
    name: "tree",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/tree-UUS43NLC.mjs")).treeCommand, "load")
  },
  {
    name: "du",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/du-TNOLWKGM.mjs")).duCommand, "load")
  },
  // Environment
  {
    name: "env",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/env-LIAAB6DX.mjs")).envCommand, "load")
  },
  {
    name: "printenv",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/env-LIAAB6DX.mjs")).printenvCommand, "load")
  },
  {
    name: "alias",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/alias-QMPFO2GL.mjs")).aliasCommand, "load")
  },
  {
    name: "unalias",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/alias-QMPFO2GL.mjs")).unaliasCommand, "load")
  },
  {
    name: "history",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/history-4GQ2YJGE.mjs")).historyCommand, "load")
  },
  // Utilities
  {
    name: "xargs",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/xargs-VLOAFEYW.mjs")).xargsCommand, "load")
  },
  {
    name: "true",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/true-G2JQF2T5.mjs")).trueCommand, "load")
  },
  {
    name: "false",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/true-G2JQF2T5.mjs")).falseCommand, "load")
  },
  {
    name: "clear",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/clear-ECGGFZOE.mjs")).clearCommand, "load")
  },
  // Shell
  {
    name: "bash",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/bash-BV44E2AQ.mjs")).bashCommand, "load")
  },
  {
    name: "sh",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/bash-BV44E2AQ.mjs")).shCommand, "load")
  },
  // Data processing
  {
    name: "jq",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/jq-JONOBNUR.mjs")).jqCommand, "load")
  },
  {
    name: "base64",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/base64-UXIW5DLW.mjs")).base64Command, "load")
  },
  {
    name: "diff",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/diff-ZFQ3G62K.mjs")).diffCommand, "load")
  },
  {
    name: "date",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/date-UX4T6DZI.mjs")).dateCommand, "load")
  },
  {
    name: "sleep",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/sleep-NLREXEY3.mjs")).sleepCommand, "load")
  },
  {
    name: "timeout",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/timeout-S2QKAXZ5.mjs")).timeoutCommand, "load")
  },
  {
    name: "time",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/time-KQHYMHQP.mjs")).timeCommand, "load")
  },
  {
    name: "seq",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/seq-NIXAHVIM.mjs")).seqCommand, "load")
  },
  {
    name: "expr",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/expr-TJEIOE4P.mjs")).exprCommand, "load")
  },
  // Checksums
  {
    name: "md5sum",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/md5sum-YUAHKVHJ.mjs")).md5sumCommand, "load")
  },
  {
    name: "sha1sum",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/sha1sum-R7H4OKNW.mjs")).sha1sumCommand, "load")
  },
  {
    name: "sha256sum",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/sha256sum-TNNNTOHR.mjs")).sha256sumCommand, "load")
  },
  // File type detection
  {
    name: "file",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/file-3NDKDBH7.mjs")).fileCommand, "load")
  },
  // HTML processing
  {
    name: "html-to-markdown",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/html-to-markdown-TH7JLL6O.mjs")).htmlToMarkdownCommand, "load")
  },
  // Help
  {
    name: "help",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/help-ANYWV4TI.mjs")).helpCommand, "load")
  },
  // PATH utilities
  {
    name: "which",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/which-WBT5TKUV.mjs")).whichCommand, "load")
  },
  // Misc utilities
  {
    name: "tac",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/tac-O6MB7KMO.mjs")).tac, "load")
  },
  {
    name: "hostname",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/hostname-J4E23M7W.mjs")).hostname, "load")
  },
  {
    name: "whoami",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/whoami-CSC7NWHZ.mjs")).whoami, "load")
  },
  {
    name: "od",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/od-C3HXAFA4.mjs")).od, "load")
  },
  // Compression
  {
    name: "gzip",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/gzip-EDT74RBR.mjs")).gzipCommand, "load")
  },
  {
    name: "gunzip",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/gzip-EDT74RBR.mjs")).gunzipCommand, "load")
  },
  {
    name: "zcat",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/gzip-EDT74RBR.mjs")).zcatCommand, "load")
  }
];
if (typeof __BROWSER__ === "undefined" || !__BROWSER__) {
  commandLoaders.push({
    name: "tar",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/tar-LMPCAEUP.mjs")).tarCommand, "load")
  });
  commandLoaders.push({
    name: "yq",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/yq-MNUMXC25.mjs")).yqCommand, "load")
  });
  commandLoaders.push({
    name: "xan",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/xan-HGI54YYH.mjs")).xanCommand, "load")
  });
  commandLoaders.push({
    name: "sqlite3",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/sqlite3-Z25OARQE.mjs")).sqlite3Command, "load")
  });
  commandLoaders.push({
    name: "python3",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/python3-IQN6VCMJ.mjs")).python3Command, "load")
  });
  commandLoaders.push({
    name: "python",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/python3-IQN6VCMJ.mjs")).pythonCommand, "load")
  });
}
var networkCommandLoaders = [
  {
    name: "curl",
    load: /* @__PURE__ */ __name$1(async () => (await import("./_chunks/curl-SQ7JITGL.mjs")).curlCommand, "load")
  }
];
var cache = /* @__PURE__ */ new Map();
function createLazyCommand(def) {
  return {
    name: def.name,
    async execute(args, ctx) {
      let cmd = cache.get(def.name);
      if (!cmd) {
        cmd = await def.load();
        cache.set(def.name, cmd);
      }
      return cmd.execute(args, ctx);
    }
  };
}
__name$1(createLazyCommand, "createLazyCommand");
function getCommandNames() {
  return commandLoaders.map((def) => def.name);
}
__name$1(getCommandNames, "getCommandNames");
function getNetworkCommandNames() {
  return networkCommandLoaders.map((def) => def.name);
}
__name$1(getNetworkCommandNames, "getNetworkCommandNames");
function createLazyCommands(filter) {
  const loaders = filter ? commandLoaders.filter((def) => filter.includes(def.name)) : commandLoaders;
  return loaders.map(createLazyCommand);
}
__name$1(createLazyCommands, "createLazyCommands");
function createNetworkCommands() {
  return networkCommandLoaders.map(createLazyCommand);
}
__name$1(createNetworkCommands, "createNetworkCommands");
function isLazyCommand(cmd) {
  return "load" in cmd && typeof cmd.load === "function";
}
__name$1(isLazyCommand, "isLazyCommand");
function defineCommand(name, execute) {
  return { name, execute };
}
__name$1(defineCommand, "defineCommand");
function createLazyCustomCommand(lazy2) {
  let cached = null;
  return {
    name: lazy2.name,
    async execute(args, ctx) {
      if (!cached) {
        cached = await lazy2.load();
      }
      return cached.execute(args, ctx);
    }
  };
}
__name$1(createLazyCustomCommand, "createLazyCustomCommand");
var textEncoder$1 = new TextEncoder();
var textDecoder$1 = new TextDecoder();
function toBuffer$1(content, encoding) {
  if (content instanceof Uint8Array) {
    return content;
  }
  if (encoding === "base64") {
    return Uint8Array.from(atob(content), (c) => c.charCodeAt(0));
  }
  if (encoding === "hex") {
    const bytes = new Uint8Array(content.length / 2);
    for (let i = 0; i < content.length; i += 2) {
      bytes[i / 2] = parseInt(content.slice(i, i + 2), 16);
    }
    return bytes;
  }
  if (encoding === "binary" || encoding === "latin1") {
    return Uint8Array.from(content, (c) => c.charCodeAt(0));
  }
  return textEncoder$1.encode(content);
}
__name$1(toBuffer$1, "toBuffer");
function fromBuffer$1(buffer, encoding) {
  if (encoding === "base64") {
    return btoa(String.fromCharCode(...buffer));
  }
  if (encoding === "hex") {
    return Array.from(buffer).map((b) => b.toString(16).padStart(2, "0")).join("");
  }
  if (encoding === "binary" || encoding === "latin1") {
    return String.fromCharCode(...buffer);
  }
  return textDecoder$1.decode(buffer);
}
__name$1(fromBuffer$1, "fromBuffer");
function getEncoding$1(options) {
  if (options === null || options === void 0) {
    return void 0;
  }
  if (typeof options === "string") {
    return options;
  }
  return options.encoding ?? void 0;
}
__name$1(getEncoding$1, "getEncoding");
var textEncoder2$1 = new TextEncoder();
function isFileInit$1(value) {
  return typeof value === "object" && value !== null && !(value instanceof Uint8Array) && "content" in value;
}
__name$1(isFileInit$1, "isFileInit");
var InMemoryFs$1 = (_t = class {
  data = /* @__PURE__ */ new Map();
  constructor(initialFiles) {
    this.data.set("/", { type: "directory", mode: 493, mtime: /* @__PURE__ */ new Date() });
    if (initialFiles) {
      for (const [path2, value] of Object.entries(initialFiles)) {
        if (isFileInit$1(value)) {
          this.writeFileSync(path2, value.content, void 0, {
            mode: value.mode,
            mtime: value.mtime
          });
        } else {
          this.writeFileSync(path2, value);
        }
      }
    }
  }
  normalizePath(path2) {
    if (!path2 || path2 === "/")
      return "/";
    let normalized = path2.endsWith("/") && path2 !== "/" ? path2.slice(0, -1) : path2;
    if (!normalized.startsWith("/")) {
      normalized = `/${normalized}`;
    }
    const parts = normalized.split("/").filter((p) => p && p !== ".");
    const resolved = [];
    for (const part of parts) {
      if (part === "..") {
        resolved.pop();
      } else {
        resolved.push(part);
      }
    }
    return `/${resolved.join("/")}` || "/";
  }
  dirname(path2) {
    const normalized = this.normalizePath(path2);
    if (normalized === "/")
      return "/";
    const lastSlash = normalized.lastIndexOf("/");
    return lastSlash === 0 ? "/" : normalized.slice(0, lastSlash);
  }
  ensureParentDirs(path2) {
    const dir = this.dirname(path2);
    if (dir === "/")
      return;
    if (!this.data.has(dir)) {
      this.ensureParentDirs(dir);
      this.data.set(dir, { type: "directory", mode: 493, mtime: /* @__PURE__ */ new Date() });
    }
  }
  // Sync method for writing files
  writeFileSync(path2, content, options, metadata) {
    const normalized = this.normalizePath(path2);
    this.ensureParentDirs(normalized);
    const encoding = getEncoding$1(options);
    const buffer = toBuffer$1(content, encoding);
    this.data.set(normalized, {
      type: "file",
      content: buffer,
      mode: metadata?.mode ?? 420,
      mtime: metadata?.mtime ?? /* @__PURE__ */ new Date()
    });
  }
  // Async public API
  async readFile(path2, options) {
    const buffer = await this.readFileBuffer(path2);
    const encoding = getEncoding$1(options);
    return fromBuffer$1(buffer, encoding);
  }
  async readFileBuffer(path2) {
    const resolvedPath = this.resolvePathWithSymlinks(path2);
    const entry = this.data.get(resolvedPath);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, open '${path2}'`);
    }
    if (entry.type !== "file") {
      throw new Error(`EISDIR: illegal operation on a directory, read '${path2}'`);
    }
    if (entry.content instanceof Uint8Array) {
      return entry.content;
    }
    return textEncoder2$1.encode(entry.content);
  }
  async writeFile(path2, content, options) {
    this.writeFileSync(path2, content, options);
  }
  async appendFile(path2, content, options) {
    const normalized = this.normalizePath(path2);
    const existing = this.data.get(normalized);
    if (existing && existing.type === "directory") {
      throw new Error(`EISDIR: illegal operation on a directory, write '${path2}'`);
    }
    const encoding = getEncoding$1(options);
    const newBuffer = toBuffer$1(content, encoding);
    if (existing?.type === "file") {
      const existingBuffer = existing.content instanceof Uint8Array ? existing.content : textEncoder2$1.encode(existing.content);
      const combined = new Uint8Array(existingBuffer.length + newBuffer.length);
      combined.set(existingBuffer);
      combined.set(newBuffer, existingBuffer.length);
      this.data.set(normalized, {
        type: "file",
        content: combined,
        mode: existing.mode,
        mtime: /* @__PURE__ */ new Date()
      });
    } else {
      this.writeFileSync(path2, content, options);
    }
  }
  async exists(path2) {
    try {
      const resolvedPath = this.resolvePathWithSymlinks(path2);
      return this.data.has(resolvedPath);
    } catch {
      return false;
    }
  }
  async stat(path2) {
    const resolvedPath = this.resolvePathWithSymlinks(path2);
    const entry = this.data.get(resolvedPath);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, stat '${path2}'`);
    }
    let size = 0;
    if (entry.type === "file" && entry.content) {
      if (entry.content instanceof Uint8Array) {
        size = entry.content.length;
      } else {
        size = textEncoder2$1.encode(entry.content).length;
      }
    }
    return {
      isFile: entry.type === "file",
      isDirectory: entry.type === "directory",
      isSymbolicLink: false,
      // stat follows symlinks, so this is always false
      mode: entry.mode,
      size,
      mtime: entry.mtime || /* @__PURE__ */ new Date()
    };
  }
  async lstat(path2) {
    const resolvedPath = this.resolveIntermediateSymlinks(path2);
    const entry = this.data.get(resolvedPath);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, lstat '${path2}'`);
    }
    if (entry.type === "symlink") {
      return {
        isFile: false,
        isDirectory: false,
        isSymbolicLink: true,
        mode: entry.mode,
        size: entry.target.length,
        mtime: entry.mtime || /* @__PURE__ */ new Date()
      };
    }
    let size = 0;
    if (entry.type === "file" && entry.content) {
      if (entry.content instanceof Uint8Array) {
        size = entry.content.length;
      } else {
        size = textEncoder2$1.encode(entry.content).length;
      }
    }
    return {
      isFile: entry.type === "file",
      isDirectory: entry.type === "directory",
      isSymbolicLink: false,
      mode: entry.mode,
      size,
      mtime: entry.mtime || /* @__PURE__ */ new Date()
    };
  }
  // Helper to resolve symlink target paths
  resolveSymlink(symlinkPath, target) {
    if (target.startsWith("/")) {
      return this.normalizePath(target);
    }
    const dir = this.dirname(symlinkPath);
    return this.normalizePath(dir === "/" ? `/${target}` : `${dir}/${target}`);
  }
  /**
   * Resolve symlinks in intermediate path components only (not the final component).
   * Used by lstat which should not follow the final symlink.
   */
  resolveIntermediateSymlinks(path2) {
    const normalized = this.normalizePath(path2);
    if (normalized === "/")
      return "/";
    const parts = normalized.slice(1).split("/");
    if (parts.length <= 1)
      return normalized;
    let resolvedPath = "";
    const seen = /* @__PURE__ */ new Set();
    for (let i = 0; i < parts.length - 1; i++) {
      const part = parts[i];
      resolvedPath = `${resolvedPath}/${part}`;
      let entry = this.data.get(resolvedPath);
      let loopCount = 0;
      const maxLoops = 40;
      while (entry && entry.type === "symlink" && loopCount < maxLoops) {
        if (seen.has(resolvedPath)) {
          throw new Error(`ELOOP: too many levels of symbolic links, lstat '${path2}'`);
        }
        seen.add(resolvedPath);
        resolvedPath = this.resolveSymlink(resolvedPath, entry.target);
        entry = this.data.get(resolvedPath);
        loopCount++;
      }
      if (loopCount >= maxLoops) {
        throw new Error(`ELOOP: too many levels of symbolic links, lstat '${path2}'`);
      }
    }
    return `${resolvedPath}/${parts[parts.length - 1]}`;
  }
  /**
   * Resolve all symlinks in a path, including intermediate components.
   * For example: /home/user/linkdir/file.txt where linkdir is a symlink to "subdir"
   * would resolve to /home/user/subdir/file.txt
   */
  resolvePathWithSymlinks(path2) {
    const normalized = this.normalizePath(path2);
    if (normalized === "/")
      return "/";
    const parts = normalized.slice(1).split("/");
    let resolvedPath = "";
    const seen = /* @__PURE__ */ new Set();
    for (const part of parts) {
      resolvedPath = `${resolvedPath}/${part}`;
      let entry = this.data.get(resolvedPath);
      let loopCount = 0;
      const maxLoops = 40;
      while (entry && entry.type === "symlink" && loopCount < maxLoops) {
        if (seen.has(resolvedPath)) {
          throw new Error(`ELOOP: too many levels of symbolic links, open '${path2}'`);
        }
        seen.add(resolvedPath);
        resolvedPath = this.resolveSymlink(resolvedPath, entry.target);
        entry = this.data.get(resolvedPath);
        loopCount++;
      }
      if (loopCount >= maxLoops) {
        throw new Error(`ELOOP: too many levels of symbolic links, open '${path2}'`);
      }
    }
    return resolvedPath;
  }
  async mkdir(path2, options) {
    this.mkdirSync(path2, options);
  }
  /**
   * Synchronous version of mkdir
   */
  mkdirSync(path2, options) {
    const normalized = this.normalizePath(path2);
    if (this.data.has(normalized)) {
      const entry = this.data.get(normalized);
      if (entry?.type === "file") {
        throw new Error(`EEXIST: file already exists, mkdir '${path2}'`);
      }
      if (!options?.recursive) {
        throw new Error(`EEXIST: directory already exists, mkdir '${path2}'`);
      }
      return;
    }
    const parent = this.dirname(normalized);
    if (parent !== "/" && !this.data.has(parent)) {
      if (options?.recursive) {
        this.mkdirSync(parent, { recursive: true });
      } else {
        throw new Error(`ENOENT: no such file or directory, mkdir '${path2}'`);
      }
    }
    this.data.set(normalized, {
      type: "directory",
      mode: 493,
      mtime: /* @__PURE__ */ new Date()
    });
  }
  async readdir(path2) {
    const entries = await this.readdirWithFileTypes(path2);
    return entries.map((e) => e.name);
  }
  async readdirWithFileTypes(path2) {
    let normalized = this.normalizePath(path2);
    let entry = this.data.get(normalized);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, scandir '${path2}'`);
    }
    const seen = /* @__PURE__ */ new Set();
    while (entry && entry.type === "symlink") {
      if (seen.has(normalized)) {
        throw new Error(`ELOOP: too many levels of symbolic links, scandir '${path2}'`);
      }
      seen.add(normalized);
      normalized = this.resolveSymlink(normalized, entry.target);
      entry = this.data.get(normalized);
    }
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, scandir '${path2}'`);
    }
    if (entry.type !== "directory") {
      throw new Error(`ENOTDIR: not a directory, scandir '${path2}'`);
    }
    const prefix = normalized === "/" ? "/" : `${normalized}/`;
    const entriesMap = /* @__PURE__ */ new Map();
    for (const [p, fsEntry] of this.data.entries()) {
      if (p === normalized)
        continue;
      if (p.startsWith(prefix)) {
        const rest = p.slice(prefix.length);
        const name = rest.split("/")[0];
        if (name && !rest.includes("/", name.length) && !entriesMap.has(name)) {
          entriesMap.set(name, {
            name,
            isFile: fsEntry.type === "file",
            isDirectory: fsEntry.type === "directory",
            isSymbolicLink: fsEntry.type === "symlink"
          });
        }
      }
    }
    return Array.from(entriesMap.values()).sort((a, b) => a.name < b.name ? -1 : a.name > b.name ? 1 : 0);
  }
  async rm(path2, options) {
    const normalized = this.normalizePath(path2);
    const entry = this.data.get(normalized);
    if (!entry) {
      if (options?.force)
        return;
      throw new Error(`ENOENT: no such file or directory, rm '${path2}'`);
    }
    if (entry.type === "directory") {
      const children = await this.readdir(normalized);
      if (children.length > 0) {
        if (!options?.recursive) {
          throw new Error(`ENOTEMPTY: directory not empty, rm '${path2}'`);
        }
        for (const child of children) {
          const childPath = normalized === "/" ? `/${child}` : `${normalized}/${child}`;
          await this.rm(childPath, options);
        }
      }
    }
    this.data.delete(normalized);
  }
  async cp(src, dest, options) {
    const srcNorm = this.normalizePath(src);
    const destNorm = this.normalizePath(dest);
    const srcEntry = this.data.get(srcNorm);
    if (!srcEntry) {
      throw new Error(`ENOENT: no such file or directory, cp '${src}'`);
    }
    if (srcEntry.type === "file") {
      this.ensureParentDirs(destNorm);
      this.data.set(destNorm, { ...srcEntry });
    } else if (srcEntry.type === "directory") {
      if (!options?.recursive) {
        throw new Error(`EISDIR: is a directory, cp '${src}'`);
      }
      await this.mkdir(destNorm, { recursive: true });
      const children = await this.readdir(srcNorm);
      for (const child of children) {
        const srcChild = srcNorm === "/" ? `/${child}` : `${srcNorm}/${child}`;
        const destChild = destNorm === "/" ? `/${child}` : `${destNorm}/${child}`;
        await this.cp(srcChild, destChild, options);
      }
    }
  }
  async mv(src, dest) {
    await this.cp(src, dest, { recursive: true });
    await this.rm(src, { recursive: true });
  }
  // Get all paths (useful for debugging/glob)
  getAllPaths() {
    return Array.from(this.data.keys());
  }
  // Resolve a path relative to a base
  resolvePath(base, path2) {
    if (path2.startsWith("/")) {
      return this.normalizePath(path2);
    }
    const combined = base === "/" ? `/${path2}` : `${base}/${path2}`;
    return this.normalizePath(combined);
  }
  // Change file/directory permissions
  async chmod(path2, mode) {
    const normalized = this.normalizePath(path2);
    const entry = this.data.get(normalized);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, chmod '${path2}'`);
    }
    entry.mode = mode;
  }
  // Create a symbolic link
  async symlink(target, linkPath) {
    const normalized = this.normalizePath(linkPath);
    if (this.data.has(normalized)) {
      throw new Error(`EEXIST: file already exists, symlink '${linkPath}'`);
    }
    this.ensureParentDirs(normalized);
    this.data.set(normalized, {
      type: "symlink",
      target,
      mode: 511,
      mtime: /* @__PURE__ */ new Date()
    });
  }
  // Create a hard link
  async link(existingPath, newPath) {
    const existingNorm = this.normalizePath(existingPath);
    const newNorm = this.normalizePath(newPath);
    const entry = this.data.get(existingNorm);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, link '${existingPath}'`);
    }
    if (entry.type !== "file") {
      throw new Error(`EPERM: operation not permitted, link '${existingPath}'`);
    }
    if (this.data.has(newNorm)) {
      throw new Error(`EEXIST: file already exists, link '${newPath}'`);
    }
    this.ensureParentDirs(newNorm);
    this.data.set(newNorm, {
      type: "file",
      content: entry.content,
      mode: entry.mode,
      mtime: entry.mtime
    });
  }
  // Read the target of a symbolic link
  async readlink(path2) {
    const normalized = this.normalizePath(path2);
    const entry = this.data.get(normalized);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, readlink '${path2}'`);
    }
    if (entry.type !== "symlink") {
      throw new Error(`EINVAL: invalid argument, readlink '${path2}'`);
    }
    return entry.target;
  }
  /**
   * Resolve all symlinks in a path to get the canonical physical path.
   * This is equivalent to POSIX realpath().
   */
  async realpath(path2) {
    const resolved = this.resolvePathWithSymlinks(path2);
    if (!this.data.has(resolved)) {
      throw new Error(`ENOENT: no such file or directory, realpath '${path2}'`);
    }
    return resolved;
  }
  // ===========================================================================
  // Workflow Serde Support
  // ===========================================================================
  static [WORKFLOW_SERIALIZE$1](instance) {
    return { data: instance.data };
  }
  static [WORKFLOW_DESERIALIZE$1](serialized) {
    const fs3 = new _t();
    fs3.data = serialized.data;
    return fs3;
  }
  /**
   * Set access and modification times of a file
   * @param path - The file path
   * @param _atime - Access time (ignored, kept for API compatibility)
   * @param mtime - Modification time
   */
  async utimes(path2, _atime, mtime) {
    const normalized = this.normalizePath(path2);
    const resolved = this.resolvePathWithSymlinks(normalized);
    const entry = this.data.get(resolved);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, utimes '${path2}'`);
    }
    entry.mtime = mtime;
  }
}, __name$1(_t, "InMemoryFs"), _t);
function isSyncInitFs(fs3) {
  const maybeFs = fs3;
  return typeof maybeFs.mkdirSync === "function" && typeof maybeFs.writeFileSync === "function";
}
__name$1(isSyncInitFs, "isSyncInitFs");
function initCommonDirectories(fs3, useDefaultLayout) {
  fs3.mkdirSync("/bin", { recursive: true });
  fs3.mkdirSync("/usr/bin", { recursive: true });
  if (useDefaultLayout) {
    fs3.mkdirSync("/home/user", { recursive: true });
    fs3.mkdirSync("/tmp", { recursive: true });
  }
}
__name$1(initCommonDirectories, "initCommonDirectories");
function initDevFiles(fs3) {
  fs3.mkdirSync("/dev", { recursive: true });
  fs3.writeFileSync("/dev/null", "");
  fs3.writeFileSync("/dev/zero", new Uint8Array(0));
  fs3.writeFileSync("/dev/stdin", "");
  fs3.writeFileSync("/dev/stdout", "");
  fs3.writeFileSync("/dev/stderr", "");
}
__name$1(initDevFiles, "initDevFiles");
function initProcFiles(fs3) {
  fs3.mkdirSync("/proc/self/fd", { recursive: true });
  fs3.writeFileSync("/proc/version", `${KERNEL_VERSION}
`);
  fs3.writeFileSync("/proc/self/exe", "/bin/bash");
  fs3.writeFileSync("/proc/self/cmdline", "bash\0");
  fs3.writeFileSync("/proc/self/comm", "bash\n");
  fs3.writeFileSync("/proc/self/status", formatProcStatus());
  fs3.writeFileSync("/proc/self/fd/0", "/dev/stdin");
  fs3.writeFileSync("/proc/self/fd/1", "/dev/stdout");
  fs3.writeFileSync("/proc/self/fd/2", "/dev/stderr");
}
__name$1(initProcFiles, "initProcFiles");
function initFilesystem(fs3, useDefaultLayout) {
  if (isSyncInitFs(fs3)) {
    initCommonDirectories(fs3, useDefaultLayout);
    initDevFiles(fs3);
    initProcFiles(fs3);
  }
}
__name$1(initFilesystem, "initFilesystem");
var SHELLOPTS_OPTIONS = [
  "allexport",
  "errexit",
  "noglob",
  "noclobber",
  "noexec",
  "nounset",
  "pipefail",
  "posix",
  "verbose",
  "xtrace"
];
var ALWAYS_ON_OPTIONS = ["braceexpand", "hashall", "interactive-comments"];
function buildShellopts(options) {
  const enabled = [];
  const allOptions = [
    ...ALWAYS_ON_OPTIONS.map((opt) => ({ name: opt, enabled: true })),
    ...SHELLOPTS_OPTIONS.map((opt) => ({ name: opt, enabled: options[opt] }))
  ].sort((a, b) => a.name.localeCompare(b.name));
  for (const opt of allOptions) {
    if (opt.enabled) {
      enabled.push(opt.name);
    }
  }
  return enabled.join(":");
}
__name$1(buildShellopts, "buildShellopts");
function updateShellopts(ctx) {
  ctx.state.env.SHELLOPTS = buildShellopts(ctx.state.options);
}
__name$1(updateShellopts, "updateShellopts");
var BASHOPTS_OPTIONS = [
  "dotglob",
  "expand_aliases",
  "extglob",
  "failglob",
  "globskipdots",
  "globstar",
  "lastpipe",
  "nocaseglob",
  "nocasematch",
  "nullglob",
  "xpg_echo"
];
function buildBashopts(shoptOptions) {
  const enabled = [];
  for (const opt of BASHOPTS_OPTIONS) {
    if (shoptOptions[opt]) {
      enabled.push(opt);
    }
  }
  return enabled.join(":");
}
__name$1(buildBashopts, "buildBashopts");
function updateBashopts(ctx) {
  ctx.state.env.BASHOPTS = buildBashopts(ctx.state.shoptOptions);
}
__name$1(updateBashopts, "updateBashopts");
var ALIAS_PREFIX = "BASH_ALIAS_";
function isLiteralUnquotedWord(word) {
  if (word.parts.length !== 1)
    return false;
  const part = word.parts[0];
  return part.type === "Literal";
}
__name$1(isLiteralUnquotedWord, "isLiteralUnquotedWord");
function getLiteralValue(word) {
  if (word.parts.length !== 1)
    return null;
  const part = word.parts[0];
  if (part.type === "Literal") {
    return part.value;
  }
  return null;
}
__name$1(getLiteralValue, "getLiteralValue");
function getAlias(ctx, name) {
  return ctx.env[`${ALIAS_PREFIX}${name}`];
}
__name$1(getAlias, "getAlias");
function expandAlias(ctx, node, aliasExpansionStack) {
  if (!node.name)
    return node;
  if (!isLiteralUnquotedWord(node.name))
    return node;
  const cmdName = getLiteralValue(node.name);
  if (!cmdName)
    return node;
  const aliasValue = getAlias(ctx, cmdName);
  if (!aliasValue)
    return node;
  if (aliasExpansionStack.has(cmdName))
    return node;
  try {
    aliasExpansionStack.add(cmdName);
    const parser = new Parser();
    let fullCommand = aliasValue;
    const expandNext = aliasValue.endsWith(" ");
    if (!expandNext) {
      for (const arg of node.args) {
        const argLiteral = wordNodeToString(arg);
        fullCommand += ` ${argLiteral}`;
      }
    }
    let expandedAst;
    try {
      expandedAst = parser.parse(fullCommand);
    } catch (e) {
      if (e instanceof ParseException) {
        throw e;
      }
      return node;
    }
    if (expandedAst.statements.length !== 1 || expandedAst.statements[0].pipelines.length !== 1 || expandedAst.statements[0].pipelines[0].commands.length !== 1) {
      return handleComplexAlias(node, aliasValue);
    }
    const expandedCmd = expandedAst.statements[0].pipelines[0].commands[0];
    if (expandedCmd.type !== "SimpleCommand") {
      return handleComplexAlias(node, aliasValue);
    }
    let newNode = {
      ...expandedCmd,
      // Preserve original assignments (prefix assignments like FOO=bar alias_cmd)
      assignments: [...node.assignments, ...expandedCmd.assignments],
      // Preserve original redirections
      redirections: [...expandedCmd.redirections, ...node.redirections],
      // Preserve line number
      line: node.line
    };
    if (expandNext && node.args.length > 0) {
      newNode = {
        ...newNode,
        args: [...newNode.args, ...node.args]
      };
      if (newNode.args.length > 0) {
        const firstArg = newNode.args[0];
        if (isLiteralUnquotedWord(firstArg)) {
          const firstArgName = getLiteralValue(firstArg);
          if (firstArgName && getAlias(ctx, firstArgName)) {
            const tempNode = {
              type: "SimpleCommand",
              name: firstArg,
              args: newNode.args.slice(1),
              assignments: [],
              redirections: []
            };
            const expandedFirst = expandAlias(ctx, tempNode, aliasExpansionStack);
            if (expandedFirst !== tempNode) {
              newNode = {
                ...newNode,
                name: expandedFirst.name,
                args: [...expandedFirst.args]
              };
            }
          }
        }
      }
    }
    return newNode;
  } catch (e) {
    aliasExpansionStack.delete(cmdName);
    throw e;
  }
}
__name$1(expandAlias, "expandAlias");
function handleComplexAlias(node, aliasValue) {
  let fullCommand = aliasValue;
  for (const arg of node.args) {
    const argLiteral = wordNodeToString(arg);
    fullCommand += ` ${argLiteral}`;
  }
  const parser = new Parser();
  const evalWord = parser.parseWordFromString("eval", false, false);
  const cmdWord = parser.parseWordFromString(`'${fullCommand.replace(/'/g, "'\\''")}'`, false, false);
  return {
    type: "SimpleCommand",
    name: evalWord,
    args: [cmdWord],
    assignments: node.assignments,
    redirections: node.redirections,
    line: node.line
  };
}
__name$1(handleComplexAlias, "handleComplexAlias");
function wordNodeToString(word) {
  let result2 = "";
  for (const part of word.parts) {
    switch (part.type) {
      case "Literal":
        result2 += part.value.replace(/([\s"'$`\\*?[\]{}()<>|&;#!])/g, "\\$1");
        break;
      case "SingleQuoted":
        result2 += `'${part.value}'`;
        break;
      case "DoubleQuoted":
        result2 += `"${part.parts.map((p) => p.type === "Literal" ? p.value : `$${p.type}`).join("")}"`;
        break;
      case "ParameterExpansion":
        result2 += `\${${part.parameter}}`;
        break;
      case "CommandSubstitution":
        result2 += `$(...)`;
        break;
      case "ArithmeticExpansion":
        result2 += `$((${part.expression}))`;
        break;
      case "Glob":
        result2 += part.pattern;
        break;
    }
  }
  return result2;
}
__name$1(wordNodeToString, "wordNodeToString");
async function expandLocalArrayAssignment(ctx, word) {
  const fullLiteral = word.parts.map((p) => p.type === "Literal" ? p.value : "\0").join("");
  const arrayMatch = fullLiteral.match(/^([a-zA-Z_][a-zA-Z0-9_]*)=\(/);
  if (!arrayMatch || !fullLiteral.endsWith(")")) {
    return null;
  }
  const name = arrayMatch[1];
  const elements = [];
  let inArrayContent = false;
  let pendingLiteral = "";
  let hasQuotedContent = false;
  for (const part of word.parts) {
    if (part.type === "Literal") {
      let value = part.value;
      if (!inArrayContent) {
        const idx = value.indexOf("=(");
        if (idx !== -1) {
          inArrayContent = true;
          value = value.slice(idx + 2);
        }
      }
      if (inArrayContent) {
        if (value.endsWith(")")) {
          value = value.slice(0, -1);
        }
        const tokens = value.split(/(\s+)/);
        for (const token of tokens) {
          if (/^\s+$/.test(token)) {
            if (pendingLiteral || hasQuotedContent) {
              elements.push(pendingLiteral);
              pendingLiteral = "";
              hasQuotedContent = false;
            }
          } else if (token) {
            pendingLiteral += token;
          }
        }
      }
    } else if (inArrayContent) {
      if (part.type === "BraceExpansion") {
        const isKeyedElement = /^\[.+\]=/.test(pendingLiteral);
        if (isKeyedElement) {
          pendingLiteral += wordToLiteralString({
            parts: [part]
          });
        } else {
          if (pendingLiteral || hasQuotedContent) {
            elements.push(pendingLiteral);
            pendingLiteral = "";
            hasQuotedContent = false;
          }
          const braceExpanded = await expandWordWithGlob(ctx, {
            type: "Word",
            parts: [part]
          });
          elements.push(...braceExpanded.values);
        }
      } else {
        if (part.type === "SingleQuoted" || part.type === "DoubleQuoted" || part.type === "Escaped") {
          hasQuotedContent = true;
        }
        const expanded = await expandWord(ctx, {
          parts: [part]
        });
        pendingLiteral += expanded;
      }
    }
  }
  if (pendingLiteral || hasQuotedContent) {
    elements.push(pendingLiteral);
  }
  const quotedElements = elements.map((elem) => {
    if (/^\[.+\]=/.test(elem)) {
      return elem;
    }
    if (elem === "") {
      return "''";
    }
    if (/[\s"'\\$`!*?[\]{}|&;<>()]/.test(elem) && !elem.startsWith("'") && !elem.startsWith('"')) {
      return `'${elem.replace(/'/g, "'\\''")}'`;
    }
    return elem;
  });
  return `${name}=(${quotedElements.join(" ")})`;
}
__name$1(expandLocalArrayAssignment, "expandLocalArrayAssignment");
async function expandScalarAssignmentArg(ctx, word) {
  let eqPartIndex = -1;
  let eqCharIndex = -1;
  let isAppend = false;
  for (let i = 0; i < word.parts.length; i++) {
    const part = word.parts[i];
    if (part.type === "Literal") {
      const appendIdx = part.value.indexOf("+=");
      if (appendIdx !== -1) {
        const before = part.value.slice(0, appendIdx);
        if (/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(before)) {
          eqPartIndex = i;
          eqCharIndex = appendIdx;
          isAppend = true;
          break;
        }
        if (/^[a-zA-Z_][a-zA-Z0-9_]*\[[^\]]+\]$/.test(before)) {
          eqPartIndex = i;
          eqCharIndex = appendIdx;
          isAppend = true;
          break;
        }
      }
      const eqIdx = part.value.indexOf("=");
      if (eqIdx !== -1 && (eqIdx === 0 || part.value[eqIdx - 1] !== "+")) {
        const before = part.value.slice(0, eqIdx);
        if (/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(before) || /^[a-zA-Z_][a-zA-Z0-9_]*\[[^\]]+\]$/.test(before)) {
          eqPartIndex = i;
          eqCharIndex = eqIdx;
          break;
        }
      }
    }
  }
  if (eqPartIndex === -1) {
    return null;
  }
  const nameParts = word.parts.slice(0, eqPartIndex);
  const eqPart = word.parts[eqPartIndex];
  if (eqPart.type !== "Literal") {
    return null;
  }
  const operatorLen = isAppend ? 2 : 1;
  const nameFromEqPart = eqPart.value.slice(0, eqCharIndex);
  const valueFromEqPart = eqPart.value.slice(eqCharIndex + operatorLen);
  const valueParts = word.parts.slice(eqPartIndex + 1);
  let name = "";
  for (const part of nameParts) {
    name += await expandWord(ctx, { parts: [part] });
  }
  name += nameFromEqPart;
  const valueWord = {
    parts: valueFromEqPart !== "" ? [{ type: "Literal", value: valueFromEqPart }, ...valueParts] : valueParts
  };
  const value = valueWord.parts.length > 0 ? await expandWord(ctx, valueWord) : "";
  const operator = isAppend ? "+=" : "=";
  return `${name}${operator}${value}`;
}
__name$1(expandScalarAssignmentArg, "expandScalarAssignmentArg");
var BROWSER_EXCLUDED_COMMANDS = [
  "tar",
  // Uses native compression modules (@mongodb-js/zstd, node-liblzma, compressjs)
  "yq",
  // Requires fast-xml-parser and other Node.js-specific parsing
  "xan",
  // Complex CSV/data processing with Node.js dependencies
  "sqlite3",
  // Uses sql.js (WASM) which requires Node.js worker threads
  "python3",
  // Uses Pyodide (WASM) which requires Node.js worker threads
  "python"
  // Alias for python3
];
function isBrowserExcludedCommand(commandName) {
  return BROWSER_EXCLUDED_COMMANDS.includes(commandName);
}
__name$1(isBrowserExcludedCommand, "isBrowserExcludedCommand");
var OK = Object.freeze({
  stdout: "",
  stderr: "",
  exitCode: 0
});
function success(stdout = "") {
  return { stdout, stderr: "", exitCode: 0 };
}
__name$1(success, "success");
function failure(stderr, exitCode = 1) {
  return { stdout: "", stderr, exitCode };
}
__name$1(failure, "failure");
function result(stdout, stderr, exitCode) {
  return { stdout, stderr, exitCode };
}
__name$1(result, "result");
function testResult(passed) {
  return { stdout: "", stderr: "", exitCode: passed ? 0 : 1 };
}
__name$1(testResult, "testResult");
function throwExecutionLimit(message, limitType, stdout = "", stderr = "") {
  throw new ExecutionLimitError(message, limitType, stdout, stderr);
}
__name$1(throwExecutionLimit, "throwExecutionLimit");
function handleBreak(ctx, args) {
  if (ctx.state.loopDepth === 0) {
    if (ctx.state.parentHasLoopContext) {
      throw new SubshellExitError();
    }
    return OK;
  }
  if (args.length > 1) {
    throw new ExitError(1, "", "bash: break: too many arguments\n");
  }
  let levels = 1;
  if (args.length > 0) {
    const n = Number.parseInt(args[0], 10);
    if (Number.isNaN(n) || n < 1) {
      throw new ExitError(128, "", `bash: break: ${args[0]}: numeric argument required
`);
    }
    levels = n;
  }
  throw new BreakError(levels);
}
__name$1(handleBreak, "handleBreak");
async function handleCd(ctx, args) {
  let target;
  let printPath = false;
  let physical = false;
  let i = 0;
  while (i < args.length) {
    if (args[i] === "--") {
      i++;
      break;
    } else if (args[i] === "-L") {
      physical = false;
      i++;
    } else if (args[i] === "-P") {
      physical = true;
      i++;
    } else if (args[i].startsWith("-") && args[i] !== "-") {
      i++;
    } else {
      break;
    }
  }
  const remainingArgs = args.slice(i);
  if (remainingArgs.length === 0) {
    target = ctx.state.env.HOME || "/";
  } else if (remainingArgs[0] === "~") {
    target = ctx.state.env.HOME || "/";
  } else if (remainingArgs[0] === "-") {
    target = ctx.state.previousDir;
    printPath = true;
  } else {
    target = remainingArgs[0];
  }
  if (!target.startsWith("/") && !target.startsWith("./") && !target.startsWith("../") && target !== "." && target !== "..") {
    const cdpath = ctx.state.env.CDPATH;
    if (cdpath) {
      const cdpathDirs = cdpath.split(":").filter((d) => d);
      for (const dir of cdpathDirs) {
        const candidate = dir.startsWith("/") ? `${dir}/${target}` : `${ctx.state.cwd}/${dir}/${target}`;
        try {
          const stat = await ctx.fs.stat(candidate);
          if (stat.isDirectory) {
            target = candidate;
            printPath = true;
            break;
          }
        } catch {
        }
      }
    }
  }
  const pathToCheck = target.startsWith("/") ? target : `${ctx.state.cwd}/${target}`;
  const parts = pathToCheck.split("/").filter((p) => p && p !== ".");
  let currentPath = "";
  for (const part of parts) {
    if (part === "..") {
      currentPath = currentPath.split("/").slice(0, -1).join("/") || "/";
    } else {
      currentPath = currentPath ? `${currentPath}/${part}` : `/${part}`;
      try {
        const stat = await ctx.fs.stat(currentPath);
        if (!stat.isDirectory) {
          return failure(`bash: cd: ${target}: Not a directory
`);
        }
      } catch {
        return failure(`bash: cd: ${target}: No such file or directory
`);
      }
    }
  }
  let newDir = currentPath || "/";
  if (physical) {
    try {
      newDir = await ctx.fs.realpath(newDir);
    } catch {
    }
  }
  ctx.state.previousDir = ctx.state.cwd;
  ctx.state.cwd = newDir;
  ctx.state.env.PWD = ctx.state.cwd;
  ctx.state.env.OLDPWD = ctx.state.previousDir;
  return success(printPath ? `${newDir}
` : "");
}
__name$1(handleCd, "handleCd");
function resolvePath(ctx, path2) {
  return ctx.fs.resolvePath(ctx.state.cwd, path2);
}
__name$1(resolvePath, "resolvePath");
var FILE_TEST_OPERATORS = [
  "-e",
  // file exists
  "-a",
  // file exists (deprecated synonym for -e)
  "-f",
  // regular file
  "-d",
  // directory
  "-r",
  // readable
  "-w",
  // writable
  "-x",
  // executable
  "-s",
  // file exists and has size > 0
  "-L",
  // symbolic link
  "-h",
  // symbolic link (synonym for -L)
  "-k",
  // sticky bit set
  "-g",
  // setgid bit set
  "-u",
  // setuid bit set
  "-G",
  // owned by effective group ID
  "-O",
  // owned by effective user ID
  "-b",
  // block special file
  "-c",
  // character special file
  "-p",
  // named pipe (FIFO)
  "-S",
  // socket
  "-t",
  // file descriptor is open and refers to a terminal
  "-N"
  // file has been modified since last read
];
function isFileTestOperator(op) {
  return FILE_TEST_OPERATORS.includes(op);
}
__name$1(isFileTestOperator, "isFileTestOperator");
async function evaluateFileTest(ctx, operator, operand) {
  const path2 = resolvePath(ctx, operand);
  switch (operator) {
    case "-e":
    case "-a":
      return ctx.fs.exists(path2);
    case "-f": {
      if (await ctx.fs.exists(path2)) {
        const stat = await ctx.fs.stat(path2);
        return stat.isFile;
      }
      return false;
    }
    case "-d": {
      if (await ctx.fs.exists(path2)) {
        const stat = await ctx.fs.stat(path2);
        return stat.isDirectory;
      }
      return false;
    }
    case "-r": {
      if (await ctx.fs.exists(path2)) {
        const stat = await ctx.fs.stat(path2);
        return (stat.mode & 256) !== 0;
      }
      return false;
    }
    case "-w": {
      if (await ctx.fs.exists(path2)) {
        const stat = await ctx.fs.stat(path2);
        return (stat.mode & 128) !== 0;
      }
      return false;
    }
    case "-x": {
      if (await ctx.fs.exists(path2)) {
        const stat = await ctx.fs.stat(path2);
        return (stat.mode & 64) !== 0;
      }
      return false;
    }
    case "-s": {
      if (await ctx.fs.exists(path2)) {
        const stat = await ctx.fs.stat(path2);
        return stat.size > 0;
      }
      return false;
    }
    case "-L":
    case "-h": {
      try {
        const stat = await ctx.fs.lstat(path2);
        return stat.isSymbolicLink;
      } catch {
        return false;
      }
    }
    case "-k": {
      if (await ctx.fs.exists(path2)) {
        const stat = await ctx.fs.stat(path2);
        return (stat.mode & 512) !== 0;
      }
      return false;
    }
    case "-g": {
      if (await ctx.fs.exists(path2)) {
        const stat = await ctx.fs.stat(path2);
        return (stat.mode & 1024) !== 0;
      }
      return false;
    }
    case "-u": {
      if (await ctx.fs.exists(path2)) {
        const stat = await ctx.fs.stat(path2);
        return (stat.mode & 2048) !== 0;
      }
      return false;
    }
    case "-G":
    case "-O":
      return ctx.fs.exists(path2);
    case "-b":
      return false;
    case "-c": {
      const charDevices = [
        "/dev/null",
        "/dev/zero",
        "/dev/random",
        "/dev/urandom",
        "/dev/tty",
        "/dev/stdin",
        "/dev/stdout",
        "/dev/stderr"
      ];
      return charDevices.some((dev) => path2 === dev || path2.endsWith(dev));
    }
    case "-p":
      return false;
    case "-S":
      return false;
    case "-t":
      return false;
    case "-N": {
      return ctx.fs.exists(path2);
    }
    default:
      return false;
  }
}
__name$1(evaluateFileTest, "evaluateFileTest");
var BINARY_FILE_TEST_OPERATORS = ["-nt", "-ot", "-ef"];
function isBinaryFileTestOperator(op) {
  return BINARY_FILE_TEST_OPERATORS.includes(op);
}
__name$1(isBinaryFileTestOperator, "isBinaryFileTestOperator");
async function evaluateBinaryFileTest(ctx, operator, left, right) {
  const leftPath = resolvePath(ctx, left);
  const rightPath = resolvePath(ctx, right);
  switch (operator) {
    case "-nt": {
      try {
        const leftStat = await ctx.fs.stat(leftPath);
        const rightStat = await ctx.fs.stat(rightPath);
        return leftStat.mtime > rightStat.mtime;
      } catch {
        return false;
      }
    }
    case "-ot": {
      try {
        const leftStat = await ctx.fs.stat(leftPath);
        const rightStat = await ctx.fs.stat(rightPath);
        return leftStat.mtime < rightStat.mtime;
      } catch {
        return false;
      }
    }
    case "-ef": {
      try {
        if (!await ctx.fs.exists(leftPath) || !await ctx.fs.exists(rightPath)) {
          return false;
        }
        const leftReal = ctx.fs.resolvePath(ctx.state.cwd, leftPath);
        const rightReal = ctx.fs.resolvePath(ctx.state.cwd, rightPath);
        return leftReal === rightReal;
      } catch {
        return false;
      }
    }
    default:
      return false;
  }
}
__name$1(evaluateBinaryFileTest, "evaluateBinaryFileTest");
var NUMERIC_OPS = /* @__PURE__ */ new Set(["-eq", "-ne", "-lt", "-le", "-gt", "-ge"]);
function isNumericOp(op) {
  return NUMERIC_OPS.has(op);
}
__name$1(isNumericOp, "isNumericOp");
function compareNumeric(op, left, right) {
  switch (op) {
    case "-eq":
      return left === right;
    case "-ne":
      return left !== right;
    case "-lt":
      return left < right;
    case "-le":
      return left <= right;
    case "-gt":
      return left > right;
    case "-ge":
      return left >= right;
  }
}
__name$1(compareNumeric, "compareNumeric");
function isStringCompareOp(op) {
  return op === "=" || op === "==" || op === "!=";
}
__name$1(isStringCompareOp, "isStringCompareOp");
function compareStrings(op, left, right, usePattern = false, nocasematch = false, extglob = false) {
  if (usePattern) {
    const isEqual2 = matchPattern(left, right, nocasematch, extglob);
    return op === "!=" ? !isEqual2 : isEqual2;
  }
  if (nocasematch) {
    const isEqual2 = left.toLowerCase() === right.toLowerCase();
    return op === "!=" ? !isEqual2 : isEqual2;
  }
  const isEqual = left === right;
  return op === "!=" ? !isEqual : isEqual;
}
__name$1(compareStrings, "compareStrings");
var STRING_TEST_OPS = /* @__PURE__ */ new Set(["-z", "-n"]);
function isStringTestOp(op) {
  return STRING_TEST_OPS.has(op);
}
__name$1(isStringTestOp, "isStringTestOp");
function evaluateStringTest(op, value) {
  switch (op) {
    case "-z":
      return value === "";
    case "-n":
      return value !== "";
  }
}
__name$1(evaluateStringTest, "evaluateStringTest");
async function evaluateVariableTest(ctx, operand) {
  const arrayMatch = operand.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
  if (arrayMatch) {
    const arrayName = arrayMatch[1];
    const indexExpr = arrayMatch[2];
    const isAssoc = ctx.state.associativeArrays?.has(arrayName);
    if (isAssoc) {
      let key2 = indexExpr;
      if (key2.startsWith("'") && key2.endsWith("'") || key2.startsWith('"') && key2.endsWith('"')) {
        key2 = key2.slice(1, -1);
      }
      key2 = key2.replace(/\$([a-zA-Z_][a-zA-Z0-9_]*)/g, (_, varName) => {
        return ctx.state.env[varName] || "";
      });
      return `${arrayName}_${key2}` in ctx.state.env;
    }
    let index;
    try {
      const parser = new Parser();
      const arithAst = parseArithmeticExpression(parser, indexExpr);
      index = await evaluateArithmetic(ctx, arithAst.expression);
    } catch {
      if (/^-?\d+$/.test(indexExpr)) {
        index = Number.parseInt(indexExpr, 10);
      } else {
        const varValue = ctx.state.env[indexExpr];
        index = varValue ? Number.parseInt(varValue, 10) : 0;
      }
    }
    if (index < 0) {
      const indices = getArrayIndices(ctx, arrayName);
      const lineNum = ctx.state.currentLine;
      if (indices.length === 0) {
        ctx.state.expansionStderr = (ctx.state.expansionStderr || "") + `bash: line ${lineNum}: ${arrayName}: bad array subscript
`;
        return false;
      }
      const maxIndex = Math.max(...indices);
      index = maxIndex + 1 + index;
      if (index < 0) {
        ctx.state.expansionStderr = (ctx.state.expansionStderr || "") + `bash: line ${lineNum}: ${arrayName}: bad array subscript
`;
        return false;
      }
    }
    return `${arrayName}_${index}` in ctx.state.env;
  }
  if (operand in ctx.state.env) {
    return true;
  }
  if (ctx.state.associativeArrays?.has(operand)) {
    return getAssocArrayKeys(ctx, operand).length > 0;
  }
  return getArrayIndices(ctx, operand).length > 0;
}
__name$1(evaluateVariableTest, "evaluateVariableTest");
async function evaluateConditional(ctx, expr) {
  switch (expr.type) {
    case "CondBinary": {
      const left = await expandWord(ctx, expr.left);
      const isRhsQuoted = expr.right.parts.length > 0 && expr.right.parts.every((p) => p.type === "SingleQuoted" || p.type === "DoubleQuoted" || // Escaped counts as quoted for pattern matching, but NOT for regex
      p.type === "Escaped" && expr.operator !== "=~");
      let right;
      if (expr.operator === "=~") {
        if (isRhsQuoted) {
          const expanded = await expandWord(ctx, expr.right);
          right = escapeRegexChars(expanded);
        } else {
          right = await expandWordForRegex(ctx, expr.right);
        }
      } else if (isStringCompareOp(expr.operator) && !isRhsQuoted) {
        right = await expandWordForPattern(ctx, expr.right);
      } else {
        right = await expandWord(ctx, expr.right);
      }
      if (isStringCompareOp(expr.operator)) {
        const nocasematch = ctx.state.shoptOptions.nocasematch;
        return compareStrings(expr.operator, left, right, !isRhsQuoted, nocasematch, true);
      }
      if (isNumericOp(expr.operator)) {
        return compareNumeric(expr.operator, await evalArithExpr(ctx, left), await evalArithExpr(ctx, right));
      }
      if (isBinaryFileTestOperator(expr.operator)) {
        return evaluateBinaryFileTest(ctx, expr.operator, left, right);
      }
      switch (expr.operator) {
        case "=~": {
          try {
            const nocasematch = ctx.state.shoptOptions.nocasematch;
            const jsPattern = posixEreToJsRegex(right);
            const regex = new RegExp(jsPattern, nocasematch ? "i" : "");
            const match = left.match(regex);
            clearArray(ctx, "BASH_REMATCH");
            if (match) {
              for (let i = 0; i < match.length; i++) {
                ctx.state.env[`BASH_REMATCH_${i}`] = match[i] || "";
              }
            }
            return match !== null;
          } catch {
            throw new Error("syntax error in regular expression");
          }
        }
        case "<":
          return left < right;
        case ">":
          return left > right;
        default:
          return false;
      }
    }
    case "CondUnary": {
      const operand = await expandWord(ctx, expr.operand);
      if (isFileTestOperator(expr.operator)) {
        return evaluateFileTest(ctx, expr.operator, operand);
      }
      if (isStringTestOp(expr.operator)) {
        return evaluateStringTest(expr.operator, operand);
      }
      if (expr.operator === "-v") {
        return await evaluateVariableTest(ctx, operand);
      }
      if (expr.operator === "-o") {
        return evaluateShellOption(ctx, operand);
      }
      return false;
    }
    case "CondNot": {
      if (ctx.state.shoptOptions.extglob) {
        if (expr.operand.type === "CondGroup" && expr.operand.expression.type === "CondWord") {
          const innerValue = await expandWord(ctx, expr.operand.expression.word);
          const extglobPattern = `!(${innerValue})`;
          return extglobPattern !== "";
        }
      }
      return !await evaluateConditional(ctx, expr.operand);
    }
    case "CondAnd": {
      const left = await evaluateConditional(ctx, expr.left);
      if (!left)
        return false;
      return await evaluateConditional(ctx, expr.right);
    }
    case "CondOr": {
      const left = await evaluateConditional(ctx, expr.left);
      if (left)
        return true;
      return await evaluateConditional(ctx, expr.right);
    }
    case "CondGroup":
      return await evaluateConditional(ctx, expr.expression);
    case "CondWord": {
      const value = await expandWord(ctx, expr.word);
      return value !== "";
    }
    default:
      return false;
  }
}
__name$1(evaluateConditional, "evaluateConditional");
async function evaluateTestArgs(ctx, args) {
  if (args.length === 0) {
    return result("", "", 1);
  }
  if (args.length === 1) {
    return testResult(Boolean(args[0]));
  }
  if (args.length === 2) {
    const op = args[0];
    const operand = args[1];
    if (op === "(") {
      return failure("test: '(' without matching ')'\n", 2);
    }
    if (isFileTestOperator(op)) {
      return testResult(await evaluateFileTest(ctx, op, operand));
    }
    if (isStringTestOp(op)) {
      return testResult(evaluateStringTest(op, operand));
    }
    if (op === "!") {
      return testResult(!operand);
    }
    if (op === "-v") {
      return testResult(await evaluateVariableTest(ctx, operand));
    }
    if (op === "-o") {
      return testResult(evaluateShellOption(ctx, operand));
    }
    if (op === "=" || op === "==" || op === "!=" || op === "<" || op === ">" || op === "-eq" || op === "-ne" || op === "-lt" || op === "-le" || op === "-gt" || op === "-ge" || op === "-nt" || op === "-ot" || op === "-ef") {
      return failure(`test: ${op}: unary operator expected
`, 2);
    }
    return result("", "", 1);
  }
  if (args.length === 3) {
    const left = args[0];
    const op = args[1];
    const right = args[2];
    if (isStringCompareOp(op)) {
      return testResult(compareStrings(op, left, right));
    }
    if (isNumericOp(op)) {
      const leftNum = parseNumericDecimal(left);
      const rightNum = parseNumericDecimal(right);
      if (!leftNum.valid || !rightNum.valid) {
        return result("", "", 2);
      }
      return testResult(compareNumeric(op, leftNum.value, rightNum.value));
    }
    if (isBinaryFileTestOperator(op)) {
      return testResult(await evaluateBinaryFileTest(ctx, op, left, right));
    }
    switch (op) {
      case "-a":
        return testResult(left !== "" && right !== "");
      case "-o":
        return testResult(left !== "" || right !== "");
      case ">":
        return testResult(left > right);
      case "<":
        return testResult(left < right);
    }
    if (left === "!") {
      const negResult = await evaluateTestArgs(ctx, [op, right]);
      return result("", negResult.stderr, negResult.exitCode === 0 ? 1 : negResult.exitCode === 1 ? 0 : negResult.exitCode);
    }
    if (left === "(" && right === ")") {
      return testResult(op !== "");
    }
  }
  if (args.length === 4) {
    if (args[0] === "!") {
      const negResult = await evaluateTestArgs(ctx, args.slice(1));
      return result("", negResult.stderr, negResult.exitCode === 0 ? 1 : negResult.exitCode === 1 ? 0 : negResult.exitCode);
    }
    if (args[0] === "(" && args[3] === ")") {
      return evaluateTestArgs(ctx, [args[1], args[2]]);
    }
  }
  const exprResult = await evaluateTestExpr(ctx, args, 0);
  if (exprResult.pos < args.length) {
    return failure("test: too many arguments\n", 2);
  }
  return testResult(exprResult.value);
}
__name$1(evaluateTestArgs, "evaluateTestArgs");
async function evaluateTestExpr(ctx, args, pos) {
  return evaluateTestOr(ctx, args, pos);
}
__name$1(evaluateTestExpr, "evaluateTestExpr");
async function evaluateTestOr(ctx, args, pos) {
  let { value, pos: newPos } = await evaluateTestAnd(ctx, args, pos);
  while (args[newPos] === "-o") {
    const right = await evaluateTestAnd(ctx, args, newPos + 1);
    value = value || right.value;
    newPos = right.pos;
  }
  return { value, pos: newPos };
}
__name$1(evaluateTestOr, "evaluateTestOr");
async function evaluateTestAnd(ctx, args, pos) {
  let { value, pos: newPos } = await evaluateTestNot(ctx, args, pos);
  while (args[newPos] === "-a") {
    const right = await evaluateTestNot(ctx, args, newPos + 1);
    value = value && right.value;
    newPos = right.pos;
  }
  return { value, pos: newPos };
}
__name$1(evaluateTestAnd, "evaluateTestAnd");
async function evaluateTestNot(ctx, args, pos) {
  if (args[pos] === "!") {
    const { value, pos: newPos } = await evaluateTestNot(ctx, args, pos + 1);
    return { value: !value, pos: newPos };
  }
  return evaluateTestPrimary(ctx, args, pos);
}
__name$1(evaluateTestNot, "evaluateTestNot");
async function evaluateTestPrimary(ctx, args, pos) {
  const token = args[pos];
  if (token === "(") {
    const { value, pos: newPos } = await evaluateTestExpr(ctx, args, pos + 1);
    return { value, pos: args[newPos] === ")" ? newPos + 1 : newPos };
  }
  const next = args[pos + 1];
  if (isStringCompareOp(next)) {
    const left = token;
    const right = args[pos + 2] ?? "";
    return { value: compareStrings(next, left, right), pos: pos + 3 };
  }
  if (isNumericOp(next)) {
    const leftParsed = parseNumericDecimal(token);
    const rightParsed = parseNumericDecimal(args[pos + 2] ?? "0");
    if (!leftParsed.valid || !rightParsed.valid) {
      return { value: false, pos: pos + 3 };
    }
    const value = compareNumeric(next, leftParsed.value, rightParsed.value);
    return { value, pos: pos + 3 };
  }
  if (isBinaryFileTestOperator(next)) {
    const left = token;
    const right = args[pos + 2] ?? "";
    const value = await evaluateBinaryFileTest(ctx, next, left, right);
    return { value, pos: pos + 3 };
  }
  if (isFileTestOperator(token)) {
    const operand = args[pos + 1] ?? "";
    const value = await evaluateFileTest(ctx, token, operand);
    return { value, pos: pos + 2 };
  }
  if (isStringTestOp(token)) {
    const operand = args[pos + 1] ?? "";
    return { value: evaluateStringTest(token, operand), pos: pos + 2 };
  }
  if (token === "-v") {
    const varName = args[pos + 1] ?? "";
    const value = await evaluateVariableTest(ctx, varName);
    return { value, pos: pos + 2 };
  }
  if (token === "-o") {
    const optName = args[pos + 1] ?? "";
    const value = evaluateShellOption(ctx, optName);
    return { value, pos: pos + 2 };
  }
  return { value: token !== void 0 && token !== "", pos: pos + 1 };
}
__name$1(evaluateTestPrimary, "evaluateTestPrimary");
function matchPattern(value, pattern, nocasematch = false, extglob = false) {
  const regex = `^${patternToRegexStr(pattern, extglob)}$`;
  const flags = nocasematch ? "is" : "s";
  return new RegExp(regex, flags).test(value);
}
__name$1(matchPattern, "matchPattern");
function patternToRegexStr(pattern, extglob) {
  let regex = "";
  for (let i = 0; i < pattern.length; i++) {
    const char = pattern[i];
    if (extglob && (char === "@" || char === "*" || char === "+" || char === "?" || char === "!") && i + 1 < pattern.length && pattern[i + 1] === "(") {
      const closeIdx = findMatchingParen(pattern, i + 1);
      if (closeIdx !== -1) {
        const content = pattern.slice(i + 2, closeIdx);
        const alternatives = splitExtglobAlternatives(content);
        const altRegexes = alternatives.map((alt) => patternToRegexStr(alt, extglob));
        const altGroup = altRegexes.length > 0 ? altRegexes.join("|") : "(?:)";
        if (char === "@") {
          regex += `(?:${altGroup})`;
        } else if (char === "*") {
          regex += `(?:${altGroup})*`;
        } else if (char === "+") {
          regex += `(?:${altGroup})+`;
        } else if (char === "?") {
          regex += `(?:${altGroup})?`;
        } else if (char === "!") {
          const hasMorePattern = closeIdx < pattern.length - 1;
          if (hasMorePattern) {
            const lengths = alternatives.map((alt) => computePatternLength(alt, extglob));
            const allSameLength = lengths.every((l) => l !== null) && lengths.every((l) => l === lengths[0]);
            if (allSameLength && lengths[0] !== null) {
              const n = lengths[0];
              if (n === 0) {
                regex += "(?:.+)";
              } else {
                const parts = [];
                if (n > 0) {
                  parts.push(`.{0,${n - 1}}`);
                }
                parts.push(`.{${n + 1},}`);
                parts.push(`(?!(?:${altGroup})).{${n}}`);
                regex += `(?:${parts.join("|")})`;
              }
            } else {
              regex += `(?:(?!(?:${altGroup})).)*?`;
            }
          } else {
            regex += `(?!(?:${altGroup})$).*`;
          }
        }
        i = closeIdx;
        continue;
      }
    }
    if (char === "\\") {
      if (i + 1 < pattern.length) {
        const next = pattern[i + 1];
        if (/[\\^$.|+(){}[\]*?]/.test(next)) {
          regex += `\\${next}`;
        } else {
          regex += next;
        }
        i++;
      } else {
        regex += "\\\\";
      }
    } else if (char === "*") {
      regex += ".*";
    } else if (char === "?") {
      regex += ".";
    } else if (char === "[") {
      const closeIdx = pattern.indexOf("]", i + 1);
      if (closeIdx !== -1) {
        regex += pattern.slice(i, closeIdx + 1);
        i = closeIdx;
      } else {
        regex += "\\[";
      }
    } else if (/[\\^$.|+(){}]/.test(char)) {
      regex += `\\${char}`;
    } else {
      regex += char;
    }
  }
  return regex;
}
__name$1(patternToRegexStr, "patternToRegexStr");
function findMatchingParen(pattern, openIdx) {
  let depth = 1;
  let i = openIdx + 1;
  while (i < pattern.length && depth > 0) {
    const c = pattern[i];
    if (c === "\\") {
      i += 2;
      continue;
    }
    if (c === "(") {
      depth++;
    } else if (c === ")") {
      depth--;
      if (depth === 0) {
        return i;
      }
    }
    i++;
  }
  return -1;
}
__name$1(findMatchingParen, "findMatchingParen");
function splitExtglobAlternatives(content) {
  const alternatives = [];
  let current = "";
  let depth = 0;
  let i = 0;
  while (i < content.length) {
    const c = content[i];
    if (c === "\\") {
      current += c;
      if (i + 1 < content.length) {
        current += content[i + 1];
        i += 2;
      } else {
        i++;
      }
      continue;
    }
    if (c === "(") {
      depth++;
      current += c;
    } else if (c === ")") {
      depth--;
      current += c;
    } else if (c === "|" && depth === 0) {
      alternatives.push(current);
      current = "";
    } else {
      current += c;
    }
    i++;
  }
  alternatives.push(current);
  return alternatives;
}
__name$1(splitExtglobAlternatives, "splitExtglobAlternatives");
function computePatternLength(pattern, extglob) {
  let length = 0;
  let i = 0;
  while (i < pattern.length) {
    const c = pattern[i];
    if (extglob && (c === "@" || c === "*" || c === "+" || c === "?" || c === "!") && i + 1 < pattern.length && pattern[i + 1] === "(") {
      const closeIdx = findMatchingParen(pattern, i + 1);
      if (closeIdx !== -1) {
        if (c === "@") {
          const content = pattern.slice(i + 2, closeIdx);
          const alts = splitExtglobAlternatives(content);
          const altLengths = alts.map((a) => computePatternLength(a, extglob));
          if (altLengths.every((l) => l !== null) && altLengths.every((l) => l === altLengths[0])) {
            length += altLengths[0];
            i = closeIdx + 1;
            continue;
          }
          return null;
        }
        return null;
      }
    }
    if (c === "*") {
      return null;
    }
    if (c === "?") {
      length += 1;
      i++;
      continue;
    }
    if (c === "[") {
      const closeIdx = pattern.indexOf("]", i + 1);
      if (closeIdx !== -1) {
        length += 1;
        i = closeIdx + 1;
        continue;
      }
      length += 1;
      i++;
      continue;
    }
    if (c === "\\") {
      length += 1;
      i += 2;
      continue;
    }
    length += 1;
    i++;
  }
  return length;
}
__name$1(computePatternLength, "computePatternLength");
function evaluateShellOption(ctx, option) {
  const optionMap = {
    // Implemented options (set -o)
    errexit: /* @__PURE__ */ __name$1(() => ctx.state.options.errexit === true, "errexit"),
    nounset: /* @__PURE__ */ __name$1(() => ctx.state.options.nounset === true, "nounset"),
    pipefail: /* @__PURE__ */ __name$1(() => ctx.state.options.pipefail === true, "pipefail"),
    xtrace: /* @__PURE__ */ __name$1(() => ctx.state.options.xtrace === true, "xtrace"),
    // Single-letter aliases for implemented options
    e: /* @__PURE__ */ __name$1(() => ctx.state.options.errexit === true, "e"),
    u: /* @__PURE__ */ __name$1(() => ctx.state.options.nounset === true, "u"),
    x: /* @__PURE__ */ __name$1(() => ctx.state.options.xtrace === true, "x")
  };
  const getter = optionMap[option];
  if (getter) {
    return getter();
  }
  return false;
}
__name$1(evaluateShellOption, "evaluateShellOption");
async function evalArithExpr(ctx, expr) {
  expr = expr.trim();
  if (expr === "")
    return 0;
  if (/^[+-]?(\d+#[a-zA-Z0-9@_]+|0[xX][0-9a-fA-F]+|0[0-7]+|\d+)$/.test(expr)) {
    return parseNumeric(expr);
  }
  try {
    const parser = new Parser();
    const arithAst = parseArithmeticExpression(parser, expr);
    return await evaluateArithmetic(ctx, arithAst.expression);
  } catch {
    return parseNumeric(expr);
  }
}
__name$1(evalArithExpr, "evalArithExpr");
function parseBaseN(digits, base) {
  let result2 = 0;
  for (const char of digits) {
    let digitValue;
    if (char >= "0" && char <= "9") {
      digitValue = char.charCodeAt(0) - 48;
    } else if (char >= "a" && char <= "z") {
      digitValue = char.charCodeAt(0) - 97 + 10;
    } else if (char >= "A" && char <= "Z") {
      digitValue = char.charCodeAt(0) - 65 + 36;
    } else if (char === "@") {
      digitValue = 62;
    } else if (char === "_") {
      digitValue = 63;
    } else {
      return Number.NaN;
    }
    if (digitValue >= base) {
      return Number.NaN;
    }
    result2 = result2 * base + digitValue;
  }
  return result2;
}
__name$1(parseBaseN, "parseBaseN");
function parseNumeric(value) {
  value = value.trim();
  if (value === "")
    return 0;
  let negative = false;
  if (value.startsWith("-")) {
    negative = true;
    value = value.slice(1);
  } else if (value.startsWith("+")) {
    value = value.slice(1);
  }
  let result2;
  const baseMatch = value.match(/^(\d+)#([a-zA-Z0-9@_]+)$/);
  if (baseMatch) {
    const base = Number.parseInt(baseMatch[1], 10);
    if (base >= 2 && base <= 64) {
      result2 = parseBaseN(baseMatch[2], base);
    } else {
      result2 = 0;
    }
  } else if (/^0[xX][0-9a-fA-F]+$/.test(value)) {
    result2 = Number.parseInt(value, 16);
  } else if (/^0[0-7]+$/.test(value)) {
    result2 = Number.parseInt(value, 8);
  } else {
    result2 = Number.parseInt(value, 10);
  }
  if (Number.isNaN(result2)) {
    result2 = 0;
  }
  return negative ? -result2 : result2;
}
__name$1(parseNumeric, "parseNumeric");
function parseNumericDecimal(value) {
  value = value.trim();
  if (value === "")
    return { value: 0, valid: true };
  let negative = false;
  if (value.startsWith("-")) {
    negative = true;
    value = value.slice(1);
  } else if (value.startsWith("+")) {
    value = value.slice(1);
  }
  if (!/^\d+$/.test(value)) {
    return { value: 0, valid: false };
  }
  const result2 = Number.parseInt(value, 10);
  if (Number.isNaN(result2)) {
    return { value: 0, valid: false };
  }
  return { value: negative ? -result2 : result2, valid: true };
}
__name$1(parseNumericDecimal, "parseNumericDecimal");
function posixEreToJsRegex(pattern) {
  let result2 = "";
  let i = 0;
  while (i < pattern.length) {
    if (pattern[i] === "\\" && i + 1 < pattern.length) {
      result2 += pattern[i] + pattern[i + 1];
      i += 2;
    } else if (pattern[i] === "[") {
      const classResult = convertPosixCharClass(pattern, i);
      result2 += classResult.converted;
      i = classResult.endIndex;
    } else {
      result2 += pattern[i];
      i++;
    }
  }
  return result2;
}
__name$1(posixEreToJsRegex, "posixEreToJsRegex");
function convertPosixCharClass(pattern, startIndex) {
  let i = startIndex + 1;
  let result2 = "[";
  if (i < pattern.length && (pattern[i] === "^" || pattern[i] === "!")) {
    result2 += "^";
    i++;
  }
  let hasLiteralCloseBracket = false;
  if (i < pattern.length && pattern[i] === "]") {
    hasLiteralCloseBracket = true;
    i++;
  }
  let hasLiteralOpenBracket = false;
  if (i < pattern.length && pattern[i] === "[" && i + 1 < pattern.length && pattern[i + 1] !== ":") {
    hasLiteralOpenBracket = true;
    i++;
  }
  let classContent = "";
  let foundClose = false;
  while (i < pattern.length) {
    const ch = pattern[i];
    if (ch === "]") {
      foundClose = true;
      i++;
      break;
    }
    if (ch === "[" && i + 1 < pattern.length && pattern[i + 1] === ":") {
      const endPos = pattern.indexOf(":]", i + 2);
      if (endPos !== -1) {
        const className = pattern.slice(i + 2, endPos);
        classContent += posixClassToJsClass(className);
        i = endPos + 2;
        continue;
      }
    }
    if (ch === "[" && i + 1 < pattern.length) {
      const next = pattern[i + 1];
      if (next === "." || next === "=") {
        const endMarker = `${next}]`;
        const endPos = pattern.indexOf(endMarker, i + 2);
        if (endPos !== -1) {
          const content = pattern.slice(i + 2, endPos);
          classContent += content;
          i = endPos + 2;
          continue;
        }
      }
    }
    if (ch === "\\" && i + 1 < pattern.length) {
      classContent += ch + pattern[i + 1];
      i += 2;
      continue;
    }
    classContent += ch;
    i++;
  }
  if (!foundClose) {
    return { converted: "\\[", endIndex: startIndex + 1 };
  }
  if (hasLiteralCloseBracket) {
    result2 += "\\]";
  }
  if (hasLiteralOpenBracket) {
    result2 += "\\[";
  }
  result2 += classContent;
  result2 += "]";
  return { converted: result2, endIndex: i };
}
__name$1(convertPosixCharClass, "convertPosixCharClass");
function posixClassToJsClass(className) {
  const mapping = {
    alnum: "a-zA-Z0-9",
    alpha: "a-zA-Z",
    ascii: "\\x00-\\x7F",
    blank: " \\t",
    cntrl: "\\x00-\\x1F\\x7F",
    digit: "0-9",
    graph: "!-~",
    lower: "a-z",
    print: " -~",
    punct: "!-/:-@\\[-`{-~",
    space: " \\t\\n\\r\\f\\v",
    upper: "A-Z",
    word: "a-zA-Z0-9_",
    xdigit: "0-9A-Fa-f"
  };
  return mapping[className] ?? "";
}
__name$1(posixClassToJsClass, "posixClassToJsClass");
function parseArrayElements(content) {
  const elements = [];
  let current = "";
  let inSingleQuote = false;
  let inDoubleQuote = false;
  let escaped = false;
  let hasContent = false;
  for (const char of content) {
    if (escaped) {
      current += char;
      escaped = false;
      hasContent = true;
      continue;
    }
    if (char === "\\") {
      escaped = true;
      continue;
    }
    if (char === "'" && !inDoubleQuote) {
      if (!inSingleQuote) {
        hasContent = true;
      }
      inSingleQuote = !inSingleQuote;
      continue;
    }
    if (char === '"' && !inSingleQuote) {
      if (!inDoubleQuote) {
        hasContent = true;
      }
      inDoubleQuote = !inDoubleQuote;
      continue;
    }
    if ((char === " " || char === "	" || char === "\n") && !inSingleQuote && !inDoubleQuote) {
      if (hasContent) {
        elements.push(current);
        current = "";
        hasContent = false;
      }
      continue;
    }
    current += char;
    hasContent = true;
  }
  if (hasContent) {
    elements.push(current);
  }
  return elements;
}
__name$1(parseArrayElements, "parseArrayElements");
function parseAssocArrayLiteral(content) {
  const entries = [];
  let pos = 0;
  while (pos < content.length) {
    while (pos < content.length && /\s/.test(content[pos])) {
      pos++;
    }
    if (pos >= content.length)
      break;
    if (content[pos] !== "[") {
      pos++;
      continue;
    }
    pos++;
    let key2 = "";
    if (content[pos] === "'" || content[pos] === '"') {
      const quote = content[pos];
      pos++;
      while (pos < content.length && content[pos] !== quote) {
        key2 += content[pos];
        pos++;
      }
      if (content[pos] === quote)
        pos++;
    } else {
      while (pos < content.length && content[pos] !== "]" && content[pos] !== "=") {
        key2 += content[pos];
        pos++;
      }
    }
    while (pos < content.length && content[pos] !== "]") {
      pos++;
    }
    if (content[pos] === "]")
      pos++;
    if (content[pos] !== "=")
      continue;
    pos++;
    let value = "";
    if (content[pos] === "'" || content[pos] === '"') {
      const quote = content[pos];
      pos++;
      while (pos < content.length && content[pos] !== quote) {
        if (content[pos] === "\\" && pos + 1 < content.length) {
          pos++;
          value += content[pos];
        } else {
          value += content[pos];
        }
        pos++;
      }
      if (content[pos] === quote)
        pos++;
    } else {
      while (pos < content.length && !/\s/.test(content[pos])) {
        value += content[pos];
        pos++;
      }
    }
    entries.push([key2, value]);
  }
  return entries;
}
__name$1(parseAssocArrayLiteral, "parseAssocArrayLiteral");
function parseAssignment(arg) {
  const arrayMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)=\((.*)\)$/s);
  if (arrayMatch) {
    return {
      name: arrayMatch[1],
      isArray: true,
      arrayElements: parseArrayElements(arrayMatch[2])
    };
  }
  const indexMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([^\]]+)\]=(.*)$/s);
  if (indexMatch) {
    return {
      name: indexMatch[1],
      isArray: false,
      arrayIndex: indexMatch[2],
      value: indexMatch[3]
    };
  }
  if (arg.includes("=")) {
    const eqIdx = arg.indexOf("=");
    return {
      name: arg.slice(0, eqIdx),
      isArray: false,
      value: arg.slice(eqIdx + 1)
    };
  }
  return {
    name: arg,
    isArray: false
  };
}
__name$1(parseAssignment, "parseAssignment");
async function evaluateArrayIndex(ctx, indexExpr) {
  try {
    const parser = new Parser();
    const arithAst = parseArithmeticExpression(parser, indexExpr);
    return await evaluateArithmetic(ctx, arithAst.expression);
  } catch {
    const num = parseInt(indexExpr, 10);
    return Number.isNaN(num) ? 0 : num;
  }
}
__name$1(evaluateArrayIndex, "evaluateArrayIndex");
async function setVariable(ctx, assignment, options = {}) {
  const { name, isArray: isArray2, arrayElements, value, arrayIndex } = assignment;
  const { makeReadonly = false, checkReadonly = true } = options;
  if (checkReadonly) {
    checkReadonlyError(ctx, name);
  }
  if (isArray2 && arrayElements) {
    for (let i = 0; i < arrayElements.length; i++) {
      ctx.state.env[`${name}_${i}`] = arrayElements[i];
    }
    ctx.state.env[`${name}__length`] = String(arrayElements.length);
  } else if (arrayIndex !== void 0 && value !== void 0) {
    const index = await evaluateArrayIndex(ctx, arrayIndex);
    ctx.state.env[`${name}_${index}`] = value;
    const currentLength = parseInt(ctx.state.env[`${name}__length`] ?? "0", 10);
    if (index >= currentLength) {
      ctx.state.env[`${name}__length`] = String(index + 1);
    }
  } else if (value !== void 0) {
    ctx.state.env[name] = value;
  }
  if (makeReadonly) {
    markReadonly(ctx, name);
  }
  return null;
}
__name$1(setVariable, "setVariable");
function markLocalVarDepth(ctx, name) {
  ctx.state.localVarDepth = ctx.state.localVarDepth || /* @__PURE__ */ new Map();
  ctx.state.localVarDepth.set(name, ctx.state.callDepth);
}
__name$1(markLocalVarDepth, "markLocalVarDepth");
function getLocalVarDepth(ctx, name) {
  return ctx.state.localVarDepth?.get(name);
}
__name$1(getLocalVarDepth, "getLocalVarDepth");
function clearLocalVarDepth(ctx, name) {
  ctx.state.localVarDepth?.delete(name);
}
__name$1(clearLocalVarDepth, "clearLocalVarDepth");
function pushLocalVarStack(ctx, name, currentValue) {
  ctx.state.localVarStack = ctx.state.localVarStack || /* @__PURE__ */ new Map();
  const stack = ctx.state.localVarStack.get(name) || [];
  stack.push({
    value: currentValue,
    scopeIndex: ctx.state.localScopes.length - 1
  });
  ctx.state.localVarStack.set(name, stack);
}
__name$1(pushLocalVarStack, "pushLocalVarStack");
function popLocalVarStack(ctx, name) {
  const stack = ctx.state.localVarStack?.get(name);
  if (!stack || stack.length === 0) {
    return void 0;
  }
  return stack.pop();
}
__name$1(popLocalVarStack, "popLocalVarStack");
function clearLocalVarStackForScope(ctx, scopeIndex) {
  if (!ctx.state.localVarStack)
    return;
  for (const [name, stack] of ctx.state.localVarStack.entries()) {
    while (stack.length > 0 && stack[stack.length - 1].scopeIndex === scopeIndex) {
      stack.pop();
    }
    if (stack.length === 0) {
      ctx.state.localVarStack.delete(name);
    }
  }
}
__name$1(clearLocalVarStackForScope, "clearLocalVarStackForScope");
var POSIX_SPECIAL_BUILTINS = /* @__PURE__ */ new Set([
  ":",
  ".",
  "break",
  "continue",
  "eval",
  "exec",
  "exit",
  "export",
  "readonly",
  "return",
  "set",
  "shift",
  "trap",
  "unset"
]);
function isPosixSpecialBuiltin(name) {
  return POSIX_SPECIAL_BUILTINS.has(name);
}
__name$1(isPosixSpecialBuiltin, "isPosixSpecialBuiltin");
var SHELL_KEYWORDS = /* @__PURE__ */ new Set([
  "if",
  "then",
  "else",
  "elif",
  "fi",
  "case",
  "esac",
  "for",
  "select",
  "while",
  "until",
  "do",
  "done",
  "in",
  "function",
  "{",
  "}",
  "time",
  "[[",
  "]]",
  "!"
]);
var SHELL_BUILTINS = /* @__PURE__ */ new Set([
  ":",
  "true",
  "false",
  "cd",
  "export",
  "unset",
  "exit",
  "local",
  "set",
  "break",
  "continue",
  "return",
  "eval",
  "shift",
  "getopts",
  "compgen",
  "complete",
  "compopt",
  "pushd",
  "popd",
  "dirs",
  "source",
  ".",
  "read",
  "mapfile",
  "readarray",
  "declare",
  "typeset",
  "readonly",
  "let",
  "command",
  "shopt",
  "exec",
  "test",
  "[",
  "echo",
  "printf",
  "pwd",
  "alias",
  "unalias",
  "type",
  "hash",
  "ulimit",
  "umask",
  "trap",
  "times",
  "wait",
  "kill",
  "jobs",
  "fg",
  "bg",
  "disown",
  "suspend",
  "fc",
  "history",
  "help",
  "enable",
  "builtin",
  "caller"
]);
async function checkOutputRedirectTarget(ctx, filePath, target, options) {
  try {
    const stat = await ctx.fs.stat(filePath);
    if (stat.isDirectory) {
      return `bash: ${target}: Is a directory
`;
    }
    if (options.checkNoclobber && ctx.state.options.noclobber && !options.isClobber && target !== "/dev/null") {
      return `bash: ${target}: cannot overwrite existing file
`;
    }
  } catch {
  }
  return null;
}
__name$1(checkOutputRedirectTarget, "checkOutputRedirectTarget");
function getFileEncoding(content) {
  for (let i = 0; i < content.length; i++) {
    if (content.charCodeAt(i) > 255) {
      return "utf8";
    }
  }
  return "binary";
}
__name$1(getFileEncoding, "getFileEncoding");
function parseRwFdContent(fdContent) {
  if (!fdContent.startsWith("__rw__:")) {
    return null;
  }
  const afterPrefix = fdContent.slice(7);
  const firstColonIdx = afterPrefix.indexOf(":");
  if (firstColonIdx === -1)
    return null;
  const pathLength = Number.parseInt(afterPrefix.slice(0, firstColonIdx), 10);
  if (Number.isNaN(pathLength) || pathLength < 0)
    return null;
  const pathStart = firstColonIdx + 1;
  const path2 = afterPrefix.slice(pathStart, pathStart + pathLength);
  const positionStart = pathStart + pathLength + 1;
  const remaining = afterPrefix.slice(positionStart);
  const posColonIdx = remaining.indexOf(":");
  if (posColonIdx === -1)
    return null;
  const position = Number.parseInt(remaining.slice(0, posColonIdx), 10);
  if (Number.isNaN(position) || position < 0)
    return null;
  const content = remaining.slice(posColonIdx + 1);
  return { path: path2, position, content };
}
__name$1(parseRwFdContent, "parseRwFdContent");
async function preExpandRedirectTargets(ctx, redirections) {
  const targets = /* @__PURE__ */ new Map();
  for (let i = 0; i < redirections.length; i++) {
    const redir = redirections[i];
    if (redir.target.type === "HereDoc") {
      continue;
    }
    const isFdRedirect = redir.operator === ">&" || redir.operator === "<&";
    if (isFdRedirect) {
      if (hasQuotedMultiValueAt(ctx, redir.target)) {
        return { targets, error: "bash: $@: ambiguous redirect\n" };
      }
      targets.set(i, await expandWord(ctx, redir.target));
    } else {
      const expandResult = await expandRedirectTarget(ctx, redir.target);
      if ("error" in expandResult) {
        return { targets, error: expandResult.error };
      }
      targets.set(i, expandResult.target);
    }
  }
  return { targets };
}
__name$1(preExpandRedirectTargets, "preExpandRedirectTargets");
function allocateFd(ctx) {
  if (ctx.state.nextFd === void 0) {
    ctx.state.nextFd = 10;
  }
  const fd = ctx.state.nextFd;
  ctx.state.nextFd++;
  return fd;
}
__name$1(allocateFd, "allocateFd");
async function processFdVariableRedirections(ctx, redirections) {
  for (const redir of redirections) {
    if (!redir.fdVariable) {
      continue;
    }
    if (!ctx.state.fileDescriptors) {
      ctx.state.fileDescriptors = /* @__PURE__ */ new Map();
    }
    if ((redir.operator === ">&" || redir.operator === "<&") && redir.target.type === "Word") {
      const target = await expandWord(ctx, redir.target);
      if (target === "-") {
        const existingFd = ctx.state.env[redir.fdVariable];
        if (existingFd !== void 0) {
          const fdNum = Number.parseInt(existingFd, 10);
          if (!Number.isNaN(fdNum)) {
            ctx.state.fileDescriptors.delete(fdNum);
          }
        }
        continue;
      }
    }
    const fd = allocateFd(ctx);
    ctx.state.env[redir.fdVariable] = String(fd);
    if (redir.target.type === "Word") {
      const target = await expandWord(ctx, redir.target);
      if (redir.operator === ">&" || redir.operator === "<&") {
        const sourceFd = Number.parseInt(target, 10);
        if (!Number.isNaN(sourceFd)) {
          const content = ctx.state.fileDescriptors.get(sourceFd);
          if (content !== void 0) {
            ctx.state.fileDescriptors.set(fd, content);
          }
          continue;
        }
      }
      if (redir.operator === ">" || redir.operator === ">>" || redir.operator === ">|" || redir.operator === "&>" || redir.operator === "&>>") {
        const filePath = ctx.fs.resolvePath(ctx.state.cwd, target);
        if (redir.operator === ">" || redir.operator === ">|" || redir.operator === "&>") {
          await ctx.fs.writeFile(filePath, "", "binary");
        }
        ctx.state.fileDescriptors.set(fd, `__file__:${filePath}`);
      } else if (redir.operator === "<<<") {
        ctx.state.fileDescriptors.set(fd, `${target}
`);
      } else if (redir.operator === "<" || redir.operator === "<>") {
        try {
          const filePath = ctx.fs.resolvePath(ctx.state.cwd, target);
          const content = await ctx.fs.readFile(filePath);
          ctx.state.fileDescriptors.set(fd, content);
        } catch {
          return result("", `bash: ${target}: No such file or directory
`, 1);
        }
      }
    }
  }
  return null;
}
__name$1(processFdVariableRedirections, "processFdVariableRedirections");
async function preOpenOutputRedirects(ctx, redirections) {
  for (const redir of redirections) {
    if (redir.target.type === "HereDoc") {
      continue;
    }
    const isGreaterAmpersand = redir.operator === ">&";
    if (redir.operator !== ">" && redir.operator !== ">|" && redir.operator !== "&>" && !isGreaterAmpersand) {
      continue;
    }
    let target;
    if (isGreaterAmpersand) {
      target = await expandWord(ctx, redir.target);
      if (target === "-" || !Number.isNaN(Number.parseInt(target, 10)) || redir.fd != null) {
        continue;
      }
    } else {
      const expandResult = await expandRedirectTarget(ctx, redir.target);
      if ("error" in expandResult) {
        return result("", expandResult.error, 1);
      }
      target = expandResult.target;
    }
    const filePath = ctx.fs.resolvePath(ctx.state.cwd, target);
    const isClobber = redir.operator === ">|";
    try {
      const stat = await ctx.fs.stat(filePath);
      if (stat.isDirectory) {
        return result("", `bash: ${target}: Is a directory
`, 1);
      }
      if (ctx.state.options.noclobber && !isClobber && !stat.isDirectory && target !== "/dev/null") {
        return result("", `bash: ${target}: cannot overwrite existing file
`, 1);
      }
    } catch {
    }
    if (target !== "/dev/null" && target !== "/dev/stdout" && target !== "/dev/stderr" && target !== "/dev/full") {
      await ctx.fs.writeFile(filePath, "", "binary");
    }
    if (target === "/dev/full") {
      return result("", `bash: /dev/full: No space left on device
`, 1);
    }
  }
  return null;
}
__name$1(preOpenOutputRedirects, "preOpenOutputRedirects");
async function applyRedirections(ctx, result2, redirections, preExpandedTargets) {
  let { stdout, stderr, exitCode } = result2;
  for (let i = 0; i < redirections.length; i++) {
    const redir = redirections[i];
    if (redir.target.type === "HereDoc") {
      continue;
    }
    let target;
    const preExpanded = preExpandedTargets?.get(i);
    if (preExpanded !== void 0) {
      target = preExpanded;
    } else {
      const isFdRedirect = redir.operator === ">&" || redir.operator === "<&";
      if (isFdRedirect) {
        if (hasQuotedMultiValueAt(ctx, redir.target)) {
          stderr += "bash: $@: ambiguous redirect\n";
          exitCode = 1;
          stdout = "";
          continue;
        }
        target = await expandWord(ctx, redir.target);
      } else {
        const expandResult = await expandRedirectTarget(ctx, redir.target);
        if ("error" in expandResult) {
          stderr += expandResult.error;
          exitCode = 1;
          stdout = "";
          continue;
        }
        target = expandResult.target;
      }
    }
    if (redir.fdVariable) {
      continue;
    }
    switch (redir.operator) {
      case ">":
      case ">|": {
        const fd = redir.fd ?? 1;
        const isClobber = redir.operator === ">|";
        if (fd === 1) {
          if (target === "/dev/stdout") {
            break;
          }
          if (target === "/dev/stderr") {
            stderr += stdout;
            stdout = "";
            break;
          }
          if (target === "/dev/full") {
            stderr += `bash: echo: write error: No space left on device
`;
            exitCode = 1;
            stdout = "";
            break;
          }
          const filePath = ctx.fs.resolvePath(ctx.state.cwd, target);
          const error = await checkOutputRedirectTarget(ctx, filePath, target, {
            checkNoclobber: true,
            isClobber
          });
          if (error) {
            stderr += error;
            exitCode = 1;
            stdout = "";
            break;
          }
          await ctx.fs.writeFile(filePath, stdout, getFileEncoding(stdout));
          stdout = "";
        } else if (fd === 2) {
          if (target === "/dev/stderr") {
            break;
          }
          if (target === "/dev/stdout") {
            stdout += stderr;
            stderr = "";
            break;
          }
          if (target === "/dev/full") {
            stderr += `bash: echo: write error: No space left on device
`;
            exitCode = 1;
            break;
          }
          if (target === "/dev/null") {
            stderr = "";
          } else {
            const filePath = ctx.fs.resolvePath(ctx.state.cwd, target);
            const error = await checkOutputRedirectTarget(ctx, filePath, target, {
              checkNoclobber: true,
              isClobber
            });
            if (error) {
              stderr += error;
              exitCode = 1;
              break;
            }
            await ctx.fs.writeFile(filePath, stderr, getFileEncoding(stderr));
            stderr = "";
          }
        }
        break;
      }
      case ">>": {
        const fd = redir.fd ?? 1;
        if (fd === 1) {
          if (target === "/dev/stdout") {
            break;
          }
          if (target === "/dev/stderr") {
            stderr += stdout;
            stdout = "";
            break;
          }
          if (target === "/dev/full") {
            stderr += `bash: echo: write error: No space left on device
`;
            exitCode = 1;
            stdout = "";
            break;
          }
          const filePath = ctx.fs.resolvePath(ctx.state.cwd, target);
          const error = await checkOutputRedirectTarget(ctx, filePath, target, {});
          if (error) {
            stderr += error;
            exitCode = 1;
            stdout = "";
            break;
          }
          await ctx.fs.appendFile(filePath, stdout, getFileEncoding(stdout));
          stdout = "";
        } else if (fd === 2) {
          if (target === "/dev/stderr") {
            break;
          }
          if (target === "/dev/stdout") {
            stdout += stderr;
            stderr = "";
            break;
          }
          if (target === "/dev/full") {
            stderr += `bash: echo: write error: No space left on device
`;
            exitCode = 1;
            break;
          }
          const filePath2 = ctx.fs.resolvePath(ctx.state.cwd, target);
          const error2 = await checkOutputRedirectTarget(ctx, filePath2, target, {});
          if (error2) {
            stderr += error2;
            exitCode = 1;
            break;
          }
          await ctx.fs.appendFile(filePath2, stderr, getFileEncoding(stderr));
          stderr = "";
        }
        break;
      }
      case ">&":
      case "<&": {
        const fd = redir.fd ?? 1;
        if (target === "-") {
          break;
        }
        if (target.endsWith("-")) {
          const sourceFdStr = target.slice(0, -1);
          const sourceFd = Number.parseInt(sourceFdStr, 10);
          if (!Number.isNaN(sourceFd)) {
            const sourceInfo = ctx.state.fileDescriptors?.get(sourceFd);
            if (sourceInfo !== void 0) {
              if (!ctx.state.fileDescriptors) {
                ctx.state.fileDescriptors = /* @__PURE__ */ new Map();
              }
              ctx.state.fileDescriptors.set(fd, sourceInfo);
              if (sourceFd >= 3) {
                ctx.state.fileDescriptors?.delete(sourceFd);
              }
            } else if (sourceFd === 1 || sourceFd === 2) {
              if (!ctx.state.fileDescriptors) {
                ctx.state.fileDescriptors = /* @__PURE__ */ new Map();
              }
              ctx.state.fileDescriptors.set(fd, `__dupout__:${sourceFd}`);
            } else if (sourceFd === 0) {
              if (!ctx.state.fileDescriptors) {
                ctx.state.fileDescriptors = /* @__PURE__ */ new Map();
              }
              ctx.state.fileDescriptors.set(fd, `__dupin__:${sourceFd}`);
            } else if (sourceFd >= 3) {
              stderr += `bash: ${sourceFd}: Bad file descriptor
`;
              exitCode = 1;
            }
          }
          break;
        }
        if (target === "2" || target === "&2") {
          if (fd === 1) {
            stderr += stdout;
            stdout = "";
          }
        } else if (target === "1" || target === "&1") {
          if (fd === 2) {
            stdout += stderr;
            stderr = "";
          } else {
            stdout += stderr;
            stderr = "";
          }
        } else {
          const targetFd = Number.parseInt(target, 10);
          if (!Number.isNaN(targetFd)) {
            const fdInfo = ctx.state.fileDescriptors?.get(targetFd);
            if (fdInfo?.startsWith("__file__:")) {
              const resolvedPath = fdInfo.slice(9);
              if (fd === 1) {
                await ctx.fs.appendFile(resolvedPath, stdout, getFileEncoding(stdout));
                stdout = "";
              } else if (fd === 2) {
                await ctx.fs.appendFile(resolvedPath, stderr, getFileEncoding(stderr));
                stderr = "";
              }
            } else if (fdInfo?.startsWith("__rw__:")) {
              const parsed = parseRwFdContent(fdInfo);
              if (parsed) {
                if (fd === 1) {
                  await ctx.fs.appendFile(parsed.path, stdout, getFileEncoding(stdout));
                  stdout = "";
                } else if (fd === 2) {
                  await ctx.fs.appendFile(parsed.path, stderr, getFileEncoding(stderr));
                  stderr = "";
                }
              }
            } else if (fdInfo?.startsWith("__dupout__:")) {
              const sourceFd = Number.parseInt(fdInfo.slice(11), 10);
              if (sourceFd === 1) ;
              else if (sourceFd === 2) {
                if (fd === 1) {
                  stderr += stdout;
                  stdout = "";
                }
              } else {
                const sourceInfo = ctx.state.fileDescriptors?.get(sourceFd);
                if (sourceInfo?.startsWith("__file__:")) {
                  const resolvedPath = sourceInfo.slice(9);
                  if (fd === 1) {
                    await ctx.fs.appendFile(resolvedPath, stdout, getFileEncoding(stdout));
                    stdout = "";
                  } else if (fd === 2) {
                    await ctx.fs.appendFile(resolvedPath, stderr, getFileEncoding(stderr));
                    stderr = "";
                  }
                }
              }
            } else if (fdInfo?.startsWith("__dupin__:")) {
              stderr += `bash: ${targetFd}: Bad file descriptor
`;
              exitCode = 1;
              stdout = "";
            } else if (targetFd >= 3) {
              stderr += `bash: ${targetFd}: Bad file descriptor
`;
              exitCode = 1;
              stdout = "";
            }
          } else if (redir.operator === ">&") {
            const filePath = ctx.fs.resolvePath(ctx.state.cwd, target);
            const error = await checkOutputRedirectTarget(ctx, filePath, target, {
              checkNoclobber: true
            });
            if (error) {
              stderr = error;
              exitCode = 1;
              stdout = "";
              break;
            }
            if (redir.fd == null) {
              const combined = stdout + stderr;
              await ctx.fs.writeFile(filePath, combined, getFileEncoding(combined));
              stdout = "";
              stderr = "";
            } else if (fd === 1) {
              await ctx.fs.writeFile(filePath, stdout, getFileEncoding(stdout));
              stdout = "";
            } else if (fd === 2) {
              await ctx.fs.writeFile(filePath, stderr, getFileEncoding(stderr));
              stderr = "";
            }
          }
        }
        break;
      }
      case "&>": {
        if (target === "/dev/full") {
          stderr = `bash: echo: write error: No space left on device
`;
          exitCode = 1;
          stdout = "";
          break;
        }
        const filePath = ctx.fs.resolvePath(ctx.state.cwd, target);
        const error = await checkOutputRedirectTarget(ctx, filePath, target, {
          checkNoclobber: true
        });
        if (error) {
          stderr = error;
          exitCode = 1;
          stdout = "";
          break;
        }
        const combined = stdout + stderr;
        await ctx.fs.writeFile(filePath, combined, getFileEncoding(combined));
        stdout = "";
        stderr = "";
        break;
      }
      case "&>>": {
        if (target === "/dev/full") {
          stderr = `bash: echo: write error: No space left on device
`;
          exitCode = 1;
          stdout = "";
          break;
        }
        const filePath = ctx.fs.resolvePath(ctx.state.cwd, target);
        const error = await checkOutputRedirectTarget(ctx, filePath, target, {});
        if (error) {
          stderr = error;
          exitCode = 1;
          stdout = "";
          break;
        }
        const combined = stdout + stderr;
        await ctx.fs.appendFile(filePath, combined, getFileEncoding(combined));
        stdout = "";
        stderr = "";
        break;
      }
    }
  }
  const fd1Info = ctx.state.fileDescriptors?.get(1);
  if (fd1Info) {
    if (fd1Info === "__dupout__:2") {
      stderr += stdout;
      stdout = "";
    } else if (fd1Info.startsWith("__file__:")) {
      const filePath = fd1Info.slice(9);
      await ctx.fs.appendFile(filePath, stdout, getFileEncoding(stdout));
      stdout = "";
    } else if (fd1Info.startsWith("__file_append__:")) {
      const filePath = fd1Info.slice(16);
      await ctx.fs.appendFile(filePath, stdout, getFileEncoding(stdout));
      stdout = "";
    }
  }
  const fd2Info = ctx.state.fileDescriptors?.get(2);
  if (fd2Info) {
    if (fd2Info === "__dupout__:1") {
      stdout += stderr;
      stderr = "";
    } else if (fd2Info.startsWith("__file__:")) {
      const filePath = fd2Info.slice(9);
      await ctx.fs.appendFile(filePath, stderr, getFileEncoding(stderr));
      stderr = "";
    } else if (fd2Info.startsWith("__file_append__:")) {
      const filePath = fd2Info.slice(16);
      await ctx.fs.appendFile(filePath, stderr, getFileEncoding(stderr));
      stderr = "";
    }
  }
  return result(stdout, stderr, exitCode);
}
__name$1(applyRedirections, "applyRedirections");
function executeFunctionDef(ctx, node) {
  if (ctx.state.options.posix && POSIX_SPECIAL_BUILTINS.has(node.name)) {
    const stderr = `bash: line ${ctx.state.currentLine}: \`${node.name}': is a special builtin
`;
    throw new ExitError(2, "", stderr);
  }
  const funcWithSource = {
    ...node,
    sourceFile: node.sourceFile ?? ctx.state.currentSource ?? "main"
  };
  ctx.state.functions.set(node.name, funcWithSource);
  return OK;
}
__name$1(executeFunctionDef, "executeFunctionDef");
async function processInputRedirections(ctx, redirections) {
  let stdin = "";
  for (const redir of redirections) {
    if ((redir.operator === "<<" || redir.operator === "<<-") && redir.target.type === "HereDoc") {
      const hereDoc = redir.target;
      let content = await expandWord(ctx, hereDoc.content);
      if (hereDoc.stripTabs) {
        content = content.split("\n").map((line) => line.replace(/^\t+/, "")).join("\n");
      }
      const fd = redir.fd ?? 0;
      if (fd === 0) {
        stdin = content;
      }
    } else if (redir.operator === "<<<" && redir.target.type === "Word") {
      stdin = `${await expandWord(ctx, redir.target)}
`;
    } else if (redir.operator === "<" && redir.target.type === "Word") {
      const target = await expandWord(ctx, redir.target);
      const filePath = ctx.fs.resolvePath(ctx.state.cwd, target);
      try {
        stdin = await ctx.fs.readFile(filePath);
      } catch {
      }
    }
  }
  return stdin;
}
__name$1(processInputRedirections, "processInputRedirections");
async function callFunction(ctx, func, args, stdin = "", callLine) {
  ctx.state.callDepth++;
  if (ctx.state.callDepth > ctx.limits.maxCallDepth) {
    ctx.state.callDepth--;
    throwExecutionLimit(`${func.name}: maximum recursion depth (${ctx.limits.maxCallDepth}) exceeded, increase executionLimits.maxCallDepth`, "recursion");
  }
  if (!ctx.state.funcNameStack) {
    ctx.state.funcNameStack = [];
  }
  if (!ctx.state.callLineStack) {
    ctx.state.callLineStack = [];
  }
  if (!ctx.state.sourceStack) {
    ctx.state.sourceStack = [];
  }
  ctx.state.funcNameStack.unshift(func.name);
  ctx.state.callLineStack.unshift(callLine ?? ctx.state.currentLine);
  ctx.state.sourceStack.unshift(func.sourceFile ?? "main");
  ctx.state.localScopes.push(/* @__PURE__ */ new Map());
  if (!ctx.state.localExportedVars) {
    ctx.state.localExportedVars = [];
  }
  ctx.state.localExportedVars.push(/* @__PURE__ */ new Set());
  const savedPositional = {};
  for (let i = 0; i < args.length; i++) {
    savedPositional[String(i + 1)] = ctx.state.env[String(i + 1)];
    ctx.state.env[String(i + 1)] = args[i];
  }
  savedPositional["@"] = ctx.state.env["@"];
  savedPositional["#"] = ctx.state.env["#"];
  ctx.state.env["@"] = args.join(" ");
  ctx.state.env["#"] = String(args.length);
  const cleanup = /* @__PURE__ */ __name$1(() => {
    const scopeIndex = ctx.state.localScopes.length - 1;
    const localScope = ctx.state.localScopes.pop();
    if (localScope) {
      for (const [varName, originalValue] of localScope) {
        if (originalValue === void 0) {
          delete ctx.state.env[varName];
        } else {
          ctx.state.env[varName] = originalValue;
        }
      }
    }
    clearLocalVarStackForScope(ctx, scopeIndex);
    if (ctx.state.fullyUnsetLocals) {
      for (const [name, entryScope] of ctx.state.fullyUnsetLocals.entries()) {
        if (entryScope === scopeIndex) {
          ctx.state.fullyUnsetLocals.delete(name);
        }
      }
    }
    if (ctx.state.localExportedVars && ctx.state.localExportedVars.length > 0) {
      const localExports = ctx.state.localExportedVars.pop();
      if (localExports) {
        for (const name of localExports) {
          ctx.state.exportedVars?.delete(name);
        }
      }
    }
    for (const [key2, value] of Object.entries(savedPositional)) {
      if (value === void 0) {
        delete ctx.state.env[key2];
      } else {
        ctx.state.env[key2] = value;
      }
    }
    ctx.state.funcNameStack?.shift();
    ctx.state.callLineStack?.shift();
    ctx.state.sourceStack?.shift();
    ctx.state.callDepth--;
  }, "cleanup");
  const { targets: preExpandedTargets, error: expandError } = await preExpandRedirectTargets(ctx, func.redirections);
  if (expandError) {
    cleanup();
    return result("", expandError, 1);
  }
  try {
    const redirectionStdin = await processInputRedirections(ctx, func.redirections);
    const effectiveStdin = stdin || redirectionStdin;
    const execResult = await ctx.executeCommand(func.body, effectiveStdin);
    cleanup();
    return applyRedirections(ctx, execResult, func.redirections, preExpandedTargets);
  } catch (error) {
    cleanup();
    if (error instanceof ReturnError) {
      const returnResult = result(error.stdout, error.stderr, error.exitCode);
      return applyRedirections(ctx, returnResult, func.redirections, preExpandedTargets);
    }
    throw error;
  }
}
__name$1(callFunction, "callFunction");
var SHELL_KEYWORDS2 = [
  "!",
  "[[",
  "]]",
  "case",
  "do",
  "done",
  "elif",
  "else",
  "esac",
  "fi",
  "for",
  "function",
  "if",
  "in",
  "then",
  "time",
  "until",
  "while",
  "{",
  "}"
];
var SHELL_BUILTINS2 = [
  ".",
  ":",
  "[",
  "alias",
  "bg",
  "bind",
  "break",
  "builtin",
  "caller",
  "cd",
  "command",
  "compgen",
  "complete",
  "compopt",
  "continue",
  "declare",
  "dirs",
  "disown",
  "echo",
  "enable",
  "eval",
  "exec",
  "exit",
  "export",
  "false",
  "fc",
  "fg",
  "getopts",
  "hash",
  "help",
  "history",
  "jobs",
  "kill",
  "let",
  "local",
  "logout",
  "mapfile",
  "popd",
  "printf",
  "pushd",
  "pwd",
  "read",
  "readarray",
  "readonly",
  "return",
  "set",
  "shift",
  "shopt",
  "source",
  "suspend",
  "test",
  "times",
  "trap",
  "true",
  "type",
  "typeset",
  "ulimit",
  "umask",
  "unalias",
  "unset",
  "wait"
];
var SHOPT_OPTIONS = [
  "autocd",
  "assoc_expand_once",
  "cdable_vars",
  "cdspell",
  "checkhash",
  "checkjobs",
  "checkwinsize",
  "cmdhist",
  "compat31",
  "compat32",
  "compat40",
  "compat41",
  "compat42",
  "compat43",
  "compat44",
  "complete_fullquote",
  "direxpand",
  "dirspell",
  "dotglob",
  "execfail",
  "expand_aliases",
  "extdebug",
  "extglob",
  "extquote",
  "failglob",
  "force_fignore",
  "globasciiranges",
  "globstar",
  "gnu_errfmt",
  "histappend",
  "histreedit",
  "histverify",
  "hostcomplete",
  "huponexit",
  "inherit_errexit",
  "interactive_comments",
  "lastpipe",
  "lithist",
  "localvar_inherit",
  "localvar_unset",
  "login_shell",
  "mailwarn",
  "no_empty_cmd_completion",
  "nocaseglob",
  "nocasematch",
  "nullglob",
  "progcomp",
  "progcomp_alias",
  "promptvars",
  "restricted_shell",
  "shift_verbose",
  "sourcepath",
  "xpg_echo"
];
var HELP_TOPICS = SHELL_BUILTINS2;
async function handleCompgen(ctx, args) {
  const actionTypes = [];
  let wordlist = null;
  let prefix = "";
  let suffix = "";
  let searchPrefix = null;
  let plusdirsOption = false;
  let dirnamesOption = false;
  let defaultOption = false;
  let excludePattern = null;
  let functionName = null;
  let commandString = null;
  const processedArgs = [];
  const validActions = [
    "alias",
    "arrayvar",
    "binding",
    "builtin",
    "command",
    "directory",
    "disabled",
    "enabled",
    "export",
    "file",
    "function",
    "group",
    "helptopic",
    "hostname",
    "job",
    "keyword",
    "running",
    "service",
    "setopt",
    "shopt",
    "signal",
    "stopped",
    "user",
    "variable"
  ];
  for (let i = 0; i < args.length; i++) {
    const arg = args[i];
    if (arg === "-v") {
      actionTypes.push("variable");
    } else if (arg === "-e") {
      actionTypes.push("export");
    } else if (arg === "-f") {
      actionTypes.push("file");
    } else if (arg === "-d") {
      actionTypes.push("directory");
    } else if (arg === "-k") {
      actionTypes.push("keyword");
    } else if (arg === "-A") {
      i++;
      if (i >= args.length) {
        return failure("compgen: -A: option requires an argument\n", 2);
      }
      const actionType = args[i];
      if (!validActions.includes(actionType)) {
        return failure(`compgen: ${actionType}: invalid action name
`, 2);
      }
      actionTypes.push(actionType);
    } else if (arg === "-W") {
      i++;
      if (i >= args.length) {
        return failure("compgen: -W: option requires an argument\n", 2);
      }
      wordlist = args[i];
    } else if (arg === "-P") {
      i++;
      if (i >= args.length) {
        return failure("compgen: -P: option requires an argument\n", 2);
      }
      prefix = args[i];
    } else if (arg === "-S") {
      i++;
      if (i >= args.length) {
        return failure("compgen: -S: option requires an argument\n", 2);
      }
      suffix = args[i];
    } else if (arg === "-o") {
      i++;
      if (i >= args.length) {
        return failure("compgen: -o: option requires an argument\n", 2);
      }
      const opt = args[i];
      if (opt === "plusdirs") {
        plusdirsOption = true;
      } else if (opt === "dirnames") {
        dirnamesOption = true;
      } else if (opt === "default") {
        defaultOption = true;
      } else if (opt === "filenames" || opt === "nospace" || opt === "bashdefault" || opt === "noquote") ;
      else {
        return failure(`compgen: ${opt}: invalid option name
`, 2);
      }
    } else if (arg === "-F") {
      i++;
      if (i >= args.length) {
        return failure("compgen: -F: option requires an argument\n", 2);
      }
      functionName = args[i];
    } else if (arg === "-C") {
      i++;
      if (i >= args.length) {
        return failure("compgen: -C: option requires an argument\n", 2);
      }
      commandString = args[i];
    } else if (arg === "-X") {
      i++;
      if (i >= args.length) {
        return failure("compgen: -X: option requires an argument\n", 2);
      }
      excludePattern = args[i];
    } else if (arg === "-G") {
      i++;
      if (i >= args.length) {
        return failure("compgen: -G: option requires an argument\n", 2);
      }
    } else if (arg === "--") {
      processedArgs.push(...args.slice(i + 1));
      break;
    } else if (!arg.startsWith("-")) {
      processedArgs.push(arg);
    }
  }
  searchPrefix = processedArgs[0] ?? null;
  const completions = [];
  if (dirnamesOption) {
    const dirCompletions = await getDirectoryCompletions(ctx, searchPrefix);
    completions.push(...dirCompletions);
  }
  if (defaultOption) {
    const fileCompletions = await getFileCompletions(ctx, searchPrefix);
    completions.push(...fileCompletions);
  }
  for (const actionType of actionTypes) {
    if (actionType === "variable") {
      const vars = getVariableNames(ctx, searchPrefix);
      completions.push(...vars);
    } else if (actionType === "export") {
      const vars = getExportedVariableNames(ctx, searchPrefix);
      completions.push(...vars);
    } else if (actionType === "function") {
      const funcs = getFunctionNames(ctx, searchPrefix);
      completions.push(...funcs);
    } else if (actionType === "builtin") {
      const builtins = getBuiltinNames(searchPrefix);
      completions.push(...builtins);
    } else if (actionType === "keyword") {
      const keywords = getKeywordNames(searchPrefix);
      completions.push(...keywords);
    } else if (actionType === "alias") {
      const aliases = getAliasNames(ctx, searchPrefix);
      completions.push(...aliases);
    } else if (actionType === "shopt") {
      const shopts = getShoptNames(searchPrefix);
      completions.push(...shopts);
    } else if (actionType === "helptopic") {
      const topics = getHelpTopicNames(searchPrefix);
      completions.push(...topics);
    } else if (actionType === "directory") {
      const dirs = await getDirectoryCompletions(ctx, searchPrefix);
      completions.push(...dirs);
    } else if (actionType === "file") {
      const files = await getFileCompletions(ctx, searchPrefix);
      completions.push(...files);
    } else if (actionType === "user") {
      const users = getUserNames();
      completions.push(...users);
    } else if (actionType === "command") {
      const commands = await getCommandCompletions(ctx, searchPrefix);
      completions.push(...commands);
    }
  }
  if (wordlist !== null) {
    try {
      const expandedWordlist = await expandWordlistString(ctx, wordlist);
      const words = splitWordlist(ctx, expandedWordlist);
      for (const word of words) {
        if (searchPrefix === null || word.startsWith(searchPrefix)) {
          completions.push(word);
        }
      }
    } catch {
      return result("", "", 1);
    }
  }
  if (plusdirsOption) {
    const dirCompletions = await getDirectoryCompletions(ctx, searchPrefix);
    for (const dir of dirCompletions) {
      if (!completions.includes(dir)) {
        completions.push(dir);
      }
    }
  }
  let functionStdout = "";
  if (functionName !== null) {
    const func = ctx.state.functions.get(functionName);
    if (func) {
      const savedEnv = {};
      savedEnv.COMP_WORDS__length = ctx.state.env.COMP_WORDS__length;
      ctx.state.env.COMP_WORDS__length = "0";
      savedEnv.COMP_CWORD = ctx.state.env.COMP_CWORD;
      ctx.state.env.COMP_CWORD = "-1";
      savedEnv.COMP_LINE = ctx.state.env.COMP_LINE;
      ctx.state.env.COMP_LINE = "";
      savedEnv.COMP_POINT = ctx.state.env.COMP_POINT;
      ctx.state.env.COMP_POINT = "0";
      const savedCompreply = {};
      for (const key2 of Object.keys(ctx.state.env)) {
        if (key2 === "COMPREPLY" || key2.startsWith("COMPREPLY_") || key2 === "COMPREPLY__length") {
          savedCompreply[key2] = ctx.state.env[key2];
          delete ctx.state.env[key2];
        }
      }
      const funcArgs = ["compgen", processedArgs[0] ?? "", ""];
      try {
        const funcResult = await callFunction(ctx, func, funcArgs, "");
        if (funcResult.exitCode !== 0) {
          restoreEnv(ctx, savedEnv);
          restoreEnv(ctx, savedCompreply);
          return result("", funcResult.stderr, 1);
        }
        functionStdout = funcResult.stdout;
        const compreplyValues = getCompreplyValues(ctx);
        completions.push(...compreplyValues);
      } catch {
        restoreEnv(ctx, savedEnv);
        restoreEnv(ctx, savedCompreply);
        return result("", "", 1);
      }
      restoreEnv(ctx, savedEnv);
      restoreEnv(ctx, savedCompreply);
    }
  }
  if (commandString !== null) {
    try {
      const ast = parse(commandString);
      const cmdResult = await ctx.executeScript(ast);
      if (cmdResult.exitCode !== 0) {
        return result("", cmdResult.stderr, cmdResult.exitCode);
      }
      if (cmdResult.stdout) {
        const lines = cmdResult.stdout.split("\n");
        for (const line of lines) {
          if (line.length > 0) {
            completions.push(line);
          }
        }
      }
    } catch (error) {
      if (error.name === "ParseException") {
        return failure(`compgen: -C: ${error.message}
`, 2);
      }
      throw error;
    }
  }
  let filteredCompletions = completions;
  if (excludePattern !== null) {
    const isNegated = excludePattern.startsWith("!");
    const pattern = isNegated ? excludePattern.slice(1) : excludePattern;
    filteredCompletions = completions.filter((c) => {
      const matches = matchPattern(c, pattern, false, true);
      return isNegated ? matches : !matches;
    });
  }
  if (filteredCompletions.length === 0 && searchPrefix !== null) {
    return result(functionStdout, "", 1);
  }
  const completionOutput = filteredCompletions.map((c) => `${prefix}${c}${suffix}`).join("\n");
  const output = functionStdout + (completionOutput ? `${completionOutput}
` : "");
  return success(output);
}
__name$1(handleCompgen, "handleCompgen");
function getVariableNames(ctx, prefix) {
  const names = /* @__PURE__ */ new Set();
  for (const key2 of Object.keys(ctx.state.env)) {
    if (key2.includes("_") && /^[a-zA-Z_][a-zA-Z0-9_]*_\d+$/.test(key2)) {
      continue;
    }
    if (key2.endsWith("__length")) {
      continue;
    }
    const baseName = key2.split("_")[0];
    if (/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key2)) {
      names.add(key2);
    } else if (baseName && /^[a-zA-Z_][a-zA-Z0-9_]*$/.test(baseName) && ctx.state.env[`${baseName}__length`] !== void 0) {
      names.add(baseName);
    }
  }
  let resultArr = Array.from(names);
  if (prefix !== null) {
    resultArr = resultArr.filter((n) => n.startsWith(prefix));
  }
  return resultArr.sort();
}
__name$1(getVariableNames, "getVariableNames");
function getExportedVariableNames(ctx, prefix) {
  const exportedVars = ctx.state.exportedVars ?? /* @__PURE__ */ new Set();
  let resultArr = Array.from(exportedVars);
  if (prefix !== null) {
    resultArr = resultArr.filter((n) => n.startsWith(prefix));
  }
  resultArr = resultArr.filter((n) => {
    if (n.includes("_") && /^[a-zA-Z_][a-zA-Z0-9_]*_\d+$/.test(n)) {
      return false;
    }
    if (n.endsWith("__length")) {
      return false;
    }
    return ctx.state.env[n] !== void 0;
  });
  return resultArr.sort();
}
__name$1(getExportedVariableNames, "getExportedVariableNames");
function getFunctionNames(ctx, prefix) {
  let resultArr = Array.from(ctx.state.functions.keys());
  if (prefix !== null) {
    resultArr = resultArr.filter((n) => n.startsWith(prefix));
  }
  return resultArr.sort();
}
__name$1(getFunctionNames, "getFunctionNames");
function getBuiltinNames(prefix) {
  let resultArr = [...SHELL_BUILTINS2];
  if (prefix !== null) {
    resultArr = resultArr.filter((n) => n.startsWith(prefix));
  }
  return resultArr.sort();
}
__name$1(getBuiltinNames, "getBuiltinNames");
function getKeywordNames(prefix) {
  let resultArr = [...SHELL_KEYWORDS2];
  if (prefix !== null) {
    resultArr = resultArr.filter((n) => n.startsWith(prefix));
  }
  return resultArr.sort();
}
__name$1(getKeywordNames, "getKeywordNames");
function getAliasNames(ctx, prefix) {
  const names = [];
  for (const key2 of Object.keys(ctx.state.env)) {
    if (key2.startsWith("BASH_ALIAS_")) {
      const aliasName = key2.slice("BASH_ALIAS_".length);
      names.push(aliasName);
    }
  }
  let resultArr = names;
  if (prefix !== null) {
    resultArr = resultArr.filter((n) => n.startsWith(prefix));
  }
  return resultArr.sort();
}
__name$1(getAliasNames, "getAliasNames");
function getShoptNames(prefix) {
  let resultArr = [...SHOPT_OPTIONS];
  if (prefix !== null) {
    resultArr = resultArr.filter((n) => n.startsWith(prefix));
  }
  return resultArr.sort();
}
__name$1(getShoptNames, "getShoptNames");
function getHelpTopicNames(prefix) {
  let resultArr = [...HELP_TOPICS];
  if (prefix !== null) {
    resultArr = resultArr.filter((n) => n.startsWith(prefix));
  }
  return resultArr.sort();
}
__name$1(getHelpTopicNames, "getHelpTopicNames");
async function getDirectoryCompletions(ctx, prefix) {
  const results = [];
  try {
    let searchDir = ctx.state.cwd;
    let matchPrefix = prefix ?? "";
    if (prefix) {
      const lastSlash = prefix.lastIndexOf("/");
      if (lastSlash !== -1) {
        const dirPart = prefix.slice(0, lastSlash) || "/";
        matchPrefix = prefix.slice(lastSlash + 1);
        if (dirPart.startsWith("/")) {
          searchDir = dirPart;
        } else {
          searchDir = `${ctx.state.cwd}/${dirPart}`;
        }
      }
    }
    const entries = await ctx.fs.readdir(searchDir);
    for (const entry of entries) {
      const fullPath = `${searchDir}/${entry}`;
      try {
        const stat = await ctx.fs.stat(fullPath);
        if (stat.isDirectory) {
          if (!matchPrefix || entry.startsWith(matchPrefix)) {
            if (prefix?.includes("/")) {
              const lastSlash = prefix.lastIndexOf("/");
              const dirPart = prefix.slice(0, lastSlash + 1);
              results.push(dirPart + entry);
            } else {
              results.push(entry);
            }
          }
        }
      } catch {
      }
    }
  } catch {
  }
  return results.sort();
}
__name$1(getDirectoryCompletions, "getDirectoryCompletions");
async function getFileCompletions(ctx, prefix) {
  const results = [];
  try {
    let searchDir = ctx.state.cwd;
    let matchPrefix = prefix ?? "";
    if (prefix) {
      const lastSlash = prefix.lastIndexOf("/");
      if (lastSlash !== -1) {
        const dirPart = prefix.slice(0, lastSlash) || "/";
        matchPrefix = prefix.slice(lastSlash + 1);
        if (dirPart.startsWith("/")) {
          searchDir = dirPart;
        } else {
          searchDir = `${ctx.state.cwd}/${dirPart}`;
        }
      }
    }
    const entries = await ctx.fs.readdir(searchDir);
    for (const entry of entries) {
      if (!matchPrefix || entry.startsWith(matchPrefix)) {
        if (prefix?.includes("/")) {
          const lastSlash = prefix.lastIndexOf("/");
          const dirPart = prefix.slice(0, lastSlash + 1);
          results.push(dirPart + entry);
        } else {
          results.push(entry);
        }
      }
    }
  } catch {
  }
  return results.sort();
}
__name$1(getFileCompletions, "getFileCompletions");
function getUserNames(_prefix) {
  return ["root", "nobody"];
}
__name$1(getUserNames, "getUserNames");
async function getCommandCompletions(ctx, prefix) {
  const commands = /* @__PURE__ */ new Set();
  for (const builtin of SHELL_BUILTINS2) {
    commands.add(builtin);
  }
  for (const func of ctx.state.functions.keys()) {
    commands.add(func);
  }
  for (const key2 of Object.keys(ctx.state.env)) {
    if (key2.startsWith("BASH_ALIAS_")) {
      commands.add(key2.slice("BASH_ALIAS_".length));
    }
  }
  for (const keyword of SHELL_KEYWORDS2) {
    commands.add(keyword);
  }
  const path2 = ctx.state.env.PATH ?? "/usr/bin:/bin";
  for (const dir of path2.split(":")) {
    if (!dir)
      continue;
    try {
      const entries = await ctx.fs.readdir(dir);
      for (const entry of entries) {
        commands.add(entry);
      }
    } catch {
    }
  }
  let resultArr = Array.from(commands);
  if (prefix !== null) {
    resultArr = resultArr.filter((c) => c.startsWith(prefix));
  }
  return resultArr.sort();
}
__name$1(getCommandCompletions, "getCommandCompletions");
async function expandWordlistString(ctx, wordlist) {
  const parser = new Parser();
  const wordNode = parser.parseWordFromString(wordlist, false, false);
  return await expandWord(ctx, wordNode);
}
__name$1(expandWordlistString, "expandWordlistString");
function splitWordlist(ctx, wordlist) {
  const ifs = ctx.state.env.IFS ?? " 	\n";
  if (ifs.length === 0) {
    return [wordlist];
  }
  const ifsSet = new Set(ifs.split(""));
  const words = [];
  let currentWord = "";
  let i = 0;
  while (i < wordlist.length) {
    const char = wordlist[i];
    if (char === "\\" && i + 1 < wordlist.length) {
      const nextChar = wordlist[i + 1];
      currentWord += nextChar;
      i += 2;
    } else if (ifsSet.has(char)) {
      if (currentWord.length > 0) {
        words.push(currentWord);
        currentWord = "";
      }
      i++;
    } else {
      currentWord += char;
      i++;
    }
  }
  if (currentWord.length > 0) {
    words.push(currentWord);
  }
  return words;
}
__name$1(splitWordlist, "splitWordlist");
function restoreEnv(ctx, saved) {
  for (const [key2, value] of Object.entries(saved)) {
    if (value === void 0) {
      delete ctx.state.env[key2];
    } else {
      ctx.state.env[key2] = value;
    }
  }
}
__name$1(restoreEnv, "restoreEnv");
function getCompreplyValues(ctx) {
  const values = [];
  const lengthKey = "COMPREPLY__length";
  const arrayLength = ctx.state.env[lengthKey];
  if (arrayLength !== void 0) {
    const elements = getArrayElements(ctx, "COMPREPLY");
    for (const [, value] of elements) {
      values.push(value);
    }
  } else if (ctx.state.env.COMPREPLY !== void 0) {
    values.push(ctx.state.env.COMPREPLY);
  }
  return values;
}
__name$1(getCompreplyValues, "getCompreplyValues");
var VALID_OPTIONS = [
  "bashdefault",
  "default",
  "dirnames",
  "filenames",
  "noquote",
  "nosort",
  "nospace",
  "plusdirs"
];
function handleComplete(ctx, args) {
  if (!ctx.state.completionSpecs) {
    ctx.state.completionSpecs = /* @__PURE__ */ new Map();
  }
  let printMode = false;
  let removeMode = false;
  let isDefault = false;
  let wordlist;
  let funcName;
  let commandStr;
  const options = [];
  const actions = [];
  const commands = [];
  for (let i = 0; i < args.length; i++) {
    const arg = args[i];
    if (arg === "-p") {
      printMode = true;
    } else if (arg === "-r") {
      removeMode = true;
    } else if (arg === "-D") {
      isDefault = true;
    } else if (arg === "-W") {
      i++;
      if (i >= args.length) {
        return failure("complete: -W: option requires an argument\n", 2);
      }
      wordlist = args[i];
    } else if (arg === "-F") {
      i++;
      if (i >= args.length) {
        return failure("complete: -F: option requires an argument\n", 2);
      }
      funcName = args[i];
    } else if (arg === "-o") {
      i++;
      if (i >= args.length) {
        return failure("complete: -o: option requires an argument\n", 2);
      }
      const opt = args[i];
      if (!VALID_OPTIONS.includes(opt)) {
        return failure(`complete: ${opt}: invalid option name
`, 2);
      }
      options.push(opt);
    } else if (arg === "-A") {
      i++;
      if (i >= args.length) {
        return failure("complete: -A: option requires an argument\n", 2);
      }
      actions.push(args[i]);
    } else if (arg === "-C") {
      i++;
      if (i >= args.length) {
        return failure("complete: -C: option requires an argument\n", 2);
      }
      commandStr = args[i];
    } else if (arg === "-G") {
      i++;
      if (i >= args.length) {
        return failure("complete: -G: option requires an argument\n", 2);
      }
    } else if (arg === "-P") {
      i++;
      if (i >= args.length) {
        return failure("complete: -P: option requires an argument\n", 2);
      }
    } else if (arg === "-S") {
      i++;
      if (i >= args.length) {
        return failure("complete: -S: option requires an argument\n", 2);
      }
    } else if (arg === "-X") {
      i++;
      if (i >= args.length) {
        return failure("complete: -X: option requires an argument\n", 2);
      }
    } else if (arg === "--") {
      commands.push(...args.slice(i + 1));
      break;
    } else if (!arg.startsWith("-")) {
      commands.push(arg);
    }
  }
  if (removeMode) {
    if (commands.length === 0) {
      ctx.state.completionSpecs.clear();
      return success("");
    }
    for (const cmd of commands) {
      ctx.state.completionSpecs.delete(cmd);
    }
    return success("");
  }
  if (printMode) {
    if (commands.length === 0) {
      return printCompletionSpecs(ctx);
    }
    return printCompletionSpecs(ctx, commands);
  }
  if (args.length === 0 || commands.length === 0 && !wordlist && !funcName && !commandStr && options.length === 0 && actions.length === 0 && !isDefault) {
    return printCompletionSpecs(ctx);
  }
  if (funcName && commands.length === 0 && !isDefault) {
    return failure("complete: -F: option requires a command name\n", 2);
  }
  if (isDefault) {
    const spec = {
      isDefault: true
    };
    if (wordlist !== void 0)
      spec.wordlist = wordlist;
    if (funcName !== void 0)
      spec.function = funcName;
    if (commandStr !== void 0)
      spec.command = commandStr;
    if (options.length > 0)
      spec.options = options;
    if (actions.length > 0)
      spec.actions = actions;
    ctx.state.completionSpecs.set("__default__", spec);
    return success("");
  }
  for (const cmd of commands) {
    const spec = {};
    if (wordlist !== void 0)
      spec.wordlist = wordlist;
    if (funcName !== void 0)
      spec.function = funcName;
    if (commandStr !== void 0)
      spec.command = commandStr;
    if (options.length > 0)
      spec.options = options;
    if (actions.length > 0)
      spec.actions = actions;
    ctx.state.completionSpecs.set(cmd, spec);
  }
  return success("");
}
__name$1(handleComplete, "handleComplete");
function printCompletionSpecs(ctx, commands) {
  const specs = ctx.state.completionSpecs;
  if (!specs || specs.size === 0) {
    if (commands && commands.length > 0) {
      let stderr = "";
      for (const cmd of commands) {
        stderr += `complete: ${cmd}: no completion specification
`;
      }
      return result("", stderr, 1);
    }
    return success("");
  }
  const output = [];
  const targetCommands = commands || Array.from(specs.keys());
  for (const cmd of targetCommands) {
    if (cmd === "__default__")
      continue;
    const spec = specs.get(cmd);
    if (!spec) {
      if (commands) {
        return result(output.join("\n") + (output.length > 0 ? "\n" : ""), `complete: ${cmd}: no completion specification
`, 1);
      }
      continue;
    }
    let line = "complete";
    if (spec.options) {
      for (const opt of spec.options) {
        line += ` -o ${opt}`;
      }
    }
    if (spec.actions) {
      for (const action of spec.actions) {
        line += ` -A ${action}`;
      }
    }
    if (spec.wordlist !== void 0) {
      if (spec.wordlist.includes(" ") || spec.wordlist.includes("'")) {
        line += ` -W '${spec.wordlist}'`;
      } else {
        line += ` -W ${spec.wordlist}`;
      }
    }
    if (spec.function !== void 0) {
      line += ` -F ${spec.function}`;
    }
    if (spec.isDefault) {
      line += " -D";
    }
    line += ` ${cmd}`;
    output.push(line);
  }
  if (output.length === 0) {
    return success("");
  }
  return success(`${output.join("\n")}
`);
}
__name$1(printCompletionSpecs, "printCompletionSpecs");
var VALID_OPTIONS2 = [
  "bashdefault",
  "default",
  "dirnames",
  "filenames",
  "noquote",
  "nosort",
  "nospace",
  "plusdirs"
];
function handleCompopt(ctx, args) {
  if (!ctx.state.completionSpecs) {
    ctx.state.completionSpecs = /* @__PURE__ */ new Map();
  }
  let isDefault = false;
  let isEmptyLine = false;
  const enableOptions = [];
  const disableOptions = [];
  const commands = [];
  for (let i = 0; i < args.length; i++) {
    const arg = args[i];
    if (arg === "-D") {
      isDefault = true;
    } else if (arg === "-E") {
      isEmptyLine = true;
    } else if (arg === "-o") {
      i++;
      if (i >= args.length) {
        return failure("compopt: -o: option requires an argument\n", 2);
      }
      const opt = args[i];
      if (!VALID_OPTIONS2.includes(opt)) {
        return failure(`compopt: ${opt}: invalid option name
`, 2);
      }
      enableOptions.push(opt);
    } else if (arg === "+o") {
      i++;
      if (i >= args.length) {
        return failure("compopt: +o: option requires an argument\n", 2);
      }
      const opt = args[i];
      if (!VALID_OPTIONS2.includes(opt)) {
        return failure(`compopt: ${opt}: invalid option name
`, 2);
      }
      disableOptions.push(opt);
    } else if (arg === "--") {
      commands.push(...args.slice(i + 1));
      break;
    } else if (!arg.startsWith("-") && !arg.startsWith("+")) {
      commands.push(arg);
    }
  }
  if (isDefault) {
    const spec = ctx.state.completionSpecs.get("__default__") ?? {
      isDefault: true
    };
    const currentOptions = new Set(spec.options ?? []);
    for (const opt of enableOptions) {
      currentOptions.add(opt);
    }
    for (const opt of disableOptions) {
      currentOptions.delete(opt);
    }
    spec.options = currentOptions.size > 0 ? Array.from(currentOptions) : void 0;
    ctx.state.completionSpecs.set("__default__", spec);
    return success("");
  }
  if (isEmptyLine) {
    const spec = ctx.state.completionSpecs.get("__empty__") ?? {};
    const currentOptions = new Set(spec.options ?? []);
    for (const opt of enableOptions) {
      currentOptions.add(opt);
    }
    for (const opt of disableOptions) {
      currentOptions.delete(opt);
    }
    spec.options = currentOptions.size > 0 ? Array.from(currentOptions) : void 0;
    ctx.state.completionSpecs.set("__empty__", spec);
    return success("");
  }
  if (commands.length > 0) {
    for (const cmd of commands) {
      const spec = ctx.state.completionSpecs.get(cmd) ?? {};
      const currentOptions = new Set(spec.options ?? []);
      for (const opt of enableOptions) {
        currentOptions.add(opt);
      }
      for (const opt of disableOptions) {
        currentOptions.delete(opt);
      }
      spec.options = currentOptions.size > 0 ? Array.from(currentOptions) : void 0;
      ctx.state.completionSpecs.set(cmd, spec);
    }
    return success("");
  }
  return failure("compopt: not currently executing completion function\n", 1);
}
__name$1(handleCompopt, "handleCompopt");
function handleContinue(ctx, args) {
  if (ctx.state.loopDepth === 0) {
    if (ctx.state.parentHasLoopContext) {
      throw new SubshellExitError();
    }
    return OK;
  }
  if (args.length > 1) {
    throw new ExitError(1, "", "bash: continue: too many arguments\n");
  }
  let levels = 1;
  if (args.length > 0) {
    const n = Number.parseInt(args[0], 10);
    if (Number.isNaN(n) || n < 1) {
      throw new ExitError(1, "", `bash: continue: ${args[0]}: numeric argument required
`);
    }
    levels = n;
  }
  throw new ContinueError(levels);
}
__name$1(handleContinue, "handleContinue");
function expandTildesInValue(ctx, value) {
  const home = ctx.state.env.HOME || "/home/user";
  const parts = value.split(":");
  const expanded = parts.map((part) => {
    if (part === "~") {
      return home;
    }
    if (part === "~root") {
      return "/root";
    }
    if (part.startsWith("~/")) {
      return home + part.slice(1);
    }
    if (part.startsWith("~root/")) {
      return `/root${part.slice(5)}`;
    }
    return part;
  });
  return expanded.join(":");
}
__name$1(expandTildesInValue, "expandTildesInValue");
function needsDollarQuoting(value) {
  for (let i = 0; i < value.length; i++) {
    const code = value.charCodeAt(i);
    if (code < 32 || code === 127) {
      return true;
    }
  }
  return false;
}
__name$1(needsDollarQuoting, "needsDollarQuoting");
function dollarQuote(value) {
  let result2 = "$'";
  for (let i = 0; i < value.length; i++) {
    const char = value[i];
    const code = value.charCodeAt(i);
    if (code === 7) {
      result2 += "\\a";
    } else if (code === 8) {
      result2 += "\\b";
    } else if (code === 9) {
      result2 += "\\t";
    } else if (code === 10) {
      result2 += "\\n";
    } else if (code === 11) {
      result2 += "\\v";
    } else if (code === 12) {
      result2 += "\\f";
    } else if (code === 13) {
      result2 += "\\r";
    } else if (code === 27) {
      result2 += "\\e";
    } else if (code === 39) {
      result2 += "\\'";
    } else if (code === 92) {
      result2 += "\\\\";
    } else if (code < 32 || code === 127) {
      result2 += `\\${code.toString(8).padStart(3, "0")}`;
    } else {
      result2 += char;
    }
  }
  result2 += "'";
  return result2;
}
__name$1(dollarQuote, "dollarQuote");
function quoteValue(value) {
  if (needsDollarQuoting(value)) {
    return dollarQuote(value);
  }
  if (/^[a-zA-Z0-9_/.:\-@%+,=]*$/.test(value)) {
    return value;
  }
  return `'${value.replace(/'/g, "'\\''")}'`;
}
__name$1(quoteValue, "quoteValue");
function quoteArrayValue(value) {
  if (needsDollarQuoting(value)) {
    return dollarQuote(value);
  }
  const escaped = value.replace(/\\/g, "\\\\").replace(/"/g, '\\"');
  return `"${escaped}"`;
}
__name$1(quoteArrayValue, "quoteArrayValue");
function quoteDeclareValue(value) {
  if (needsDollarQuoting(value)) {
    return dollarQuote(value);
  }
  const escaped = value.replace(/\\/g, "\\\\").replace(/"/g, '\\"');
  return `"${escaped}"`;
}
__name$1(quoteDeclareValue, "quoteDeclareValue");
function getVariableFlags(ctx, name) {
  let flags = "";
  if (ctx.state.integerVars?.has(name)) {
    flags += "i";
  }
  if (ctx.state.lowercaseVars?.has(name)) {
    flags += "l";
  }
  if (isNameref(ctx, name)) {
    flags += "n";
  }
  if (ctx.state.readonlyVars?.has(name)) {
    flags += "r";
  }
  if (ctx.state.uppercaseVars?.has(name)) {
    flags += "u";
  }
  if (ctx.state.exportedVars?.has(name)) {
    flags += "x";
  }
  return flags === "" ? "--" : `-${flags}`;
}
__name$1(getVariableFlags, "getVariableFlags");
function formatAssocValue(value) {
  if (value === "") {
    return "''";
  }
  if (/[\s'\\]/.test(value)) {
    const escaped = value.replace(/'/g, "'\\''");
    return `'${escaped}'`;
  }
  return value;
}
__name$1(formatAssocValue, "formatAssocValue");
function printSpecificVariables(ctx, names) {
  let stdout = "";
  let stderr = "";
  let anyNotFound = false;
  for (const name of names) {
    const flags = getVariableFlags(ctx, name);
    const isAssoc = ctx.state.associativeArrays?.has(name);
    if (isAssoc) {
      const keys = getAssocArrayKeys(ctx, name);
      if (keys.length === 0) {
        stdout += `declare -A ${name}=()
`;
      } else {
        const elements = keys.map((key2) => {
          const value2 = ctx.state.env[`${name}_${key2}`] ?? "";
          const formattedValue = formatAssocValue(value2);
          return `['${key2}']=${formattedValue}`;
        });
        stdout += `declare -A ${name}=(${elements.join(" ")})
`;
      }
      continue;
    }
    const arrayIndices = getArrayIndices(ctx, name);
    if (arrayIndices.length > 0) {
      const elements = arrayIndices.map((index) => {
        const value2 = ctx.state.env[`${name}_${index}`] ?? "";
        return `[${index}]=${quoteArrayValue(value2)}`;
      });
      stdout += `declare -a ${name}=(${elements.join(" ")})
`;
      continue;
    }
    if (ctx.state.env[`${name}__length`] !== void 0) {
      stdout += `declare -a ${name}=()
`;
      continue;
    }
    const value = ctx.state.env[name];
    if (value !== void 0) {
      stdout += `declare ${flags} ${name}=${quoteDeclareValue(value)}
`;
    } else {
      const isDeclared = ctx.state.declaredVars?.has(name);
      const isLocalVar = ctx.state.localVarDepth?.has(name);
      if (isDeclared || isLocalVar) {
        stdout += `declare ${flags} ${name}
`;
      } else {
        stderr += `bash: declare: ${name}: not found
`;
        anyNotFound = true;
      }
    }
  }
  return result(stdout, stderr, anyNotFound ? 1 : 0);
}
__name$1(printSpecificVariables, "printSpecificVariables");
function printAllVariables(ctx, filters) {
  const { filterExport, filterReadonly, filterNameref, filterIndexedArray, filterAssocArray } = filters;
  const hasFilter = filterExport || filterReadonly || filterNameref || filterIndexedArray || filterAssocArray;
  let stdout = "";
  const varNames = /* @__PURE__ */ new Set();
  for (const key2 of Object.keys(ctx.state.env)) {
    if (key2.startsWith("BASH_"))
      continue;
    if (key2.endsWith("__length")) {
      const baseName = key2.slice(0, -8);
      varNames.add(baseName);
      continue;
    }
    const underscoreIdx = key2.lastIndexOf("_");
    if (underscoreIdx > 0) {
      const baseName = key2.slice(0, underscoreIdx);
      const suffix = key2.slice(underscoreIdx + 1);
      if (/^\d+$/.test(suffix) || ctx.state.associativeArrays?.has(baseName)) {
        varNames.add(baseName);
        continue;
      }
    }
    varNames.add(key2);
  }
  if (ctx.state.localVarDepth) {
    for (const name of ctx.state.localVarDepth.keys()) {
      varNames.add(name);
    }
  }
  if (ctx.state.associativeArrays) {
    for (const name of ctx.state.associativeArrays) {
      varNames.add(name);
    }
  }
  const sortedNames = Array.from(varNames).sort();
  for (const name of sortedNames) {
    const flags = getVariableFlags(ctx, name);
    const isAssoc = ctx.state.associativeArrays?.has(name);
    const arrayIndices = getArrayIndices(ctx, name);
    const isIndexedArray = !isAssoc && (arrayIndices.length > 0 || ctx.state.env[`${name}__length`] !== void 0);
    if (hasFilter) {
      if (filterAssocArray && !isAssoc)
        continue;
      if (filterIndexedArray && !isIndexedArray)
        continue;
      if (filterExport && !ctx.state.exportedVars?.has(name))
        continue;
      if (filterReadonly && !ctx.state.readonlyVars?.has(name))
        continue;
      if (filterNameref && !isNameref(ctx, name))
        continue;
    }
    if (isAssoc) {
      const keys = getAssocArrayKeys(ctx, name);
      if (keys.length === 0) {
        stdout += `declare -A ${name}=()
`;
      } else {
        const elements = keys.map((key2) => {
          const value2 = ctx.state.env[`${name}_${key2}`] ?? "";
          const formattedValue = formatAssocValue(value2);
          return `['${key2}']=${formattedValue}`;
        });
        stdout += `declare -A ${name}=(${elements.join(" ")})
`;
      }
      continue;
    }
    if (arrayIndices.length > 0) {
      const elements = arrayIndices.map((index) => {
        const value2 = ctx.state.env[`${name}_${index}`] ?? "";
        return `[${index}]=${quoteArrayValue(value2)}`;
      });
      stdout += `declare -a ${name}=(${elements.join(" ")})
`;
      continue;
    }
    if (ctx.state.env[`${name}__length`] !== void 0) {
      stdout += `declare -a ${name}=()
`;
      continue;
    }
    const value = ctx.state.env[name];
    if (value !== void 0) {
      stdout += `declare ${flags} ${name}=${quoteDeclareValue(value)}
`;
    }
  }
  return success(stdout);
}
__name$1(printAllVariables, "printAllVariables");
function listAssociativeArrays(ctx) {
  let stdout = "";
  const assocNames = Array.from(ctx.state.associativeArrays ?? []).sort();
  for (const name of assocNames) {
    const keys = getAssocArrayKeys(ctx, name);
    if (keys.length === 0) {
      stdout += `declare -A ${name}=()
`;
    } else {
      const elements = keys.map((key2) => {
        const value = ctx.state.env[`${name}_${key2}`] ?? "";
        const formattedValue = formatAssocValue(value);
        return `['${key2}']=${formattedValue}`;
      });
      stdout += `declare -A ${name}=(${elements.join(" ")})
`;
    }
  }
  return success(stdout);
}
__name$1(listAssociativeArrays, "listAssociativeArrays");
function listIndexedArrays(ctx) {
  let stdout = "";
  const arrayNames = /* @__PURE__ */ new Set();
  for (const key2 of Object.keys(ctx.state.env)) {
    if (key2.startsWith("BASH_"))
      continue;
    if (key2.endsWith("__length")) {
      const baseName = key2.slice(0, -8);
      if (!ctx.state.associativeArrays?.has(baseName)) {
        arrayNames.add(baseName);
      }
      continue;
    }
    const lastUnderscore = key2.lastIndexOf("_");
    if (lastUnderscore > 0) {
      const baseName = key2.slice(0, lastUnderscore);
      const suffix = key2.slice(lastUnderscore + 1);
      if (/^\d+$/.test(suffix)) {
        if (!ctx.state.associativeArrays?.has(baseName)) {
          arrayNames.add(baseName);
        }
      }
    }
  }
  const sortedNames = Array.from(arrayNames).sort();
  for (const name of sortedNames) {
    const indices = getArrayIndices(ctx, name);
    if (indices.length === 0) {
      stdout += `declare -a ${name}=()
`;
    } else {
      const elements = indices.map((index) => {
        const value = ctx.state.env[`${name}_${index}`] ?? "";
        return `[${index}]=${quoteArrayValue(value)}`;
      });
      stdout += `declare -a ${name}=(${elements.join(" ")})
`;
    }
  }
  return success(stdout);
}
__name$1(listIndexedArrays, "listIndexedArrays");
function listAllVariables(ctx) {
  let stdout = "";
  const varNames = /* @__PURE__ */ new Set();
  for (const key2 of Object.keys(ctx.state.env)) {
    if (key2.startsWith("BASH_"))
      continue;
    if (key2.endsWith("__length")) {
      const baseName = key2.slice(0, -8);
      varNames.add(baseName);
      continue;
    }
    const underscoreIdx = key2.lastIndexOf("_");
    if (underscoreIdx > 0) {
      const baseName = key2.slice(0, underscoreIdx);
      const suffix = key2.slice(underscoreIdx + 1);
      if (/^\d+$/.test(suffix) || ctx.state.associativeArrays?.has(baseName)) {
        varNames.add(baseName);
        continue;
      }
    }
    varNames.add(key2);
  }
  const sortedNames = Array.from(varNames).sort();
  for (const name of sortedNames) {
    const isAssoc = ctx.state.associativeArrays?.has(name);
    if (isAssoc) {
      continue;
    }
    const arrayIndices = getArrayIndices(ctx, name);
    if (arrayIndices.length > 0 || ctx.state.env[`${name}__length`] !== void 0) {
      continue;
    }
    const value = ctx.state.env[name];
    if (value !== void 0) {
      stdout += `${name}=${quoteValue(value)}
`;
    }
  }
  return success(stdout);
}
__name$1(listAllVariables, "listAllVariables");
function markInteger(ctx, name) {
  ctx.state.integerVars ??= /* @__PURE__ */ new Set();
  ctx.state.integerVars.add(name);
}
__name$1(markInteger, "markInteger");
function isInteger(ctx, name) {
  return ctx.state.integerVars?.has(name) ?? false;
}
__name$1(isInteger, "isInteger");
function markLowercase(ctx, name) {
  ctx.state.lowercaseVars ??= /* @__PURE__ */ new Set();
  ctx.state.lowercaseVars.add(name);
  ctx.state.uppercaseVars?.delete(name);
}
__name$1(markLowercase, "markLowercase");
function isLowercase(ctx, name) {
  return ctx.state.lowercaseVars?.has(name) ?? false;
}
__name$1(isLowercase, "isLowercase");
function markUppercase(ctx, name) {
  ctx.state.uppercaseVars ??= /* @__PURE__ */ new Set();
  ctx.state.uppercaseVars.add(name);
  ctx.state.lowercaseVars?.delete(name);
}
__name$1(markUppercase, "markUppercase");
function isUppercase(ctx, name) {
  return ctx.state.uppercaseVars?.has(name) ?? false;
}
__name$1(isUppercase, "isUppercase");
function applyCaseTransform(ctx, name, value) {
  if (isLowercase(ctx, name)) {
    return value.toLowerCase();
  }
  if (isUppercase(ctx, name)) {
    return value.toUpperCase();
  }
  return value;
}
__name$1(applyCaseTransform, "applyCaseTransform");
async function evaluateIntegerValue(ctx, value) {
  try {
    const parser = new Parser();
    const arithAst = parseArithmeticExpression(parser, value);
    const result2 = await evaluateArithmetic(ctx, arithAst.expression);
    return String(result2);
  } catch {
    return "0";
  }
}
__name$1(evaluateIntegerValue, "evaluateIntegerValue");
function parseArrayAssignment(arg) {
  const nameMatch = arg.match(/^[a-zA-Z_][a-zA-Z0-9_]*/);
  if (!nameMatch)
    return null;
  const name = nameMatch[0];
  let pos = name.length;
  if (arg[pos] !== "[")
    return null;
  let depth = 0;
  const subscriptStart = pos + 1;
  for (; pos < arg.length; pos++) {
    if (arg[pos] === "[")
      depth++;
    else if (arg[pos] === "]") {
      depth--;
      if (depth === 0)
        break;
    }
  }
  if (depth !== 0)
    return null;
  const indexExpr = arg.slice(subscriptStart, pos);
  pos++;
  if (arg[pos] !== "=")
    return null;
  pos++;
  const value = arg.slice(pos);
  return { name, indexExpr, value };
}
__name$1(parseArrayAssignment, "parseArrayAssignment");
async function handleDeclare(ctx, args) {
  let declareArray = false;
  let declareAssoc = false;
  let declareReadonly = false;
  let declareExport = false;
  let printMode = false;
  let declareNameref = false;
  let removeNameref = false;
  let removeArray = false;
  let removeExport = false;
  let declareInteger = false;
  let declareLowercase = false;
  let declareUppercase = false;
  let functionMode = false;
  let functionNamesOnly = false;
  let declareGlobal = false;
  const processedArgs = [];
  for (let i = 0; i < args.length; i++) {
    const arg = args[i];
    if (arg === "-a") {
      declareArray = true;
    } else if (arg === "-A") {
      declareAssoc = true;
    } else if (arg === "-r") {
      declareReadonly = true;
    } else if (arg === "-x") {
      declareExport = true;
    } else if (arg === "-p") {
      printMode = true;
    } else if (arg === "-n") {
      declareNameref = true;
    } else if (arg === "+n") {
      removeNameref = true;
    } else if (arg === "+a") {
      removeArray = true;
    } else if (arg === "+x") {
      removeExport = true;
    } else if (arg === "--") {
      processedArgs.push(...args.slice(i + 1));
      break;
    } else if (arg.startsWith("+")) {
      for (const flag of arg.slice(1)) {
        if (flag === "n")
          removeNameref = true;
        else if (flag === "a")
          removeArray = true;
        else if (flag === "x")
          removeExport = true;
        else if (flag === "r") ;
        else if (flag === "i") ;
        else if (flag === "f" || flag === "F") ;
        else {
          return result("", `bash: typeset: +${flag}: invalid option
`, 2);
        }
      }
    } else if (arg === "-i") {
      declareInteger = true;
    } else if (arg === "-l") {
      declareLowercase = true;
    } else if (arg === "-u") {
      declareUppercase = true;
    } else if (arg === "-f") {
      functionMode = true;
    } else if (arg === "-F") {
      functionNamesOnly = true;
    } else if (arg === "-g") {
      declareGlobal = true;
    } else if (arg.startsWith("-")) {
      for (const flag of arg.slice(1)) {
        if (flag === "a")
          declareArray = true;
        else if (flag === "A")
          declareAssoc = true;
        else if (flag === "r")
          declareReadonly = true;
        else if (flag === "x")
          declareExport = true;
        else if (flag === "p")
          printMode = true;
        else if (flag === "n")
          declareNameref = true;
        else if (flag === "i")
          declareInteger = true;
        else if (flag === "l")
          declareLowercase = true;
        else if (flag === "u")
          declareUppercase = true;
        else if (flag === "f")
          functionMode = true;
        else if (flag === "F")
          functionNamesOnly = true;
        else if (flag === "g")
          declareGlobal = true;
        else {
          return result("", `bash: typeset: -${flag}: invalid option
`, 2);
        }
      }
    } else {
      processedArgs.push(arg);
    }
  }
  const isInsideFunction = ctx.state.localScopes.length > 0;
  const createLocal = isInsideFunction && !declareGlobal;
  const saveToLocalScope = /* @__PURE__ */ __name$1((name) => {
    if (!createLocal)
      return;
    const currentScope = ctx.state.localScopes[ctx.state.localScopes.length - 1];
    if (!currentScope.has(name)) {
      currentScope.set(name, ctx.state.env[name]);
    }
  }, "saveToLocalScope");
  const saveArrayToLocalScope = /* @__PURE__ */ __name$1((name) => {
    if (!createLocal)
      return;
    const currentScope = ctx.state.localScopes[ctx.state.localScopes.length - 1];
    if (!currentScope.has(name)) {
      currentScope.set(name, ctx.state.env[name]);
    }
    const prefix = `${name}_`;
    for (const key2 of Object.keys(ctx.state.env)) {
      if (key2.startsWith(prefix) && !key2.includes("__")) {
        if (!currentScope.has(key2)) {
          currentScope.set(key2, ctx.state.env[key2]);
        }
      }
    }
    const lengthKey = `${name}__length`;
    if (ctx.state.env[lengthKey] !== void 0 && !currentScope.has(lengthKey)) {
      currentScope.set(lengthKey, ctx.state.env[lengthKey]);
    }
  }, "saveArrayToLocalScope");
  const markAsLocalIfNeeded = /* @__PURE__ */ __name$1((name) => {
    if (createLocal) {
      markLocalVarDepth(ctx, name);
    }
  }, "markAsLocalIfNeeded");
  if (functionNamesOnly) {
    if (processedArgs.length === 0) {
      const funcNames = Array.from(ctx.state.functions.keys()).sort();
      let stdout2 = "";
      for (const name of funcNames) {
        stdout2 += `declare -f ${name}
`;
      }
      return success(stdout2);
    }
    let allExist = true;
    let stdout = "";
    for (const name of processedArgs) {
      if (ctx.state.functions.has(name)) {
        stdout += `${name}
`;
      } else {
        allExist = false;
      }
    }
    return result(stdout, "", allExist ? 0 : 1);
  }
  if (functionMode) {
    if (processedArgs.length === 0) {
      let stdout = "";
      const funcNames = Array.from(ctx.state.functions.keys()).sort();
      for (const name of funcNames) {
        stdout += `${name} ()
{
    # function body
}
`;
      }
      return success(stdout);
    }
    let allExist = true;
    for (const name of processedArgs) {
      if (!ctx.state.functions.has(name)) {
        allExist = false;
      }
    }
    return result("", "", allExist ? 0 : 1);
  }
  if (printMode && processedArgs.length > 0) {
    return printSpecificVariables(ctx, processedArgs);
  }
  if (printMode && processedArgs.length === 0) {
    return printAllVariables(ctx, {
      filterExport: declareExport,
      filterReadonly: declareReadonly,
      filterNameref: declareNameref,
      filterIndexedArray: declareArray,
      filterAssocArray: declareAssoc
    });
  }
  if (processedArgs.length === 0 && declareAssoc && !printMode) {
    return listAssociativeArrays(ctx);
  }
  if (processedArgs.length === 0 && declareArray && !printMode) {
    return listIndexedArrays(ctx);
  }
  if (processedArgs.length === 0 && !printMode) {
    return listAllVariables(ctx);
  }
  let stderr = "";
  let exitCode = 0;
  for (const arg of processedArgs) {
    const arrayMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)=\((.*)\)$/s);
    if (arrayMatch && !removeArray) {
      const name = arrayMatch[1];
      const content = arrayMatch[2];
      if (declareAssoc) {
        const existingIndices = getArrayIndices(ctx, name);
        if (existingIndices.length > 0) {
          stderr += `bash: declare: ${name}: cannot convert indexed to associative array
`;
          exitCode = 1;
          continue;
        }
      }
      if (declareArray || !declareAssoc && !declareArray) {
        if (ctx.state.associativeArrays?.has(name)) {
          stderr += `bash: declare: ${name}: cannot convert associative to indexed array
`;
          exitCode = 1;
          continue;
        }
      }
      saveArrayToLocalScope(name);
      if (declareAssoc) {
        ctx.state.associativeArrays ??= /* @__PURE__ */ new Set();
        ctx.state.associativeArrays.add(name);
      }
      clearArray(ctx, name);
      delete ctx.state.env[name];
      delete ctx.state.env[`${name}__length`];
      if (declareAssoc && content.includes("[")) {
        const entries = parseAssocArrayLiteral(content);
        for (const [key2, rawValue] of entries) {
          const value = expandTildesInValue(ctx, rawValue);
          ctx.state.env[`${name}_${key2}`] = value;
        }
      } else if (declareAssoc) {
        const elements = parseArrayElements(content);
        for (let i = 0; i < elements.length; i += 2) {
          const key2 = elements[i];
          const value = i + 1 < elements.length ? expandTildesInValue(ctx, elements[i + 1]) : "";
          ctx.state.env[`${name}_${key2}`] = value;
        }
      } else {
        const elements = parseArrayElements(content);
        const hasKeyedElements = elements.some((el) => /^\[[^\]]+\]=/.test(el));
        if (hasKeyedElements) {
          let currentIndex = 0;
          for (const element of elements) {
            const keyedMatch = element.match(/^\[([^\]]+)\]=(.*)$/);
            if (keyedMatch) {
              const indexExpr = keyedMatch[1];
              const rawValue = keyedMatch[2];
              const value = expandTildesInValue(ctx, rawValue);
              let index;
              if (/^-?\d+$/.test(indexExpr)) {
                index = Number.parseInt(indexExpr, 10);
              } else {
                try {
                  const parser = new Parser();
                  const arithAst = parseArithmeticExpression(parser, indexExpr);
                  index = await evaluateArithmetic(ctx, arithAst.expression);
                } catch {
                  index = 0;
                }
              }
              ctx.state.env[`${name}_${index}`] = value;
              currentIndex = index + 1;
            } else {
              const value = expandTildesInValue(ctx, element);
              ctx.state.env[`${name}_${currentIndex}`] = value;
              currentIndex++;
            }
          }
        } else {
          for (let i = 0; i < elements.length; i++) {
            ctx.state.env[`${name}_${i}`] = elements[i];
          }
          ctx.state.env[`${name}__length`] = String(elements.length);
        }
      }
      markAsLocalIfNeeded(name);
      if (declareReadonly) {
        markReadonly(ctx, name);
      }
      if (declareExport) {
        markExported(ctx, name);
      }
      continue;
    }
    if (removeNameref) {
      const name = arg.includes("=") ? arg.slice(0, arg.indexOf("=")) : arg;
      unmarkNameref(ctx, name);
      if (!arg.includes("=")) {
        continue;
      }
    }
    if (removeExport) {
      const name = arg.includes("=") ? arg.slice(0, arg.indexOf("=")) : arg;
      unmarkExported(ctx, name);
      if (!arg.includes("=")) {
        continue;
      }
    }
    const arrayAssignMatch = parseArrayAssignment(arg);
    if (arrayAssignMatch) {
      const { name, indexExpr, value } = arrayAssignMatch;
      checkReadonlyError(ctx, name);
      saveArrayToLocalScope(name);
      let index;
      try {
        const parser = new Parser();
        const arithAst = parseArithmeticExpression(parser, indexExpr);
        index = await evaluateArithmetic(ctx, arithAst.expression);
      } catch {
        const num = parseInt(indexExpr, 10);
        index = Number.isNaN(num) ? 0 : num;
      }
      ctx.state.env[`${name}_${index}`] = value;
      const currentLength = parseInt(ctx.state.env[`${name}__length`] ?? "0", 10);
      if (index >= currentLength) {
        ctx.state.env[`${name}__length`] = String(index + 1);
      }
      markAsLocalIfNeeded(name);
      if (declareReadonly) {
        markReadonly(ctx, name);
      }
      if (declareExport) {
        markExported(ctx, name);
      }
      continue;
    }
    const arrayAppendMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\+=\((.*)\)$/s);
    if (arrayAppendMatch && !removeArray) {
      const name = arrayAppendMatch[1];
      const content = arrayAppendMatch[2];
      checkReadonlyError(ctx, name);
      saveArrayToLocalScope(name);
      const newElements = parseArrayElements(content);
      if (ctx.state.associativeArrays?.has(name)) {
        const entries = parseAssocArrayLiteral(content);
        for (const [key2, rawValue] of entries) {
          const value = expandTildesInValue(ctx, rawValue);
          ctx.state.env[`${name}_${key2}`] = value;
        }
      } else {
        const existingIndices = getArrayIndices(ctx, name);
        let startIndex = 0;
        if (existingIndices.length === 0 && ctx.state.env[name] !== void 0) {
          const scalarValue = ctx.state.env[name];
          ctx.state.env[`${name}_0`] = scalarValue;
          delete ctx.state.env[name];
          startIndex = 1;
        } else if (existingIndices.length > 0) {
          startIndex = Math.max(...existingIndices) + 1;
        }
        for (let i = 0; i < newElements.length; i++) {
          ctx.state.env[`${name}_${startIndex + i}`] = expandTildesInValue(ctx, newElements[i]);
        }
        const newLength = startIndex + newElements.length;
        ctx.state.env[`${name}__length`] = String(newLength);
      }
      markAsLocalIfNeeded(name);
      if (declareReadonly) {
        markReadonly(ctx, name);
      }
      if (declareExport) {
        markExported(ctx, name);
      }
      continue;
    }
    const appendMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\+=(.*)$/);
    if (appendMatch) {
      const name = appendMatch[1];
      let appendValue = expandTildesInValue(ctx, appendMatch[2]);
      checkReadonlyError(ctx, name);
      saveToLocalScope(name);
      if (declareInteger) {
        markInteger(ctx, name);
      }
      if (declareLowercase) {
        markLowercase(ctx, name);
      }
      if (declareUppercase) {
        markUppercase(ctx, name);
      }
      const existingIndices = getArrayIndices(ctx, name);
      const isArray2 = existingIndices.length > 0 || ctx.state.associativeArrays?.has(name);
      if (isInteger(ctx, name)) {
        const existing = ctx.state.env[name] ?? "0";
        const existingNum = parseInt(existing, 10) || 0;
        const appendNum = parseInt(await evaluateIntegerValue(ctx, appendValue), 10) || 0;
        appendValue = String(existingNum + appendNum);
        ctx.state.env[name] = appendValue;
      } else if (isArray2) {
        appendValue = applyCaseTransform(ctx, name, appendValue);
        const element0Key = `${name}_0`;
        const existing = ctx.state.env[element0Key] ?? "";
        ctx.state.env[element0Key] = existing + appendValue;
      } else {
        appendValue = applyCaseTransform(ctx, name, appendValue);
        const existing = ctx.state.env[name] ?? "";
        ctx.state.env[name] = existing + appendValue;
      }
      markAsLocalIfNeeded(name);
      if (declareReadonly) {
        markReadonly(ctx, name);
      }
      if (declareExport) {
        markExported(ctx, name);
      }
      if (ctx.state.options.allexport && !removeExport) {
        ctx.state.exportedVars = ctx.state.exportedVars || /* @__PURE__ */ new Set();
        ctx.state.exportedVars.add(name);
      }
      continue;
    }
    if (arg.includes("=")) {
      const eqIdx = arg.indexOf("=");
      const name = arg.slice(0, eqIdx);
      let value = arg.slice(eqIdx + 1);
      if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(name)) {
        stderr += `bash: typeset: \`${name}': not a valid identifier
`;
        exitCode = 1;
        continue;
      }
      checkReadonlyError(ctx, name);
      saveToLocalScope(name);
      if (declareNameref) {
        if (value !== "" && !/^[a-zA-Z_][a-zA-Z0-9_]*(\[.+\])?$/.test(value)) {
          stderr += `bash: declare: \`${value}': invalid variable name for name reference
`;
          exitCode = 1;
          continue;
        }
        ctx.state.env[name] = value;
        markNameref(ctx, name);
        if (value !== "" && targetExists(ctx, value)) {
          markNamerefBound(ctx, name);
        }
        markAsLocalIfNeeded(name);
        if (declareReadonly) {
          markReadonly(ctx, name);
        }
        if (declareExport) {
          markExported(ctx, name);
        }
        continue;
      }
      if (declareInteger) {
        markInteger(ctx, name);
      }
      if (declareLowercase) {
        markLowercase(ctx, name);
      }
      if (declareUppercase) {
        markUppercase(ctx, name);
      }
      if (isInteger(ctx, name)) {
        value = await evaluateIntegerValue(ctx, value);
      }
      value = applyCaseTransform(ctx, name, value);
      if (isNameref(ctx, name)) {
        const resolved = resolveNameref(ctx, name);
        if (resolved && resolved !== name) {
          ctx.state.env[resolved] = value;
        } else {
          ctx.state.env[name] = value;
        }
      } else {
        ctx.state.env[name] = value;
      }
      markAsLocalIfNeeded(name);
      if (declareReadonly) {
        markReadonly(ctx, name);
      }
      if (declareExport) {
        markExported(ctx, name);
      }
      if (ctx.state.options.allexport && !removeExport) {
        ctx.state.exportedVars = ctx.state.exportedVars || /* @__PURE__ */ new Set();
        ctx.state.exportedVars.add(name);
      }
    } else {
      const name = arg;
      if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(name)) {
        stderr += `bash: typeset: \`${name}': not a valid identifier
`;
        exitCode = 1;
        continue;
      }
      if (declareArray || declareAssoc) {
        saveArrayToLocalScope(name);
      } else {
        saveToLocalScope(name);
      }
      if (declareNameref) {
        markNameref(ctx, name);
        const existingValue = ctx.state.env[name];
        if (existingValue !== void 0 && existingValue !== "" && !/^[a-zA-Z_][a-zA-Z0-9_]*(\[.+\])?$/.test(existingValue)) {
          markNamerefInvalid(ctx, name);
        } else if (existingValue && targetExists(ctx, existingValue)) {
          markNamerefBound(ctx, name);
        }
        markAsLocalIfNeeded(name);
        if (declareReadonly) {
          markReadonly(ctx, name);
        }
        if (declareExport) {
          markExported(ctx, name);
        }
        continue;
      }
      if (declareInteger) {
        markInteger(ctx, name);
      }
      if (declareLowercase) {
        markLowercase(ctx, name);
      }
      if (declareUppercase) {
        markUppercase(ctx, name);
      }
      if (declareAssoc) {
        const existingIndices = getArrayIndices(ctx, name);
        if (existingIndices.length > 0) {
          stderr += `bash: declare: ${name}: cannot convert indexed to associative array
`;
          exitCode = 1;
          continue;
        }
        ctx.state.associativeArrays ??= /* @__PURE__ */ new Set();
        ctx.state.associativeArrays.add(name);
      }
      const hasArrayElements = Object.keys(ctx.state.env).some((key2) => key2.startsWith(`${name}_`) && !key2.startsWith(`${name}__length`));
      if (!(name in ctx.state.env) && !hasArrayElements) {
        if (declareArray || declareAssoc) {
          ctx.state.env[`${name}__length`] = "0";
        } else {
          ctx.state.declaredVars ??= /* @__PURE__ */ new Set();
          ctx.state.declaredVars.add(name);
        }
      }
      markAsLocalIfNeeded(name);
      if (declareReadonly) {
        markReadonly(ctx, name);
      }
      if (declareExport) {
        markExported(ctx, name);
      }
    }
  }
  return result("", stderr, exitCode);
}
__name$1(handleDeclare, "handleDeclare");
async function handleReadonly(ctx, args) {
  const processedArgs = [];
  for (let i = 0; i < args.length; i++) {
    const arg = args[i];
    if (arg === "-a") ;
    else if (arg === "-A") ;
    else if (arg === "-p") ;
    else if (arg === "--") {
      processedArgs.push(...args.slice(i + 1));
      break;
    } else if (!arg.startsWith("-")) {
      processedArgs.push(arg);
    }
  }
  if (processedArgs.length === 0) {
    let stdout = "";
    const readonlyNames = Array.from(ctx.state.readonlyVars || []).sort();
    for (const name of readonlyNames) {
      const value = ctx.state.env[name];
      if (value !== void 0) {
        const escapedValue = value.replace(/\\/g, "\\\\").replace(/"/g, '\\"');
        stdout += `declare -r ${name}="${escapedValue}"
`;
      }
    }
    return success(stdout);
  }
  for (const arg of processedArgs) {
    const arrayAppendMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\+=\((.*)\)$/s);
    if (arrayAppendMatch) {
      const name = arrayAppendMatch[1];
      const content = arrayAppendMatch[2];
      checkReadonlyError(ctx, name);
      const newElements = parseArrayElements(content);
      if (ctx.state.associativeArrays?.has(name)) {
        const entries = parseAssocArrayLiteral(content);
        for (const [key2, rawValue] of entries) {
          const value = expandTildesInValue(ctx, rawValue);
          ctx.state.env[`${name}_${key2}`] = value;
        }
      } else {
        const existingIndices = getArrayIndices(ctx, name);
        let startIndex = 0;
        if (existingIndices.length === 0 && ctx.state.env[name] !== void 0) {
          const scalarValue = ctx.state.env[name];
          ctx.state.env[`${name}_0`] = scalarValue;
          delete ctx.state.env[name];
          startIndex = 1;
        } else if (existingIndices.length > 0) {
          startIndex = Math.max(...existingIndices) + 1;
        }
        for (let i = 0; i < newElements.length; i++) {
          ctx.state.env[`${name}_${startIndex + i}`] = expandTildesInValue(ctx, newElements[i]);
        }
        const newLength = startIndex + newElements.length;
        ctx.state.env[`${name}__length`] = String(newLength);
      }
      markReadonly(ctx, name);
      continue;
    }
    const appendMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\+=(.*)$/);
    if (appendMatch) {
      const name = appendMatch[1];
      const appendValue = expandTildesInValue(ctx, appendMatch[2]);
      checkReadonlyError(ctx, name);
      const existing = ctx.state.env[name] ?? "";
      ctx.state.env[name] = existing + appendValue;
      markReadonly(ctx, name);
      continue;
    }
    const assignment = parseAssignment(arg);
    if (assignment.value === void 0 && !assignment.isArray) {
      markReadonly(ctx, assignment.name);
      continue;
    }
    const error = await setVariable(ctx, assignment, { makeReadonly: true });
    if (error) {
      return error;
    }
  }
  return OK;
}
__name$1(handleReadonly, "handleReadonly");
function getStack(ctx) {
  ctx.state.directoryStack ??= [];
  return ctx.state.directoryStack;
}
__name$1(getStack, "getStack");
function formatPath(path2, home) {
  if (home && path2 === home) {
    return "~";
  }
  if (home && path2.startsWith(`${home}/`)) {
    return `~${path2.slice(home.length)}`;
  }
  return path2;
}
__name$1(formatPath, "formatPath");
function normalizePath(path2) {
  const parts = path2.split("/").filter((p) => p && p !== ".");
  const result2 = [];
  for (const part of parts) {
    if (part === "..") {
      result2.pop();
    } else {
      result2.push(part);
    }
  }
  return `/${result2.join("/")}`;
}
__name$1(normalizePath, "normalizePath");
async function handlePushd(ctx, args) {
  const stack = getStack(ctx);
  let targetDir;
  for (let i = 0; i < args.length; i++) {
    const arg = args[i];
    if (arg === "--") {
      if (i + 1 < args.length) {
        if (targetDir !== void 0) {
          return failure("bash: pushd: too many arguments\n", 2);
        }
        targetDir = args[i + 1];
        i++;
      }
    } else if (arg.startsWith("-") && arg !== "-") {
      return failure(`bash: pushd: ${arg}: invalid option
`, 2);
    } else {
      if (targetDir !== void 0) {
        return failure("bash: pushd: too many arguments\n", 2);
      }
      targetDir = arg;
    }
  }
  if (targetDir === void 0) {
    if (stack.length < 2) {
      return failure("bash: pushd: no other directory\n", 1);
    }
    const top = stack[0];
    stack[0] = stack[1];
    stack[1] = top;
    targetDir = stack[0];
  }
  let resolvedDir;
  if (targetDir.startsWith("/")) {
    resolvedDir = targetDir;
  } else if (targetDir === "..") {
    const parts = ctx.state.cwd.split("/").filter((p) => p);
    parts.pop();
    resolvedDir = `/${parts.join("/")}`;
  } else if (targetDir === ".") {
    resolvedDir = ctx.state.cwd;
  } else if (targetDir.startsWith("~")) {
    const home2 = ctx.state.env.HOME || "/";
    resolvedDir = home2 + targetDir.slice(1);
  } else {
    resolvedDir = `${ctx.state.cwd}/${targetDir}`;
  }
  resolvedDir = normalizePath(resolvedDir);
  try {
    const stat = await ctx.fs.stat(resolvedDir);
    if (!stat.isDirectory) {
      return failure(`bash: pushd: ${targetDir}: Not a directory
`, 1);
    }
  } catch {
    return failure(`bash: pushd: ${targetDir}: No such file or directory
`, 1);
  }
  stack.unshift(ctx.state.cwd);
  ctx.state.previousDir = ctx.state.cwd;
  ctx.state.cwd = resolvedDir;
  ctx.state.env.PWD = resolvedDir;
  ctx.state.env.OLDPWD = ctx.state.previousDir;
  const home = ctx.state.env.HOME || "";
  const output = `${[resolvedDir, ...stack].map((p) => formatPath(p, home)).join(" ")}
`;
  return success(output);
}
__name$1(handlePushd, "handlePushd");
function handlePopd(ctx, args) {
  const stack = getStack(ctx);
  for (const arg of args) {
    if (arg === "--") {
      continue;
    }
    if (arg.startsWith("-") && arg !== "-") {
      return failure(`bash: popd: ${arg}: invalid option
`, 2);
    }
    return failure("bash: popd: too many arguments\n", 2);
  }
  if (stack.length === 0) {
    return failure("bash: popd: directory stack empty\n", 1);
  }
  const newDir = stack.shift();
  if (!newDir) {
    return failure("bash: popd: directory stack empty\n", 1);
  }
  ctx.state.previousDir = ctx.state.cwd;
  ctx.state.cwd = newDir;
  ctx.state.env.PWD = newDir;
  ctx.state.env.OLDPWD = ctx.state.previousDir;
  const home = ctx.state.env.HOME || "";
  const output = `${[newDir, ...stack].map((p) => formatPath(p, home)).join(" ")}
`;
  return success(output);
}
__name$1(handlePopd, "handlePopd");
function handleDirs(ctx, args) {
  const stack = getStack(ctx);
  let clearStack = false;
  let longFormat = false;
  let perLine = false;
  let withNumbers = false;
  for (const arg of args) {
    if (arg === "--") {
      continue;
    }
    if (arg.startsWith("-")) {
      for (const flag of arg.slice(1)) {
        if (flag === "c")
          clearStack = true;
        else if (flag === "l")
          longFormat = true;
        else if (flag === "p")
          perLine = true;
        else if (flag === "v") {
          perLine = true;
          withNumbers = true;
        } else {
          return failure(`bash: dirs: -${flag}: invalid option
`, 2);
        }
      }
    } else {
      return failure("bash: dirs: too many arguments\n", 1);
    }
  }
  if (clearStack) {
    ctx.state.directoryStack = [];
    return OK;
  }
  const fullStack = [ctx.state.cwd, ...stack];
  const home = ctx.state.env.HOME || "";
  let output;
  if (withNumbers) {
    output = fullStack.map((p, i) => {
      const path2 = longFormat ? p : formatPath(p, home);
      return ` ${i}  ${path2}`;
    }).join("\n");
    output += "\n";
  } else if (perLine) {
    output = fullStack.map((p) => longFormat ? p : formatPath(p, home)).join("\n") + "\n";
  } else {
    output = fullStack.map((p) => longFormat ? p : formatPath(p, home)).join(" ") + "\n";
  }
  return success(output);
}
__name$1(handleDirs, "handleDirs");
async function handleEval(ctx, args, stdin) {
  let evalArgs = args;
  if (evalArgs.length > 0) {
    const first = evalArgs[0];
    if (first === "--") {
      evalArgs = evalArgs.slice(1);
    } else if (first.startsWith("-") && first !== "-" && first.length > 1) {
      return failure(`bash: eval: ${first}: invalid option
eval: usage: eval [arg ...]
`, 2);
    }
  }
  if (evalArgs.length === 0) {
    return OK;
  }
  const command = evalArgs.join(" ");
  if (command.trim() === "") {
    return OK;
  }
  const savedGroupStdin = ctx.state.groupStdin;
  const effectiveStdin = stdin ?? ctx.state.groupStdin;
  if (effectiveStdin !== void 0) {
    ctx.state.groupStdin = effectiveStdin;
  }
  try {
    const ast = parse(command);
    return await ctx.executeScript(ast);
  } catch (error) {
    if (error instanceof BreakError || error instanceof ContinueError || error instanceof ReturnError || error instanceof ExitError) {
      throw error;
    }
    if (error.name === "ParseException") {
      return failure(`bash: eval: ${error.message}
`);
    }
    throw error;
  } finally {
    ctx.state.groupStdin = savedGroupStdin;
  }
}
__name$1(handleEval, "handleEval");
function handleExit(ctx, args) {
  let exitCode;
  let stderr = "";
  if (args.length === 0) {
    exitCode = ctx.state.lastExitCode;
  } else {
    const arg = args[0];
    const parsed = Number.parseInt(arg, 10);
    if (arg === "" || Number.isNaN(parsed) || !/^-?\d+$/.test(arg)) {
      stderr = `bash: exit: ${arg}: numeric argument required
`;
      exitCode = 2;
    } else {
      exitCode = (parsed % 256 + 256) % 256;
    }
  }
  throw new ExitError(exitCode, "", stderr);
}
__name$1(handleExit, "handleExit");
function handleExport(ctx, args) {
  let unexport = false;
  const processedArgs = [];
  for (const arg of args) {
    if (arg === "-n") {
      unexport = true;
    } else if (arg === "-p") ;
    else if (arg === "--") ;
    else {
      processedArgs.push(arg);
    }
  }
  if (processedArgs.length === 0 && !unexport) {
    let stdout = "";
    const exportedVars = ctx.state.exportedVars ?? /* @__PURE__ */ new Set();
    const sortedNames = Array.from(exportedVars).sort();
    for (const name of sortedNames) {
      const value = ctx.state.env[name];
      if (value !== void 0) {
        const escapedValue = value.replace(/\\/g, "\\\\").replace(/"/g, '\\"');
        stdout += `declare -x ${name}="${escapedValue}"
`;
      }
    }
    return success(stdout);
  }
  if (unexport) {
    for (const arg of processedArgs) {
      let name;
      let value;
      if (arg.includes("=")) {
        const eqIdx = arg.indexOf("=");
        name = arg.slice(0, eqIdx);
        value = expandTildesInValue(ctx, arg.slice(eqIdx + 1));
        ctx.state.env[name] = value;
      } else {
        name = arg;
      }
      unmarkExported(ctx, name);
    }
    return OK;
  }
  let stderr = "";
  let exitCode = 0;
  for (const arg of processedArgs) {
    let name;
    let value;
    let isAppend = false;
    const appendMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\+=(.*)$/);
    if (appendMatch) {
      name = appendMatch[1];
      value = expandTildesInValue(ctx, appendMatch[2]);
      isAppend = true;
    } else if (arg.includes("=")) {
      const eqIdx = arg.indexOf("=");
      name = arg.slice(0, eqIdx);
      value = expandTildesInValue(ctx, arg.slice(eqIdx + 1));
    } else {
      name = arg;
    }
    if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(name)) {
      stderr += `bash: export: \`${arg}': not a valid identifier
`;
      exitCode = 1;
      continue;
    }
    if (value !== void 0) {
      if (isAppend) {
        const existing = ctx.state.env[name] ?? "";
        ctx.state.env[name] = existing + value;
      } else {
        ctx.state.env[name] = value;
      }
    } else {
      if (!(name in ctx.state.env)) {
        ctx.state.env[name] = "";
      }
    }
    markExported(ctx, name);
  }
  return result("", stderr, exitCode);
}
__name$1(handleExport, "handleExport");
function handleGetopts(ctx, args) {
  if (args.length < 2) {
    return failure("bash: getopts: usage: getopts optstring name [arg ...]\n");
  }
  const optstring = args[0];
  const varName = args[1];
  const invalidVarName = !/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(varName);
  const silentMode = optstring.startsWith(":");
  const actualOptstring = silentMode ? optstring.slice(1) : optstring;
  let argsToProcess;
  if (args.length > 2) {
    argsToProcess = args.slice(2);
  } else {
    const paramCount = Number.parseInt(ctx.state.env["#"] || "0", 10);
    argsToProcess = [];
    for (let i = 1; i <= paramCount; i++) {
      argsToProcess.push(ctx.state.env[String(i)] || "");
    }
  }
  let optind = Number.parseInt(ctx.state.env.OPTIND || "1", 10);
  if (optind < 1) {
    optind = 1;
  }
  const charIndex = Number.parseInt(ctx.state.env.__GETOPTS_CHARINDEX || "0", 10);
  ctx.state.env.OPTARG = "";
  if (optind > argsToProcess.length) {
    if (!invalidVarName) {
      ctx.state.env[varName] = "?";
    }
    ctx.state.env.OPTIND = String(argsToProcess.length + 1);
    ctx.state.env.__GETOPTS_CHARINDEX = "0";
    return { exitCode: invalidVarName ? 2 : 1, stdout: "", stderr: "" };
  }
  const currentArg = argsToProcess[optind - 1];
  if (!currentArg || currentArg === "-" || !currentArg.startsWith("-")) {
    if (!invalidVarName) {
      ctx.state.env[varName] = "?";
    }
    return { exitCode: invalidVarName ? 2 : 1, stdout: "", stderr: "" };
  }
  if (currentArg === "--") {
    ctx.state.env.OPTIND = String(optind + 1);
    ctx.state.env.__GETOPTS_CHARINDEX = "0";
    if (!invalidVarName) {
      ctx.state.env[varName] = "?";
    }
    return { exitCode: invalidVarName ? 2 : 1, stdout: "", stderr: "" };
  }
  const startIndex = charIndex === 0 ? 1 : charIndex;
  const optChar = currentArg[startIndex];
  if (!optChar) {
    ctx.state.env.OPTIND = String(optind + 1);
    ctx.state.env.__GETOPTS_CHARINDEX = "0";
    return handleGetopts(ctx, args);
  }
  const optIndex = actualOptstring.indexOf(optChar);
  if (optIndex === -1) {
    let stderrMsg = "";
    if (!silentMode) {
      stderrMsg = `bash: illegal option -- ${optChar}
`;
    } else {
      ctx.state.env.OPTARG = optChar;
    }
    if (!invalidVarName) {
      ctx.state.env[varName] = "?";
    }
    if (startIndex + 1 < currentArg.length) {
      ctx.state.env.__GETOPTS_CHARINDEX = String(startIndex + 1);
      ctx.state.env.OPTIND = String(optind);
    } else {
      ctx.state.env.OPTIND = String(optind + 1);
      ctx.state.env.__GETOPTS_CHARINDEX = "0";
    }
    return { exitCode: invalidVarName ? 2 : 0, stdout: "", stderr: stderrMsg };
  }
  const requiresArg = optIndex + 1 < actualOptstring.length && actualOptstring[optIndex + 1] === ":";
  if (requiresArg) {
    if (startIndex + 1 < currentArg.length) {
      ctx.state.env.OPTARG = currentArg.slice(startIndex + 1);
      ctx.state.env.OPTIND = String(optind + 1);
      ctx.state.env.__GETOPTS_CHARINDEX = "0";
    } else {
      if (optind >= argsToProcess.length) {
        let stderrMsg = "";
        if (!silentMode) {
          stderrMsg = `bash: option requires an argument -- ${optChar}
`;
          if (!invalidVarName) {
            ctx.state.env[varName] = "?";
          }
        } else {
          ctx.state.env.OPTARG = optChar;
          if (!invalidVarName) {
            ctx.state.env[varName] = ":";
          }
        }
        ctx.state.env.OPTIND = String(optind + 1);
        ctx.state.env.__GETOPTS_CHARINDEX = "0";
        return {
          exitCode: invalidVarName ? 2 : 0,
          stdout: "",
          stderr: stderrMsg
        };
      }
      ctx.state.env.OPTARG = argsToProcess[optind];
      ctx.state.env.OPTIND = String(optind + 2);
      ctx.state.env.__GETOPTS_CHARINDEX = "0";
    }
  } else {
    if (startIndex + 1 < currentArg.length) {
      ctx.state.env.__GETOPTS_CHARINDEX = String(startIndex + 1);
      ctx.state.env.OPTIND = String(optind);
    } else {
      ctx.state.env.OPTIND = String(optind + 1);
      ctx.state.env.__GETOPTS_CHARINDEX = "0";
    }
  }
  if (!invalidVarName) {
    ctx.state.env[varName] = optChar;
  }
  return { exitCode: invalidVarName ? 2 : 0, stdout: "", stderr: "" };
}
__name$1(handleGetopts, "handleGetopts");
async function handleHash(ctx, args) {
  if (!ctx.state.hashTable) {
    ctx.state.hashTable = /* @__PURE__ */ new Map();
  }
  let clearTable = false;
  let deleteMode = false;
  let listMode = false;
  let pathMode = false;
  let showPath = false;
  let pathname = "";
  const names = [];
  let i = 0;
  while (i < args.length) {
    const arg = args[i];
    if (arg === "--") {
      i++;
      names.push(...args.slice(i));
      break;
    }
    if (arg === "-r") {
      clearTable = true;
      i++;
    } else if (arg === "-d") {
      deleteMode = true;
      i++;
    } else if (arg === "-l") {
      listMode = true;
      i++;
    } else if (arg === "-t") {
      showPath = true;
      i++;
    } else if (arg === "-p") {
      pathMode = true;
      i++;
      if (i >= args.length) {
        return failure("bash: hash: -p: option requires an argument\n", 1);
      }
      pathname = args[i];
      i++;
    } else if (arg.startsWith("-") && arg.length > 1) {
      for (const char of arg.slice(1)) {
        if (char === "r") {
          clearTable = true;
        } else if (char === "d") {
          deleteMode = true;
        } else if (char === "l") {
          listMode = true;
        } else if (char === "t") {
          showPath = true;
        } else if (char === "p") {
          return failure("bash: hash: -p: option requires an argument\n", 1);
        } else {
          return failure(`bash: hash: -${char}: invalid option
`, 1);
        }
      }
      i++;
    } else {
      names.push(arg);
      i++;
    }
  }
  if (clearTable) {
    ctx.state.hashTable.clear();
    return OK;
  }
  if (deleteMode) {
    if (names.length === 0) {
      return failure("bash: hash: -d: option requires an argument\n", 1);
    }
    let hasError2 = false;
    let stderr2 = "";
    for (const name of names) {
      if (!ctx.state.hashTable.has(name)) {
        stderr2 += `bash: hash: ${name}: not found
`;
        hasError2 = true;
      } else {
        ctx.state.hashTable.delete(name);
      }
    }
    if (hasError2) {
      return failure(stderr2, 1);
    }
    return OK;
  }
  if (showPath) {
    if (names.length === 0) {
      return failure("bash: hash: -t: option requires an argument\n", 1);
    }
    let stdout = "";
    let hasError2 = false;
    let stderr2 = "";
    for (const name of names) {
      const cachedPath = ctx.state.hashTable.get(name);
      if (cachedPath) {
        if (names.length > 1) {
          stdout += `${name}	${cachedPath}
`;
        } else {
          stdout += `${cachedPath}
`;
        }
      } else {
        stderr2 += `bash: hash: ${name}: not found
`;
        hasError2 = true;
      }
    }
    if (hasError2) {
      return { exitCode: 1, stdout, stderr: stderr2 };
    }
    return success(stdout);
  }
  if (pathMode) {
    if (names.length === 0) {
      return failure("bash: hash: usage: hash [-lr] [-p pathname] [-dt] [name ...]\n", 1);
    }
    const name = names[0];
    ctx.state.hashTable.set(name, pathname);
    return OK;
  }
  if (names.length === 0) {
    if (ctx.state.hashTable.size === 0) {
      return success("hash: hash table empty\n");
    }
    let stdout = "";
    if (listMode) {
      for (const [name, path2] of ctx.state.hashTable) {
        stdout += `builtin hash -p ${path2} ${name}
`;
      }
    } else {
      stdout = "hits	command\n";
      for (const [, path2] of ctx.state.hashTable) {
        stdout += `   1	${path2}
`;
      }
    }
    return success(stdout);
  }
  let hasError = false;
  let stderr = "";
  const pathEnv = ctx.state.env.PATH || "/usr/bin:/bin";
  const pathDirs = pathEnv.split(":");
  for (const name of names) {
    if (name.includes("/")) {
      stderr += `bash: hash: ${name}: cannot use / in name
`;
      hasError = true;
      continue;
    }
    let found = false;
    for (const dir of pathDirs) {
      if (!dir)
        continue;
      const fullPath = `${dir}/${name}`;
      if (await ctx.fs.exists(fullPath)) {
        ctx.state.hashTable.set(name, fullPath);
        found = true;
        break;
      }
    }
    if (!found) {
      stderr += `bash: hash: ${name}: not found
`;
      hasError = true;
    }
  }
  if (hasError) {
    return failure(stderr, 1);
  }
  return OK;
}
__name$1(handleHash, "handleHash");
var BUILTIN_HELP = {
  ":": [
    ": [arguments]",
    `Null command.
    No effect; the command does nothing.
    Exit Status:
    Always succeeds.`
  ],
  ".": [
    ". filename [arguments]",
    `Execute commands from a file in the current shell.
    Read and execute commands from FILENAME in the current shell.
    The entries in $PATH are used to find the directory containing FILENAME.
    Exit Status:
    Returns the status of the last command executed in FILENAME.`
  ],
  "[": [
    "[ arg... ]",
    `Evaluate conditional expression.
    This is a synonym for the "test" builtin, but the last argument must
    be a literal \`]', to match the opening \`['.`
  ],
  alias: [
    "alias [-p] [name[=value] ... ]",
    `Define or display aliases.
    Without arguments, \`alias' prints the list of aliases in the reusable
    form \`alias NAME=VALUE' on standard output.
    Exit Status:
    alias returns true unless a NAME is supplied for which no alias has been
    defined.`
  ],
  bg: [
    "bg [job_spec ...]",
    `Move jobs to the background.
    Place the jobs identified by each JOB_SPEC in the background, as if they
    had been started with \`&'.`
  ],
  break: [
    "break [n]",
    `Exit for, while, or until loops.
    Exit a FOR, WHILE or UNTIL loop.  If N is specified, break N enclosing
    loops.
    Exit Status:
    The exit status is 0 unless N is not greater than or equal to 1.`
  ],
  builtin: [
    "builtin [shell-builtin [arg ...]]",
    `Execute shell builtins.
    Execute SHELL-BUILTIN with arguments ARGs without performing command
    lookup.  This is useful when you wish to reimplement a shell builtin
    as a shell function, but need to execute the builtin within the function.
    Exit Status:
    Returns the exit status of SHELL-BUILTIN, or false if SHELL-BUILTIN is
    not a shell builtin.`
  ],
  caller: [
    "caller [expr]",
    `Return the context of the current subroutine call.
    Without EXPR, returns "$line $filename".  With EXPR, returns
    "$line $subroutine $filename"; this extra information can be used to
    provide a stack trace.
    Exit Status:
    Returns 0 unless the shell is not executing a subroutine call or
    EXPR is invalid.`
  ],
  cd: [
    "cd [-L|-P] [dir]",
    `Change the shell working directory.
    Change the current directory to DIR.  The default DIR is the value of the
    HOME shell variable.

    The variable CDPATH defines the search path for the directory containing
    DIR.  Alternative directory names in CDPATH are separated by a colon (:).
    A null directory name is the same as the current directory.  If DIR begins
    with a slash (/), then CDPATH is not used.

    If the directory is not found, and the shell option \`cdable_vars' is set,
    the word is assumed to be a variable name.  If that variable has a value,
    its value is used for DIR.

    Options:
      -L	force symbolic links to be followed
      -P	use the physical directory structure without following symbolic
    	links

    The default is to follow symbolic links, as if \`-L' were specified.

    Exit Status:
    Returns 0 if the directory is changed; non-zero otherwise.`
  ],
  command: [
    "command [-pVv] command [arg ...]",
    `Execute a simple command or display information about commands.
    Runs COMMAND with ARGS suppressing shell function lookup, or display
    information about the specified COMMANDs.

    Options:
      -p	use a default value for PATH that is guaranteed to find all of
    	the standard utilities
      -v	print a description of COMMAND similar to the \`type' builtin
      -V	print a more verbose description of each COMMAND

    Exit Status:
    Returns exit status of COMMAND, or failure if COMMAND is not found.`
  ],
  compgen: [
    "compgen [-abcdefgjksuv] [-o option] [-A action] [-G globpat] [-W wordlist]  [-F function] [-C command] [-X filterpat] [-P prefix] [-S suffix] [word]",
    `Display possible completions depending on the options.
    Intended to be used from within a shell function generating possible
    completions.  If the optional WORD argument is supplied, matches against
    WORD are generated.
    Exit Status:
    Returns success unless an invalid option is supplied or an error occurs.`
  ],
  complete: [
    "complete [-abcdefgjksuv] [-pr] [-DEI] [-o option] [-A action] [-G globpat] [-W wordlist]  [-F function] [-C command] [-X filterpat] [-P prefix] [-S suffix] [name ...]",
    `Specify how arguments are to be completed.
    For each NAME, specify how arguments are to be completed.
    Exit Status:
    Returns success unless an invalid option is supplied or an error occurs.`
  ],
  continue: [
    "continue [n]",
    `Resume for, while, or until loops.
    Resumes the next iteration of the enclosing FOR, WHILE or UNTIL loop.
    If N is specified, resumes the Nth enclosing loop.
    Exit Status:
    The exit status is 0 unless N is not greater than or equal to 1.`
  ],
  declare: [
    "declare [-aAfFgilnrtux] [-p] [name[=value] ...]",
    `Set variable values and attributes.
    Declare variables and give them attributes.  If no NAMEs are given,
    display the attributes and values of all variables.

    Options:
      -a	to make NAMEs indexed arrays (if supported)
      -A	to make NAMEs associative arrays (if supported)
      -i	to make NAMEs have the \`integer' attribute
      -l	to convert the value of each NAME to lower case on assignment
      -n	make NAME a reference to the variable named by its value
      -r	to make NAMEs readonly
      -t	to make NAMEs have the \`trace' attribute
      -u	to convert the value of each NAME to upper case on assignment
      -x	to make NAMEs export

    Exit Status:
    Returns success unless an invalid option is supplied or a variable
    assignment error occurs.`
  ],
  dirs: [
    "dirs [-clpv] [+N] [-N]",
    `Display directory stack.
    Display the list of currently remembered directories.  Directories
    find their way onto the list with the \`pushd' command; you can get
    back up through the list with the \`popd' command.
    Exit Status:
    Returns success unless an invalid option is supplied or an error occurs.`
  ],
  disown: [
    "disown [-h] [-ar] [jobspec ...]",
    `Remove jobs from current shell.
    Without any JOBSPECs, remove the current job.`
  ],
  echo: [
    "echo [-neE] [arg ...]",
    `Write arguments to the standard output.
    Display the ARGs, separated by a single space character and followed by a
    newline, on the standard output.

    Options:
      -n	do not append a newline
      -e	enable interpretation of the following backslash escapes
      -E	explicitly suppress interpretation of backslash escapes

    Exit Status:
    Returns success unless a write error occurs.`
  ],
  enable: [
    "enable [-a] [-dnps] [-f filename] [name ...]",
    `Enable and disable shell builtins.
    Enables and disables builtin shell commands.
    Exit Status:
    Returns success unless NAME is not a shell builtin or an error occurs.`
  ],
  eval: [
    "eval [arg ...]",
    `Execute arguments as a shell command.
    Combine ARGs into a single string, use the result as input to the shell,
    and execute the resulting commands.
    Exit Status:
    Returns exit status of command or success if command is null.`
  ],
  exec: [
    "exec [-cl] [-a name] [command [arguments ...]] [redirection ...]",
    `Replace the shell with the given command.
    Execute COMMAND, replacing this shell with the specified program.
    ARGUMENTS become the arguments to COMMAND.  If COMMAND is not specified,
    any redirections take effect in the current shell.
    Exit Status:
    Returns success unless COMMAND is not found or a redirection error occurs.`
  ],
  exit: [
    "exit [n]",
    `Exit the shell.
    Exits the shell with a status of N.  If N is omitted, the exit status
    is that of the last command executed.`
  ],
  export: [
    "export [-fn] [name[=value] ...] or export -p",
    `Set export attribute for shell variables.
    Marks each NAME for automatic export to the environment of subsequently
    executed commands.  If VALUE is supplied, assign VALUE before exporting.

    Options:
      -f	refer to shell functions
      -n	remove the export property from each NAME
      -p	display a list of all exported variables and functions

    Exit Status:
    Returns success unless an invalid option is given or NAME is invalid.`
  ],
  false: [
    "false",
    `Return an unsuccessful result.
    Exit Status:
    Always fails.`
  ],
  fc: [
    "fc [-e ename] [-lnr] [first] [last] or fc -s [pat=rep] [command]",
    `Display or execute commands from the history list.
    Exit Status:
    Returns success or status of executed command.`
  ],
  fg: [
    "fg [job_spec]",
    `Move job to the foreground.
    Place the job identified by JOB_SPEC in the foreground, making it the
    current job.`
  ],
  getopts: [
    "getopts optstring name [arg]",
    `Parse option arguments.
    Getopts is used by shell procedures to parse positional parameters
    as options.

    OPTSTRING contains the option letters to be recognized; if a letter
    is followed by a colon, the option is expected to have an argument,
    which should be separated from it by white space.
    Exit Status:
    Returns success if an option is found; fails if the end of options is
    encountered or an error occurs.`
  ],
  hash: [
    "hash [-lr] [-p pathname] [-dt] [name ...]",
    `Remember or display program locations.
    Determine and remember the full pathname of each command NAME.
    Exit Status:
    Returns success unless NAME is not found or an invalid option is given.`
  ],
  help: [
    "help [-s] [pattern ...]",
    `Display information about builtin commands.
    Displays brief summaries of builtin commands.  If PATTERN is
    specified, gives detailed help on all commands matching PATTERN,
    otherwise the list of help topics is printed.

    Options:
      -s	output only a short usage synopsis for each topic matching
    	PATTERN

    Exit Status:
    Returns success unless PATTERN is not found.`
  ],
  history: [
    "history [-c] [-d offset] [n] or history -anrw [filename] or history -ps arg [arg...]",
    `Display or manipulate the history list.
    Display the history list with line numbers, prefixing each modified
    entry with a \`*'.
    Exit Status:
    Returns success unless an invalid option is given or an error occurs.`
  ],
  jobs: [
    "jobs [-lnprs] [jobspec ...] or jobs -x command [args]",
    `Display status of jobs.
    Lists the active jobs.
    Exit Status:
    Returns success unless an invalid option is given or an error occurs.`
  ],
  kill: [
    "kill [-s sigspec | -n signum | -sigspec] pid | jobspec ... or kill -l [sigspec]",
    `Send a signal to a job.
    Send the processes identified by PID or JOBSPEC the signal named by
    SIGSPEC or SIGNUM.
    Exit Status:
    Returns success unless an invalid option is given or an error occurs.`
  ],
  let: [
    "let arg [arg ...]",
    `Evaluate arithmetic expressions.
    Evaluate each ARG as an arithmetic expression.  Evaluation is done in
    fixed-width integers with no check for overflow, though division by 0
    is trapped and flagged as an error.
    Exit Status:
    If the last ARG evaluates to 0, let returns 1; 0 is returned otherwise.`
  ],
  local: [
    "local [option] name[=value] ...",
    `Define local variables.
    Create a local variable called NAME, and give it VALUE.  OPTION can
    be any option accepted by \`declare'.

    Local can only be used within a function; it makes the variable NAME
    have a visible scope restricted to that function and its children.
    Exit Status:
    Returns success unless an invalid option is supplied, a variable
    assignment error occurs, or the shell is not executing a function.`
  ],
  logout: [
    "logout [n]",
    `Exit a login shell.
    Exits a login shell with exit status N.  Returns an error if not executed
    in a login shell.`
  ],
  mapfile: [
    "mapfile [-d delim] [-n count] [-O origin] [-s count] [-t] [-u fd] [-C callback] [-c quantum] [array]",
    `Read lines from the standard input into an indexed array variable.
    Read lines from the standard input into the indexed array variable ARRAY,
    or from file descriptor FD if the -u option is supplied.

    Options:
      -d delim	Use DELIM to terminate lines, instead of newline
      -n count	Copy at most COUNT lines
      -O origin	Begin assigning to ARRAY at index ORIGIN
      -s count	Discard the first COUNT lines read
      -t	Remove a trailing DELIM from each line read (default newline)
      -u fd	Read lines from file descriptor FD instead of standard input

    Exit Status:
    Returns success unless an invalid option is given or ARRAY is readonly.`
  ],
  popd: [
    "popd [-n] [+N | -N]",
    `Remove directories from stack.
    Removes entries from the directory stack.
    Exit Status:
    Returns success unless an invalid argument is supplied or the directory
    change fails.`
  ],
  printf: [
    "printf [-v var] format [arguments]",
    `Formats and prints ARGUMENTS under control of the FORMAT.

    Options:
      -v var	assign the output to shell variable VAR rather than
    		display it on the standard output

    FORMAT is a character string which contains three types of objects: plain
    characters, which are simply copied to standard output; character escape
    sequences, which are converted and copied to the standard output; and
    format specifications, each of which causes printing of the next successive
    argument.
    Exit Status:
    Returns success unless an invalid option is given or a write or assignment
    error occurs.`
  ],
  pushd: [
    "pushd [-n] [+N | -N | dir]",
    `Add directories to stack.
    Adds a directory to the top of the directory stack, or rotates
    the stack, making the new top of the stack the current working
    directory.
    Exit Status:
    Returns success unless an invalid argument is supplied or the directory
    change fails.`
  ],
  pwd: [
    "pwd [-LP]",
    `Print the name of the current working directory.

    Options:
      -L	print the value of $PWD if it names the current working
    	directory
      -P	print the physical directory, without any symbolic links

    By default, \`pwd' behaves as if \`-L' were specified.
    Exit Status:
    Returns 0 unless an invalid option is given or the current directory
    cannot be read.`
  ],
  read: [
    "read [-ers] [-a array] [-d delim] [-i text] [-n nchars] [-N nchars] [-p prompt] [-t timeout] [-u fd] [name ...]",
    `Read a line from the standard input and split it into fields.
    Reads a single line from the standard input, or from file descriptor FD
    if the -u option is supplied.  The line is split into fields as with word
    splitting, and the first word is assigned to the first NAME, the second
    word to the second NAME, and so on, with any leftover words assigned to
    the last NAME.
    Exit Status:
    The return code is zero, unless end-of-file is encountered, read times out,
    or an invalid file descriptor is supplied as the argument to -u.`
  ],
  readarray: [
    "readarray [-d delim] [-n count] [-O origin] [-s count] [-t] [-u fd] [-C callback] [-c quantum] [array]",
    `Read lines from a file into an array variable.
    A synonym for \`mapfile'.`
  ],
  readonly: [
    "readonly [-aAf] [name[=value] ...] or readonly -p",
    `Mark shell variables as unchangeable.
    Mark each NAME as read-only; the values of these NAMEs may not be
    changed by subsequent assignment.
    Exit Status:
    Returns success unless an invalid option is given or NAME is invalid.`
  ],
  return: [
    "return [n]",
    `Return from a shell function.
    Causes a function or sourced script to exit with the return value
    specified by N.  If N is omitted, the return status is that of the
    last command executed within the function or script.
    Exit Status:
    Returns N, or failure if the shell is not executing a function or script.`
  ],
  set: [
    "set [-abefhkmnptuvxBCHP] [-o option-name] [--] [arg ...]",
    `Set or unset values of shell options and positional parameters.
    Change the value of shell attributes and positional parameters, or
    display the names and values of shell variables.

    Options:
      -e  Exit immediately if a command exits with a non-zero status.
      -u  Treat unset variables as an error when substituting.
      -x  Print commands and their arguments as they are executed.
      -o option-name
          Set the variable corresponding to option-name

    Exit Status:
    Returns success unless an invalid option is given.`
  ],
  shift: [
    "shift [n]",
    `Shift positional parameters.
    Rename the positional parameters $N+1,$N+2 ... to $1,$2 ...  If N is
    not given, it is assumed to be 1.
    Exit Status:
    Returns success unless N is negative or greater than $#.`
  ],
  shopt: [
    "shopt [-pqsu] [-o] [optname ...]",
    `Set and unset shell options.
    Change the setting of each shell option OPTNAME.  Without any option
    arguments, list each supplied OPTNAME, or all shell options if no
    OPTNAMEs are given, with an indication of whether or not each is set.

    Options:
      -o	restrict OPTNAMEs to those defined for use with \`set -o'
      -p	print each shell option with an indication of its status
      -q	suppress output
      -s	enable (set) each OPTNAME
      -u	disable (unset) each OPTNAME

    Exit Status:
    Returns success if OPTNAME is enabled; fails if an invalid option is
    given or OPTNAME is disabled.`
  ],
  source: [
    "source filename [arguments]",
    `Execute commands from a file in the current shell.
    Read and execute commands from FILENAME in the current shell.
    The entries in $PATH are used to find the directory containing FILENAME.
    Exit Status:
    Returns the status of the last command executed in FILENAME.`
  ],
  suspend: [
    "suspend [-f]",
    `Suspend shell execution.
    Suspend the execution of this shell until it receives a SIGCONT signal.`
  ],
  test: [
    "test [expr]",
    `Evaluate conditional expression.
    Exits with a status of 0 (true) or 1 (false) depending on
    the evaluation of EXPR.  Expressions may be unary or binary.
    Exit Status:
    Returns success if EXPR evaluates to true; fails if EXPR evaluates to
    false or an invalid argument is given.`
  ],
  times: [
    "times",
    `Display process times.
    Prints the accumulated user and system times for the shell and all of its
    child processes.
    Exit Status:
    Always succeeds.`
  ],
  trap: [
    "trap [-lp] [[arg] signal_spec ...]",
    `Trap signals and other events.
    Defines and activates handlers to be run when the shell receives signals
    or other conditions.
    Exit Status:
    Returns success unless a SIGSPEC is invalid or an invalid option is given.`
  ],
  true: [
    "true",
    `Return a successful result.
    Exit Status:
    Always succeeds.`
  ],
  type: [
    "type [-afptP] name [name ...]",
    `Display information about command type.
    For each NAME, indicate how it would be interpreted if used as a
    command name.

    Options:
      -a	display all locations containing an executable named NAME
      -f	suppress shell function lookup
      -P	force a PATH search for each NAME, even if it is an alias,
    	builtin, or function, and returns the name of the disk file
    	that would be executed
      -p	returns either the name of the disk file that would be executed,
    	or nothing if \`type -t NAME' would not return \`file'
      -t	output a single word which is one of \`alias', \`keyword',
    	\`function', \`builtin', \`file' or \`', if NAME is an alias,
    	shell reserved word, shell function, shell builtin, disk file,
    	or not found, respectively

    Exit Status:
    Returns success if all of the NAMEs are found; fails if any are not found.`
  ],
  typeset: [
    "typeset [-aAfFgilnrtux] [-p] name[=value] ...",
    `Set variable values and attributes.
    A synonym for \`declare'.`
  ],
  ulimit: [
    "ulimit [-SHabcdefiklmnpqrstuvxPT] [limit]",
    `Modify shell resource limits.
    Provides control over the resources available to the shell and processes
    it creates, on systems that allow such control.
    Exit Status:
    Returns success unless an invalid option is supplied or an error occurs.`
  ],
  umask: [
    "umask [-p] [-S] [mode]",
    `Display or set file mode mask.
    Sets the user file-creation mask to MODE.  If MODE is omitted, prints
    the current value of the mask.
    Exit Status:
    Returns success unless MODE is invalid or an invalid option is given.`
  ],
  unalias: [
    "unalias [-a] name [name ...]",
    `Remove each NAME from the list of defined aliases.
    Exit Status:
    Returns success unless a NAME is not an existing alias.`
  ],
  unset: [
    "unset [-f] [-v] [-n] [name ...]",
    `Unset values and attributes of shell variables and functions.
    For each NAME, remove the corresponding variable or function.

    Options:
      -f	treat each NAME as a shell function
      -v	treat each NAME as a shell variable
      -n	treat each NAME as a name reference and unset the variable itself
    	rather than the variable it references

    Without options, unset first tries to unset a variable, and if that fails,
    tries to unset a function.
    Exit Status:
    Returns success unless an invalid option is given or a NAME is read-only.`
  ],
  wait: [
    "wait [-fn] [id ...]",
    `Wait for job completion and return exit status.
    Waits for each process identified by an ID, which may be a process ID or a
    job specification, and reports its termination status.
    Exit Status:
    Returns the status of the last ID; fails if ID is invalid or an invalid
    option is given.`
  ]
};
var ALL_BUILTINS = Object.keys(BUILTIN_HELP).sort();
function handleHelp(_ctx, args) {
  let shortForm = false;
  const patterns = [];
  let i = 0;
  while (i < args.length) {
    const arg = args[i];
    if (arg === "--") {
      i++;
      while (i < args.length) {
        patterns.push(args[i]);
        i++;
      }
      break;
    }
    if (arg.startsWith("-") && arg.length > 1) {
      for (let j = 1; j < arg.length; j++) {
        const flag = arg[j];
        if (flag === "s") {
          shortForm = true;
        } else {
          return failure(`bash: help: -${flag}: invalid option
`, 2);
        }
      }
      i++;
    } else {
      patterns.push(arg);
      i++;
    }
  }
  if (patterns.length === 0) {
    return listAllBuiltins();
  }
  let stdout = "";
  let hasError = false;
  let stderr = "";
  for (const pattern of patterns) {
    const matches = findMatchingBuiltins(pattern);
    if (matches.length === 0) {
      stderr += `bash: help: no help topics match \`${pattern}'.  Try \`help help' or \`man -k ${pattern}' or \`info ${pattern}'.
`;
      hasError = true;
      continue;
    }
    for (const name of matches) {
      const [synopsis, description] = BUILTIN_HELP[name];
      if (shortForm) {
        stdout += `${name}: ${synopsis}
`;
      } else {
        stdout += `${name}: ${synopsis}
${description}
`;
      }
    }
  }
  return {
    exitCode: hasError ? 1 : 0,
    stdout,
    stderr
  };
}
__name$1(handleHelp, "handleHelp");
function findMatchingBuiltins(pattern) {
  const regexPattern = pattern.replace(/[.+^${}()|[\]\\]/g, "\\$&").replace(/\*/g, ".*").replace(/\?/g, ".");
  const regex = new RegExp(`^${regexPattern}$`);
  return ALL_BUILTINS.filter((name) => regex.test(name));
}
__name$1(findMatchingBuiltins, "findMatchingBuiltins");
function listAllBuiltins() {
  const lines = [];
  lines.push("just-bash shell builtins");
  lines.push("These shell commands are defined internally. Type `help' to see this list.");
  lines.push("Type `help name' to find out more about the function `name'.");
  lines.push("");
  const maxWidth = 36;
  const builtins = ALL_BUILTINS.slice();
  const midpoint = Math.ceil(builtins.length / 2);
  for (let i = 0; i < midpoint; i++) {
    const left = builtins[i] || "";
    const right = builtins[i + midpoint] || "";
    const leftPadded = left.padEnd(maxWidth);
    lines.push(right ? `${leftPadded}${right}` : left);
  }
  return success(`${lines.join("\n")}
`);
}
__name$1(listAllBuiltins, "listAllBuiltins");
function parseLetArgs(args) {
  const expressions = [];
  let current = "";
  let parenDepth = 0;
  for (const arg of args) {
    for (const ch of arg) {
      if (ch === "(")
        parenDepth++;
      else if (ch === ")")
        parenDepth--;
    }
    if (current) {
      current += ` ${arg}`;
    } else {
      current = arg;
    }
    if (parenDepth === 0) {
      expressions.push(current);
      current = "";
    }
  }
  if (current) {
    expressions.push(current);
  }
  return expressions;
}
__name$1(parseLetArgs, "parseLetArgs");
async function handleLet(ctx, args) {
  if (args.length === 0) {
    return failure("bash: let: expression expected\n");
  }
  const expressions = parseLetArgs(args);
  let lastResult = 0;
  for (const expr of expressions) {
    try {
      const script = parse(`(( ${expr} ))`);
      const statement = script.statements[0];
      if (statement && statement.pipelines.length > 0 && statement.pipelines[0].commands.length > 0) {
        const command = statement.pipelines[0].commands[0];
        if (command.type === "ArithmeticCommand") {
          const arithNode = command;
          lastResult = await evaluateArithmetic(ctx, arithNode.expression.expression);
        }
      }
    } catch (error) {
      return failure(`bash: let: ${expr}: ${error.message}
`);
    }
  }
  return result("", "", lastResult === 0 ? 1 : 0);
}
__name$1(handleLet, "handleLet");
async function handleLocal(ctx, args) {
  if (ctx.state.localScopes.length === 0) {
    return failure("bash: local: can only be used in a function\n");
  }
  const currentScope = ctx.state.localScopes[ctx.state.localScopes.length - 1];
  let stderr = "";
  let exitCode = 0;
  let declareNameref = false;
  let declareArray = false;
  const processedArgs = [];
  for (const arg of args) {
    if (arg === "-n") {
      declareNameref = true;
    } else if (arg === "-a") {
      declareArray = true;
    } else if (arg === "-p") ;
    else if (arg.startsWith("-") && !arg.includes("=")) {
      for (const flag of arg.slice(1)) {
        if (flag === "n")
          declareNameref = true;
        else if (flag === "a")
          declareArray = true;
        else ;
      }
    } else {
      processedArgs.push(arg);
    }
  }
  if (processedArgs.length === 0) {
    let stdout = "";
    const localNames = Array.from(currentScope.keys()).filter((key2) => !key2.includes("_") || !key2.match(/_\d+$/)).filter((key2) => !key2.includes("__length")).sort();
    for (const name of localNames) {
      const value = ctx.state.env[name];
      if (value !== void 0) {
        stdout += `${name}=${value}
`;
      }
    }
    return result(stdout, "", 0);
  }
  for (const arg of processedArgs) {
    let name;
    let value;
    const arrayMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)=\((.*)\)$/s);
    if (arrayMatch) {
      name = arrayMatch[1];
      const content = arrayMatch[2];
      if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(name)) {
        stderr += `bash: local: \`${arg}': not a valid identifier
`;
        exitCode = 1;
        continue;
      }
      checkReadonlyError(ctx, name, "bash");
      if (!currentScope.has(name)) {
        currentScope.set(name, ctx.state.env[name]);
        const prefix2 = `${name}_`;
        for (const key2 of Object.keys(ctx.state.env)) {
          if (key2.startsWith(prefix2) && !key2.includes("__")) {
            if (!currentScope.has(key2)) {
              currentScope.set(key2, ctx.state.env[key2]);
            }
          }
        }
      }
      const prefix = `${name}_`;
      for (const key2 of Object.keys(ctx.state.env)) {
        if (key2.startsWith(prefix) && !key2.includes("__")) {
          delete ctx.state.env[key2];
        }
      }
      const elements = parseArrayElements(content);
      for (let i = 0; i < elements.length; i++) {
        ctx.state.env[`${name}_${i}`] = elements[i];
      }
      ctx.state.env[`${name}__length`] = String(elements.length);
      markLocalVarDepth(ctx, name);
      if (declareNameref) {
        markNameref(ctx, name);
      }
      continue;
    }
    const arrayAppendMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\+=\((.*)\)$/s);
    if (arrayAppendMatch) {
      name = arrayAppendMatch[1];
      const content = arrayAppendMatch[2];
      checkReadonlyError(ctx, name, "bash");
      if (!currentScope.has(name)) {
        currentScope.set(name, ctx.state.env[name]);
        const prefix = `${name}_`;
        for (const key2 of Object.keys(ctx.state.env)) {
          if (key2.startsWith(prefix) && !key2.includes("__")) {
            if (!currentScope.has(key2)) {
              currentScope.set(key2, ctx.state.env[key2]);
            }
          }
        }
        const lengthKey = `${name}__length`;
        if (ctx.state.env[lengthKey] !== void 0 && !currentScope.has(lengthKey)) {
          currentScope.set(lengthKey, ctx.state.env[lengthKey]);
        }
      }
      const newElements = parseArrayElements(content);
      const existingIndices = getArrayIndices(ctx, name);
      let startIndex = 0;
      if (existingIndices.length === 0 && ctx.state.env[name] !== void 0) {
        const scalarValue = ctx.state.env[name];
        ctx.state.env[`${name}_0`] = scalarValue;
        delete ctx.state.env[name];
        startIndex = 1;
      } else if (existingIndices.length > 0) {
        startIndex = Math.max(...existingIndices) + 1;
      }
      for (let i = 0; i < newElements.length; i++) {
        ctx.state.env[`${name}_${startIndex + i}`] = expandTildesInValue(ctx, newElements[i]);
      }
      const newLength = startIndex + newElements.length;
      ctx.state.env[`${name}__length`] = String(newLength);
      markLocalVarDepth(ctx, name);
      if (declareNameref) {
        markNameref(ctx, name);
      }
      continue;
    }
    const appendMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\+=(.*)$/);
    if (appendMatch) {
      name = appendMatch[1];
      const appendValue = expandTildesInValue(ctx, appendMatch[2]);
      checkReadonlyError(ctx, name, "bash");
      if (!currentScope.has(name)) {
        currentScope.set(name, ctx.state.env[name]);
      }
      const existing = ctx.state.env[name] ?? "";
      ctx.state.env[name] = existing + appendValue;
      markLocalVarDepth(ctx, name);
      if (declareNameref) {
        markNameref(ctx, name);
      }
      continue;
    }
    const indexMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[([^\]]+)\]=(.*)$/s);
    if (indexMatch) {
      name = indexMatch[1];
      const indexExpr = indexMatch[2];
      const indexValue = expandTildesInValue(ctx, indexMatch[3]);
      checkReadonlyError(ctx, name, "bash");
      if (!currentScope.has(name)) {
        currentScope.set(name, ctx.state.env[name]);
        const prefix = `${name}_`;
        for (const key2 of Object.keys(ctx.state.env)) {
          if (key2.startsWith(prefix) && !key2.includes("__")) {
            if (!currentScope.has(key2)) {
              currentScope.set(key2, ctx.state.env[key2]);
            }
          }
        }
        const lengthKey = `${name}__length`;
        if (ctx.state.env[lengthKey] !== void 0 && !currentScope.has(lengthKey)) {
          currentScope.set(lengthKey, ctx.state.env[lengthKey]);
        }
      }
      let index;
      try {
        const parser = new Parser();
        const arithAst = parseArithmeticExpression(parser, indexExpr);
        index = await evaluateArithmetic(ctx, arithAst.expression);
      } catch {
        const num = parseInt(indexExpr, 10);
        index = Number.isNaN(num) ? 0 : num;
      }
      ctx.state.env[`${name}_${index}`] = indexValue;
      const currentLength = parseInt(ctx.state.env[`${name}__length`] ?? "0", 10);
      if (index >= currentLength) {
        ctx.state.env[`${name}__length`] = String(index + 1);
      }
      markLocalVarDepth(ctx, name);
      if (declareNameref) {
        markNameref(ctx, name);
      }
      continue;
    }
    if (arg.includes("=")) {
      const eqIdx = arg.indexOf("=");
      name = arg.slice(0, eqIdx);
      value = expandTildesInValue(ctx, arg.slice(eqIdx + 1));
    } else {
      name = arg;
    }
    if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(name)) {
      stderr += `bash: local: \`${arg}': not a valid identifier
`;
      exitCode = 1;
      continue;
    }
    const wasAlreadyLocal = currentScope.has(name);
    if (value !== void 0) {
      let savedValue = ctx.state.env[name];
      if (ctx.state.tempEnvBindings) {
        const tempEnvAccessed = ctx.state.accessedTempEnvVars?.has(name);
        const tempEnvMutated = ctx.state.mutatedTempEnvVars?.has(name);
        if (!tempEnvAccessed && !tempEnvMutated) {
          for (let i = ctx.state.tempEnvBindings.length - 1; i >= 0; i--) {
            const bindings = ctx.state.tempEnvBindings[i];
            if (bindings.has(name)) {
              savedValue = bindings.get(name);
              break;
            }
          }
        }
      }
      pushLocalVarStack(ctx, name, savedValue);
    }
    if (!wasAlreadyLocal) {
      let savedValue = ctx.state.env[name];
      if (ctx.state.tempEnvBindings) {
        for (let i = ctx.state.tempEnvBindings.length - 1; i >= 0; i--) {
          const bindings = ctx.state.tempEnvBindings[i];
          if (bindings.has(name)) {
            savedValue = bindings.get(name);
            break;
          }
        }
      }
      currentScope.set(name, savedValue);
      if (declareArray) {
        const prefix = `${name}_`;
        for (const key2 of Object.keys(ctx.state.env)) {
          if (key2.startsWith(prefix) && !key2.includes("__")) {
            if (!currentScope.has(key2)) {
              currentScope.set(key2, ctx.state.env[key2]);
            }
          }
        }
        const lengthKey = `${name}__length`;
        if (ctx.state.env[lengthKey] !== void 0 && !currentScope.has(lengthKey)) {
          currentScope.set(lengthKey, ctx.state.env[lengthKey]);
        }
      }
    }
    if (declareArray && value === void 0) {
      const prefix = `${name}_`;
      for (const key2 of Object.keys(ctx.state.env)) {
        if (key2.startsWith(prefix) && !key2.includes("__")) {
          delete ctx.state.env[key2];
        }
      }
      ctx.state.env[`${name}__length`] = "0";
    } else if (value !== void 0) {
      checkReadonlyError(ctx, name, "bash");
      if (declareNameref && value !== "" && !/^[a-zA-Z_][a-zA-Z0-9_]*(\[.+\])?$/.test(value)) {
        stderr += `bash: local: \`${value}': invalid variable name for name reference
`;
        exitCode = 1;
        continue;
      }
      ctx.state.env[name] = value;
      if (ctx.state.options.allexport) {
        ctx.state.exportedVars = ctx.state.exportedVars || /* @__PURE__ */ new Set();
        ctx.state.exportedVars.add(name);
      }
    } else {
      const hasTempEnvBinding = ctx.state.tempEnvBindings?.some((bindings) => bindings.has(name));
      if (!wasAlreadyLocal && !hasTempEnvBinding) {
        delete ctx.state.env[name];
      }
    }
    markLocalVarDepth(ctx, name);
    if (declareNameref) {
      markNameref(ctx, name);
    }
  }
  return result("", stderr, exitCode);
}
__name$1(handleLocal, "handleLocal");
function handleMapfile(ctx, args, stdin) {
  let delimiter = "\n";
  let maxCount = 0;
  let origin = 0;
  let skipCount = 0;
  let trimDelimiter = false;
  let arrayName = "MAPFILE";
  let i = 0;
  while (i < args.length) {
    const arg = args[i];
    if (arg === "-d" && i + 1 < args.length) {
      delimiter = args[i + 1] === "" ? "\0" : args[i + 1] || "\n";
      i += 2;
    } else if (arg === "-n" && i + 1 < args.length) {
      maxCount = Number.parseInt(args[i + 1], 10) || 0;
      i += 2;
    } else if (arg === "-O" && i + 1 < args.length) {
      origin = Number.parseInt(args[i + 1], 10) || 0;
      i += 2;
    } else if (arg === "-s" && i + 1 < args.length) {
      skipCount = Number.parseInt(args[i + 1], 10) || 0;
      i += 2;
    } else if (arg === "-t") {
      trimDelimiter = true;
      i++;
    } else if (arg === "-u" || arg === "-C" || arg === "-c") {
      i += 2;
    } else if (!arg.startsWith("-")) {
      arrayName = arg;
      i++;
    } else {
      i++;
    }
  }
  let effectiveStdin = stdin;
  if (!effectiveStdin && ctx.state.groupStdin !== void 0) {
    effectiveStdin = ctx.state.groupStdin;
  }
  const lines = [];
  let remaining = effectiveStdin;
  let lineCount = 0;
  let skipped = 0;
  while (remaining.length > 0) {
    const delimIndex = remaining.indexOf(delimiter);
    if (delimIndex === -1) {
      if (remaining.length > 0) {
        if (skipped < skipCount) {
          skipped++;
        } else if (maxCount === 0 || lineCount < maxCount) {
          let lastLine = remaining;
          const nulIdx = lastLine.indexOf("\0");
          if (nulIdx !== -1) {
            lastLine = lastLine.substring(0, nulIdx);
          }
          lines.push(lastLine);
          lineCount++;
        }
      }
      break;
    }
    let line = remaining.substring(0, delimIndex);
    const nulIndex = line.indexOf("\0");
    if (nulIndex !== -1) {
      line = line.substring(0, nulIndex);
    }
    if (!trimDelimiter && delimiter !== "\0") {
      line += delimiter;
    }
    remaining = remaining.substring(delimIndex + delimiter.length);
    if (skipped < skipCount) {
      skipped++;
      continue;
    }
    if (maxCount > 0 && lineCount >= maxCount) {
      break;
    }
    lines.push(line);
    lineCount++;
  }
  if (origin === 0) {
    clearArray(ctx, arrayName);
  }
  for (let j = 0; j < lines.length; j++) {
    ctx.state.env[`${arrayName}_${origin + j}`] = lines[j];
  }
  const existingLength = parseInt(ctx.state.env[`${arrayName}__length`] || "0", 10);
  const newEndIndex = origin + lines.length;
  ctx.state.env[`${arrayName}__length`] = String(Math.max(existingLength, newEndIndex));
  if (ctx.state.groupStdin !== void 0 && !stdin) {
    ctx.state.groupStdin = "";
  }
  return result("", "", 0);
}
__name$1(handleMapfile, "handleMapfile");
function parseRwFdContent2(fdContent) {
  if (!fdContent.startsWith("__rw__:")) {
    return null;
  }
  const afterPrefix = fdContent.slice(7);
  const firstColonIdx = afterPrefix.indexOf(":");
  if (firstColonIdx === -1)
    return null;
  const pathLength = Number.parseInt(afterPrefix.slice(0, firstColonIdx), 10);
  if (Number.isNaN(pathLength) || pathLength < 0)
    return null;
  const pathStart = firstColonIdx + 1;
  const path2 = afterPrefix.slice(pathStart, pathStart + pathLength);
  const positionStart = pathStart + pathLength + 1;
  const remaining = afterPrefix.slice(positionStart);
  const posColonIdx = remaining.indexOf(":");
  if (posColonIdx === -1)
    return null;
  const position = Number.parseInt(remaining.slice(0, posColonIdx), 10);
  if (Number.isNaN(position) || position < 0)
    return null;
  const content = remaining.slice(posColonIdx + 1);
  return { path: path2, position, content };
}
__name$1(parseRwFdContent2, "parseRwFdContent");
function encodeRwFdContent(path2, position, content) {
  return `__rw__:${path2.length}:${path2}:${position}:${content}`;
}
__name$1(encodeRwFdContent, "encodeRwFdContent");
function handleRead(ctx, args, stdin, stdinSourceFd = -1) {
  let raw = false;
  let delimiter = "\n";
  let nchars = -1;
  let ncharsExact = -1;
  let arrayName = null;
  let fileDescriptor = -1;
  let timeout = -1;
  const varNames = [];
  let i = 0;
  let invalidNArg = false;
  const parseOption = /* @__PURE__ */ __name$1((opt, argIndex) => {
    let j = 1;
    while (j < opt.length) {
      const ch = opt[j];
      if (ch === "r") {
        raw = true;
        j++;
      } else if (ch === "s") {
        j++;
      } else if (ch === "d") {
        if (j + 1 < opt.length) {
          delimiter = opt.substring(j + 1);
          return { nextArgIndex: argIndex + 1 };
        } else if (argIndex + 1 < args.length) {
          delimiter = args[argIndex + 1];
          return { nextArgIndex: argIndex + 2 };
        }
        return { nextArgIndex: argIndex + 1 };
      } else if (ch === "n") {
        if (j + 1 < opt.length) {
          const numStr = opt.substring(j + 1);
          nchars = Number.parseInt(numStr, 10);
          if (Number.isNaN(nchars) || nchars < 0) {
            invalidNArg = true;
            nchars = 0;
          }
          return { nextArgIndex: argIndex + 1 };
        } else if (argIndex + 1 < args.length) {
          nchars = Number.parseInt(args[argIndex + 1], 10);
          if (Number.isNaN(nchars) || nchars < 0) {
            invalidNArg = true;
            nchars = 0;
          }
          return { nextArgIndex: argIndex + 2 };
        }
        return { nextArgIndex: argIndex + 1 };
      } else if (ch === "N") {
        if (j + 1 < opt.length) {
          const numStr = opt.substring(j + 1);
          ncharsExact = Number.parseInt(numStr, 10);
          if (Number.isNaN(ncharsExact) || ncharsExact < 0) {
            invalidNArg = true;
            ncharsExact = 0;
          }
          return { nextArgIndex: argIndex + 1 };
        } else if (argIndex + 1 < args.length) {
          ncharsExact = Number.parseInt(args[argIndex + 1], 10);
          if (Number.isNaN(ncharsExact) || ncharsExact < 0) {
            invalidNArg = true;
            ncharsExact = 0;
          }
          return { nextArgIndex: argIndex + 2 };
        }
        return { nextArgIndex: argIndex + 1 };
      } else if (ch === "a") {
        if (j + 1 < opt.length) {
          arrayName = opt.substring(j + 1);
          return { nextArgIndex: argIndex + 1 };
        } else if (argIndex + 1 < args.length) {
          arrayName = args[argIndex + 1];
          return { nextArgIndex: argIndex + 2 };
        }
        return { nextArgIndex: argIndex + 1 };
      } else if (ch === "p") {
        if (j + 1 < opt.length) {
          opt.substring(j + 1);
          return { nextArgIndex: argIndex + 1 };
        } else if (argIndex + 1 < args.length) {
          args[argIndex + 1];
          return { nextArgIndex: argIndex + 2 };
        }
        return { nextArgIndex: argIndex + 1 };
      } else if (ch === "u") {
        if (j + 1 < opt.length) {
          const numStr = opt.substring(j + 1);
          fileDescriptor = Number.parseInt(numStr, 10);
          if (Number.isNaN(fileDescriptor) || fileDescriptor < 0) {
            return { nextArgIndex: -2 };
          }
          return { nextArgIndex: argIndex + 1 };
        } else if (argIndex + 1 < args.length) {
          fileDescriptor = Number.parseInt(args[argIndex + 1], 10);
          if (Number.isNaN(fileDescriptor) || fileDescriptor < 0) {
            return { nextArgIndex: -2 };
          }
          return { nextArgIndex: argIndex + 2 };
        }
        return { nextArgIndex: argIndex + 1 };
      } else if (ch === "t") {
        if (j + 1 < opt.length) {
          const numStr = opt.substring(j + 1);
          timeout = Number.parseFloat(numStr);
          if (Number.isNaN(timeout)) {
            timeout = 0;
          }
          return { nextArgIndex: argIndex + 1 };
        } else if (argIndex + 1 < args.length) {
          timeout = Number.parseFloat(args[argIndex + 1]);
          if (Number.isNaN(timeout)) {
            timeout = 0;
          }
          return { nextArgIndex: argIndex + 2 };
        }
        return { nextArgIndex: argIndex + 1 };
      } else if (ch === "e" || ch === "i" || ch === "P") {
        if (ch === "i" && argIndex + 1 < args.length) {
          return { nextArgIndex: argIndex + 2 };
        }
        j++;
      } else {
        j++;
      }
    }
    return { nextArgIndex: argIndex + 1 };
  }, "parseOption");
  while (i < args.length) {
    const arg = args[i];
    if (arg.startsWith("-") && arg.length > 1 && arg !== "--") {
      const parseResult = parseOption(arg, i);
      if (parseResult.nextArgIndex === -1) {
        return { stdout: "", stderr: "", exitCode: 2 };
      }
      if (parseResult.nextArgIndex === -2) {
        return { stdout: "", stderr: "", exitCode: 1 };
      }
      i = parseResult.nextArgIndex;
    } else if (arg === "--") {
      i++;
      while (i < args.length) {
        varNames.push(args[i]);
        i++;
      }
    } else {
      varNames.push(arg);
      i++;
    }
  }
  if (invalidNArg) {
    return result("", "", 1);
  }
  if (varNames.length === 0 && arrayName === null) {
    varNames.push("REPLY");
  }
  if (timeout === 0) {
    if (arrayName) {
      clearArray(ctx, arrayName);
    } else {
      for (const name of varNames) {
        ctx.state.env[name] = "";
      }
      if (varNames.length === 0) {
        ctx.state.env.REPLY = "";
      }
    }
    return result("", "", 0);
  }
  if (timeout < 0 && timeout !== -1) {
    return result("", "", 1);
  }
  let effectiveStdin = stdin;
  if (fileDescriptor >= 0) {
    if (ctx.state.fileDescriptors) {
      effectiveStdin = ctx.state.fileDescriptors.get(fileDescriptor) || "";
    } else {
      effectiveStdin = "";
    }
  } else if (!effectiveStdin && ctx.state.groupStdin !== void 0) {
    effectiveStdin = ctx.state.groupStdin;
  }
  const effectiveDelimiter = delimiter === "" ? "\0" : delimiter;
  let line = "";
  let consumed = 0;
  let foundDelimiter = true;
  const consumeInput = /* @__PURE__ */ __name$1((bytesConsumed) => {
    if (fileDescriptor >= 0 && ctx.state.fileDescriptors) {
      ctx.state.fileDescriptors.set(fileDescriptor, effectiveStdin.substring(bytesConsumed));
    } else if (stdinSourceFd >= 0 && ctx.state.fileDescriptors) {
      const fdContent = ctx.state.fileDescriptors.get(stdinSourceFd);
      if (fdContent?.startsWith("__rw__:")) {
        const parsed = parseRwFdContent2(fdContent);
        if (parsed) {
          const newPosition = parsed.position + bytesConsumed;
          ctx.state.fileDescriptors.set(stdinSourceFd, encodeRwFdContent(parsed.path, newPosition, parsed.content));
        }
      }
    } else if (ctx.state.groupStdin !== void 0 && !stdin) {
      ctx.state.groupStdin = effectiveStdin.substring(bytesConsumed);
    }
  }, "consumeInput");
  if (ncharsExact >= 0) {
    const toRead = Math.min(ncharsExact, effectiveStdin.length);
    line = effectiveStdin.substring(0, toRead);
    consumed = toRead;
    foundDelimiter = toRead >= ncharsExact;
    consumeInput(consumed);
    const varName = varNames[0] || "REPLY";
    ctx.state.env[varName] = line;
    for (let j = 1; j < varNames.length; j++) {
      ctx.state.env[varNames[j]] = "";
    }
    return result("", "", foundDelimiter ? 0 : 1);
  } else if (nchars >= 0) {
    let charCount = 0;
    let inputPos = 0;
    let hitDelimiter = false;
    while (inputPos < effectiveStdin.length && charCount < nchars) {
      const char = effectiveStdin[inputPos];
      if (char === effectiveDelimiter) {
        consumed = inputPos + 1;
        hitDelimiter = true;
        break;
      }
      if (!raw && char === "\\" && inputPos + 1 < effectiveStdin.length) {
        const nextChar = effectiveStdin[inputPos + 1];
        if (nextChar === effectiveDelimiter && effectiveDelimiter === "\n") {
          inputPos += 2;
          consumed = inputPos;
          continue;
        }
        if (nextChar === effectiveDelimiter) {
          inputPos += 2;
          charCount++;
          line += nextChar;
          consumed = inputPos;
          continue;
        }
        line += nextChar;
        inputPos += 2;
        charCount++;
        consumed = inputPos;
      } else {
        line += char;
        inputPos++;
        charCount++;
        consumed = inputPos;
      }
    }
    foundDelimiter = charCount >= nchars || hitDelimiter;
    consumeInput(consumed);
  } else {
    consumed = 0;
    let inputPos = 0;
    while (inputPos < effectiveStdin.length) {
      const char = effectiveStdin[inputPos];
      if (char === effectiveDelimiter) {
        consumed = inputPos + effectiveDelimiter.length;
        foundDelimiter = true;
        break;
      }
      if (!raw && char === "\\" && inputPos + 1 < effectiveStdin.length) {
        const nextChar = effectiveStdin[inputPos + 1];
        if (nextChar === "\n") {
          inputPos += 2;
          continue;
        }
        if (nextChar === effectiveDelimiter) {
          line += nextChar;
          inputPos += 2;
          continue;
        }
        line += char;
        line += nextChar;
        inputPos += 2;
        continue;
      }
      line += char;
      inputPos++;
    }
    if (inputPos >= effectiveStdin.length) {
      foundDelimiter = false;
      consumed = inputPos;
      if (line.length === 0 && effectiveStdin.length === 0) {
        for (const name of varNames) {
          ctx.state.env[name] = "";
        }
        if (arrayName) {
          clearArray(ctx, arrayName);
        }
        return result("", "", 1);
      }
    }
    consumeInput(consumed);
  }
  if (effectiveDelimiter === "\n" && line.endsWith("\n")) {
    line = line.slice(0, -1);
  }
  const processBackslashEscapes = /* @__PURE__ */ __name$1((s) => {
    if (raw)
      return s;
    return s.replace(/\\(.)/g, "$1");
  }, "processBackslashEscapes");
  if (varNames.length === 1 && varNames[0] === "REPLY") {
    ctx.state.env.REPLY = processBackslashEscapes(line);
    return result("", "", foundDelimiter ? 0 : 1);
  }
  const ifs = getIfs(ctx.state.env);
  if (arrayName) {
    const { words: words2 } = splitByIfsForRead(line, ifs, void 0, raw);
    clearArray(ctx, arrayName);
    for (let j = 0; j < words2.length; j++) {
      ctx.state.env[`${arrayName}_${j}`] = processBackslashEscapes(words2[j]);
    }
    return result("", "", foundDelimiter ? 0 : 1);
  }
  const maxSplit = varNames.length;
  const { words, wordStarts } = splitByIfsForRead(line, ifs, maxSplit, raw);
  for (let j = 0; j < varNames.length; j++) {
    const name = varNames[j];
    if (j < varNames.length - 1) {
      ctx.state.env[name] = processBackslashEscapes(words[j] ?? "");
    } else {
      if (j < wordStarts.length) {
        let value = line.substring(wordStarts[j]);
        value = stripTrailingIfsWhitespace(value, ifs, raw);
        value = processBackslashEscapes(value);
        ctx.state.env[name] = value;
      } else {
        ctx.state.env[name] = "";
      }
    }
  }
  return result("", "", foundDelimiter ? 0 : 1);
}
__name$1(handleRead, "handleRead");
function handleReturn(ctx, args) {
  if (ctx.state.callDepth === 0 && ctx.state.sourceDepth === 0) {
    return failure("bash: return: can only `return' from a function or sourced script\n");
  }
  let exitCode = ctx.state.lastExitCode;
  if (args.length > 0) {
    const arg = args[0];
    const n = Number.parseInt(arg, 10);
    if (arg === "" || Number.isNaN(n) || !/^-?\d+$/.test(arg)) {
      return failure(`bash: return: ${arg}: numeric argument required
`, 2);
    }
    exitCode = (n % 256 + 256) % 256;
  }
  throw new ReturnError(exitCode);
}
__name$1(handleReturn, "handleReturn");
var SET_USAGE = `set: usage: set [-eux] [+eux] [-o option] [+o option]
Options:
  -e            Exit immediately if a command exits with non-zero status
  +e            Disable -e
  -u            Treat unset variables as an error when substituting
  +u            Disable -u
  -x            Print commands and their arguments as they are executed
  +x            Disable -x
  -o errexit    Same as -e
  +o errexit    Disable errexit
  -o nounset    Same as -u
  +o nounset    Disable nounset
  -o pipefail   Return status of last failing command in pipeline
  +o pipefail   Disable pipefail
  -o xtrace     Same as -x
  +o xtrace     Disable xtrace
`;
var SHORT_OPTION_MAP = {
  e: "errexit",
  u: "nounset",
  x: "xtrace",
  v: "verbose",
  // Implemented options
  f: "noglob",
  C: "noclobber",
  a: "allexport",
  n: "noexec",
  // No-ops (accepted for compatibility)
  h: null,
  b: null,
  m: null,
  B: null,
  H: null,
  P: null,
  T: null,
  E: null,
  p: null
};
var LONG_OPTION_MAP = {
  errexit: "errexit",
  pipefail: "pipefail",
  nounset: "nounset",
  xtrace: "xtrace",
  verbose: "verbose",
  // Implemented options
  noclobber: "noclobber",
  noglob: "noglob",
  allexport: "allexport",
  noexec: "noexec",
  posix: "posix",
  vi: "vi",
  emacs: "emacs",
  // No-ops (accepted for compatibility)
  notify: null,
  monitor: null,
  braceexpand: null,
  histexpand: null,
  physical: null,
  functrace: null,
  errtrace: null,
  privileged: null,
  hashall: null,
  ignoreeof: null,
  "interactive-comments": null,
  keyword: null,
  onecmd: null
};
var DISPLAY_OPTIONS = [
  "errexit",
  "nounset",
  "pipefail",
  "verbose",
  "xtrace",
  "posix",
  "allexport",
  "noclobber",
  "noglob",
  "noexec",
  "vi",
  "emacs"
];
var NOOP_DISPLAY_OPTIONS = [
  "braceexpand",
  "errtrace",
  "functrace",
  "hashall",
  "histexpand",
  "history",
  "ignoreeof",
  "interactive-comments",
  "keyword",
  "monitor",
  "nolog",
  "notify",
  "onecmd",
  "physical",
  "privileged"
];
function setShellOption(ctx, optionKey, value) {
  if (optionKey !== null) {
    if (value) {
      if (optionKey === "vi") {
        ctx.state.options.emacs = false;
      } else if (optionKey === "emacs") {
        ctx.state.options.vi = false;
      }
    }
    ctx.state.options[optionKey] = value;
    updateShellopts(ctx);
  }
}
__name$1(setShellOption, "setShellOption");
function hasNonOptionArg(args, i) {
  return i + 1 < args.length && !args[i + 1].startsWith("-") && !args[i + 1].startsWith("+");
}
__name$1(hasNonOptionArg, "hasNonOptionArg");
function formatArrayOutput(ctx, arrayName) {
  const indices = getArrayIndices(ctx, arrayName);
  if (indices.length === 0) {
    return `${arrayName}=()`;
  }
  const elements = indices.map((i) => {
    const value = ctx.state.env[`${arrayName}_${i}`] ?? "";
    return `[${i}]=${quoteArrayValue(value)}`;
  });
  return `${arrayName}=(${elements.join(" ")})`;
}
__name$1(formatArrayOutput, "formatArrayOutput");
function quoteAssocKey(key2) {
  if (/^[a-zA-Z0-9_]+$/.test(key2)) {
    return key2;
  }
  const escaped = key2.replace(/\\/g, "\\\\").replace(/"/g, '\\"');
  return `"${escaped}"`;
}
__name$1(quoteAssocKey, "quoteAssocKey");
function formatAssocArrayOutput(ctx, arrayName) {
  const keys = getAssocArrayKeys(ctx, arrayName);
  if (keys.length === 0) {
    return `${arrayName}=()`;
  }
  const elements = keys.map((k) => {
    const value = ctx.state.env[`${arrayName}_${k}`] ?? "";
    return `[${quoteAssocKey(k)}]=${quoteArrayValue(value)}`;
  });
  return `${arrayName}=(${elements.join(" ")} )`;
}
__name$1(formatAssocArrayOutput, "formatAssocArrayOutput");
function getIndexedArrayNames(ctx) {
  const arrayNames = /* @__PURE__ */ new Set();
  const assocArrays = ctx.state.associativeArrays ?? /* @__PURE__ */ new Set();
  for (const key2 of Object.keys(ctx.state.env)) {
    const match = key2.match(/^([a-zA-Z_][a-zA-Z0-9_]*)_(\d+)$/);
    if (match) {
      const name = match[1];
      if (!assocArrays.has(name)) {
        arrayNames.add(name);
      }
    }
  }
  return arrayNames;
}
__name$1(getIndexedArrayNames, "getIndexedArrayNames");
function getAssocArrayNames(ctx) {
  return ctx.state.associativeArrays ?? /* @__PURE__ */ new Set();
}
__name$1(getAssocArrayNames, "getAssocArrayNames");
function handleSet(ctx, args) {
  if (args.includes("--help")) {
    return success(SET_USAGE);
  }
  if (args.length === 0) {
    const indexedArrayNames = getIndexedArrayNames(ctx);
    const assocArrayNames = getAssocArrayNames(ctx);
    const isAssocArrayElement = /* @__PURE__ */ __name$1((key2) => {
      for (const arrayName of assocArrayNames) {
        const prefix = `${arrayName}_`;
        const metadataSuffix = `${arrayName}__length`;
        if (key2 === metadataSuffix) {
          continue;
        }
        if (key2.startsWith(prefix)) {
          const elemKey = key2.slice(prefix.length);
          if (elemKey.startsWith("_length")) {
            continue;
          }
          return true;
        }
      }
      return false;
    }, "isAssocArrayElement");
    const scalarEntries = [];
    for (const [key2, value] of Object.entries(ctx.state.env)) {
      if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key2)) {
        continue;
      }
      if (indexedArrayNames.has(key2)) {
        continue;
      }
      if (assocArrayNames.has(key2)) {
        continue;
      }
      const arrayElementMatch = key2.match(/^([a-zA-Z_][a-zA-Z0-9_]*)_(\d+)$/);
      if (arrayElementMatch && indexedArrayNames.has(arrayElementMatch[1])) {
        continue;
      }
      const arrayMetadataMatch = key2.match(/^([a-zA-Z_][a-zA-Z0-9_]*)__length$/);
      if (arrayMetadataMatch && indexedArrayNames.has(arrayMetadataMatch[1])) {
        continue;
      }
      if (isAssocArrayElement(key2)) {
        continue;
      }
      if (arrayMetadataMatch && assocArrayNames.has(arrayMetadataMatch[1])) {
        continue;
      }
      scalarEntries.push([key2, value]);
    }
    const lines = [];
    for (const [key2, value] of scalarEntries.sort(([a], [b]) => a < b ? -1 : a > b ? 1 : 0)) {
      lines.push(`${key2}=${quoteValue(value)}`);
    }
    for (const arrayName of [...indexedArrayNames].sort((a, b) => a < b ? -1 : a > b ? 1 : 0)) {
      lines.push(formatArrayOutput(ctx, arrayName));
    }
    for (const arrayName of [...assocArrayNames].sort((a, b) => a < b ? -1 : a > b ? 1 : 0)) {
      lines.push(formatAssocArrayOutput(ctx, arrayName));
    }
    lines.sort((a, b) => {
      const nameA = a.split("=")[0];
      const nameB = b.split("=")[0];
      return nameA < nameB ? -1 : nameA > nameB ? 1 : 0;
    });
    return success(lines.length > 0 ? `${lines.join("\n")}
` : "");
  }
  let i = 0;
  while (i < args.length) {
    const arg = args[i];
    if ((arg === "-o" || arg === "+o") && hasNonOptionArg(args, i)) {
      const optName = args[i + 1];
      if (!(optName in LONG_OPTION_MAP)) {
        const errorMsg = `bash: set: ${optName}: invalid option name
${SET_USAGE}`;
        if (ctx.state.options.posix) {
          throw new PosixFatalError(1, "", errorMsg);
        }
        return failure(errorMsg);
      }
      setShellOption(ctx, LONG_OPTION_MAP[optName], arg === "-o");
      i += 2;
      continue;
    }
    if (arg === "-o") {
      const implementedOutput = DISPLAY_OPTIONS.map((opt) => `${opt.padEnd(16)}${ctx.state.options[opt] ? "on" : "off"}`);
      const noopOutput = NOOP_DISPLAY_OPTIONS.map((opt) => `${opt.padEnd(16)}off`);
      const allOptions = [...implementedOutput, ...noopOutput].sort();
      return success(`${allOptions.join("\n")}
`);
    }
    if (arg === "+o") {
      const implementedOutput = DISPLAY_OPTIONS.map((opt) => `set ${ctx.state.options[opt] ? "-o" : "+o"} ${opt}`);
      const noopOutput = NOOP_DISPLAY_OPTIONS.map((opt) => `set +o ${opt}`);
      const allOptions = [...implementedOutput, ...noopOutput].sort();
      return success(`${allOptions.join("\n")}
`);
    }
    if (arg.length > 1 && (arg[0] === "-" || arg[0] === "+") && arg[1] !== "-") {
      const enable = arg[0] === "-";
      for (let j = 1; j < arg.length; j++) {
        const flag = arg[j];
        if (!(flag in SHORT_OPTION_MAP)) {
          const errorMsg = `bash: set: ${arg[0]}${flag}: invalid option
${SET_USAGE}`;
          if (ctx.state.options.posix) {
            throw new PosixFatalError(1, "", errorMsg);
          }
          return failure(errorMsg);
        }
        setShellOption(ctx, SHORT_OPTION_MAP[flag], enable);
      }
      i++;
      continue;
    }
    if (arg === "--") {
      setPositionalParameters(ctx, args.slice(i + 1));
      return OK;
    }
    if (arg === "-") {
      ctx.state.options.xtrace = false;
      ctx.state.options.verbose = false;
      updateShellopts(ctx);
      if (i + 1 < args.length) {
        setPositionalParameters(ctx, args.slice(i + 1));
        return OK;
      }
      i++;
      continue;
    }
    if (arg === "+") {
      i++;
      continue;
    }
    if (arg.startsWith("-") || arg.startsWith("+")) {
      const errorMsg = `bash: set: ${arg}: invalid option
${SET_USAGE}`;
      if (ctx.state.options.posix) {
        throw new PosixFatalError(1, "", errorMsg);
      }
      return failure(errorMsg);
    }
    setPositionalParameters(ctx, args.slice(i));
    return OK;
  }
  return OK;
}
__name$1(handleSet, "handleSet");
function setPositionalParameters(ctx, params) {
  let i = 1;
  while (ctx.state.env[String(i)] !== void 0) {
    delete ctx.state.env[String(i)];
    i++;
  }
  for (let j = 0; j < params.length; j++) {
    ctx.state.env[String(j + 1)] = params[j];
  }
  ctx.state.env["#"] = String(params.length);
  ctx.state.env["@"] = params.join(" ");
  ctx.state.env["*"] = params.join(" ");
}
__name$1(setPositionalParameters, "setPositionalParameters");
function handleShift(ctx, args) {
  let n = 1;
  if (args.length > 0) {
    const parsed = Number.parseInt(args[0], 10);
    if (Number.isNaN(parsed) || parsed < 0) {
      const errorMsg = `bash: shift: ${args[0]}: numeric argument required
`;
      if (ctx.state.options.posix) {
        throw new PosixFatalError(1, "", errorMsg);
      }
      return failure(errorMsg);
    }
    n = parsed;
  }
  const currentCount = Number.parseInt(ctx.state.env["#"] || "0", 10);
  if (n > currentCount) {
    const errorMsg = "bash: shift: shift count out of range\n";
    if (ctx.state.options.posix) {
      throw new PosixFatalError(1, "", errorMsg);
    }
    return failure(errorMsg);
  }
  if (n === 0) {
    return OK;
  }
  const params = [];
  for (let i = 1; i <= currentCount; i++) {
    params.push(ctx.state.env[String(i)] || "");
  }
  const newParams = params.slice(n);
  for (let i = 1; i <= currentCount; i++) {
    delete ctx.state.env[String(i)];
  }
  for (let i = 0; i < newParams.length; i++) {
    ctx.state.env[String(i + 1)] = newParams[i];
  }
  ctx.state.env["#"] = String(newParams.length);
  ctx.state.env["@"] = newParams.join(" ");
  return OK;
}
__name$1(handleShift, "handleShift");
async function handleSource(ctx, args) {
  let sourceArgs = args;
  if (sourceArgs.length > 0 && sourceArgs[0] === "--") {
    sourceArgs = sourceArgs.slice(1);
  }
  if (sourceArgs.length === 0) {
    return result("", "bash: source: filename argument required\n", 2);
  }
  const filename = sourceArgs[0];
  let _resolvedPath = null;
  let content = null;
  if (filename.includes("/")) {
    const directPath = ctx.fs.resolvePath(ctx.state.cwd, filename);
    try {
      content = await ctx.fs.readFile(directPath);
      _resolvedPath = directPath;
    } catch {
    }
  } else {
    const pathEnv = ctx.state.env.PATH || "";
    const pathDirs = pathEnv.split(":").filter((d) => d);
    for (const dir of pathDirs) {
      const candidate = ctx.fs.resolvePath(ctx.state.cwd, `${dir}/${filename}`);
      try {
        const stat = await ctx.fs.stat(candidate);
        if (stat.isDirectory) {
          continue;
        }
        content = await ctx.fs.readFile(candidate);
        _resolvedPath = candidate;
        break;
      } catch {
      }
    }
    if (content === null) {
      const directPath = ctx.fs.resolvePath(ctx.state.cwd, filename);
      try {
        content = await ctx.fs.readFile(directPath);
        _resolvedPath = directPath;
      } catch {
      }
    }
  }
  if (content === null) {
    return failure(`bash: ${filename}: No such file or directory
`);
  }
  const savedPositional = {};
  if (sourceArgs.length > 1) {
    for (let i = 1; i <= 9; i++) {
      savedPositional[String(i)] = ctx.state.env[String(i)];
    }
    savedPositional["#"] = ctx.state.env["#"];
    savedPositional["@"] = ctx.state.env["@"];
    const scriptArgs = sourceArgs.slice(1);
    ctx.state.env["#"] = String(scriptArgs.length);
    ctx.state.env["@"] = scriptArgs.join(" ");
    for (let i = 0; i < scriptArgs.length && i < 9; i++) {
      ctx.state.env[String(i + 1)] = scriptArgs[i];
    }
    for (let i = scriptArgs.length + 1; i <= 9; i++) {
      delete ctx.state.env[String(i)];
    }
  }
  const savedSource = ctx.state.currentSource;
  const cleanup = /* @__PURE__ */ __name$1(() => {
    ctx.state.sourceDepth--;
    ctx.state.currentSource = savedSource;
    if (sourceArgs.length > 1) {
      for (const [key2, value] of Object.entries(savedPositional)) {
        if (value === void 0) {
          delete ctx.state.env[key2];
        } else {
          ctx.state.env[key2] = value;
        }
      }
    }
  }, "cleanup");
  ctx.state.sourceDepth++;
  ctx.state.currentSource = filename;
  try {
    const ast = parse(content);
    const result2 = await ctx.executeScript(ast);
    cleanup();
    return result2;
  } catch (error) {
    cleanup();
    if (error instanceof ExitError) {
      throw error;
    }
    if (error instanceof ReturnError) {
      return result(error.stdout, error.stderr, error.exitCode);
    }
    if (error.name === "ParseException") {
      return failure(`bash: ${filename}: ${error.message}
`);
    }
    throw error;
  }
}
__name$1(handleSource, "handleSource");
function isValidVariableName(name) {
  return /^[a-zA-Z_][a-zA-Z0-9_]*$/.test(name);
}
__name$1(isValidVariableName, "isValidVariableName");
function isQuotedStringIndex(indexExpr) {
  return indexExpr.startsWith("'") && indexExpr.endsWith("'") || indexExpr.startsWith('"') && indexExpr.endsWith('"');
}
__name$1(isQuotedStringIndex, "isQuotedStringIndex");
async function evaluateArrayIndex2(ctx, indexExpr) {
  if (isQuotedStringIndex(indexExpr)) {
    return null;
  }
  try {
    const parser = new Parser();
    const arithAst = parseArithmeticExpression(parser, indexExpr);
    return await evaluateArithmetic(ctx, arithAst.expression);
  } catch {
    const num = parseInt(indexExpr, 10);
    return Number.isNaN(num) ? 0 : num;
  }
}
__name$1(evaluateArrayIndex2, "evaluateArrayIndex");
function performCellUnset(ctx, varName) {
  const hasStackEntry = ctx.state.localVarStack?.has(varName);
  if (hasStackEntry) {
    const stackEntry = popLocalVarStack(ctx, varName);
    if (stackEntry) {
      if (stackEntry.value === void 0) {
        delete ctx.state.env[varName];
      } else {
        ctx.state.env[varName] = stackEntry.value;
      }
      const remainingStack = ctx.state.localVarStack?.get(varName);
      if (!remainingStack || remainingStack.length === 0) {
        clearLocalVarDepth(ctx, varName);
        ctx.state.localVarStack?.delete(varName);
        ctx.state.fullyUnsetLocals = ctx.state.fullyUnsetLocals || /* @__PURE__ */ new Map();
        ctx.state.fullyUnsetLocals.set(varName, stackEntry.scopeIndex);
        if (handleTempEnvUnset(ctx, varName)) ;
      } else {
        const topEntry = remainingStack[remainingStack.length - 1];
        ctx.state.localVarDepth = ctx.state.localVarDepth || /* @__PURE__ */ new Map();
        ctx.state.localVarDepth.set(varName, topEntry.scopeIndex + 1);
      }
      return true;
    }
    delete ctx.state.env[varName];
    clearLocalVarDepth(ctx, varName);
    ctx.state.localVarStack?.delete(varName);
    ctx.state.fullyUnsetLocals = ctx.state.fullyUnsetLocals || /* @__PURE__ */ new Map();
    ctx.state.fullyUnsetLocals.set(varName, 0);
    return true;
  }
  for (let i = ctx.state.localScopes.length - 1; i >= 0; i--) {
    const scope = ctx.state.localScopes[i];
    if (scope.has(varName)) {
      const outerValue = scope.get(varName);
      if (outerValue === void 0) {
        delete ctx.state.env[varName];
      } else {
        ctx.state.env[varName] = outerValue;
      }
      scope.delete(varName);
      let foundOuterScope = false;
      for (let j = i - 1; j >= 0; j--) {
        if (ctx.state.localScopes[j].has(varName)) {
          if (ctx.state.localVarDepth) {
            ctx.state.localVarDepth.set(varName, j + 1);
          }
          foundOuterScope = true;
          break;
        }
      }
      if (!foundOuterScope) {
        clearLocalVarDepth(ctx, varName);
      }
      return true;
    }
  }
  return false;
}
__name$1(performCellUnset, "performCellUnset");
function handleTempEnvUnset(ctx, varName) {
  if (!ctx.state.tempEnvBindings || ctx.state.tempEnvBindings.length === 0) {
    return false;
  }
  for (let i = ctx.state.tempEnvBindings.length - 1; i >= 0; i--) {
    const bindings = ctx.state.tempEnvBindings[i];
    if (bindings.has(varName)) {
      const underlyingValue = bindings.get(varName);
      if (underlyingValue === void 0) {
        delete ctx.state.env[varName];
      } else {
        ctx.state.env[varName] = underlyingValue;
      }
      bindings.delete(varName);
      return true;
    }
  }
  return false;
}
__name$1(handleTempEnvUnset, "handleTempEnvUnset");
async function expandAssocSubscript(ctx, subscriptExpr) {
  if (subscriptExpr.startsWith("'") && subscriptExpr.endsWith("'")) {
    return subscriptExpr.slice(1, -1);
  }
  if (subscriptExpr.startsWith('"') && subscriptExpr.endsWith('"')) {
    const inner = subscriptExpr.slice(1, -1);
    const parser = new Parser();
    const wordNode = parser.parseWordFromString(inner, true, false);
    return expandWord(ctx, wordNode);
  }
  if (subscriptExpr.includes("$")) {
    const parser = new Parser();
    const wordNode = parser.parseWordFromString(subscriptExpr, false, false);
    return expandWord(ctx, wordNode);
  }
  return subscriptExpr;
}
__name$1(expandAssocSubscript, "expandAssocSubscript");
async function handleUnset(ctx, args) {
  let mode = "both";
  let stderr = "";
  let exitCode = 0;
  for (const arg of args) {
    if (arg === "-v") {
      mode = "variable";
      continue;
    }
    if (arg === "-f") {
      mode = "function";
      continue;
    }
    if (mode === "function") {
      ctx.state.functions.delete(arg);
      continue;
    }
    if (mode === "variable") {
      const arrayMatchVar = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
      if (arrayMatchVar) {
        const arrayName = arrayMatchVar[1];
        const indexExpr = arrayMatchVar[2];
        if (indexExpr === "@" || indexExpr === "*") {
          const elements = getArrayElements(ctx, arrayName);
          for (const [idx] of elements) {
            delete ctx.state.env[`${arrayName}_${idx}`];
          }
          delete ctx.state.env[arrayName];
          continue;
        }
        const isAssoc = ctx.state.associativeArrays?.has(arrayName);
        if (isAssoc) {
          const key2 = await expandAssocSubscript(ctx, indexExpr);
          delete ctx.state.env[`${arrayName}_${key2}`];
          continue;
        }
        const isIndexedArray = isArray(ctx, arrayName);
        const isDeclaredButUnset = ctx.state.declaredVars?.has(arrayName);
        const isScalar = (arrayName in ctx.state.env || isDeclaredButUnset) && !isIndexedArray && !isAssoc;
        if (isScalar) {
          stderr += `bash: unset: ${arrayName}: not an array variable
`;
          exitCode = 1;
          continue;
        }
        const index = await evaluateArrayIndex2(ctx, indexExpr);
        if (index === null && isIndexedArray) {
          stderr += `bash: unset: ${indexExpr}: not a valid identifier
`;
          exitCode = 1;
          continue;
        }
        if (index === null) {
          continue;
        }
        if (index < 0) {
          const elements = getArrayElements(ctx, arrayName);
          const len = elements.length;
          const lineNum = ctx.state.currentLine;
          if (len === 0) {
            stderr += `bash: line ${lineNum}: unset: [${index}]: bad array subscript
`;
            exitCode = 1;
            continue;
          }
          const actualPos = len + index;
          if (actualPos < 0) {
            stderr += `bash: line ${lineNum}: unset: [${index}]: bad array subscript
`;
            exitCode = 1;
            continue;
          }
          const actualIndex = elements[actualPos][0];
          delete ctx.state.env[`${arrayName}_${actualIndex}`];
          continue;
        }
        delete ctx.state.env[`${arrayName}_${index}`];
        continue;
      }
      if (!isValidVariableName(arg)) {
        stderr += `bash: unset: \`${arg}': not a valid identifier
`;
        exitCode = 1;
        continue;
      }
      let targetName2 = arg;
      if (isNameref(ctx, arg)) {
        const resolved = resolveNameref(ctx, arg);
        if (resolved && resolved !== arg) {
          targetName2 = resolved;
        }
      }
      if (isReadonly(ctx, targetName2)) {
        stderr += `bash: unset: ${targetName2}: cannot unset: readonly variable
`;
        exitCode = 1;
        continue;
      }
      const localDepth2 = getLocalVarDepth(ctx, targetName2);
      if (localDepth2 !== void 0 && localDepth2 !== ctx.state.callDepth) {
        performCellUnset(ctx, targetName2);
      } else if (ctx.state.fullyUnsetLocals?.has(targetName2)) {
        delete ctx.state.env[targetName2];
      } else if (localDepth2 !== void 0) {
        const tempEnvAccessed = ctx.state.accessedTempEnvVars?.has(targetName2);
        const tempEnvMutated = ctx.state.mutatedTempEnvVars?.has(targetName2);
        if ((tempEnvAccessed || tempEnvMutated) && ctx.state.localVarStack?.has(targetName2)) {
          const stackEntry = popLocalVarStack(ctx, targetName2);
          if (stackEntry) {
            if (stackEntry.value === void 0) {
              delete ctx.state.env[targetName2];
            } else {
              ctx.state.env[targetName2] = stackEntry.value;
            }
          } else {
            delete ctx.state.env[targetName2];
          }
        } else {
          delete ctx.state.env[targetName2];
        }
      } else if (!handleTempEnvUnset(ctx, targetName2)) {
        delete ctx.state.env[targetName2];
      }
      ctx.state.exportedVars?.delete(targetName2);
      continue;
    }
    const arrayMatch = arg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
    if (arrayMatch) {
      const arrayName = arrayMatch[1];
      const indexExpr = arrayMatch[2];
      if (indexExpr === "@" || indexExpr === "*") {
        const elements = getArrayElements(ctx, arrayName);
        for (const [idx] of elements) {
          delete ctx.state.env[`${arrayName}_${idx}`];
        }
        delete ctx.state.env[arrayName];
        continue;
      }
      const isAssoc = ctx.state.associativeArrays?.has(arrayName);
      if (isAssoc) {
        const key2 = await expandAssocSubscript(ctx, indexExpr);
        delete ctx.state.env[`${arrayName}_${key2}`];
        continue;
      }
      const isIndexedArray = isArray(ctx, arrayName);
      const isScalar = arrayName in ctx.state.env && !isIndexedArray && !isAssoc;
      if (isScalar) {
        stderr += `bash: unset: ${arrayName}: not an array variable
`;
        exitCode = 1;
        continue;
      }
      const index = await evaluateArrayIndex2(ctx, indexExpr);
      if (index === null && isIndexedArray) {
        stderr += `bash: unset: ${indexExpr}: not a valid identifier
`;
        exitCode = 1;
        continue;
      }
      if (index === null) {
        continue;
      }
      if (index < 0) {
        const elements = getArrayElements(ctx, arrayName);
        const len = elements.length;
        const lineNum = ctx.state.currentLine;
        if (len === 0) {
          stderr += `bash: line ${lineNum}: unset: [${index}]: bad array subscript
`;
          exitCode = 1;
          continue;
        }
        const actualPos = len + index;
        if (actualPos < 0) {
          stderr += `bash: line ${lineNum}: unset: [${index}]: bad array subscript
`;
          exitCode = 1;
          continue;
        }
        const actualIndex = elements[actualPos][0];
        delete ctx.state.env[`${arrayName}_${actualIndex}`];
        continue;
      }
      delete ctx.state.env[`${arrayName}_${index}`];
      continue;
    }
    if (!isValidVariableName(arg)) {
      stderr += `bash: unset: \`${arg}': not a valid identifier
`;
      exitCode = 1;
      continue;
    }
    let targetName = arg;
    if (isNameref(ctx, arg)) {
      const resolved = resolveNameref(ctx, arg);
      if (resolved && resolved !== arg) {
        targetName = resolved;
      }
    }
    if (isReadonly(ctx, targetName)) {
      stderr += `bash: unset: ${targetName}: cannot unset: readonly variable
`;
      exitCode = 1;
      continue;
    }
    const localDepth = getLocalVarDepth(ctx, targetName);
    if (localDepth !== void 0 && localDepth !== ctx.state.callDepth) {
      performCellUnset(ctx, targetName);
    } else if (ctx.state.fullyUnsetLocals?.has(targetName)) {
      delete ctx.state.env[targetName];
    } else if (localDepth !== void 0) {
      const tempEnvAccessed = ctx.state.accessedTempEnvVars?.has(targetName);
      const tempEnvMutated = ctx.state.mutatedTempEnvVars?.has(targetName);
      if ((tempEnvAccessed || tempEnvMutated) && ctx.state.localVarStack?.has(targetName)) {
        const stackEntry = popLocalVarStack(ctx, targetName);
        if (stackEntry) {
          if (stackEntry.value === void 0) {
            delete ctx.state.env[targetName];
          } else {
            ctx.state.env[targetName] = stackEntry.value;
          }
        } else {
          delete ctx.state.env[targetName];
        }
      } else {
        delete ctx.state.env[targetName];
      }
    } else if (!handleTempEnvUnset(ctx, targetName)) {
      delete ctx.state.env[targetName];
    }
    ctx.state.exportedVars?.delete(targetName);
    ctx.state.functions.delete(arg);
  }
  return result("", stderr, exitCode);
}
__name$1(handleUnset, "handleUnset");
var SHOPT_OPTIONS2 = [
  "extglob",
  "dotglob",
  "nullglob",
  "failglob",
  "globstar",
  "globskipdots",
  "nocaseglob",
  "nocasematch",
  "expand_aliases",
  "lastpipe",
  "xpg_echo"
];
var STUB_OPTIONS = [
  "autocd",
  "cdable_vars",
  "cdspell",
  "checkhash",
  "checkjobs",
  "checkwinsize",
  "cmdhist",
  "compat31",
  "compat32",
  "compat40",
  "compat41",
  "compat42",
  "compat43",
  "compat44",
  "complete_fullquote",
  "direxpand",
  "dirspell",
  "execfail",
  "extdebug",
  "extquote",
  "force_fignore",
  "globasciiranges",
  "gnu_errfmt",
  "histappend",
  "histreedit",
  "histverify",
  "hostcomplete",
  "huponexit",
  "inherit_errexit",
  "interactive_comments",
  "lithist",
  "localvar_inherit",
  "localvar_unset",
  "login_shell",
  "mailwarn",
  "no_empty_cmd_completion",
  "progcomp",
  "progcomp_alias",
  "promptvars",
  "restricted_shell",
  "shift_verbose",
  "sourcepath"
];
function isShoptOption(opt) {
  return SHOPT_OPTIONS2.includes(opt);
}
__name$1(isShoptOption, "isShoptOption");
function isStubOption(opt) {
  return STUB_OPTIONS.includes(opt);
}
__name$1(isStubOption, "isStubOption");
function handleShopt(ctx, args) {
  let setFlag = false;
  let unsetFlag = false;
  let printFlag = false;
  let quietFlag = false;
  let oFlag = false;
  const optionNames = [];
  let i = 0;
  while (i < args.length) {
    const arg = args[i];
    if (arg === "--") {
      i++;
      break;
    }
    if (arg.startsWith("-") && arg.length > 1) {
      for (let j = 1; j < arg.length; j++) {
        const flag = arg[j];
        switch (flag) {
          case "s":
            setFlag = true;
            break;
          case "u":
            unsetFlag = true;
            break;
          case "p":
            printFlag = true;
            break;
          case "q":
            quietFlag = true;
            break;
          case "o":
            oFlag = true;
            break;
          default:
            return {
              exitCode: 2,
              stdout: "",
              stderr: `shopt: -${flag}: invalid option
`
            };
        }
      }
      i++;
    } else {
      break;
    }
  }
  while (i < args.length) {
    optionNames.push(args[i]);
    i++;
  }
  if (oFlag) {
    return handleSetOptions(ctx, optionNames, setFlag, unsetFlag, printFlag, quietFlag);
  }
  if (setFlag && unsetFlag) {
    return {
      exitCode: 1,
      stdout: "",
      stderr: "shopt: cannot set and unset shell options simultaneously\n"
    };
  }
  if (optionNames.length === 0) {
    if (setFlag || unsetFlag) {
      const output3 = [];
      for (const opt of SHOPT_OPTIONS2) {
        const value = ctx.state.shoptOptions[opt];
        if (setFlag && value) {
          output3.push(printFlag ? `shopt -s ${opt}` : `${opt}		on`);
        } else if (unsetFlag && !value) {
          output3.push(printFlag ? `shopt -u ${opt}` : `${opt}		off`);
        }
      }
      return {
        exitCode: 0,
        stdout: output3.length > 0 ? `${output3.join("\n")}
` : "",
        stderr: ""
      };
    }
    const output2 = [];
    for (const opt of SHOPT_OPTIONS2) {
      const value = ctx.state.shoptOptions[opt];
      output2.push(printFlag ? `shopt ${value ? "-s" : "-u"} ${opt}` : `${opt}		${value ? "on" : "off"}`);
    }
    return {
      exitCode: 0,
      stdout: `${output2.join("\n")}
`,
      stderr: ""
    };
  }
  let hasError = false;
  let stderr = "";
  const output = [];
  for (const name of optionNames) {
    if (!isShoptOption(name) && !isStubOption(name)) {
      stderr += `shopt: ${name}: invalid shell option name
`;
      hasError = true;
      continue;
    }
    if (setFlag) {
      if (isShoptOption(name)) {
        ctx.state.shoptOptions[name] = true;
        updateBashopts(ctx);
      }
    } else if (unsetFlag) {
      if (isShoptOption(name)) {
        ctx.state.shoptOptions[name] = false;
        updateBashopts(ctx);
      }
    } else {
      if (isShoptOption(name)) {
        const value = ctx.state.shoptOptions[name];
        if (quietFlag) {
          if (!value) {
            hasError = true;
          }
        } else if (printFlag) {
          output.push(`shopt ${value ? "-s" : "-u"} ${name}`);
          if (!value) {
            hasError = true;
          }
        } else {
          output.push(`${name}		${value ? "on" : "off"}`);
          if (!value) {
            hasError = true;
          }
        }
      } else {
        if (quietFlag) {
          hasError = true;
        } else if (printFlag) {
          output.push(`shopt -u ${name}`);
          hasError = true;
        } else {
          output.push(`${name}		off`);
          hasError = true;
        }
      }
    }
  }
  return {
    exitCode: hasError ? 1 : 0,
    stdout: output.length > 0 ? `${output.join("\n")}
` : "",
    stderr
  };
}
__name$1(handleShopt, "handleShopt");
function handleSetOptions(ctx, optionNames, setFlag, unsetFlag, printFlag, quietFlag) {
  const SET_OPTIONS = {
    errexit: "errexit",
    pipefail: "pipefail",
    nounset: "nounset",
    xtrace: "xtrace",
    verbose: "verbose",
    posix: "posix",
    allexport: "allexport",
    noclobber: "noclobber",
    noglob: "noglob",
    noexec: "noexec",
    vi: "vi",
    emacs: "emacs"
  };
  const NOOP_OPTIONS = [
    "braceexpand",
    "errtrace",
    "functrace",
    "hashall",
    "histexpand",
    "history",
    "ignoreeof",
    "interactive-comments",
    "keyword",
    "monitor",
    "nolog",
    "notify",
    "onecmd",
    "physical",
    "privileged"
  ];
  const ALL_SET_OPTIONS = [...Object.keys(SET_OPTIONS), ...NOOP_OPTIONS].sort();
  if (optionNames.length === 0) {
    const output2 = [];
    for (const opt of ALL_SET_OPTIONS) {
      const isNoOp = NOOP_OPTIONS.includes(opt);
      const value = isNoOp ? false : ctx.state.options[SET_OPTIONS[opt]];
      if (setFlag && !value)
        continue;
      if (unsetFlag && value)
        continue;
      output2.push(printFlag ? `set ${value ? "-o" : "+o"} ${opt}` : `${opt}		${value ? "on" : "off"}`);
    }
    return {
      exitCode: 0,
      stdout: output2.length > 0 ? `${output2.join("\n")}
` : "",
      stderr: ""
    };
  }
  let hasError = false;
  let stderr = "";
  const output = [];
  for (const name of optionNames) {
    const isImplemented = name in SET_OPTIONS;
    const isNoOp = NOOP_OPTIONS.includes(name);
    if (!isImplemented && !isNoOp) {
      stderr += `shopt: ${name}: invalid option name
`;
      hasError = true;
      continue;
    }
    if (isNoOp) {
      if (setFlag || unsetFlag) ;
      else {
        if (quietFlag) {
          hasError = true;
        } else if (printFlag) {
          output.push(`set +o ${name}`);
          hasError = true;
        } else {
          output.push(`${name}		off`);
          hasError = true;
        }
      }
      continue;
    }
    const key2 = SET_OPTIONS[name];
    if (setFlag) {
      if (key2 === "vi") {
        ctx.state.options.emacs = false;
      } else if (key2 === "emacs") {
        ctx.state.options.vi = false;
      }
      ctx.state.options[key2] = true;
      updateShellopts(ctx);
    } else if (unsetFlag) {
      ctx.state.options[key2] = false;
      updateShellopts(ctx);
    } else {
      const value = ctx.state.options[key2];
      if (quietFlag) {
        if (!value) {
          hasError = true;
        }
      } else if (printFlag) {
        output.push(`set ${value ? "-o" : "+o"} ${name}`);
        if (!value) {
          hasError = true;
        }
      } else {
        output.push(`${name}		${value ? "on" : "off"}`);
        if (!value) {
          hasError = true;
        }
      }
    }
  }
  return {
    exitCode: hasError ? 1 : 0,
    stdout: output.length > 0 ? `${output.join("\n")}
` : "",
    stderr
  };
}
__name$1(handleSetOptions, "handleSetOptions");
async function resolveCommand(ctx, commandName, pathOverride) {
  if (commandName.includes("/")) {
    const resolvedPath = ctx.fs.resolvePath(ctx.state.cwd, commandName);
    if (!await ctx.fs.exists(resolvedPath)) {
      return { error: "not_found", path: resolvedPath };
    }
    const cmdName = resolvedPath.split("/").pop() || commandName;
    const cmd = ctx.commands.get(cmdName);
    try {
      const stat = await ctx.fs.stat(resolvedPath);
      if (stat.isDirectory) {
        return { error: "permission_denied", path: resolvedPath };
      }
      if (cmd) {
        return { cmd, path: resolvedPath };
      }
      const isExecutable = (stat.mode & 73) !== 0;
      if (!isExecutable) {
        return { error: "permission_denied", path: resolvedPath };
      }
      return { script: true, path: resolvedPath };
    } catch {
      return { error: "not_found", path: resolvedPath };
    }
  }
  if (!pathOverride && ctx.state.hashTable) {
    const cachedPath = ctx.state.hashTable.get(commandName);
    if (cachedPath) {
      if (await ctx.fs.exists(cachedPath)) {
        const cmd = ctx.commands.get(commandName);
        if (cmd) {
          return { cmd, path: cachedPath };
        }
        try {
          const stat = await ctx.fs.stat(cachedPath);
          if (!stat.isDirectory && (stat.mode & 73) !== 0) {
            return { script: true, path: cachedPath };
          }
        } catch {
        }
      } else {
        ctx.state.hashTable.delete(commandName);
      }
    }
  }
  const pathEnv = pathOverride ?? ctx.state.env.PATH ?? "/usr/bin:/bin";
  const pathDirs = pathEnv.split(":");
  for (const dir of pathDirs) {
    if (!dir)
      continue;
    const resolvedDir = dir.startsWith("/") ? dir : ctx.fs.resolvePath(ctx.state.cwd, dir);
    const fullPath = `${resolvedDir}/${commandName}`;
    if (await ctx.fs.exists(fullPath)) {
      try {
        const stat = await ctx.fs.stat(fullPath);
        if (stat.isDirectory) {
          continue;
        }
        const isExecutable = (stat.mode & 73) !== 0;
        const cmd = ctx.commands.get(commandName);
        const isSystemDir = dir === "/bin" || dir === "/usr/bin";
        if (cmd && isSystemDir) {
          return { cmd, path: fullPath };
        }
        if (isExecutable) {
          if (cmd && !isSystemDir) {
            return { script: true, path: fullPath };
          }
          if (!cmd) {
            return { script: true, path: fullPath };
          }
        }
      } catch {
      }
    }
  }
  const usrBinExists = await ctx.fs.exists("/usr/bin");
  if (!usrBinExists) {
    const cmd = ctx.commands.get(commandName);
    if (cmd) {
      return { cmd, path: `/usr/bin/${commandName}` };
    }
  }
  return null;
}
__name$1(resolveCommand, "resolveCommand");
async function findCommandInPath(ctx, commandName) {
  const paths = [];
  if (commandName.includes("/")) {
    const resolvedPath = ctx.fs.resolvePath(ctx.state.cwd, commandName);
    if (await ctx.fs.exists(resolvedPath)) {
      try {
        const stat = await ctx.fs.stat(resolvedPath);
        if (!stat.isDirectory) {
          const isExecutable = (stat.mode & 73) !== 0;
          if (isExecutable) {
            paths.push(commandName);
          }
        }
      } catch {
      }
    }
    return paths;
  }
  const pathEnv = ctx.state.env.PATH || "/usr/bin:/bin";
  const pathDirs = pathEnv.split(":");
  for (const dir of pathDirs) {
    if (!dir)
      continue;
    const resolvedDir = dir.startsWith("/") ? dir : ctx.fs.resolvePath(ctx.state.cwd, dir);
    const fullPath = `${resolvedDir}/${commandName}`;
    if (await ctx.fs.exists(fullPath)) {
      try {
        const stat = await ctx.fs.stat(fullPath);
        if (stat.isDirectory) {
          continue;
        }
      } catch {
        continue;
      }
      paths.push(dir.startsWith("/") ? fullPath : `${dir}/${commandName}`);
    }
  }
  return paths;
}
__name$1(findCommandInPath, "findCommandInPath");
async function handleType(ctx, args, findFirstInPath2, findCommandInPath2) {
  let typeOnly = false;
  let pathOnly = false;
  let forcePathSearch = false;
  let showAll = false;
  let suppressFunctions = false;
  const names = [];
  for (const arg of args) {
    if (arg.startsWith("-") && arg.length > 1) {
      for (const char of arg.slice(1)) {
        if (char === "t") {
          typeOnly = true;
        } else if (char === "p") {
          pathOnly = true;
        } else if (char === "P") {
          forcePathSearch = true;
        } else if (char === "a") {
          showAll = true;
        } else if (char === "f") {
          suppressFunctions = true;
        }
      }
    } else {
      names.push(arg);
    }
  }
  let stdout = "";
  let stderr = "";
  let exitCode = 0;
  let anyFileFound = false;
  let anyNotFound = false;
  for (const name of names) {
    let foundAny = false;
    if (forcePathSearch) {
      if (showAll) {
        const allPaths = await findCommandInPath2(name);
        if (allPaths.length > 0) {
          for (const p of allPaths) {
            stdout += `${p}
`;
          }
          anyFileFound = true;
          foundAny = true;
        }
      } else {
        const pathResult = await findFirstInPath2(name);
        if (pathResult) {
          stdout += `${pathResult}
`;
          anyFileFound = true;
          foundAny = true;
        }
      }
      if (!foundAny) {
        anyNotFound = true;
      }
      continue;
    }
    const hasFunction = !suppressFunctions && ctx.state.functions.has(name);
    if (showAll && hasFunction) {
      if (pathOnly) ;
      else if (typeOnly) {
        stdout += "function\n";
      } else {
        const funcDef = ctx.state.functions.get(name);
        const funcSource = funcDef ? formatFunctionSource(name, funcDef) : `${name} is a function
`;
        stdout += funcSource;
      }
      foundAny = true;
    }
    const alias = ctx.state.env[`BASH_ALIAS_${name}`];
    const hasAlias = alias !== void 0;
    if (hasAlias && (showAll || !foundAny)) {
      if (pathOnly) ;
      else if (typeOnly) {
        stdout += "alias\n";
      } else {
        stdout += `${name} is aliased to \`${alias}'
`;
      }
      foundAny = true;
      if (!showAll) {
        continue;
      }
    }
    const hasKeyword = SHELL_KEYWORDS.has(name);
    if (hasKeyword && (showAll || !foundAny)) {
      if (pathOnly) ;
      else if (typeOnly) {
        stdout += "keyword\n";
      } else {
        stdout += `${name} is a shell keyword
`;
      }
      foundAny = true;
      if (!showAll) {
        continue;
      }
    }
    if (!showAll && hasFunction && !foundAny) {
      if (pathOnly) ;
      else if (typeOnly) {
        stdout += "function\n";
      } else {
        const funcDef = ctx.state.functions.get(name);
        const funcSource = funcDef ? formatFunctionSource(name, funcDef) : `${name} is a function
`;
        stdout += funcSource;
      }
      foundAny = true;
      continue;
    }
    const hasBuiltin = SHELL_BUILTINS.has(name);
    if (hasBuiltin && (showAll || !foundAny)) {
      if (pathOnly) ;
      else if (typeOnly) {
        stdout += "builtin\n";
      } else {
        stdout += `${name} is a shell builtin
`;
      }
      foundAny = true;
      if (!showAll) {
        continue;
      }
    }
    if (showAll) {
      const allPaths = await findCommandInPath2(name);
      for (const pathResult of allPaths) {
        if (pathOnly) {
          stdout += `${pathResult}
`;
        } else if (typeOnly) {
          stdout += "file\n";
        } else {
          stdout += `${name} is ${pathResult}
`;
        }
        anyFileFound = true;
        foundAny = true;
      }
    } else if (!foundAny) {
      const pathResult = await findFirstInPath2(name);
      if (pathResult) {
        if (pathOnly) {
          stdout += `${pathResult}
`;
        } else if (typeOnly) {
          stdout += "file\n";
        } else {
          stdout += `${name} is ${pathResult}
`;
        }
        anyFileFound = true;
        foundAny = true;
      }
    }
    if (!foundAny) {
      anyNotFound = true;
      if (!typeOnly && !pathOnly) {
        let shouldPrintError = true;
        if (name.includes("/")) {
          const resolvedPath = ctx.fs.resolvePath(ctx.state.cwd, name);
          if (await ctx.fs.exists(resolvedPath)) {
            shouldPrintError = false;
          }
        }
        if (shouldPrintError) {
          stderr += `bash: type: ${name}: not found
`;
        }
      }
    }
  }
  if (pathOnly) {
    exitCode = anyNotFound && !anyFileFound ? 1 : 0;
  } else if (forcePathSearch) {
    exitCode = anyNotFound ? 1 : 0;
  } else {
    exitCode = anyNotFound ? 1 : 0;
  }
  return result(stdout, stderr, exitCode);
}
__name$1(handleType, "handleType");
function formatFunctionSource(name, funcDef) {
  let bodyStr;
  if (funcDef.body.type === "Group") {
    const group = funcDef.body;
    bodyStr = group.body.map((s) => serializeCompoundCommand(s)).join("; ");
  } else {
    bodyStr = serializeCompoundCommand(funcDef.body);
  }
  return `${name} is a function
${name} () 
{ 
    ${bodyStr}
}
`;
}
__name$1(formatFunctionSource, "formatFunctionSource");
function serializeCompoundCommand(node) {
  if (Array.isArray(node)) {
    return node.map((s) => serializeCompoundCommand(s)).join("; ");
  }
  if (node.type === "Statement") {
    const parts = [];
    for (let i = 0; i < node.pipelines.length; i++) {
      const pipeline = node.pipelines[i];
      parts.push(serializePipeline(pipeline));
      if (node.operators[i]) {
        parts.push(node.operators[i]);
      }
    }
    return parts.join(" ");
  }
  if (node.type === "SimpleCommand") {
    const cmd = node;
    const parts = [];
    if (cmd.name) {
      parts.push(serializeWord(cmd.name));
    }
    for (const arg of cmd.args) {
      parts.push(serializeWord(arg));
    }
    return parts.join(" ");
  }
  if (node.type === "Group") {
    const group = node;
    const body = group.body.map((s) => serializeCompoundCommand(s)).join("; ");
    return `{ ${body}; }`;
  }
  return "...";
}
__name$1(serializeCompoundCommand, "serializeCompoundCommand");
function serializePipeline(pipeline) {
  const parts = pipeline.commands.map((cmd) => serializeCompoundCommand(cmd));
  return (pipeline.negated ? "! " : "") + parts.join(" | ");
}
__name$1(serializePipeline, "serializePipeline");
function serializeWord(word) {
  let result2 = "";
  for (const part of word.parts) {
    if (part.type === "Literal") {
      result2 += part.value;
    } else if (part.type === "DoubleQuoted") {
      result2 += `"${part.parts.map((p) => serializeWordPart(p)).join("")}"`;
    } else if (part.type === "SingleQuoted") {
      result2 += `'${part.value}'`;
    } else {
      result2 += serializeWordPart(part);
    }
  }
  return result2;
}
__name$1(serializeWord, "serializeWord");
function serializeWordPart(part) {
  const p = part;
  if (p.type === "Literal") {
    return p.value ?? "";
  }
  if (p.type === "Variable") {
    return `$${p.name}`;
  }
  return "";
}
__name$1(serializeWordPart, "serializeWordPart");
async function handleCommandV(ctx, names, _showPath, verboseDescribe) {
  let stdout = "";
  let stderr = "";
  let exitCode = 0;
  for (const name of names) {
    if (!name) {
      exitCode = 1;
      continue;
    }
    const alias = ctx.state.env[`BASH_ALIAS_${name}`];
    if (alias !== void 0) {
      if (verboseDescribe) {
        stdout += `${name} is an alias for "${alias}"
`;
      } else {
        stdout += `alias ${name}='${alias}'
`;
      }
    } else if (SHELL_KEYWORDS.has(name)) {
      if (verboseDescribe) {
        stdout += `${name} is a shell keyword
`;
      } else {
        stdout += `${name}
`;
      }
    } else if (SHELL_BUILTINS.has(name)) {
      if (verboseDescribe) {
        stdout += `${name} is a shell builtin
`;
      } else {
        stdout += `${name}
`;
      }
    } else if (ctx.state.functions.has(name)) {
      if (verboseDescribe) {
        stdout += `${name} is a function
`;
      } else {
        stdout += `${name}
`;
      }
    } else if (name.includes("/")) {
      const resolvedPath = ctx.fs.resolvePath(ctx.state.cwd, name);
      let found = false;
      if (await ctx.fs.exists(resolvedPath)) {
        try {
          const stat = await ctx.fs.stat(resolvedPath);
          if (!stat.isDirectory) {
            const isExecutable = (stat.mode & 73) !== 0;
            if (isExecutable) {
              if (verboseDescribe) {
                stdout += `${name} is ${name}
`;
              } else {
                stdout += `${name}
`;
              }
              found = true;
            }
          }
        } catch {
        }
      }
      if (!found) {
        if (verboseDescribe) {
          stderr += `${name}: not found
`;
        }
        exitCode = 1;
      }
    } else if (ctx.commands.has(name)) {
      const pathEnv = ctx.state.env.PATH ?? "/usr/bin:/bin";
      const pathDirs = pathEnv.split(":");
      let foundPath = null;
      for (const dir of pathDirs) {
        if (!dir)
          continue;
        const cmdPath = `${dir}/${name}`;
        try {
          const stat = await ctx.fs.stat(cmdPath);
          if (!stat.isDirectory && (stat.mode & 73) !== 0) {
            foundPath = cmdPath;
            break;
          }
        } catch {
        }
      }
      if (!foundPath) {
        foundPath = `/usr/bin/${name}`;
      }
      if (verboseDescribe) {
        stdout += `${name} is ${foundPath}
`;
      } else {
        stdout += `${foundPath}
`;
      }
    } else {
      if (verboseDescribe) {
        stderr += `${name}: not found
`;
      }
      exitCode = 1;
    }
  }
  return result(stdout, stderr, exitCode);
}
__name$1(handleCommandV, "handleCommandV");
async function findFirstInPath(ctx, name) {
  if (name.includes("/")) {
    const resolvedPath = ctx.fs.resolvePath(ctx.state.cwd, name);
    if (await ctx.fs.exists(resolvedPath)) {
      try {
        const stat = await ctx.fs.stat(resolvedPath);
        if (stat.isDirectory) {
          return null;
        }
        const isExecutable = (stat.mode & 73) !== 0;
        if (!isExecutable) {
          return null;
        }
      } catch {
        return null;
      }
      return name;
    }
    return null;
  }
  const pathEnv = ctx.state.env.PATH ?? "/usr/bin:/bin";
  const pathDirs = pathEnv.split(":");
  for (const dir of pathDirs) {
    if (!dir)
      continue;
    const resolvedDir = dir.startsWith("/") ? dir : ctx.fs.resolvePath(ctx.state.cwd, dir);
    const fullPath = `${resolvedDir}/${name}`;
    if (await ctx.fs.exists(fullPath)) {
      try {
        const stat = await ctx.fs.stat(fullPath);
        if (stat.isDirectory) {
          continue;
        }
      } catch {
        continue;
      }
      return `${dir}/${name}`;
    }
  }
  if (ctx.commands.has(name)) {
    for (const dir of pathDirs) {
      if (dir === "/usr/bin" || dir === "/bin") {
        return `${dir}/${name}`;
      }
    }
    return `/usr/bin/${name}`;
  }
  return null;
}
__name$1(findFirstInPath, "findFirstInPath");
async function dispatchBuiltin(dispatchCtx, commandName, args, _quotedArgs, stdin, skipFunctions, _useDefaultPath, stdinSourceFd) {
  const { ctx, runCommand } = dispatchCtx;
  if (commandName === "export") {
    return handleExport(ctx, args);
  }
  if (commandName === "unset") {
    return handleUnset(ctx, args);
  }
  if (commandName === "exit") {
    return handleExit(ctx, args);
  }
  if (commandName === "local") {
    return handleLocal(ctx, args);
  }
  if (commandName === "set") {
    return handleSet(ctx, args);
  }
  if (commandName === "break") {
    return handleBreak(ctx, args);
  }
  if (commandName === "continue") {
    return handleContinue(ctx, args);
  }
  if (commandName === "return") {
    return handleReturn(ctx, args);
  }
  if (commandName === "eval" && ctx.state.options.posix) {
    return handleEval(ctx, args, stdin);
  }
  if (commandName === "shift") {
    return handleShift(ctx, args);
  }
  if (commandName === "getopts") {
    return handleGetopts(ctx, args);
  }
  if (commandName === "compgen") {
    return handleCompgen(ctx, args);
  }
  if (commandName === "complete") {
    return handleComplete(ctx, args);
  }
  if (commandName === "compopt") {
    return handleCompopt(ctx, args);
  }
  if (commandName === "pushd") {
    return await handlePushd(ctx, args);
  }
  if (commandName === "popd") {
    return handlePopd(ctx, args);
  }
  if (commandName === "dirs") {
    return handleDirs(ctx, args);
  }
  if (commandName === "source" || commandName === ".") {
    return handleSource(ctx, args);
  }
  if (commandName === "read") {
    return handleRead(ctx, args, stdin, stdinSourceFd);
  }
  if (commandName === "mapfile" || commandName === "readarray") {
    return handleMapfile(ctx, args, stdin);
  }
  if (commandName === "declare" || commandName === "typeset") {
    return handleDeclare(ctx, args);
  }
  if (commandName === "readonly") {
    return handleReadonly(ctx, args);
  }
  if (!skipFunctions) {
    const func = ctx.state.functions.get(commandName);
    if (func) {
      return callFunction(ctx, func, args, stdin);
    }
  }
  if (commandName === "eval") {
    return handleEval(ctx, args, stdin);
  }
  if (commandName === "cd") {
    return await handleCd(ctx, args);
  }
  if (commandName === ":" || commandName === "true") {
    return OK;
  }
  if (commandName === "false") {
    return testResult(false);
  }
  if (commandName === "let") {
    return handleLet(ctx, args);
  }
  if (commandName === "command") {
    return handleCommandBuiltin(dispatchCtx, args, stdin);
  }
  if (commandName === "builtin") {
    return handleBuiltinBuiltin(dispatchCtx, args, stdin);
  }
  if (commandName === "shopt") {
    return handleShopt(ctx, args);
  }
  if (commandName === "exec") {
    if (args.length === 0) {
      return OK;
    }
    const [cmd, ...rest] = args;
    return runCommand(cmd, rest, [], stdin, false, false, -1);
  }
  if (commandName === "wait") {
    return OK;
  }
  if (commandName === "type") {
    return await handleType(ctx, args, (name) => findFirstInPath(ctx, name), (name) => findCommandInPath(ctx, name));
  }
  if (commandName === "hash") {
    return handleHash(ctx, args);
  }
  if (commandName === "help") {
    return handleHelp(ctx, args);
  }
  if (commandName === "[" || commandName === "test") {
    let testArgs = args;
    if (commandName === "[") {
      if (args[args.length - 1] !== "]") {
        return failure("[: missing `]'\n", 2);
      }
      testArgs = args.slice(0, -1);
    }
    return evaluateTestArgs(ctx, testArgs);
  }
  return null;
}
__name$1(dispatchBuiltin, "dispatchBuiltin");
async function handleCommandBuiltin(dispatchCtx, args, stdin) {
  const { ctx, runCommand } = dispatchCtx;
  if (args.length === 0) {
    return OK;
  }
  let useDefaultPath = false;
  let verboseDescribe = false;
  let showPath = false;
  let cmdArgs = args;
  while (cmdArgs.length > 0 && cmdArgs[0].startsWith("-")) {
    const opt = cmdArgs[0];
    if (opt === "--") {
      cmdArgs = cmdArgs.slice(1);
      break;
    }
    for (const char of opt.slice(1)) {
      if (char === "p") {
        useDefaultPath = true;
      } else if (char === "V") {
        verboseDescribe = true;
      } else if (char === "v") {
        showPath = true;
      }
    }
    cmdArgs = cmdArgs.slice(1);
  }
  if (cmdArgs.length === 0) {
    return OK;
  }
  if (showPath || verboseDescribe) {
    return await handleCommandV(ctx, cmdArgs, showPath, verboseDescribe);
  }
  const [cmd, ...rest] = cmdArgs;
  return runCommand(cmd, rest, [], stdin, true, useDefaultPath, -1);
}
__name$1(handleCommandBuiltin, "handleCommandBuiltin");
async function handleBuiltinBuiltin(dispatchCtx, args, stdin) {
  const { runCommand } = dispatchCtx;
  if (args.length === 0) {
    return OK;
  }
  let cmdArgs = args;
  if (cmdArgs[0] === "--") {
    cmdArgs = cmdArgs.slice(1);
    if (cmdArgs.length === 0) {
      return OK;
    }
  }
  const cmd = cmdArgs[0];
  if (!SHELL_BUILTINS.has(cmd)) {
    return failure(`bash: builtin: ${cmd}: not a shell builtin
`);
  }
  const [, ...rest] = cmdArgs;
  return runCommand(cmd, rest, [], stdin, true, false, -1);
}
__name$1(handleBuiltinBuiltin, "handleBuiltinBuiltin");
async function executeExternalCommand(dispatchCtx, commandName, args, stdin, useDefaultPath) {
  const { ctx, buildExportedEnv, executeUserScript: executeUserScript2 } = dispatchCtx;
  const defaultPath = "/usr/bin:/bin";
  const resolved = await resolveCommand(ctx, commandName, useDefaultPath ? defaultPath : void 0);
  if (!resolved) {
    if (isBrowserExcludedCommand(commandName)) {
      return failure(`bash: ${commandName}: command not available in browser environments. Exclude '${commandName}' from your commands or use the Node.js bundle.
`, 127);
    }
    return failure(`bash: ${commandName}: command not found
`, 127);
  }
  if ("error" in resolved) {
    if (resolved.error === "permission_denied") {
      return failure(`bash: ${commandName}: Permission denied
`, 126);
    }
    return failure(`bash: ${commandName}: No such file or directory
`, 127);
  }
  if ("script" in resolved) {
    if (!commandName.includes("/")) {
      if (!ctx.state.hashTable) {
        ctx.state.hashTable = /* @__PURE__ */ new Map();
      }
      ctx.state.hashTable.set(commandName, resolved.path);
    }
    return await executeUserScript2(resolved.path, args, stdin);
  }
  const { cmd, path: cmdPath } = resolved;
  if (!commandName.includes("/")) {
    if (!ctx.state.hashTable) {
      ctx.state.hashTable = /* @__PURE__ */ new Map();
    }
    ctx.state.hashTable.set(commandName, cmdPath);
  }
  const effectiveStdin = stdin || ctx.state.groupStdin || "";
  const exportedEnv = buildExportedEnv();
  const cmdCtx = {
    fs: ctx.fs,
    cwd: ctx.state.cwd,
    env: ctx.state.env,
    exportedEnv,
    stdin: effectiveStdin,
    limits: ctx.limits,
    exec: ctx.execFn,
    fetch: ctx.fetch,
    getRegisteredCommands: /* @__PURE__ */ __name$1(() => Array.from(ctx.commands.keys()), "getRegisteredCommands"),
    sleep: ctx.sleep,
    trace: ctx.trace,
    fileDescriptors: ctx.state.fileDescriptors,
    xpgEcho: ctx.state.shoptOptions.xpg_echo
  };
  try {
    return await cmd.execute(args, cmdCtx);
  } catch (error) {
    return failure(`${commandName}: ${getErrorMessage(error)}
`);
  }
}
__name$1(executeExternalCommand, "executeExternalCommand");
async function executeCondition(ctx, statements) {
  const savedInCondition = ctx.state.inCondition;
  ctx.state.inCondition = true;
  let stdout = "";
  let stderr = "";
  let exitCode = 0;
  try {
    for (const stmt of statements) {
      const result2 = await ctx.executeStatement(stmt);
      stdout += result2.stdout;
      stderr += result2.stderr;
      exitCode = result2.exitCode;
    }
  } finally {
    ctx.state.inCondition = savedInCondition;
  }
  return { stdout, stderr, exitCode };
}
__name$1(executeCondition, "executeCondition");
function handleLoopError(error, stdout, stderr, loopDepth) {
  if (error instanceof BreakError) {
    stdout += error.stdout;
    stderr += error.stderr;
    if (error.levels > 1 && loopDepth > 1) {
      error.levels--;
      error.stdout = stdout;
      error.stderr = stderr;
      return { action: "rethrow", stdout, stderr, error };
    }
    return { action: "break", stdout, stderr };
  }
  if (error instanceof ContinueError) {
    stdout += error.stdout;
    stderr += error.stderr;
    if (error.levels > 1 && loopDepth > 1) {
      error.levels--;
      error.stdout = stdout;
      error.stderr = stderr;
      return { action: "rethrow", stdout, stderr, error };
    }
    return { action: "continue", stdout, stderr };
  }
  if (error instanceof ReturnError || error instanceof ErrexitError || error instanceof ExitError || error instanceof ExecutionLimitError) {
    error.prependOutput(stdout, stderr);
    return { action: "rethrow", stdout, stderr, error };
  }
  const message = getErrorMessage(error);
  return {
    action: "error",
    stdout,
    stderr: `${stderr}${message}
`,
    exitCode: 1
  };
}
__name$1(handleLoopError, "handleLoopError");
async function executeStatements(ctx, statements, initialStdout = "", initialStderr = "") {
  let stdout = initialStdout;
  let stderr = initialStderr;
  let exitCode = 0;
  try {
    for (const stmt of statements) {
      const result2 = await ctx.executeStatement(stmt);
      stdout += result2.stdout;
      stderr += result2.stderr;
      exitCode = result2.exitCode;
    }
  } catch (error) {
    if (isScopeExitError(error) || error instanceof ErrexitError || error instanceof ExitError || error instanceof ExecutionLimitError || error instanceof SubshellExitError) {
      error.prependOutput(stdout, stderr);
      throw error;
    }
    return {
      stdout,
      stderr: `${stderr}${getErrorMessage(error)}
`,
      exitCode: 1
    };
  }
  return { stdout, stderr, exitCode };
}
__name$1(executeStatements, "executeStatements");
async function executeIf(ctx, node) {
  let stdout = "";
  let stderr = "";
  for (const clause of node.clauses) {
    const condResult = await executeCondition(ctx, clause.condition);
    stdout += condResult.stdout;
    stderr += condResult.stderr;
    if (condResult.exitCode === 0) {
      return executeStatements(ctx, clause.body, stdout, stderr);
    }
  }
  if (node.elseBody) {
    return executeStatements(ctx, node.elseBody, stdout, stderr);
  }
  return result(stdout, stderr, 0);
}
__name$1(executeIf, "executeIf");
async function executeFor(ctx, node) {
  const preOpenError = await preOpenOutputRedirects(ctx, node.redirections);
  if (preOpenError) {
    return preOpenError;
  }
  let stdout = "";
  let stderr = "";
  let exitCode = 0;
  let iterations = 0;
  if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(node.variable)) {
    return failure(`bash: \`${node.variable}': not a valid identifier
`);
  }
  let words = [];
  if (node.words === null) {
    words = (ctx.state.env["@"] || "").split(" ").filter(Boolean);
  } else if (node.words.length === 0) {
    words = [];
  } else {
    try {
      for (const word of node.words) {
        const expanded = await expandWordWithGlob(ctx, word);
        words.push(...expanded.values);
      }
    } catch (e) {
      if (e instanceof GlobError) {
        return { stdout: "", stderr: e.stderr, exitCode: 1 };
      }
      throw e;
    }
  }
  ctx.state.loopDepth++;
  try {
    for (const value of words) {
      iterations++;
      if (iterations > ctx.limits.maxLoopIterations) {
        throwExecutionLimit(`for loop: too many iterations (${ctx.limits.maxLoopIterations}), increase executionLimits.maxLoopIterations`, "iterations", stdout, stderr);
      }
      ctx.state.env[node.variable] = value;
      try {
        for (const stmt of node.body) {
          const stmtResult = await ctx.executeStatement(stmt);
          stdout += stmtResult.stdout;
          stderr += stmtResult.stderr;
          exitCode = stmtResult.exitCode;
        }
      } catch (error) {
        const loopResult = handleLoopError(error, stdout, stderr, ctx.state.loopDepth);
        stdout = loopResult.stdout;
        stderr = loopResult.stderr;
        if (loopResult.action === "break")
          break;
        if (loopResult.action === "continue")
          continue;
        if (loopResult.action === "error") {
          const bodyResult2 = result(stdout, stderr, loopResult.exitCode ?? 1);
          return applyRedirections(ctx, bodyResult2, node.redirections);
        }
        throw loopResult.error;
      }
    }
  } finally {
    ctx.state.loopDepth--;
  }
  const bodyResult = result(stdout, stderr, exitCode);
  return applyRedirections(ctx, bodyResult, node.redirections);
}
__name$1(executeFor, "executeFor");
async function executeCStyleFor(ctx, node) {
  const preOpenError = await preOpenOutputRedirects(ctx, node.redirections);
  if (preOpenError) {
    return preOpenError;
  }
  const loopLine = node.line;
  if (loopLine !== void 0) {
    ctx.state.currentLine = loopLine;
  }
  let stdout = "";
  let stderr = "";
  let exitCode = 0;
  let iterations = 0;
  if (node.init) {
    await evaluateArithmetic(ctx, node.init.expression);
  }
  ctx.state.loopDepth++;
  try {
    while (true) {
      iterations++;
      if (iterations > ctx.limits.maxLoopIterations) {
        throwExecutionLimit(`for loop: too many iterations (${ctx.limits.maxLoopIterations}), increase executionLimits.maxLoopIterations`, "iterations", stdout, stderr);
      }
      if (node.condition) {
        if (loopLine !== void 0) {
          ctx.state.currentLine = loopLine;
        }
        const condResult = await evaluateArithmetic(ctx, node.condition.expression);
        if (condResult === 0)
          break;
      }
      try {
        for (const stmt of node.body) {
          const stmtResult = await ctx.executeStatement(stmt);
          stdout += stmtResult.stdout;
          stderr += stmtResult.stderr;
          exitCode = stmtResult.exitCode;
        }
      } catch (error) {
        const loopResult = handleLoopError(error, stdout, stderr, ctx.state.loopDepth);
        stdout = loopResult.stdout;
        stderr = loopResult.stderr;
        if (loopResult.action === "break")
          break;
        if (loopResult.action === "continue") {
          if (node.update) {
            await evaluateArithmetic(ctx, node.update.expression);
          }
          continue;
        }
        if (loopResult.action === "error") {
          const bodyResult2 = result(stdout, stderr, loopResult.exitCode ?? 1);
          return applyRedirections(ctx, bodyResult2, node.redirections);
        }
        throw loopResult.error;
      }
      if (node.update) {
        await evaluateArithmetic(ctx, node.update.expression);
      }
    }
  } finally {
    ctx.state.loopDepth--;
  }
  const bodyResult = result(stdout, stderr, exitCode);
  return applyRedirections(ctx, bodyResult, node.redirections);
}
__name$1(executeCStyleFor, "executeCStyleFor");
async function executeWhile(ctx, node, stdin = "") {
  let stdout = "";
  let stderr = "";
  let exitCode = 0;
  let iterations = 0;
  let effectiveStdin = stdin;
  for (const redir of node.redirections) {
    if ((redir.operator === "<<" || redir.operator === "<<-") && redir.target.type === "HereDoc") {
      const hereDoc = redir.target;
      let content = await expandWord(ctx, hereDoc.content);
      if (hereDoc.stripTabs) {
        content = content.split("\n").map((line) => line.replace(/^\t+/, "")).join("\n");
      }
      effectiveStdin = content;
    } else if (redir.operator === "<<<" && redir.target.type === "Word") {
      effectiveStdin = `${await expandWord(ctx, redir.target)}
`;
    } else if (redir.operator === "<" && redir.target.type === "Word") {
      try {
        const target = await expandWord(ctx, redir.target);
        const filePath = ctx.fs.resolvePath(ctx.state.cwd, target);
        effectiveStdin = await ctx.fs.readFile(filePath);
      } catch {
        const target = await expandWord(ctx, redir.target);
        return failure(`bash: ${target}: No such file or directory
`);
      }
    }
  }
  const savedGroupStdin = ctx.state.groupStdin;
  if (effectiveStdin) {
    ctx.state.groupStdin = effectiveStdin;
  }
  ctx.state.loopDepth++;
  try {
    while (true) {
      iterations++;
      if (iterations > ctx.limits.maxLoopIterations) {
        throwExecutionLimit(`while loop: too many iterations (${ctx.limits.maxLoopIterations}), increase executionLimits.maxLoopIterations`, "iterations", stdout, stderr);
      }
      let conditionExitCode = 0;
      let shouldBreak = false;
      let shouldContinue = false;
      const savedInCondition = ctx.state.inCondition;
      ctx.state.inCondition = true;
      try {
        for (const stmt of node.condition) {
          const result2 = await ctx.executeStatement(stmt);
          stdout += result2.stdout;
          stderr += result2.stderr;
          conditionExitCode = result2.exitCode;
        }
      } catch (error) {
        if (error instanceof BreakError) {
          stdout += error.stdout;
          stderr += error.stderr;
          if (error.levels > 1 && ctx.state.loopDepth > 1) {
            error.levels--;
            error.stdout = stdout;
            error.stderr = stderr;
            ctx.state.inCondition = savedInCondition;
            throw error;
          }
          shouldBreak = true;
        } else if (error instanceof ContinueError) {
          stdout += error.stdout;
          stderr += error.stderr;
          if (error.levels > 1 && ctx.state.loopDepth > 1) {
            error.levels--;
            error.stdout = stdout;
            error.stderr = stderr;
            ctx.state.inCondition = savedInCondition;
            throw error;
          }
          shouldContinue = true;
        } else {
          ctx.state.inCondition = savedInCondition;
          throw error;
        }
      } finally {
        ctx.state.inCondition = savedInCondition;
      }
      if (shouldBreak)
        break;
      if (shouldContinue)
        continue;
      if (conditionExitCode !== 0)
        break;
      try {
        for (const stmt of node.body) {
          const stmtResult = await ctx.executeStatement(stmt);
          stdout += stmtResult.stdout;
          stderr += stmtResult.stderr;
          exitCode = stmtResult.exitCode;
        }
      } catch (error) {
        const loopResult = handleLoopError(error, stdout, stderr, ctx.state.loopDepth);
        stdout = loopResult.stdout;
        stderr = loopResult.stderr;
        if (loopResult.action === "break")
          break;
        if (loopResult.action === "continue")
          continue;
        if (loopResult.action === "error") {
          return result(stdout, stderr, loopResult.exitCode ?? 1);
        }
        throw loopResult.error;
      }
    }
  } finally {
    ctx.state.loopDepth--;
    ctx.state.groupStdin = savedGroupStdin;
  }
  return result(stdout, stderr, exitCode);
}
__name$1(executeWhile, "executeWhile");
async function executeUntil(ctx, node) {
  let stdout = "";
  let stderr = "";
  let exitCode = 0;
  let iterations = 0;
  ctx.state.loopDepth++;
  try {
    while (true) {
      iterations++;
      if (iterations > ctx.limits.maxLoopIterations) {
        throwExecutionLimit(`until loop: too many iterations (${ctx.limits.maxLoopIterations}), increase executionLimits.maxLoopIterations`, "iterations", stdout, stderr);
      }
      const condResult = await executeCondition(ctx, node.condition);
      stdout += condResult.stdout;
      stderr += condResult.stderr;
      if (condResult.exitCode === 0)
        break;
      try {
        for (const stmt of node.body) {
          const stmtResult = await ctx.executeStatement(stmt);
          stdout += stmtResult.stdout;
          stderr += stmtResult.stderr;
          exitCode = stmtResult.exitCode;
        }
      } catch (error) {
        const loopResult = handleLoopError(error, stdout, stderr, ctx.state.loopDepth);
        stdout = loopResult.stdout;
        stderr = loopResult.stderr;
        if (loopResult.action === "break")
          break;
        if (loopResult.action === "continue")
          continue;
        if (loopResult.action === "error") {
          return result(stdout, stderr, loopResult.exitCode ?? 1);
        }
        throw loopResult.error;
      }
    }
  } finally {
    ctx.state.loopDepth--;
  }
  return result(stdout, stderr, exitCode);
}
__name$1(executeUntil, "executeUntil");
async function executeCase(ctx, node) {
  const preOpenError = await preOpenOutputRedirects(ctx, node.redirections);
  if (preOpenError) {
    return preOpenError;
  }
  let stdout = "";
  let stderr = "";
  let exitCode = 0;
  const value = await expandWord(ctx, node.word);
  let fallThrough = false;
  for (let i = 0; i < node.items.length; i++) {
    const item = node.items[i];
    let matched = fallThrough;
    if (!fallThrough) {
      for (const pattern of item.patterns) {
        let patternStr = await expandWord(ctx, pattern);
        if (isWordFullyQuoted(pattern)) {
          patternStr = escapeGlobChars(patternStr);
        }
        const nocasematch = ctx.state.shoptOptions.nocasematch;
        const extglob = ctx.state.shoptOptions.extglob;
        if (matchPattern(value, patternStr, nocasematch, extglob)) {
          matched = true;
          break;
        }
      }
    }
    if (matched) {
      const bodyResult2 = await executeStatements(ctx, item.body, stdout, stderr);
      stdout = bodyResult2.stdout;
      stderr = bodyResult2.stderr;
      exitCode = bodyResult2.exitCode;
      if (item.terminator === ";;") {
        break;
      } else if (item.terminator === ";&") {
        fallThrough = true;
      } else {
        fallThrough = false;
      }
    } else {
      fallThrough = false;
    }
  }
  const bodyResult = result(stdout, stderr, exitCode);
  return applyRedirections(ctx, bodyResult, node.redirections);
}
__name$1(executeCase, "executeCase");
function isWordLiteralMatch(word, targets) {
  if (word.parts.length !== 1) {
    return false;
  }
  const part = word.parts[0];
  if (part.type !== "Literal") {
    return false;
  }
  return targets.includes(part.value);
}
__name$1(isWordLiteralMatch, "isWordLiteralMatch");
function parseRwFdContent3(fdContent) {
  if (!fdContent.startsWith("__rw__:")) {
    return null;
  }
  const afterPrefix = fdContent.slice(7);
  const firstColonIdx = afterPrefix.indexOf(":");
  if (firstColonIdx === -1) {
    return null;
  }
  const pathLength = Number.parseInt(afterPrefix.slice(0, firstColonIdx), 10);
  if (Number.isNaN(pathLength) || pathLength < 0) {
    return null;
  }
  const pathStart = firstColonIdx + 1;
  const path2 = afterPrefix.slice(pathStart, pathStart + pathLength);
  const positionStart = pathStart + pathLength + 1;
  const remaining = afterPrefix.slice(positionStart);
  const posColonIdx = remaining.indexOf(":");
  if (posColonIdx === -1) {
    return null;
  }
  const position = Number.parseInt(remaining.slice(0, posColonIdx), 10);
  if (Number.isNaN(position) || position < 0) {
    return null;
  }
  const content = remaining.slice(posColonIdx + 1);
  return { path: path2, position, content };
}
__name$1(parseRwFdContent3, "parseRwFdContent");
var DEFAULT_PS4 = "+ ";
async function getXtracePrefix(ctx) {
  const ps4 = ctx.state.env.PS4;
  if (ps4 === void 0) {
    return DEFAULT_PS4;
  }
  if (ps4 === "") {
    return "";
  }
  try {
    const parser = new Parser();
    const wordNode = parser.parseWordFromString(ps4, false, false);
    const expanded = await expandWord(ctx, wordNode);
    return expanded;
  } catch {
    ctx.state.expansionStderr = `${ctx.state.expansionStderr || ""}bash: ${ps4}: bad substitution
`;
    return ps4 || DEFAULT_PS4;
  }
}
__name$1(getXtracePrefix, "getXtracePrefix");
function formatTraceLine(parts) {
  return parts.map((part) => quoteForTrace(part)).join(" ");
}
__name$1(formatTraceLine, "formatTraceLine");
function quoteForTrace(value) {
  if (value === "") {
    return "''";
  }
  const needsQuoting = /[\s'"\\$`!*?[\]{}|&;<>()~#\n\t]/.test(value);
  if (!needsQuoting) {
    return value;
  }
  const hasControlChars = /[\x00-\x1f\x7f]/.test(value);
  const hasNewline = value.includes("\n");
  const hasTab = value.includes("	");
  const hasBackslash = value.includes("\\");
  const hasSingleQuote = value.includes("'");
  if (hasControlChars || hasNewline || hasTab || hasBackslash) {
    let escaped2 = "";
    for (const char of value) {
      const code = char.charCodeAt(0);
      if (char === "\n") {
        escaped2 += "\\n";
      } else if (char === "	") {
        escaped2 += "\\t";
      } else if (char === "\\") {
        escaped2 += "\\\\";
      } else if (char === "'") {
        escaped2 += "'";
      } else if (char === '"') {
        escaped2 += '"';
      } else if (code < 32 || code === 127) {
        if (code < 256) {
          escaped2 += `\\x${code.toString(16).padStart(2, "0")}`;
        } else {
          escaped2 += `\\u${code.toString(16).padStart(4, "0")}`;
        }
      } else {
        escaped2 += char;
      }
    }
    return `$'${escaped2}'`;
  }
  if (!hasSingleQuote) {
    return `'${value}'`;
  }
  const escaped = value.replace(/([\\$`"])/g, "\\$1");
  return `"${escaped}"`;
}
__name$1(quoteForTrace, "quoteForTrace");
async function traceSimpleCommand(ctx, commandName, args) {
  if (!ctx.state.options.xtrace) {
    return "";
  }
  const prefix = await getXtracePrefix(ctx);
  const parts = [commandName, ...args];
  const traceLine = formatTraceLine(parts);
  return `${prefix}${traceLine}
`;
}
__name$1(traceSimpleCommand, "traceSimpleCommand");
async function traceAssignment(ctx, name, value) {
  if (!ctx.state.options.xtrace) {
    return "";
  }
  const prefix = await getXtracePrefix(ctx);
  return `${prefix}${name}=${value}
`;
}
__name$1(traceAssignment, "traceAssignment");
async function executePipeline(ctx, node, executeCommand) {
  const startTime = node.timed ? performance.now() : 0;
  let stdin = "";
  let lastResult = OK;
  let pipefailExitCode = 0;
  const pipestatusExitCodes = [];
  const isMultiCommandPipeline = node.commands.length > 1;
  const savedLastArg = ctx.state.lastArg;
  for (let i = 0; i < node.commands.length; i++) {
    const command = node.commands[i];
    const isLast = i === node.commands.length - 1;
    if (isMultiCommandPipeline) {
      ctx.state.lastArg = "";
    }
    const runsInSubshell = isMultiCommandPipeline && (!isLast || !ctx.state.shoptOptions.lastpipe);
    const savedEnv = runsInSubshell ? { ...ctx.state.env } : null;
    let result2;
    try {
      result2 = await executeCommand(command, stdin);
    } catch (error) {
      if (error instanceof BadSubstitutionError) {
        result2 = {
          stdout: error.stdout,
          stderr: error.stderr,
          exitCode: 1
        };
      } else if (error instanceof ExitError && node.commands.length > 1) {
        result2 = {
          stdout: error.stdout,
          stderr: error.stderr,
          exitCode: error.exitCode
        };
      } else if (error instanceof ErrexitError && node.commands.length > 1) {
        result2 = {
          stdout: error.stdout,
          stderr: error.stderr,
          exitCode: error.exitCode
        };
      } else {
        if (savedEnv) {
          ctx.state.env = savedEnv;
        }
        throw error;
      }
    }
    if (savedEnv) {
      ctx.state.env = savedEnv;
    }
    pipestatusExitCodes.push(result2.exitCode);
    if (result2.exitCode !== 0) {
      pipefailExitCode = result2.exitCode;
    }
    if (!isLast) {
      const pipeStderrToNext = node.pipeStderr?.[i] ?? false;
      if (pipeStderrToNext) {
        stdin = result2.stderr + result2.stdout;
        lastResult = {
          stdout: "",
          stderr: "",
          exitCode: result2.exitCode
        };
      } else {
        stdin = result2.stdout;
        lastResult = {
          stdout: "",
          stderr: result2.stderr,
          exitCode: result2.exitCode
        };
      }
    } else {
      lastResult = result2;
    }
  }
  const shouldSetPipestatus = node.commands.length > 1 || node.commands.length === 1 && node.commands[0].type === "SimpleCommand";
  if (shouldSetPipestatus) {
    for (const key2 of Object.keys(ctx.state.env)) {
      if (key2.startsWith("PIPESTATUS_")) {
        delete ctx.state.env[key2];
      }
    }
    for (let i = 0; i < pipestatusExitCodes.length; i++) {
      ctx.state.env[`PIPESTATUS_${i}`] = String(pipestatusExitCodes[i]);
    }
    ctx.state.env.PIPESTATUS__length = String(pipestatusExitCodes.length);
  }
  if (ctx.state.options.pipefail && pipefailExitCode !== 0) {
    lastResult = {
      ...lastResult,
      exitCode: pipefailExitCode
    };
  }
  if (node.negated) {
    lastResult = {
      ...lastResult,
      exitCode: lastResult.exitCode === 0 ? 1 : 0
    };
  }
  if (node.timed) {
    const endTime = performance.now();
    const elapsedSeconds = (endTime - startTime) / 1e3;
    const minutes = Math.floor(elapsedSeconds / 60);
    const seconds = elapsedSeconds % 60;
    let timingOutput;
    if (node.timePosix) {
      timingOutput = `real ${elapsedSeconds.toFixed(2)}
user 0.00
sys 0.00
`;
    } else {
      const realStr = `${minutes}m${seconds.toFixed(3)}s`;
      timingOutput = `
real	${realStr}
user	0m0.000s
sys	0m0.000s
`;
    }
    lastResult = {
      ...lastResult,
      stderr: lastResult.stderr + timingOutput
    };
  }
  if (isMultiCommandPipeline && !ctx.state.shoptOptions.lastpipe) {
    ctx.state.lastArg = savedLastArg;
  }
  return lastResult;
}
__name$1(executePipeline, "executePipeline");
async function processAssignments(ctx, node) {
  const tempAssignments = {};
  let xtraceOutput = "";
  for (const assignment of node.assignments) {
    const name = assignment.name;
    if (assignment.array) {
      const arrayResult = await processArrayAssignment(ctx, node, name, assignment.array, assignment.append, tempAssignments);
      if (arrayResult.error) {
        return {
          continueToNext: false,
          xtraceOutput,
          tempAssignments,
          error: arrayResult.error
        };
      }
      xtraceOutput += arrayResult.xtraceOutput;
      if (arrayResult.continueToNext) {
        continue;
      }
    }
    const value = assignment.value ? await expandWord(ctx, assignment.value) : "";
    const emptySubscriptMatch = name.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[\]$/);
    if (emptySubscriptMatch) {
      return {
        continueToNext: false,
        xtraceOutput,
        tempAssignments,
        error: result("", `bash: ${name}: bad array subscript
`, 1)
      };
    }
    const subscriptMatch = name.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
    if (subscriptMatch) {
      const subscriptResult = await processSubscriptAssignment(ctx, node, subscriptMatch[1], subscriptMatch[2], value, assignment.append, tempAssignments);
      if (subscriptResult.error) {
        return {
          continueToNext: false,
          xtraceOutput,
          tempAssignments,
          error: subscriptResult.error
        };
      }
      if (subscriptResult.continueToNext) {
        continue;
      }
    }
    const scalarResult = await processScalarAssignment(ctx, node, name, value, assignment.append, tempAssignments);
    if (scalarResult.error) {
      return {
        continueToNext: false,
        xtraceOutput,
        tempAssignments,
        error: scalarResult.error
      };
    }
    xtraceOutput += scalarResult.xtraceOutput;
    if (scalarResult.continueToNext) ;
  }
  return {
    continueToNext: false,
    xtraceOutput,
    tempAssignments
  };
}
__name$1(processAssignments, "processAssignments");
async function processArrayAssignment(ctx, node, name, array2, append, tempAssignments) {
  let xtraceOutput = "";
  if (/\[.+\]$/.test(name)) {
    return {
      continueToNext: false,
      xtraceOutput: "",
      error: result("", `bash: ${name}: cannot assign list to array member
`, 1)
    };
  }
  if (isNameref(ctx, name)) {
    const target = getNamerefTarget(ctx, name);
    if (target === void 0 || target === "") {
      throw new ExitError(1, "", "");
    }
    const resolved = resolveNameref(ctx, name);
    if (resolved && /^[a-zA-Z_][a-zA-Z0-9_]*\[@\]$/.test(resolved)) {
      return {
        continueToNext: false,
        xtraceOutput: "",
        error: result("", `bash: ${name}: cannot assign list to array member
`, 1)
      };
    }
  }
  if (isReadonly(ctx, name)) {
    if (node.name) {
      xtraceOutput += `bash: ${name}: readonly variable
`;
      return { continueToNext: true, xtraceOutput };
    }
    checkReadonlyError(ctx, name);
  }
  const isAssoc = ctx.state.associativeArrays?.has(name);
  const hasKeyedElements = checkHasKeyedElements(array2);
  const clearExistingElements = /* @__PURE__ */ __name$1(() => {
    const prefix = `${name}_`;
    for (const key2 of Object.keys(ctx.state.env)) {
      if (key2.startsWith(prefix) && !key2.includes("__")) {
        delete ctx.state.env[key2];
      }
    }
    delete ctx.state.env[name];
  }, "clearExistingElements");
  if (isAssoc && hasKeyedElements) {
    await processAssociativeArrayAssignment(ctx, node, name, array2, append, clearExistingElements, (msg) => {
      xtraceOutput += msg;
    });
  } else if (hasKeyedElements) {
    await processIndexedArrayWithKeysAssignment(ctx, name, array2, append, clearExistingElements);
  } else {
    await processSimpleArrayAssignment(ctx, name, array2, append, clearExistingElements);
  }
  if (node.name) {
    tempAssignments[name] = ctx.state.env[name];
    const elements = array2.map((el) => wordToLiteralString(el));
    const stringified = `(${elements.join(" ")})`;
    ctx.state.env[name] = stringified;
  }
  return { continueToNext: true, xtraceOutput };
}
__name$1(processArrayAssignment, "processArrayAssignment");
function checkHasKeyedElements(array2) {
  return array2.some((element) => {
    if (element.parts.length >= 2) {
      const first = element.parts[0];
      const second = element.parts[1];
      if (first.type !== "Glob" || !first.pattern.startsWith("[")) {
        return false;
      }
      if (first.pattern === "[" && (second.type === "DoubleQuoted" || second.type === "SingleQuoted")) {
        if (element.parts.length < 3)
          return false;
        const third = element.parts[2];
        if (third.type !== "Literal")
          return false;
        return third.value.startsWith("]=") || third.value.startsWith("]+=");
      }
      if (second.type !== "Literal") {
        return false;
      }
      if (second.value.startsWith("]")) {
        return second.value.startsWith("]=") || second.value.startsWith("]+=");
      }
      if (first.pattern.endsWith("]")) {
        return second.value.startsWith("=") || second.value.startsWith("+=");
      }
      return false;
    }
    return false;
  });
}
__name$1(checkHasKeyedElements, "checkHasKeyedElements");
async function processAssociativeArrayAssignment(ctx, node, name, array2, append, clearExistingElements, addXtraceOutput) {
  const pendingElements = [];
  for (const element of array2) {
    const parsed = parseKeyedElementFromWord(element);
    if (parsed) {
      const { key: key2, valueParts, append: elementAppend } = parsed;
      let value;
      if (valueParts.length > 0) {
        const valueWord = { parts: valueParts };
        value = await expandWord(ctx, valueWord);
      } else {
        value = "";
      }
      value = expandTildesInValue(ctx, value);
      pendingElements.push({
        type: "keyed",
        key: key2,
        value,
        append: elementAppend
      });
    } else {
      const expandedValue = await expandWord(ctx, element);
      pendingElements.push({ type: "invalid", expandedValue });
    }
  }
  if (!append) {
    clearExistingElements();
  }
  for (const pending of pendingElements) {
    if (pending.type === "keyed") {
      if (pending.append) {
        const existing = ctx.state.env[`${name}_${pending.key}`] ?? "";
        ctx.state.env[`${name}_${pending.key}`] = existing + pending.value;
      } else {
        ctx.state.env[`${name}_${pending.key}`] = pending.value;
      }
    } else {
      const lineNum = node.line ?? ctx.state.currentLine ?? 1;
      addXtraceOutput(`bash: line ${lineNum}: ${name}: ${pending.expandedValue}: must use subscript when assigning associative array
`);
    }
  }
}
__name$1(processAssociativeArrayAssignment, "processAssociativeArrayAssignment");
async function processIndexedArrayWithKeysAssignment(ctx, name, array2, append, clearExistingElements) {
  const pendingElements = [];
  for (const element of array2) {
    const parsed = parseKeyedElementFromWord(element);
    if (parsed) {
      const { key: indexExpr, valueParts, append: elementAppend } = parsed;
      let value;
      if (valueParts.length > 0) {
        const valueWord = { parts: valueParts };
        value = await expandWord(ctx, valueWord);
      } else {
        value = "";
      }
      value = expandTildesInValue(ctx, value);
      pendingElements.push({
        type: "keyed",
        indexExpr,
        value,
        append: elementAppend
      });
    } else {
      const expanded = await expandWordWithGlob(ctx, element);
      pendingElements.push({ type: "non-keyed", values: expanded.values });
    }
  }
  if (!append) {
    clearExistingElements();
  }
  let currentIndex = 0;
  for (const pending of pendingElements) {
    if (pending.type === "keyed") {
      let index;
      try {
        const parser = new Parser();
        const arithAst = parseArithmeticExpression(parser, pending.indexExpr);
        index = await evaluateArithmetic(ctx, arithAst.expression, false);
      } catch {
        if (/^-?\d+$/.test(pending.indexExpr)) {
          index = Number.parseInt(pending.indexExpr, 10);
        } else {
          const varValue = ctx.state.env[pending.indexExpr];
          index = varValue ? Number.parseInt(varValue, 10) : 0;
          if (Number.isNaN(index))
            index = 0;
        }
      }
      if (pending.append) {
        const existing = ctx.state.env[`${name}_${index}`] ?? "";
        ctx.state.env[`${name}_${index}`] = existing + pending.value;
      } else {
        ctx.state.env[`${name}_${index}`] = pending.value;
      }
      currentIndex = index + 1;
    } else {
      for (const val of pending.values) {
        ctx.state.env[`${name}_${currentIndex++}`] = val;
      }
    }
  }
}
__name$1(processIndexedArrayWithKeysAssignment, "processIndexedArrayWithKeysAssignment");
async function processSimpleArrayAssignment(ctx, name, array2, append, clearExistingElements) {
  const allElements = [];
  for (const element of array2) {
    const expanded = await expandWordWithGlob(ctx, element);
    allElements.push(...expanded.values);
  }
  let startIndex = 0;
  if (append) {
    const elements = getArrayElements(ctx, name);
    if (elements.length > 0) {
      const maxIndex = Math.max(...elements.map(([idx]) => typeof idx === "number" ? idx : 0));
      startIndex = maxIndex + 1;
    } else if (ctx.state.env[name] !== void 0) {
      const scalarValue = ctx.state.env[name];
      ctx.state.env[`${name}_0`] = scalarValue;
      delete ctx.state.env[name];
      startIndex = 1;
    }
  } else {
    clearExistingElements();
  }
  for (let i = 0; i < allElements.length; i++) {
    ctx.state.env[`${name}_${startIndex + i}`] = allElements[i];
  }
  if (!append) {
    ctx.state.env[`${name}__length`] = String(allElements.length);
  }
}
__name$1(processSimpleArrayAssignment, "processSimpleArrayAssignment");
async function processSubscriptAssignment(ctx, node, arrayName, subscriptExpr, value, append, tempAssignments) {
  let resolvedArrayName = arrayName;
  if (isNameref(ctx, arrayName)) {
    const resolved = resolveNameref(ctx, arrayName);
    if (resolved && resolved !== arrayName) {
      if (resolved.includes("[")) {
        return {
          continueToNext: false,
          xtraceOutput: "",
          error: result("", `bash: \`${resolved}': not a valid identifier
`, 1)
        };
      }
      resolvedArrayName = resolved;
    }
  }
  if (isReadonly(ctx, resolvedArrayName)) {
    if (node.name) {
      return { continueToNext: true, xtraceOutput: "" };
    }
    checkReadonlyError(ctx, resolvedArrayName);
  }
  const isAssoc = ctx.state.associativeArrays?.has(resolvedArrayName);
  let envKey;
  if (isAssoc) {
    envKey = await computeAssocArrayEnvKey(ctx, resolvedArrayName, subscriptExpr);
  } else {
    const indexResult = await computeIndexedArrayIndex(ctx, resolvedArrayName, subscriptExpr);
    if (indexResult.error) {
      return {
        continueToNext: false,
        xtraceOutput: "",
        error: indexResult.error
      };
    }
    envKey = `${resolvedArrayName}_${indexResult.index}`;
  }
  const finalValue = append ? (ctx.state.env[envKey] || "") + value : value;
  if (node.name) {
    tempAssignments[envKey] = ctx.state.env[envKey];
    ctx.state.env[envKey] = finalValue;
  } else {
    const localDepth = getLocalVarDepth(ctx, resolvedArrayName);
    if (localDepth !== void 0 && localDepth === ctx.state.callDepth && ctx.state.localScopes.length > 0) {
      const currentScope = ctx.state.localScopes[ctx.state.localScopes.length - 1];
      if (!currentScope.has(envKey)) {
        currentScope.set(envKey, ctx.state.env[envKey]);
      }
    }
    ctx.state.env[envKey] = finalValue;
  }
  return { continueToNext: true, xtraceOutput: "" };
}
__name$1(processSubscriptAssignment, "processSubscriptAssignment");
async function computeAssocArrayEnvKey(ctx, arrayName, subscriptExpr) {
  let key2;
  if (subscriptExpr.startsWith("'") && subscriptExpr.endsWith("'")) {
    key2 = subscriptExpr.slice(1, -1);
  } else if (subscriptExpr.startsWith('"') && subscriptExpr.endsWith('"')) {
    const inner = subscriptExpr.slice(1, -1);
    const parser = new Parser();
    const wordNode = parser.parseWordFromString(inner, true, false);
    key2 = await expandWord(ctx, wordNode);
  } else if (subscriptExpr.includes("$")) {
    const parser = new Parser();
    const wordNode = parser.parseWordFromString(subscriptExpr, false, false);
    key2 = await expandWord(ctx, wordNode);
  } else {
    key2 = subscriptExpr;
  }
  return `${arrayName}_${key2}`;
}
__name$1(computeAssocArrayEnvKey, "computeAssocArrayEnvKey");
async function computeIndexedArrayIndex(ctx, arrayName, subscriptExpr) {
  let evalExpr = subscriptExpr;
  if (subscriptExpr.startsWith('"') && subscriptExpr.endsWith('"') && subscriptExpr.length >= 2) {
    evalExpr = subscriptExpr.slice(1, -1);
  }
  let index;
  if (/^-?\d+$/.test(evalExpr)) {
    index = Number.parseInt(evalExpr, 10);
  } else {
    try {
      const parser = new Parser();
      const arithAst = parseArithmeticExpression(parser, evalExpr);
      index = await evaluateArithmetic(ctx, arithAst.expression, false);
    } catch (e) {
      if (e instanceof ArithmeticError) {
        const lineNum = ctx.state.currentLine;
        const errorMsg = `bash: line ${lineNum}: ${subscriptExpr}: ${e.message}
`;
        if (e.fatal) {
          throw new ExitError(1, "", errorMsg);
        }
        return { index: 0, error: result("", errorMsg, 1) };
      }
      const varValue = ctx.state.env[subscriptExpr];
      index = varValue ? Number.parseInt(varValue, 10) : 0;
    }
    if (Number.isNaN(index))
      index = 0;
  }
  if (index < 0) {
    const elements = getArrayElements(ctx, arrayName);
    if (elements.length === 0) {
      const lineNum = ctx.state.currentLine;
      return {
        index: 0,
        error: result("", `bash: line ${lineNum}: ${arrayName}[${subscriptExpr}]: bad array subscript
`, 1)
      };
    }
    const maxIndex = Math.max(...elements.map(([idx]) => typeof idx === "number" ? idx : 0));
    index = maxIndex + 1 + index;
    if (index < 0) {
      const lineNum = ctx.state.currentLine;
      return {
        index: 0,
        error: result("", `bash: line ${lineNum}: ${arrayName}[${subscriptExpr}]: bad array subscript
`, 1)
      };
    }
  }
  return { index };
}
__name$1(computeIndexedArrayIndex, "computeIndexedArrayIndex");
async function processScalarAssignment(ctx, node, name, value, append, tempAssignments) {
  let xtraceOutput = "";
  let targetName = name;
  let namerefArrayRef = null;
  if (isNameref(ctx, name)) {
    const resolved = resolveNamerefForAssignment(ctx, name, value);
    if (resolved === void 0) {
      return {
        continueToNext: false,
        xtraceOutput: "",
        error: result("", `bash: ${name}: circular name reference
`, 1)
      };
    }
    if (resolved === null) {
      return { continueToNext: true, xtraceOutput: "" };
    }
    targetName = resolved;
    const arrayRefMatch = targetName.match(/^([a-zA-Z_][a-zA-Z0-9_]*)\[(.+)\]$/);
    if (arrayRefMatch) {
      namerefArrayRef = {
        arrayName: arrayRefMatch[1],
        subscriptExpr: arrayRefMatch[2]
      };
      targetName = arrayRefMatch[1];
    }
  }
  if (isReadonly(ctx, targetName)) {
    if (node.name) {
      xtraceOutput += `bash: ${targetName}: readonly variable
`;
      return { continueToNext: true, xtraceOutput };
    }
    checkReadonlyError(ctx, targetName);
  }
  let finalValue;
  if (isInteger(ctx, targetName)) {
    try {
      const parser = new Parser();
      if (append) {
        const currentVal = ctx.state.env[targetName] || "0";
        const expr = `(${currentVal}) + (${value})`;
        const arithAst = parseArithmeticExpression(parser, expr);
        finalValue = String(await evaluateArithmetic(ctx, arithAst.expression));
      } else {
        const arithAst = parseArithmeticExpression(parser, value);
        finalValue = String(await evaluateArithmetic(ctx, arithAst.expression));
      }
    } catch {
      finalValue = "0";
    }
  } else {
    const { isArray: isArray2 } = await import("./_chunks/expansion-JQZIG5LU.mjs");
    const appendKey = isArray2(ctx, targetName) ? `${targetName}_0` : targetName;
    finalValue = append ? (ctx.state.env[appendKey] || "") + value : value;
  }
  finalValue = applyCaseTransform(ctx, targetName, finalValue);
  xtraceOutput += await traceAssignment(ctx, targetName, finalValue);
  let actualEnvKey = targetName;
  if (namerefArrayRef) {
    actualEnvKey = await computeNamerefArrayEnvKey(ctx, namerefArrayRef);
  } else {
    const { isArray: isArray2 } = await import("./_chunks/expansion-JQZIG5LU.mjs");
    if (isArray2(ctx, targetName)) {
      actualEnvKey = `${targetName}_0`;
    }
  }
  if (node.name) {
    tempAssignments[actualEnvKey] = ctx.state.env[actualEnvKey];
    ctx.state.env[actualEnvKey] = finalValue;
  } else {
    ctx.state.env[actualEnvKey] = finalValue;
    if (ctx.state.options.allexport) {
      ctx.state.exportedVars = ctx.state.exportedVars || /* @__PURE__ */ new Set();
      ctx.state.exportedVars.add(targetName);
    }
    if (ctx.state.tempEnvBindings?.some((b) => b.has(targetName))) {
      ctx.state.mutatedTempEnvVars = ctx.state.mutatedTempEnvVars || /* @__PURE__ */ new Set();
      ctx.state.mutatedTempEnvVars.add(targetName);
    }
  }
  return { continueToNext: false, xtraceOutput };
}
__name$1(processScalarAssignment, "processScalarAssignment");
async function computeNamerefArrayEnvKey(ctx, namerefArrayRef) {
  const { arrayName, subscriptExpr } = namerefArrayRef;
  const isAssoc = ctx.state.associativeArrays?.has(arrayName);
  if (isAssoc) {
    return computeAssocArrayEnvKey(ctx, arrayName, subscriptExpr);
  }
  let index;
  if (/^-?\d+$/.test(subscriptExpr)) {
    index = Number.parseInt(subscriptExpr, 10);
  } else {
    try {
      const parser = new Parser();
      const arithAst = parseArithmeticExpression(parser, subscriptExpr);
      index = await evaluateArithmetic(ctx, arithAst.expression, false);
    } catch {
      const varValue = ctx.state.env[subscriptExpr];
      index = varValue ? Number.parseInt(varValue, 10) : 0;
    }
    if (Number.isNaN(index))
      index = 0;
  }
  if (index < 0) {
    const elements = getArrayElements(ctx, arrayName);
    if (elements.length > 0) {
      const maxIdx = Math.max(...elements.map((e) => e[0]));
      index = maxIdx + 1 + index;
    }
  }
  return `${arrayName}_${index}`;
}
__name$1(computeNamerefArrayEnvKey, "computeNamerefArrayEnvKey");
async function executeSubshell(ctx, node, stdin, executeStatement) {
  const preOpenError = await preOpenOutputRedirects(ctx, node.redirections);
  if (preOpenError) {
    return preOpenError;
  }
  const savedEnv = { ...ctx.state.env };
  const savedCwd = ctx.state.cwd;
  const savedOptions = { ...ctx.state.options };
  const savedLocalScopes = ctx.state.localScopes;
  const savedLocalVarStack = ctx.state.localVarStack;
  const savedLocalVarDepth = ctx.state.localVarDepth;
  const savedFullyUnsetLocals = ctx.state.fullyUnsetLocals;
  ctx.state.localScopes = savedLocalScopes.map((scope) => new Map(scope));
  if (savedLocalVarStack) {
    ctx.state.localVarStack = /* @__PURE__ */ new Map();
    for (const [name, stack] of savedLocalVarStack.entries()) {
      ctx.state.localVarStack.set(name, stack.map((entry) => ({ ...entry })));
    }
  }
  if (savedLocalVarDepth) {
    ctx.state.localVarDepth = new Map(savedLocalVarDepth);
  }
  if (savedFullyUnsetLocals) {
    ctx.state.fullyUnsetLocals = new Map(savedFullyUnsetLocals);
  }
  const savedLoopDepth = ctx.state.loopDepth;
  const savedParentHasLoopContext = ctx.state.parentHasLoopContext;
  ctx.state.parentHasLoopContext = savedLoopDepth > 0;
  ctx.state.loopDepth = 0;
  const savedLastArg = ctx.state.lastArg;
  const savedBashPid = ctx.state.bashPid;
  ctx.state.bashPid = ctx.state.nextVirtualPid++;
  const savedGroupStdin = ctx.state.groupStdin;
  if (stdin) {
    ctx.state.groupStdin = stdin;
  }
  let stdout = "";
  let stderr = "";
  let exitCode = 0;
  const restore = /* @__PURE__ */ __name$1(() => {
    ctx.state.env = savedEnv;
    ctx.state.cwd = savedCwd;
    ctx.state.options = savedOptions;
    ctx.state.localScopes = savedLocalScopes;
    ctx.state.localVarStack = savedLocalVarStack;
    ctx.state.localVarDepth = savedLocalVarDepth;
    ctx.state.fullyUnsetLocals = savedFullyUnsetLocals;
    ctx.state.loopDepth = savedLoopDepth;
    ctx.state.parentHasLoopContext = savedParentHasLoopContext;
    ctx.state.groupStdin = savedGroupStdin;
    ctx.state.bashPid = savedBashPid;
    ctx.state.lastArg = savedLastArg;
  }, "restore");
  try {
    for (const stmt of node.body) {
      const res = await executeStatement(stmt);
      stdout += res.stdout;
      stderr += res.stderr;
      exitCode = res.exitCode;
    }
  } catch (error) {
    restore();
    if (error instanceof ExecutionLimitError) {
      throw error;
    }
    if (error instanceof SubshellExitError) {
      stdout += error.stdout;
      stderr += error.stderr;
      const bodyResult3 = result(stdout, stderr, 0);
      return applyRedirections(ctx, bodyResult3, node.redirections);
    }
    if (error instanceof BreakError || error instanceof ContinueError) {
      stdout += error.stdout;
      stderr += error.stderr;
      const bodyResult3 = result(stdout, stderr, 0);
      return applyRedirections(ctx, bodyResult3, node.redirections);
    }
    if (error instanceof ExitError) {
      stdout += error.stdout;
      stderr += error.stderr;
      const bodyResult3 = result(stdout, stderr, error.exitCode);
      return applyRedirections(ctx, bodyResult3, node.redirections);
    }
    if (error instanceof ReturnError) {
      stdout += error.stdout;
      stderr += error.stderr;
      const bodyResult3 = result(stdout, stderr, error.exitCode);
      return applyRedirections(ctx, bodyResult3, node.redirections);
    }
    if (error instanceof ErrexitError) {
      const bodyResult3 = result(stdout + error.stdout, stderr + error.stderr, error.exitCode);
      return applyRedirections(ctx, bodyResult3, node.redirections);
    }
    const bodyResult2 = result(stdout, `${stderr}${getErrorMessage(error)}
`, 1);
    return applyRedirections(ctx, bodyResult2, node.redirections);
  }
  restore();
  const bodyResult = result(stdout, stderr, exitCode);
  return applyRedirections(ctx, bodyResult, node.redirections);
}
__name$1(executeSubshell, "executeSubshell");
async function executeGroup(ctx, node, stdin, executeStatement) {
  let stdout = "";
  let stderr = "";
  let exitCode = 0;
  const fdVarError = await processFdVariableRedirections(ctx, node.redirections);
  if (fdVarError) {
    return fdVarError;
  }
  let effectiveStdin = stdin;
  for (const redir of node.redirections) {
    if ((redir.operator === "<<" || redir.operator === "<<-") && redir.target.type === "HereDoc") {
      const hereDoc = redir.target;
      let content = await expandWord(ctx, hereDoc.content);
      if (hereDoc.stripTabs) {
        content = content.split("\n").map((line) => line.replace(/^\t+/, "")).join("\n");
      }
      const fd = redir.fd ?? 0;
      if (fd !== 0) {
        if (!ctx.state.fileDescriptors) {
          ctx.state.fileDescriptors = /* @__PURE__ */ new Map();
        }
        ctx.state.fileDescriptors.set(fd, content);
      } else {
        effectiveStdin = content;
      }
    } else if (redir.operator === "<<<" && redir.target.type === "Word") {
      effectiveStdin = `${await expandWord(ctx, redir.target)}
`;
    } else if (redir.operator === "<" && redir.target.type === "Word") {
      try {
        const target = await expandWord(ctx, redir.target);
        const filePath = ctx.fs.resolvePath(ctx.state.cwd, target);
        effectiveStdin = await ctx.fs.readFile(filePath);
      } catch {
        const target = await expandWord(ctx, redir.target);
        return result("", `bash: ${target}: No such file or directory
`, 1);
      }
    }
  }
  const savedGroupStdin = ctx.state.groupStdin;
  if (effectiveStdin) {
    ctx.state.groupStdin = effectiveStdin;
  }
  try {
    for (const stmt of node.body) {
      const res = await executeStatement(stmt);
      stdout += res.stdout;
      stderr += res.stderr;
      exitCode = res.exitCode;
    }
  } catch (error) {
    ctx.state.groupStdin = savedGroupStdin;
    if (error instanceof ExecutionLimitError) {
      throw error;
    }
    if (isScopeExitError(error) || error instanceof ErrexitError || error instanceof ExitError) {
      error.prependOutput(stdout, stderr);
      throw error;
    }
    return result(stdout, `${stderr}${getErrorMessage(error)}
`, 1);
  }
  ctx.state.groupStdin = savedGroupStdin;
  const bodyResult = result(stdout, stderr, exitCode);
  return applyRedirections(ctx, bodyResult, node.redirections);
}
__name$1(executeGroup, "executeGroup");
async function executeUserScript(ctx, scriptPath, args, stdin, executeScript) {
  let content;
  try {
    content = await ctx.fs.readFile(scriptPath);
  } catch {
    return failure(`bash: ${scriptPath}: No such file or directory
`, 127);
  }
  if (content.startsWith("#!")) {
    const firstNewline = content.indexOf("\n");
    if (firstNewline !== -1) {
      content = content.slice(firstNewline + 1);
    }
  }
  const savedEnv = { ...ctx.state.env };
  const savedCwd = ctx.state.cwd;
  const savedOptions = { ...ctx.state.options };
  const savedLoopDepth = ctx.state.loopDepth;
  const savedParentHasLoopContext = ctx.state.parentHasLoopContext;
  const savedLastArg = ctx.state.lastArg;
  const savedBashPid = ctx.state.bashPid;
  const savedGroupStdin = ctx.state.groupStdin;
  const savedSource = ctx.state.currentSource;
  ctx.state.parentHasLoopContext = savedLoopDepth > 0;
  ctx.state.loopDepth = 0;
  ctx.state.bashPid = ctx.state.nextVirtualPid++;
  if (stdin) {
    ctx.state.groupStdin = stdin;
  }
  ctx.state.currentSource = scriptPath;
  ctx.state.env["0"] = scriptPath;
  ctx.state.env["#"] = String(args.length);
  ctx.state.env["@"] = args.join(" ");
  ctx.state.env["*"] = args.join(" ");
  for (let i = 0; i < args.length && i < 9; i++) {
    ctx.state.env[String(i + 1)] = args[i];
  }
  for (let i = args.length + 1; i <= 9; i++) {
    delete ctx.state.env[String(i)];
  }
  const cleanup = /* @__PURE__ */ __name$1(() => {
    ctx.state.env = savedEnv;
    ctx.state.cwd = savedCwd;
    ctx.state.options = savedOptions;
    ctx.state.loopDepth = savedLoopDepth;
    ctx.state.parentHasLoopContext = savedParentHasLoopContext;
    ctx.state.lastArg = savedLastArg;
    ctx.state.bashPid = savedBashPid;
    ctx.state.groupStdin = savedGroupStdin;
    ctx.state.currentSource = savedSource;
  }, "cleanup");
  try {
    const parser = new Parser();
    const ast = parser.parse(content);
    const execResult = await executeScript(ast);
    cleanup();
    return execResult;
  } catch (error) {
    cleanup();
    if (error instanceof ExitError) {
      throw error;
    }
    if (error instanceof ExecutionLimitError) {
      throw error;
    }
    if (error.name === "ParseException") {
      return failure(`bash: ${scriptPath}: ${error.message}
`);
    }
    throw error;
  }
}
__name$1(executeUserScript, "executeUserScript");
var Interpreter = (_u = class {
  ctx;
  constructor(options, state) {
    this.ctx = {
      state,
      fs: options.fs,
      commands: options.commands,
      limits: options.limits,
      execFn: options.exec,
      executeScript: this.executeScript.bind(this),
      executeStatement: this.executeStatement.bind(this),
      executeCommand: this.executeCommand.bind(this),
      fetch: options.fetch,
      sleep: options.sleep,
      trace: options.trace
    };
  }
  /**
   * Build environment record containing only exported variables.
   * In bash, only exported variables are passed to child processes.
   * This includes both permanently exported variables (via export/declare -x)
   * and temporarily exported variables (prefix assignments like FOO=bar cmd).
   */
  buildExportedEnv() {
    const exportedVars = this.ctx.state.exportedVars;
    const tempExportedVars = this.ctx.state.tempExportedVars;
    const allExported = /* @__PURE__ */ new Set();
    if (exportedVars) {
      for (const name of exportedVars) {
        allExported.add(name);
      }
    }
    if (tempExportedVars) {
      for (const name of tempExportedVars) {
        allExported.add(name);
      }
    }
    if (allExported.size === 0) {
      return {};
    }
    const env = {};
    for (const name of allExported) {
      const value = this.ctx.state.env[name];
      if (value !== void 0) {
        env[name] = value;
      }
    }
    return env;
  }
  async executeScript(node) {
    let stdout = "";
    let stderr = "";
    let exitCode = 0;
    for (const statement of node.statements) {
      try {
        const result2 = await this.executeStatement(statement);
        stdout += result2.stdout;
        stderr += result2.stderr;
        exitCode = result2.exitCode;
        this.ctx.state.lastExitCode = exitCode;
        this.ctx.state.env["?"] = String(exitCode);
      } catch (error) {
        if (error instanceof ExitError) {
          error.prependOutput(stdout, stderr);
          throw error;
        }
        if (error instanceof PosixFatalError) {
          stdout += error.stdout;
          stderr += error.stderr;
          exitCode = error.exitCode;
          this.ctx.state.lastExitCode = exitCode;
          this.ctx.state.env["?"] = String(exitCode);
          return { stdout, stderr, exitCode, env: { ...this.ctx.state.env } };
        }
        if (error instanceof ExecutionLimitError) {
          throw error;
        }
        if (error instanceof ErrexitError) {
          stdout += error.stdout;
          stderr += error.stderr;
          exitCode = error.exitCode;
          this.ctx.state.lastExitCode = exitCode;
          this.ctx.state.env["?"] = String(exitCode);
          return { stdout, stderr, exitCode, env: { ...this.ctx.state.env } };
        }
        if (error instanceof NounsetError) {
          stdout += error.stdout;
          stderr += error.stderr;
          exitCode = 1;
          this.ctx.state.lastExitCode = exitCode;
          this.ctx.state.env["?"] = String(exitCode);
          return { stdout, stderr, exitCode, env: { ...this.ctx.state.env } };
        }
        if (error instanceof BadSubstitutionError) {
          stdout += error.stdout;
          stderr += error.stderr;
          exitCode = 1;
          this.ctx.state.lastExitCode = exitCode;
          this.ctx.state.env["?"] = String(exitCode);
          return { stdout, stderr, exitCode, env: { ...this.ctx.state.env } };
        }
        if (error instanceof ArithmeticError) {
          stdout += error.stdout;
          stderr += error.stderr;
          exitCode = 1;
          this.ctx.state.lastExitCode = exitCode;
          this.ctx.state.env["?"] = String(exitCode);
          continue;
        }
        if (error instanceof BraceExpansionError) {
          stdout += error.stdout;
          stderr += error.stderr;
          exitCode = 1;
          this.ctx.state.lastExitCode = exitCode;
          this.ctx.state.env["?"] = String(exitCode);
          continue;
        }
        if (error instanceof BreakError || error instanceof ContinueError) {
          if (this.ctx.state.loopDepth > 0) {
            error.prependOutput(stdout, stderr);
            throw error;
          }
          stdout += error.stdout;
          stderr += error.stderr;
          continue;
        }
        if (error instanceof ReturnError) {
          error.prependOutput(stdout, stderr);
          throw error;
        }
        throw error;
      }
    }
    return { stdout, stderr, exitCode, env: { ...this.ctx.state.env } };
  }
  /**
   * Execute a user script file found in PATH.
   */
  async executeUserScript(scriptPath, args, stdin = "") {
    return executeUserScript(this.ctx, scriptPath, args, stdin, (ast) => this.executeScript(ast));
  }
  async executeStatement(node) {
    this.ctx.state.commandCount++;
    if (this.ctx.state.commandCount > this.ctx.limits.maxCommandCount) {
      throwExecutionLimit(`too many commands executed (>${this.ctx.limits.maxCommandCount}), increase executionLimits.maxCommandCount`, "commands");
    }
    if (node.deferredError) {
      throw new ParseException(node.deferredError.message, node.line ?? 1, 1);
    }
    if (this.ctx.state.options.noexec) {
      return OK;
    }
    this.ctx.state.errexitSafe = false;
    let stdout = "";
    let stderr = "";
    if (this.ctx.state.options.verbose && !this.ctx.state.suppressVerbose && node.sourceText) {
      stderr += `${node.sourceText}
`;
    }
    let exitCode = 0;
    let lastExecutedIndex = -1;
    let lastPipelineNegated = false;
    for (let i = 0; i < node.pipelines.length; i++) {
      const pipeline = node.pipelines[i];
      const operator = i > 0 ? node.operators[i - 1] : null;
      if (operator === "&&" && exitCode !== 0)
        continue;
      if (operator === "||" && exitCode === 0)
        continue;
      const result2 = await this.executePipeline(pipeline);
      stdout += result2.stdout;
      stderr += result2.stderr;
      exitCode = result2.exitCode;
      lastExecutedIndex = i;
      lastPipelineNegated = pipeline.negated;
      this.ctx.state.lastExitCode = exitCode;
      this.ctx.state.env["?"] = String(exitCode);
    }
    const wasShortCircuited = lastExecutedIndex < node.pipelines.length - 1;
    const innerWasSafe = this.ctx.state.errexitSafe;
    this.ctx.state.errexitSafe = wasShortCircuited || lastPipelineNegated || innerWasSafe;
    if (this.ctx.state.options.errexit && exitCode !== 0 && lastExecutedIndex === node.pipelines.length - 1 && !lastPipelineNegated && !this.ctx.state.inCondition && !innerWasSafe) {
      throw new ErrexitError(exitCode, stdout, stderr);
    }
    return result(stdout, stderr, exitCode);
  }
  async executePipeline(node) {
    return executePipeline(this.ctx, node, (cmd, stdin) => this.executeCommand(cmd, stdin));
  }
  async executeCommand(node, stdin) {
    switch (node.type) {
      case "SimpleCommand":
        return this.executeSimpleCommand(node, stdin);
      case "If":
        return executeIf(this.ctx, node);
      case "For":
        return executeFor(this.ctx, node);
      case "CStyleFor":
        return executeCStyleFor(this.ctx, node);
      case "While":
        return executeWhile(this.ctx, node, stdin);
      case "Until":
        return executeUntil(this.ctx, node);
      case "Case":
        return executeCase(this.ctx, node);
      case "Subshell":
        return this.executeSubshell(node, stdin);
      case "Group":
        return this.executeGroup(node, stdin);
      case "FunctionDef":
        return executeFunctionDef(this.ctx, node);
      case "ArithmeticCommand":
        return this.executeArithmeticCommand(node);
      case "ConditionalCommand":
        return this.executeConditionalCommand(node);
      default:
        return OK;
    }
  }
  async executeSimpleCommand(node, stdin) {
    try {
      return await this.executeSimpleCommandInner(node, stdin);
    } catch (error) {
      if (error instanceof GlobError) {
        return failure(error.stderr);
      }
      throw error;
    }
  }
  async executeSimpleCommandInner(node, stdin) {
    if (node.line !== void 0) {
      this.ctx.state.currentLine = node.line;
    }
    if (this.ctx.state.shoptOptions.expand_aliases && node.name) {
      let currentNode = node;
      let maxExpansions = 100;
      while (maxExpansions > 0) {
        const expandedNode = this.expandAlias(currentNode);
        if (expandedNode === currentNode) {
          break;
        }
        currentNode = expandedNode;
        maxExpansions--;
      }
      this.aliasExpansionStack.clear();
      if (currentNode !== node) {
        node = currentNode;
      }
    }
    this.ctx.state.expansionStderr = "";
    const assignmentResult = await processAssignments(this.ctx, node);
    if (assignmentResult.error) {
      return assignmentResult.error;
    }
    const tempAssignments = assignmentResult.tempAssignments;
    const xtraceAssignmentOutput = assignmentResult.xtraceOutput;
    if (!node.name) {
      if (node.redirections.length > 0) {
        const redirectError = await preOpenOutputRedirects(this.ctx, node.redirections);
        if (redirectError) {
          return redirectError;
        }
        const baseResult = result("", xtraceAssignmentOutput, 0);
        return applyRedirections(this.ctx, baseResult, node.redirections);
      }
      this.ctx.state.lastArg = "";
      const stderrOutput = (this.ctx.state.expansionStderr || "") + xtraceAssignmentOutput;
      this.ctx.state.expansionStderr = "";
      return result("", stderrOutput, this.ctx.state.lastExitCode);
    }
    const isLiteralAssignmentBuiltinForExport = node.name && isWordLiteralMatch(node.name, [
      "local",
      "declare",
      "typeset",
      "export",
      "readonly"
    ]);
    const tempExportedVars = Object.keys(tempAssignments);
    if (tempExportedVars.length > 0 && !isLiteralAssignmentBuiltinForExport) {
      this.ctx.state.tempExportedVars = this.ctx.state.tempExportedVars || /* @__PURE__ */ new Set();
      for (const name of tempExportedVars) {
        this.ctx.state.tempExportedVars.add(name);
      }
    }
    const fdVarError = await processFdVariableRedirections(this.ctx, node.redirections);
    if (fdVarError) {
      for (const [name, value] of Object.entries(tempAssignments)) {
        if (value === void 0)
          delete this.ctx.state.env[name];
        else
          this.ctx.state.env[name] = value;
      }
      return fdVarError;
    }
    let stdinSourceFd = -1;
    for (const redir of node.redirections) {
      if ((redir.operator === "<<" || redir.operator === "<<-") && redir.target.type === "HereDoc") {
        const hereDoc = redir.target;
        let content = await expandWord(this.ctx, hereDoc.content);
        if (hereDoc.stripTabs) {
          content = content.split("\n").map((line) => line.replace(/^\t+/, "")).join("\n");
        }
        const fd = redir.fd ?? 0;
        if (fd !== 0) {
          if (!this.ctx.state.fileDescriptors) {
            this.ctx.state.fileDescriptors = /* @__PURE__ */ new Map();
          }
          this.ctx.state.fileDescriptors.set(fd, content);
        } else {
          stdin = content;
        }
        continue;
      }
      if (redir.operator === "<<<" && redir.target.type === "Word") {
        stdin = `${await expandWord(this.ctx, redir.target)}
`;
        continue;
      }
      if (redir.operator === "<" && redir.target.type === "Word") {
        try {
          const target = await expandWord(this.ctx, redir.target);
          const filePath = this.ctx.fs.resolvePath(this.ctx.state.cwd, target);
          stdin = await this.ctx.fs.readFile(filePath);
        } catch {
          const target = await expandWord(this.ctx, redir.target);
          for (const [name, value] of Object.entries(tempAssignments)) {
            if (value === void 0)
              delete this.ctx.state.env[name];
            else
              this.ctx.state.env[name] = value;
          }
          return failure(`bash: ${target}: No such file or directory
`);
        }
      }
      if (redir.operator === "<&" && redir.target.type === "Word") {
        const target = await expandWord(this.ctx, redir.target);
        const sourceFd = Number.parseInt(target, 10);
        if (!Number.isNaN(sourceFd) && this.ctx.state.fileDescriptors) {
          const fdContent = this.ctx.state.fileDescriptors.get(sourceFd);
          if (fdContent !== void 0) {
            if (fdContent.startsWith("__rw__:")) {
              const parsed = parseRwFdContent3(fdContent);
              if (parsed) {
                stdin = parsed.content.slice(parsed.position);
                stdinSourceFd = sourceFd;
              }
            } else if (fdContent.startsWith("__file__:") || fdContent.startsWith("__file_append__:")) ;
            else {
              stdin = fdContent;
            }
          }
        }
      }
    }
    const commandName = await expandWord(this.ctx, node.name);
    const args = [];
    const quotedArgs = [];
    const isLiteralAssignmentBuiltin = isWordLiteralMatch(node.name, [
      "local",
      "declare",
      "typeset",
      "export",
      "readonly"
    ]) && (commandName === "local" || commandName === "declare" || commandName === "typeset" || commandName === "export" || commandName === "readonly");
    if (isLiteralAssignmentBuiltin) {
      for (const arg of node.args) {
        const arrayAssignResult = await expandLocalArrayAssignment(this.ctx, arg);
        if (arrayAssignResult) {
          args.push(arrayAssignResult);
          quotedArgs.push(true);
        } else {
          const scalarAssignResult = await expandScalarAssignmentArg(this.ctx, arg);
          if (scalarAssignResult !== null) {
            args.push(scalarAssignResult);
            quotedArgs.push(true);
          } else {
            const expanded = await expandWordWithGlob(this.ctx, arg);
            for (const value of expanded.values) {
              args.push(value);
              quotedArgs.push(expanded.quoted);
            }
          }
        }
      }
    } else {
      for (const arg of node.args) {
        const expanded = await expandWordWithGlob(this.ctx, arg);
        for (const value of expanded.values) {
          args.push(value);
          quotedArgs.push(expanded.quoted);
        }
      }
    }
    if (!commandName) {
      const isOnlyExpansions = node.name.parts.every((p) => p.type === "CommandSubstitution" || p.type === "ParameterExpansion" || p.type === "ArithmeticExpansion");
      if (isOnlyExpansions) {
        if (args.length > 0) {
          const newCommandName = args.shift();
          quotedArgs.shift();
          return await this.runCommand(newCommandName, args, quotedArgs, stdin, false, false, stdinSourceFd);
        }
        return result("", "", this.ctx.state.lastExitCode);
      }
      return failure("bash: : command not found\n", 127);
    }
    if (commandName === "exec" && (args.length === 0 || args[0] === "--")) {
      for (const redir of node.redirections) {
        if (redir.target.type === "HereDoc")
          continue;
        if (redir.fdVariable)
          continue;
        const target = await expandWord(this.ctx, redir.target);
        const fd = redir.fd ?? (redir.operator === "<" || redir.operator === "<>" ? 0 : 1);
        if (!this.ctx.state.fileDescriptors) {
          this.ctx.state.fileDescriptors = /* @__PURE__ */ new Map();
        }
        switch (redir.operator) {
          case ">":
          case ">|": {
            const filePath = this.ctx.fs.resolvePath(this.ctx.state.cwd, target);
            await this.ctx.fs.writeFile(filePath, "", "utf8");
            this.ctx.state.fileDescriptors.set(fd, `__file__:${filePath}`);
            break;
          }
          case ">>": {
            const filePath = this.ctx.fs.resolvePath(this.ctx.state.cwd, target);
            this.ctx.state.fileDescriptors.set(fd, `__file_append__:${filePath}`);
            break;
          }
          case "<": {
            const filePath = this.ctx.fs.resolvePath(this.ctx.state.cwd, target);
            try {
              const content = await this.ctx.fs.readFile(filePath);
              this.ctx.state.fileDescriptors.set(fd, content);
            } catch {
              return failure(`bash: ${target}: No such file or directory
`);
            }
            break;
          }
          case "<>": {
            const filePath = this.ctx.fs.resolvePath(this.ctx.state.cwd, target);
            try {
              const content = await this.ctx.fs.readFile(filePath);
              this.ctx.state.fileDescriptors.set(fd, `__rw__:${filePath.length}:${filePath}:0:${content}`);
            } catch {
              await this.ctx.fs.writeFile(filePath, "", "utf8");
              this.ctx.state.fileDescriptors.set(fd, `__rw__:${filePath.length}:${filePath}:0:`);
            }
            break;
          }
          case ">&": {
            if (target === "-") {
              this.ctx.state.fileDescriptors.delete(fd);
            } else if (target.endsWith("-")) {
              const sourceFdStr = target.slice(0, -1);
              const sourceFd = Number.parseInt(sourceFdStr, 10);
              if (!Number.isNaN(sourceFd)) {
                const sourceInfo = this.ctx.state.fileDescriptors.get(sourceFd);
                if (sourceInfo !== void 0) {
                  this.ctx.state.fileDescriptors.set(fd, sourceInfo);
                } else {
                  this.ctx.state.fileDescriptors.set(fd, `__dupout__:${sourceFd}`);
                }
                this.ctx.state.fileDescriptors.delete(sourceFd);
              }
            } else {
              const sourceFd = Number.parseInt(target, 10);
              if (!Number.isNaN(sourceFd)) {
                this.ctx.state.fileDescriptors.set(fd, `__dupout__:${sourceFd}`);
              }
            }
            break;
          }
          case "<&": {
            if (target === "-") {
              this.ctx.state.fileDescriptors.delete(fd);
            } else if (target.endsWith("-")) {
              const sourceFdStr = target.slice(0, -1);
              const sourceFd = Number.parseInt(sourceFdStr, 10);
              if (!Number.isNaN(sourceFd)) {
                const sourceInfo = this.ctx.state.fileDescriptors.get(sourceFd);
                if (sourceInfo !== void 0) {
                  this.ctx.state.fileDescriptors.set(fd, sourceInfo);
                } else {
                  this.ctx.state.fileDescriptors.set(fd, `__dupin__:${sourceFd}`);
                }
                this.ctx.state.fileDescriptors.delete(sourceFd);
              }
            } else {
              const sourceFd = Number.parseInt(target, 10);
              if (!Number.isNaN(sourceFd)) {
                this.ctx.state.fileDescriptors.set(fd, `__dupin__:${sourceFd}`);
              }
            }
            break;
          }
        }
      }
      for (const [name, value] of Object.entries(tempAssignments)) {
        if (value === void 0)
          delete this.ctx.state.env[name];
        else
          this.ctx.state.env[name] = value;
      }
      if (this.ctx.state.tempExportedVars) {
        for (const name of Object.keys(tempAssignments)) {
          this.ctx.state.tempExportedVars.delete(name);
        }
      }
      return OK;
    }
    const xtraceOutput = await traceSimpleCommand(this.ctx, commandName, args);
    if (Object.keys(tempAssignments).length > 0) {
      this.ctx.state.tempEnvBindings = this.ctx.state.tempEnvBindings || [];
      this.ctx.state.tempEnvBindings.push(new Map(Object.entries(tempAssignments)));
    }
    let cmdResult;
    let controlFlowError = null;
    try {
      cmdResult = await this.runCommand(commandName, args, quotedArgs, stdin, false, false, stdinSourceFd);
    } catch (error) {
      if (error instanceof BreakError || error instanceof ContinueError) {
        controlFlowError = error;
        cmdResult = OK;
      } else {
        throw error;
      }
    }
    const stderrPrefix = xtraceAssignmentOutput + xtraceOutput;
    if (stderrPrefix) {
      cmdResult = {
        ...cmdResult,
        stderr: stderrPrefix + cmdResult.stderr
      };
    }
    cmdResult = await applyRedirections(this.ctx, cmdResult, node.redirections);
    if (controlFlowError) {
      throw controlFlowError;
    }
    if (args.length > 0) {
      let lastArg = args[args.length - 1];
      if ((commandName === "declare" || commandName === "local" || commandName === "typeset") && /^[a-zA-Z_][a-zA-Z0-9_]*=\(/.test(lastArg)) {
        const match = lastArg.match(/^([a-zA-Z_][a-zA-Z0-9_]*)=\(/);
        if (match) {
          lastArg = match[1];
        }
      }
      this.ctx.state.lastArg = lastArg;
    } else {
      this.ctx.state.lastArg = commandName;
    }
    const isPosixSpecialWithPersistence = isPosixSpecialBuiltin(commandName) && commandName !== "unset" && commandName !== "eval";
    const shouldRestoreTempAssignments = !this.ctx.state.options.posix || !isPosixSpecialWithPersistence;
    if (shouldRestoreTempAssignments) {
      for (const [name, value] of Object.entries(tempAssignments)) {
        if (this.ctx.state.fullyUnsetLocals?.has(name)) {
          continue;
        }
        if (value === void 0)
          delete this.ctx.state.env[name];
        else
          this.ctx.state.env[name] = value;
      }
    }
    if (this.ctx.state.tempExportedVars) {
      for (const name of Object.keys(tempAssignments)) {
        this.ctx.state.tempExportedVars.delete(name);
      }
    }
    if (Object.keys(tempAssignments).length > 0 && this.ctx.state.tempEnvBindings) {
      this.ctx.state.tempEnvBindings.pop();
    }
    if (this.ctx.state.expansionStderr) {
      cmdResult = {
        ...cmdResult,
        stderr: this.ctx.state.expansionStderr + cmdResult.stderr
      };
      this.ctx.state.expansionStderr = "";
    }
    return cmdResult;
  }
  async runCommand(commandName, args, quotedArgs, stdin, skipFunctions = false, useDefaultPath = false, stdinSourceFd = -1) {
    const dispatchCtx = {
      ctx: this.ctx,
      runCommand: /* @__PURE__ */ __name$1((name, a, qa, s, sf, udp, ssf) => this.runCommand(name, a, qa, s, sf, udp, ssf), "runCommand"),
      buildExportedEnv: /* @__PURE__ */ __name$1(() => this.buildExportedEnv(), "buildExportedEnv"),
      executeUserScript: /* @__PURE__ */ __name$1((path2, a, s) => this.executeUserScript(path2, a, s), "executeUserScript")
    };
    const builtinResult = await dispatchBuiltin(dispatchCtx, commandName, args, quotedArgs, stdin, skipFunctions, useDefaultPath, stdinSourceFd);
    if (builtinResult !== null) {
      return builtinResult;
    }
    return executeExternalCommand(dispatchCtx, commandName, args, stdin, useDefaultPath);
  }
  // Alias expansion state
  aliasExpansionStack = /* @__PURE__ */ new Set();
  expandAlias(node) {
    return expandAlias(this.ctx.state, node, this.aliasExpansionStack);
  }
  async findCommandInPath(commandName) {
    return findCommandInPath(this.ctx, commandName);
  }
  async executeSubshell(node, stdin = "") {
    return executeSubshell(this.ctx, node, stdin, (stmt) => this.executeStatement(stmt));
  }
  async executeGroup(node, stdin = "") {
    return executeGroup(this.ctx, node, stdin, (stmt) => this.executeStatement(stmt));
  }
  async executeArithmeticCommand(node) {
    if (node.line !== void 0) {
      this.ctx.state.currentLine = node.line;
    }
    const preOpenError = await preOpenOutputRedirects(this.ctx, node.redirections);
    if (preOpenError) {
      return preOpenError;
    }
    try {
      const arithResult = await evaluateArithmetic(this.ctx, node.expression.expression);
      let bodyResult = testResult(arithResult !== 0);
      if (this.ctx.state.expansionStderr) {
        bodyResult = {
          ...bodyResult,
          stderr: this.ctx.state.expansionStderr + bodyResult.stderr
        };
        this.ctx.state.expansionStderr = "";
      }
      return applyRedirections(this.ctx, bodyResult, node.redirections);
    } catch (error) {
      const bodyResult = failure(`bash: arithmetic expression: ${error.message}
`);
      return applyRedirections(this.ctx, bodyResult, node.redirections);
    }
  }
  async executeConditionalCommand(node) {
    if (node.line !== void 0) {
      this.ctx.state.currentLine = node.line;
    }
    const preOpenError = await preOpenOutputRedirects(this.ctx, node.redirections);
    if (preOpenError) {
      return preOpenError;
    }
    try {
      const condResult = await evaluateConditional(this.ctx, node.expression);
      let bodyResult = testResult(condResult);
      if (this.ctx.state.expansionStderr) {
        bodyResult = {
          ...bodyResult,
          stderr: this.ctx.state.expansionStderr + bodyResult.stderr
        };
        this.ctx.state.expansionStderr = "";
      }
      return applyRedirections(this.ctx, bodyResult, node.redirections);
    } catch (error) {
      const exitCode = error instanceof ArithmeticError ? 1 : 2;
      const bodyResult = failure(`bash: conditional expression: ${error.message}
`, exitCode);
      return applyRedirections(this.ctx, bodyResult, node.redirections);
    }
  }
}, __name$1(_u, "Interpreter"), _u);
var DEFAULT_LIMITS$1 = {
  maxCallDepth: 100,
  maxCommandCount: 1e4,
  maxLoopIterations: 1e4,
  maxAwkIterations: 1e4,
  maxSedIterations: 1e4,
  maxJqIterations: 1e4,
  maxSqliteTimeoutMs: 5e3,
  maxPythonTimeoutMs: 3e4
};
function resolveLimits$1(userLimits) {
  if (!userLimits) {
    return { ...DEFAULT_LIMITS$1 };
  }
  return {
    maxCallDepth: userLimits.maxCallDepth ?? DEFAULT_LIMITS$1.maxCallDepth,
    maxCommandCount: userLimits.maxCommandCount ?? DEFAULT_LIMITS$1.maxCommandCount,
    maxLoopIterations: userLimits.maxLoopIterations ?? DEFAULT_LIMITS$1.maxLoopIterations,
    maxAwkIterations: userLimits.maxAwkIterations ?? DEFAULT_LIMITS$1.maxAwkIterations,
    maxSedIterations: userLimits.maxSedIterations ?? DEFAULT_LIMITS$1.maxSedIterations,
    maxJqIterations: userLimits.maxJqIterations ?? DEFAULT_LIMITS$1.maxJqIterations,
    maxSqliteTimeoutMs: userLimits.maxSqliteTimeoutMs ?? DEFAULT_LIMITS$1.maxSqliteTimeoutMs,
    maxPythonTimeoutMs: userLimits.maxPythonTimeoutMs ?? DEFAULT_LIMITS$1.maxPythonTimeoutMs
  };
}
__name$1(resolveLimits$1, "resolveLimits");
function parseUrl(urlString) {
  try {
    const url = new URL(urlString);
    return {
      origin: url.origin,
      pathname: url.pathname,
      href: url.href
    };
  } catch {
    return null;
  }
}
__name$1(parseUrl, "parseUrl");
function normalizeAllowListEntry(entry) {
  const parsed = parseUrl(entry);
  if (!parsed) {
    return null;
  }
  return {
    origin: parsed.origin,
    // Keep the pathname exactly as specified (including trailing slash if present)
    pathPrefix: parsed.pathname
  };
}
__name$1(normalizeAllowListEntry, "normalizeAllowListEntry");
function matchesAllowListEntry(url, allowedEntry) {
  const parsedUrl = parseUrl(url);
  if (!parsedUrl) {
    return false;
  }
  const normalizedEntry = normalizeAllowListEntry(allowedEntry);
  if (!normalizedEntry) {
    return false;
  }
  if (parsedUrl.origin !== normalizedEntry.origin) {
    return false;
  }
  if (normalizedEntry.pathPrefix === "/" || normalizedEntry.pathPrefix === "") {
    return true;
  }
  return parsedUrl.pathname.startsWith(normalizedEntry.pathPrefix);
}
__name$1(matchesAllowListEntry, "matchesAllowListEntry");
function isUrlAllowed(url, allowedUrlPrefixes) {
  if (!allowedUrlPrefixes || allowedUrlPrefixes.length === 0) {
    return false;
  }
  return allowedUrlPrefixes.some((entry) => matchesAllowListEntry(url, entry));
}
__name$1(isUrlAllowed, "isUrlAllowed");
var NetworkAccessDeniedError = (_v = class extends Error {
  constructor(url) {
    super(`Network access denied: URL not in allow-list: ${url}`);
    this.name = "NetworkAccessDeniedError";
  }
}, __name$1(_v, "NetworkAccessDeniedError"), _v);
var TooManyRedirectsError = (_w = class extends Error {
  constructor(maxRedirects) {
    super(`Too many redirects (max: ${maxRedirects})`);
    this.name = "TooManyRedirectsError";
  }
}, __name$1(_w, "TooManyRedirectsError"), _w);
var RedirectNotAllowedError = (_x = class extends Error {
  constructor(url) {
    super(`Redirect target not in allow-list: ${url}`);
    this.name = "RedirectNotAllowedError";
  }
}, __name$1(_x, "RedirectNotAllowedError"), _x);
var MethodNotAllowedError = (_y = class extends Error {
  constructor(method, allowedMethods) {
    super(`HTTP method '${method}' not allowed. Allowed methods: ${allowedMethods.join(", ")}`);
    this.name = "MethodNotAllowedError";
  }
}, __name$1(_y, "MethodNotAllowedError"), _y);
var DEFAULT_MAX_REDIRECTS = 20;
var DEFAULT_TIMEOUT_MS = 3e4;
var DEFAULT_ALLOWED_METHODS = ["GET", "HEAD"];
var BODYLESS_METHODS = /* @__PURE__ */ new Set(["GET", "HEAD", "OPTIONS"]);
var REDIRECT_CODES = /* @__PURE__ */ new Set([301, 302, 303, 307, 308]);
function createSecureFetch(config2) {
  const maxRedirects = config2.maxRedirects ?? DEFAULT_MAX_REDIRECTS;
  const timeoutMs = config2.timeoutMs ?? DEFAULT_TIMEOUT_MS;
  const allowedMethods = config2.dangerouslyAllowFullInternetAccess ? ["GET", "HEAD", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"] : config2.allowedMethods ?? DEFAULT_ALLOWED_METHODS;
  function checkAllowed(url) {
    if (config2.dangerouslyAllowFullInternetAccess) {
      return;
    }
    if (!isUrlAllowed(url, config2.allowedUrlPrefixes ?? [])) {
      throw new NetworkAccessDeniedError(url);
    }
  }
  __name$1(checkAllowed, "checkAllowed");
  function checkMethodAllowed(method) {
    if (config2.dangerouslyAllowFullInternetAccess) {
      return;
    }
    const upperMethod = method.toUpperCase();
    if (!allowedMethods.includes(upperMethod)) {
      throw new MethodNotAllowedError(upperMethod, allowedMethods);
    }
  }
  __name$1(checkMethodAllowed, "checkMethodAllowed");
  async function secureFetch(url, options = {}) {
    const method = options.method?.toUpperCase() ?? "GET";
    checkAllowed(url);
    checkMethodAllowed(method);
    let currentUrl = url;
    let redirectCount = 0;
    const followRedirects = options.followRedirects ?? true;
    const effectiveTimeout = options.timeoutMs !== void 0 ? Math.min(options.timeoutMs, timeoutMs) : timeoutMs;
    while (true) {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), effectiveTimeout);
      try {
        const fetchOptions = {
          method,
          headers: options.headers,
          signal: controller.signal,
          redirect: "manual"
          // Handle redirects manually to check allow-list
        };
        if (options.body && !BODYLESS_METHODS.has(method)) {
          fetchOptions.body = options.body;
        }
        const response = await fetch(currentUrl, fetchOptions);
        if (REDIRECT_CODES.has(response.status) && followRedirects) {
          const location = response.headers.get("location");
          if (!location) {
            return await responseToResult(response, currentUrl);
          }
          const redirectUrl = new URL(location, currentUrl).href;
          if (!config2.dangerouslyAllowFullInternetAccess) {
            if (!isUrlAllowed(redirectUrl, config2.allowedUrlPrefixes ?? [])) {
              throw new RedirectNotAllowedError(redirectUrl);
            }
          }
          redirectCount++;
          if (redirectCount > maxRedirects) {
            throw new TooManyRedirectsError(maxRedirects);
          }
          currentUrl = redirectUrl;
          continue;
        }
        return await responseToResult(response, currentUrl);
      } finally {
        clearTimeout(timeoutId);
      }
    }
  }
  __name$1(secureFetch, "secureFetch");
  return secureFetch;
}
__name$1(createSecureFetch, "createSecureFetch");
async function responseToResult(response, url) {
  const headers = {};
  response.headers.forEach((value, key2) => {
    headers[key2.toLowerCase()] = value;
  });
  return {
    status: response.status,
    statusText: response.statusText,
    headers,
    body: await response.text(),
    url
  };
}
__name$1(responseToResult, "responseToResult");
var Bash = (_z = class {
  fs;
  commands = /* @__PURE__ */ new Map();
  useDefaultLayout = false;
  limits;
  secureFetch;
  sleepFn;
  traceFn;
  logger;
  // Interpreter state (shared with interpreter instances)
  state;
  constructor(options = {}) {
    const fs3 = options.fs ?? new InMemoryFs$1(options.files);
    this.fs = fs3;
    this.useDefaultLayout = !options.cwd && !options.files;
    const cwd = options.cwd || (this.useDefaultLayout ? "/home/user" : "/");
    const env = {
      HOME: this.useDefaultLayout ? "/home/user" : "/",
      PATH: "/usr/bin:/bin",
      IFS: " 	\n",
      OSTYPE: "linux-gnu",
      MACHTYPE: "x86_64-pc-linux-gnu",
      HOSTTYPE: "x86_64",
      HOSTNAME: "localhost",
      // Match hostname command in sandboxed environment
      PWD: cwd,
      OLDPWD: cwd,
      OPTIND: "1",
      // getopts option index
      ...options.env
    };
    this.limits = resolveLimits$1({
      ...options.executionLimits,
      // Support deprecated individual options (they override executionLimits if set)
      ...options.maxCallDepth !== void 0 && {
        maxCallDepth: options.maxCallDepth
      },
      ...options.maxCommandCount !== void 0 && {
        maxCommandCount: options.maxCommandCount
      },
      ...options.maxLoopIterations !== void 0 && {
        maxLoopIterations: options.maxLoopIterations
      }
    });
    if (options.network) {
      this.secureFetch = createSecureFetch(options.network);
    }
    this.sleepFn = options.sleep;
    this.traceFn = options.trace;
    this.logger = options.logger;
    this.state = {
      env,
      cwd,
      previousDir: "/home/user",
      functions: /* @__PURE__ */ new Map(),
      localScopes: [],
      callDepth: 0,
      sourceDepth: 0,
      commandCount: 0,
      lastExitCode: 0,
      lastArg: "",
      // $_ is initially empty (or could be shell name)
      startTime: Date.now(),
      lastBackgroundPid: 0,
      bashPid: process.pid,
      // BASHPID starts as the main process PID
      nextVirtualPid: process.pid + 1,
      // Counter for unique subshell PIDs
      currentLine: 1,
      // $LINENO starts at 1
      options: {
        errexit: false,
        pipefail: false,
        nounset: false,
        xtrace: false,
        verbose: false,
        posix: false,
        allexport: false,
        noclobber: false,
        noglob: false,
        noexec: false,
        vi: false,
        emacs: false
      },
      shoptOptions: {
        extglob: false,
        dotglob: false,
        nullglob: false,
        failglob: false,
        globstar: false,
        globskipdots: true,
        // Default to true in bash >=5.2
        nocaseglob: false,
        nocasematch: false,
        expand_aliases: false,
        lastpipe: false,
        xpg_echo: false
      },
      inCondition: false,
      loopDepth: 0,
      // Export standard shell variables by default (matches bash behavior)
      // These variables are typically inherited from the parent shell environment
      exportedVars: /* @__PURE__ */ new Set([
        "HOME",
        "PATH",
        "PWD",
        "OLDPWD",
        // Also export any user-provided environment variables
        ...Object.keys(options.env || {})
      ]),
      // SHELLOPTS and BASHOPTS are readonly
      readonlyVars: /* @__PURE__ */ new Set(["SHELLOPTS", "BASHOPTS"]),
      // Hash table for PATH command lookup caching
      hashTable: /* @__PURE__ */ new Map()
    };
    this.state.env.SHELLOPTS = buildShellopts(this.state.options);
    this.state.env.BASHOPTS = buildBashopts(this.state.shoptOptions);
    initFilesystem(fs3, this.useDefaultLayout);
    if (cwd !== "/" && fs3 instanceof InMemoryFs$1) {
      try {
        fs3.mkdirSync(cwd, { recursive: true });
      } catch {
      }
    }
    for (const cmd of createLazyCommands(options.commands)) {
      this.registerCommand(cmd);
    }
    if (options.network) {
      for (const cmd of createNetworkCommands()) {
        this.registerCommand(cmd);
      }
    }
    if (options.customCommands) {
      for (const cmd of options.customCommands) {
        if (isLazyCommand(cmd)) {
          this.registerCommand(createLazyCustomCommand(cmd));
        } else {
          this.registerCommand(cmd);
        }
      }
    }
  }
  // ===========================================================================
  // Workflow Serde Support
  // ===========================================================================
  /**
   * Serialize Bash instance for Workflow DevKit.
   * Serializes filesystem and interpreter state. Callbacks (logger, trace, sleep,
   * secureFetch) and custom commands are NOT serialized - re-register after deserialize.
   */
  static [WORKFLOW_SERIALIZE$1](instance) {
    return {
      fs: instance.fs,
      state: instance.state,
      limits: instance.limits
    };
  }
  /**
   * Deserialize Bash instance for Workflow DevKit.
   * Note: Only works with InMemoryFs. Callbacks must be re-configured after deserialize.
   */
  static [WORKFLOW_DESERIALIZE$1](serialized) {
    const bash = new _z({ fs: serialized.fs });
    bash.state = serialized.state;
    bash.limits = serialized.limits;
    return bash;
  }
  registerCommand(command) {
    this.commands.set(command.name, command);
    const fs3 = this.fs;
    if (typeof fs3.writeFileSync === "function") {
      const stub = `#!/bin/bash
# Built-in command: ${command.name}
`;
      try {
        fs3.writeFileSync(`/bin/${command.name}`, stub);
      } catch {
      }
      try {
        fs3.writeFileSync(`/usr/bin/${command.name}`, stub);
      } catch {
      }
    }
  }
  logResult(result2) {
    if (this.logger) {
      if (result2.stdout) {
        this.logger.debug("stdout", { output: result2.stdout });
      }
      if (result2.stderr) {
        this.logger.info("stderr", { output: result2.stderr });
      }
      this.logger.info("exit", { exitCode: result2.exitCode });
    }
    return result2;
  }
  async exec(commandLine, options) {
    if (this.state.callDepth === 0) {
      this.state.commandCount = 0;
    }
    this.state.commandCount++;
    if (this.state.commandCount > this.limits.maxCommandCount) {
      return {
        stdout: "",
        stderr: `bash: maximum command count (${this.limits.maxCommandCount}) exceeded (possible infinite loop). Increase with executionLimits.maxCommandCount option.
`,
        exitCode: 1,
        env: { ...this.state.env, ...options?.env }
      };
    }
    if (!commandLine.trim()) {
      return {
        stdout: "",
        stderr: "",
        exitCode: 0,
        env: { ...this.state.env, ...options?.env }
      };
    }
    this.logger?.info("exec", { command: commandLine });
    const effectiveCwd = options?.cwd ?? this.state.cwd;
    let newPwd;
    let newCwd = effectiveCwd;
    if (options?.cwd) {
      if (options.env && "PWD" in options.env) {
        newPwd = options.env.PWD;
      } else if (options?.env && !("PWD" in options.env)) {
        try {
          newPwd = await this.fs.realpath(effectiveCwd);
          newCwd = newPwd;
        } catch {
          newPwd = effectiveCwd;
        }
      } else {
        newPwd = effectiveCwd;
      }
    }
    const execState = {
      ...this.state,
      env: {
        ...this.state.env,
        ...options?.env,
        // Update PWD when cwd option is provided
        ...newPwd !== void 0 ? { PWD: newPwd } : {}
      },
      cwd: newCwd,
      // Deep copy mutable objects to prevent interference
      functions: new Map(this.state.functions),
      localScopes: [...this.state.localScopes],
      options: { ...this.state.options },
      // Share hashTable reference - it should persist across exec calls
      hashTable: this.state.hashTable
    };
    let normalized = commandLine;
    if (!options?.rawScript) {
      normalized = normalizeScript(commandLine);
    }
    try {
      const ast = parse(normalized);
      const interpreterOptions = {
        fs: this.fs,
        commands: this.commands,
        limits: this.limits,
        exec: this.exec.bind(this),
        fetch: this.secureFetch,
        sleep: this.sleepFn,
        trace: this.traceFn
      };
      const interpreter = new Interpreter(interpreterOptions, execState);
      const result2 = await interpreter.executeScript(ast);
      return this.logResult(result2);
    } catch (error) {
      if (error instanceof ExitError) {
        return this.logResult({
          stdout: error.stdout,
          stderr: error.stderr,
          exitCode: error.exitCode,
          env: { ...this.state.env, ...options?.env }
        });
      }
      if (error instanceof PosixFatalError) {
        return this.logResult({
          stdout: error.stdout,
          stderr: error.stderr,
          exitCode: error.exitCode,
          env: { ...this.state.env, ...options?.env }
        });
      }
      if (error instanceof ArithmeticError) {
        return this.logResult({
          stdout: error.stdout,
          stderr: error.stderr,
          exitCode: 1,
          env: { ...this.state.env, ...options?.env }
        });
      }
      if (error instanceof ExecutionLimitError) {
        return this.logResult({
          stdout: error.stdout,
          stderr: error.stderr,
          exitCode: ExecutionLimitError.EXIT_CODE,
          env: { ...this.state.env, ...options?.env }
        });
      }
      if (error.name === "ParseException") {
        return this.logResult({
          stdout: "",
          stderr: `bash: syntax error: ${error.message}
`,
          exitCode: 2,
          env: { ...this.state.env, ...options?.env }
        });
      }
      if (error instanceof LexerError) {
        return this.logResult({
          stdout: "",
          stderr: `bash: ${error.message}
`,
          exitCode: 2,
          env: { ...this.state.env, ...options?.env }
        });
      }
      if (error instanceof RangeError) {
        return this.logResult({
          stdout: "",
          stderr: `bash: ${error.message}
`,
          exitCode: 1,
          env: { ...this.state.env, ...options?.env }
        });
      }
      throw error;
    }
  }
  // ===========================================================================
  // PUBLIC API
  // ===========================================================================
  async readFile(path2) {
    return this.fs.readFile(this.fs.resolvePath(this.state.cwd, path2));
  }
  async writeFile(path2, content) {
    return this.fs.writeFile(this.fs.resolvePath(this.state.cwd, path2), content);
  }
  getCwd() {
    return this.state.cwd;
  }
  getEnv() {
    return { ...this.state.env };
  }
}, __name$1(_z, "Bash"), _z);
function normalizeScript(script) {
  const lines = script.split("\n");
  const result2 = [];
  const pendingDelimiters = [];
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    if (pendingDelimiters.length > 0) {
      const current = pendingDelimiters[pendingDelimiters.length - 1];
      const lineToCheck = current.stripTabs ? line.replace(/^\t+/, "") : line;
      if (lineToCheck === current.delimiter) {
        result2.push(line.trimStart());
        pendingDelimiters.pop();
        continue;
      }
      result2.push(line);
      continue;
    }
    const normalizedLine = line.trimStart();
    result2.push(normalizedLine);
    const heredocPattern = /<<(-?)\s*(['"]?)([\w-]+)\2/g;
    for (const match of normalizedLine.matchAll(heredocPattern)) {
      const stripTabs = match[1] === "-";
      const delimiter = match[3];
      pendingDelimiters.push({ delimiter, stripTabs });
    }
  }
  return result2.join("\n");
}
__name$1(normalizeScript, "normalizeScript");
_A = class {
  baseFs;
  mounts = /* @__PURE__ */ new Map();
  constructor(options) {
    this.baseFs = options?.base ?? new InMemoryFs$1();
    if (options?.mounts) {
      for (const { mountPoint, filesystem } of options.mounts) {
        this.mount(mountPoint, filesystem);
      }
    }
  }
  /**
   * Mount a filesystem at the specified virtual path.
   *
   * @param mountPoint - The virtual path where the filesystem will be accessible
   * @param filesystem - The filesystem to mount
   * @throws Error if mounting at root '/' or inside an existing mount
   */
  mount(mountPoint, filesystem) {
    this.validateMountPath(mountPoint);
    const normalized = this.normalizePath(mountPoint);
    this.validateMount(normalized);
    this.mounts.set(normalized, {
      mountPoint: normalized,
      filesystem
    });
  }
  /**
   * Unmount the filesystem at the specified path.
   *
   * @param mountPoint - The virtual path to unmount
   * @throws Error if no filesystem is mounted at this path
   */
  unmount(mountPoint) {
    const normalized = this.normalizePath(mountPoint);
    if (!this.mounts.has(normalized)) {
      throw new Error(`No filesystem mounted at '${mountPoint}'`);
    }
    this.mounts.delete(normalized);
  }
  /**
   * Get all current mounts.
   */
  getMounts() {
    return Array.from(this.mounts.values()).map((entry) => ({
      mountPoint: entry.mountPoint,
      filesystem: entry.filesystem
    }));
  }
  /**
   * Check if a path is exactly a mount point.
   */
  isMountPoint(path2) {
    const normalized = this.normalizePath(path2);
    return this.mounts.has(normalized);
  }
  /**
   * Validate mount path format before normalization.
   * Rejects paths containing . or .. segments.
   */
  validateMountPath(mountPoint) {
    const segments = mountPoint.split("/");
    for (const segment of segments) {
      if (segment === "." || segment === "..") {
        throw new Error(`Invalid mount point '${mountPoint}': contains '.' or '..' segments`);
      }
    }
  }
  /**
   * Validate that a mount point is allowed.
   */
  validateMount(mountPoint) {
    if (mountPoint === "/") {
      throw new Error("Cannot mount at root '/'");
    }
    for (const existingMount of this.mounts.keys()) {
      if (existingMount === mountPoint) {
        continue;
      }
      if (mountPoint.startsWith(`${existingMount}/`)) {
        throw new Error(`Cannot mount at '${mountPoint}': inside existing mount '${existingMount}'`);
      }
      if (existingMount.startsWith(`${mountPoint}/`)) {
        throw new Error(`Cannot mount at '${mountPoint}': would contain existing mount '${existingMount}'`);
      }
    }
  }
  /**
   * Normalize a path to a consistent format.
   */
  normalizePath(path2) {
    if (!path2 || path2 === "/")
      return "/";
    let normalized = path2.endsWith("/") && path2 !== "/" ? path2.slice(0, -1) : path2;
    if (!normalized.startsWith("/")) {
      normalized = `/${normalized}`;
    }
    const parts = normalized.split("/").filter((p) => p && p !== ".");
    const resolved = [];
    for (const part of parts) {
      if (part === "..") {
        resolved.pop();
      } else {
        resolved.push(part);
      }
    }
    return `/${resolved.join("/")}`;
  }
  /**
   * Route a path to the appropriate filesystem.
   * Returns the filesystem and the relative path within that filesystem.
   */
  routePath(path2) {
    const normalized = this.normalizePath(path2);
    let bestMatch = null;
    let bestMatchLength = 0;
    for (const entry of this.mounts.values()) {
      const mp = entry.mountPoint;
      if (normalized === mp) {
        return { fs: entry.filesystem, relativePath: "/" };
      }
      if (normalized.startsWith(`${mp}/`)) {
        if (mp.length > bestMatchLength) {
          bestMatch = entry;
          bestMatchLength = mp.length;
        }
      }
    }
    if (bestMatch) {
      const relativePath = normalized.slice(bestMatchLength);
      return {
        fs: bestMatch.filesystem,
        relativePath: relativePath || "/"
      };
    }
    return { fs: this.baseFs, relativePath: normalized };
  }
  /**
   * Get mount points that are immediate children of a directory.
   */
  getChildMountPoints(dirPath) {
    const normalized = this.normalizePath(dirPath);
    const prefix = normalized === "/" ? "/" : `${normalized}/`;
    const children = [];
    for (const mountPoint of this.mounts.keys()) {
      if (mountPoint.startsWith(prefix)) {
        const remainder = mountPoint.slice(prefix.length);
        const childName = remainder.split("/")[0];
        if (childName && !children.includes(childName)) {
          children.push(childName);
        }
      }
    }
    return children;
  }
  // ==================== IFileSystem Implementation ====================
  async readFile(path2, options) {
    const { fs: fs3, relativePath } = this.routePath(path2);
    return fs3.readFile(relativePath, options);
  }
  async readFileBuffer(path2) {
    const { fs: fs3, relativePath } = this.routePath(path2);
    return fs3.readFileBuffer(relativePath);
  }
  async writeFile(path2, content, options) {
    const { fs: fs3, relativePath } = this.routePath(path2);
    return fs3.writeFile(relativePath, content, options);
  }
  async appendFile(path2, content, options) {
    const { fs: fs3, relativePath } = this.routePath(path2);
    return fs3.appendFile(relativePath, content, options);
  }
  async exists(path2) {
    const normalized = this.normalizePath(path2);
    if (this.mounts.has(normalized)) {
      return true;
    }
    const childMounts = this.getChildMountPoints(normalized);
    if (childMounts.length > 0) {
      return true;
    }
    const { fs: fs3, relativePath } = this.routePath(path2);
    return fs3.exists(relativePath);
  }
  async stat(path2) {
    const normalized = this.normalizePath(path2);
    const mountEntry = this.mounts.get(normalized);
    if (mountEntry) {
      try {
        return await mountEntry.filesystem.stat("/");
      } catch {
        return {
          isFile: false,
          isDirectory: true,
          isSymbolicLink: false,
          mode: 493,
          size: 0,
          mtime: /* @__PURE__ */ new Date()
        };
      }
    }
    const childMounts = this.getChildMountPoints(normalized);
    if (childMounts.length > 0) {
      try {
        const baseStat = await this.baseFs.stat(normalized);
        return baseStat;
      } catch {
        return {
          isFile: false,
          isDirectory: true,
          isSymbolicLink: false,
          mode: 493,
          size: 0,
          mtime: /* @__PURE__ */ new Date()
        };
      }
    }
    const { fs: fs3, relativePath } = this.routePath(path2);
    return fs3.stat(relativePath);
  }
  async lstat(path2) {
    const normalized = this.normalizePath(path2);
    const mountEntry = this.mounts.get(normalized);
    if (mountEntry) {
      try {
        return await mountEntry.filesystem.lstat("/");
      } catch {
        return {
          isFile: false,
          isDirectory: true,
          isSymbolicLink: false,
          mode: 493,
          size: 0,
          mtime: /* @__PURE__ */ new Date()
        };
      }
    }
    const childMounts = this.getChildMountPoints(normalized);
    if (childMounts.length > 0) {
      try {
        return await this.baseFs.lstat(normalized);
      } catch {
        return {
          isFile: false,
          isDirectory: true,
          isSymbolicLink: false,
          mode: 493,
          size: 0,
          mtime: /* @__PURE__ */ new Date()
        };
      }
    }
    const { fs: fs3, relativePath } = this.routePath(path2);
    return fs3.lstat(relativePath);
  }
  async mkdir(path2, options) {
    const normalized = this.normalizePath(path2);
    if (this.mounts.has(normalized)) {
      if (options?.recursive) {
        return;
      }
      throw new Error(`EEXIST: directory already exists, mkdir '${path2}'`);
    }
    const childMounts = this.getChildMountPoints(normalized);
    if (childMounts.length > 0 && options?.recursive) {
      return;
    }
    const { fs: fs3, relativePath } = this.routePath(path2);
    return fs3.mkdir(relativePath, options);
  }
  async readdir(path2) {
    const normalized = this.normalizePath(path2);
    const entries = /* @__PURE__ */ new Set();
    let readdirError = null;
    const { fs: fs3, relativePath } = this.routePath(path2);
    try {
      const fsEntries = await fs3.readdir(relativePath);
      for (const entry of fsEntries) {
        entries.add(entry);
      }
    } catch (err) {
      const code = err.code;
      const message = err.message || "";
      if (code !== "ENOENT" && !message.includes("ENOENT")) {
        throw err;
      }
      readdirError = err;
    }
    const childMounts = this.getChildMountPoints(normalized);
    for (const child of childMounts) {
      entries.add(child);
    }
    if (entries.size === 0 && readdirError && !this.mounts.has(normalized)) {
      throw readdirError;
    }
    return Array.from(entries).sort();
  }
  async rm(path2, options) {
    const normalized = this.normalizePath(path2);
    if (this.mounts.has(normalized)) {
      throw new Error(`EBUSY: mount point, cannot remove '${path2}'`);
    }
    const childMounts = this.getChildMountPoints(normalized);
    if (childMounts.length > 0) {
      throw new Error(`EBUSY: contains mount points, cannot remove '${path2}'`);
    }
    const { fs: fs3, relativePath } = this.routePath(path2);
    return fs3.rm(relativePath, options);
  }
  async cp(src, dest, options) {
    const srcRoute = this.routePath(src);
    const destRoute = this.routePath(dest);
    if (srcRoute.fs === destRoute.fs) {
      return srcRoute.fs.cp(srcRoute.relativePath, destRoute.relativePath, options);
    }
    return this.crossMountCopy(src, dest, options);
  }
  async mv(src, dest) {
    const normalized = this.normalizePath(src);
    if (this.mounts.has(normalized)) {
      throw new Error(`EBUSY: mount point, cannot move '${src}'`);
    }
    const srcRoute = this.routePath(src);
    const destRoute = this.routePath(dest);
    if (srcRoute.fs === destRoute.fs) {
      return srcRoute.fs.mv(srcRoute.relativePath, destRoute.relativePath);
    }
    await this.cp(src, dest, { recursive: true });
    await this.rm(src, { recursive: true });
  }
  resolvePath(base, path2) {
    if (path2.startsWith("/")) {
      return this.normalizePath(path2);
    }
    const combined = base === "/" ? `/${path2}` : `${base}/${path2}`;
    return this.normalizePath(combined);
  }
  getAllPaths() {
    const allPaths = /* @__PURE__ */ new Set();
    for (const p of this.baseFs.getAllPaths()) {
      allPaths.add(p);
    }
    for (const mountPoint of this.mounts.keys()) {
      const parts = mountPoint.split("/").filter(Boolean);
      let current = "";
      for (const part of parts) {
        current = `${current}/${part}`;
        allPaths.add(current);
      }
      const entry = this.mounts.get(mountPoint);
      if (!entry)
        continue;
      for (const p of entry.filesystem.getAllPaths()) {
        if (p === "/") {
          allPaths.add(mountPoint);
        } else {
          allPaths.add(`${mountPoint}${p}`);
        }
      }
    }
    return Array.from(allPaths).sort();
  }
  async chmod(path2, mode) {
    const normalized = this.normalizePath(path2);
    const mountEntry = this.mounts.get(normalized);
    if (mountEntry) {
      return mountEntry.filesystem.chmod("/", mode);
    }
    const { fs: fs3, relativePath } = this.routePath(path2);
    return fs3.chmod(relativePath, mode);
  }
  async symlink(target, linkPath) {
    const { fs: fs3, relativePath } = this.routePath(linkPath);
    return fs3.symlink(target, relativePath);
  }
  async link(existingPath, newPath) {
    const existingRoute = this.routePath(existingPath);
    const newRoute = this.routePath(newPath);
    if (existingRoute.fs !== newRoute.fs) {
      throw new Error(`EXDEV: cross-device link not permitted, link '${existingPath}' -> '${newPath}'`);
    }
    return existingRoute.fs.link(existingRoute.relativePath, newRoute.relativePath);
  }
  async readlink(path2) {
    const { fs: fs3, relativePath } = this.routePath(path2);
    return fs3.readlink(relativePath);
  }
  /**
   * Resolve all symlinks in a path to get the canonical physical path.
   * This is equivalent to POSIX realpath().
   */
  async realpath(path2) {
    const normalized = this.normalizePath(path2);
    const mountEntry = this.mounts.get(normalized);
    if (mountEntry) {
      return normalized;
    }
    const { fs: fs3, relativePath } = this.routePath(path2);
    const resolvedRelative = await fs3.realpath(relativePath);
    for (const [mp, _entry] of this.mounts) {
      if (normalized === mp || normalized.startsWith(`${mp}/`)) {
        if (resolvedRelative === "/") {
          return mp;
        }
        return `${mp}${resolvedRelative}`;
      }
    }
    return resolvedRelative;
  }
  /**
   * Perform a cross-mount copy operation.
   */
  async crossMountCopy(src, dest, options) {
    const srcStat = await this.lstat(src);
    if (srcStat.isFile) {
      const content = await this.readFileBuffer(src);
      await this.writeFile(dest, content);
      await this.chmod(dest, srcStat.mode);
    } else if (srcStat.isDirectory) {
      if (!options?.recursive) {
        throw new Error(`cp: ${src} is a directory (not copied)`);
      }
      await this.mkdir(dest, { recursive: true });
      const children = await this.readdir(src);
      for (const child of children) {
        const srcChild = src === "/" ? `/${child}` : `${src}/${child}`;
        const destChild = dest === "/" ? `/${child}` : `${dest}/${child}`;
        await this.crossMountCopy(srcChild, destChild, options);
      }
    } else if (srcStat.isSymbolicLink) {
      const target = await this.readlink(src);
      await this.symlink(target, dest);
    }
  }
  /**
   * Set access and modification times of a file
   * @param path - The file path
   * @param atime - Access time
   * @param mtime - Modification time
   */
  async utimes(path2, atime, mtime) {
    const { fs: fs3, relativePath } = this.routePath(path2);
    return fs3.utimes(relativePath, atime, mtime);
  }
}, __name$1(_A, "MountableFs"), _A;
var DEFAULT_MOUNT_POINT = "/home/user/project";
var OverlayFs = (_B = class {
  root;
  mountPoint;
  readOnly;
  memory = /* @__PURE__ */ new Map();
  deleted = /* @__PURE__ */ new Set();
  constructor(options) {
    this.root = path.resolve(options.root);
    const mp = options.mountPoint ?? DEFAULT_MOUNT_POINT;
    this.mountPoint = mp === "/" ? "/" : mp.replace(/\/+$/, "");
    if (!this.mountPoint.startsWith("/")) {
      throw new Error(`Mount point must be an absolute path: ${mp}`);
    }
    this.readOnly = options.readOnly ?? false;
    if (!fs2.existsSync(this.root)) {
      throw new Error(`OverlayFs root does not exist: ${this.root}`);
    }
    const stat = fs2.statSync(this.root);
    if (!stat.isDirectory()) {
      throw new Error(`OverlayFs root is not a directory: ${this.root}`);
    }
    this.createMountPointDirs();
  }
  /**
   * Throws an error if the filesystem is in read-only mode.
   */
  assertWritable(operation) {
    if (this.readOnly) {
      throw new Error(`EROFS: read-only file system, ${operation}`);
    }
  }
  /**
   * Create directory entries for the mount point path
   */
  createMountPointDirs() {
    const parts = this.mountPoint.split("/").filter(Boolean);
    let current = "";
    for (const part of parts) {
      current += `/${part}`;
      if (!this.memory.has(current)) {
        this.memory.set(current, {
          type: "directory",
          mode: 493,
          mtime: /* @__PURE__ */ new Date()
        });
      }
    }
    if (!this.memory.has("/")) {
      this.memory.set("/", {
        type: "directory",
        mode: 493,
        mtime: /* @__PURE__ */ new Date()
      });
    }
  }
  /**
   * Get the mount point for this overlay
   */
  getMountPoint() {
    return this.mountPoint;
  }
  /**
   * Create a virtual directory in memory (sync, for initialization)
   */
  mkdirSync(path2, _options) {
    const normalized = this.normalizePath(path2);
    const parts = normalized.split("/").filter(Boolean);
    let current = "";
    for (const part of parts) {
      current += `/${part}`;
      if (!this.memory.has(current)) {
        this.memory.set(current, {
          type: "directory",
          mode: 493,
          mtime: /* @__PURE__ */ new Date()
        });
      }
    }
  }
  /**
   * Create a virtual file in memory (sync, for initialization)
   */
  writeFileSync(path2, content) {
    const normalized = this.normalizePath(path2);
    const parent = this.getDirname(normalized);
    if (parent !== "/") {
      this.mkdirSync(parent);
    }
    const buffer = content instanceof Uint8Array ? content : new TextEncoder().encode(content);
    this.memory.set(normalized, {
      type: "file",
      content: buffer,
      mode: 420,
      mtime: /* @__PURE__ */ new Date()
    });
  }
  getDirname(path2) {
    const lastSlash = path2.lastIndexOf("/");
    return lastSlash === 0 ? "/" : path2.slice(0, lastSlash);
  }
  /**
   * Normalize a virtual path (resolve . and .., ensure starts with /)
   */
  normalizePath(path2) {
    if (!path2 || path2 === "/")
      return "/";
    let normalized = path2.endsWith("/") && path2 !== "/" ? path2.slice(0, -1) : path2;
    if (!normalized.startsWith("/")) {
      normalized = `/${normalized}`;
    }
    const parts = normalized.split("/").filter((p) => p && p !== ".");
    const resolved = [];
    for (const part of parts) {
      if (part === "..") {
        resolved.pop();
      } else {
        resolved.push(part);
      }
    }
    return `/${resolved.join("/")}` || "/";
  }
  /**
   * Check if a normalized virtual path is under the mount point.
   * Returns the relative path within the mount point, or null if not under it.
   */
  getRelativeToMount(normalizedPath) {
    if (this.mountPoint === "/") {
      return normalizedPath;
    }
    if (normalizedPath === this.mountPoint) {
      return "/";
    }
    if (normalizedPath.startsWith(`${this.mountPoint}/`)) {
      return normalizedPath.slice(this.mountPoint.length);
    }
    return null;
  }
  /**
   * Convert a virtual path to a real filesystem path.
   * Returns null if the path is not under the mount point or would escape the root.
   */
  toRealPath(virtualPath) {
    const normalized = this.normalizePath(virtualPath);
    const relativePath = this.getRelativeToMount(normalized);
    if (relativePath === null) {
      return null;
    }
    const realPath = path.join(this.root, relativePath);
    const resolvedReal = path.resolve(realPath);
    if (!resolvedReal.startsWith(this.root) && resolvedReal !== this.root.replace(/\/$/, "")) {
      return null;
    }
    return resolvedReal;
  }
  dirname(path2) {
    const normalized = this.normalizePath(path2);
    if (normalized === "/")
      return "/";
    const lastSlash = normalized.lastIndexOf("/");
    return lastSlash === 0 ? "/" : normalized.slice(0, lastSlash);
  }
  ensureParentDirs(path2) {
    const dir = this.dirname(path2);
    if (dir === "/")
      return;
    if (!this.memory.has(dir)) {
      this.ensureParentDirs(dir);
      this.memory.set(dir, {
        type: "directory",
        mode: 493,
        mtime: /* @__PURE__ */ new Date()
      });
    }
    this.deleted.delete(dir);
  }
  /**
   * Check if a path exists in the overlay (memory + real fs - deleted)
   */
  async existsInOverlay(virtualPath) {
    const normalized = this.normalizePath(virtualPath);
    if (this.deleted.has(normalized)) {
      return false;
    }
    if (this.memory.has(normalized)) {
      return true;
    }
    const realPath = this.toRealPath(normalized);
    if (!realPath) {
      return false;
    }
    try {
      await fs2.promises.access(realPath);
      return true;
    } catch {
      return false;
    }
  }
  async readFile(path2, options) {
    const buffer = await this.readFileBuffer(path2);
    const encoding = getEncoding$1(options);
    return fromBuffer$1(buffer, encoding);
  }
  async readFileBuffer(path2, seen = /* @__PURE__ */ new Set()) {
    const normalized = this.normalizePath(path2);
    if (seen.has(normalized)) {
      throw new Error(`ELOOP: too many levels of symbolic links, open '${path2}'`);
    }
    seen.add(normalized);
    if (this.deleted.has(normalized)) {
      throw new Error(`ENOENT: no such file or directory, open '${path2}'`);
    }
    const memEntry = this.memory.get(normalized);
    if (memEntry) {
      if (memEntry.type === "symlink") {
        const target = this.resolveSymlink(normalized, memEntry.target);
        return this.readFileBuffer(target, seen);
      }
      if (memEntry.type !== "file") {
        throw new Error(`EISDIR: illegal operation on a directory, read '${path2}'`);
      }
      return memEntry.content;
    }
    const realPath = this.toRealPath(normalized);
    if (!realPath) {
      throw new Error(`ENOENT: no such file or directory, open '${path2}'`);
    }
    try {
      const stat = await fs2.promises.lstat(realPath);
      if (stat.isSymbolicLink()) {
        const target = await fs2.promises.readlink(realPath);
        const resolvedTarget = this.resolveSymlink(normalized, target);
        return this.readFileBuffer(resolvedTarget, seen);
      }
      if (stat.isDirectory()) {
        throw new Error(`EISDIR: illegal operation on a directory, read '${path2}'`);
      }
      const content = await fs2.promises.readFile(realPath);
      return new Uint8Array(content);
    } catch (e) {
      if (e.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, open '${path2}'`);
      }
      throw e;
    }
  }
  async writeFile(path2, content, options) {
    this.assertWritable(`write '${path2}'`);
    const normalized = this.normalizePath(path2);
    this.ensureParentDirs(normalized);
    const encoding = getEncoding$1(options);
    const buffer = toBuffer$1(content, encoding);
    this.memory.set(normalized, {
      type: "file",
      content: buffer,
      mode: 420,
      mtime: /* @__PURE__ */ new Date()
    });
    this.deleted.delete(normalized);
  }
  async appendFile(path2, content, options) {
    this.assertWritable(`append '${path2}'`);
    const normalized = this.normalizePath(path2);
    const encoding = getEncoding$1(options);
    const newBuffer = toBuffer$1(content, encoding);
    let existingBuffer;
    try {
      existingBuffer = await this.readFileBuffer(normalized);
    } catch {
      existingBuffer = new Uint8Array(0);
    }
    const combined = new Uint8Array(existingBuffer.length + newBuffer.length);
    combined.set(existingBuffer);
    combined.set(newBuffer, existingBuffer.length);
    this.ensureParentDirs(normalized);
    this.memory.set(normalized, {
      type: "file",
      content: combined,
      mode: 420,
      mtime: /* @__PURE__ */ new Date()
    });
    this.deleted.delete(normalized);
  }
  async exists(path2) {
    return this.existsInOverlay(path2);
  }
  async stat(path2, seen = /* @__PURE__ */ new Set()) {
    const normalized = this.normalizePath(path2);
    if (seen.has(normalized)) {
      throw new Error(`ELOOP: too many levels of symbolic links, stat '${path2}'`);
    }
    seen.add(normalized);
    if (this.deleted.has(normalized)) {
      throw new Error(`ENOENT: no such file or directory, stat '${path2}'`);
    }
    const entry = this.memory.get(normalized);
    if (entry) {
      if (entry.type === "symlink") {
        const target = this.resolveSymlink(normalized, entry.target);
        return this.stat(target, seen);
      }
      let size = 0;
      if (entry.type === "file") {
        size = entry.content.length;
      }
      return {
        isFile: entry.type === "file",
        isDirectory: entry.type === "directory",
        isSymbolicLink: false,
        mode: entry.mode,
        size,
        mtime: entry.mtime
      };
    }
    const realPath = this.toRealPath(normalized);
    if (!realPath) {
      throw new Error(`ENOENT: no such file or directory, stat '${path2}'`);
    }
    try {
      const stat = await fs2.promises.stat(realPath);
      return {
        isFile: stat.isFile(),
        isDirectory: stat.isDirectory(),
        isSymbolicLink: false,
        mode: stat.mode,
        size: stat.size,
        mtime: stat.mtime
      };
    } catch (e) {
      if (e.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, stat '${path2}'`);
      }
      throw e;
    }
  }
  async lstat(path2) {
    const normalized = this.normalizePath(path2);
    if (this.deleted.has(normalized)) {
      throw new Error(`ENOENT: no such file or directory, lstat '${path2}'`);
    }
    const entry = this.memory.get(normalized);
    if (entry) {
      if (entry.type === "symlink") {
        return {
          isFile: false,
          isDirectory: false,
          isSymbolicLink: true,
          mode: entry.mode,
          size: entry.target.length,
          mtime: entry.mtime
        };
      }
      let size = 0;
      if (entry.type === "file") {
        size = entry.content.length;
      }
      return {
        isFile: entry.type === "file",
        isDirectory: entry.type === "directory",
        isSymbolicLink: false,
        mode: entry.mode,
        size,
        mtime: entry.mtime
      };
    }
    const realPath = this.toRealPath(normalized);
    if (!realPath) {
      throw new Error(`ENOENT: no such file or directory, lstat '${path2}'`);
    }
    try {
      const stat = await fs2.promises.lstat(realPath);
      return {
        isFile: stat.isFile(),
        isDirectory: stat.isDirectory(),
        isSymbolicLink: stat.isSymbolicLink(),
        mode: stat.mode,
        size: stat.size,
        mtime: stat.mtime
      };
    } catch (e) {
      if (e.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, lstat '${path2}'`);
      }
      throw e;
    }
  }
  resolveSymlink(symlinkPath, target) {
    if (target.startsWith("/")) {
      return this.normalizePath(target);
    }
    const dir = this.dirname(symlinkPath);
    return this.normalizePath(dir === "/" ? `/${target}` : `${dir}/${target}`);
  }
  async mkdir(path2, options) {
    this.assertWritable(`mkdir '${path2}'`);
    const normalized = this.normalizePath(path2);
    const exists = await this.existsInOverlay(normalized);
    if (exists) {
      if (!options?.recursive) {
        throw new Error(`EEXIST: file already exists, mkdir '${path2}'`);
      }
      return;
    }
    const parent = this.dirname(normalized);
    if (parent !== "/") {
      const parentExists = await this.existsInOverlay(parent);
      if (!parentExists) {
        if (options?.recursive) {
          await this.mkdir(parent, { recursive: true });
        } else {
          throw new Error(`ENOENT: no such file or directory, mkdir '${path2}'`);
        }
      }
    }
    this.memory.set(normalized, {
      type: "directory",
      mode: 493,
      mtime: /* @__PURE__ */ new Date()
    });
    this.deleted.delete(normalized);
  }
  /**
   * Core readdir implementation that returns entries with file types.
   * Both readdir and readdirWithFileTypes use this shared implementation.
   */
  async readdirCore(path2, normalized) {
    if (this.deleted.has(normalized)) {
      throw new Error(`ENOENT: no such file or directory, scandir '${path2}'`);
    }
    const entriesMap = /* @__PURE__ */ new Map();
    const deletedChildren = /* @__PURE__ */ new Set();
    const prefix = normalized === "/" ? "/" : `${normalized}/`;
    for (const deletedPath of this.deleted) {
      if (deletedPath.startsWith(prefix)) {
        const rest = deletedPath.slice(prefix.length);
        const name = rest.split("/")[0];
        if (name && !rest.includes("/", name.length)) {
          deletedChildren.add(name);
        }
      }
    }
    for (const [memPath, entry] of this.memory) {
      if (memPath === normalized)
        continue;
      if (memPath.startsWith(prefix)) {
        const rest = memPath.slice(prefix.length);
        const name = rest.split("/")[0];
        if (name && !deletedChildren.has(name) && !rest.includes("/", 1)) {
          entriesMap.set(name, {
            name,
            isFile: entry.type === "file",
            isDirectory: entry.type === "directory",
            isSymbolicLink: entry.type === "symlink"
          });
        }
      }
    }
    const realPath = this.toRealPath(normalized);
    if (realPath) {
      try {
        const realEntries = await fs2.promises.readdir(realPath, {
          withFileTypes: true
        });
        for (const dirent of realEntries) {
          if (!deletedChildren.has(dirent.name) && !entriesMap.has(dirent.name)) {
            entriesMap.set(dirent.name, {
              name: dirent.name,
              isFile: dirent.isFile(),
              isDirectory: dirent.isDirectory(),
              isSymbolicLink: dirent.isSymbolicLink()
            });
          }
        }
      } catch (e) {
        if (e.code === "ENOENT") {
          if (!this.memory.has(normalized)) {
            throw new Error(`ENOENT: no such file or directory, scandir '${path2}'`);
          }
        } else if (e.code !== "ENOTDIR") {
          throw e;
        }
      }
    }
    return entriesMap;
  }
  /**
   * Follow symlinks to resolve the final directory path.
   * Returns outsideOverlay: true if the symlink points outside the overlay or
   * the resolved target doesn't exist (security - broken symlinks return []).
   */
  async resolveForReaddir(path2, followedSymlink = false) {
    let normalized = this.normalizePath(path2);
    const seen = /* @__PURE__ */ new Set();
    let didFollowSymlink = followedSymlink;
    let entry = this.memory.get(normalized);
    while (entry && entry.type === "symlink") {
      if (seen.has(normalized)) {
        throw new Error(`ELOOP: too many levels of symbolic links, scandir '${path2}'`);
      }
      seen.add(normalized);
      didFollowSymlink = true;
      normalized = this.resolveSymlink(normalized, entry.target);
      entry = this.memory.get(normalized);
    }
    if (entry) {
      return { normalized, outsideOverlay: false };
    }
    const relativePath = this.getRelativeToMount(normalized);
    if (relativePath === null) {
      return { normalized, outsideOverlay: true };
    }
    const realPath = this.toRealPath(normalized);
    if (!realPath) {
      return { normalized, outsideOverlay: true };
    }
    try {
      const stat = await fs2.promises.lstat(realPath);
      if (stat.isSymbolicLink()) {
        const target = await fs2.promises.readlink(realPath);
        const resolvedTarget = this.resolveSymlink(normalized, target);
        return this.resolveForReaddir(resolvedTarget, true);
      }
      return { normalized, outsideOverlay: false };
    } catch {
      if (didFollowSymlink) {
        return { normalized, outsideOverlay: true };
      }
      return { normalized, outsideOverlay: false };
    }
  }
  async readdir(path2) {
    const { normalized, outsideOverlay } = await this.resolveForReaddir(path2);
    if (outsideOverlay) {
      return [];
    }
    const entriesMap = await this.readdirCore(path2, normalized);
    return Array.from(entriesMap.keys()).sort((a, b) => a < b ? -1 : a > b ? 1 : 0);
  }
  async readdirWithFileTypes(path2) {
    const { normalized, outsideOverlay } = await this.resolveForReaddir(path2);
    if (outsideOverlay) {
      return [];
    }
    const entriesMap = await this.readdirCore(path2, normalized);
    return Array.from(entriesMap.values()).sort((a, b) => a.name < b.name ? -1 : a.name > b.name ? 1 : 0);
  }
  async rm(path2, options) {
    this.assertWritable(`rm '${path2}'`);
    const normalized = this.normalizePath(path2);
    const exists = await this.existsInOverlay(normalized);
    if (!exists) {
      if (options?.force)
        return;
      throw new Error(`ENOENT: no such file or directory, rm '${path2}'`);
    }
    try {
      const stat = await this.stat(normalized);
      if (stat.isDirectory) {
        const children = await this.readdir(normalized);
        if (children.length > 0) {
          if (!options?.recursive) {
            throw new Error(`ENOTEMPTY: directory not empty, rm '${path2}'`);
          }
          for (const child of children) {
            const childPath = normalized === "/" ? `/${child}` : `${normalized}/${child}`;
            await this.rm(childPath, options);
          }
        }
      }
    } catch {
    }
    this.deleted.add(normalized);
    this.memory.delete(normalized);
  }
  async cp(src, dest, options) {
    this.assertWritable(`cp '${dest}'`);
    const srcNorm = this.normalizePath(src);
    const destNorm = this.normalizePath(dest);
    const srcExists = await this.existsInOverlay(srcNorm);
    if (!srcExists) {
      throw new Error(`ENOENT: no such file or directory, cp '${src}'`);
    }
    const srcStat = await this.stat(srcNorm);
    if (srcStat.isFile) {
      const content = await this.readFileBuffer(srcNorm);
      await this.writeFile(destNorm, content);
    } else if (srcStat.isDirectory) {
      if (!options?.recursive) {
        throw new Error(`EISDIR: is a directory, cp '${src}'`);
      }
      await this.mkdir(destNorm, { recursive: true });
      const children = await this.readdir(srcNorm);
      for (const child of children) {
        const srcChild = srcNorm === "/" ? `/${child}` : `${srcNorm}/${child}`;
        const destChild = destNorm === "/" ? `/${child}` : `${destNorm}/${child}`;
        await this.cp(srcChild, destChild, options);
      }
    }
  }
  async mv(src, dest) {
    this.assertWritable(`mv '${dest}'`);
    await this.cp(src, dest, { recursive: true });
    await this.rm(src, { recursive: true });
  }
  resolvePath(base, path2) {
    if (path2.startsWith("/")) {
      return this.normalizePath(path2);
    }
    const combined = base === "/" ? `/${path2}` : `${base}/${path2}`;
    return this.normalizePath(combined);
  }
  getAllPaths() {
    const paths = new Set(this.memory.keys());
    for (const deleted of this.deleted) {
      paths.delete(deleted);
    }
    this.scanRealFs("/", paths);
    return Array.from(paths);
  }
  scanRealFs(virtualDir, paths) {
    if (this.deleted.has(virtualDir))
      return;
    const realPath = this.toRealPath(virtualDir);
    if (!realPath)
      return;
    try {
      const entries = fs2.readdirSync(realPath);
      for (const entry of entries) {
        const virtualPath = virtualDir === "/" ? `/${entry}` : `${virtualDir}/${entry}`;
        if (this.deleted.has(virtualPath))
          continue;
        paths.add(virtualPath);
        const entryRealPath = path.join(realPath, entry);
        const stat = fs2.statSync(entryRealPath);
        if (stat.isDirectory()) {
          this.scanRealFs(virtualPath, paths);
        }
      }
    } catch {
    }
  }
  async chmod(path2, mode) {
    this.assertWritable(`chmod '${path2}'`);
    const normalized = this.normalizePath(path2);
    const exists = await this.existsInOverlay(normalized);
    if (!exists) {
      throw new Error(`ENOENT: no such file or directory, chmod '${path2}'`);
    }
    const entry = this.memory.get(normalized);
    if (entry) {
      entry.mode = mode;
      return;
    }
    const stat = await this.stat(normalized);
    if (stat.isFile) {
      const content = await this.readFileBuffer(normalized);
      this.memory.set(normalized, {
        type: "file",
        content,
        mode,
        mtime: /* @__PURE__ */ new Date()
      });
    } else if (stat.isDirectory) {
      this.memory.set(normalized, {
        type: "directory",
        mode,
        mtime: /* @__PURE__ */ new Date()
      });
    }
  }
  async symlink(target, linkPath) {
    this.assertWritable(`symlink '${linkPath}'`);
    const normalized = this.normalizePath(linkPath);
    const exists = await this.existsInOverlay(normalized);
    if (exists) {
      throw new Error(`EEXIST: file already exists, symlink '${linkPath}'`);
    }
    this.ensureParentDirs(normalized);
    this.memory.set(normalized, {
      type: "symlink",
      target,
      mode: 511,
      mtime: /* @__PURE__ */ new Date()
    });
    this.deleted.delete(normalized);
  }
  async link(existingPath, newPath) {
    this.assertWritable(`link '${newPath}'`);
    const existingNorm = this.normalizePath(existingPath);
    const newNorm = this.normalizePath(newPath);
    const existingExists = await this.existsInOverlay(existingNorm);
    if (!existingExists) {
      throw new Error(`ENOENT: no such file or directory, link '${existingPath}'`);
    }
    const existingStat = await this.stat(existingNorm);
    if (!existingStat.isFile) {
      throw new Error(`EPERM: operation not permitted, link '${existingPath}'`);
    }
    const newExists = await this.existsInOverlay(newNorm);
    if (newExists) {
      throw new Error(`EEXIST: file already exists, link '${newPath}'`);
    }
    const content = await this.readFileBuffer(existingNorm);
    this.ensureParentDirs(newNorm);
    this.memory.set(newNorm, {
      type: "file",
      content,
      mode: existingStat.mode,
      mtime: /* @__PURE__ */ new Date()
    });
    this.deleted.delete(newNorm);
  }
  async readlink(path2) {
    const normalized = this.normalizePath(path2);
    if (this.deleted.has(normalized)) {
      throw new Error(`ENOENT: no such file or directory, readlink '${path2}'`);
    }
    const entry = this.memory.get(normalized);
    if (entry) {
      if (entry.type !== "symlink") {
        throw new Error(`EINVAL: invalid argument, readlink '${path2}'`);
      }
      return entry.target;
    }
    const realPath = this.toRealPath(normalized);
    if (!realPath) {
      throw new Error(`ENOENT: no such file or directory, readlink '${path2}'`);
    }
    try {
      return await fs2.promises.readlink(realPath);
    } catch (e) {
      if (e.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, readlink '${path2}'`);
      }
      if (e.code === "EINVAL") {
        throw new Error(`EINVAL: invalid argument, readlink '${path2}'`);
      }
      throw e;
    }
  }
  /**
   * Resolve all symlinks in a path to get the canonical physical path.
   * This is equivalent to POSIX realpath().
   */
  async realpath(path2) {
    const normalized = this.normalizePath(path2);
    const seen = /* @__PURE__ */ new Set();
    const resolveAll = /* @__PURE__ */ __name$1(async (p) => {
      const parts = p === "/" ? [] : p.slice(1).split("/");
      let resolved = "";
      for (const part of parts) {
        resolved = `${resolved}/${part}`;
        if (seen.has(resolved)) {
          throw new Error(`ELOOP: too many levels of symbolic links, realpath '${path2}'`);
        }
        if (this.deleted.has(resolved)) {
          throw new Error(`ENOENT: no such file or directory, realpath '${path2}'`);
        }
        let entry = this.memory.get(resolved);
        let loopCount = 0;
        const maxLoops = 40;
        while (entry && entry.type === "symlink" && loopCount < maxLoops) {
          seen.add(resolved);
          resolved = this.resolveSymlink(resolved, entry.target);
          loopCount++;
          if (seen.has(resolved)) {
            throw new Error(`ELOOP: too many levels of symbolic links, realpath '${path2}'`);
          }
          if (this.deleted.has(resolved)) {
            throw new Error(`ENOENT: no such file or directory, realpath '${path2}'`);
          }
          entry = this.memory.get(resolved);
        }
        if (loopCount >= maxLoops) {
          throw new Error(`ELOOP: too many levels of symbolic links, realpath '${path2}'`);
        }
        if (!entry) {
          const realPath = this.toRealPath(resolved);
          if (realPath) {
            try {
              const stat = await fs2.promises.lstat(realPath);
              if (stat.isSymbolicLink()) {
                const target = await fs2.promises.readlink(realPath);
                seen.add(resolved);
                resolved = this.resolveSymlink(resolved, target);
                return resolveAll(resolved);
              }
            } catch (e) {
              if (e.code === "ENOENT") {
                throw new Error(`ENOENT: no such file or directory, realpath '${path2}'`);
              }
              throw e;
            }
          }
        }
      }
      return resolved || "/";
    }, "resolveAll");
    const result2 = await resolveAll(normalized);
    const exists = await this.existsInOverlay(result2);
    if (!exists) {
      throw new Error(`ENOENT: no such file or directory, realpath '${path2}'`);
    }
    return result2;
  }
  /**
   * Set access and modification times of a file
   * @param path - The file path
   * @param _atime - Access time (ignored, kept for API compatibility)
   * @param mtime - Modification time
   */
  async utimes(path2, _atime, mtime) {
    this.assertWritable(`utimes '${path2}'`);
    const normalized = this.normalizePath(path2);
    const exists = await this.existsInOverlay(normalized);
    if (!exists) {
      throw new Error(`ENOENT: no such file or directory, utimes '${path2}'`);
    }
    const entry = this.memory.get(normalized);
    if (entry) {
      entry.mtime = mtime;
      return;
    }
    const stat = await this.stat(normalized);
    if (stat.isFile) {
      const content = await this.readFileBuffer(normalized);
      this.memory.set(normalized, {
        type: "file",
        content,
        mode: stat.mode,
        mtime
      });
    } else if (stat.isDirectory) {
      this.memory.set(normalized, {
        type: "directory",
        mode: stat.mode,
        mtime
      });
    }
  }
}, __name$1(_B, "OverlayFs"), _B);
_C = class {
  root;
  constructor(options) {
    this.root = path.resolve(options.root);
    if (!fs2.existsSync(this.root)) {
      throw new Error(`ReadWriteFs root does not exist: ${this.root}`);
    }
    const stat = fs2.statSync(this.root);
    if (!stat.isDirectory()) {
      throw new Error(`ReadWriteFs root is not a directory: ${this.root}`);
    }
  }
  /**
   * Convert a virtual path to a real filesystem path.
   */
  toRealPath(virtualPath) {
    const normalized = this.normalizePath(virtualPath);
    const realPath = path.join(this.root, normalized);
    return path.resolve(realPath);
  }
  /**
   * Normalize a virtual path (resolve . and .., ensure starts with /)
   */
  normalizePath(path2) {
    if (!path2 || path2 === "/")
      return "/";
    let normalized = path2.endsWith("/") && path2 !== "/" ? path2.slice(0, -1) : path2;
    if (!normalized.startsWith("/")) {
      normalized = `/${normalized}`;
    }
    const parts = normalized.split("/").filter((p) => p && p !== ".");
    const resolved = [];
    for (const part of parts) {
      if (part === "..") {
        resolved.pop();
      } else {
        resolved.push(part);
      }
    }
    return `/${resolved.join("/")}` || "/";
  }
  async readFile(path2, options) {
    const buffer = await this.readFileBuffer(path2);
    const encoding = getEncoding$1(options);
    return fromBuffer$1(buffer, encoding);
  }
  async readFileBuffer(path2) {
    const realPath = this.toRealPath(path2);
    try {
      const content = await fs2.promises.readFile(realPath);
      return new Uint8Array(content);
    } catch (e) {
      const err = e;
      if (err.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, open '${path2}'`);
      }
      if (err.code === "EISDIR") {
        throw new Error(`EISDIR: illegal operation on a directory, read '${path2}'`);
      }
      throw e;
    }
  }
  async writeFile(path$1, content, options) {
    const realPath = this.toRealPath(path$1);
    const encoding = getEncoding$1(options);
    const buffer = toBuffer$1(content, encoding);
    const dir = path.dirname(realPath);
    await fs2.promises.mkdir(dir, { recursive: true });
    await fs2.promises.writeFile(realPath, buffer);
  }
  async appendFile(path$1, content, options) {
    const realPath = this.toRealPath(path$1);
    const encoding = getEncoding$1(options);
    const buffer = toBuffer$1(content, encoding);
    const dir = path.dirname(realPath);
    await fs2.promises.mkdir(dir, { recursive: true });
    await fs2.promises.appendFile(realPath, buffer);
  }
  async exists(path2) {
    const realPath = this.toRealPath(path2);
    try {
      await fs2.promises.access(realPath);
      return true;
    } catch {
      return false;
    }
  }
  async stat(path2) {
    const realPath = this.toRealPath(path2);
    try {
      const stat = await fs2.promises.stat(realPath);
      return {
        isFile: stat.isFile(),
        isDirectory: stat.isDirectory(),
        isSymbolicLink: false,
        // stat follows symlinks
        mode: stat.mode,
        size: stat.size,
        mtime: stat.mtime
      };
    } catch (e) {
      const err = e;
      if (err.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, stat '${path2}'`);
      }
      throw e;
    }
  }
  async lstat(path2) {
    const realPath = this.toRealPath(path2);
    try {
      const stat = await fs2.promises.lstat(realPath);
      return {
        isFile: stat.isFile(),
        isDirectory: stat.isDirectory(),
        isSymbolicLink: stat.isSymbolicLink(),
        mode: stat.mode,
        size: stat.size,
        mtime: stat.mtime
      };
    } catch (e) {
      const err = e;
      if (err.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, lstat '${path2}'`);
      }
      throw e;
    }
  }
  async mkdir(path2, options) {
    const realPath = this.toRealPath(path2);
    try {
      await fs2.promises.mkdir(realPath, { recursive: options?.recursive });
    } catch (e) {
      const err = e;
      if (err.code === "EEXIST") {
        throw new Error(`EEXIST: file already exists, mkdir '${path2}'`);
      }
      if (err.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, mkdir '${path2}'`);
      }
      throw e;
    }
  }
  async readdir(path2) {
    const entries = await this.readdirWithFileTypes(path2);
    return entries.map((e) => e.name);
  }
  async readdirWithFileTypes(path2) {
    const realPath = this.toRealPath(path2);
    try {
      const entries = await fs2.promises.readdir(realPath, {
        withFileTypes: true
      });
      return entries.map((dirent) => ({
        name: dirent.name,
        isFile: dirent.isFile(),
        isDirectory: dirent.isDirectory(),
        isSymbolicLink: dirent.isSymbolicLink()
      })).sort((a, b) => a.name < b.name ? -1 : a.name > b.name ? 1 : 0);
    } catch (e) {
      const err = e;
      if (err.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, scandir '${path2}'`);
      }
      if (err.code === "ENOTDIR") {
        throw new Error(`ENOTDIR: not a directory, scandir '${path2}'`);
      }
      throw e;
    }
  }
  async rm(path2, options) {
    const realPath = this.toRealPath(path2);
    try {
      await fs2.promises.rm(realPath, {
        recursive: options?.recursive ?? false,
        force: options?.force ?? false
      });
    } catch (e) {
      const err = e;
      if (err.code === "ENOENT" && !options?.force) {
        throw new Error(`ENOENT: no such file or directory, rm '${path2}'`);
      }
      if (err.code === "ENOTEMPTY") {
        throw new Error(`ENOTEMPTY: directory not empty, rm '${path2}'`);
      }
      throw e;
    }
  }
  async cp(src, dest, options) {
    const srcReal = this.toRealPath(src);
    const destReal = this.toRealPath(dest);
    try {
      await fs2.promises.cp(srcReal, destReal, {
        recursive: options?.recursive ?? false
      });
    } catch (e) {
      const err = e;
      if (err.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, cp '${src}'`);
      }
      if (err.code === "EISDIR") {
        throw new Error(`EISDIR: is a directory, cp '${src}'`);
      }
      throw e;
    }
  }
  async mv(src, dest) {
    const srcReal = this.toRealPath(src);
    const destReal = this.toRealPath(dest);
    const destDir = path.dirname(destReal);
    await fs2.promises.mkdir(destDir, { recursive: true });
    try {
      await fs2.promises.rename(srcReal, destReal);
    } catch (e) {
      const err = e;
      if (err.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, mv '${src}'`);
      }
      if (err.code === "EXDEV") {
        await this.cp(src, dest, { recursive: true });
        await this.rm(src, { recursive: true });
        return;
      }
      throw e;
    }
  }
  resolvePath(base, path2) {
    if (path2.startsWith("/")) {
      return this.normalizePath(path2);
    }
    const combined = base === "/" ? `/${path2}` : `${base}/${path2}`;
    return this.normalizePath(combined);
  }
  getAllPaths() {
    const paths = [];
    this.scanDir("/", paths);
    return paths;
  }
  scanDir(virtualDir, paths) {
    const realPath = this.toRealPath(virtualDir);
    try {
      const entries = fs2.readdirSync(realPath);
      for (const entry of entries) {
        const virtualPath = virtualDir === "/" ? `/${entry}` : `${virtualDir}/${entry}`;
        paths.push(virtualPath);
        const entryRealPath = path.join(realPath, entry);
        const stat = fs2.statSync(entryRealPath);
        if (stat.isDirectory()) {
          this.scanDir(virtualPath, paths);
        }
      }
    } catch {
    }
  }
  async chmod(path2, mode) {
    const realPath = this.toRealPath(path2);
    try {
      await fs2.promises.chmod(realPath, mode);
    } catch (e) {
      const err = e;
      if (err.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, chmod '${path2}'`);
      }
      throw e;
    }
  }
  async symlink(target, linkPath) {
    const realLinkPath = this.toRealPath(linkPath);
    try {
      await fs2.promises.symlink(target, realLinkPath);
    } catch (e) {
      const err = e;
      if (err.code === "EEXIST") {
        throw new Error(`EEXIST: file already exists, symlink '${linkPath}'`);
      }
      throw e;
    }
  }
  async link(existingPath, newPath) {
    const realExisting = this.toRealPath(existingPath);
    const realNew = this.toRealPath(newPath);
    try {
      await fs2.promises.link(realExisting, realNew);
    } catch (e) {
      const err = e;
      if (err.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, link '${existingPath}'`);
      }
      if (err.code === "EEXIST") {
        throw new Error(`EEXIST: file already exists, link '${newPath}'`);
      }
      if (err.code === "EPERM") {
        throw new Error(`EPERM: operation not permitted, link '${existingPath}'`);
      }
      throw e;
    }
  }
  async readlink(path2) {
    const realPath = this.toRealPath(path2);
    try {
      return await fs2.promises.readlink(realPath);
    } catch (e) {
      const err = e;
      if (err.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, readlink '${path2}'`);
      }
      if (err.code === "EINVAL") {
        throw new Error(`EINVAL: invalid argument, readlink '${path2}'`);
      }
      throw e;
    }
  }
  /**
   * Resolve all symlinks in a path to get the canonical physical path.
   * This is equivalent to POSIX realpath().
   */
  async realpath(path2) {
    const realPath = this.toRealPath(path2);
    try {
      const resolved = await fs2.promises.realpath(realPath);
      if (resolved.startsWith(this.root)) {
        const relative = resolved.slice(this.root.length);
        return relative || "/";
      }
      return resolved;
    } catch (e) {
      const err = e;
      if (err.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, realpath '${path2}'`);
      }
      if (err.code === "ELOOP") {
        throw new Error(`ELOOP: too many levels of symbolic links, realpath '${path2}'`);
      }
      throw e;
    }
  }
  /**
   * Set access and modification times of a file
   * @param path - The file path
   * @param atime - Access time
   * @param mtime - Modification time
   */
  async utimes(path2, atime, mtime) {
    const realPath = this.toRealPath(path2);
    try {
      await fs2.promises.utimes(realPath, atime, mtime);
    } catch (e) {
      const err = e;
      if (err.code === "ENOENT") {
        throw new Error(`ENOENT: no such file or directory, utimes '${path2}'`);
      }
      throw e;
    }
  }
}, __name$1(_C, "ReadWriteFs"), _C;
var Command = (_D = class {
  cmdId;
  cwd;
  startedAt;
  exitCode;
  bashEnv;
  cmdLine;
  env;
  explicitCwd;
  resultPromise;
  constructor(bashEnv, cmdLine, cwd, env, explicitCwd = false) {
    this.cmdId = crypto.randomUUID();
    this.cwd = cwd;
    this.startedAt = /* @__PURE__ */ new Date();
    this.bashEnv = bashEnv;
    this.cmdLine = cmdLine;
    this.env = env;
    this.explicitCwd = explicitCwd;
    this.resultPromise = this.execute();
  }
  async execute() {
    const options = this.env || this.explicitCwd ? { cwd: this.explicitCwd ? this.cwd : void 0, env: this.env } : void 0;
    const result2 = await this.bashEnv.exec(this.cmdLine, options);
    this.exitCode = result2.exitCode;
    return result2;
  }
  async *logs() {
    const result2 = await this.resultPromise;
    if (result2.stdout) {
      yield { type: "stdout", data: result2.stdout, timestamp: /* @__PURE__ */ new Date() };
    }
    if (result2.stderr) {
      yield { type: "stderr", data: result2.stderr, timestamp: /* @__PURE__ */ new Date() };
    }
  }
  async wait() {
    await this.resultPromise;
    return this;
  }
  async output() {
    const result2 = await this.resultPromise;
    return result2.stdout + result2.stderr;
  }
  async stdout() {
    const result2 = await this.resultPromise;
    return result2.stdout;
  }
  async stderr() {
    const result2 = await this.resultPromise;
    return result2.stderr;
  }
  async kill() {
  }
}, __name$1(_D, "Command"), _D);
_E = class {
  bashEnv;
  constructor(bashEnv) {
    this.bashEnv = bashEnv;
  }
  static async create(opts) {
    let fs3 = opts?.fs;
    if (opts?.overlayRoot) {
      if (opts?.fs) {
        throw new Error("Cannot specify both 'fs' and 'overlayRoot' options");
      }
      fs3 = new OverlayFs({ root: opts.overlayRoot });
    }
    const bashEnv = new Bash({
      env: opts?.env,
      cwd: opts?.cwd,
      // Bash-specific extensions
      fs: fs3,
      maxCallDepth: opts?.maxCallDepth,
      maxCommandCount: opts?.maxCommandCount,
      maxLoopIterations: opts?.maxLoopIterations,
      network: opts?.network
    });
    return new _E(bashEnv);
  }
  async runCommand(cmd, opts) {
    const cwd = opts?.cwd ?? this.bashEnv.getCwd();
    const explicitCwd = opts?.cwd !== void 0;
    return new Command(this.bashEnv, cmd, cwd, opts?.env, explicitCwd);
  }
  async writeFiles(files) {
    for (const [path2, content] of Object.entries(files)) {
      let data;
      if (typeof content === "string") {
        data = content;
      } else {
        if (content.encoding === "base64") {
          data = Buffer.from(content.content, "base64").toString("utf-8");
        } else {
          data = content.content;
        }
      }
      const parentDir = path2.substring(0, path2.lastIndexOf("/")) || "/";
      if (parentDir !== "/") {
        await this.bashEnv.exec(`mkdir -p ${parentDir}`);
      }
      await this.bashEnv.writeFile(path2, data);
    }
  }
  async readFile(path2, encoding) {
    const content = await this.bashEnv.readFile(path2);
    if (encoding === "base64") {
      return Buffer.from(content).toString("base64");
    }
    return content;
  }
  async mkDir(path2, opts) {
    const flags = opts?.recursive ? "-p" : "";
    await this.bashEnv.exec(`mkdir ${flags} ${path2}`);
  }
  async stop() {
  }
  async extendTimeout(_ms) {
  }
  get domain() {
    return void 0;
  }
  /**
   * Bash-specific: Get the underlying Bash instance for advanced operations.
   * Not available in Vercel Sandbox API.
   */
  get bashEnvInstance() {
    return this.bashEnv;
  }
}, __name$1(_E, "Sandbox"), _E;
var __defProp2 = Object.defineProperty;
var __name = (target, value) => __defProp2(target, "name", { value, configurable: true });
var registeredSteps = /* @__PURE__ */ new Map();
function registerStepFunction(stepId, stepFn) {
  registeredSteps.set(stepId, stepFn);
}
__name(registerStepFunction, "registerStepFunction");
async function __builtin_response_array_buffer(res) {
  return res.arrayBuffer();
}
__name(__builtin_response_array_buffer, "__builtin_response_array_buffer");
async function __builtin_response_json(res) {
  return res.json();
}
__name(__builtin_response_json, "__builtin_response_json");
async function __builtin_response_text(res) {
  return res.text();
}
__name(__builtin_response_text, "__builtin_response_text");
registerStepFunction("__builtin_response_array_buffer", __builtin_response_array_buffer);
registerStepFunction("__builtin_response_json", __builtin_response_json);
registerStepFunction("__builtin_response_text", __builtin_response_text);
async function serialBashWorkflow() {
  throw new Error("You attempted to execute workflow serialBashWorkflow function directly. To start a workflow, use start(serialBashWorkflow) from workflow/api");
}
__name(serialBashWorkflow, "serialBashWorkflow");
serialBashWorkflow.workflowId = "workflow//workflows/bash-workflow.ts//serialBashWorkflow";
async function createBash() {
  console.log(Bash);
  console.log(Bash.classId);
  const bash = new Bash();
  await bash.exec("mkdir -p /data");
  await bash.exec('echo "created" > /data/log.txt');
  console.log("Created Bash instance with /data/log.txt");
  return bash;
}
__name(createBash, "createBash");
async function appendToLog(bash, label) {
  await bash.exec(`echo "${label}: modified" >> /data/log.txt`);
  console.log(`Appended ${label} to log`);
  return bash;
}
__name(appendToLog, "appendToLog");
async function getResults(bash) {
  const result2 = await bash.exec("cat /data/log.txt");
  console.log("Read final results");
  return {
    log: result2.stdout
  };
}
__name(getResults, "getResults");
registerStepFunction("step//workflows/bash-workflow.ts//createBash", createBash);
registerStepFunction("step//workflows/bash-workflow.ts//appendToLog", appendToLog);
registerStepFunction("step//workflows/bash-workflow.ts//getResults", getResults);
function getRegistry(global = globalThis) {
  const g = global;
  let registry = g[WORKFLOW_CLASS_REGISTRY];
  if (!registry) {
    registry = /* @__PURE__ */ new Map();
    g[WORKFLOW_CLASS_REGISTRY] = registry;
  }
  return registry;
}
__name(getRegistry, "getRegistry");
function registerSerializationClass(classId, cls) {
  getRegistry().set(classId, cls);
  Object.defineProperty(cls, "classId", {
    value: classId,
    writable: false,
    enumerable: false,
    configurable: false
  });
}
__name(registerSerializationClass, "registerSerializationClass");
var __defProp22 = Object.defineProperty;
var __name2 = /* @__PURE__ */ __name((target, value) => __defProp22(target, "name", {
  value,
  configurable: true
}), "__name");
var WORKFLOW_SERIALIZE = /* @__PURE__ */ Symbol.for("workflow-serialize");
var WORKFLOW_DESERIALIZE = /* @__PURE__ */ Symbol.for("workflow-deserialize");
var textEncoder = new TextEncoder();
var textDecoder = new TextDecoder();
function toBuffer(content, encoding) {
  if (content instanceof Uint8Array) {
    return content;
  }
  if (encoding === "base64") {
    return Uint8Array.from(atob(content), (c) => c.charCodeAt(0));
  }
  if (encoding === "hex") {
    const bytes = new Uint8Array(content.length / 2);
    for (let i = 0; i < content.length; i += 2) {
      bytes[i / 2] = parseInt(content.slice(i, i + 2), 16);
    }
    return bytes;
  }
  if (encoding === "binary" || encoding === "latin1") {
    return Uint8Array.from(content, (c) => c.charCodeAt(0));
  }
  return textEncoder.encode(content);
}
__name(toBuffer, "toBuffer");
__name2(toBuffer, "toBuffer");
function fromBuffer(buffer, encoding) {
  if (encoding === "base64") {
    return btoa(String.fromCharCode(...buffer));
  }
  if (encoding === "hex") {
    return Array.from(buffer).map((b) => b.toString(16).padStart(2, "0")).join("");
  }
  if (encoding === "binary" || encoding === "latin1") {
    return String.fromCharCode(...buffer);
  }
  return textDecoder.decode(buffer);
}
__name(fromBuffer, "fromBuffer");
__name2(fromBuffer, "fromBuffer");
function getEncoding(options) {
  if (options === null || options === void 0) {
    return void 0;
  }
  if (typeof options === "string") {
    return options;
  }
  return options.encoding ?? void 0;
}
__name(getEncoding, "getEncoding");
__name2(getEncoding, "getEncoding");
var textEncoder2 = new TextEncoder();
function isFileInit(value) {
  return typeof value === "object" && value !== null && !(value instanceof Uint8Array) && "content" in value;
}
__name(isFileInit, "isFileInit");
__name2(isFileInit, "isFileInit");
var InMemoryFs = (_F = class {
  data = /* @__PURE__ */ new Map();
  constructor(initialFiles) {
    this.data.set("/", {
      type: "directory",
      mode: 493,
      mtime: /* @__PURE__ */ new Date()
    });
    if (initialFiles) {
      for (const [path2, value] of Object.entries(initialFiles)) {
        if (isFileInit(value)) {
          this.writeFileSync(path2, value.content, void 0, {
            mode: value.mode,
            mtime: value.mtime
          });
        } else {
          this.writeFileSync(path2, value);
        }
      }
    }
  }
  normalizePath(path2) {
    if (!path2 || path2 === "/") return "/";
    let normalized = path2.endsWith("/") && path2 !== "/" ? path2.slice(0, -1) : path2;
    if (!normalized.startsWith("/")) {
      normalized = `/${normalized}`;
    }
    const parts = normalized.split("/").filter((p) => p && p !== ".");
    const resolved = [];
    for (const part of parts) {
      if (part === "..") {
        resolved.pop();
      } else {
        resolved.push(part);
      }
    }
    return `/${resolved.join("/")}` || "/";
  }
  dirname(path2) {
    const normalized = this.normalizePath(path2);
    if (normalized === "/") return "/";
    const lastSlash = normalized.lastIndexOf("/");
    return lastSlash === 0 ? "/" : normalized.slice(0, lastSlash);
  }
  ensureParentDirs(path2) {
    const dir = this.dirname(path2);
    if (dir === "/") return;
    if (!this.data.has(dir)) {
      this.ensureParentDirs(dir);
      this.data.set(dir, {
        type: "directory",
        mode: 493,
        mtime: /* @__PURE__ */ new Date()
      });
    }
  }
  // Sync method for writing files
  writeFileSync(path2, content, options, metadata) {
    const normalized = this.normalizePath(path2);
    this.ensureParentDirs(normalized);
    const encoding = getEncoding(options);
    const buffer = toBuffer(content, encoding);
    this.data.set(normalized, {
      type: "file",
      content: buffer,
      mode: metadata?.mode ?? 420,
      mtime: metadata?.mtime ?? /* @__PURE__ */ new Date()
    });
  }
  // Async public API
  async readFile(path2, options) {
    const buffer = await this.readFileBuffer(path2);
    const encoding = getEncoding(options);
    return fromBuffer(buffer, encoding);
  }
  async readFileBuffer(path2) {
    const resolvedPath = this.resolvePathWithSymlinks(path2);
    const entry = this.data.get(resolvedPath);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, open '${path2}'`);
    }
    if (entry.type !== "file") {
      throw new Error(`EISDIR: illegal operation on a directory, read '${path2}'`);
    }
    if (entry.content instanceof Uint8Array) {
      return entry.content;
    }
    return textEncoder2.encode(entry.content);
  }
  async writeFile(path2, content, options) {
    this.writeFileSync(path2, content, options);
  }
  async appendFile(path2, content, options) {
    const normalized = this.normalizePath(path2);
    const existing = this.data.get(normalized);
    if (existing && existing.type === "directory") {
      throw new Error(`EISDIR: illegal operation on a directory, write '${path2}'`);
    }
    const encoding = getEncoding(options);
    const newBuffer = toBuffer(content, encoding);
    if (existing?.type === "file") {
      const existingBuffer = existing.content instanceof Uint8Array ? existing.content : textEncoder2.encode(existing.content);
      const combined = new Uint8Array(existingBuffer.length + newBuffer.length);
      combined.set(existingBuffer);
      combined.set(newBuffer, existingBuffer.length);
      this.data.set(normalized, {
        type: "file",
        content: combined,
        mode: existing.mode,
        mtime: /* @__PURE__ */ new Date()
      });
    } else {
      this.writeFileSync(path2, content, options);
    }
  }
  async exists(path2) {
    try {
      const resolvedPath = this.resolvePathWithSymlinks(path2);
      return this.data.has(resolvedPath);
    } catch {
      return false;
    }
  }
  async stat(path2) {
    const resolvedPath = this.resolvePathWithSymlinks(path2);
    const entry = this.data.get(resolvedPath);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, stat '${path2}'`);
    }
    let size = 0;
    if (entry.type === "file" && entry.content) {
      if (entry.content instanceof Uint8Array) {
        size = entry.content.length;
      } else {
        size = textEncoder2.encode(entry.content).length;
      }
    }
    return {
      isFile: entry.type === "file",
      isDirectory: entry.type === "directory",
      isSymbolicLink: false,
      // stat follows symlinks, so this is always false
      mode: entry.mode,
      size,
      mtime: entry.mtime || /* @__PURE__ */ new Date()
    };
  }
  async lstat(path2) {
    const resolvedPath = this.resolveIntermediateSymlinks(path2);
    const entry = this.data.get(resolvedPath);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, lstat '${path2}'`);
    }
    if (entry.type === "symlink") {
      return {
        isFile: false,
        isDirectory: false,
        isSymbolicLink: true,
        mode: entry.mode,
        size: entry.target.length,
        mtime: entry.mtime || /* @__PURE__ */ new Date()
      };
    }
    let size = 0;
    if (entry.type === "file" && entry.content) {
      if (entry.content instanceof Uint8Array) {
        size = entry.content.length;
      } else {
        size = textEncoder2.encode(entry.content).length;
      }
    }
    return {
      isFile: entry.type === "file",
      isDirectory: entry.type === "directory",
      isSymbolicLink: false,
      mode: entry.mode,
      size,
      mtime: entry.mtime || /* @__PURE__ */ new Date()
    };
  }
  // Helper to resolve symlink target paths
  resolveSymlink(symlinkPath, target) {
    if (target.startsWith("/")) {
      return this.normalizePath(target);
    }
    const dir = this.dirname(symlinkPath);
    return this.normalizePath(dir === "/" ? `/${target}` : `${dir}/${target}`);
  }
  /**
  * Resolve symlinks in intermediate path components only (not the final component).
  * Used by lstat which should not follow the final symlink.
  */
  resolveIntermediateSymlinks(path2) {
    const normalized = this.normalizePath(path2);
    if (normalized === "/") return "/";
    const parts = normalized.slice(1).split("/");
    if (parts.length <= 1) return normalized;
    let resolvedPath = "";
    const seen = /* @__PURE__ */ new Set();
    for (let i = 0; i < parts.length - 1; i++) {
      const part = parts[i];
      resolvedPath = `${resolvedPath}/${part}`;
      let entry = this.data.get(resolvedPath);
      let loopCount = 0;
      const maxLoops = 40;
      while (entry && entry.type === "symlink" && loopCount < maxLoops) {
        if (seen.has(resolvedPath)) {
          throw new Error(`ELOOP: too many levels of symbolic links, lstat '${path2}'`);
        }
        seen.add(resolvedPath);
        resolvedPath = this.resolveSymlink(resolvedPath, entry.target);
        entry = this.data.get(resolvedPath);
        loopCount++;
      }
      if (loopCount >= maxLoops) {
        throw new Error(`ELOOP: too many levels of symbolic links, lstat '${path2}'`);
      }
    }
    return `${resolvedPath}/${parts[parts.length - 1]}`;
  }
  /**
  * Resolve all symlinks in a path, including intermediate components.
  * For example: /home/user/linkdir/file.txt where linkdir is a symlink to "subdir"
  * would resolve to /home/user/subdir/file.txt
  */
  resolvePathWithSymlinks(path2) {
    const normalized = this.normalizePath(path2);
    if (normalized === "/") return "/";
    const parts = normalized.slice(1).split("/");
    let resolvedPath = "";
    const seen = /* @__PURE__ */ new Set();
    for (const part of parts) {
      resolvedPath = `${resolvedPath}/${part}`;
      let entry = this.data.get(resolvedPath);
      let loopCount = 0;
      const maxLoops = 40;
      while (entry && entry.type === "symlink" && loopCount < maxLoops) {
        if (seen.has(resolvedPath)) {
          throw new Error(`ELOOP: too many levels of symbolic links, open '${path2}'`);
        }
        seen.add(resolvedPath);
        resolvedPath = this.resolveSymlink(resolvedPath, entry.target);
        entry = this.data.get(resolvedPath);
        loopCount++;
      }
      if (loopCount >= maxLoops) {
        throw new Error(`ELOOP: too many levels of symbolic links, open '${path2}'`);
      }
    }
    return resolvedPath;
  }
  async mkdir(path2, options) {
    this.mkdirSync(path2, options);
  }
  /**
  * Synchronous version of mkdir
  */
  mkdirSync(path2, options) {
    const normalized = this.normalizePath(path2);
    if (this.data.has(normalized)) {
      const entry = this.data.get(normalized);
      if (entry?.type === "file") {
        throw new Error(`EEXIST: file already exists, mkdir '${path2}'`);
      }
      if (!options?.recursive) {
        throw new Error(`EEXIST: directory already exists, mkdir '${path2}'`);
      }
      return;
    }
    const parent = this.dirname(normalized);
    if (parent !== "/" && !this.data.has(parent)) {
      if (options?.recursive) {
        this.mkdirSync(parent, {
          recursive: true
        });
      } else {
        throw new Error(`ENOENT: no such file or directory, mkdir '${path2}'`);
      }
    }
    this.data.set(normalized, {
      type: "directory",
      mode: 493,
      mtime: /* @__PURE__ */ new Date()
    });
  }
  async readdir(path2) {
    const entries = await this.readdirWithFileTypes(path2);
    return entries.map((e) => e.name);
  }
  async readdirWithFileTypes(path2) {
    let normalized = this.normalizePath(path2);
    let entry = this.data.get(normalized);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, scandir '${path2}'`);
    }
    const seen = /* @__PURE__ */ new Set();
    while (entry && entry.type === "symlink") {
      if (seen.has(normalized)) {
        throw new Error(`ELOOP: too many levels of symbolic links, scandir '${path2}'`);
      }
      seen.add(normalized);
      normalized = this.resolveSymlink(normalized, entry.target);
      entry = this.data.get(normalized);
    }
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, scandir '${path2}'`);
    }
    if (entry.type !== "directory") {
      throw new Error(`ENOTDIR: not a directory, scandir '${path2}'`);
    }
    const prefix = normalized === "/" ? "/" : `${normalized}/`;
    const entriesMap = /* @__PURE__ */ new Map();
    for (const [p, fsEntry] of this.data.entries()) {
      if (p === normalized) continue;
      if (p.startsWith(prefix)) {
        const rest = p.slice(prefix.length);
        const name = rest.split("/")[0];
        if (name && !rest.includes("/", name.length) && !entriesMap.has(name)) {
          entriesMap.set(name, {
            name,
            isFile: fsEntry.type === "file",
            isDirectory: fsEntry.type === "directory",
            isSymbolicLink: fsEntry.type === "symlink"
          });
        }
      }
    }
    return Array.from(entriesMap.values()).sort((a, b) => a.name < b.name ? -1 : a.name > b.name ? 1 : 0);
  }
  async rm(path2, options) {
    const normalized = this.normalizePath(path2);
    const entry = this.data.get(normalized);
    if (!entry) {
      if (options?.force) return;
      throw new Error(`ENOENT: no such file or directory, rm '${path2}'`);
    }
    if (entry.type === "directory") {
      const children = await this.readdir(normalized);
      if (children.length > 0) {
        if (!options?.recursive) {
          throw new Error(`ENOTEMPTY: directory not empty, rm '${path2}'`);
        }
        for (const child of children) {
          const childPath = normalized === "/" ? `/${child}` : `${normalized}/${child}`;
          await this.rm(childPath, options);
        }
      }
    }
    this.data.delete(normalized);
  }
  async cp(src, dest, options) {
    const srcNorm = this.normalizePath(src);
    const destNorm = this.normalizePath(dest);
    const srcEntry = this.data.get(srcNorm);
    if (!srcEntry) {
      throw new Error(`ENOENT: no such file or directory, cp '${src}'`);
    }
    if (srcEntry.type === "file") {
      this.ensureParentDirs(destNorm);
      this.data.set(destNorm, {
        ...srcEntry
      });
    } else if (srcEntry.type === "directory") {
      if (!options?.recursive) {
        throw new Error(`EISDIR: is a directory, cp '${src}'`);
      }
      await this.mkdir(destNorm, {
        recursive: true
      });
      const children = await this.readdir(srcNorm);
      for (const child of children) {
        const srcChild = srcNorm === "/" ? `/${child}` : `${srcNorm}/${child}`;
        const destChild = destNorm === "/" ? `/${child}` : `${destNorm}/${child}`;
        await this.cp(srcChild, destChild, options);
      }
    }
  }
  async mv(src, dest) {
    await this.cp(src, dest, {
      recursive: true
    });
    await this.rm(src, {
      recursive: true
    });
  }
  // Get all paths (useful for debugging/glob)
  getAllPaths() {
    return Array.from(this.data.keys());
  }
  // Resolve a path relative to a base
  resolvePath(base, path2) {
    if (path2.startsWith("/")) {
      return this.normalizePath(path2);
    }
    const combined = base === "/" ? `/${path2}` : `${base}/${path2}`;
    return this.normalizePath(combined);
  }
  // Change file/directory permissions
  async chmod(path2, mode) {
    const normalized = this.normalizePath(path2);
    const entry = this.data.get(normalized);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, chmod '${path2}'`);
    }
    entry.mode = mode;
  }
  // Create a symbolic link
  async symlink(target, linkPath) {
    const normalized = this.normalizePath(linkPath);
    if (this.data.has(normalized)) {
      throw new Error(`EEXIST: file already exists, symlink '${linkPath}'`);
    }
    this.ensureParentDirs(normalized);
    this.data.set(normalized, {
      type: "symlink",
      target,
      mode: 511,
      mtime: /* @__PURE__ */ new Date()
    });
  }
  // Create a hard link
  async link(existingPath, newPath) {
    const existingNorm = this.normalizePath(existingPath);
    const newNorm = this.normalizePath(newPath);
    const entry = this.data.get(existingNorm);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, link '${existingPath}'`);
    }
    if (entry.type !== "file") {
      throw new Error(`EPERM: operation not permitted, link '${existingPath}'`);
    }
    if (this.data.has(newNorm)) {
      throw new Error(`EEXIST: file already exists, link '${newPath}'`);
    }
    this.ensureParentDirs(newNorm);
    this.data.set(newNorm, {
      type: "file",
      content: entry.content,
      mode: entry.mode,
      mtime: entry.mtime
    });
  }
  // Read the target of a symbolic link
  async readlink(path2) {
    const normalized = this.normalizePath(path2);
    const entry = this.data.get(normalized);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, readlink '${path2}'`);
    }
    if (entry.type !== "symlink") {
      throw new Error(`EINVAL: invalid argument, readlink '${path2}'`);
    }
    return entry.target;
  }
  /**
  * Resolve all symlinks in a path to get the canonical physical path.
  * This is equivalent to POSIX realpath().
  */
  async realpath(path2) {
    const resolved = this.resolvePathWithSymlinks(path2);
    if (!this.data.has(resolved)) {
      throw new Error(`ENOENT: no such file or directory, realpath '${path2}'`);
    }
    return resolved;
  }
  // ===========================================================================
  // Workflow Serde Support
  // ===========================================================================
  static [WORKFLOW_SERIALIZE](instance) {
    return {
      data: instance.data
    };
  }
  static [WORKFLOW_DESERIALIZE](serialized) {
    const fs = new _F();
    fs.data = serialized.data;
    return fs;
  }
  /**
  * Set access and modification times of a file
  * @param path - The file path
  * @param _atime - Access time (ignored, kept for API compatibility)
  * @param mtime - Modification time
  */
  async utimes(path2, _atime, mtime) {
    const normalized = this.normalizePath(path2);
    const resolved = this.resolvePathWithSymlinks(normalized);
    const entry = this.data.get(resolved);
    if (!entry) {
      throw new Error(`ENOENT: no such file or directory, utimes '${path2}'`);
    }
    entry.mtime = mtime;
  }
}, __name(_F, "_InMemoryFs"), __name2(_F, "InMemoryFs"), _F);
var DEFAULT_LIMITS = {
  maxCallDepth: 100,
  maxCommandCount: 1e4,
  maxLoopIterations: 1e4,
  maxAwkIterations: 1e4,
  maxSedIterations: 1e4,
  maxJqIterations: 1e4,
  maxSqliteTimeoutMs: 5e3,
  maxPythonTimeoutMs: 3e4
};
function resolveLimits(userLimits) {
  if (!userLimits) {
    return {
      ...DEFAULT_LIMITS
    };
  }
  return {
    maxCallDepth: userLimits.maxCallDepth ?? DEFAULT_LIMITS.maxCallDepth,
    maxCommandCount: userLimits.maxCommandCount ?? DEFAULT_LIMITS.maxCommandCount,
    maxLoopIterations: userLimits.maxLoopIterations ?? DEFAULT_LIMITS.maxLoopIterations,
    maxAwkIterations: userLimits.maxAwkIterations ?? DEFAULT_LIMITS.maxAwkIterations,
    maxSedIterations: userLimits.maxSedIterations ?? DEFAULT_LIMITS.maxSedIterations,
    maxJqIterations: userLimits.maxJqIterations ?? DEFAULT_LIMITS.maxJqIterations,
    maxSqliteTimeoutMs: userLimits.maxSqliteTimeoutMs ?? DEFAULT_LIMITS.maxSqliteTimeoutMs,
    maxPythonTimeoutMs: userLimits.maxPythonTimeoutMs ?? DEFAULT_LIMITS.maxPythonTimeoutMs
  };
}
__name(resolveLimits, "resolveLimits");
__name2(resolveLimits, "resolveLimits");
var Bash2 = (_G = class {
  fs;
  state;
  limits;
  constructor(options = {}) {
    this.fs = options.fs ?? new InMemoryFs();
    this.limits = resolveLimits(options.executionLimits);
    this.state = {
      env: {
        HOME: "/home/user",
        PATH: "/usr/bin:/bin",
        IFS: " 	\n",
        PWD: "/home/user",
        OLDPWD: "/home/user",
        OPTIND: "1"
      },
      cwd: "/home/user",
      previousDir: "/home/user",
      lastExitCode: 0,
      lastArg: "",
      currentLine: 1,
      options: {
        errexit: false,
        pipefail: false,
        nounset: false,
        xtrace: false,
        verbose: false,
        posix: false,
        allexport: false,
        noclobber: false,
        noglob: false,
        noexec: false,
        vi: false,
        emacs: false
      },
      shoptOptions: {
        extglob: false,
        dotglob: false,
        nullglob: false,
        failglob: false,
        globstar: false,
        globskipdots: true,
        nocaseglob: false,
        nocasematch: false,
        expand_aliases: false,
        lastpipe: false,
        xpg_echo: false
      },
      functions: /* @__PURE__ */ new Map(),
      localScopes: [],
      callDepth: 0,
      sourceDepth: 0,
      commandCount: 0,
      startTime: Date.now(),
      lastBackgroundPid: 0,
      bashPid: typeof process !== "undefined" ? process.pid : 1,
      nextVirtualPid: (typeof process !== "undefined" ? process.pid : 1) + 1,
      exportedVars: /* @__PURE__ */ new Set([
        "HOME",
        "PATH",
        "PWD",
        "OLDPWD"
      ]),
      readonlyVars: /* @__PURE__ */ new Set([
        "SHELLOPTS",
        "BASHOPTS"
      ]),
      hashTable: /* @__PURE__ */ new Map(),
      inCondition: false,
      loopDepth: 0
    };
  }
  // ===========================================================================
  // Workflow Serde Support
  // ===========================================================================
  /**
  * Serialize Bash instance for Workflow DevKit.
  * Serializes filesystem and interpreter state.
  */
  static [WORKFLOW_SERIALIZE](instance) {
    return {
      fs: instance.fs,
      state: instance.state,
      limits: instance.limits
    };
  }
  /**
  * Deserialize Bash instance for Workflow DevKit.
  */
  static [WORKFLOW_DESERIALIZE](serialized) {
    const bash = new _G({
      fs: serialized.fs
    });
    bash.state = serialized.state;
    bash.limits = serialized.limits;
    return bash;
  }
  // ===========================================================================
  // Read-only accessors (for workflow context inspection)
  // ===========================================================================
  getCwd() {
    return this.state.cwd;
  }
  getEnv() {
    return {
      ...this.state.env
    };
  }
}, __name(_G, "_Bash"), __name2(_G, "Bash"), _G);
registerSerializationClass("class//just-bash//Bash", Bash2);
registerSerializationClass("class//just-bash//InMemoryFs", InMemoryFs);
const _g3m8Xw = async ({ req }) => {
  try {
    return await stepEntrypoint(req);
  } catch (error) {
    console.error("Handler error:", error);
    return new Response("Internal Server Error", { status: 500 });
  }
};
const workflowCode = `globalThis.__private_workflows = new Map();
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });

// workflows/bash-workflow.ts
async function serialBashWorkflow() {
  let bash = await createBash();
  console.log(bash);
  bash = await appendToLog(bash, "step2");
  bash = await appendToLog(bash, "step3");
  bash = await appendToLog(bash, "step4");
  return await getResults(bash);
}
__name(serialBashWorkflow, "serialBashWorkflow");
serialBashWorkflow.workflowId = "workflow//workflows/bash-workflow.ts//serialBashWorkflow";
globalThis.__private_workflows.set("workflow//workflows/bash-workflow.ts//serialBashWorkflow", serialBashWorkflow);
var createBash = globalThis[Symbol.for("WORKFLOW_USE_STEP")]("step//workflows/bash-workflow.ts//createBash");
var appendToLog = globalThis[Symbol.for("WORKFLOW_USE_STEP")]("step//workflows/bash-workflow.ts//appendToLog");
var getResults = globalThis[Symbol.for("WORKFLOW_USE_STEP")]("step//workflows/bash-workflow.ts//getResults");

// ../../../../packages/core/dist/symbols.js
var WORKFLOW_USE_STEP = Symbol.for("WORKFLOW_USE_STEP");
var WORKFLOW_CREATE_HOOK = Symbol.for("WORKFLOW_CREATE_HOOK");
var WORKFLOW_SLEEP = Symbol.for("WORKFLOW_SLEEP");
var WORKFLOW_CONTEXT = Symbol.for("WORKFLOW_CONTEXT");
var WORKFLOW_GET_STREAM_ID = Symbol.for("WORKFLOW_GET_STREAM_ID");
var STABLE_ULID = Symbol.for("WORKFLOW_STABLE_ULID");
var STREAM_NAME_SYMBOL = Symbol.for("WORKFLOW_STREAM_NAME");
var STREAM_TYPE_SYMBOL = Symbol.for("WORKFLOW_STREAM_TYPE");
var BODY_INIT_SYMBOL = Symbol.for("BODY_INIT");
var WEBHOOK_RESPONSE_WRITABLE = Symbol.for("WEBHOOK_RESPONSE_WRITABLE");
var WORKFLOW_CLASS_REGISTRY = Symbol.for("workflow-class-registry");

// ../../../../packages/core/dist/class-serialization.js
function getRegistry(global = globalThis) {
  const g = global;
  let registry = g[WORKFLOW_CLASS_REGISTRY];
  if (!registry) {
    registry = /* @__PURE__ */ new Map();
    g[WORKFLOW_CLASS_REGISTRY] = registry;
  }
  return registry;
}
__name(getRegistry, "getRegistry");
function registerSerializationClass(classId, cls) {
  getRegistry().set(classId, cls);
  Object.defineProperty(cls, "classId", {
    value: classId,
    writable: false,
    enumerable: false,
    configurable: false
  });
}
__name(registerSerializationClass, "registerSerializationClass");

// ../../dist/bundle/workflow-entry.js
var __defProp2 = Object.defineProperty;
var __name2 = /* @__PURE__ */ __name((target, value) => __defProp2(target, "name", {
  value,
  configurable: true
}), "__name");
var WORKFLOW_SERIALIZE = /* @__PURE__ */ Symbol.for("workflow-serialize");
var WORKFLOW_DESERIALIZE = /* @__PURE__ */ Symbol.for("workflow-deserialize");
var textEncoder = new TextEncoder();
var textDecoder = new TextDecoder();
function toBuffer(content, encoding) {
  if (content instanceof Uint8Array) {
    return content;
  }
  if (encoding === "base64") {
    return Uint8Array.from(atob(content), (c) => c.charCodeAt(0));
  }
  if (encoding === "hex") {
    const bytes = new Uint8Array(content.length / 2);
    for (let i = 0; i < content.length; i += 2) {
      bytes[i / 2] = parseInt(content.slice(i, i + 2), 16);
    }
    return bytes;
  }
  if (encoding === "binary" || encoding === "latin1") {
    return Uint8Array.from(content, (c) => c.charCodeAt(0));
  }
  return textEncoder.encode(content);
}
__name(toBuffer, "toBuffer");
__name2(toBuffer, "toBuffer");
function fromBuffer(buffer, encoding) {
  if (encoding === "base64") {
    return btoa(String.fromCharCode(...buffer));
  }
  if (encoding === "hex") {
    return Array.from(buffer).map((b) => b.toString(16).padStart(2, "0")).join("");
  }
  if (encoding === "binary" || encoding === "latin1") {
    return String.fromCharCode(...buffer);
  }
  return textDecoder.decode(buffer);
}
__name(fromBuffer, "fromBuffer");
__name2(fromBuffer, "fromBuffer");
function getEncoding(options) {
  if (options === null || options === void 0) {
    return void 0;
  }
  if (typeof options === "string") {
    return options;
  }
  return options.encoding ?? void 0;
}
__name(getEncoding, "getEncoding");
__name2(getEncoding, "getEncoding");
var textEncoder2 = new TextEncoder();
function isFileInit(value) {
  return typeof value === "object" && value !== null && !(value instanceof Uint8Array) && "content" in value;
}
__name(isFileInit, "isFileInit");
__name2(isFileInit, "isFileInit");
var InMemoryFs = class _InMemoryFs {
  static {
    __name(this, "_InMemoryFs");
  }
  static {
    __name2(this, "InMemoryFs");
  }
  data = /* @__PURE__ */ new Map();
  constructor(initialFiles) {
    this.data.set("/", {
      type: "directory",
      mode: 493,
      mtime: /* @__PURE__ */ new Date()
    });
    if (initialFiles) {
      for (const [path, value] of Object.entries(initialFiles)) {
        if (isFileInit(value)) {
          this.writeFileSync(path, value.content, void 0, {
            mode: value.mode,
            mtime: value.mtime
          });
        } else {
          this.writeFileSync(path, value);
        }
      }
    }
  }
  normalizePath(path) {
    if (!path || path === "/") return "/";
    let normalized = path.endsWith("/") && path !== "/" ? path.slice(0, -1) : path;
    if (!normalized.startsWith("/")) {
      normalized = \`/\${normalized}\`;
    }
    const parts = normalized.split("/").filter((p) => p && p !== ".");
    const resolved = [];
    for (const part of parts) {
      if (part === "..") {
        resolved.pop();
      } else {
        resolved.push(part);
      }
    }
    return \`/\${resolved.join("/")}\` || "/";
  }
  dirname(path) {
    const normalized = this.normalizePath(path);
    if (normalized === "/") return "/";
    const lastSlash = normalized.lastIndexOf("/");
    return lastSlash === 0 ? "/" : normalized.slice(0, lastSlash);
  }
  ensureParentDirs(path) {
    const dir = this.dirname(path);
    if (dir === "/") return;
    if (!this.data.has(dir)) {
      this.ensureParentDirs(dir);
      this.data.set(dir, {
        type: "directory",
        mode: 493,
        mtime: /* @__PURE__ */ new Date()
      });
    }
  }
  // Sync method for writing files
  writeFileSync(path, content, options, metadata) {
    const normalized = this.normalizePath(path);
    this.ensureParentDirs(normalized);
    const encoding = getEncoding(options);
    const buffer = toBuffer(content, encoding);
    this.data.set(normalized, {
      type: "file",
      content: buffer,
      mode: metadata?.mode ?? 420,
      mtime: metadata?.mtime ?? /* @__PURE__ */ new Date()
    });
  }
  // Async public API
  async readFile(path, options) {
    const buffer = await this.readFileBuffer(path);
    const encoding = getEncoding(options);
    return fromBuffer(buffer, encoding);
  }
  async readFileBuffer(path) {
    const resolvedPath = this.resolvePathWithSymlinks(path);
    const entry = this.data.get(resolvedPath);
    if (!entry) {
      throw new Error(\`ENOENT: no such file or directory, open '\${path}'\`);
    }
    if (entry.type !== "file") {
      throw new Error(\`EISDIR: illegal operation on a directory, read '\${path}'\`);
    }
    if (entry.content instanceof Uint8Array) {
      return entry.content;
    }
    return textEncoder2.encode(entry.content);
  }
  async writeFile(path, content, options) {
    this.writeFileSync(path, content, options);
  }
  async appendFile(path, content, options) {
    const normalized = this.normalizePath(path);
    const existing = this.data.get(normalized);
    if (existing && existing.type === "directory") {
      throw new Error(\`EISDIR: illegal operation on a directory, write '\${path}'\`);
    }
    const encoding = getEncoding(options);
    const newBuffer = toBuffer(content, encoding);
    if (existing?.type === "file") {
      const existingBuffer = existing.content instanceof Uint8Array ? existing.content : textEncoder2.encode(existing.content);
      const combined = new Uint8Array(existingBuffer.length + newBuffer.length);
      combined.set(existingBuffer);
      combined.set(newBuffer, existingBuffer.length);
      this.data.set(normalized, {
        type: "file",
        content: combined,
        mode: existing.mode,
        mtime: /* @__PURE__ */ new Date()
      });
    } else {
      this.writeFileSync(path, content, options);
    }
  }
  async exists(path) {
    try {
      const resolvedPath = this.resolvePathWithSymlinks(path);
      return this.data.has(resolvedPath);
    } catch {
      return false;
    }
  }
  async stat(path) {
    const resolvedPath = this.resolvePathWithSymlinks(path);
    const entry = this.data.get(resolvedPath);
    if (!entry) {
      throw new Error(\`ENOENT: no such file or directory, stat '\${path}'\`);
    }
    let size = 0;
    if (entry.type === "file" && entry.content) {
      if (entry.content instanceof Uint8Array) {
        size = entry.content.length;
      } else {
        size = textEncoder2.encode(entry.content).length;
      }
    }
    return {
      isFile: entry.type === "file",
      isDirectory: entry.type === "directory",
      isSymbolicLink: false,
      // stat follows symlinks, so this is always false
      mode: entry.mode,
      size,
      mtime: entry.mtime || /* @__PURE__ */ new Date()
    };
  }
  async lstat(path) {
    const resolvedPath = this.resolveIntermediateSymlinks(path);
    const entry = this.data.get(resolvedPath);
    if (!entry) {
      throw new Error(\`ENOENT: no such file or directory, lstat '\${path}'\`);
    }
    if (entry.type === "symlink") {
      return {
        isFile: false,
        isDirectory: false,
        isSymbolicLink: true,
        mode: entry.mode,
        size: entry.target.length,
        mtime: entry.mtime || /* @__PURE__ */ new Date()
      };
    }
    let size = 0;
    if (entry.type === "file" && entry.content) {
      if (entry.content instanceof Uint8Array) {
        size = entry.content.length;
      } else {
        size = textEncoder2.encode(entry.content).length;
      }
    }
    return {
      isFile: entry.type === "file",
      isDirectory: entry.type === "directory",
      isSymbolicLink: false,
      mode: entry.mode,
      size,
      mtime: entry.mtime || /* @__PURE__ */ new Date()
    };
  }
  // Helper to resolve symlink target paths
  resolveSymlink(symlinkPath, target) {
    if (target.startsWith("/")) {
      return this.normalizePath(target);
    }
    const dir = this.dirname(symlinkPath);
    return this.normalizePath(dir === "/" ? \`/\${target}\` : \`\${dir}/\${target}\`);
  }
  /**
  * Resolve symlinks in intermediate path components only (not the final component).
  * Used by lstat which should not follow the final symlink.
  */
  resolveIntermediateSymlinks(path) {
    const normalized = this.normalizePath(path);
    if (normalized === "/") return "/";
    const parts = normalized.slice(1).split("/");
    if (parts.length <= 1) return normalized;
    let resolvedPath = "";
    const seen = /* @__PURE__ */ new Set();
    for (let i = 0; i < parts.length - 1; i++) {
      const part = parts[i];
      resolvedPath = \`\${resolvedPath}/\${part}\`;
      let entry = this.data.get(resolvedPath);
      let loopCount = 0;
      const maxLoops = 40;
      while (entry && entry.type === "symlink" && loopCount < maxLoops) {
        if (seen.has(resolvedPath)) {
          throw new Error(\`ELOOP: too many levels of symbolic links, lstat '\${path}'\`);
        }
        seen.add(resolvedPath);
        resolvedPath = this.resolveSymlink(resolvedPath, entry.target);
        entry = this.data.get(resolvedPath);
        loopCount++;
      }
      if (loopCount >= maxLoops) {
        throw new Error(\`ELOOP: too many levels of symbolic links, lstat '\${path}'\`);
      }
    }
    return \`\${resolvedPath}/\${parts[parts.length - 1]}\`;
  }
  /**
  * Resolve all symlinks in a path, including intermediate components.
  * For example: /home/user/linkdir/file.txt where linkdir is a symlink to "subdir"
  * would resolve to /home/user/subdir/file.txt
  */
  resolvePathWithSymlinks(path) {
    const normalized = this.normalizePath(path);
    if (normalized === "/") return "/";
    const parts = normalized.slice(1).split("/");
    let resolvedPath = "";
    const seen = /* @__PURE__ */ new Set();
    for (const part of parts) {
      resolvedPath = \`\${resolvedPath}/\${part}\`;
      let entry = this.data.get(resolvedPath);
      let loopCount = 0;
      const maxLoops = 40;
      while (entry && entry.type === "symlink" && loopCount < maxLoops) {
        if (seen.has(resolvedPath)) {
          throw new Error(\`ELOOP: too many levels of symbolic links, open '\${path}'\`);
        }
        seen.add(resolvedPath);
        resolvedPath = this.resolveSymlink(resolvedPath, entry.target);
        entry = this.data.get(resolvedPath);
        loopCount++;
      }
      if (loopCount >= maxLoops) {
        throw new Error(\`ELOOP: too many levels of symbolic links, open '\${path}'\`);
      }
    }
    return resolvedPath;
  }
  async mkdir(path, options) {
    this.mkdirSync(path, options);
  }
  /**
  * Synchronous version of mkdir
  */
  mkdirSync(path, options) {
    const normalized = this.normalizePath(path);
    if (this.data.has(normalized)) {
      const entry = this.data.get(normalized);
      if (entry?.type === "file") {
        throw new Error(\`EEXIST: file already exists, mkdir '\${path}'\`);
      }
      if (!options?.recursive) {
        throw new Error(\`EEXIST: directory already exists, mkdir '\${path}'\`);
      }
      return;
    }
    const parent = this.dirname(normalized);
    if (parent !== "/" && !this.data.has(parent)) {
      if (options?.recursive) {
        this.mkdirSync(parent, {
          recursive: true
        });
      } else {
        throw new Error(\`ENOENT: no such file or directory, mkdir '\${path}'\`);
      }
    }
    this.data.set(normalized, {
      type: "directory",
      mode: 493,
      mtime: /* @__PURE__ */ new Date()
    });
  }
  async readdir(path) {
    const entries = await this.readdirWithFileTypes(path);
    return entries.map((e) => e.name);
  }
  async readdirWithFileTypes(path) {
    let normalized = this.normalizePath(path);
    let entry = this.data.get(normalized);
    if (!entry) {
      throw new Error(\`ENOENT: no such file or directory, scandir '\${path}'\`);
    }
    const seen = /* @__PURE__ */ new Set();
    while (entry && entry.type === "symlink") {
      if (seen.has(normalized)) {
        throw new Error(\`ELOOP: too many levels of symbolic links, scandir '\${path}'\`);
      }
      seen.add(normalized);
      normalized = this.resolveSymlink(normalized, entry.target);
      entry = this.data.get(normalized);
    }
    if (!entry) {
      throw new Error(\`ENOENT: no such file or directory, scandir '\${path}'\`);
    }
    if (entry.type !== "directory") {
      throw new Error(\`ENOTDIR: not a directory, scandir '\${path}'\`);
    }
    const prefix = normalized === "/" ? "/" : \`\${normalized}/\`;
    const entriesMap = /* @__PURE__ */ new Map();
    for (const [p, fsEntry] of this.data.entries()) {
      if (p === normalized) continue;
      if (p.startsWith(prefix)) {
        const rest = p.slice(prefix.length);
        const name = rest.split("/")[0];
        if (name && !rest.includes("/", name.length) && !entriesMap.has(name)) {
          entriesMap.set(name, {
            name,
            isFile: fsEntry.type === "file",
            isDirectory: fsEntry.type === "directory",
            isSymbolicLink: fsEntry.type === "symlink"
          });
        }
      }
    }
    return Array.from(entriesMap.values()).sort((a, b) => a.name < b.name ? -1 : a.name > b.name ? 1 : 0);
  }
  async rm(path, options) {
    const normalized = this.normalizePath(path);
    const entry = this.data.get(normalized);
    if (!entry) {
      if (options?.force) return;
      throw new Error(\`ENOENT: no such file or directory, rm '\${path}'\`);
    }
    if (entry.type === "directory") {
      const children = await this.readdir(normalized);
      if (children.length > 0) {
        if (!options?.recursive) {
          throw new Error(\`ENOTEMPTY: directory not empty, rm '\${path}'\`);
        }
        for (const child of children) {
          const childPath = normalized === "/" ? \`/\${child}\` : \`\${normalized}/\${child}\`;
          await this.rm(childPath, options);
        }
      }
    }
    this.data.delete(normalized);
  }
  async cp(src, dest, options) {
    const srcNorm = this.normalizePath(src);
    const destNorm = this.normalizePath(dest);
    const srcEntry = this.data.get(srcNorm);
    if (!srcEntry) {
      throw new Error(\`ENOENT: no such file or directory, cp '\${src}'\`);
    }
    if (srcEntry.type === "file") {
      this.ensureParentDirs(destNorm);
      this.data.set(destNorm, {
        ...srcEntry
      });
    } else if (srcEntry.type === "directory") {
      if (!options?.recursive) {
        throw new Error(\`EISDIR: is a directory, cp '\${src}'\`);
      }
      await this.mkdir(destNorm, {
        recursive: true
      });
      const children = await this.readdir(srcNorm);
      for (const child of children) {
        const srcChild = srcNorm === "/" ? \`/\${child}\` : \`\${srcNorm}/\${child}\`;
        const destChild = destNorm === "/" ? \`/\${child}\` : \`\${destNorm}/\${child}\`;
        await this.cp(srcChild, destChild, options);
      }
    }
  }
  async mv(src, dest) {
    await this.cp(src, dest, {
      recursive: true
    });
    await this.rm(src, {
      recursive: true
    });
  }
  // Get all paths (useful for debugging/glob)
  getAllPaths() {
    return Array.from(this.data.keys());
  }
  // Resolve a path relative to a base
  resolvePath(base, path) {
    if (path.startsWith("/")) {
      return this.normalizePath(path);
    }
    const combined = base === "/" ? \`/\${path}\` : \`\${base}/\${path}\`;
    return this.normalizePath(combined);
  }
  // Change file/directory permissions
  async chmod(path, mode) {
    const normalized = this.normalizePath(path);
    const entry = this.data.get(normalized);
    if (!entry) {
      throw new Error(\`ENOENT: no such file or directory, chmod '\${path}'\`);
    }
    entry.mode = mode;
  }
  // Create a symbolic link
  async symlink(target, linkPath) {
    const normalized = this.normalizePath(linkPath);
    if (this.data.has(normalized)) {
      throw new Error(\`EEXIST: file already exists, symlink '\${linkPath}'\`);
    }
    this.ensureParentDirs(normalized);
    this.data.set(normalized, {
      type: "symlink",
      target,
      mode: 511,
      mtime: /* @__PURE__ */ new Date()
    });
  }
  // Create a hard link
  async link(existingPath, newPath) {
    const existingNorm = this.normalizePath(existingPath);
    const newNorm = this.normalizePath(newPath);
    const entry = this.data.get(existingNorm);
    if (!entry) {
      throw new Error(\`ENOENT: no such file or directory, link '\${existingPath}'\`);
    }
    if (entry.type !== "file") {
      throw new Error(\`EPERM: operation not permitted, link '\${existingPath}'\`);
    }
    if (this.data.has(newNorm)) {
      throw new Error(\`EEXIST: file already exists, link '\${newPath}'\`);
    }
    this.ensureParentDirs(newNorm);
    this.data.set(newNorm, {
      type: "file",
      content: entry.content,
      mode: entry.mode,
      mtime: entry.mtime
    });
  }
  // Read the target of a symbolic link
  async readlink(path) {
    const normalized = this.normalizePath(path);
    const entry = this.data.get(normalized);
    if (!entry) {
      throw new Error(\`ENOENT: no such file or directory, readlink '\${path}'\`);
    }
    if (entry.type !== "symlink") {
      throw new Error(\`EINVAL: invalid argument, readlink '\${path}'\`);
    }
    return entry.target;
  }
  /**
  * Resolve all symlinks in a path to get the canonical physical path.
  * This is equivalent to POSIX realpath().
  */
  async realpath(path) {
    const resolved = this.resolvePathWithSymlinks(path);
    if (!this.data.has(resolved)) {
      throw new Error(\`ENOENT: no such file or directory, realpath '\${path}'\`);
    }
    return resolved;
  }
  // ===========================================================================
  // Workflow Serde Support
  // ===========================================================================
  static [WORKFLOW_SERIALIZE](instance) {
    return {
      data: instance.data
    };
  }
  static [WORKFLOW_DESERIALIZE](serialized) {
    const fs = new _InMemoryFs();
    fs.data = serialized.data;
    return fs;
  }
  /**
  * Set access and modification times of a file
  * @param path - The file path
  * @param _atime - Access time (ignored, kept for API compatibility)
  * @param mtime - Modification time
  */
  async utimes(path, _atime, mtime) {
    const normalized = this.normalizePath(path);
    const resolved = this.resolvePathWithSymlinks(normalized);
    const entry = this.data.get(resolved);
    if (!entry) {
      throw new Error(\`ENOENT: no such file or directory, utimes '\${path}'\`);
    }
    entry.mtime = mtime;
  }
};
var DEFAULT_LIMITS = {
  maxCallDepth: 100,
  maxCommandCount: 1e4,
  maxLoopIterations: 1e4,
  maxAwkIterations: 1e4,
  maxSedIterations: 1e4,
  maxJqIterations: 1e4,
  maxSqliteTimeoutMs: 5e3,
  maxPythonTimeoutMs: 3e4
};
function resolveLimits(userLimits) {
  if (!userLimits) {
    return {
      ...DEFAULT_LIMITS
    };
  }
  return {
    maxCallDepth: userLimits.maxCallDepth ?? DEFAULT_LIMITS.maxCallDepth,
    maxCommandCount: userLimits.maxCommandCount ?? DEFAULT_LIMITS.maxCommandCount,
    maxLoopIterations: userLimits.maxLoopIterations ?? DEFAULT_LIMITS.maxLoopIterations,
    maxAwkIterations: userLimits.maxAwkIterations ?? DEFAULT_LIMITS.maxAwkIterations,
    maxSedIterations: userLimits.maxSedIterations ?? DEFAULT_LIMITS.maxSedIterations,
    maxJqIterations: userLimits.maxJqIterations ?? DEFAULT_LIMITS.maxJqIterations,
    maxSqliteTimeoutMs: userLimits.maxSqliteTimeoutMs ?? DEFAULT_LIMITS.maxSqliteTimeoutMs,
    maxPythonTimeoutMs: userLimits.maxPythonTimeoutMs ?? DEFAULT_LIMITS.maxPythonTimeoutMs
  };
}
__name(resolveLimits, "resolveLimits");
__name2(resolveLimits, "resolveLimits");
var Bash = class _Bash {
  static {
    __name(this, "_Bash");
  }
  static {
    __name2(this, "Bash");
  }
  fs;
  state;
  limits;
  constructor(options = {}) {
    this.fs = options.fs ?? new InMemoryFs();
    this.limits = resolveLimits(options.executionLimits);
    this.state = {
      env: {
        HOME: "/home/user",
        PATH: "/usr/bin:/bin",
        IFS: " 	\\n",
        PWD: "/home/user",
        OLDPWD: "/home/user",
        OPTIND: "1"
      },
      cwd: "/home/user",
      previousDir: "/home/user",
      lastExitCode: 0,
      lastArg: "",
      currentLine: 1,
      options: {
        errexit: false,
        pipefail: false,
        nounset: false,
        xtrace: false,
        verbose: false,
        posix: false,
        allexport: false,
        noclobber: false,
        noglob: false,
        noexec: false,
        vi: false,
        emacs: false
      },
      shoptOptions: {
        extglob: false,
        dotglob: false,
        nullglob: false,
        failglob: false,
        globstar: false,
        globskipdots: true,
        nocaseglob: false,
        nocasematch: false,
        expand_aliases: false,
        lastpipe: false,
        xpg_echo: false
      },
      functions: /* @__PURE__ */ new Map(),
      localScopes: [],
      callDepth: 0,
      sourceDepth: 0,
      commandCount: 0,
      startTime: Date.now(),
      lastBackgroundPid: 0,
      bashPid: typeof process !== "undefined" ? process.pid : 1,
      nextVirtualPid: (typeof process !== "undefined" ? process.pid : 1) + 1,
      exportedVars: /* @__PURE__ */ new Set([
        "HOME",
        "PATH",
        "PWD",
        "OLDPWD"
      ]),
      readonlyVars: /* @__PURE__ */ new Set([
        "SHELLOPTS",
        "BASHOPTS"
      ]),
      hashTable: /* @__PURE__ */ new Map(),
      inCondition: false,
      loopDepth: 0
    };
  }
  // ===========================================================================
  // Workflow Serde Support
  // ===========================================================================
  /**
  * Serialize Bash instance for Workflow DevKit.
  * Serializes filesystem and interpreter state.
  */
  static [WORKFLOW_SERIALIZE](instance) {
    return {
      fs: instance.fs,
      state: instance.state,
      limits: instance.limits
    };
  }
  /**
  * Deserialize Bash instance for Workflow DevKit.
  */
  static [WORKFLOW_DESERIALIZE](serialized) {
    const bash = new _Bash({
      fs: serialized.fs
    });
    bash.state = serialized.state;
    bash.limits = serialized.limits;
    return bash;
  }
  // ===========================================================================
  // Read-only accessors (for workflow context inspection)
  // ===========================================================================
  getCwd() {
    return this.state.cwd;
  }
  getEnv() {
    return {
      ...this.state.env
    };
  }
};
registerSerializationClass("class//just-bash//Bash", Bash);
registerSerializationClass("class//just-bash//InMemoryFs", InMemoryFs);
//# sourceMappingURL=data:application/json;base64,
`;
const POST = workflowEntrypoint(workflowCode);
const _psdlYg = async ({ req }) => {
  try {
    return await POST(req);
  } catch (error) {
    console.error("Handler error:", error);
    return new Response("Internal Server Error", { status: 500 });
  }
};
const assets = {};
function readAsset(id) {
  const serverDir = dirname(fileURLToPath(globalThis.__nitro_main__));
  return promises.readFile(resolve(serverDir, assets[id].path));
}
const publicAssetBases = {};
function isPublicAssetURL(id = "") {
  if (assets[id]) {
    return true;
  }
  for (const base in publicAssetBases) {
    if (id.startsWith(base)) {
      return true;
    }
  }
  return false;
}
function getAsset(id) {
  return assets[id];
}
const METHODS = /* @__PURE__ */ new Set(["HEAD", "GET"]);
const EncodingMap = {
  gzip: ".gz",
  br: ".br"
};
const _IEtHhy = defineHandler((event) => {
  if (event.req.method && !METHODS.has(event.req.method)) {
    return;
  }
  let id = decodePath(withLeadingSlash(withoutTrailingSlash(event.url.pathname)));
  let asset;
  const encodingHeader = event.req.headers.get("accept-encoding") || "";
  const encodings = [...encodingHeader.split(",").map((e) => EncodingMap[e.trim()]).filter(Boolean).sort(), ""];
  if (encodings.length > 1) {
    event.res.headers.append("Vary", "Accept-Encoding");
  }
  for (const encoding of encodings) {
    for (const _id of [id + encoding, joinURL(id, "index.html" + encoding)]) {
      const _asset = getAsset(_id);
      if (_asset) {
        asset = _asset;
        id = _id;
        break;
      }
    }
  }
  if (!asset) {
    if (isPublicAssetURL(id)) {
      event.res.headers.delete("Cache-Control");
      throw new HTTPError({ status: 404 });
    }
    return;
  }
  const ifNotMatch = event.req.headers.get("if-none-match") === asset.etag;
  if (ifNotMatch) {
    event.res.status = 304;
    event.res.statusText = "Not Modified";
    return "";
  }
  const ifModifiedSinceH = event.req.headers.get("if-modified-since");
  const mtimeDate = new Date(asset.mtime);
  if (ifModifiedSinceH && asset.mtime && new Date(ifModifiedSinceH) >= mtimeDate) {
    event.res.status = 304;
    event.res.statusText = "Not Modified";
    return "";
  }
  if (asset.type) {
    event.res.headers.set("Content-Type", asset.type);
  }
  if (asset.etag && !event.res.headers.has("ETag")) {
    event.res.headers.set("ETag", asset.etag);
  }
  if (asset.mtime && !event.res.headers.has("Last-Modified")) {
    event.res.headers.set("Last-Modified", mtimeDate.toUTCString());
  }
  if (asset.encoding && !event.res.headers.has("Content-Encoding")) {
    event.res.headers.set("Content-Encoding", asset.encoding);
  }
  if (asset.size > 0 && !event.res.headers.has("Content-Length")) {
    event.res.headers.set("Content-Length", asset.size.toString());
  }
  return readAsset(id);
});
const _lazy_CI8LL_ = defineLazyEventHandler(() => import("./_routes/api/bash.mjs"));
const findRoute = /* @__PURE__ */ (() => {
  const $0 = { route: "/.well-known/workflow/v1/step", handler: toEventHandler(_g3m8Xw) }, $1 = { route: "/.well-known/workflow/v1/flow", handler: toEventHandler(_psdlYg) }, $2 = { route: "/api/bash", method: "post", handler: _lazy_CI8LL_ }, $3 = { route: "/.well-known/workflow/v1/webhook/:token", handler: toEventHandler(_n9RDGO) };
  return (m, p) => {
    if (p.charCodeAt(p.length - 1) === 47) p = p.slice(0, -1) || "/";
    if (p === "/.well-known/workflow/v1/step") {
      return { data: $0 };
    }
    if (p === "/.well-known/workflow/v1/flow") {
      return { data: $1 };
    }
    if (p === "/api/bash") {
      if (m === "POST") return { data: $2 };
    }
    let s = p.split("/"), l = s.length - 1;
    if (s[1] === ".well-known") {
      if (s[2] === "workflow") {
        if (s[3] === "v1") {
          if (s[4] === "webhook") {
            if (l === 5 || l === 4) {
              if (l >= 5) return { data: $3, params: { "token": s[5] } };
            }
          }
        }
      }
    }
  };
})();
const globalMiddleware = [
  toEventHandler(_IEtHhy)
].filter(Boolean);
const APP_ID = "default";
function useNitroApp() {
  let instance = useNitroApp._instance;
  if (instance) {
    return instance;
  }
  instance = useNitroApp._instance = createNitroApp();
  globalThis.__nitro__ = globalThis.__nitro__ || {};
  globalThis.__nitro__[APP_ID] = instance;
  return instance;
}
function createNitroApp() {
  const hooks = void 0;
  const captureError = (error, errorCtx) => {
    if (errorCtx?.event) {
      const errors = errorCtx.event.req.context?.nitro?.errors;
      if (errors) {
        errors.push({
          error,
          context: errorCtx
        });
      }
    }
  };
  const h3App = createH3App({ onError(error, event) {
    return errorHandler(error, event);
  } });
  let appHandler = (req) => {
    req.context ||= {};
    req.context.nitro = req.context.nitro || { errors: [] };
    return h3App.fetch(req);
  };
  const app = {
    fetch: appHandler,
    h3: h3App,
    hooks,
    captureError
  };
  return app;
}
function createH3App(config2) {
  const h3App = new H3Core(config2);
  h3App["~findRoute"] = (event) => findRoute(event.req.method, event.url.pathname);
  h3App["~middleware"].push(...globalMiddleware);
  return h3App;
}
function _captureError(error, type) {
  console.error(`[${type}]`, error);
  useNitroApp().captureError?.(error, { tags: [type] });
}
function trapUnhandledErrors() {
  process.on("unhandledRejection", (error) => _captureError(error, "unhandledRejection"));
  process.on("uncaughtException", (error) => _captureError(error, "uncaughtException"));
}
const port = Number.parseInt(process.env.NITRO_PORT || process.env.PORT || "") || 3e3;
const host = process.env.NITRO_HOST || process.env.HOST;
const cert = process.env.NITRO_SSL_CERT;
const key = process.env.NITRO_SSL_KEY;
const nitroApp = useNitroApp();
serve({
  port,
  hostname: host,
  tls: cert && key ? {
    cert,
    key
  } : void 0,
  fetch: nitroApp.fetch
});
trapUnhandledErrors();
const nodeServer = {};
export {
  DEFAULT_BATCH_SIZE as D,
  ExecutionLimitError as E,
  Run as R,
  __name$1 as _,
  escapeRegexChars as a,
  expandRedirectTarget as b,
  expandWord as c,
  expandWordForPattern as d,
  nodeServer as default,
  escapeGlobChars as e,
  expandWordForRegex as f,
  expandWordWithGlob as g,
  getArrayElements as h,
  getVariable as i,
  hasQuotedMultiValueAt as j,
  isArray as k,
  isWordFullyQuoted as l,
  getErrorMessage as m,
  __toESM as n,
  __commonJS as o,
  __require as p,
  __export as q,
  __esm as r,
  start as s,
  __toCommonJS as t
};
