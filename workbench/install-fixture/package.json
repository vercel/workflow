{
  "name": "install-fixture",
  "version": "0.0.0",
  "private": true,
  "description": "E2E fixture: shadcn-installable Workflow patterns are installed into this app in CI, then typechecked, built, and smoke-run. app/workflows/ is populated by the install step.",
  "scripts": {
    "materialize": "bun ../../docs/scripts/materialize-patterns.ts",
    "typecheck": "tsc --noEmit",
    "build": "next build",
    "clean": "rm -rf .next .swc app/.well-known/workflow app/workflows && mkdir -p app/workflows"
  },
  "dependencies": {
    "@ai-sdk/react": "2.0.76",
    "@chat-adapter/slack": "4.30.0",
    "@chat-adapter/state-redis": "4.30.0",
    "@vercel/sandbox": "2.2.1",
    "@workflow/ai": "workspace:*",
    "ai": "catalog:",
    "chat": "4.30.0",
    "ms": "2.1.3",
    "next": "16.2.11",
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "resend": "6.12.4",
    "stripe": "22.2.0",
    "workflow": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/ms": "2.1.0",
    "@types/node": "catalog:",
    "@types/react": "^19",
    "typescript": "catalog:"
  }
}
