{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["WORKFLOW_E2E_BASE_PATH", "WORKFLOW_PUBLIC_MANIFEST"],
      "outputs": [".output/**", ".nitro/**"]
    }
  }
}
