# Imported trace data is never committed — it is regenerated by the importers and
# may contain customer/production data. Only the tooling + fixtures are tracked.
data/
raw/
*.tar.gz
*.jsonl
!fixtures/*.jsonl
