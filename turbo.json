{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build", ".svelte-kit", ".vercel/output"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build", ".svelte-kit", ".vercel/output"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build", ".svelte-kit", ".vercel/output"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build", ".svelte-kit", ".vercel/output"]
    },
    "clean": {
      "cache": false
    }
  }
}
