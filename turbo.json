{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build", ".svelte-kit"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build", ".svelte-kit"]
    },
    "typecheck": {
      "outputs": ["dist/**", "build", ".svelte-kit"]
    },
    "test": {
      "outputs": ["dist/**", "build", ".svelte-kit"]
    },
    "clean": {
      "cache": false
    }
  }
}
