\* ProbeCache firstMiss_never: 29197a10f, never-answering target (liveness is vacuous here, TLC rejects it as a tautology; only the state space and TypeOK are checked)
CONSTANTS
    Rule = "firstMiss"
    Lat = "never"
    Gaps = {1, 30, 61}
    MaxGlitches = 2
    K = 20
    EvictEnabled = FALSE
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
    NoLongFallbackRun
