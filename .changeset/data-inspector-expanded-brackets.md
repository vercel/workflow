---
'@workflow/web-shared': patch
---

Fix the data inspector showing an object/array's contents twice when expanded: expanded nodes now render bracket delimiters (`{ … }` / `[ … ]`) instead of repeating the inline preview.
