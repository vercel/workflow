---
'@workflow/world-vercel': minor
'@workflow/world-local': minor
'@workflow/world': minor
---

Add `WORKFLOW_NATIVE_FETCH` to run the Vercel and Local World HTTP layers on the runtime's own `fetch()` connection pool instead of the pools those Worlds build for themselves.
