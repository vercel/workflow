---
'@workflow/nitro': minor
---

Start the World at server boot via a Nitro server plugin so in-flight runs recover after a restart without requiring a workflow operation (covers Express/Hono/Fastify/Nuxt).
