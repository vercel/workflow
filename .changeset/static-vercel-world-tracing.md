---
"@workflow/next": patch
"@workflow/builders": patch
---

Ensure Next.js deployments externalize the built-in Vercel world runtime package and bundled Build Output API webhook functions can initialize workflow world storage.
