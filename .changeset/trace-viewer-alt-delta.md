---
'@workflow/web-shared': patch
---

Trace viewer: holding Alt now measures the delta between the selected span and the span under the cursor (Figma-style), instead of showing every consecutive-span gap at once.
