---
"@workflow/world-vercel": minor
---

Support `WORKFLOW_VERCEL_PROTECTION_BYPASS` env var to send a Vercel Deployment Protection bypass secret on outbound requests. Also support `VERCEL_WORKFLOW_SERVER_URL` env var to override the workflow-server URL (previously only configurable by editing the source).
