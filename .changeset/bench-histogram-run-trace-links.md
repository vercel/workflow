---
---

Log the run id and Datadog trace id of each sequential-steps benchmark iteration, so a suspicious STSO distribution can be opened in APM directly instead of hunted down by deployment id and time window.
