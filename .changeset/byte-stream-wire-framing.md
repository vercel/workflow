---
"@workflow/core": minor
"workflow": minor
---

Add wire-level framing for byte streams (`type: 'bytes'`) so consumers can identify chunk boundaries — a prerequisite for transparent auto-reconnect.
