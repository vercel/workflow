---
"@workflow/world-vercel": minor
---

Advertise queue-first experimental start-hook admission (30-day TTL cap, 255-byte token cap) and map workflow-server `hook_conflict` responses to `HookConflictError` in the shared response classifier.
