# AUTO-GENERATED FILE - DO NOT EDIT DIRECTLY
# This file is generated from community-worlds.json
# Run: node scripts/generate-community-worlds-workflow.mjs

name: Community Worlds Tests

on:
  push:
    branches:
      - main
    tags:
      - "!*"
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Packages
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 10.14.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm turbo run build --filter='!./workbench/*'

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            node_modules
            packages/*/dist
          retention-days: 1

  e2e-turso:
    name: E2E Turso World Tests
    runs-on: ubuntu-latest
    needs: build
    continue-on-error: true
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}
      WORKFLOW_TARGET_WORLD: "@workflow-worlds/turso"
      WORKFLOW_TURSO_DATABASE_URL: "file:workflow.db"
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10.14.0

      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Turso World
        run: pnpm --filter nextjs-turbopack add @workflow-worlds/turso

      - name: Resolve symlinks
        run: ./scripts/resolve-symlinks.sh workbench/nextjs-turbopack

      - name: Run E2E Tests
        run: cd workbench/nextjs-turbopack && pnpm dev & echo "starting tests in 15 seconds" && sleep 15 && pnpm vitest run packages/core/e2e/dev.test.ts && sleep 10 && pnpm run test:e2e
        env:
          APP_NAME: "nextjs-turbopack"
          DEPLOYMENT_URL: "http://localhost:3000"
          DEV_TEST_CONFIG: '{"name":"nextjs-turbopack","project":"workbench-nextjs-turbopack-workflow","generatedStepPath":"app/.well-known/workflow/v1/step/route.js","generatedWorkflowPath":"app/.well-known/workflow/v1/flow/route.js","apiFilePath":"app/api/chat/route.ts","apiFileImportPath":"../../.."}'

  e2e-mongodb:
    name: E2E MongoDB World Tests
    runs-on: ubuntu-latest
    needs: build
    continue-on-error: true
    services:
      mongodb:
        image: mongo:7
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.runCommand({ ping: 1 })'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}
      WORKFLOW_TARGET_WORLD: "@workflow-worlds/mongodb"
      WORKFLOW_MONGODB_URI: "mongodb://localhost:27017"
      WORKFLOW_MONGODB_DATABASE_NAME: workflow
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10.14.0

      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Install MongoDB World
        run: pnpm --filter nextjs-turbopack add @workflow-worlds/mongodb

      - name: Resolve symlinks
        run: ./scripts/resolve-symlinks.sh workbench/nextjs-turbopack

      - name: Run E2E Tests
        run: cd workbench/nextjs-turbopack && pnpm dev & echo "starting tests in 15 seconds" && sleep 15 && pnpm vitest run packages/core/e2e/dev.test.ts && sleep 10 && pnpm run test:e2e
        env:
          APP_NAME: "nextjs-turbopack"
          DEPLOYMENT_URL: "http://localhost:3000"
          DEV_TEST_CONFIG: '{"name":"nextjs-turbopack","project":"workbench-nextjs-turbopack-workflow","generatedStepPath":"app/.well-known/workflow/v1/step/route.js","generatedWorkflowPath":"app/.well-known/workflow/v1/flow/route.js","apiFilePath":"app/api/chat/route.ts","apiFileImportPath":"../../.."}'

  e2e-redis:
    name: E2E Redis World Tests
    runs-on: ubuntu-latest
    needs: build
    continue-on-error: true
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}
      WORKFLOW_TARGET_WORLD: "@workflow-worlds/redis"
      WORKFLOW_REDIS_URI: "redis://localhost:6379"
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10.14.0

      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Redis World
        run: pnpm --filter nextjs-turbopack add @workflow-worlds/redis

      - name: Resolve symlinks
        run: ./scripts/resolve-symlinks.sh workbench/nextjs-turbopack

      - name: Run E2E Tests
        run: cd workbench/nextjs-turbopack && pnpm dev & echo "starting tests in 15 seconds" && sleep 15 && pnpm vitest run packages/core/e2e/dev.test.ts && sleep 10 && pnpm run test:e2e
        env:
          APP_NAME: "nextjs-turbopack"
          DEPLOYMENT_URL: "http://localhost:3000"
          DEV_TEST_CONFIG: '{"name":"nextjs-turbopack","project":"workbench-nextjs-turbopack-workflow","generatedStepPath":"app/.well-known/workflow/v1/step/route.js","generatedWorkflowPath":"app/.well-known/workflow/v1/flow/route.js","apiFilePath":"app/api/chat/route.ts","apiFileImportPath":"../../.."}'

  benchmark-turso:
    name: Benchmark Turso World
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 30
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}
      WORKFLOW_TARGET_WORLD: "@workflow-worlds/turso"
      WORKFLOW_TURSO_DATABASE_URL: "file:workflow.db"
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10.14.0

      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Turso World
        run: pnpm --filter nextjs-turbopack add @workflow-worlds/turso

      - name: Resolve symlinks
        run: ./scripts/resolve-symlinks.sh workbench/nextjs-turbopack

      - name: Build workbench
        run: pnpm turbo run build --filter='./workbench/nextjs-turbopack'

      - name: Run benchmarks
        env:
          DEPLOYMENT_URL: "http://localhost:3000"
          APP_NAME: "nextjs-turbopack"
        run: |
          cd workbench/nextjs-turbopack
          pnpm start &
          echo "Waiting for server to start..."
          sleep 15
          cd ../..
          pnpm vitest bench packages/core/e2e/bench.bench.ts --run --outputJson=bench-results-nextjs-turbopack-turso.json

      - name: Render benchmark results
        uses: ./.github/actions/render-benchmarks
        with:
          benchmark-file: bench-results-nextjs-turbopack-turso.json
          app-name: nextjs-turbopack
          backend: turso

      - name: Upload benchmark results
        uses: actions/upload-artifact@v4
        with:
          name: bench-results-nextjs-turbopack-turso
          path: |
            bench-results-nextjs-turbopack-turso.json
            bench-timings-nextjs-turbopack-turso.json

  benchmark-mongodb:
    name: Benchmark MongoDB World
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 30
    services:
      mongodb:
        image: mongo:7
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.runCommand({ ping: 1 })'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}
      WORKFLOW_TARGET_WORLD: "@workflow-worlds/mongodb"
      WORKFLOW_MONGODB_URI: "mongodb://localhost:27017"
      WORKFLOW_MONGODB_DATABASE_NAME: workflow
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10.14.0

      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Install MongoDB World
        run: pnpm --filter nextjs-turbopack add @workflow-worlds/mongodb

      - name: Resolve symlinks
        run: ./scripts/resolve-symlinks.sh workbench/nextjs-turbopack

      - name: Build workbench
        run: pnpm turbo run build --filter='./workbench/nextjs-turbopack'

      - name: Run benchmarks
        env:
          DEPLOYMENT_URL: "http://localhost:3000"
          APP_NAME: "nextjs-turbopack"
        run: |
          cd workbench/nextjs-turbopack
          pnpm start &
          echo "Waiting for server to start..."
          sleep 15
          cd ../..
          pnpm vitest bench packages/core/e2e/bench.bench.ts --run --outputJson=bench-results-nextjs-turbopack-mongodb.json

      - name: Render benchmark results
        uses: ./.github/actions/render-benchmarks
        with:
          benchmark-file: bench-results-nextjs-turbopack-mongodb.json
          app-name: nextjs-turbopack
          backend: mongodb

      - name: Upload benchmark results
        uses: actions/upload-artifact@v4
        with:
          name: bench-results-nextjs-turbopack-mongodb
          path: |
            bench-results-nextjs-turbopack-mongodb.json
            bench-timings-nextjs-turbopack-mongodb.json

  benchmark-redis:
    name: Benchmark Redis World
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 30
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}
      WORKFLOW_TARGET_WORLD: "@workflow-worlds/redis"
      WORKFLOW_REDIS_URI: "redis://localhost:6379"
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10.14.0

      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: "pnpm"

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: .

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Redis World
        run: pnpm --filter nextjs-turbopack add @workflow-worlds/redis

      - name: Resolve symlinks
        run: ./scripts/resolve-symlinks.sh workbench/nextjs-turbopack

      - name: Build workbench
        run: pnpm turbo run build --filter='./workbench/nextjs-turbopack'

      - name: Run benchmarks
        env:
          DEPLOYMENT_URL: "http://localhost:3000"
          APP_NAME: "nextjs-turbopack"
        run: |
          cd workbench/nextjs-turbopack
          pnpm start &
          echo "Waiting for server to start..."
          sleep 15
          cd ../..
          pnpm vitest bench packages/core/e2e/bench.bench.ts --run --outputJson=bench-results-nextjs-turbopack-redis.json

      - name: Render benchmark results
        uses: ./.github/actions/render-benchmarks
        with:
          benchmark-file: bench-results-nextjs-turbopack-redis.json
          app-name: nextjs-turbopack
          backend: redis

      - name: Upload benchmark results
        uses: actions/upload-artifact@v4
        with:
          name: bench-results-nextjs-turbopack-redis
          path: |
            bench-results-nextjs-turbopack-redis.json
            bench-timings-nextjs-turbopack-redis.json

  summary:
    name: Test Results Summary
    runs-on: ubuntu-latest
    needs: [e2e-turso, e2e-mongodb, e2e-redis, benchmark-turso, benchmark-mongodb, benchmark-redis]
    if: always()
    steps:
      - name: Generate Summary
        uses: actions/github-script@v7
        with:
          script: |
            const worlds = [{"id":"turso","name":"Turso","package":"@workflow-worlds/turso"},{"id":"mongodb","name":"MongoDB","package":"@workflow-worlds/mongodb"},{"id":"redis","name":"Redis","package":"@workflow-worlds/redis"}];
            
            let summary = '## Community Worlds E2E Test Results\n\n';
            summary += '| World | Package | E2E Status | Benchmark Status |\n';
            summary += '|:------|:--------|:-----------|:-----------------|\n';
            
            const e2eResults = {
              'turso': '${{ needs.e2e-turso.result }}',
              'mongodb': '${{ needs.e2e-mongodb.result }}',
              'redis': '${{ needs.e2e-redis.result }}',
            };
            
            const benchmarkResults = {
              'turso': '${{ needs.benchmark-turso.result }}',
              'mongodb': '${{ needs.benchmark-mongodb.result }}',
              'redis': '${{ needs.benchmark-redis.result }}',
            };
            
            const getEmoji = (result) => result === 'success' ? '✅' : result === 'failure' ? '❌' : result === 'skipped' ? '⏭️' : '❓';
            
            for (const world of worlds) {
              const e2eResult = e2eResults[world.id];
              const benchResult = benchmarkResults[world.id];
              summary += `| ${world.name} | \`${world.package}\` | ${getEmoji(e2eResult)} ${e2eResult} | ${getEmoji(benchResult)} ${benchResult} |\n`;
            }
            
            summary += '\n> Note: These tests use `continue-on-error: true` so failures are informational only.\n';
            
            await core.summary.addRaw(summary).write();
