name: 'Setup Workflow Dev Environment'
description: 'Setup Node.js, pnpm, Rust, and install dependencies for Workflow development. Note: Checkout must be done before calling this action.'

inputs:
  node-version:
    description: 'Node.js version to use'
    required: false
    default: '22.x'
  pnpm-version:
    description: 'pnpm version to use'
    required: false
    default: '10.14.0'
  build-packages:
    description: 'Whether to build packages (excludes workbenches)'
    required: false
    default: 'true'

runs:
  using: 'composite'
  steps:
    - name: Setup Rust
      uses: actions-rust-lang/setup-rust-toolchain@v1
      with:
        toolchain: stable

    - name: Setup pnpm
      uses: pnpm/action-setup@v3
      with:
        version: ${{ inputs.pnpm-version }}

    - name: Setup Node.js ${{ inputs.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
        cache: 'pnpm'

    - name: Install Dependencies
      shell: bash
      run: pnpm install --frozen-lockfile

    - name: Build all packages
      if: ${{ inputs.build-packages == 'true' }}
      shell: bash
      run: pnpm turbo run build --filter='!./workbench/*'
