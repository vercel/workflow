\* The unread-buffered-payload interplay (direct step skip + wait gating +
\* idle retirement) isolated on its minimal schedule -- the shape most
\* sensitive to window length.
\* Expected result: all invariants hold.
SPECIFICATION Spec
CONSTANTS
  Schedule <- UnreadHookSchedule
  NInv = 2
  Barriers = TRUE
INVARIANTS
  TypeOK
  ArrivalsWellFormed
  PrefixConsistency
  Determinism
