\* Regression guard: the current discipline (#3406, direct-only skip)
\* checked against the exact minimal schedule that broke the pre-#3406
\* engine (see ReplayDeliveryPre3406.cfg).
\* Expected result: all invariants hold.
SPECIFICATION Spec
CONSTANTS
  Schedule <- SkipHazardSchedule
  NInv = 2
  Barriers = TRUE
  SkipMode = "direct"
INVARIANTS
  TypeOK
  ArrivalsWellFormed
  PrefixConsistency
  Determinism
