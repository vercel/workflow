{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src", "drizzle.config.ts"],
      "outputs": ["dist"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    }
  }
}
