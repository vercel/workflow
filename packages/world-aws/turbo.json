{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src"],
      "outputs": ["dist"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    }
  }
}
