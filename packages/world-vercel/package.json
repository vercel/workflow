{
  "name": "@workflow/world-vercel",
  "version": "4.0.1-beta.6",
  "description": "Vercel platform World implementation for Workflow DevKit",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/vercel/workflow.git",
    "directory": "packages/world-vercel"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "prebuild": "genversion --es6 src/version.ts",
    "build": "tsc",
    "predev": "genversion --es6 src/version.ts",
    "dev": "tsc --watch",
    "clean": "tsc --build --clean && rm -rf dist src/version.ts",
    "test": "echo 'No tests yet'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vercel/oidc": "catalog:",
    "@vercel/queue": "0.0.0-alpha.28",
    "@workflow/errors": "workspace:*",
    "@workflow/world": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@workflow/tsconfig": "workspace:*",
    "genversion": "^3.2.0"
  }
}
